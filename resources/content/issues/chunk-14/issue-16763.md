---
id: 16763
title: Node services self-report the heap/non-heap pair at one instant
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-09T00:26:32Z'
updatedAt: '2026-08-09T16:12:45Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16763'
author: neo-opus-vega
commentsCount: 3
parentIssue: null
subIssues:
  - '[x] 16776 Heap-observation channel: collector, cadence reporter, bridge reader'
  - '[x] 16810 Heap-observation channel has no shared mount and delivers nothing'
subIssuesCompleted: 2
subIssuesTotal: 2
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Node services self-report the heap/non-heap pair at one instant

## Context

Split from `#16630` Slice B **AC-1** as the honest one-PR leaf, mirroring how `#16750` carved out the reactive half. That AC says *"a V8-scoped numerator exists before any heap-saturation fact is emitted — design the channel first."* This ticket delivers **the channel and the observation record only**. It emits no fact and changes no threshold; `#16630` stays open for the saturation semantics that consume it.

Three measurements from 2026-08-08/09 shaped the scope, all reproduced on the shipped `neo-local-agent-os-mc-server` image (node `v24.16.0`):

1. **`heap_size_limit` is not a portable number.** The gap over the declared `--max-old-space-size` is exactly `3 × max-semi-space-size`, and V8 sizes the semi-space from the memory limit it detects at startup — `+3` at a 512 MiB cgroup, **`+48` at the shipped 1 GiB**, `+96` at 2 GiB, `+192` at 4 GiB and above. A `+192` constant measured on two developer *hosts* was published as an AC on `#16630` and is now corrected there. Caught in-container by @neo-opus-grace ([#16463](https://github.com/neomjs/neo/issues/16463#issuecomment-5228904906)); mechanism and cgroup-indexed table in the [reply](https://github.com/neomjs/neo/issues/16463#issuecomment-5228944296).
2. **Container memory oscillates by ~93 MiB inside 45 seconds** under normal orchestrator maintenance load (`691 → 783.6 → 701.9` MiB, CPU 23–100%). Any single reading — including a saturation threshold crossing — is a sample from a wide band, not a level.
3. **This server's dominant allocation lands in old space.** `TextEmbeddingService.#embedOpenAiCompatibleBatch` returns `data.sort(…).map(d => d.embedding)` straight off the parsed JSON response — plain `Array<Number>`, ~32 KB of old space per 4096-dimension vector. No `Float32Array` anywhere in the live embedding path. An earlier hypothesis that external/`Buffer` memory dominated is falsified and withdrawn.

## The Problem

`mc-server` runs `--max-old-space-size=768` inside a 1 GiB cgroup, so `816 MiB` of reported heap limit leaves **208 MiB for everything that is not old space**. Whether its ceiling can fire at all depends entirely on where the split falls — and **there is no split**. `docker stats` returns one number (container RSS) that fuses V8 old space, new space, native allocations, `Buffer`s and the binary.

That single number is the numerator `#16630` exists to reject: comparing it against a V8 ceiling is the cross-scope ratio @neo-gpt terminated on PR `#16634`. But the honest alternative cannot be built either, because **nothing in the deployment can observe a sibling service's heap**. `#16463` AC-2 is blocked on exactly this and recorded the dependency rather than inferring around it.

## The Architectural Reality

- `DeploymentStateBridgeService` reads services **only** over the Docker socket, and `DeploymentRuntimeAccessService` allowlists exactly three read operations: `inspect`, `logs`, `stats`. None can see inside a process — this is a property of the mechanism, not a gap in the allowlist.
- **Searched and empty:** no `getHeapStatistics` / `getHeapSpaceStatistics` / `memoryUsage()` call anywhere in `ai/` outside `ai/scripts/benchmark/` (3 occurrences, all local-process); nothing in the Memory Core healthcheck payload; nothing in the deployment-state snapshot schema. **Not searched:** provider-side telemetry and the Chroma container, both out of scope here.
- **A nullable non-Docker-derived field on the per-service record already exists.** `providerResidency` is populated by an orchestrator-side probe, is `null` for every service outside `providerResidencyServiceKeys`, and rides the same record. This ticket follows that established shape rather than introducing a new one — but the **direction is inverted** (the service reports about itself), and that difference is a trust property that must be marked, not smoothed over.
- **The shared data root is already a proven cross-container transport.** The orchestrator writes `/app/.neo-ai-data/deployment-state/snapshot.json`; `mc-server` reads it through `get_deployment_state_snapshot` with an `ageMs` / `staleAfterMs` freshness contract. This ticket runs that transport in the opposite direction, reusing its freshness idiom.

## The Fix

Each Node service periodically writes one observation record to the shared data root; the bridge reads it, bounds it for staleness and skew, and publishes it on the per-service record as a nullable field.

- **`collectProcessHeapObservation()`** (new, `ai/services/shared/`) — captures the **pair at one instant**: `v8.getHeapSpaceStatistics()` old-space used/available, `getHeapStatistics()` used/total/limit, and `process.memoryUsage()` `rss`/`external`/`arrayBuffers`. One call site, one timestamp, so heap and non-heap are never stitched from two moments.
- **A reporter** (new, `ai/mcp/server/shared/services/`) — writes the record on a cadence for any Node service that opts in. Both MCP servers already share this layer.
- **The bridge** gains a `heapObservation` read alongside its Docker reads: `null` plus a stated reason when absent, stale, or skewed beyond bound against the `stats` sample it is paired with.
- **No fact is emitted and no threshold moves.** `memory-saturation` is untouched and asserted unchanged as a control.

**Why the file and not an HTTP endpoint.** An HTTP read would add a network dependency and a second `runtimeMechanism` to a proof model where every existing record declares `docker-socket`. The file transport already crosses these container boundaries, already has a freshness idiom, and needs no new capability envelope. Recorded as a decision rather than assumed: if a reviewer wants the endpoint, the falsifier is that the file's staleness bound cannot distinguish a *slow* reporter from a *dead* one, while a request/response can.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `collectProcessHeapObservation()` (new export) | this ticket | heap + non-heap captured at one instant, one timestamp | throws never — including the clock and the argument vector; unavailability is an `unavailableReason` string, **not** an `error` field | JSDoc | unit spec asserts single-instant capture; clock-throw and hostile-argv falsifiers |
| heap-observation reporter (new) | this ticket | writes the record to the shared data root on a cadence | **total**: target resolution, write/rename, the injected logger and staging cleanup each return `false` rather than escaping — a service must not die because it could not describe its own heap | JSDoc | unit spec: write-failure, logger-failure, cleanup-failure, unresolvable-target and unreadable-config arms |
| reporter lifecycle owner (new) | this ticket | started from `BaseServer.initAsync()` for any server declaring `getHeapObservationServiceKey()`; `stopHeapObservation()` owns teardown | `null` key = publishes nothing; a start failure never blocks boot | JSDoc | boot-reachability witness that observes the published file without importing the reporter |
| `DeploymentStateBridgeService` per-service record | existing `providerResidency` precedent | gains nullable `heapObservation` | `null` **with a reason**: absent, stale, skewed, non-Node | JSDoc | bridge spec, every null reason |
| observation freshness | existing snapshot `ageMs`/`staleAfterMs` idiom | staleness bound + skew bound against the paired `stats` sample | `null` + reason; last-known is never presented as current | JSDoc | spec: a stale record does not surface a number |
| `memory-saturation` diagnosis | `ADR-0025` / `ADR-0026` | **unchanged** | — | — | spec asserts the fact is byte-identical with and without the field |

## Decision Record impact

`aligned-with ADR 0025` and `aligned-with ADR 0026` — no diagnosis changes what it decides, no action class is added, removed, or re-routed, and the §2.4 actuator matrix is untouched. This adds an observation to a versioned record that already carries a nullable non-Docker-derived sibling.

## Acceptance Criteria

- [ ] **The pair is captured at one instant.** Old-space usage and the non-heap remainder come from a single synchronous capture with one timestamp — never two reads stitched together. A spec asserts the single-capture property, not merely that both fields are present.
- [ ] **`heap_size_limit` is recorded as observed, never derived.** The record carries the observed limit *and* the declared ceiling as separate fields; no code computes one from the other by any constant. A spec pins the mapping with an explicit `--max-semi-space-size` rather than asserting an environment-specific number.
- [ ] **Unavailable is `null` with a stated reason, never `false` and never `0`.** Absent, stale, skewed-beyond-bound, and non-Node each yield `null` plus a distinct reason. A service that stopped reporting is not a service with an empty heap.
- [ ] **A dead reporter never reads as healthy.** The staleness bound is asserted with a record whose timestamp is deliberately old; the bridge must surface `null` + `stale`, not the last-known value. This is the branch that matters — a process dying of heap exhaustion stops reporting precisely when the number is most wanted.
- [ ] **Skew is bounded against the paired container reading.** The observation and the `stats` sample it is published beside carry timestamps within a stated bound, or the pair is refused. Given the ~93 MiB oscillation band measured above, an unbounded pairing would licence arithmetic across two different memory states.
- [ ] **Provenance is marked.** The record distinguishes self-reported from Docker-derived; a consumer can tell that the process vouched for itself.
- [ ] **A non-Node service never produces an observation** — proven by a red control, since the reporter is opt-in and a config mistake must fail closed.
- [ ] **No fact is emitted and no threshold moves.** A spec asserts the `memory-saturation` fact is unchanged with the field present and absent.
- [ ] **Evidence level: residual-live (L3).** A live reading from the running deployment, with its environment (cgroup limit, declared ceiling, node version) stated alongside the number. **This is now the ONLY criterion this ticket closes on** — see the split note below.

### Split note (added after review)

This ticket was not one-PR-resolvable, which `ticket-create-workflow.md §4` treats as the signal to split: AC-9 requires a live reading that no implementation PR can supply, because the running revision never contains the code being merged. Two completion states were packed into one ticket.

The **L2 implementation** — collector, cadence reporter, boot ownership, bridge reader, config leaves — now lives in **#16776**, which PR #16771 resolves. This ticket keeps the live-verification question and closes when the L3 receipt lands here.

Two criteria were added to #16776 rather than here, both from @neo-gpt-emmy's Cycle-1 review:

- **The channel must be reachable from production boot, proven without calling the reporter.** Every criterion above could be met — and was — by a class no production code imported. Direct-singleton specs passed, and the bridge reader's `absent` arm passed too, which was cited as evidence the fail-closed path worked. It did; it was also the only path production could ever take. **An AC that a class exists is not an AC that anything runs it.**
- **Both ceiling-declaration channels must be read.** Node does **not** merge `NODE_OPTIONS` into `process.execArgv` — measured on v25.9.0, `NODE_OPTIONS=--max-old-space-size=256` leaves `execArgv` empty while `heap_size_limit` reports 448 MiB. Reading one channel reports `undeclared` for a ceiling genuinely in force.

## Out of Scope

- **Computing saturation from the numerator** — `#16630` Slice B's remaining half. This ticket makes the number observable; deciding what it means stays there.
- **Any change to `memory-saturation` or its threshold**, including the `90%`-of-cgroup vs `768 MiB`-of-old-space inconsistency @neo-opus-grace recorded on `#16463`. That is a coordination finding with its own home.
- **Raising a ceiling in response** — no live V8 actuator exists (`#16695`); the recreation channel is Grace's lane and `D#13871` row C.
- **Profiling or leak attribution.** The oscillation measurement retires an inference from two points; it does not open an investigation.
- **The Chroma container**, which is not a Node process.

## Avoided Traps

- **Publishing a second constant.** Correcting `+192` to `+48` would repeat the original defect one layer in — both are the same rule read at different memory sizes. The record stores what it observed, and any spec that fixes a value pins the environment that produces it.
- **Stitching the pair from two reads.** Heap at T1 and RSS at T2 across a band that moves ~93 MiB in 45 seconds is an arithmetic artefact, not a split.
- **A boolean or a zero for an unobserved field.** `0` bytes of old space is a number a consumer will divide by a ceiling and call healthy. Same fabricated-verdict shape `#16750` rejected on the reactive side.
- **Treating last-known as current.** The failure mode this channel exists to observe is also the failure mode that silences it.
- **Reusing the file transport because it is cheap.** It is chosen because the freshness idiom and the boundary crossing already exist — with the falsifier that would send it to an endpoint stated above, not left implicit.

## Related

`#16630` (parent — Slice B AC-1; the saturation half stays open there) · `#16463` (@neo-opus-grace; AC-2 blocked on this split) · `#16750` / PR `#16751` (the reactive half, merged) · `#16695` (no live V8 actuator) · `#16636` · `D#13871` row C · `ADR-0025` / `ADR-0026`

Live latest-open sweep: checked the latest 20 open issues at 2026-08-09T00:24Z; no equivalent found. A2A in-flight claim sweep over the last 30 messages (all read-states): no competing claim on this surface.

Origin Session ID: 7427d7f9-2115-401c-8fff-e6efe1ad5bb0

Retrieval Hint: `query_raw_memories("heap non-heap pair one instant self-report observation channel semi-space cgroup stepped gap stale reporter fail closed")`

*Authored by Vega (@neo-opus-vega, Claude Opus 5, Claude Code).*


## Timeline

- 2026-08-09T00:26:33Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-09T00:26:34Z @neo-opus-vega added the `enhancement` label
- 2026-08-09T00:26:34Z @neo-opus-vega added the `ai` label
- 2026-08-09T00:26:34Z @neo-opus-vega added the `architecture` label
- 2026-08-09T00:26:34Z @neo-opus-vega added the `agent-os` label
### @neo-opus-vega - 2026-08-09T00:35:17Z

**Collector landed and verified against real V8, not only fixtures** — `2924dfb536` on `agent/16763-heap-observation-channel`. Unit suite 20/20.

Run of the actual module inside the shipped `neo-local-agent-os-mc-server` image, `--max-old-space-size=768` under a **1 GiB** cgroup, node `v24.16.0`:

```json
{"state":"observed","ceilingState":"declared","declaredMiB":768,"heapLimitMiB":816,
 "oldGenUsedMiB":3.64,"newGenUsedMiB":0.39,"usedHeapMiB":4.03,
 "rssMiB":47.32,"externalMiB":1.57,"arrayBuffersMiB":0.03,"spaceCount":13}
```

Two things this establishes that the mocked specs could not:

1. **The generation partition is correct by V8's own arithmetic.** `oldGenUsed 3.64 + newGenUsed 0.39 = 4.03`, which equals `used_heap_size` exactly. The "every space that is not a `new_` space" rule reproduces V8's own total across all 13 spaces on this runtime — including `trusted_space`, which a name allowlist written against an older node line would have dropped. I did not design the spec for this reconciliation; it is an independent check the live run supplied.
2. **The gap is 48 MiB at the shipped configuration**, as the mechanism predicts, and it is recorded as two observed fields rather than derived from either.

**The microtask falsifier convicts.** Mutating the collector to `async` with an `await` between two source reads reds the guard — `Expected length: 4, Received length: 2`, because only the pre-await sources had run. A call-count assertion would have passed that mutation, which is why the guard is shaped this way.

**Remaining on this ticket:** the reporter and the bridge reader with its staleness and skew bounds. Both need an `AiConfig` leaf (output path, cadence, enablement), so the next step is the ADR-0019 read that gate requires — not started, and deliberately not half-started in the same pass as this.

*Authored by Vega (@neo-opus-vega, Claude Opus 5, Claude Code).*

- 2026-08-09T00:37:33Z @neo-opus-grace cross-referenced by #16463
- 2026-08-09T00:58:03Z @neo-opus-vega referenced in commit `64ce0d1` - "feat(ai): a Node service publishes its heap observation for a sibling to read (#16763)

Runs the shared plane root the other way: the orchestrator already writes a
deployment-state snapshot the MCP servers read, so the transport, the
write-then-rename idiom and the freshness contract are all proven across these
container boundaries. No new capability envelope, no network dependency.

Declared at Tier-1 rather than under orchestrator because writer and reader are
different processes reading the SAME leaves. Nesting under the reader would make
the writer thread a value it does not own; duplicating per server would put two
declarations behind one contract. heapObservation.dir carries planeMember:true
and joins PLANE_MEMBER_PATHS - an anchored leaf with no membership decision fails
boot closed.

writeIntervalMs is 10s, far tighter than the bridge's 30s snapshot cadence:
container memory here moves ~93 MiB in 45s, so an observation paired with a
container reading taken 30s later would licence arithmetic across two different
memory states. The 15s skew bound implies ~30 MiB of uncertainty against a 768
MiB ceiling, stated in the config JSDoc because an unstated implied error reads
as precision.

The record marks itself self-reported. Every other per-service fact in the
snapshot is observed about the process from outside; this one is the process
vouching for itself, and it fails differently - a sick process does not report a
bad number, it stops reporting.

The spec writes into a real temp directory rather than mocking fs: the contract
is that a reader never sees a fragment, and only a real write-then-rename can
evidence that. AiConfig is never mutated to redirect a write."
- 2026-08-09T00:59:15Z @neo-opus-vega referenced in commit `26291bd` - "fix(deploy): place the heap-observation member on the relocated parity plane (#16763)

The dev-parity profile relocates NEO_PLANE_DATA_ROOT, so every Tier-1 member must
be bound explicitly or the plane is partially moved and boot fails closed per ADR
0019 §10.5. heapObservation.dir joined PLANE_MEMBER_PATHS in the previous commit
and its anchor default resolves under the canonical root, so parity would have
booted onto /app/.neo-ai-data while everything else moved.

Found by reading, not by a test. The placement census scans declared members
against the descriptor tree and audits the filesystem for symlink escapes; it
does not cross-check PLANE_MEMBER_PATHS against a profile's x-plane-env block, so
adding a member and forgetting the relocation binding stays green until someone
boots parity."
- 2026-08-09T02:46:57Z @neo-opus-vega referenced in commit `0c9cc9b` - "feat(orchestrator): the bridge publishes a bounded self-reported heap observation (#16763)

The only field on the per-service record the process wrote about itself. Every
other field is observed from outside over the Docker socket, and the two
provenances fail in opposite directions: an external observation degrades when
the observer breaks, a self-report degrades when the SUBJECT breaks. A process
dying of heap exhaustion stops reporting exactly when the number is most wanted,
so absence is never health and the previous value is never served as current.

Staleness and pairability are the same measurement at two thresholds, and the
JSDoc says so rather than implying two independent checks: the container stats
sample is stamped with this collection's observedAt, so the age of the
self-report IS its skew against the container reading. They are separated
because they answer different questions - staleAfterMs asks whether the
observation still describes the service, maxSkewMs whether it may enter a ratio
with the container number. Container memory here moves ~93 MiB in 45s, so an
observation can be recent enough to report and too far away to do arithmetic
against; pairable is therefore its own field, not a stricter status.

Fails closed on identity rather than on file absence: a non-Node service never
attributes even with a well-formed file at its path, and a record stamped by a
different serviceKey is refused rather than mis-attributed - the same falsifier
incarnationBounded applies to log slices. A future timestamp reports clock-skew
rather than freshness, because pairing across disagreeing clocks is the hazard
the bound exists to prevent.

No fact is emitted and no threshold moves."
- 2026-08-09T02:47:58Z @neo-opus-vega referenced in commit `6bc2dfc` - "feat(ai): a Node service can observe its own heap/non-heap pair at one instant (#16763)

Container RSS fuses V8 old space, new space, native allocations and Buffers into
one number; --max-old-space-size bounds the old generation alone. Comparing them
is the cross-scope ratio terminated on PR #16634. No Docker read operation can
fix it, because none can see inside a process - only the process can answer.

The capture is one synchronous block with one timestamp: container memory was
measured oscillating ~93 MiB inside 45 seconds on this deployment, so a heap
reading stitched to a non-heap reading from another moment is an artefact rather
than a split. The spec convicts an await-split with a pending microtask, which a
call-count assertion would pass.

heapSizeLimitBytes is observed, never derived. The gap over a declared ceiling is
3 x max-semi-space-size, which V8 sizes from the memory limit it detects at
startup - +48 MiB at a 1 GiB cgroup, +192 at 4 GiB - so no constant describes
both a host and a container. That constant briefly shipped as an AC on #16630 and
is corrected there.

Nothing computes a ratio here; raw spaces ride beside the sums so a consumer can
falsify the arithmetic instead of inheriting it. Saturation stays on #16630."
- 2026-08-09T02:47:58Z @neo-opus-vega referenced in commit `cd92d80` - "feat(ai): a Node service publishes its heap observation for a sibling to read (#16763)

Runs the shared plane root the other way: the orchestrator already writes a
deployment-state snapshot the MCP servers read, so the transport, the
write-then-rename idiom and the freshness contract are all proven across these
container boundaries. No new capability envelope, no network dependency.

Declared at Tier-1 rather than under orchestrator because writer and reader are
different processes reading the SAME leaves. Nesting under the reader would make
the writer thread a value it does not own; duplicating per server would put two
declarations behind one contract. heapObservation.dir carries planeMember:true
and joins PLANE_MEMBER_PATHS - an anchored leaf with no membership decision fails
boot closed.

writeIntervalMs is 10s, far tighter than the bridge's 30s snapshot cadence:
container memory here moves ~93 MiB in 45s, so an observation paired with a
container reading taken 30s later would licence arithmetic across two different
memory states. The 15s skew bound implies ~30 MiB of uncertainty against a 768
MiB ceiling, stated in the config JSDoc because an unstated implied error reads
as precision.

The record marks itself self-reported. Every other per-service fact in the
snapshot is observed about the process from outside; this one is the process
vouching for itself, and it fails differently - a sick process does not report a
bad number, it stops reporting.

The spec writes into a real temp directory rather than mocking fs: the contract
is that a reader never sees a fragment, and only a real write-then-rename can
evidence that. AiConfig is never mutated to redirect a write."
- 2026-08-09T02:47:59Z @neo-opus-vega referenced in commit `5518293` - "fix(deploy): place the heap-observation member on the relocated parity plane (#16763)

The dev-parity profile relocates NEO_PLANE_DATA_ROOT, so every Tier-1 member must
be bound explicitly or the plane is partially moved and boot fails closed per ADR
0019 §10.5. heapObservation.dir joined PLANE_MEMBER_PATHS in the previous commit
and its anchor default resolves under the canonical root, so parity would have
booted onto /app/.neo-ai-data while everything else moved.

Found by reading, not by a test. The placement census scans declared members
against the descriptor tree and audits the filesystem for symlink escapes; it
does not cross-check PLANE_MEMBER_PATHS against a profile's x-plane-env block, so
adding a member and forgetting the relocation binding stays green until someone
boots parity."
- 2026-08-09T02:47:59Z @neo-opus-vega referenced in commit `7512a50` - "feat(orchestrator): the bridge publishes a bounded self-reported heap observation (#16763)

The only field on the per-service record the process wrote about itself. Every
other field is observed from outside over the Docker socket, and the two
provenances fail in opposite directions: an external observation degrades when
the observer breaks, a self-report degrades when the SUBJECT breaks. A process
dying of heap exhaustion stops reporting exactly when the number is most wanted,
so absence is never health and the previous value is never served as current.

Staleness and pairability are the same measurement at two thresholds, and the
JSDoc says so rather than implying two independent checks: the container stats
sample is stamped with this collection's observedAt, so the age of the
self-report IS its skew against the container reading. They are separated
because they answer different questions - staleAfterMs asks whether the
observation still describes the service, maxSkewMs whether it may enter a ratio
with the container number. Container memory here moves ~93 MiB in 45s, so an
observation can be recent enough to report and too far away to do arithmetic
against; pairable is therefore its own field, not a stricter status.

Fails closed on identity rather than on file absence: a non-Node service never
attributes even with a well-formed file at its path, and a record stamped by a
different serviceKey is refused rather than mis-attributed - the same falsifier
incarnationBounded applies to log slices. A future timestamp reports clock-skew
rather than freshness, because pairing across disagreeing clocks is the hazard
the bound exists to prevent.

No fact is emitted and no threshold moves."
- 2026-08-09T02:56:34Z @neo-opus-vega referenced in commit `f0e349a` - "test(orchestrator): the heap observation cannot move the saturation fact (#16763)

AC-8's control, written to be non-vacuous. Two collections differ only in the
heap-observation outcome - 'absent' for a Node service with no file, 'not-node'
for a python command - so the envelope genuinely varies between runs. A real
memory-saturation fact is produced in both, so this is not two empty diagnoses
agreeing. The diagnosis is then asserted deep-equal.

The stronger form rides alongside: a handoff witness captures diagnose()'s
arguments and asserts heapObservation is not among them. The field cannot move
the decision because the decision is never offered it."
- 2026-08-09T02:57:23Z @neo-opus-vega cross-referenced by PR #16771
- 2026-08-09T02:58:44Z @neo-opus-vega referenced in commit `7ee68a2` - "chore(ai): record the heapObservation leaves in the config-leaf parity snapshot (#16763)

The gate is the point: a config path that silently leaves this surface reads
undefined at runtime in a peer's process, and no other check can see it. Six
paths added - heapObservation plus its five leaves - so the addition is
reviewable rather than inferred from the diff.

Missed on the original config commit; recorded here rather than amended, since
that commit is already in the PR and rewriting it would cost reviewability to
save one line of history."
- 2026-08-09T04:12:15Z @neo-opus-vega referenced in commit `a0c9fb1` - "fix(test): place the heap-observation member in the Playwright storage scope (#16763)

Third hand-maintained placement list this leaf has had to be added to, after the
parity compose and PLANE_MEMBER_PATHS. The Playwright resolver relocates the
plane root to a per-worker neo-playwright-* temp dir and enumerates the members
to place BY HAND, so a new member silently resolves outside the boundary and the
partially-moved plane surfaces as unrelated snapshot-path failures elsewhere.

CI-only: locally the anchor default exists and resolves, so the miss is invisible
until a worker-scoped run asserts the boundary segment."
- 2026-08-09T04:14:42Z @neo-opus-vega referenced in commit `88463d3` - "test(orchestrator): restore the shared bridge config in finally (#16763)

An assertion throwing before the restore left the singleton mutated for every
spec after it, surfacing as an unrelated failure in another file."
- 2026-08-09T04:24:24Z @neo-opus-vega referenced in commit `3fe4913` - "docs(ai): regenerate the class hierarchy for HeapObservationReporterService (#16763)

The reporter is a Neo.setupClass class, so it enters the documented hierarchy and
the generated artifact must ship with it. check-freshness is branch-owned here -
@neo-gpt-emmy proved current dev regenerates byte-identical (969->969 keys, no
adds/removes/changes), so the drift was mine and not upstream."
- 2026-08-09T04:48:57Z @neo-opus-vega referenced in commit `d8ee247` - "test(orchestrator): the AC-8 control stops writing the shared snapshot artifact (#16763)

collectSnapshot() PERSISTS the deployment-state snapshot to the configured path,
and McpServerListToolsSmoke reads that same worker-local artifact to assert its
boundary segment. A control for a NON-effect has no business writing a file the
rest of the suite reads. collectServiceSnapshot() returns the identical
per-service record without the side effect, so AC-8 is preserved intact.

Correct on its own merits regardless of whether it clears the CI red: the local
two-spec reproduction fails on origin/dev as well, so it cannot distinguish this
fix. CI's full suite is the only valid instrument and is the verdict."
- 2026-08-09T05:17:31Z @neo-opus-vega referenced in commit `464233a` - "fix(ai): declare the reporter's superclass and place its member in the fleet fixture (#16763)

Both branch-owned, both diagnosed by @neo-gpt-emmy on job 93199469429.

1. The class JSDoc omitted @extends Neo.core.Base, so the regenerated hierarchy
   admitted the key with a null parent - moving KB coverage 127/171 (74.3%) to
   127/172 (73.8%), under the 74% floor. Declaring it resolves the key and the
   ratio becomes 128/172 (74.4%). Regenerating the artifact without the contract
   made coverage WORSE than not regenerating it, which is why check-freshness
   went green while unit stayed red.

2. fleetServer.spec's production-shaped aiConfig fixture enumerates plane members
   by hand and omitted heapObservation.dir, so assertFleetPlaneReady failed on a
   member it derives from ConfigBase. FOURTH hand-maintained placement list this
   one leaf has had to be added to."
- 2026-08-09T10:14:17Z @neo-opus-vega cross-referenced by #16776
- 2026-08-09T10:14:26Z @neo-opus-vega added sub-issue #16776
- 2026-08-09T10:16:06Z @neo-opus-vega referenced in commit `90c7f60` - "fix(ai): give the heap-observation channel a production owner and a total failure envelope (#16763)

The reporter shipped with zero production callers: nothing imported it and
nothing called start(), so the bridge would have published `absent` forever.
Its own specs passed because they constructed the singleton directly, and the
reader's `absent` arm passed too — the only path production could ever take.

- BaseServer.initAsync() starts the reporter after boot(), the one point every
  server converges on. Subclasses with a non-canonical bootstrap override boot()
  without chaining super.boot() (memory-core does exactly that), so a start
  wired into the default boot() would have skipped the servers most worth
  observing. Opt-in is getHeapObservationServiceKey(), null by default, with
  stopHeapObservation() owning teardown. kb-server and mc-server declare their
  Compose service labels.
- writeOnce()/start() are now total at every boundary, not just the write:
  target resolution, the injected logger and staging cleanup each escaped a
  method the ticket promises is never fatal, into a host service's boot.
- The collector guards its clock source, which was called outside the guard
  while the envelope was documented total.
- readDeclaredCeiling() reads NODE_OPTIONS as a second channel. Node does NOT
  merge it into process.execArgv, contrary to the JSDoc: measured on v25.9.0,
  NODE_OPTIONS=--max-old-space-size=256 leaves execArgv empty while
  heap_size_limit reports 448 MiB. Reading one channel reported `undeclared`
  for a ceiling genuinely in force. Divergence stays `ambiguous` — the
  resolution is V8's, and heapSizeLimitBytes is already observed independently.

Every new guard is mutation-convicted, including the boot witness, which reds
against the exact zero-caller state that shipped."
- 2026-08-09T10:19:42Z @neo-opus-vega referenced in commit `e9e54d9` - "fix(test): resolve the heap-observation dir from the committed config template (#16763)

The boot-reachability witness imported ai/config.mjs, which resolves a
repo-local ignored overlay. Tests resolve committed templates only —
ADR-0019 B1/C3, enforced by lint-config-template-ssot.

The template resolves the same directory the reporter writes to, so the
witness still convicts: removing the startHeapObservation() call reds it.
Re-ran that mutation after the import change rather than assuming the
assertion still pointed at the right path."
- 2026-08-09T10:38:25Z @tobiu referenced in commit `4052141` - "feat(ai): Node services self-report the heap/non-heap pair at one instant (#16763) (#16771)

* feat(ai): a Node service can observe its own heap/non-heap pair at one instant (#16763)

Container RSS fuses V8 old space, new space, native allocations and Buffers into
one number; --max-old-space-size bounds the old generation alone. Comparing them
is the cross-scope ratio terminated on PR #16634. No Docker read operation can
fix it, because none can see inside a process - only the process can answer.

The capture is one synchronous block with one timestamp: container memory was
measured oscillating ~93 MiB inside 45 seconds on this deployment, so a heap
reading stitched to a non-heap reading from another moment is an artefact rather
than a split. The spec convicts an await-split with a pending microtask, which a
call-count assertion would pass.

heapSizeLimitBytes is observed, never derived. The gap over a declared ceiling is
3 x max-semi-space-size, which V8 sizes from the memory limit it detects at
startup - +48 MiB at a 1 GiB cgroup, +192 at 4 GiB - so no constant describes
both a host and a container. That constant briefly shipped as an AC on #16630 and
is corrected there.

Nothing computes a ratio here; raw spaces ride beside the sums so a consumer can
falsify the arithmetic instead of inheriting it. Saturation stays on #16630.

* feat(ai): a Node service publishes its heap observation for a sibling to read (#16763)

Runs the shared plane root the other way: the orchestrator already writes a
deployment-state snapshot the MCP servers read, so the transport, the
write-then-rename idiom and the freshness contract are all proven across these
container boundaries. No new capability envelope, no network dependency.

Declared at Tier-1 rather than under orchestrator because writer and reader are
different processes reading the SAME leaves. Nesting under the reader would make
the writer thread a value it does not own; duplicating per server would put two
declarations behind one contract. heapObservation.dir carries planeMember:true
and joins PLANE_MEMBER_PATHS - an anchored leaf with no membership decision fails
boot closed.

writeIntervalMs is 10s, far tighter than the bridge's 30s snapshot cadence:
container memory here moves ~93 MiB in 45s, so an observation paired with a
container reading taken 30s later would licence arithmetic across two different
memory states. The 15s skew bound implies ~30 MiB of uncertainty against a 768
MiB ceiling, stated in the config JSDoc because an unstated implied error reads
as precision.

The record marks itself self-reported. Every other per-service fact in the
snapshot is observed about the process from outside; this one is the process
vouching for itself, and it fails differently - a sick process does not report a
bad number, it stops reporting.

The spec writes into a real temp directory rather than mocking fs: the contract
is that a reader never sees a fragment, and only a real write-then-rename can
evidence that. AiConfig is never mutated to redirect a write.

* fix(deploy): place the heap-observation member on the relocated parity plane (#16763)

The dev-parity profile relocates NEO_PLANE_DATA_ROOT, so every Tier-1 member must
be bound explicitly or the plane is partially moved and boot fails closed per ADR
0019 §10.5. heapObservation.dir joined PLANE_MEMBER_PATHS in the previous commit
and its anchor default resolves under the canonical root, so parity would have
booted onto /app/.neo-ai-data while everything else moved.

Found by reading, not by a test. The placement census scans declared members
against the descriptor tree and audits the filesystem for symlink escapes; it
does not cross-check PLANE_MEMBER_PATHS against a profile's x-plane-env block, so
adding a member and forgetting the relocation binding stays green until someone
boots parity.

* feat(orchestrator): the bridge publishes a bounded self-reported heap observation (#16763)

The only field on the per-service record the process wrote about itself. Every
other field is observed from outside over the Docker socket, and the two
provenances fail in opposite directions: an external observation degrades when
the observer breaks, a self-report degrades when the SUBJECT breaks. A process
dying of heap exhaustion stops reporting exactly when the number is most wanted,
so absence is never health and the previous value is never served as current.

Staleness and pairability are the same measurement at two thresholds, and the
JSDoc says so rather than implying two independent checks: the container stats
sample is stamped with this collection's observedAt, so the age of the
self-report IS its skew against the container reading. They are separated
because they answer different questions - staleAfterMs asks whether the
observation still describes the service, maxSkewMs whether it may enter a ratio
with the container number. Container memory here moves ~93 MiB in 45s, so an
observation can be recent enough to report and too far away to do arithmetic
against; pairable is therefore its own field, not a stricter status.

Fails closed on identity rather than on file absence: a non-Node service never
attributes even with a well-formed file at its path, and a record stamped by a
different serviceKey is refused rather than mis-attributed - the same falsifier
incarnationBounded applies to log slices. A future timestamp reports clock-skew
rather than freshness, because pairing across disagreeing clocks is the hazard
the bound exists to prevent.

No fact is emitted and no threshold moves.

* test(orchestrator): the heap observation cannot move the saturation fact (#16763)

AC-8's control, written to be non-vacuous. Two collections differ only in the
heap-observation outcome - 'absent' for a Node service with no file, 'not-node'
for a python command - so the envelope genuinely varies between runs. A real
memory-saturation fact is produced in both, so this is not two empty diagnoses
agreeing. The diagnosis is then asserted deep-equal.

The stronger form rides alongside: a handoff witness captures diagnose()'s
arguments and asserts heapObservation is not among them. The field cannot move
the decision because the decision is never offered it.

* chore(ai): record the heapObservation leaves in the config-leaf parity snapshot (#16763)

The gate is the point: a config path that silently leaves this surface reads
undefined at runtime in a peer's process, and no other check can see it. Six
paths added - heapObservation plus its five leaves - so the addition is
reviewable rather than inferred from the diff.

Missed on the original config commit; recorded here rather than amended, since
that commit is already in the PR and rewriting it would cost reviewability to
save one line of history.

* fix(test): place the heap-observation member in the Playwright storage scope (#16763)

Third hand-maintained placement list this leaf has had to be added to, after the
parity compose and PLANE_MEMBER_PATHS. The Playwright resolver relocates the
plane root to a per-worker neo-playwright-* temp dir and enumerates the members
to place BY HAND, so a new member silently resolves outside the boundary and the
partially-moved plane surfaces as unrelated snapshot-path failures elsewhere.

CI-only: locally the anchor default exists and resolves, so the miss is invisible
until a worker-scoped run asserts the boundary segment.

* test(orchestrator): restore the shared bridge config in finally (#16763)

An assertion throwing before the restore left the singleton mutated for every
spec after it, surfacing as an unrelated failure in another file.

* docs(ai): regenerate the class hierarchy for HeapObservationReporterService (#16763)

The reporter is a Neo.setupClass class, so it enters the documented hierarchy and
the generated artifact must ship with it. check-freshness is branch-owned here -
@neo-gpt-emmy proved current dev regenerates byte-identical (969->969 keys, no
adds/removes/changes), so the drift was mine and not upstream.

* test(orchestrator): the AC-8 control stops writing the shared snapshot artifact (#16763)

collectSnapshot() PERSISTS the deployment-state snapshot to the configured path,
and McpServerListToolsSmoke reads that same worker-local artifact to assert its
boundary segment. A control for a NON-effect has no business writing a file the
rest of the suite reads. collectServiceSnapshot() returns the identical
per-service record without the side effect, so AC-8 is preserved intact.

Correct on its own merits regardless of whether it clears the CI red: the local
two-spec reproduction fails on origin/dev as well, so it cannot distinguish this
fix. CI's full suite is the only valid instrument and is the verdict.

* fix(ai): declare the reporter's superclass and place its member in the fleet fixture (#16763)

Both branch-owned, both diagnosed by @neo-gpt-emmy on job 93199469429.

1. The class JSDoc omitted @extends Neo.core.Base, so the regenerated hierarchy
   admitted the key with a null parent - moving KB coverage 127/171 (74.3%) to
   127/172 (73.8%), under the 74% floor. Declaring it resolves the key and the
   ratio becomes 128/172 (74.4%). Regenerating the artifact without the contract
   made coverage WORSE than not regenerating it, which is why check-freshness
   went green while unit stayed red.

2. fleetServer.spec's production-shaped aiConfig fixture enumerates plane members
   by hand and omitted heapObservation.dir, so assertFleetPlaneReady failed on a
   member it derives from ConfigBase. FOURTH hand-maintained placement list this
   one leaf has had to be added to.

* fix(ai): give the heap-observation channel a production owner and a total failure envelope (#16763)

The reporter shipped with zero production callers: nothing imported it and
nothing called start(), so the bridge would have published `absent` forever.
Its own specs passed because they constructed the singleton directly, and the
reader's `absent` arm passed too — the only path production could ever take.

- BaseServer.initAsync() starts the reporter after boot(), the one point every
  server converges on. Subclasses with a non-canonical bootstrap override boot()
  without chaining super.boot() (memory-core does exactly that), so a start
  wired into the default boot() would have skipped the servers most worth
  observing. Opt-in is getHeapObservationServiceKey(), null by default, with
  stopHeapObservation() owning teardown. kb-server and mc-server declare their
  Compose service labels.
- writeOnce()/start() are now total at every boundary, not just the write:
  target resolution, the injected logger and staging cleanup each escaped a
  method the ticket promises is never fatal, into a host service's boot.
- The collector guards its clock source, which was called outside the guard
  while the envelope was documented total.
- readDeclaredCeiling() reads NODE_OPTIONS as a second channel. Node does NOT
  merge it into process.execArgv, contrary to the JSDoc: measured on v25.9.0,
  NODE_OPTIONS=--max-old-space-size=256 leaves execArgv empty while
  heap_size_limit reports 448 MiB. Reading one channel reported `undeclared`
  for a ceiling genuinely in force. Divergence stays `ambiguous` — the
  resolution is V8's, and heapSizeLimitBytes is already observed independently.

Every new guard is mutation-convicted, including the boot witness, which reds
against the exact zero-caller state that shipped.

* fix(test): resolve the heap-observation dir from the committed config template (#16763)

The boot-reachability witness imported ai/config.mjs, which resolves a
repo-local ignored overlay. Tests resolve committed templates only —
ADR-0019 B1/C3, enforced by lint-config-template-ssot.

The template resolves the same directory the reporter writes to, so the
witness still convicts: removing the startHeapObservation() call reds it.
Re-ran that mutation after the import change rather than assuming the
assertion still pointed at the right path."
### @neo-opus-vega - 2026-08-09T10:43:41Z

## L2 landed; the L3 residual is deployment-gated, and here is the evidence it is not yet obtainable

PR #16771 merged at `4052141fce49` (10:38Z, @tobiu). #16776 closed with it. This ticket stays open for AC-9, exactly as the split intended.

**The running plane does not yet contain this code.** A snapshot 15.5s old, `status: available` — so the bridge is alive and reporting, just at an older revision:

```
chroma:       heapObservation FIELD ABSENT
kb-server:    heapObservation FIELD ABSENT
mc-server:    heapObservation FIELD ABSENT
orchestrator: heapObservation FIELD ABSENT
```

Absent as a **field**, not `unavailable` with a reason — which is the discriminator worth stating. A running-but-older orchestrator emits no `heapObservation` key at all; the merged reader would emit the key with `status: unavailable` and a reason (`absent`, `not-node`, …) even when it has nothing to report. So this output distinguishes *"the reader is not deployed"* from *"the reader is deployed and sees nothing"*, and it is the first.

**AC-9 therefore cannot be captured until the canonical plane is rebuilt onto a revision containing `4052141fce49`.** Merged is not running; a checked-in reader proves nothing about a live one.

### What to capture when the plane next carries this revision

Owner: @neo-opus-vega. Three readings, all from `get_deployment_state_snapshot`:

1. **AC-9 (closes this ticket):** a live `heapObservation` for `kb-server` and `mc-server` with `status: available`, stated alongside its environment — cgroup limit, declared ceiling, node version. The number without its environment is not the receipt AC-9 asks for.
2. **Cadence/skew:** `pairable: true` in steady state at the 10 s write cadence against the 15 s skew bound. If it is `false` in steady state, the bound is mis-sized rather than the channel broken — those are different repairs and the reading tells them apart.
3. **`ceilingSources` reads `['exec-argv']`** for both servers. A `node-options` entry would mean a ceiling arrived through the channel `docker-compose.yml:409` deliberately forbids, because `ProcessSupervisorService` spawns children with `{...process.env}` and would multiply the container budget per concurrent Node process. That reading is a deployment-drift check, not a formality.

I am **not** rebuilding the plane to obtain this right now: nine peers are mid-turn on a live Agent OS, and a rebuild to satisfy my own residual would spend their working environment on my receipt. The reading is cheap and non-destructive once a rebuild happens for any reason — it is three fields out of a snapshot already produced every 30 s.


- 2026-08-09T10:45:36Z @neo-opus-vega cross-referenced by #16630
- 2026-08-09T14:36:42Z @neo-opus-vega cross-referenced by #16795
- 2026-08-09T15:45:37Z @neo-opus-grace cross-referenced by #16695
- 2026-08-09T15:50:22Z @neo-opus-grace cross-referenced by #16706
- 2026-08-09T16:09:40Z @neo-opus-vega added sub-issue #16810
### @neo-opus-vega - 2026-08-09T16:12:45Z

## AC-9 is no longer blocked on a rebuild — the rebuild happened, and the channel is delivering nothing

My `10:43Z` comment said the receipt could not be captured until the plane carried `4052141fce49`. **It does now, and the reading is not the one I expected.**

```
kb-server -> {status: "unavailable", unavailableReason: "absent"}
mc-server -> {status: "unavailable", unavailableReason: "absent"}
chroma    -> {status: "unavailable", unavailableReason: "not-node"}
```

**The discriminator I wrote into that same comment is what proves this is not deployment lag.** I stated that a running-but-older orchestrator emits *no* `heapObservation` key at all, while the merged reader emits the key with `status: unavailable` and a reason. The key is present with a reason, so the reader is deployed. Independently confirmed by two other instruments:

```
git merge-base --is-ancestor 5fe2725f2922 55219f40d8   ->  YES
mc-server healthcheck: deployedRevision 55219f40d8…, startedAt 14:39:06Z, runtimeFreshness "current"
```

I nearly filed this observation under my own standing note that the plane had not been rebuilt — the note and the observation *agreed*, which is exactly why it almost survived. Three instruments, different mechanisms, one conclusion.

### Root cause: the channel has no shared mount

`AiConfig.heapObservation.dir` resolves to `<planeDataRoot>/heap-observation` (`ai/configBase.mjs:957`). That path appears **nowhere** in `ai/deploy/docker-compose.yml` — no volume, no mount, no entry in the top-level `volumes:` block. `kb-server` and `mc-server` write into their own container layers; the orchestrator reads its own empty one (`DeploymentStateBridgeService.mjs:763` → `ENOENT` → `absent`).

**The false premise is in this ticket's own body, and it is mine.** The Architectural Reality section asserts *"the shared data root is already a proven cross-container transport"* — true of `deployment-state`, which has a named volume, and generalized from there to the whole data root. `/app/.neo-ai-data` is a path, not a shared volume; only specific subdirectories are shared, each by an explicit mount. `chroma` reading `not-node` while the reporting services read `absent` is the tell: the reader's identity gate works, only the transport is missing.

**Why nothing caught it.** Every witness for this channel runs in one process on one filesystem, so the write and the read were always the same directory — the failure is a topology fact and is not expressible in an in-process fixture. And it failed silently rather than loudly because **writing into a container's own layer succeeds**: the reporter's deliberate totality never engaged, because there was no failure to swallow. A successful local write and a delivered one are indistinguishable from the writer's side, so the metric failed closed while the envelope reported a healthy reporter.

### Disposition

The topology fix and its guard are `#16810`, resolved by PR `#16811` — the same L2/L3 split this ticket already used for `#16776`. **This ticket stays open for AC-9**, unchanged: an L2 topology change cannot supply a live reading, and the plane must be recreated (`compose up -d`, never `restart` — a restart re-runs the baked container config and never attaches a new volume) before the receipt exists.

The three readings to capture then are the ones already listed above: `status: available` with its environment, `pairable: true` in steady state, and `ceilingSources: ['exec-argv']`.

One correction to my broadcast: I initially described all four Node services as affected. Only `kb-server` and `mc-server` declare `getHeapObservationServiceKey()`; `fleet-server` and `orchestrator` do not report at all, so **their `absent` is the honest answer** and is out of scope.


- 2026-08-09T17:02:55Z @neo-gpt-emmy cross-referenced by #16819
- 2026-08-09T21:28:11Z @neo-gpt-emmy cross-referenced by #16838
- 2026-08-09T22:11:03Z @neo-gpt-emmy cross-referenced by PR #16839

