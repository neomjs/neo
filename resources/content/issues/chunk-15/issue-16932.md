---
id: 16932
title: 'The ask tool defaults to a metered cloud model, and its local path was never wired — plus 31b is reachable from six places'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-opus-grace
createdAt: '2026-08-10T22:24:21Z'
updatedAt: '2026-08-11T07:01:01Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16932'
author: neo-opus-grace
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-11T07:01:01Z'
---
# The ask tool defaults to a metered cloud model, and its local path was never wired — plus 31b is reachable from six places

Operator directive, prio 0, issued 2026-08-11 after the Gemini API key was deleted: the `ask` tool must stop defaulting to a metered cloud model, and `gemma-4-31b-it` must be removed everywhere.

Two independent defects, one directive.

## 1. `ask` defaulted to a metered remote model

`ai/mcp/server/knowledge-base/configBase.mjs` declared:

```js
provider: leaf('gemini',            'NEO_KB_ASK_PROVIDER', 'string'),
model   : leaf('gemini-2.5-flash',  'NEO_KB_ASK_MODEL',    'string'),
```

Every `ask_knowledge_base` call billed a cloud provider **by default** — roughly **EUR 70/month** steady-state — and required a dedicated key that a peer subsequently exposed. The key has now been deleted upstream, so on `dev` the ask path is not merely expensive, it is **broken**.

The block's existing cost-safety machinery — a 20/min runaway breaker, a dedicated env-only key, a per-path budget cap — all *bounds a metered call*. None of it removes the meter, and none of it can do anything about a leaked key.

## 2. The ask path could not have worked locally anyway

`ai/services/knowledge-base/SearchService.mjs` built its provider config as:

```js
openAiCompatibleConfig: {...aiConfig.openAiCompatible, ...(ask.baseUrl ? {host: ask.baseUrl} : {}), model: ask.model}
```

Measured against the live KB config, `{...aiConfig.openAiCompatible}` is **`{}`**. An AiConfig node is a `Neo.state.Provider` proxy: its `get` trap walks the parent chain, but its `ownKeys` trap (`Provider#getTopLevelDataKeys`, `src/state/Provider.mjs:676`) enumerates **local `#dataConfigs` only**. These leaves live on the Tier-1 root, so from the Knowledge Base child every named read is correct and every enumeration is empty.

The spread silently dropped `host`, leaving `OpenAiCompatible` to fall back to its class default `http://127.0.0.1:8000` — **Chroma's port**. A chat request aimed at a vector database.

This never surfaced because the default was `gemini`, which ignores that object entirely. **Switching the default to local is precisely what would have exposed it**, so the two defects had to be fixed together or the "fix" would have shipped a broken ask path.

## 3. `gemma-4-31b-it` was reachable from several places

26b was selected over 31b on measured performance grounds. LM Studio and MLX **JIT-load whatever id they are handed**, so a stray 31b string is not a naming inconsistency — it is a load instruction for a ~20 GB dense model that then sits resident beside the 15.6 GB one already serving traffic (observed live: both resident simultaneously, 31b holding a 60-minute idle TTL).

Consumable occurrences found: the MLX task leaf, both parity Compose profiles' `NEO_KB_ASK_MODEL`, the parity-latency diagnostics script, the mock server's chat-model fallback, and `OpenAiCompatible`'s class default — the last being an **Ollama**-namespaced `gemma4:31b` inside the OpenAI-compatible provider, an id no OpenAI-compatible server can serve.

## Acceptance Criteria

- [ ] **AC-1** — `askSynthesis.provider` defaults to `openAiCompatible` and `askSynthesis.model` to `google/gemma-4-26b-a4b`; remote synthesis remains available via `NEO_KB_ASK_PROVIDER` / `NEO_KB_ASK_API_KEY`.
- [ ] **AC-2** — the ask path resolves a **host**. `SearchService` reads the four leaves `buildChatModel` consumes at the use site and never materializes an AiConfig node.
- [ ] **AC-3** — `NEO_KB_ASK_API_KEY` (a Gemini credential) is never forwarded to a local OpenAI-compatible endpoint.
- [ ] **AC-4** — no `gemma-4-31b-it` / `gemma4:31b` / `gemma4-31b` remains in any **consumable** position under `ai/` or `test/`. Explanatory prose that records *why 26b was chosen*, and dated historical benchmarks, are retained deliberately — deleting the rationale for a decision is not the same as removing the value.
- [ ] **AC-5** — a spec fails if the ask default regresses to a metered remote id, asserting on the **model id** and not only the provider name: a provider flip is visible in review, a model-id string edit is not.
- [ ] **AC-6** — the ask model id equals the Tier-1 `openAiCompatible.model` id, asserted across both files, so the two cannot drift into a second resident chat model.

## Out of scope

The `resources/content/archive/**` corpus contains ~180 historical references to 31b inside archived issues, PRs and discussions. Those are **records of what was said at the time** and are deliberately untouched — rewriting them would falsify history rather than fix configuration.

The broader defect class this sits inside — 30 unit specs carrying `test.skip(!!process.env.NEO_TEST_SKIP_CI, …)`, which skips in CI and therefore runs **only** against an operator's live LM Studio and Chroma — is tracked separately; it is what let a test dictate deployment model policy in the first place.


## Timeline

- 2026-08-10T22:24:23Z @neo-opus-grace added the `bug` label
- 2026-08-10T22:24:23Z @neo-opus-grace added the `ai` label
- 2026-08-10T22:24:45Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-10T22:27:15Z @neo-opus-grace cross-referenced by PR #16933
### @neo-opus-grace - 2026-08-10T22:34:20Z

**Scope extended — AC-7 added, and the headline number was wrong.**

Removing the 31b string stops one spec loading one wrong model. It does not stop the next spec, because the mechanism was never the string — it was the **guard**.

- [ ] **AC-7** — no unit spec may reach a live model server through the inverted `test.skip(!!NEO_TEST_SKIP_CI, …)` guard. Live-model specs default OFF everywhere and opt in via `NEO_TEST_LIVE_MODELS=true`, with a source-level arm that fails if the pair reappears.

**Two corrections to my own scoping, recorded because both would have shipped a large confident wrong change:**

1. **30 specs carry the inverted guard; only 4 reach a live service, and only 2 reach a model server.** The other 26 skip CI for slowness — legitimate, unrelated. `SessionSummarization` (LM Studio) and `KeepAlive` (Ollama daemon) are the load-instruction vectors; `GraphService` and `ChromaRecovery` touch Chroma, which the unit harness already isolates by construction on a run-scoped free port.

2. **The new guard's first version flagged 18 specs and was a false-positive machine.** It matched any spec *naming* a model port — mostly config-assertion specs (`configBase.spec.mjs`, `generateKimiSeatConfig.spec.mjs`) that carry the host as expected data and never open a socket. Gating those would have disabled real coverage for zero safety. The signal is the **pair**: reaches a model host **and** uses the skip-in-CI guard, because that pair means the author already knew live services were required and reached for the backwards guard to say so.

Mutation-verified: restoring the inverted guard on `KeepAlive.spec.mjs` fails the new arm and names the file. Non-vacuity asserts both halves of the pair are detectable independently, so an empty offender list cannot mean one matcher went blind.

Source-level rather than behavioural on purpose: the failure is a spec doing something to a machine CI does not have, so CI is structurally incapable of observing it.

- 2026-08-10T22:34:20Z @neo-opus-grace referenced in commit `616c343` - "fix(test): the live-model guard skipped CI and ran on the operator's machine (#16932)

Removing the 31b string stops one spec from loading one wrong model. It does not
stop the next spec from loading the next one, because the mechanism was never the
string — it was the guard.

Two specs that drive a REAL model server carried
`test.skip(!!process.env.NEO_TEST_SKIP_CI, ...)`: skip in CI, RUN everywhere else.
Everywhere else is the operator's machine, the one with models loaded, the real
Chroma, and finite RAM. CI — which has none of that and nothing to lose — was the
only environment exempted. Identical in shape to skipping on a missing API key:
the condition meaning "unsafe to run here" wired as the condition to skip, so the
guard fired exactly where running was harmless.

Both now default OFF everywhere and opt in via `NEO_TEST_LIVE_MODELS=true`. The
coverage is preserved for anyone who has deliberately provisioned a model server;
what is removed is a routine `npm run test-unit` doing live inference against
whatever LM Studio happens to be listening.

SCOPE, because the headline number was wrong. 30 specs carry the inverted guard,
but only FOUR reach a live service, and only TWO reach a model server. The other
26 skip CI for slowness — a legitimate and unrelated concern. Changing all 30
would have been a large confident wrong change.

The new guard was narrowed for the same reason. Its first version flagged every
spec merely NAMING a model port and reported 18 — almost all config-assertion
specs carrying the host as expected data, which never open a socket. Gating those
would disable real coverage to buy no safety. The signal is not "mentions a host";
it is the PAIR — reaches a model host AND uses the skip-in-CI guard — because that
pair means the author already knew live services were needed and reached for the
backwards guard to say so.

Mutation-verified: restoring the inverted guard on `KeepAlive.spec.mjs` fails the
new arm and names that file. Non-vacuity asserts BOTH halves of the pair are
detectable separately, so an empty offender list cannot mean "one matcher went
blind".

Why source-level and not behavioural: the failure is a spec doing something to a
machine CI does not have, so CI is structurally incapable of observing it. Reading
the guard is the only check that runs where the defect is invisible.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-10T22:44:56Z @neo-opus-grace referenced in commit `857d112` - "fix(test): seatCostReport built its fixtures at import and swept them on a hook (#16932)

Found while verifying the suite is clean after the live-model guard change — a
claim I could not make honestly while the full run kept failing.

`fixtureDir` was a module-scope `mkdtempSync` with `writeSyntheticFixtures()`
called beside it at IMPORT time, while cleanup was a top-level `test.afterAll`.
Those are two different lifecycles, and nothing tied the directory's existence to
the window in which the tests needed it. The two CLI arms — which spawn a child
process that reads through that directory — died on
`ENOENT … /seatcost-fixtures-XXXXXX/kimi-wire.jsonl`, and every run additionally
leaked one uncleaned temp dir. 52 had accumulated on this machine.

Creation now happens in `beforeAll` beside the `afterAll` that removes it.

WHY THIS MATTERED MORE THAN ONE SPEC. It failed in EVERY full-suite run and passed
in isolation, so it was read as flake — by me, three times in one night. It is not
flake. It is deterministic and merely needs enough sibling files in the worker to
surface. That distinction is load-bearing: a suite cannot be parallelized or
path-scoped while reproducible failures are dismissed as noise, and dismissing
them is exactly what a `workers: 1` CI configuration trains everyone to do.

Measured, full suite at 8 workers: 12679 passed, 1 failed, 2.9 min — against 4-7
failures across earlier runs tonight. The one remaining failure
(`ResizeObserver.spec.mjs:302`) is a Body-layer spec unrelated to this change and
present on a clean tree.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
### @neo-opus-grace - 2026-08-10T22:45:12Z

**AC-8 added — and the "flake" was deterministic all along.**

- [ ] **AC-8** — `seatCostReport.spec.mjs` creates its fixture directory in the same lifecycle that removes it, so the full suite stops failing on a swept fixture and stops leaking temp dirs.

Found while verifying the suite is clean after the guard change — a claim I could not make honestly while the full run kept failing.

`fixtureDir` was a module-scope `mkdtempSync` with `writeSyntheticFixtures()` beside it at **import** time, while cleanup was a top-level `test.afterAll`. Two different lifecycles, nothing tying the directory's existence to the window the tests needed it. The two CLI arms spawn a child that reads through that directory and died on `ENOENT … /seatcost-fixtures-XXXXXX/kimi-wire.jsonl`. Every run also leaked one uncleaned temp dir — **52 had accumulated** on this machine.

**The part worth recording: this was never flake.** It failed in *every* full-suite run and passed in isolation, so it was read as noise — by me, three times in one night, each time as a reason my own PR's red was "not mine". It is deterministic; it just needs enough sibling files in the worker to surface.

That distinction is load-bearing for the scoping/parallelization work: a suite cannot be safely parallelized or path-scoped while reproducible failures are dismissed as flake, and a `workers: 1` CI configuration is precisely what trains everyone to dismiss them.

**Measured, full suite at 8 workers:** `12679 passed · 1 failed · 2.9 min`, against 4–7 failures across earlier runs tonight. The remaining failure (`ResizeObserver.spec.mjs:302`) is Body-layer, unrelated, and present on a clean tree.

For context on the size of the prize: CI runs this suite at `workers: 1` (`playwright.config.unit.mjs:190`, no comment justifying it) and takes ~14 minutes, while the config's own documentation 60 lines above states the profiling project self-caps *"while the global stays wide for the bulk-parallelism win"*.

- 2026-08-11T00:20:35Z @neo-opus-grace referenced in commit `7993a34` - "fix(test): ResizeObserver installed its DOM globals at import and deleted them on a hook (#16932)

The same lifecycle mismatch as the seatCostReport fixture, in the Body tier, and
the last deterministic failure in the full suite.

`document`, `window`, `getComputedStyle`, `ResizeObserver` and the rAF pair are
installed at module scope — an IMPORT side effect — while `afterAll` restores
them to their Node values, which for `ResizeObserver` means DELETING it. Under
`fullyParallel` a worker can receive a second slice of the same file: the module
is already in the import cache so its top level never re-runs, but the previous
slice's `afterAll` has already torn the globals down. The next `beforeEach` then
dies inside `src/main/addon/ResizeObserver.mjs` with
`ReferenceError: ResizeObserver is not defined` — a production stack trace for a
harness defect, which is a large part of why it was never read as one.

`beforeEach` now re-installs them, so the globals are a per-test precondition
owned by the same lifecycle that tears them down. The import-time assignment
stays: the addon module is imported at this file's top level and must find a DOM
already in place, and the delete-stubs-then-import ordering above it is
load-bearing.

NOT FLAKE, AGAIN. It passed in isolation (7 passed) and failed in every full run
— the same signature as the fixture bug, and the same misreading. Reproduced
inside the `unit` project alone in 9.2s, which made it cheap to fix once someone
looked instead of re-running it.

Measured, full suite at 8 workers with both repairs: 12667 passed, 2 failed,
2.3 min. Both specs I repaired are gone from the failing set; the two that remain
(`McpServersHealth`, `MemoryCoreRecorderService`) vary between runs and are not
claimed as fixed here.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T00:56:49Z @neo-opus-grace referenced in commit `9c004de` - "fix(agent-os): the MLX substitution dropped `-it` and swapped in the BASE model (#16932)

Caught while writing my own review notes, not by a test — I flagged the id as
unverified to @neo-gpt, then verified it instead of leaving him to.

Replacing `mlx-community/gemma-4-31b-it-bf16` I pattern-matched
`mlx-community/gemma-4-26b-a4b-bf16`, which appears NOWHERE in this repo except
where I typed it. Checked against the mlx-community registry: it does exist, so
it would not have 404'd — but it is the BASE model. The original carried `-it`,
the INSTRUCTION-TUNED weights, which is what a chat / graph-parsing role needs.

That combination is the bad one. A fabricated id that does not resolve fails
loudly on first use. A fabricated id that resolves to the wrong VARIANT produces
no error at all, just worse output from whoever eventually flips
`NEO_ORCHESTRATOR_MLX_ENABLED` — with a config line that looks deliberate.

Now `mlx-community/gemma-4-26b-a4b-it-bf16`, with the registry check recorded at
the leaf so the next substitution does not re-derive it by pattern.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T01:14:36Z @neo-opus-grace referenced in commit `c65b94a` - "fix(knowledge-base): a host override inherited the displaced endpoint's credential (#16932)

@neo-gpt's finding, and it is the same defect I had just congratulated myself for
closing — one level up.

I removed the GEMINI key from the OpenAI-compatible branch and described the
credential boundary as closed. It was not. With a Tier-1 coordinate of
`https://managed.example` + `sk-remote`, the documented local override
`NEO_KB_ASK_BASE_URL=http://127.0.0.1:1234` moved the HOST while leaving
`sk-remote` attached, so the transport emitted `Authorization: Bearer sk-remote`
at a local LM Studio. One cross-provider leak closed, one cross-endpoint leak
opened, identical shape. He reproduced it against the real provider factory.

The rule his review states, which I did not have: A HOST AND ITS CREDENTIAL ARE
ONE COORDINATE. An own-endpoint override is BY DEFINITION not the Tier-1
endpoint, so the key travels with its own host or not at all. Empty is the safe
default — an endpoint that needs a credential fails loudly, while a wrong
credential leaves silently.

The construction moved into an exported `buildAskProviderConfigs`, pure and
drivable. Asserting this through SearchService alone would have meant recomputing
the expected value inside the test, which proves the arithmetic and not the
contract — and a producer-blind fixture is exactly what let the broadcast-only
read-state resolver survive review on #16918 earlier tonight.

Two controls, as the review specified:
- override present  -> local host, EMPTY key (mutation: restoring the inherited
  key fails this arm alone)
- override absent   -> Tier-1 host, Tier-1 key (non-vacuity: without it the first
  arm passes against a producer that never sends a key at all, which would break
  every managed deployment while looking secure)

Also corrected the PR body: "a credential stopped going somewhere it had no
business being" was true of the Gemini key and false as a boundary claim while
this branch existed.

636 passed across the knowledge-base + KB-config surfaces.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T07:01:01Z @tobiu referenced in commit `d6cba78` - "fix(knowledge-base): ask billed a cloud model by default, and its local path was never wired (#16932) (#16933)

* fix(knowledge-base): ask billed a cloud model by default, and its local path was never wired (#16932)

Operator directive, prio 0, after the Gemini API key was deleted: ask must stop
defaulting to a metered remote model, and `gemma-4-31b-it` must go.

THE DEFAULT. `askSynthesis` declared `provider: leaf('gemini')` +
`model: leaf('gemini-2.5-flash')`, so every `ask_knowledge_base` call billed a
cloud provider by nobody's decision — ~EUR 70/month steady-state — behind a
dedicated key a peer then exposed. The key is now deleted, so on dev the ask path
is not merely expensive, it is broken.

The block's cost-safety machinery (20/min runaway breaker, dedicated env-only key,
per-path budget cap) all BOUNDS a metered call. None of it removes the meter, and
none of it survives a leaked key. The default is now local; remote synthesis is
preserved and one env var away, so cloud spend costs a deliberate act rather than
silence.

AND THE LOCAL PATH COULD NOT HAVE WORKED. SearchService built its provider config
as a spread of the AiConfig node. Measured against the live KB config that spread
is `{}` — an AiConfig node is a `Neo.state.Provider` proxy whose `get` trap walks
the parent chain while its `ownKeys` trap (`Provider#getTopLevelDataKeys`)
enumerates local `#dataConfigs` only, and these leaves live on the Tier-1 root. It
silently dropped `host`, leaving the provider to fall back to its class default
`http://127.0.0.1:8000` — Chroma's port. A chat request aimed at a vector database.

That defect was invisible while the default was `gemini`, which ignores the object
entirely. Switching the default to local is exactly what exposes it, so shipping
the switch alone would have shipped a broken ask path. Both leaves are now read at
the use site, and the Gemini key is no longer forwarded to a local endpoint that
never asked for one.

31b. LM Studio and MLX JIT-load whatever id they are handed, so a stray
`gemma-4-31b-it` is not a naming inconsistency — it is a load instruction for a
~20 GB dense model. Observed live tonight: 31b and 26b resident simultaneously,
31b holding a 60-minute idle TTL. Removed from every consumable position: the MLX
task leaf, both parity Compose profiles, the parity-latency script, the mock
server fallback, and the OpenAI-compatible provider's class default — the last an
OLLAMA-namespaced id no such server can serve. That default is now `null`: a model
is a deployment decision the SSOT owns, and an omission should fail loudly rather
than resolve to a wrong model.

Retained deliberately: prose recording WHY 26b was chosen over 31b, and dated
benchmarks. Deleting the rationale for a decision is not removing the value.

The regression arm asserts the MODEL ID, not just the provider name — a provider
flip is visible in review, a model-id string edit is not — and cross-checks it
against the Tier-1 leaf so the two cannot drift into a second resident model.
Mutation-verified: restoring the remote model fails both arms.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(test): the live-model guard skipped CI and ran on the operator's machine (#16932)

Removing the 31b string stops one spec from loading one wrong model. It does not
stop the next spec from loading the next one, because the mechanism was never the
string — it was the guard.

Two specs that drive a REAL model server carried
`test.skip(!!process.env.NEO_TEST_SKIP_CI, ...)`: skip in CI, RUN everywhere else.
Everywhere else is the operator's machine, the one with models loaded, the real
Chroma, and finite RAM. CI — which has none of that and nothing to lose — was the
only environment exempted. Identical in shape to skipping on a missing API key:
the condition meaning "unsafe to run here" wired as the condition to skip, so the
guard fired exactly where running was harmless.

Both now default OFF everywhere and opt in via `NEO_TEST_LIVE_MODELS=true`. The
coverage is preserved for anyone who has deliberately provisioned a model server;
what is removed is a routine `npm run test-unit` doing live inference against
whatever LM Studio happens to be listening.

SCOPE, because the headline number was wrong. 30 specs carry the inverted guard,
but only FOUR reach a live service, and only TWO reach a model server. The other
26 skip CI for slowness — a legitimate and unrelated concern. Changing all 30
would have been a large confident wrong change.

The new guard was narrowed for the same reason. Its first version flagged every
spec merely NAMING a model port and reported 18 — almost all config-assertion
specs carrying the host as expected data, which never open a socket. Gating those
would disable real coverage to buy no safety. The signal is not "mentions a host";
it is the PAIR — reaches a model host AND uses the skip-in-CI guard — because that
pair means the author already knew live services were needed and reached for the
backwards guard to say so.

Mutation-verified: restoring the inverted guard on `KeepAlive.spec.mjs` fails the
new arm and names that file. Non-vacuity asserts BOTH halves of the pair are
detectable separately, so an empty offender list cannot mean "one matcher went
blind".

Why source-level and not behavioural: the failure is a spec doing something to a
machine CI does not have, so CI is structurally incapable of observing it. Reading
the guard is the only check that runs where the defect is invisible.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(test): seatCostReport built its fixtures at import and swept them on a hook (#16932)

Found while verifying the suite is clean after the live-model guard change — a
claim I could not make honestly while the full run kept failing.

`fixtureDir` was a module-scope `mkdtempSync` with `writeSyntheticFixtures()`
called beside it at IMPORT time, while cleanup was a top-level `test.afterAll`.
Those are two different lifecycles, and nothing tied the directory's existence to
the window in which the tests needed it. The two CLI arms — which spawn a child
process that reads through that directory — died on
`ENOENT … /seatcost-fixtures-XXXXXX/kimi-wire.jsonl`, and every run additionally
leaked one uncleaned temp dir. 52 had accumulated on this machine.

Creation now happens in `beforeAll` beside the `afterAll` that removes it.

WHY THIS MATTERED MORE THAN ONE SPEC. It failed in EVERY full-suite run and passed
in isolation, so it was read as flake — by me, three times in one night. It is not
flake. It is deterministic and merely needs enough sibling files in the worker to
surface. That distinction is load-bearing: a suite cannot be parallelized or
path-scoped while reproducible failures are dismissed as noise, and dismissing
them is exactly what a `workers: 1` CI configuration trains everyone to do.

Measured, full suite at 8 workers: 12679 passed, 1 failed, 2.9 min — against 4-7
failures across earlier runs tonight. The one remaining failure
(`ResizeObserver.spec.mjs:302`) is a Body-layer spec unrelated to this change and
present on a clean tree.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(test): ResizeObserver installed its DOM globals at import and deleted them on a hook (#16932)

The same lifecycle mismatch as the seatCostReport fixture, in the Body tier, and
the last deterministic failure in the full suite.

`document`, `window`, `getComputedStyle`, `ResizeObserver` and the rAF pair are
installed at module scope — an IMPORT side effect — while `afterAll` restores
them to their Node values, which for `ResizeObserver` means DELETING it. Under
`fullyParallel` a worker can receive a second slice of the same file: the module
is already in the import cache so its top level never re-runs, but the previous
slice's `afterAll` has already torn the globals down. The next `beforeEach` then
dies inside `src/main/addon/ResizeObserver.mjs` with
`ReferenceError: ResizeObserver is not defined` — a production stack trace for a
harness defect, which is a large part of why it was never read as one.

`beforeEach` now re-installs them, so the globals are a per-test precondition
owned by the same lifecycle that tears them down. The import-time assignment
stays: the addon module is imported at this file's top level and must find a DOM
already in place, and the delete-stubs-then-import ordering above it is
load-bearing.

NOT FLAKE, AGAIN. It passed in isolation (7 passed) and failed in every full run
— the same signature as the fixture bug, and the same misreading. Reproduced
inside the `unit` project alone in 9.2s, which made it cheap to fix once someone
looked instead of re-running it.

Measured, full suite at 8 workers with both repairs: 12667 passed, 2 failed,
2.3 min. Both specs I repaired are gone from the failing set; the two that remain
(`McpServersHealth`, `MemoryCoreRecorderService`) vary between runs and are not
claimed as fixed here.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(agent-os): the MLX substitution dropped `-it` and swapped in the BASE model (#16932)

Caught while writing my own review notes, not by a test — I flagged the id as
unverified to @neo-gpt, then verified it instead of leaving him to.

Replacing `mlx-community/gemma-4-31b-it-bf16` I pattern-matched
`mlx-community/gemma-4-26b-a4b-bf16`, which appears NOWHERE in this repo except
where I typed it. Checked against the mlx-community registry: it does exist, so
it would not have 404'd — but it is the BASE model. The original carried `-it`,
the INSTRUCTION-TUNED weights, which is what a chat / graph-parsing role needs.

That combination is the bad one. A fabricated id that does not resolve fails
loudly on first use. A fabricated id that resolves to the wrong VARIANT produces
no error at all, just worse output from whoever eventually flips
`NEO_ORCHESTRATOR_MLX_ENABLED` — with a config line that looks deliberate.

Now `mlx-community/gemma-4-26b-a4b-it-bf16`, with the registry check recorded at
the leaf so the next substitution does not re-derive it by pattern.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(knowledge-base): a host override inherited the displaced endpoint's credential (#16932)

@neo-gpt's finding, and it is the same defect I had just congratulated myself for
closing — one level up.

I removed the GEMINI key from the OpenAI-compatible branch and described the
credential boundary as closed. It was not. With a Tier-1 coordinate of
`https://managed.example` + `sk-remote`, the documented local override
`NEO_KB_ASK_BASE_URL=http://127.0.0.1:1234` moved the HOST while leaving
`sk-remote` attached, so the transport emitted `Authorization: Bearer sk-remote`
at a local LM Studio. One cross-provider leak closed, one cross-endpoint leak
opened, identical shape. He reproduced it against the real provider factory.

The rule his review states, which I did not have: A HOST AND ITS CREDENTIAL ARE
ONE COORDINATE. An own-endpoint override is BY DEFINITION not the Tier-1
endpoint, so the key travels with its own host or not at all. Empty is the safe
default — an endpoint that needs a credential fails loudly, while a wrong
credential leaves silently.

The construction moved into an exported `buildAskProviderConfigs`, pure and
drivable. Asserting this through SearchService alone would have meant recomputing
the expected value inside the test, which proves the arithmetic and not the
contract — and a producer-blind fixture is exactly what let the broadcast-only
read-state resolver survive review on #16918 earlier tonight.

Two controls, as the review specified:
- override present  -> local host, EMPTY key (mutation: restoring the inherited
  key fails this arm alone)
- override absent   -> Tier-1 host, Tier-1 key (non-vacuity: without it the first
  arm passes against a producer that never sends a key at all, which would break
  every managed deployment while looking secure)

Also corrected the PR body: "a credential stopped going somewhere it had no
business being" was true of the Gemini key and false as a boundary claim while
this branch existed.

636 passed across the knowledge-base + KB-config surfaces.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

---------

Co-authored-by: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T07:01:01Z @tobiu closed this issue

