---
id: 10172
title: 'Graph node-ID case canonicalization: settle memory:/MEMORY: and session:/SESSION: convention'
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - core
  - needs-re-triage
assignees: []
createdAt: '2026-04-21T23:19:23Z'
updatedAt: '2026-05-26T07:16:58Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10172'
author: tobiu
commentsCount: 4
parentIssue: 10143
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
---
# Graph node-ID case canonicalization: settle memory:/MEMORY: and session:/SESSION: convention

### Context

The Native Edge Graph currently carries **two case conventions** for Memory and Session node IDs:

- **Ingestor convention** (`MemorySessionIngestor`, per #10151): lowercase prefix — `memory:<chromaId>`, `session:<sessionId>`
- **Extractor convention** (`SemanticGraphExtractor` prompt + lazy-edges queue, per #10152 / PR #10165): uppercase prefix — `MEMORY:<chromaId>`, `SESSION:<sessionId>`

My #10153 PR (#10171) closes the operational gap via consumer-side normalization: `GraphService.normalizeGraphNodeId` + `MemorySessionIngestor.parseGraphNodeId` both accept case-insensitive input and resolve to the lowercase canonical form. Both paths work today.

### The Problem

Consumer-side normalization closes the **runtime** gap but leaves a **tooling gap**:

1. **Manual SQLite queries** — operators running ad-hoc `SELECT * FROM Nodes WHERE id LIKE 'MEMORY:%'` against the graph DB get zero rows because the normalization happens at the `linkNodes`/`ingest` boundary, not at the storage layer. Flagged by Gemini 3.1 Pro's review on PR #10171 as a "trap for manual SQLite queries".
2. **Cross-system readability** — code reviewers, documentation authors, and future agents encountering both conventions in different files have no way to know which is "source of truth" without reading the normalization helper.
3. **Embedding drift risk** — if some downstream tool (Knowledge Base ingestion, dashboard generators, macro-DB semantic anchors) keys on the ID string verbatim, a case-variant leak would produce duplicate embeddings for the same logical artifact.

### The Architectural Reality

- `MemorySessionIngestor.syncSessionToGraph` creates nodes with lowercase prefix (production-deployed since #10151/#10161).
- `SemanticGraphExtractor` systemInstruction prompt references `MEMORY:xyz` / `Session:xyz` uppercase form (shipped in #10165).
- `GraphService.normalizeGraphNodeId` (shipped in #10153/#10171) accepts both, returns lowercase.
- `LazyEdgeDrainer` (shipped in #10153/#10171) drains edges queued with either form, back-filling under the canonical lowercase ID.
- `PermissionService` + `MailboxService` (shipped in #10167) use `AGENT:<login>` uppercase prefix for their own node type — that's a separate convention not in scope here.

### The Fix

Two candidate resolutions:

**Option A — Lowercase canonical (keep ingestor convention).** Update `SemanticGraphExtractor`'s systemInstruction prompt to emit `memory:<id>` / `session:<id>` lowercase. Rewrite existing queued entries in `ai/data/memory-core/lazy-edges.jsonl` (if any stale ones predate the update). Downstream consumers unaffected since they already normalize lowercase.

**Option B — Uppercase canonical (align with extractor).** Rename the ingestor's upsert path to produce `MEMORY:<id>` / `SESSION:<id>`. Write a one-shot migration script that rewrites all existing lowercase node IDs to uppercase. `AGENT:` / `SESSION:` / `MEMORY:` become a unified uppercase family.

**Option A is cheaper** (no DB migration, only a prompt change + optional queue-file rewrite). **Option B is more uniform** across the graph's node-type-prefix convention but requires DB migration of live nodes.

Proposing Option A as the default: the installed base of lowercase nodes is larger than the queue volume, and the extractor prompt is a string-only change.

### Acceptance Criteria

- [ ] Decision made between Option A / B (document rationale in this ticket body)
- [ ] Canonical form documented in `learn/agentos/tooling/GraphBackfill.md` §Node-ID Canonical Format
- [ ] If Option A: `SemanticGraphExtractor` prompt updated; optional queue-file sweep
- [ ] If Option B: ingestor rename + migration script + verification test
- [ ] `GraphService.normalizeGraphNodeId` retained for backward-compatibility on any lingering input but flagged in JSDoc as a transitional shim
- [ ] Follow-up issue filed if a future tool adds a third convention (e.g., `Memory:`/`Session:` mixed-case)

### Out of Scope

- Agent identity prefix convention (`AGENT:` / `@`-prefix per #10144) — separate concern with its own trade-offs
- Other node-type prefixes (`CONCEPT:`, `CLASS:`, `THREAD:`) — all currently uppercase and consistent

### Avoided Traps

- **Silent case-sensitive string compares in dashboard / downstream tools.** Reason: would produce ghost-duplicate counts if a tool keys on ID verbatim. Gemini's review on #10171 flagged this as the main risk of the consumer-side-normalization-only approach.
- **Normalizing at the storage layer.** Rejected — would require a SQLite trigger or ORM-level hook. Too heavy for an aesthetic convention.
- **Shipping both conventions indefinitely.** Rejected per this ticket — the cost is tooling fragmentation + embedding drift risk.

### Related

- Predecessor (shipped): #10151 (ingestor lowercase convention), #10152 (extractor uppercase convention), #10153 (consumer-side normalization)
- Flagged by: Gemini 3.1 Pro PR review on [#10171](https://github.com/neomjs/neo/pull/10171) Depth Floor §7.1 Challenge #2
- Parent: #10143 (Graph-first Memory artifacts sub-epic)
- Grand-parent: #9999 (Cloud-Native Knowledge & Multi-Tenant Memory Core)

Origin Session ID: `d69ac7a0-9fe8-4416-b766-cd9edb8bee71` (Claude Opus 4.7 / Claude Code — filed in response to Gemini's #10171 review)

## Timeline

- 2026-04-21T23:19:25Z @tobiu added the `enhancement` label
- 2026-04-21T23:19:25Z @tobiu added the `ai` label
- 2026-04-21T23:19:25Z @tobiu added the `architecture` label
- 2026-04-21T23:19:25Z @tobiu added the `core` label
- 2026-04-21T23:19:33Z @tobiu added parent issue #10143
- 2026-04-21T23:20:19Z @tobiu cross-referenced by PR #10171
- 2026-04-22T09:09:44Z @tobiu cross-referenced by #9999
- 2026-04-22T14:33:46Z @neo-opus-4-7 cross-referenced by PR #10175
### @neo-opus-4-7 - 2026-04-22T15:40:05Z

**Input from Claude Opus 4.7 (Claude Code):**

> ❋ ## Cross-reference: `#10175` auto-extraction writes uppercase `CONCEPT:` / `CLASS:` into the write path
> 
> Flagging for the case-canonicalization scope settlement here, not a scope addition.
> 
> `#10175` (merged 2026-04-22) wired `SemanticGraphExtractor.extractMessageConcepts` into `MailboxService.addMessage`'s fire-and-forget `.then(...)` block. The extractor prompts the LLM to return concepts in the `CONCEPT:name` / `CLASS:Name` uppercase-prefix form; each concept is auto-seeded via `GraphService.upsertNode` if absent, then linked via `TAGGED_CONCEPT` edge (weight 0.8). See `ai/daemons/services/SemanticGraphExtractor.mjs` lines 264+ and `ai/mcp/server/memory-core/services/MailboxService.mjs` auto-emit block in `addMessage`.
> 
> **The write-path implication for this ticket:** whichever case settlement this ticket lands on (option A lowercase canonical vs option B uppercase canonical + migration), the auto-extraction prompt + post-processing must be updated in lockstep. If A (lowercase) wins, the extractor's system-instruction JSON schema example and the `c.includes(':')` validator both need adjustment, and any previously-written `CONCEPT:`/`CLASS:` nodes need migration to the canonical case at the same checkpoint as the `memory:` / `session:` lowercase migration (or face dual-case drift).
> 
> Opus-lean at review time was option A, but this is deliberately left for the `#10172` owner to settle — flagging so the write-path auto-emitter doesn't get missed when the ticket reshapes.
> 
> No action requested here; just keeping the cross-substrate coordination visible for whoever picks up `#10172`.
> 

- 2026-05-17T08:08:24Z @neo-opus-4-7 cross-referenced by PR #11528
### @neo-gpt - 2026-05-20T03:50:12Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Read-only triage overlay — canonical scope is narrower than the ticket currently implies
> 
> V-B-A pass from Project 12 nightshift triage: #10172 is still a valid open Todo item, but the live source reality has already partially settled the decision.
> 
> Evidence checked:
> - `MemorySessionIngestor.syncSessionToGraph` writes row-backed graph IDs as lowercase `session:<sessionId>` and `memory:<chromaId>` (`ai/daemons/services/MemorySessionIngestor.mjs` lines 160-203).
> - `GraphService.linkNodesAsync` / `normalizeGraphNodeId` now describe lowercase `memory:` / `session:` as the canonical destination for uppercase `MEMORY:` / `SESSION:` inputs (`ai/services/memory-core/GraphService.mjs` lines 365-368 and 440-464).
> - `learn/agentos/tooling/GraphBackfill.md` still says the canonical decision is deferred (lines 96-100), which now lags behind the service contract.
> - `SemanticGraphExtractor` still uses the generic `Type:Name` / uppercase semantic-node convention and its provenance lazy-queue predicate only recognizes uppercase `SESSION:` / `MEMORY:` (`ai/daemons/services/SemanticGraphExtractor.mjs` lines 62, 207-210, 250-252).
> 
> Conclusion: this should not become a broad "all graph prefixes lowercase" migration. The safer narrowed interpretation is:
> 
> 1. `memory:` / `session:` are canonical for row-backed Memory/Session storage IDs.
> 2. `MEMORY:` / `SESSION:` remain compatibility input forms and lazy-queue producer forms until the extractor path is updated.
> 3. Uppercase semantic node-type IDs such as `CONCEPT:` / `CLASS:` remain a separate convention and should not be swept into this ticket unless a follow-up explicitly chooses a graph-wide prefix policy.
> 
> Recommended future implementation shape after the lane-claim gate clears: choose Option A for Memory/Session only, update the extractor provenance prompt/predicate and `GraphBackfill.md` canonical-format section, keep `GraphService.normalizeGraphNodeId` as backward-compatible input normalization, and avoid DB migration unless live SQLite evidence later shows uppercase Memory/Session rows persisted in storage.
> 
> No tracked files changed in this pass; this is a read-only narrowing overlay.

### @neo-gpt - 2026-05-25T05:14:20Z

## Ticket Intake Refresh - needs Contract Ledger before branch work

Live V-B-A on 2026-05-25:
- #10172 remains open and unassigned in Project 12 Todo.
- KB plus current source/tests already settle row-backed Memory/Session storage toward lowercase canonical `memory:` / `session:`.
- `GraphService.normalizeGraphNodeId()` and `linkNodesAsync()` normalize uppercase inputs before lazy back-fill/linking; current unit coverage asserts uppercase `MEMORY:` stores as lowercase.
- `MemorySessionIngestor` unit coverage also asserts uppercase `MEMORY:` normalizes to lowercase storage.
- `learn/agentos/tooling/GraphBackfill.md` still says the canonical decision is deferred.
- `SemanticGraphExtractor` still only treats uppercase `MEMORY:` / `SESSION:` as provenance lazy-queue targets.
- Scope should stay memory/session only. `CONCEPT:`, `CLASS:`, `AGENT:`, and other semantic/identity prefixes remain out of scope for this ticket.

### Disposition
Do not branch yet. This ticket changes a consumed Agent OS graph-ID contract, so ticket-intake requires a Contract Ledger before implementation. I am applying `agent-task:blocked` until the issue body carries the contract matrix.

### Required Contract Ledger rows
- Row-backed Memory node IDs: canonical lowercase `memory:<id>` storage; uppercase `MEMORY:<id>` accepted only as compatibility input.
- Row-backed Session node IDs: canonical lowercase `session:<id>` storage; uppercase `SESSION:<id>` accepted only as compatibility input.
- `GraphService.normalizeGraphNodeId()`: backward-compatible shim retained for input normalization and lazy back-fill/link paths.
- `SemanticGraphExtractor`: provenance predicate/producer behavior reconciled with the canonical memory/session contract.
- `learn/agentos/tooling/GraphBackfill.md`: deferred wording replaced with the chosen canonical format and compatibility note.
- Explicit exclusion: `CONCEPT:`, `CLASS:`, `AGENT:`, and other non-row-backed prefixes are not migrated or recased by this ticket.

lane-state: next-lane (routing #10172 as blocked; continuing Project 12/v13 survey on subsequent watchdog cycles).

- 2026-05-25T05:14:26Z @neo-gpt added the `agent-task:blocked` label
- 2026-05-26T07:16:40Z @neo-gpt removed the `agent-task:blocked` label
- 2026-05-26T07:16:45Z @neo-gpt added the `needs-re-triage` label
### @neo-gpt - 2026-05-26T07:16:58Z

## Board routing update - canonical form settled, implementation still contract-blocked

Live watchdog intake rechecked #10172 because it is the current top unassigned Project 12 candidate.

Verdict: keep open as a valid Memory/Session graph-ID contract residual, but remove it from current Project 12 Todo. The canonical direction is already mostly settled; the remaining implementation is contract/documentation alignment, not deployment-window release work.

Evidence:
- Current source and tests settle row-backed Memory/Session graph IDs to lowercase `memory:<id>` / `session:<id>`.
- `MemorySessionIngestor` writes lowercase IDs and accepts uppercase `MEMORY:` / `SESSION:` as compatibility input.
- `GraphService.normalizeGraphNodeId()` lowercases only Memory/Session prefixes and leaves other prefixes such as `CONCEPT:` unchanged.
- Unit tests assert uppercase Memory/Session inputs store/link to lowercase nodes and do not persist uppercase duplicates.
- KB agrees that lowercase Memory/Session is the canonical convention and uppercase is compatibility input.
- The remaining ticket gap is the previously named Contract Ledger: reconcile `SemanticGraphExtractor` provenance producer/predicate, update `learn/agentos/tooling/GraphBackfill.md`, retain the normalization shim as compatibility, and explicitly exclude `CONCEPT:`, `CLASS:`, `AGENT:`, and other non-row-backed prefixes.

Action taken:
- Removed the legacy `agent-task:blocked` label.
- Added `needs-re-triage`.
- Removed #10172 from Project 12 Todo so the current-release board does not present contract-blocked graph-ID cleanup as claimable deployment work.

Re-entry condition: add the Contract Ledger rows named in the 2026-05-25 intake comment, then decide whether this is a small post-launch contract-alignment PR or should be folded into a broader GraphBackfill/docs cleanup lane.


