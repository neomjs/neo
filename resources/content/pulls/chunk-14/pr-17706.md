---
number: 17706
title: 'fix(ai): fail stale script-plane authority (#17705)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-24T12:32:47Z'
updatedAt: '2026-08-24T12:40:03Z'
closedAt: null
mergedAt: null
head: codex/17705-script-plane-stale-authority
base: dev
url: 'https://github.com/neomjs/neo/pull/17706'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17705

The script-plane identity ratchet now treats authority that no longer matches observation as a CI failure. The repaired `buildKbAgentFaqs.mjs` edge is removed; stale unresolved-edge and known-conflict entries both return red with exact identities, while current held authority remains green.

Related: #17500 · #17182 · #17217

Decision Record impact: `aligned-with` the identity-ledger and plane-authority contracts already carried by #16929 / ADR 0014; no amendment.

Evidence: L3 (red-first mutation contract, exact-head focused suite, and direct production CLI) → L3 required (the ticket changes CI admission behavior and its executable authority receipt). No scoped residuals.

## AC Evidence

| AC-1 | Removed the retired `buildKbAgentFaqs.mjs::unresolved-specifier` identity repaired by #17182 / PR #17199. |
| AC-2 | A fictional unresolved-edge identity returns `exitCode: 1` and is returned in `resolved`. |
| AC-3 | A fictional known-conflict identity returns `exitCode: 1` and is returned in `resolvedConflicts`. |
| AC-4 | A fully matched edge ledger and a currently reproducing held conflict each return `exitCode: 0`. |
| AC-5 | The unchanged-count substitution falsifier remains red and names both the appeared and retired identities. |
| AC-6 | Exact head `91ee1c7b27`: direct script-plane CLI exits 0 with 75 roots, 12 current unresolved edges, and no stale-authority notice. |
| AC-7 | Exact head `91ee1c7b27`: focused script-plane unit suite passes 70/70. |

## Deltas from ticket

None. Both stale ledgers use the same existing `runLint()` predicate; no new lint, taxonomy, census, or authority class was added.

## Test Evidence

- RED: test-only mutation run failed exactly 2 new assertions while 68 neighboring tests passed: stale edge `0 → expected 1`; stale conflict `0 → expected 1`.
- GREEN: `npx playwright test --config test/playwright/playwright.config.unit.mjs test/playwright/unit/ai/scripts/lint/scriptPlaneClosure.spec.mjs` — 70/70 at `91ee1c7b27`.
- GREEN: `node ai/scripts/lint/lint-script-plane.mjs` — 75 roots, 12 unresolved edges all known, held temporal-summary conflict surfaced, exit 0 at `91ee1c7b27`.
- GREEN: agent preflight and the commit-hook whitespace, shorthand, AiConfig mutation, atomic-write, JSDoc, ticket-archaeology, block-alignment, and parse gates passed. Agent preflight separately reported two existing non-blocking Tier-1 overlay warnings outside this diff.

## Post-Merge Validation

None. The existing Script Plane Lint workflow and focused mutations are the standing CI contract.

## Signal Ledger

- `gpt` author signal: Emmy's source-Discussion signal, carried by Epic #17500 at its corrected final body anchor.
- `claude` non-author signal: Vega's `[GRADUATION_APPROVED]`, revalidated in the independent Epic Review at https://github.com/neomjs/neo/issues/17500#issuecomment-5376063521.
- Family-keyed quorum remains current for this extraction-proof authority repair.

## Unresolved Dissent

None at the corrected Discussion/Epic authority anchor.

## Unresolved Liveness

Kimi is benched/unhosted and Gemini is operator-benched; neither absence is counted as consent or a hold gate.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session 0dc1379e-5329-4fba-80ca-f6466822f7c9.
