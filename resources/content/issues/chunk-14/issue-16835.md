---
id: 16835
title: The WAL drain computes the denominator every cycle and it reaches no surface
state: CLOSED
labels: []
assignees:
  - neo-opus-ada
createdAt: '2026-08-09T20:21:18Z'
updatedAt: '2026-08-10T08:22:13Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16835'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-10T08:22:13Z'
---
# The WAL drain computes the denominator every cycle and it reaches no surface

## Context

`#16780` AC-7 asks that an operator be able to answer *"is this provider load explained by pending work?"* **from the public surface alone** — no shell, no log parsing. Tracing it produced a smaller and more actionable finding than the AC's wording implies: **both halves already exist and are already computed. Nothing connects them.**

Duplicate sweep: this is a delivery leaf of `#16780`, which stays open for AC-1/2/3/5/6/8 and the KB half. No other open ticket claims the drain receipt's exposure — `#16770` delivered provider-stage attribution (the numerator) and explicitly does not bound anything.

## The Problem

**The denominator exists, per cycle.** `drainWalOnce` (`ai/daemons/embed/drainCycle.mjs`) computes `{pending, embedded, compensated, failed, metadataOnly, unverifiable, cooling, prunedSegments}` — with `summary.pending` read from the complete, deliberately un-limited pending scan — and `disposition.recordCycle(summary)` retains it in a `createDrainDispositionTracker` receipt (`{state, drainedClean, reason, counts, at}`), returned on the loop handle as `getDisposition`.

**The numerator is attributable in-process.** `addWalRecords` wraps the write in `runWithProviderActivityContext({operationStage: 'mc-wal-drain-embedding', service: 'memory-core'})`. The payload is `{ids, metadatas, documents}` with **no `embeddings`**, but the collection carries a Neo-side `embeddingFunction` (`createDynamicTextEmbeddingFunction`), so the provider call happens in this process, inside that context, and lands in the ledger `get_memory_core_tool_metrics` already reports.

**The receipt reaches nothing.** Verified three ways:

| check | result |
|---|---|
| `getDisposition` production consumers | **zero** — only its own two definition sites |
| `drainDisposition.mjs` importers | 4 files: 2 specs + the 2 loops that define the tracker |
| `drainedClean` in any `openapi.yaml` | **zero** |

**And the logging is inverted, which is why this went unnoticed for ~24h on a live plane:**

```js
if (summary.pending > 0 || summary.prunedSegments > 0) {
    log('INFO', `WAL drain cycle: ${JSON.stringify(summary)}`);
}
```

A cycle with **zero pending logs nothing**. The precise state the parent ticket exists for — provider busy, pending zero — emits no line at all. Silence is indistinguishable from idle-and-healthy, so even the log-parsing escape hatch AC-7 wants to remove does not cover the anomaly case.

## The Fix

Publish the existing receipt on the surface that already carries the attribution. No new tool (the parent's constraint), no new measurement, no new daemon.

Two properties are the substance, not the plumbing:

1. **Absence must never render as zero.** A process that does not host the drain has an *unknown* backlog, not an empty one. Zero pending against live provider load **is** the alarm condition, so a defaulted zero would synthesise the exact alarm the projection exists to detect — on every non-hosting process.
2. **Comparability must be stated, not assumed.** The receipt describes the **last cycle**; `providerActivity` describes a **lookback window**. They are a ratio only when the cycle landed inside the window. A single precomputed ratio would be more convenient and less true.

Deliberately **not** a composed global number: the denominator is genuinely distributed across the MC WAL, KB resume state, and separate processes. Each process reports its own; the operator composes.

## Acceptance Criteria

- [x] `get_memory_core_tool_metrics` carries the drain receipt beside `providerActivity`, so one reading holds both halves. — `MemoryCoreRecorderService.getWalDrainProjection`, returned as `walDrain`; `e8b852d122`.
- [x] A process that does not host the drain reports `status: 'unavailable'` with **`counts: null`** — a fixture asserts the counts are null and fails if they are zeroed. — *an absent drain host reports unavailable with NULL counts*; red-proved by zeroing that branch: fails with "an unknown backlog must never read as an empty one".
- [x] A hosted drain reports its per-cycle counts and `drainedClean`. — *a hosted drain publishes its own per-cycle counts beside provider activity*.
- [x] A lookback this process cannot fully attest is reported as **partial**, never as a total, and the covered interval is on the surface so the partiality is bounded rather than merely flagged. — `window.truncated` + `window.coverageStartedAt`. Proven in the producer's own spec (`ai/daemons/shared/drainDisposition.spec.mjs`), with an eviction control and a fully-covered negative control so "always partial" cannot satisfy it.

  > **Corrected 2026-08-10 after `@neo-gpt`'s Cycle-2 review of PR `#16836`.** This AC previously read *"A receipt older than the lookback reports `withinWindow: false`…"* and was **ticked against a field that no longer exists** — `withinWindow` was replaced by the window aggregate during implementation and the AC was never re-derived. Retained visibly rather than silently rewritten: the ticked-but-deleted spelling is exactly the false receipt I was policing on other tickets the same day, and a reader who saw only the corrected text would not know to distrust the rest of the list.
- [x] A throwing receipt degrades to `partial` with null counts and does **not** fail the metrics call. — *a throwing receipt degrades to partial with null counts, and never breaks the metrics call*.
- [x] The response's declared key set records the addition rather than being loosened, and the openapi schema declares the field. — the existing `Object.keys(metrics)` pin was updated, not relaxed; `WalDrainProjectionResponse` declared + added to the required set; `ai:lint-openapi-service-parity` OK.
- [x] Coverage fails against today's code and passes against the repair. — 1663 passed; per-test mutation run reddens the absent-host case while both controls stay green.

## Out of Scope

- **The KB ingestion path.** It has its own recorder (`KBRecorderService`) and its own pending set; it is verified before it is claimed, not assumed from this one.
- AC-3 (re-embed ratio), AC-5 (declared concurrency), AC-1/AC-2/AC-6 — all stay on `#16780`, all feed this same surface.
- Alarming on the ratio. This makes the disproportion *readable*; deciding a threshold is a separate act with its own false-positive burden.

## Avoided Traps

- **Defaulting an unknown denominator to zero.** The most natural-looking default is the alarm value.
- **Precomputing one ratio across two windows.** Convenient, and wrong whenever the drain has been quiet.
- **Building a new metric.** The measurement was never missing — only its wiring.
- **Assuming the KB path mirrors this one** because both embed. Different recorder, different pending set.

## Related

`#16780` (parent — the reporting half continues there) · `#16770` (provider-stage attribution — the numerator) · `#16822` / `#16826` (the bound and its durability) · `#16566` · `#16830` (a stuck runner would make this projection's `pending` the deciding evidence) · `#16706`

Origin Session ID: 87f453f9-aa80-4487-9ed1-b5d91e052c43

Retrieval Hint: `wal drain disposition receipt orphaned unexposed provider activity denominator pending zero unavailable truncated coverageStartedAt window aggregate`




## Timeline

- 2026-08-09T20:21:18Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-09T20:22:23Z @neo-opus-ada referenced in commit `e8b852d` - "feat(memory-core): publish the WAL drain receipt beside provider activity (#16835)

An operator reading four cores of embedding attribution had no way to learn
that nothing was pending. Both halves of that question already existed:
drainWalOnce computes {pending, embedded, failed, cooling, outstanding} every
sweep and recordCycle retains it in a disposition receipt, while the provider
call is attributable in-process because the collection carries a Neo-side
embedding function and the write runs inside a provider-activity context.

The receipt reached nothing. getDisposition had zero production consumers,
drainDisposition.mjs was imported only by the two loops defining it and their
specs, and drainedClean appeared in no openapi surface. Orphaned substrate:
computed, retained, unreachable.

The logging made it worse rather than mitigating it. The cycle summary is
logged only when pending > 0 or segments were pruned, so a zero-pending cycle
emits nothing at all — the exact state worth alarming on is the silent one, and
silence is indistinguishable from idle-and-healthy. Even log parsing, which
this AC exists to make unnecessary, could not have answered it.

Two properties carry the change, and neither is plumbing.

Absence never renders as zero. A process that does not host the drain has an
UNKNOWN backlog, not an empty one, so counts stays null and the status says
why. Zero pending against live provider load IS the alarm condition, so a
defaulted zero would synthesise that alarm on every non-hosting process — the
most natural-looking default is the failure.

Comparability is stated rather than assumed. The receipt describes the last
cycle; providerActivity describes a lookback window. withinWindow reports
whether they may be divided at all. A single precomputed ratio would be more
convenient and false whenever the drain has been quiet.

Deliberately not a composed global number: the denominator is distributed
across the MC WAL, KB resume state, and separate processes. Each process
reports its own.

The declared key set records the new field rather than being loosened — it is
a contract surface, and it caught the addition, which is what it is for.

Red-proved: returning a zeroed counts object on the absent branch fails with
"an unknown backlog must never read as an empty one"; the hosted and
throwing-receipt controls stay green under that mutation.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>"
- 2026-08-09T20:23:03Z @neo-opus-ada cross-referenced by PR #16836
- 2026-08-09T20:46:19Z @neo-opus-ada referenced in commit `81b6795` - "fix(memory-core): drop the null enum member that orphaned nullable (#16835)

CI red on the first head. The new state property declared `type: string` and
also listed a literal `null` inside its enum, which contradicts itself:
nullability is already carried by `nullable: true`. Standalone the schema
compiled fine, which is why it passed review-by-eye — the failure only appears
after the doc -> Zod -> JSON-Schema round-trip, where the converter drops the
type and leaves `nullable` behind, and AJV then rejects "nullable cannot be
used without type".

Two other specs went red with it and needed no change of their own: both
validate the composed schema, so an invalid document failed them downstream.

The process lesson is the larger one. I ran the two suites I EDITED and not the
specs that CONSUME what I edited — changing an openapi document should have
pulled in the openapi validation suite before the push, not after the red. The
full ai/ tree is what I ran this time, and a pre/post comparison isolates the
one failure that was mine: OpenApiValidatorCompliance fails on the pre-fix tree
and passes on this one, while seatCostReport fails identically on both and is
not attributable here.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>"
- 2026-08-09T22:18:19Z @neo-opus-ada referenced in commit `0ba712b` - "fix(memory-core): build the comparability instead of asserting it (#16835)

@neo-gpt requested changes on two defects and both were real.

P1: withinWindow was a false relationship. It proved only that the latest
completed cycle's timestamp fell inside the activity lookback; it never proved
that cycle produced the lookback's provider activity. He executed two ordinary
healthy sequences against the exact head and both defeated it. A cycle that had
selected one item and was still waiting on the provider reported totalInFlight 1
beside pending 0 — the tracker cannot speak until the call returns, so live work
read as no work. And a work-bearing cycle that embedded one item had its receipt
overwritten by the next idle poll while its activity was still inside the 60s
window, again reporting pending 0. Both licensed an operator to divide real
provider load by zero, which fails the parent ticket's own negative control
before any threshold exists.

The tracker is a last-value latch, and its own module prose says the disposition
must be sampled across a window. One latch cannot serve a current-cycle question
and a windowed question at once, so it now answers both separately: the drain
publishes what a cycle SELECTED before the provider call blocks, and completed
cycles are retained in a bounded ring the projection aggregates over the caller's
own sinceTs. window.truncated marks a lookback older than retained history,
because a partial aggregate presented as a total is the same false zero the
null-not-zero rule already refuses one level up.

P2: walDrain is declared required on the response and both early-return arms
omitted it. Exact-head execution showed disabledHasWalDrain false and
unavailableHasWalDrain false. The OpenAPI and parity checks validate the
DECLARATION, not the runtime branch, so a required field was absent while every
schema gate stayed green. Both arms now return the full shape through one shared
builder, and a test covers the arm rather than the declaration.

His two falsifiers are now regression tests, named for what they defeated.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>"
- 2026-08-10T07:34:37Z @neo-opus-ada referenced in commit `9e766a5` - "fix(memory-core): truncation is about coverage, not ring capacity (#16835)

A fresh tracker answered a lookback it could not see with cycles=1, every total
zero, truncated=false — a COMPLETE 'nothing happened' for a window it never
observed. Every process restart reaches that state, so it is the daily case.

The predicate was history.length === historyLimit && history[0].at > sinceTs.
The first conjunct made the flag exclusively about eviction. Losing the head of
a lookback by never having watched it is the same loss of coverage, and it was
the one nobody could see.

Now the tracker records the instant it began observing, remembers the newest
cycle the ring dropped, and truncates on either boundary. coverageStartedAt
reaches the wire too: truncated alone says an answer is partial without saying
which part, which a consumer cannot act on.

Found by @neo-gpt in the Cycle-2 review. The producer had NO spec of its own —
every test drove it through a stubbed provider, so the recorder proved it relays
whatever it is handed and a wrong producer passed all of them. The specs added
here drive the real tracker, with an eviction control and a fully-covered
negative control so 'always partial' cannot satisfy them."
- 2026-08-10T08:04:54Z @neo-opus-ada referenced in commit `14b2a3c` - "fix(memory-core): a failed cycle is a hole in the window, not an absence of work (#16835)

The third way to lose coverage, and the one that survived two repairs.

A cycle can START, reach the provider — durably, on the activity ledger — then
throw during post-add verification, pending re-read, marker append or prune;
startDrainLoop routes every such throw to recordFailure. It cleared inProgress
and pushed nothing, so the cycle never became a history entry and the window
aggregated straight past the hole: providerActivity 1 against window totals 0,
with truncated:false advertising that interval as a complete denominator.

Found by @neo-gpt in Cycle 3, EXECUTED against the exact-head tracker with the
real providerActivityLedger blob rather than read off the source. My two earlier
repairs closed restart and eviction and left this one, which is the harder shape:
coverage lost by an event INSIDE the window rather than at its edge.

The failed cycle is deliberately not a history entry — its counts are unknowable,
which is what failed means. Only the newest failure timestamp is kept, because
the question a lookback asks is 'did any failure land at or after sinceTs' and
the newest answers it for every sinceTs without an unbounded list.

Red-proofed: disabling the conjunct reddens the new witness while the
outside-the-lookback control stays green — 1 failed, 15 passed, 16 total, nothing
skipped. That control is the bound: a failure older than the lookback is not this
interval's gap, and truncating on it would make any plane that ever failed
permanently unattestable."
- 2026-08-10T08:22:13Z @tobiu referenced in commit `70d45b1` - "feat(memory-core): publish the WAL drain receipt beside provider activity (#16835) (#16836)

* feat(memory-core): publish the WAL drain receipt beside provider activity (#16835)

An operator reading four cores of embedding attribution had no way to learn
that nothing was pending. Both halves of that question already existed:
drainWalOnce computes {pending, embedded, failed, cooling, outstanding} every
sweep and recordCycle retains it in a disposition receipt, while the provider
call is attributable in-process because the collection carries a Neo-side
embedding function and the write runs inside a provider-activity context.

The receipt reached nothing. getDisposition had zero production consumers,
drainDisposition.mjs was imported only by the two loops defining it and their
specs, and drainedClean appeared in no openapi surface. Orphaned substrate:
computed, retained, unreachable.

The logging made it worse rather than mitigating it. The cycle summary is
logged only when pending > 0 or segments were pruned, so a zero-pending cycle
emits nothing at all — the exact state worth alarming on is the silent one, and
silence is indistinguishable from idle-and-healthy. Even log parsing, which
this AC exists to make unnecessary, could not have answered it.

Two properties carry the change, and neither is plumbing.

Absence never renders as zero. A process that does not host the drain has an
UNKNOWN backlog, not an empty one, so counts stays null and the status says
why. Zero pending against live provider load IS the alarm condition, so a
defaulted zero would synthesise that alarm on every non-hosting process — the
most natural-looking default is the failure.

Comparability is stated rather than assumed. The receipt describes the last
cycle; providerActivity describes a lookback window. withinWindow reports
whether they may be divided at all. A single precomputed ratio would be more
convenient and false whenever the drain has been quiet.

Deliberately not a composed global number: the denominator is distributed
across the MC WAL, KB resume state, and separate processes. Each process
reports its own.

The declared key set records the new field rather than being loosened — it is
a contract surface, and it caught the addition, which is what it is for.

Red-proved: returning a zeroed counts object on the absent branch fails with
"an unknown backlog must never read as an empty one"; the hosted and
throwing-receipt controls stay green under that mutation.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>

* fix(memory-core): drop the null enum member that orphaned nullable (#16835)

CI red on the first head. The new state property declared `type: string` and
also listed a literal `null` inside its enum, which contradicts itself:
nullability is already carried by `nullable: true`. Standalone the schema
compiled fine, which is why it passed review-by-eye — the failure only appears
after the doc -> Zod -> JSON-Schema round-trip, where the converter drops the
type and leaves `nullable` behind, and AJV then rejects "nullable cannot be
used without type".

Two other specs went red with it and needed no change of their own: both
validate the composed schema, so an invalid document failed them downstream.

The process lesson is the larger one. I ran the two suites I EDITED and not the
specs that CONSUME what I edited — changing an openapi document should have
pulled in the openapi validation suite before the push, not after the red. The
full ai/ tree is what I ran this time, and a pre/post comparison isolates the
one failure that was mine: OpenApiValidatorCompliance fails on the pre-fix tree
and passes on this one, while seatCostReport fails identically on both and is
not attributable here.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>

* fix(memory-core): build the comparability instead of asserting it (#16835)

@neo-gpt requested changes on two defects and both were real.

P1: withinWindow was a false relationship. It proved only that the latest
completed cycle's timestamp fell inside the activity lookback; it never proved
that cycle produced the lookback's provider activity. He executed two ordinary
healthy sequences against the exact head and both defeated it. A cycle that had
selected one item and was still waiting on the provider reported totalInFlight 1
beside pending 0 — the tracker cannot speak until the call returns, so live work
read as no work. And a work-bearing cycle that embedded one item had its receipt
overwritten by the next idle poll while its activity was still inside the 60s
window, again reporting pending 0. Both licensed an operator to divide real
provider load by zero, which fails the parent ticket's own negative control
before any threshold exists.

The tracker is a last-value latch, and its own module prose says the disposition
must be sampled across a window. One latch cannot serve a current-cycle question
and a windowed question at once, so it now answers both separately: the drain
publishes what a cycle SELECTED before the provider call blocks, and completed
cycles are retained in a bounded ring the projection aggregates over the caller's
own sinceTs. window.truncated marks a lookback older than retained history,
because a partial aggregate presented as a total is the same false zero the
null-not-zero rule already refuses one level up.

P2: walDrain is declared required on the response and both early-return arms
omitted it. Exact-head execution showed disabledHasWalDrain false and
unavailableHasWalDrain false. The OpenAPI and parity checks validate the
DECLARATION, not the runtime branch, so a required field was absent while every
schema gate stayed green. Both arms now return the full shape through one shared
builder, and a test covers the arm rather than the declaration.

His two falsifiers are now regression tests, named for what they defeated.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>

* fix(memory-core): truncation is about coverage, not ring capacity (#16835)

A fresh tracker answered a lookback it could not see with cycles=1, every total
zero, truncated=false — a COMPLETE 'nothing happened' for a window it never
observed. Every process restart reaches that state, so it is the daily case.

The predicate was history.length === historyLimit && history[0].at > sinceTs.
The first conjunct made the flag exclusively about eviction. Losing the head of
a lookback by never having watched it is the same loss of coverage, and it was
the one nobody could see.

Now the tracker records the instant it began observing, remembers the newest
cycle the ring dropped, and truncates on either boundary. coverageStartedAt
reaches the wire too: truncated alone says an answer is partial without saying
which part, which a consumer cannot act on.

Found by @neo-gpt in the Cycle-2 review. The producer had NO spec of its own —
every test drove it through a stubbed provider, so the recorder proved it relays
whatever it is handed and a wrong producer passed all of them. The specs added
here drive the real tracker, with an eviction control and a fully-covered
negative control so 'always partial' cannot satisfy them.

* fix(memory-core): a failed cycle is a hole in the window, not an absence of work (#16835)

The third way to lose coverage, and the one that survived two repairs.

A cycle can START, reach the provider — durably, on the activity ledger — then
throw during post-add verification, pending re-read, marker append or prune;
startDrainLoop routes every such throw to recordFailure. It cleared inProgress
and pushed nothing, so the cycle never became a history entry and the window
aggregated straight past the hole: providerActivity 1 against window totals 0,
with truncated:false advertising that interval as a complete denominator.

Found by @neo-gpt in Cycle 3, EXECUTED against the exact-head tracker with the
real providerActivityLedger blob rather than read off the source. My two earlier
repairs closed restart and eviction and left this one, which is the harder shape:
coverage lost by an event INSIDE the window rather than at its edge.

The failed cycle is deliberately not a history entry — its counts are unknowable,
which is what failed means. Only the newest failure timestamp is kept, because
the question a lookback asks is 'did any failure land at or after sinceTs' and
the newest answers it for every sinceTs without an unbounded list.

Red-proofed: disabling the conjunct reddens the new witness while the
outside-the-lookback control stays green — 1 failed, 15 passed, 16 total, nothing
skipped. That control is the bound: a failure older than the lookback is not this
interval's gap, and truncating on it would make any plane that ever failed
permanently unattestable."
- 2026-08-10T08:22:13Z @tobiu closed this issue

