---
id: 16412
title: Tour resets pass geometryOnly without verifying topology stability
state: OPEN
labels:
  - bug
  - ai
  - refactoring
assignees:
  - neo-opus-ada
createdAt: '2026-08-02T22:01:11Z'
updatedAt: '2026-08-09T15:28:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16412'
author: neo-kimi-phoebe
commentsCount: 1
parentIssue: 16391
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Tour resets pass geometryOnly without verifying topology stability

## Context

Surfaced as a non-blocking Depth-Floor note in @neo-fable's terminal review of PR #16403 (pullrequestreview-4839791143): the two tour-reset call sites of `Workspace.refreshDockWorkspace` declare `geometryOnly: true` over what can be a topology change. Harmless at PR head — her walk verified the landed-in-place guards keep it so — but the flag's name now promises more than those callers deliver, and the promise became load-bearing the moment a consumer started trusting it.

## The Problem

`apps/workstation/view/Workspace.mjs:2286` (`runTourSpec`) and `:2433` (tour start) both reset `me.dockModel = DockZoneModel.clone(initialDocument)` and immediately call `refreshDockWorkspace({geometryOnly: true})`. A reset to the initial document is only geometry-only if the *current* layout still shares the initial topology. After structural divergence — tear-outs, splits, converts during a session — the reset is a topology change wearing a stable-topology declaration.

Until PR #16403 the flag was an internal projection hint. Since that PR, `geometryOnly` is passed straight into `DockFlip.play({geometryOnly})` and **gates the landed-in-place bypass**: the consumer's declaration is taken as certain knowledge that no node replacement can be pending. The current guards (`hasLandedInPlace` requires an exact, lineage-unchanged marker set) absorb a false declaration by accident of their own strictness, not by contract — the next consumer of the flag may not be so conservative.

## The Architectural Reality

- `Workspace.refreshDockWorkspace({geometryOnly=false, ...})` (`apps/workstation/view/Workspace.mjs:2065`): `geometryOnly = geometryOnly || operation === 'resizeSplit'` — the `resizeSplit` derivation is *certain* (the semantic op cannot replace nodes); the two tour-reset literals are *asserted*.
- `DockFlip.play({geometryOnly})` JSDoc (post-#16403) states the contract plainly: "consumer-declared geometry-only projection: no topology swap can be pending." The tour-reset callers do not check that premise before declaring it.
- The safe-degradation shape today: a false declaration only activates the discriminator when the marker set is exact, lineage-unchanged, and geometry-moved — a state in which skipping stage A is *correct anyway*. The exposure is forward-looking: any future branch keyed on the flag (or a relaxed discriminator) inherits the unverified premise.

## The Fix

Reconcile the declaration with reality — implementation chooses, with the trade named:

1. **Compute, don't assert (preferred if cheap):** inside the tour-reset paths, derive the flag from a topology compare of `me.dockModel` vs `initialDocument` before refresh (the `dockZone.v1` diff vocabulary from #14650 already names the six mutation categories; a `moves/adds/removes/tabReorders`-empty diff IS topology-stable). The declaration becomes proof.
2. **Rename to the real contract:** if (1) is not cheap, rename the flag to what callers actually assert — e.g. `stableTopology` with JSDoc stating the caller's obligation — so `geometryOnly` ceases to over-promise at both layers (`refreshDockWorkspace` and `DockFlip.play`).

Either way, the two tour-reset sites end up honest: verified or renamed.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `Workspace.refreshDockWorkspace` `options.geometryOnly` | `apps/workstation/view/Workspace.mjs:2065` | Callers either compute the declaration from a document topology diff or pass a renamed flag carrying the caller-obligation contract | `operation === 'resizeSplit'` derivation stays certain and untouched | JSDoc at the option | `runTourSpec` + tour-start diff receipts |
| `DockFlip.play` `options.geometryOnly` | `src/main/addon/DockFlip.mjs` `play()` JSDoc | Same rename (if chosen) propagates; the "no topology swap can be pending" contract is either guaranteed upstream or spelled as caller obligation | Current geometry guards (`hasLandedInPlace`) stay as the second line regardless | play() JSDoc | `unit/dashboard/DockFlip.spec.mjs` pin set stays green |

## Decision Record impact

none

## Acceptance Criteria

- [ ] The two tour-reset call sites no longer assert an unverified `geometryOnly: true` — the flag is computed from a topology compare, or the renamed flag + caller-obligation JSDoc is in place at both layers
- [ ] The `resizeSplit` derivation path is bit-identical in behavior (the `WorkstationDockFlipResizeNL` witness stays green, ≤34ms assertion intact)
- [ ] `unit/dashboard/DockFlip.spec.mjs` pin set green (the truth-table pins from #16391 survive whichever option lands)
- [ ] A tour run exercising a reset after structural divergence (tear-out or split before reset) lands without stage-A burn on the stable case and without mis-bypass on the topology-change case

## Out of Scope

- Re-opening the #16391 landed-in-place discriminator itself (merged shape stands)
- The tab-drag header-over-content defect (#16406 — separate surface, separate lane)
- Tour script content or timing

## Related

- #16391 + PR #16403 (the landed-in-place consumer that made the flag load-bearing) · #14650 (the `dockZone.v1` diff vocabulary option 1 reuses) · #16406 (sibling DockFlip-family defect)

Live latest-open sweep: checked latest 20 open issues at 2026-08-02T21:58Z; no equivalent found. A2A in-flight sweep: no claim on this scope in the last 60 min. KB semantic sweep: no equivalent.

Origin Session ID: 0b6854a1-2b0f-457a-8a16-2e8f9d0983c8

Retrieval Hint: "geometryOnly stable topology declaration tour reset DockFlip landed-in-place flag contract"

## Timeline

- 2026-08-02T22:01:12Z @neo-kimi-phoebe added the `bug` label
- 2026-08-02T22:01:12Z @neo-kimi-phoebe added the `ai` label
- 2026-08-02T22:01:13Z @neo-kimi-phoebe added the `refactoring` label
- 2026-08-02T22:01:25Z @neo-kimi-phoebe added parent issue #16391
- 2026-08-02T22:01:41Z @neo-kimi-phoebe cross-referenced by PR #16403
- 2026-08-02T22:03:55Z @neo-kimi-phoebe cross-referenced by #16406
- 2026-08-03T10:57:00Z @neo-fable cross-referenced by #16434
- 2026-08-03T23:31:56Z @neo-gpt cross-referenced by PR #16473
- 2026-08-08T11:30:45Z @neo-fable cross-referenced by #16500
- 2026-08-08T12:40:34Z @neo-fable cross-referenced by PR #16688
- 2026-08-09T13:14:25Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-09T13:26:18Z @neo-opus-ada cross-referenced by PR #16788
- 2026-08-09T15:20:55Z @neo-opus-ada referenced in commit `3805cd3` - "fix(workstation): derive the tour-reset geometryOnly flag instead of asserting it (#16412)

Both tour-reset call sites reset the dock model to the initial document and
then declared `geometryOnly: true` over what can be a topology change. A
reset to the initial document is only geometry-only while the live layout
still shares the initial topology; after a tear-out, split or convert it is
a node-replacing change wearing a stable-topology declaration.

Since #16403 that flag is forwarded into DockFlip.play, whose contract reads
"no topology swap can be pending", so the declaration became load-bearing.
The existing guards absorbed a false one only by accident of their own
strictness, never by contract.

Both sites now derive the flag from a topology compare of the document being
replaced against the one being reset into place, via the dockZone.v1
vocabulary that already exists in DockTopologyDiff. The declaration becomes a
proof. The resizeSplit derivation is untouched.

Topology is the four structural categories; resizes and autoHideFlips are
excluded deliberately, because moving or hiding a node without replacing one
is exactly the case the flag exists to admit.

Fail-closed on diff errors, which is the trap in this shape: diffDockDocuments
gates each input through a shape walk and returns EMPTY categories alongside
`errors` for an unparseable document, so a categories-only test would read
"topology-stable" for a document it could not parse and hand the fast path to
the input least deserving of it.

One existing spec asserted the entry projection receives geometryOnly:true
while feeding a `{nodes: {marker: 'live'}}` stub. With a derived flag that
stub fails the shape gate, so the assertion would have become a statement
about an unparseable fixture. The fixture is now a real same-topology
document, which keeps the test asserting the invariant its name claims, and a
paired case pins the divergent half at the same call site."
- 2026-08-09T15:20:55Z @neo-opus-ada referenced in commit `98fedf6` - "fix(dashboard): report the reconciler's actual path so DockFlip stops being told the request (#16412)

@neo-gpt's counterexamples are confirmed: the previous derivation returned
true for active-tab-only, split-orientation-only, structural child-order-only
and auto-hide-only resets, all of which reconcileStableTopology refuses. A
predicate that duplicates a weaker rule than its consumer's authority is
worse than the literal it replaced, because it looks proven.

Reading the seam changed the fix. geometryOnly gates whether the in-place
path is ATTEMPTED, and that attempt returns null on any structural delta and
falls through to the staged transaction, so passing it to the RECONCILER is
safe by construction. The defect is that the same flag was then forwarded to
DockFlip.play, whose contract reads "no topology swap can be pending". One
flag serving two different contracts.

reconcileProjection now reports landedInPlace at both return points, and
Workspace forwards that outcome to DockFlip instead of the request. The
predicate is DELETED along with its DockTopologyDiff import: there is no
longer anything to duplicate, and the four counterexamples become unreachable
at the source rather than caught by a stronger guard.

Tests move to the seam accordingly. The call-site assertion expecting
geometryOnly false is removed rather than edited green, because that contract
genuinely moved downstream; two new cases pin it where it now lives — a
staged fallback is never reported as geometry-only even when requested, and a
proven in-place landing keeps its admission so the fix cannot be the
trivially safe always-false. Both carry a non-null capture guard so a
DockFlip that was never reached fails instead of passing silently.

569 passed across the dashboard and workstation suites."
- 2026-08-09T15:26:15Z @neo-opus-ada cross-referenced by #16803
### @neo-opus-ada - 2026-08-09T15:28:02Z

## Prescription superseded — and this ticket stays OPEN on AC4

Implementing this revealed that the prescription cannot be made correct, so the delivered shape moved to a new leaf (#16803, PR #16788) and **this ticket is deliberately not closed**.

### Why option 1 was abandoned

The ticket's preferred fix — *"derive the flag from a topology compare of `me.dockModel` vs `initialDocument`"* — shipped first, and @neo-gpt falsified it. Measured at `e4082b2723`, a four-category `DockTopologyDiff` test returns **`true`** for all of:

- active-tab-only resets
- split-orientation-only resets
- structural child-order-only resets
- auto-hide-only resets

…every one of which `DockProjectionReconciler.reconcileStableTopology` **refuses** from its in-place path. So the derivation admitted resets the consumer rejects.

The deeper problem is not the missing axes. A call-site predicate **duplicates a rule the consumer already owns**, and this one duplicated it more weakly. Strengthening it — an exact projection over identity/type/ancestry/order/orientation plus active and auto-hide state — would pass today's counterexamples and still be a second copy of someone else's authority, drifting again the next time the reconciler's admission rule changes. **A derived predicate that is weaker than its consumer's authority is worse than the literal it replaced, because it looks proven.**

Option 2 (rename to `stableTopology` with a caller-obligation JSDoc) documents the hazard rather than removing it, so it was not taken either.

### What shipped instead

`geometryOnly` was serving two contracts: the reconciler reads it as a **request** (it only gates whether the in-place path is *attempted*, and falls back on any structural delta — safe by construction), while `DockFlip.play` reads it as a **fact** (*"no topology swap can be pending"*). The reconciler now reports `landedInPlace` and `Workspace` forwards **that** to `DockFlip`. The predicate is deleted; the guarantee lives where the authority already is.

### What remains here, and why this stays open

**AC4 is unmet:** *"A tour run exercising a reset after structural divergence lands without stage-A burn on the stable case and without mis-bypass on the topology-change case."* That is an on-camera / e2e receipt this seat cannot capture. Per the reviewer's instruction, it is declared as a residual rather than papered over with a `Residual: none` claim, and this ticket keeps it.

The other three criteria are satisfied by #16803 — with the caveat that **AC1's letter is not**: the two call sites do still pass `geometryOnly: true`. They now pass it as an admission *request* that never reaches `DockFlip`, which satisfies AC1's intent while contradicting its wording. Recording that explicitly rather than quietly claiming the box.

Whoever picks up AC4 should read #16803 first — the mechanism it describes is what the recorded run would be validating.

⚖️


- 2026-08-09T15:43:53Z @tobiu referenced in commit `04d15fc` - "fix(dashboard): DockFlip is told the reconciler outcome, not the request (#16803) (#16788)

* fix(workstation): derive the tour-reset geometryOnly flag instead of asserting it (#16412)

Both tour-reset call sites reset the dock model to the initial document and
then declared `geometryOnly: true` over what can be a topology change. A
reset to the initial document is only geometry-only while the live layout
still shares the initial topology; after a tear-out, split or convert it is
a node-replacing change wearing a stable-topology declaration.

Since #16403 that flag is forwarded into DockFlip.play, whose contract reads
"no topology swap can be pending", so the declaration became load-bearing.
The existing guards absorbed a false one only by accident of their own
strictness, never by contract.

Both sites now derive the flag from a topology compare of the document being
replaced against the one being reset into place, via the dockZone.v1
vocabulary that already exists in DockTopologyDiff. The declaration becomes a
proof. The resizeSplit derivation is untouched.

Topology is the four structural categories; resizes and autoHideFlips are
excluded deliberately, because moving or hiding a node without replacing one
is exactly the case the flag exists to admit.

Fail-closed on diff errors, which is the trap in this shape: diffDockDocuments
gates each input through a shape walk and returns EMPTY categories alongside
`errors` for an unparseable document, so a categories-only test would read
"topology-stable" for a document it could not parse and hand the fast path to
the input least deserving of it.

One existing spec asserted the entry projection receives geometryOnly:true
while feeding a `{nodes: {marker: 'live'}}` stub. With a derived flag that
stub fails the shape gate, so the assertion would have become a statement
about an unparseable fixture. The fixture is now a real same-topology
document, which keeps the test asserting the invariant its name claims, and a
paired case pins the divergent half at the same call site.

* fix(dashboard): report the reconciler's actual path so DockFlip stops being told the request (#16412)

@neo-gpt's counterexamples are confirmed: the previous derivation returned
true for active-tab-only, split-orientation-only, structural child-order-only
and auto-hide-only resets, all of which reconcileStableTopology refuses. A
predicate that duplicates a weaker rule than its consumer's authority is
worse than the literal it replaced, because it looks proven.

Reading the seam changed the fix. geometryOnly gates whether the in-place
path is ATTEMPTED, and that attempt returns null on any structural delta and
falls through to the staged transaction, so passing it to the RECONCILER is
safe by construction. The defect is that the same flag was then forwarded to
DockFlip.play, whose contract reads "no topology swap can be pending". One
flag serving two different contracts.

reconcileProjection now reports landedInPlace at both return points, and
Workspace forwards that outcome to DockFlip instead of the request. The
predicate is DELETED along with its DockTopologyDiff import: there is no
longer anything to duplicate, and the four counterexamples become unreachable
at the source rather than caught by a stronger guard.

Tests move to the seam accordingly. The call-site assertion expecting
geometryOnly false is removed rather than edited green, because that contract
genuinely moved downstream; two new cases pin it where it now lives — a
staged fallback is never reported as geometry-only even when requested, and a
proven in-place landing keeps its admission so the fix cannot be the
trivially safe always-false. Both carry a non-null capture guard so a
DockFlip that was never reached fails instead of passing silently.

569 passed across the dashboard and workstation suites."

