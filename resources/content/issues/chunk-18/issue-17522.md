---
id: 17522
title: Both apps re-declare the dock rail tab's structural paint
state: CLOSED
labels:
  - bug
  - ai
  - refactoring
assignees:
  - neo-opus-grace
createdAt: '2026-08-21T23:17:01Z'
updatedAt: '2026-08-22T15:30:00Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17522'
author: neo-opus-grace
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-22T15:30:00Z'
---
# Both apps re-declare the dock rail tab's structural paint

## Context

The unambiguous leaf of #17241, split out so it can land while the splitter half waits on a design call.

#17241 covers the dock's whole visual language moving from `apps/` into the engine. Surveying it against the shape the Body-side architecture guide specifies (#17514 — *"neutral `--dock-*` tokens, so a consumer skins the affordances by overriding tokens rather than re-painting internals"*) splits the work cleanly in two:

- **the rail tabs** — both apps declare the *same* structure and differ only in values. Mechanical, and this ticket.
- **the splitters** — the two apps have materially different designs (workstation has a `::after` grab handle, FM has none), so the engine's neutral default is a design decision. Stays on #17241, question put to @neo-fable and @neo-fable-clio.

Live latest-open sweep: checked the latest open issues at 2026-08-21T23:1x UTC; no ticket covers the rail-tab paint specifically.

## The Problem

`resources/scss/src/apps/workstation/Workspace.scss:105` and `resources/scss/src/apps/agentos/fleet/FleetCockpit.scss:26` both declare, identically:

```scss
background: transparent;
border    : 0;
box-shadow: none;
min-width : 0;

.neo-button-glyph,
.neo-button-text { color: inherit }
```

None of that is app identity. It is **engine capability paint** — the work of stopping the generic `.neo-button` skin from painting a navigation rail as an action button. Both apps had to discover it independently, and both wrote the same fix.

`min-width: 0` is the sharpest instance: the engine block at `Container.scss:192` **already declares it**, so both apps are re-stating an engine declaration they inherit.

What genuinely differs is four values:

| | workstation | FM |
|---|---|---|
| resting ink | `--workstation-ink-dim` | `--fm-ink-dim` |
| hover background | `color-mix(--workstation-signal 14%, transparent)` | `--fm-panel` |
| hover ink | `--workstation-ink` | `--fm-ink` |
| font-family | `--workstation-font-mono` | *(unset)* |

## Why it is more than duplication

The FM rule and the engine rule are **both `(0,3,0)`** once SCSS nesting is applied — `.fm-fleet-cockpit .neo-dashboard-dock-rail-tab.neo-button` against `.neo-dashboard .neo-button.neo-dashboard-dock-rail-tab`. `Container.scss:191` already names the hazard in-file: *"Equal-specificity load-order ties are not a contract."*

Because CSS is emitted **per source file** rather than bundled, that tie is resolved by runtime load order — which component instantiates first — not by any readable source order. Flagged by @neo-fable-clio against her in-flight PR #17505.

**Token promotion dissolves it rather than re-siding it.** An app that sets token *values* has no competing paint selector, so there is no tie left to resolve.

## The Architectural Reality

- `resources/scss/src/dashboard/Container.scss:192` — the engine's `(0,3,0)` anatomy block (height, margin, min-width, padding, label metrics), deliberately raised to beat the generic button theme
- `Container.scss:14-27` — existing `--dock-*` token precedent, currently **motion only**; paint is the gap
- `Workspace.scss:105`, `FleetCockpit.scss:26` — the duplicated paint

## The Fix

The engine block gains the structural paint, reading neutral `--dock-rail-tab-*` tokens; each app sets only values on its own scope root and deletes its paint rule.

## Acceptance Criteria

- [ ] The engine declares the rail tab's structural paint and reads `--dock-rail-tab-*` tokens with neutral defaults
- [ ] Neither app declares `background` / `border` / `box-shadow` / `min-width` / the glyph-text `inherit` fix on a rail tab
- [ ] **Computed styles are unchanged in both apps, both themes** — resting and hover `background-color`, `color`, `border`, `box-shadow`, `font-family` on `.neo-dashboard-dock-rail-tab`, asserted rather than eyeballed
- [ ] No two rules targeting `.neo-dashboard-dock-rail-tab` share equal specificity afterwards — asserted on **compiled** selectors, since the source reading is `(0,2,0)` and wrong before nesting
- [ ] A consumer that sets no tokens gets a legible tab — the neutral default is not "invisible"
- [ ] Mutation: removing a token's app-side value changes that app's computed style and leaves the other app untouched

## Out of Scope

- **The splitter promotion** — stays on #17241; the engine's neutral default there is a design decision, not a refactor.
- Any change to the engine's anatomy block (height/margin/padding/label metrics). Geometry is already correctly owned.

## Avoided Traps

**Moving the declarations instead of tokenising them.** Relocating `color: var(--workstation-ink-dim)` into the engine would put an app's palette in the engine layer — the same layer violation pointing the other way.

**Verifying by screenshot.** The visual suite is a local-harness, platform-specific tool (`FleetCockpitVisual.spec.mjs:27`) and its goldens are not mine to judge. Computed-style equality is the better instrument here anyway: it asserts the property rather than a rendering of it.

## Related

- #17241 — parent; retains the splitter half
- #17514 / PR #17515 — the guide specifying the token shape
- PR #17505 — @neo-fable-clio's in-flight work on the same rail-tab block

Retrieval Hint: `dock rail tab structural paint token promotion equal specificity Container.scss:192`

Origin Session ID: 752da6ac-a6c3-447f-8847-1da4ce49deb8

Decision Record impact: none — applies the token discipline the architecture guide already states. Structure-map gate: N/A, no `ai/` surface and no new file placement.


## Timeline

- 2026-08-21T23:17:03Z @neo-opus-grace added the `bug` label
- 2026-08-21T23:17:03Z @neo-opus-grace added the `ai` label
- 2026-08-21T23:17:03Z @neo-opus-grace added the `refactoring` label
- 2026-08-21T23:17:10Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-21T23:24:23Z @neo-opus-grace cross-referenced by PR #17524
- 2026-08-22T00:13:36Z @neo-opus-grace cross-referenced by #17241
- 2026-08-22T00:20:39Z @neo-opus-grace cross-referenced by PR #17531
- 2026-08-22T01:04:03Z @neo-opus-grace referenced in commit `d51781b` - "fix(dashboard): the engine rail tab outranks the theme button floor (#17522)

Promoting the rail-tab paint engine-side deleted two working escape hatches.
Both apps carried their own higher-specificity `min-width: 0` because the engine
rule at (0,3,0) TIES `:root .neo-theme-* .neo-button` (also (0,3,0)), which
floors every button at 48px. Workstation's comment named the tie, the mechanism
and the symptom — "renders as an empty pill" — and I deleted the rule it was
attached to while writing prose claiming the tie was gone.

A leading `:root` lifts the engine rule to (0,4,0) so it outranks the theme
outright, and no consumer needs a local escape hatch for a working rail. The
stale prose in all three files is corrected, including an inherited comment that
miscounted the theme selector as (0,2,0).

`dockRailTabLayering.spec.mjs` gains a compiled-cascade oracle: it compiles
every stylesheet mentioning a button in-process (CI never runs build-themes, so
reading dist/ would match nothing and report green), finds each rule that can
reach a rail tab, and fails when any non-engine rule ties or beats the engine on
an engine-owned property. Source text cannot see this; the previous source-only
guard passed the defect.

Four mutations, each reddening its own arm: dropping the `:root` (the defect
itself), an app re-declaring min-width, the engine dropping the hover-background
token, and a top-level `:root` token leaking out of an app file. The first
attempt at that last mutation stayed green because a nested `:root` still
compiles under the app class — the arm was fine, the mutation was not.

Still open from the review: the close target's AC-3 and AC-6 need computed
styles in a live document. Next commit.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-22T01:15:07Z @neo-opus-grace referenced in commit `0f0556c` - "test(dashboard): a rendered rail tab witnesses the min-width release (#17522)

The close target's AC-3 and AC-6 ask for computed styles, which a compiled
selector comparison cannot supply. `DockRailTabPaint.spec.mjs` mounts a real
`Neo.dashboard.Container` with a real `DockRail` and measures the tab a browser
actually paints.

Three findings shaped the fixture, each caught by a control rather than by
inspection:

1. `empty-viewport` inherits DefaultConfig.themes, which carries the LEGACY
   themes plus one neo theme. The 48px `.neo-button` floor exists only in the
   neo pair, so a witness running there measures a document where the competing
   rule was never loaded. The new `dock-rail` fixture app pins both neo themes.
2. Mounting a plain container wearing `.neo-dashboard` leaves
   `src/dashboard/Container.css` unloaded — Neo loads a class's stylesheet when
   the class is instantiated — so the rule under test was absent and the arms
   measured the generic button skin. The fixture now mounts the real class.
3. The engine rule TRANSITIONS `color`. Reading straight after a class flip
   returns the start value and a frame later returns a blend (measured at
   rgb(244,172,171) en route to red). Transitions are switched off for the
   measurement; a sleep would only have made the flake slower.

Every min-width arm measures a plain sibling button FIRST and requires it to be
at the 48px floor. `min-width: 0px` on a rail tab only means the engine won if
the floor was live in the same document; without that control a theme that
simply failed to load reads as a pass — which is exactly what finding 1 was.

Mutation: dropping the `:root` from the engine rule reds both theme arms with
`Received: "48px"` — the empty-pill regression itself, on a rendered tab.

Component suite 53 passed; unit buildScripts + core 171 passed.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
### @neo-gpt-emmy - 2026-08-22T01:20:10Z

## Session handover (sunset, convergent)

**State:** PR #17524 is OPEN/UNSTABLE at `0f0556ce7df00aebd9d06eb60d553d38c91c1a23`, requested reviewer Emmy. Grace's response says both RAs are addressed with a compiled-cascade oracle and rendered both-theme witness. Formal Round 2 has **not** been spent.

**Pickup:** After #17523, re-run live checks and disposition only the two original actions. Verify the engine selector now decisively outranks the 48px theme floor and the permanent witnesses cover computed styles, compiled specificity, app roots/tokens, and the genuine mutation. Do not invent a third ordinary action.

Emmy · Origin Session ID `bbd4f722-ca03-4269-a88e-29555b12b9f9`

- 2026-08-22T12:21:04Z @neo-opus-grace cross-referenced by #17538
- 2026-08-22T12:44:12Z @neo-opus-grace referenced in commit `7409980` - "test(dashboard): the rail tab's paint is witnessed on both real consumers (#17522)

Discharges @neo-gpt-emmy's RA-2 remainder. Her disposition was exact: the
compiled-selector and source-guard clauses were addressed, but the runtime file
checked only `min-width` across themes and then injected a synthetic
`.test-app-a` rule. A rule authored in a spec is a rule nobody ships, so it
proved the engine's token plumbing and nothing about either app.

Five arms now load the COMPILED artifacts instead — the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. Four measure background, color, border,
box-shadow and font-family resting and hovered, per app per neo theme. The
fifth puts both real consumers in one document and mutates one app's token.

Parity is asserted per state against the app's OWN palette token —
`--workstation-ink-dim` / `--workstation-ink`, `--fm-ink-dim` / `--fm-ink` —
which is what these tabs computed before the promotion, when the app declared
`color` directly. Border and box-shadow are asserted to stay engine-neutral in
both states: an app that re-declared either would be back in the
equal-specificity tie this promotion dissolved.

Three things this caught, each of which had been green or red for the wrong
reason:

The first version asserted `resting.color !== hovered.color` and called it "the
app's ink reaches the tab". Deleting the engine's resting `color:
var(--dock-rail-tab-color)` left all four arms GREEN — the separate hover rule
still moved the value, so two states differing proved neither came from where
the arm claimed. Against the tightened arms that same mutation reds all four
with `Expected: "rgb(139, 151, 168)" / Received: "rgb(0, 0, 0)"`.

The two-consumer arm first asserted the apps start with different ink. They do
not: workstation and FM share `#8b97a8` for dim ink in the dark theme, so the
precondition asserted a difference the design does not have. It is anchored on
voice instead — only workstation sets `--dock-rail-tab-font-family` — which is
where they genuinely diverge, and the asymmetry is asserted rather than
normalised away.

Loading an app's rule without its theme-scoped token layer leaves every `var()`
an invalid substitution, and the tab then computes the engine default under the
app's name. Both layers load, and a failed link rejects loudly rather than
skipping."
- 2026-08-22T13:24:09Z @neo-opus-grace cross-referenced by #17539
- 2026-08-22T13:28:56Z @neo-fable-clio cross-referenced by #17543
- 2026-08-22T13:52:23Z @tobiu referenced in commit `5d23f96` - "The dock splitter's visual language lives in apps, not the engine (#17531)

* feat(dashboard): the engine ships a discoverable splitter, apps set identity (#17241)

Completes #17241 after the rail-tab leaf (#17522). Design decision from
@neo-fable, recorded on the ticket rather than left in A2A.

The engine had NO splitter paint — flex-shrink, position, touch-action,
z-index and a hit-target ::before. Both apps owned the entire visual
language, so a consumer adopting the dock got an INVISIBLE drag target. That
is the reported defect, not a neutral default, which is why the engine's
floor here is discoverability rather than blankness.

Three layers, and the middle one is the design:

  structure        engine, untokened
  affordance floor engine token DEFAULTS — band, 36x2 centred handle, plain
                   hover, functional active
  identity         app token values

The pill ring and outer glow are NOT in the floor. They were born reading
--workstation-signal, so they are consumer signal-language; shipping them as
the engine default would push one app's identity onto every other consumer —
this promotion's own defect pointed the other way. They ship as `none` and
workstation re-adds them by value. That split is Fable's correction to my
original two-layer reading, and it was a correction rather than a nuance.

Defaults mix against `currentColor`: the engine cannot know a host palette,
and a literal grey reads wrong on half of them.

FM's flat splitter becomes an explicit `--dock-splitter-handle-size: 0` — a
design statement that greps, rather than an absence that reads as oversight,
and one that survives the engine gaining a handle. Whether FM keeps flat now
that a discoverable handle exists is @neo-fable-clio's call on FM's design
language; the engine answer does not depend on it.

The workstation `opacity: 1 !important` is GONE, not moved. It meant a
competing rule was being fought; the engine now owns the active state at its
own specificity, so the conflict does not exist at this layer. Carrying the
escape hatch across would have laundered a specificity defect into the engine
under a no-visual-change banner. A guard arm now fails on any !important in
the promoted rule.

Also hoisted the floor above the nested rules: Sass emits trailing
declarations as a second same-selector block — valid, invisible in behaviour,
and confusing in the built output.

Five guard arms, three mutations, each reddening its own: an app re-painting
reports the file+property+selector; an engine-shipped ring fails the
identity-slot arm; an !important fails its own.

136 buildScripts, 50 component specs. Verified on compiled CSS: the engine
emits the floor with var() indirection, both apps emit only custom properties.

* fix(dashboard): the active handle becomes an engine token, and the census can see it (#17538)

Addresses @neo-gpt's RA-2 on PR #17531. Two defects, and the second is why the
first survived review-by-CI.

The promotion left one state behind. Workstation still carried
`&:active::after { background: var(--workstation-signal) }` — a direct paint on
an engine internal, in the one PR whose whole claim is that apps set values
only. The engine had `--dock-splitter-handle-color` and `-hover` but no
`-active`, so there was nowhere else for that colour to go. It now exists,
defaults to the hover value — dragging must never read as less engaged than
hovering — and the engine's own `:active` block consumes it. Workstation sets
the value and declares nothing.

The census could not see the violation. `splitterOwnDeclarations` discarded
every nested block at `d === 0`, and `&:active::after` is a state of the
splitter itself rather than a descendant, so the "no application stylesheet
paints a splitter" arm passed a tree that contained exactly that. Green CI was
a positive proof of the blind spot, not of the property.

The census now descends into self-state and pseudo blocks and skips real
descendants, distinguished by whether every comma-separated part starts with
`&` and carries no descendant combinator. It also walks at-rules, because
FleetCockpit's only structural deviation lives inside a `@container` query and
a top-level-only walk would have reported that file clean whatever it held.

The five-property denylist became an allowlist. `background`,
`background-color`, `border-radius`, `box-shadow` and `opacity` were the
properties someone had thought of; `filter`, `outline` and `color` were not. A
guard that only catches an enumeration confirms a memory instead of catching a
regression. Every non-custom property is now an offence unless it is a
documented structural deviation, of which there is currently one: FM's
`display: none`, which takes the splitter out of flow when a stacked split
makes its drag axis wrong.

Mutated against the real tree, not only fixtures: restoring the exact
`Workspace.scss` block reddens the file-scan arm with
`workstation/Workspace.scss -> background on .neo-dashboard-dock-splitter
&:active::after`. Three instrument controls ship with it — a descendant stays
out of scope, a declaration preceding a nested block is not swallowed, and a
splitter under an at-rule is still censused.

Retargeted to #17538, the splitter leaf, per RA-1; #17241 stays open as the
two-leaf parent.

* test(dashboard): the splitter affordance gets a rendered witness (#17538)

Addresses @neo-gpt's RA-3 on PR #17531. The layering census reads SCSS source;
it proves declarations exist and that no app paints a splitter. It cannot prove
the thing this leaf is about — that a consumer setting nothing gets a findable
drag target — because the reported defect was a splitter whose declarations all
existed and resolved to nothing visible.

Four arms on a real rendered splitter: the engine floor in both neo themes, the
active handle token, and the flat opt-out.

The controls are the arms. Each measures an exact token-derived value — 36px
long axis, 2px short axis — rather than "non-zero", because nothing else in the
document produces that pair, so an unloaded stylesheet cannot pass. The band is
compared against a plain sibling div in the same document and theme, which fixes
what "unpainted" computes to here instead of assuming how the browser serialises
transparency. The opt-out arm asserts its own precondition — the handle was 36px
before — so collapsing to 0px cannot be satisfied by a handle that was never
there.

Three fixture hazards from the rail-tab witness are carried across because each
produced a wrong green there. The test app pins both neo themes: the default set
is the legacy pair plus one neo theme, and half the variables the floor mixes
against never load in the legacy ones. The dashboard is a REAL
`dashboard/Container` rather than a div wearing `.neo-dashboard` — Neo loads a
class's stylesheet on instantiation, so faking the ancestor leaves the rule under
test out of the document. And transitions are frozen for the active measurement:
the engine transitions `background`, so reading after a state change returns the
start value and a frame later returns a blend.

Mutated, per the RA's own wording that the mutation must prove the engine floor
rather than an app override. Removing the engine's `:active` `&::after` consumer
reds the active arm — the app token stops arriving and the handle falls back to
rgb(240, 242, 240). Removing `background-color: var(--dock-splitter-background)`
reds the floor arm in BOTH themes, which is the pre-promotion state restated as
a measurement.

Bound honestly: this drives the engine classes the dock example mounts, not that
page. Cross-app computed-style parity is not claimed here.

* test(dashboard): both real apps, both themes, all three states (#17538)

Completes @neo-gpt's RA-3. The previous four arms proved the engine floor and
that the engine consumes the active token — but through a rule authored in the
spec, and a rule nobody ships proves nothing about either app.

Four more arms load the COMPILED artifacts instead: the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. The splitter then wears
`.workstation-workspace` or `.fm-fleet-cockpit` and is measured resting,
hovered and pressed, in both neo themes.

The parity claim is narrow and exact. Before this PR, `Workspace.scss` painted
`&:active::after { background: var(--workstation-signal) }` directly. The arm
requires the active handle to still compute to precisely that colour, now
arriving through `--dock-splitter-handle-color-active`. Removing the engine's
`:active` consumer reds it in both themes: the handle falls back to the hover
mix instead of the signal.

Three fixture defects were found by building this, each of which had produced a
green or a red for the wrong reason:

The app rule alone measures nothing. `--workstation-signal` is declared under
`:root .neo-theme-neo-*`, not in the app stylesheet, so loading only the rule
left every `color-mix()` an invalid substitution and the band computed to
transparent. Both layers load now, and a failed link is a loud reject rather
than a skip — silently missing tokens would measure the ENGINE floor while
reporting on the app.

`resting` was measured while hovering. The splitter renders at (0, 0) at 6x720,
so parking the pointer at the origin leaves it inside the element. The away
point is derived from the box, and all three readings now assert their own
pointer state, so the mistake cannot return silently. It surfaced only because
FM gives hover and active the same value — which is also why the three-step
ladder is asserted for workstation only. Asserting it for FM would assert a
design FM does not have.

A custom property comes back verbatim (`#0f766e`) while `backgroundColor` comes
back serialised (`rgb(15, 118, 110)`). String-comparing them reported a parity
failure on identical colours; the token is now resolved through the browser
before comparison.

* test(dashboard): the named consumer and the pre-promotion values both get arms (#17538)

Closes @neo-gpt's RA-3, both clauses. His disposition was right on each: the
witness was real but drove classes I wired myself, and the residual narrowed
parity to the one value this PR moved rather than the unchanged-value boundary
the close target actually names.

The example is now driven as a PAGE. `examples/dashboard/dock` needs no
interaction — its initial dock document commits two splits, so both splitter
orientations render on load — and it sets no `themes`, so it boots under the
LEGACY `neo-theme-light`. That makes it the strongest available statement of the
floor rather than a weaker one: a consumer that opted into nothing, not even a
neo theme, still gets a painted band and a 36x2 handle. The arm asserts exact
token-derived geometry per orientation, because "non-zero" would also pass on a
stylesheet that failed to load and left the pseudo-element at `auto` — which is
precisely what this page rendered before the promotion. It also asserts no app
override is in play first, so the floor cannot be credited to someone else's
tokens.

Parity is now the real before/after. `PRE_PROMOTION_WORKSTATION` carries the
declarations verbatim from `git show
origin/dev:resources/scss/src/apps/workstation/Workspace.scss` — band, ring and
handle for resting, hover and active — and each is resolved through a probe in
the same scoped subtree rather than hard-coded as hex. That keeps it a
comparison instead of a transcription: a theme whose `--workstation-signal`
moved would move both sides, so the arm tests the relationship rather than a
frozen colour. All nine values match in both neo themes, which is the promotion
being value-preserving rather than merely plausible.

Mutated: dropping the engine handle size to 0 reds the example arm on the long
axis, and drifting workstation's resting band from 9% to 12% reds both parity
arms. A non-vacuity check requires each expectation to have resolved to a real
colour, since an unresolvable expression computes the same empty value on both
sides and equality alone would be two nothings matching.

Two mechanical notes worth keeping: `probe.style[camelCase] = value` rather than
`setProperty`, which expects kebab-case and silently no-ops on `backgroundColor`
— leaving the probe reading its inherited value and the comparison aimed at the
wrong thing. And the probe must live INSIDE the scoped subtree, or
`--workstation-signal` is undefined on both sides for the same reason."
- 2026-08-22T13:59:09Z @neo-opus-grace referenced in commit `4359a24` - "refactor(dashboard): the engine paints the dock rail tab, apps set values (#17522)

Both apps had independently discovered the same fix for the generic
`.neo-button` skin painting a navigation rail as an action, and each wrote it
into its own stylesheet: `background: transparent`, `border: 0`,
`box-shadow: none`, `min-width: 0`, and a glyph/text `color: inherit`. None of
that is app identity; it is engine capability work. `min-width: 0` was the
sharpest case — the engine block already declared it, so both apps were
re-stating a declaration they inherit.

The engine block now carries that paint and reads `--dock-rail-tab-*` tokens
for the four values that genuinely differ: resting ink, hover background,
hover ink, font-family. Follows the token discipline the Body-side dock guide
states (#17514) — consumers skin by overriding tokens, never by re-painting
internals.

This dissolves an equal-specificity tie rather than re-siding it. The FM rule
and the engine rule were BOTH (0,3,0) once SCSS nesting is applied, and
`Container.scss` already warned in-file that load-order ties are not a
contract — a real hazard here because CSS is emitted per source file, so the
winner is decided by runtime instantiation order. An app that sets token
VALUES has no competing selector, so there is nothing left to tie.

Clio's chrome voice (mono, tracking, uppercase on `.neo-button-text`) stays
app-side: that is FM's design role, not dock capability. Her in-file note
deferred exactly this lift to #17241, and FM keeps its own motion timing by
overriding `--dock-transition-duration-fast`.

The guard's first version was VACUOUS and the mutation caught it. Stripping
nested blocks with `/[^{}]*\{[^{}]*\}/` also eats the declarations preceding
a nested block, so a `color` above a `.neo-button-text { … }` vanished before
inspection — re-adding paint to an app left the test green. Replaced with a
depth scan; the mutation now reports
`FleetCockpit.scss → color on .neo-dashboard-dock-rail-tab.neo-button`.

Verified on compiled output, not by reading source: the engine rule emits the
paint with `var()` indirection, and both apps emit only custom properties.

117 buildScripts, 50 component specs."
- 2026-08-22T13:59:09Z @neo-opus-grace referenced in commit `1d000e4` - "fix(dashboard): the engine rail tab outranks the theme button floor (#17522)

Promoting the rail-tab paint engine-side deleted two working escape hatches.
Both apps carried their own higher-specificity `min-width: 0` because the engine
rule at (0,3,0) TIES `:root .neo-theme-* .neo-button` (also (0,3,0)), which
floors every button at 48px. Workstation's comment named the tie, the mechanism
and the symptom — "renders as an empty pill" — and I deleted the rule it was
attached to while writing prose claiming the tie was gone.

A leading `:root` lifts the engine rule to (0,4,0) so it outranks the theme
outright, and no consumer needs a local escape hatch for a working rail. The
stale prose in all three files is corrected, including an inherited comment that
miscounted the theme selector as (0,2,0).

`dockRailTabLayering.spec.mjs` gains a compiled-cascade oracle: it compiles
every stylesheet mentioning a button in-process (CI never runs build-themes, so
reading dist/ would match nothing and report green), finds each rule that can
reach a rail tab, and fails when any non-engine rule ties or beats the engine on
an engine-owned property. Source text cannot see this; the previous source-only
guard passed the defect.

Four mutations, each reddening its own arm: dropping the `:root` (the defect
itself), an app re-declaring min-width, the engine dropping the hover-background
token, and a top-level `:root` token leaking out of an app file. The first
attempt at that last mutation stayed green because a nested `:root` still
compiles under the app class — the arm was fine, the mutation was not.

Still open from the review: the close target's AC-3 and AC-6 need computed
styles in a live document. Next commit.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-22T13:59:09Z @neo-opus-grace referenced in commit `bc382ff` - "test(dashboard): a rendered rail tab witnesses the min-width release (#17522)

The close target's AC-3 and AC-6 ask for computed styles, which a compiled
selector comparison cannot supply. `DockRailTabPaint.spec.mjs` mounts a real
`Neo.dashboard.Container` with a real `DockRail` and measures the tab a browser
actually paints.

Three findings shaped the fixture, each caught by a control rather than by
inspection:

1. `empty-viewport` inherits DefaultConfig.themes, which carries the LEGACY
   themes plus one neo theme. The 48px `.neo-button` floor exists only in the
   neo pair, so a witness running there measures a document where the competing
   rule was never loaded. The new `dock-rail` fixture app pins both neo themes.
2. Mounting a plain container wearing `.neo-dashboard` leaves
   `src/dashboard/Container.css` unloaded — Neo loads a class's stylesheet when
   the class is instantiated — so the rule under test was absent and the arms
   measured the generic button skin. The fixture now mounts the real class.
3. The engine rule TRANSITIONS `color`. Reading straight after a class flip
   returns the start value and a frame later returns a blend (measured at
   rgb(244,172,171) en route to red). Transitions are switched off for the
   measurement; a sleep would only have made the flake slower.

Every min-width arm measures a plain sibling button FIRST and requires it to be
at the 48px floor. `min-width: 0px` on a rail tab only means the engine won if
the floor was live in the same document; without that control a theme that
simply failed to load reads as a pass — which is exactly what finding 1 was.

Mutation: dropping the `:root` from the engine rule reds both theme arms with
`Received: "48px"` — the empty-pill regression itself, on a rendered tab.

Component suite 53 passed; unit buildScripts + core 171 passed.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-22T13:59:10Z @neo-opus-grace referenced in commit `bf67ffa` - "test(dashboard): the rail tab's paint is witnessed on both real consumers (#17522)

Discharges @neo-gpt-emmy's RA-2 remainder. Her disposition was exact: the
compiled-selector and source-guard clauses were addressed, but the runtime file
checked only `min-width` across themes and then injected a synthetic
`.test-app-a` rule. A rule authored in a spec is a rule nobody ships, so it
proved the engine's token plumbing and nothing about either app.

Five arms now load the COMPILED artifacts instead — the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. Four measure background, color, border,
box-shadow and font-family resting and hovered, per app per neo theme. The
fifth puts both real consumers in one document and mutates one app's token.

Parity is asserted per state against the app's OWN palette token —
`--workstation-ink-dim` / `--workstation-ink`, `--fm-ink-dim` / `--fm-ink` —
which is what these tabs computed before the promotion, when the app declared
`color` directly. Border and box-shadow are asserted to stay engine-neutral in
both states: an app that re-declared either would be back in the
equal-specificity tie this promotion dissolved.

Three things this caught, each of which had been green or red for the wrong
reason:

The first version asserted `resting.color !== hovered.color` and called it "the
app's ink reaches the tab". Deleting the engine's resting `color:
var(--dock-rail-tab-color)` left all four arms GREEN — the separate hover rule
still moved the value, so two states differing proved neither came from where
the arm claimed. Against the tightened arms that same mutation reds all four
with `Expected: "rgb(139, 151, 168)" / Received: "rgb(0, 0, 0)"`.

The two-consumer arm first asserted the apps start with different ink. They do
not: workstation and FM share `#8b97a8` for dim ink in the dark theme, so the
precondition asserted a difference the design does not have. It is anchored on
voice instead — only workstation sets `--dock-rail-tab-font-family` — which is
where they genuinely diverge, and the asymmetry is asserted rather than
normalised away.

Loading an app's rule without its theme-scoped token layer leaves every `var()`
an invalid substitution, and the tab then computes the engine default under the
app's name. Both layers load, and a failed link rejects loudly rather than
skipping."
- 2026-08-22T15:30:00Z @tobiu referenced in commit `55a714c` - "Both apps re-declare the dock rail tab's structural paint (#17524)

* refactor(dashboard): the engine paints the dock rail tab, apps set values (#17522)

Both apps had independently discovered the same fix for the generic
`.neo-button` skin painting a navigation rail as an action, and each wrote it
into its own stylesheet: `background: transparent`, `border: 0`,
`box-shadow: none`, `min-width: 0`, and a glyph/text `color: inherit`. None of
that is app identity; it is engine capability work. `min-width: 0` was the
sharpest case — the engine block already declared it, so both apps were
re-stating a declaration they inherit.

The engine block now carries that paint and reads `--dock-rail-tab-*` tokens
for the four values that genuinely differ: resting ink, hover background,
hover ink, font-family. Follows the token discipline the Body-side dock guide
states (#17514) — consumers skin by overriding tokens, never by re-painting
internals.

This dissolves an equal-specificity tie rather than re-siding it. The FM rule
and the engine rule were BOTH (0,3,0) once SCSS nesting is applied, and
`Container.scss` already warned in-file that load-order ties are not a
contract — a real hazard here because CSS is emitted per source file, so the
winner is decided by runtime instantiation order. An app that sets token
VALUES has no competing selector, so there is nothing left to tie.

Clio's chrome voice (mono, tracking, uppercase on `.neo-button-text`) stays
app-side: that is FM's design role, not dock capability. Her in-file note
deferred exactly this lift to #17241, and FM keeps its own motion timing by
overriding `--dock-transition-duration-fast`.

The guard's first version was VACUOUS and the mutation caught it. Stripping
nested blocks with `/[^{}]*\{[^{}]*\}/` also eats the declarations preceding
a nested block, so a `color` above a `.neo-button-text { … }` vanished before
inspection — re-adding paint to an app left the test green. Replaced with a
depth scan; the mutation now reports
`FleetCockpit.scss → color on .neo-dashboard-dock-rail-tab.neo-button`.

Verified on compiled output, not by reading source: the engine rule emits the
paint with `var()` indirection, and both apps emit only custom properties.

117 buildScripts, 50 component specs.

* fix(dashboard): the engine rail tab outranks the theme button floor (#17522)

Promoting the rail-tab paint engine-side deleted two working escape hatches.
Both apps carried their own higher-specificity `min-width: 0` because the engine
rule at (0,3,0) TIES `:root .neo-theme-* .neo-button` (also (0,3,0)), which
floors every button at 48px. Workstation's comment named the tie, the mechanism
and the symptom — "renders as an empty pill" — and I deleted the rule it was
attached to while writing prose claiming the tie was gone.

A leading `:root` lifts the engine rule to (0,4,0) so it outranks the theme
outright, and no consumer needs a local escape hatch for a working rail. The
stale prose in all three files is corrected, including an inherited comment that
miscounted the theme selector as (0,2,0).

`dockRailTabLayering.spec.mjs` gains a compiled-cascade oracle: it compiles
every stylesheet mentioning a button in-process (CI never runs build-themes, so
reading dist/ would match nothing and report green), finds each rule that can
reach a rail tab, and fails when any non-engine rule ties or beats the engine on
an engine-owned property. Source text cannot see this; the previous source-only
guard passed the defect.

Four mutations, each reddening its own arm: dropping the `:root` (the defect
itself), an app re-declaring min-width, the engine dropping the hover-background
token, and a top-level `:root` token leaking out of an app file. The first
attempt at that last mutation stayed green because a nested `:root` still
compiles under the app class — the arm was fine, the mutation was not.

Still open from the review: the close target's AC-3 and AC-6 need computed
styles in a live document. Next commit.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>

* test(dashboard): a rendered rail tab witnesses the min-width release (#17522)

The close target's AC-3 and AC-6 ask for computed styles, which a compiled
selector comparison cannot supply. `DockRailTabPaint.spec.mjs` mounts a real
`Neo.dashboard.Container` with a real `DockRail` and measures the tab a browser
actually paints.

Three findings shaped the fixture, each caught by a control rather than by
inspection:

1. `empty-viewport` inherits DefaultConfig.themes, which carries the LEGACY
   themes plus one neo theme. The 48px `.neo-button` floor exists only in the
   neo pair, so a witness running there measures a document where the competing
   rule was never loaded. The new `dock-rail` fixture app pins both neo themes.
2. Mounting a plain container wearing `.neo-dashboard` leaves
   `src/dashboard/Container.css` unloaded — Neo loads a class's stylesheet when
   the class is instantiated — so the rule under test was absent and the arms
   measured the generic button skin. The fixture now mounts the real class.
3. The engine rule TRANSITIONS `color`. Reading straight after a class flip
   returns the start value and a frame later returns a blend (measured at
   rgb(244,172,171) en route to red). Transitions are switched off for the
   measurement; a sleep would only have made the flake slower.

Every min-width arm measures a plain sibling button FIRST and requires it to be
at the 48px floor. `min-width: 0px` on a rail tab only means the engine won if
the floor was live in the same document; without that control a theme that
simply failed to load reads as a pass — which is exactly what finding 1 was.

Mutation: dropping the `:root` from the engine rule reds both theme arms with
`Received: "48px"` — the empty-pill regression itself, on a rendered tab.

Component suite 53 passed; unit buildScripts + core 171 passed.

Reported-by: @neo-gpt-emmy
Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>

* test(dashboard): the rail tab's paint is witnessed on both real consumers (#17522)

Discharges @neo-gpt-emmy's RA-2 remainder. Her disposition was exact: the
compiled-selector and source-guard clauses were addressed, but the runtime file
checked only `min-width` across themes and then injected a synthetic
`.test-app-a` rule. A rule authored in a spec is a rule nobody ships, so it
proved the engine's token plumbing and nothing about either app.

Five arms now load the COMPILED artifacts instead — the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. Four measure background, color, border,
box-shadow and font-family resting and hovered, per app per neo theme. The
fifth puts both real consumers in one document and mutates one app's token.

Parity is asserted per state against the app's OWN palette token —
`--workstation-ink-dim` / `--workstation-ink`, `--fm-ink-dim` / `--fm-ink` —
which is what these tabs computed before the promotion, when the app declared
`color` directly. Border and box-shadow are asserted to stay engine-neutral in
both states: an app that re-declared either would be back in the
equal-specificity tie this promotion dissolved.

Three things this caught, each of which had been green or red for the wrong
reason:

The first version asserted `resting.color !== hovered.color` and called it "the
app's ink reaches the tab". Deleting the engine's resting `color:
var(--dock-rail-tab-color)` left all four arms GREEN — the separate hover rule
still moved the value, so two states differing proved neither came from where
the arm claimed. Against the tightened arms that same mutation reds all four
with `Expected: "rgb(139, 151, 168)" / Received: "rgb(0, 0, 0)"`.

The two-consumer arm first asserted the apps start with different ink. They do
not: workstation and FM share `#8b97a8` for dim ink in the dark theme, so the
precondition asserted a difference the design does not have. It is anchored on
voice instead — only workstation sets `--dock-rail-tab-font-family` — which is
where they genuinely diverge, and the asymmetry is asserted rather than
normalised away.

Loading an app's rule without its theme-scoped token layer leaves every `var()`
an invalid substitution, and the tab then computes the engine default under the
app's name. Both layers load, and a failed link rejects loudly rather than
skipping.

---------

Co-authored-by: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-22T15:30:00Z @tobiu closed this issue

