---
id: 11658
title: 'KB Ingestion Phase 0/1B: Source/Parser registry + useDefaultSources/useDefaultParsers configs'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-05-19T16:06:58Z'
updatedAt: '2026-05-20T00:52:41Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11658'
author: neo-opus-ada
commentsCount: 0
parentIssue: 11625
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-20T00:52:41Z'
---
# KB Ingestion Phase 0/1B: Source/Parser registry + useDefaultSources/useDefaultParsers configs

## Context

Phase 0/1B sub-ticket of [#11625](https://github.com/neomjs/neo/issues/11625) (Phase 0/1 of Epic [#11624](https://github.com/neomjs/neo/issues/11624) — Cloud-Native KB Ingestion). Continues the increment pattern set by Phase 0/1A ([#11629](https://github.com/neomjs/neo/issues/11629) merged as PR [#11647](https://github.com/neomjs/neo/pull/11647)) which shipped `parsed-chunk-v1.schema.json` + `backup-record-v1.schema.json` + the path-identity tuple + deletion-signaling contract.

Phase 0/1A defined the **data contracts**. Phase 0/1B replaces the **hardcoded source-list** at `ai/services/knowledge-base/DatabaseService.mjs:470-481` with a data-driven registry, gated by `useDefaultSources` / `useDefaultParsers` boolean configs — the substrate floor that lets cloud deployments mix Neo's curated sources with tenant-supplied custom sources.

## The Problem

`DatabaseService.createKnowledgeBase()` currently iterates a hardcoded array of 10 Neo-curated Source classes (`AdrSource`, `ApiSource`, `ConceptSource`, etc.). Cloud-deployed Agent OS workspaces need to:

1. **Toggle Neo's defaults off** — e.g., a tenant whose external repo has nothing to do with Neo's curated content can pass `useDefaultSources: false` and ingest only their own content.
2. **Register custom Source classes** — a tenant with a `.proto` parser, an ES5 codebase, or a C++ project needs a registration API that doesn't require forking Neo.
3. **Override per-source paths** — `ApiSource.sourceMap` etc. currently hardcode Neo's local file layout; cloud deployments need per-tenant path config.

The path-prefix matcher pattern from the FileSystemIngestor incident (#11650/#11651) is the cautionary tale here — hardcoded structural assumptions become substrate-level invariants that are hard to unwind once consumed.

## The Architectural Reality

This phase touches:

| File | Change |
|---|---|
| **NEW** `ai/services/knowledge-base/source/SourceRegistry.mjs` | Registry singleton holding registered Source classes + custom Parser classes. Default sources auto-register on import when `aiConfig.useDefaultSources !== false`. Provides `registerSource(class, options)` + `registerParser(class, options)` public API. |
| `ai/mcp/server/knowledge-base/config.mjs` + `config.template.mjs` | Add `useDefaultSources: true` (default) + `useDefaultParsers: true` (default) booleans. Add `customSources: []` + `customParsers: []` arrays (default empty) for declarative registration via config. |
| `ai/services/knowledge-base/DatabaseService.mjs:470-481` | Replace hardcoded sources array with `SourceRegistry.getSources()`. Honor `aiConfig.useDefaultSources` toggle. |
| `ai/services/knowledge-base/source/index.mjs` (or `_export.mjs` if existing convention) | Centralized re-export of all default Source classes; auto-registers them when registry-singleton imports the index. |
| Per-source `sourceMap` / path config | Externalized to `aiConfig.knowledgeBase.sourcePaths.<sourceName>` with sensible Neo-default values; consumers read from config instead of hardcoded constants. |

The registration API mirrors the established `Neo.setupClass` pattern — Source classes are singleton-extending `Neo.core.Base` subclasses, so `SourceRegistry.registerSource(MySource)` accepts the class itself; the registry calls `Neo.setupClass(MySource)` if not already done.

## The Fix

### 1. `SourceRegistry` singleton (new)

```js
class SourceRegistry extends Base {
    static config = {
        className: 'Neo.ai.services.knowledge-base.source.SourceRegistry',
        singleton: true
    }

    #sources = new Map();  // sourceName → Source class
    #parsers = new Map();  // parserId → Parser class

    registerSource(SourceClass, {sourceName} = {}) { /* ... */ }
    registerParser(ParserClass, {parserId} = {}) { /* ... */ }

    getSources()  { return Array.from(this.#sources.values()); }
    getParsers()  { return Array.from(this.#parsers.values()); }
    hasSource(n)  { return this.#sources.has(n); }
    hasParser(id) { return this.#parsers.has(id); }
}
```

### 2. Auto-registration of defaults

`ai/services/knowledge-base/source/index.mjs` imports each default Source class + calls `SourceRegistry.registerSource(...)` for each, conditionally on `aiConfig.useDefaultSources !== false`.

### 3. `DatabaseService.createKnowledgeBase()` refactor

```js
async createKnowledgeBase() {
    // ...
    const sources = SourceRegistry.getSources();
    // ... existing iteration logic unchanged
}
```

### 4. Config additions

`config.mjs` + `config.template.mjs` get:

```js
knowledgeBase: {
    useDefaultSources: true,
    useDefaultParsers: true,
    customSources    : [],  // [{className, sourceName, sourcePath}, ...]
    customParsers    : [],  // [{className, parserId, parserVersion}, ...]
    sourcePaths      : {
        ApiSource    : 'docs/output/all.json',
        LearningSource: 'learn/tree.json',
        // ...
    }
}
```

### 5. Byte-equivalence fixture test

A unit test that:
1. Generates `dist/ai-knowledge-base.jsonl` with the pre-registry code path (snapshot fixture).
2. Generates the same with the post-registry code path under `useDefaultSources: true`.
3. Asserts byte-for-byte equivalence (or chunk-set equivalence — file order may differ if registry iteration semantics change).

This guarantees the refactor doesn't subtly change Neo's KB output.

## Acceptance Criteria

- [ ] `SourceRegistry.mjs` exists with `registerSource` / `registerParser` / `getSources` / `getParsers` / `hasSource` / `hasParser` public methods
- [ ] `useDefaultSources` (default `true`) + `useDefaultParsers` (default `true`) configs in `config.mjs` + `config.template.mjs`
- [ ] `customSources` + `customParsers` arrays in config (default `[]`) for declarative-registration path
- [ ] Default Source classes auto-register via `ai/services/knowledge-base/source/index.mjs`
- [ ] `DatabaseService.createKnowledgeBase()` uses `SourceRegistry.getSources()` instead of hardcoded array
- [ ] Per-source path config externalized under `aiConfig.knowledgeBase.sourcePaths.*`
- [ ] Unit tests under `test/playwright/unit/ai/services/knowledge-base/source/` for SourceRegistry: register/dedup/unregister/list semantics, `useDefaultSources: false` skip-defaults behavior, custom source registration round-trip
- [ ] Byte-equivalence fixture test passes (current Neo KB output stable post-refactor under `useDefaultSources: true`)
- [ ] Existing `npm run ai:sync-kb` produces unchanged output under default config (manual smoke test documented in PR)
- [ ] JSDoc + Anchor & Echo discipline on all new public surfaces

## Out of Scope

- Cross-server push pipeline + MCP small-batch facade (Phase 2 [#11626](https://github.com/neomjs/neo/issues/11626) — blocked by all of Phase 0/1)
- KB Tenant Isolation (write-side stamping + read-side filter; tracked separately in remaining Phase 0/1 ACs — will be a sibling Phase 0/1C sub-issue)
- Custom Parser registration end-to-end runtime (this PR ships the API surface + config; actual cross-language parser execution belongs to Phase 2 / Phase 3 demos)
- ES5 + C++ workspace integration fixtures (deferred to Phase 2 per #11625 body)
- HTTP / streaming transport for cross-server tenant push (Phase 2)

## Avoided Traps

| Trap | Why rejected |
|---|---|
| Skipping the registry, keeping hardcoded array but adding `useDefaultSources: false` early-return | The hardcoded array is the substrate-level invariant. Registry refactor is required for tenant-supplied sources; gating early-return doesn't enable custom registration. |
| Putting config in `aiConfig.knowledgeBase.sources` as inline registration object | Loses class-extension semantics; tenants need to subclass `Base.mjs` extract() method. Class registration matches the Source-class shape. |
| Auto-registering Neo defaults via `customSources` config | Couples default discipline to operator config. Defaults must be code-level discoverable (`ai/services/knowledge-base/source/index.mjs`) so adding a new Neo source doesn't require config sync. |
| Renaming `Source.extract` signature to accept `sourcePath` parameter | Cross-cuts every existing Source class and the test suite. Keep `extract(writeStream, createHashFn)` stable; per-source path config is read inside each source's class via `aiConfig.knowledgeBase.sourcePaths.<name>`. |

## Related

- **Parent Phase Epic:** [#11625](https://github.com/neomjs/neo/issues/11625) (Phase 0/1: Contracts, Source/Parser Registry, KB Tenant Isolation)
- **Predecessor Sub-ticket:** [#11629](https://github.com/neomjs/neo/issues/11629) (Phase 0/1A: schemas + identity tuple + deletion-signaling) → merged as PR [#11647](https://github.com/neomjs/neo/pull/11647)
- **Parent Epic:** [#11624](https://github.com/neomjs/neo/issues/11624)
- **Origin Discussion:** [#11623](https://github.com/neomjs/neo/discussions/11623)
- **Sibling that depends on this:** [#11626](https://github.com/neomjs/neo/issues/11626) Phase 2 (Ingestion Service + MCP facade) — `KnowledgeBaseIngestionService` consumes the registry for tenant-source discovery
- **Substrate-audit-consumer-sweep anchor:** Phase 0/1A repair cycles (GPT C1 + Gemini C1 + operator V-B-A) identified consumer surfaces that need stable contracts before this registry layer can land safely — those landed in Phase 0/1A first.

## Origin Session ID

`7360e917-1733-4cdd-a6f3-5ac51c34b838`

## Handoff Retrieval Hints

- `query_raw_memories({query: 'Source registry useDefaultSources useDefaultParsers KB ingestion phase 0/1B'})`
- `ask_knowledge_base({query: 'DatabaseService createKnowledgeBase hardcoded sources array', type: 'src'})`
- Empirical anchor: `ai/services/knowledge-base/DatabaseService.mjs:470-481` hardcoded source array — the substrate-mechanical surface to refactor
- Pattern reference: `ai/services/knowledge-base/parser/` from Phase 0/1A shipped #11647 (schemas + JSDoc convention)

## Timeline

- 2026-05-19T16:06:59Z @neo-opus-ada added the `enhancement` label
- 2026-05-19T16:07:00Z @neo-opus-ada added the `ai` label
- 2026-05-19T16:07:00Z @neo-opus-ada added the `architecture` label
- 2026-05-19T16:07:07Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-05-19T16:07:17Z @neo-opus-ada added parent issue #11625
- 2026-05-19T16:14:11Z @neo-opus-ada cross-referenced by PR #11659
- 2026-05-19T16:43:26Z @neo-opus-ada referenced in commit `d5bd78d` - "fix(ai): refactor _export.mjs into testable applyConfigToRegistry fn + config-driven tests (#11658)

Addresses 2 of 3 Required Actions from @neo-gpt's Cycle 1 review on PR #11659
(commentId PRR_kwDODSospM8AAAABAY3bSg):

**RA2 — Direct coverage for config-driven registration path:**
Extracted the import-time auto-registration into an exported pure function
`applyConfigToRegistry(registry, config, {defaults})`. The function is invoked
once at module-load time with the production aiConfig (preserves zero-config
default-register-on-import shape); tests now exercise the same logic with mocked
config shapes against a fresh registry instance. 7 new tests cover:
- `useDefaultSources: false` skips defaults (Required Action 2 acceptance criterion)
- `useDefaultSources` truthy default registers all 10 sources
- Declarative `customSources` entry with explicit `sourceName` override
- Declarative `customSources` entry deriving sourceName from className final segment
- Declarative `customParsers` entry with explicit `parserId` override
- Defensive skip on malformed customSources entries (null entry, missing SourceClass, falsy SourceClass)
- Full cloud-deployment shape: defaults disabled + 2 tenant sources + 1 tenant parser

**RA3 — Fix JSDoc shape mismatch:**
The `_export.mjs` module JSDoc previously described `customSources = [{className,
sourceName, ...}, ...]` while the implemented + configured shape is `{SourceClass,
sourceName?}`. The class object (not a className string) is passed via `SourceClass`
— preserving Neo's class-extension semantics. JSDoc now correctly documents the
implemented shape with a worked code example.

**RA1 (close-target semantics) handled separately** via PR body edit (change
`Resolves #11658` → `Related: #11658` so the ticket remains open for the deferred
per-source-path AC). No code change required for RA1.

Test verification: `npm run test-unit -- test/playwright/unit/ai/services/knowledge-base/source/SourceRegistry.spec.mjs`
→ 18 passed in 715ms (11 originals + 7 new). (#11658)"
- 2026-05-20T00:52:42Z @tobiu closed this issue
- 2026-05-20T01:01:12Z @neo-opus-ada cross-referenced by #11660
- 2026-05-20T01:02:15Z @neo-gpt cross-referenced by #11625
- 2026-05-20T01:10:00Z @neo-opus-ada cross-referenced by PR #11661
- 2026-05-20T02:20:33Z @neo-opus-ada cross-referenced by #11626
- 2026-05-20T02:42:55Z @neo-gpt cross-referenced by #11630
- 2026-05-20T07:48:25Z @tobiu referenced in commit `d1b3cf7` - "feat(ai): externalize per-source paths to aiConfig.sourcePaths (#11660) (#11661)

* feat(ai): externalize per-source paths to aiConfig.sourcePaths (#11660)

Phase 0/1B-β of Epic #11624 (Cloud-Native KB Ingestion). Closes the deferred AC
from #11658 (PR #11659) — per-source path externalization across the 10 default
Neo Source classes. Cloud deployments where the tenant's filesystem layout differs
from Neo's can now reuse Neo's curated Source classes while overriding their input
paths via `aiConfig.sourcePaths.<SourceName>`. #11658 stays open until this PR
merges, then can be closed as fully resolved.

**Surfaces touched:**

- `ai/mcp/server/knowledge-base/config.template.mjs` (canonical) + paired
  gitignored `config.mjs`: new `sourcePaths` config object with all 10 Neo
  source defaults pre-populated.
- 10 Source classes refactored to read `aiConfig.sourcePaths?.<SourceName> ??
  '<hardcoded-fallback>'` at each path-resolution point. Hardcoded fallback
  preserves byte-equivalence for pre-#11660 deployments whose local config.mjs
  was generated before this commit (the `??` rhs IS the legacy default).

**Shape variation per source class:**

- **Single-path string** (AdrSource, ConceptSource, ReleaseNotesSource,
  SkillSource, TestSource, LearningSource): `'learn/agentos/decisions'` etc.
- **Array of paths** (DiscussionSource, PullRequestSource, TicketSource):
  `['primary', 'archive']` — supports cloud deployments without an archive
  subdir (single-element arrays work).
- **Object map** (ApiSource): `{path: type}` sourceMap-style for multi-directory
  indexing.

Each Source class interprets its own entry shape; the config is just a
per-source override surface.

**LearningSource special-case:** the config entry points at the tree.json file;
the base directory containing the .md files is derived as the path's containing
directory (so a cloud deployment can put guides under `docs/guides/tree.json`
+ `docs/guides/*.md` without changing the Source class).

**Tests:**

- New spec `SourcePathsConfig.spec.mjs` — 36 tests covering: byte-equivalence
  anchor (default config matches each hardcoded fallback exactly), override
  precedence, missing-key fallthrough, defensive whole-object-deletion
  fallthrough, LearningSource base-derivation semantics.
- Regression: 30 pre-existing source + DatabaseService.backup tests pass; 18
  SourceRegistry tests pass; full 66-test source-tree run zero failures
  (after re-run confirmed earlier interleaving flake was transient).

(#11660)

* fix(ai): serialize SourcePathsConfig spec for fullyParallel local execution (#11660)

Addresses Required Action 1 from @neo-gpt's Cycle 1 review on PR #11661
(formal reviewId PRR_kwDODSospM8AAAABAcNngw, lightweight hold commentId
IC_kwDODSospM8AAAABC9dlIg): the spec mutates the shared `aiConfig.sourcePaths`
singleton via per-test override + delete patterns. Under local `fullyParallel`
(workers=9), the per-test try/finally restoration windows overlap and cause
cross-test races where one test's `delete aiConfig.sourcePaths[X]` is observed
by a concurrent test as missing.

CI uses `workers: 1` per playwright.config.unit.mjs which masks the issue at
the CI surface, but locally `npm run test-unit -- <single-file>` ran 9 workers
and exposed the race.

Fix: `test.describe.configure({mode: 'serial'})` at the top of the spec file.
Tests now run sequentially within the worker, eliminating the overlap window.
Verified: 36 passed in 599ms after the change.

Same pattern as `feedback_symmetric_spec_cleanup.md` memory anchor:
"Playwright fullyParallel interleaves specs in same worker; cross-singleton
mutations need symmetric beforeEach+afterEach resets, not just beforeEach."
My in-test try/finally was the symmetric reset but couldn't compensate for
concurrent-window overlap; serial mode is the correct shape.

(#11660)

* fix(ai): byte-equivalence anchors text-grep template instead of live config (#11660)

Addresses Cycle 1 re-review hold from @neo-gpt commentId IC_kwDODSospM8AAAABC9gB_A
(formal Cycle 2 still pending in-progress CI on cbdd01806):

GPT's clone's gitignored `ai/mcp/server/knowledge-base/config.mjs` was created
before this PR's template update + the clone hadn't re-run `bootstrapWorktree.mjs`
to refresh. The spec's byte-equivalence anchor assertions
(`expect(aiConfig.sourcePaths?.[name]).toBe(fallback)`) were coupled to local
config-freshness — passing on fresh clones, failing on stale clones.

Root cause: the spec's `aiConfig.sourcePaths?.[name]` assertion conflated two
distinct concerns:
  1. Stale-clone protection (the `??` fallback in Source classes works regardless)
  2. Cross-file drift detection (Source-class inline fallback vs template default)

Fixed:

- Byte-equivalence anchor reframed as a text-grep against the canonical git-tracked
  template file (`fs.readFile(TEMPLATE_PATH)` + `expect(templateText).toContain(...)`).
  This is stale-clone-safe (the template is git-tracked, never stale) and still
  catches Source-class-inline-fallback vs template-default drift if either side
  changes without the other.
- Direct `templateConfig` module import was attempted but triggers `Neo.setupClass`
  namespace collision under `unitTestMode` (both config.mjs + config.template.mjs
  register the same className). Text-grep avoids the namespace registry entirely.
- Override + missing-key runtime tests still mutate `aiConfig.sourcePaths` to verify
  resolution behavior. Added defensive `if (!aiConfig.sourcePaths) aiConfig.sourcePaths = {}`
  in beforeAll so stale-clone fresh test runs don't crash on the mutations. afterAll
  restores the original state (undefined or otherwise) so clone-freshness leaks
  don't propagate to subsequent specs.

Test verification: 36 passed in 631ms post-fix.

Substrate-evolution insight: the byte-equivalence anchor design is the right
intent (detect cross-file drift) but the implementation has to be careful about
what it's actually asserting. Asserting on the live config conflates "fresh
clone" with "no drift". Asserting on the canonical template separates concerns.

(#11660)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-20T11:17:13Z @neo-opus-ada cross-referenced by #11633
- 2026-05-20T22:02:38Z @neo-opus-ada cross-referenced by #11637
- 2026-05-20T23:33:06Z @neo-opus-ada cross-referenced by PR #11704

