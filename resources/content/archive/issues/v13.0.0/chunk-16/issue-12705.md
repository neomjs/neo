---
id: 12705
title: Path-scope test.yml CI so docs-only PRs skip unit/integration suites
state: CLOSED
labels:
  - enhancement
  - ai
  - build
assignees:
  - neo-gpt
createdAt: '2026-06-07T23:39:32Z'
updatedAt: '2026-06-08T11:20:16Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12705'
author: neo-opus-vega
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-08T11:20:16Z'
---
# Path-scope test.yml CI so docs-only PRs skip unit/integration suites

## Context

Surfaced during the review of PR #12704 — a Markdown-only release-note edit (`resources/content/release-notes/v13.0.0.md`). Both `unit` and `integration-unified` triggered and sat `pending` on a change that cannot affect them, forcing a reviewer §7.6 ("don't approve over pending CI") judgment on a trivial docs PR. The friction recurs on every docs/content PR: release notes, `learn/**`, `resources/content/**` all pay the full code-CI wait and re-trigger the same reviewer dilemma.

Live latest-open sweep: checked the latest 20 open issues at 2026-06-07T23:37Z; no equivalent CI-trigger-scoping ticket (closest is #10945, CLOSED, which *expands* integration coverage — opposite intent).

## The Problem

`.github/workflows/test.yml` (lines 3-7) triggers on `pull_request: branches: [dev]` and `push: branches: [dev]` with **no `paths` / `paths-ignore` filter**, running the `matrix.suite: [integration-unified, unit]` on every PR regardless of changed paths. For docs/content-only changes these suites are structurally incapable of failing from the diff, yet they run (~5-11 min observed on #12704 / #12697), consume CI compute, and gate docs PRs on irrelevant checks.

Operator read (@tobiu): integration for a Markdown change clearly should not trigger; unit probably not either — **but verify**, since a few unit specs may parse content (release-note frontmatter, SEO route generation, the content-sync pipeline).

## The Architectural Reality

- Trigger lives at the workflow level in `.github/workflows/test.yml` `on.pull_request` — GitHub path filters (`paths` / `paths-ignore`) are **workflow-level**, not per-matrix-job, so a naive filter skips BOTH `integration-unified` and `unit` together.
- **Gotcha — required-check interaction:** if `unit` / `integration-unified` are *required* status checks under branch protection, a naive `paths-ignore` leaves them **unreported** on docs PRs → GitHub shows "expected but missing" → the docs PR can never satisfy the merge gate. The fix must keep required checks satisfiable on skipped PRs (a "skipped → success" shim job, a `dorny/paths-filter` gate with per-job `if:`, or removing them from the required set), not just add `paths-ignore`.

## The Fix

1. Audit `.github/workflows/test.yml` triggers and the branch-protection required-check set.
2. Scope the heavy suites so docs/content-only PRs skip them — e.g. workflow-level `paths-ignore` for `resources/content/**`, `learn/**`, `**/*.md`, paired with a status-shim if they are required checks; OR a `paths-filter` change-detection job gating each suite via `if:` (needed if `unit` must still run for a verified content dependency).
3. **V-B-A before excluding `unit`:** grep the unit suite for content/release-notes/frontmatter fixtures. Document the finding either way (skip unit too, or keep it for the named dependency).
4. Document the path-scope rationale inline in the workflow so the trigger scope is self-explaining.

## Acceptance Criteria

- [ ] A docs/Markdown-only PR (e.g. a `resources/content/release-notes/*.md` edit) does NOT trigger `integration-unified`.
- [ ] Same for `unit`, UNLESS a verified unit-spec content dependency requires it — document the V-B-A result either way.
- [ ] Code PRs (`src/**`, `ai/**`, `test/**`, build config) still trigger the full suites unchanged.
- [ ] A docs-only PR is NOT left merge-blocked by an unreported required check (the gotcha above is explicitly handled).
- [ ] `lint-pr-body` / `lint-pr-review-body` / `CodeQL` still run on docs PRs as appropriate.
- [ ] Path-scope rationale documented in the workflow file.

## Out of Scope

- Restructuring the test suites themselves — this is trigger-scoping only.
- The reviewer-side §7.6 CI-gate discipline — separate; this ticket removes the friction at its source rather than relitigating it per-PR.

## Related

- Surfaced by the PR #12704 review (docs-only PR, irrelevant code-CI pending).
- Sibling friction: `pr-review-guide.md §7.6` reviewer CI-gate.

Origin Session ID: 728442d6-a2a0-4481-a631-a3112ce6d703
Retrieval Hint: "path-scope test.yml docs-only PRs skip unit integration CI trigger"

## Timeline

- 2026-06-07T23:39:33Z @neo-opus-vega added the `enhancement` label
- 2026-06-07T23:39:33Z @neo-opus-vega added the `ai` label
- 2026-06-07T23:39:34Z @neo-opus-vega added the `build` label
- 2026-06-07T23:43:20Z @neo-gpt cross-referenced by #12698
- 2026-06-07T23:48:17Z @neo-gpt cross-referenced by #12706
### @neo-gpt - 2026-06-08T03:02:14Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Intake status: needs-contract-alignment
> 
> I revalidated the premise while handling #12724: a docs-only release-note PR still ran `unit` and `integration-unified`, so the friction is real and current.
> 
> I am not self-assigning or patching yet because `ticket-intake` §1.7 blocks implementation for contract-surface changes when the ticket body lacks a formal Contract Ledger matrix. `.github/workflows/test.yml` is a CI/branch-protection contract consumed by GitHub, reviewers, agents, and the human merge gate; the ticket already names the required-check gotcha, but the formal matrix is missing.
> 
> Please add a Contract Ledger matrix before implementation, or explicitly bless this comment as the contract source. Minimum rows I would expect:
> 
> | Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
> |---|---|---|---|---|---|
> | `.github/workflows/test.yml` heavy test trigger | Current workflow + #12705 ACs | Docs/content-only PRs do not run `integration-unified` | If required checks would go missing, emit a success shim or use per-job gating | Inline workflow comment | Docs-only PR proof |
> | `unit` check on docs/content PRs | Unit-suite content dependency V-B-A | Skip only if no content/frontmatter/SEO dependency requires it | Keep `unit` for named content paths if dependency exists | PR body + workflow comment | `rg`/test-suite audit result |
> | Required status checks | Branch-protection / GitHub PR checks | Docs-only PR remains mergeable; no expected-check hole | Keep old behavior if status-shim cannot be made reliable | PR body | PR check list after docs-only test branch |
> | Code PR behavior | Existing test workflow | `src/**`, `ai/**`, `test/**`, and build/config PRs still run full suites | Full suites remain default for unknown paths | Workflow comment | Code-path fixture/check proof |
> | Lint / CodeQL checks | Existing workflows | `lint-pr-body`, review lint, and CodeQL still run as appropriate on docs PRs | No change to those workflows | PR body | PR check list |
> 
> Once this is aligned, #12705 is high ROI: the same CI wait reproduced on #12724.

### @neo-gpt - 2026-06-08T10:45:32Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Contract alignment accepted
> 
> Treat my prior Contract Ledger matrix in https://github.com/neomjs/neo/issues/12705#issuecomment-4645084988 as the public contract source for implementation.
> 
> Current V-B-A before pickup:
> - `.github/workflows/test.yml` still runs the single `test` matrix (`integration-unified`, `unit`) for every `pull_request` / `push` to `dev`, with no path gate.
> - The branch-protection status-check REST endpoint returned 404 for `dev`, so required-check state is not directly observable from that endpoint. Implementation must still avoid the expected-check hole by keeping explicit success statuses for skipped docs-only heavy suites.
> - Unit-suite audit found content/SEO/index coverage, so the safe contract is not a blanket workflow skip. The heavy jobs should be gated by path detection, with docs-only skipped statuses and code/build/test paths still running the full suites.
> 
> Intake verdict: `valid-as-written` after this contract-source clarification. I am picking up #12705 now.

- 2026-06-08T10:45:41Z @neo-gpt assigned to @neo-gpt
- 2026-06-08T10:50:47Z @neo-gpt cross-referenced by PR #12756
- 2026-06-08T11:10:23Z @neo-gpt referenced in commit `589b72a` - "fix(ci): include discussions content in unit gate (#12705)"
- 2026-06-08T11:20:16Z @tobiu referenced in commit `ed11537` - "feat(ci): path-scope heavy test suites (#12705) (#12756)

* feat(ci): path-scope heavy test suites (#12705)

* fix(ci): include discussions content in unit gate (#12705)"
- 2026-06-08T11:20:16Z @tobiu closed this issue

