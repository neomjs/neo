---
id: 16279
title: 'osascript wake adapter refuses OpenCode by name, leaving desktop seats undeliverable'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-kimi-phoebe
createdAt: '2026-08-01T17:06:49Z'
updatedAt: '2026-08-01T19:14:56Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16279'
author: neo-kimi-phoebe
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-01T19:14:56Z'
---
# osascript wake adapter refuses OpenCode by name, leaving desktop seats undeliverable

## Context

The wake receiver's `osascript` adapter — the only GUI-app delivery path that needs no plugin, no envelope, and no per-boot credential — refuses OpenCode by name: `localWakeAdapters.mjs:532` (`if (!['Antigravity', 'Claude', 'Codex'].includes(appName)) return 'skipped'`). The result: every OpenCode desktop seat is undeliverable through the GUI path, and the only remaining adapter (`opencode-server`) depends on the wake-envelope plugin, which has never been observed arming in the desktop app (no load-time log line in any log source, and a marker `console.log` at file top is never evaluated — verified 2026-08-01 against both the v1.18.5 desktop and an isolated `opencode serve` from a project-level plant).

The full chain diagnosis this ticket closes is on the record: route published and live (401-on-forged-signature), receiver delivering to the route, and the dispatch dying at the adapter — the same seat-class as the kimi CLI seats this lane already treats as first-class.

Live latest-open sweep: checked latest 20 open issues at 2026-08-01T16:55Z; no equivalent found. A2A in-flight claim sweep over the last 30 messages at the same timestamp: no competing `[lane-claim]` on OpenCode adapter support.

## The Problem

`deliverOsascript` gates on a three-name allowlist. OpenCode's desktop app is an Electron app whose process name is exactly `OpenCode`, whose prompt accepts the standard `cmd+a` / `cmd+x` / `cmd+v` / Return choreography the adapter's script already performs, and whose per-seat `userDataDir` (`ai.opencode.desktop`) resolves cleanly through the existing `resolveGuiInstancePid` path. Nothing in the adapter's AppleScript is Claude- or Codex-specific beyond the optional `tabShortcut` / `focusSeedSequence` / Codex-Esc branches — the generic path (activate → frontmost assert → clear-and-preserve prompt → paste payload → Return → restore user input) applies to OpenCode unchanged. The only barrier is the allowlist line.

Verified on this machine: the receiver delivered a real wake to my route at 13:06Z today (signature verified, dispatch attempted), and it failed in the opencode-server adapter for envelope reasons that the osascript path does not have.

## The Architectural Reality

- `ai/daemons/wake/localWakeAdapters.mjs` — `deliverOsascript` (:527) with the allowlist at :532; `buildOsascriptArgs` (:574) is app-generic except the optional Codex-only `key code 53` (Esc) branch and the focus-seed options, all of which are opt-in metadata an OpenCode route simply omits.
- `resolveGuiInstancePid` already handles `userDataDir`-shaped instance tuples for GUI apps; OpenCode's per-seat data dir follows the same convention as Claude's (`Library/Application Support/<app>`).
- The receiver, manifest, generator, and route for my seat are all proven live (the 13:06Z record); the missing link is strictly the adapter allowlist.
- The Accessibility permission for the receiver's controlling terminal was already exercised on this host this morning (Ada's cohort wakes), so the TCC gate is not new surface.

## The Fix

1. Add `'OpenCode'` to the adapter allowlist at `localWakeAdapters.mjs:532`.
2. Spec: an OpenCode-named route passes the gate (dispatch reaches the osascript spawn with the generic arg vector), and the existing three names keep passing — asserted via the adapter's existing fake-effects harness in `localWakeAdapters.spec.mjs`.
3. No OpenCode-specific script branches: the generic choreography is the contract; if live proof shows an Electron quirk (e.g., prompt not focused), that lands as a follow-up with the quirk named, not preemptively.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| osascript app allowlist | `localWakeAdapters.mjs:532` | `OpenCode` admitted to the GUI delivery path | unsupported names keep returning `skipped` | adapter JSDoc | spec: OpenCode route dispatches via the generic arg vector |
| OpenCode instance tuple | per-seat `userDataDir` (`ai.opencode.desktop`) | resolves through the existing GUI-instance pid path | unresolvable → `skipped` (named once #16259/#16264 lands) | manifest docs | spec or live record names the outcome |

## Decision Record impact

`none`. The adapter's app surface is implementation detail inside the wake lane; no ADR governs the allowlist.

## Acceptance Criteria

- [ ] A route with `harnessTargetMetadata: {adapter: 'osascript', appName: 'OpenCode', addressType: 'userDataDir', instanceAddress: <seat data dir>}` dispatches through the osascript path instead of returning `skipped` — spec-pinned.
- [ ] The existing three app names keep their behavior (positive control).
- [ ] Post-merge live proof on this machine: a high-priority non-suppressed DM to @neo-kimi-phoebe delivers through the receiver with state `delivered` and the wake visible in the OpenCode desktop — recorded as PMV.

## Out of Scope

- The wake-envelope plugin's desktop loading (the opencode-server adapter's dependency; upstream-level, tracked separately).
- The desktop internal server's per-boot credential behavior (proven to not accept env-provisioned creds; the osascript path needs no credentials).
- TCC/Accessibility provisioning for the receiver's controlling context (already exercised on this host; documented in the runbook for fresh hosts).

## Avoided Traps

- **Preemptive Electron-specific script branches** — the generic path is proven for two other Electron-family apps; adding quirks before the live proof invents requirements.
- **Widening the opencode-server envelope contract instead** — the desktop internal server demonstrably does not accept env-provisioned credentials (every endpoint 401 with byte-equal spawn-env values), and per-boot generated credentials are only discoverable from inside the app process; the osascript path has no credential surface at all.

## Related

- #16233 / PR #16249 — the manifest generator whose routes this unblocks for OpenCode seats
- #16259 / PR #16264 — the failure-reason surfacing that will name future `skipped` outcomes
- #16267 / PR #16271 — SIGHUP manifest reload (route adoption without restart)
- #16180 — the signed receiver this adapter serves

Origin Session ID: a9c25413-6846-4dee-a4e7-513e7600e0af

Retrieval Hint: `osascript adapter OpenCode allowlist GUI wake delivery Electron desktop seat`

## Timeline

- 2026-08-01T17:06:50Z @neo-kimi-phoebe added the `bug` label
- 2026-08-01T17:06:50Z @neo-kimi-phoebe added the `ai` label
- 2026-08-01T17:07:07Z @neo-kimi-phoebe assigned to @neo-kimi-phoebe
- 2026-08-01T17:14:34Z @neo-kimi-phoebe cross-referenced by PR #16281
- 2026-08-01T17:43:31Z @tobiu referenced in commit `ce57ce4` - "feat(wake): admit OpenCode to the osascript delivery adapter (#16279) (#16281)

The osascript adapter — the only GUI wake path that needs no plugin, no
envelope, and no per-boot credential — refused OpenCode by name at the
appName allowlist, leaving every OpenCode desktop seat undeliverable
through the GUI path. The adapter's AppleScript is app-generic (activate,
frontmost assert, clear-and-preserve prompt, paste payload, Return,
restore the user's draft) and applies to OpenCode's Electron desktop
unchanged; the only barrier was the allowlist line.

Verified live before writing this: my seat's route is published and the
receiver delivered a real wake to it at 13:06Z today (signature verified,
dispatch attempted) — the chain is proven to the adapter boundary, and
the desktop's internal server does not accept env-provisioned credentials
(every endpoint 401 with byte-equal spawn-env values), while osascript
needs no credentials at all.

Spec: an OpenCode-named route with a userDataDir tuple dispatches through
the generic choreography (activate line, instance-frontmost on the
resolved pid, payload clipboard set, Return submit) and gets NO
Codex-only Esc prelude — with the existing three app names kept as
positive controls. Full wake tree 250/250; full unit 10691 passed with
the 3 named load-flakes in untouched memory-core files.

PMV on this machine post-merge: receiver restart from merged dev, then a
high-priority non-suppressed DM to @neo-kimi-phoebe flips the record to
delivered with the wake visible in the OpenCode desktop."
- 2026-08-01T17:43:31Z @tobiu closed this issue
### @neo-kimi-phoebe - 2026-08-01T18:51:15Z

## [ledger amendment, assignee authority] The validation surfaces the adapter fix missed

PR #16281 shipped the dispatch-side allowlist (`localWakeAdapters.mjs`) and was **incomplete**: declaring the route at all hits two further appName gates, found live when I tried to flip my own subscription (`Invalid appName 'OpenCode'`). Amending the Contract Ledger with the surfaces, per the discipline that the ledger must match shipped reality before merge:

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| osascript app allowlist (dispatch) | `localWakeAdapters.mjs:537` | `OpenCode` admitted to the GUI delivery path — **shipped in PR #16281** | unsupported names keep returning `skipped` | adapter JSDoc | spec: OpenCode route dispatches via the generic arg vector |
| osascript route validation (manifest loader) | `receiver.mjs:107` | `OpenCode` route validates at load | other names keep failing validation | receiver JSDoc | spec: OpenCode route passes `loadWakeReceiverManifest` |
| `validAppNames` (subscription registration) | `WakeSubscriptionService.mjs:107` | `OpenCode` accepted as a canonical appName | non-canonical names keep rejecting with the updated list | service JSDoc | spec: OpenCode subscribe accepted; rejection message lists all four |

The discovery method itself is worth noting: I found the second and third gates by attempting the production flip on my own seat — the subscription update threw, and the receiver-loader gate followed by inspection. AC-1's "a route with `appName: 'OpenCode'` dispatches" was never achievable while the route could not be declared; these rows are that AC's full contract.

— Phoebe 🔆

- 2026-08-01T18:58:24Z @neo-kimi-phoebe cross-referenced by PR #16293
- 2026-08-01T19:11:10Z @neo-kimi-phoebe reopened this issue
- 2026-08-01T19:14:56Z @tobiu referenced in commit `cf5f366` - "feat(wake): admit OpenCode at the route-declaration and registration gates (#16279) (#16293)

PR #16281 admitted OpenCode to the osascript dispatch adapter but missed the
two gates that make the route declarable at all — found live by attempting
the production flip on my own seat (the subscription update threw
'Invalid appName'):

1. receiver.mjs manifest-loader validation rejected OpenCode osascript
   routes at load (the route could never reach the adapter it was just
   admitted to).
2. WakeSubscriptionService.validAppNames rejected 'OpenCode' at subscribe
   and update time (the route could never be declared).

Both lists now admit OpenCode, matching the dispatch-side admission. The
ticket's Contract Ledger is amended on the ticket with all three
validation surfaces (assignee authority), because AC-1's 'a route with
appName OpenCode dispatches' was never achievable while the route could
not be declared — these rows are that AC's full contract.

Specs: OpenCode osascript route passes loadWakeReceiverManifest (0600);
OpenCode appName accepted on subscribe; the non-canonical rejection
message updated to list all four names (existing pin adjusted).

Receipts: wake tree + WakeSubscriptionService suite 344/344; full unit
10676 passed with 2 named load-flakes in untouched memory-core files."
- 2026-08-01T19:14:56Z @tobiu closed this issue

