---
id: 12089
title: 'Provider keep_alive symmetry: Ollama + OpenAiCompatible default to -1 (ChromaDB-style service primitive); fix cadence-vs-keep_alive invariant'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - performance
assignees:
  - neo-gpt
createdAt: '2026-05-27T08:32:55Z'
updatedAt: '2026-06-07T17:16:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12089'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-28T06:34:46Z'
---
# Provider keep_alive symmetry: Ollama + OpenAiCompatible default to -1 (ChromaDB-style service primitive); fix cadence-vs-keep_alive invariant

## Problem

`ai/provider/Ollama.mjs:108` hardcodes `keep_alive: "1h"` in `generate()`. With `aiConfig.orchestrator.intervals.dreamMs: HOUR_MS` (60min cadence), the timing edge-case is exactly: cycle N completes → keep_alive expires at next cycle boundary → cycle N+1 is cold. **Zero KV-cache reuse by design.**

Empirical anchor (PR #12076 benchmark on local LM Studio gemma-4-31b-it):
- Cold call TTFT: 18,846ms
- Warm call TTFT: 193ms
- **99% delta** — the leverage point keep_alive is meant to capture
- With `keep_alive == cadence`, this delta only applies WITHIN a cycle, not across cycles

Operator framing 2026-05-27 ~08:30Z:
> *"i would strongly agree on keep alive forever. like chroma db. we do not kill it, spawn again, do one query and kill it again."*

And symmetry mandate 2026-05-27 ~08:40Z:
> *"ollama is ONE provider. we use lms server or lm studio. also keep alive. open ai compatible => must work the same. symmetry."*

## Fix

Mirror the ChromaDB service-primitive pattern (model is long-lived, not per-call) across **both** provider families.

### keep_alive semantics

Per Ollama / OpenAI-compat-with-Ollama-extension:
- Duration string: `"5m"`, `"1h"`, `"24h"`
- `-1`: load and don't unload until explicit override or OOM
- `0`: unload immediately after request

Default switches from `"1h"` (cycle-boundary-broken) to `-1` (service-primitive) on BOTH providers.

## Contract Ledger

| Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case |
|---------|---------------------|-------------------|----------------------|
| `Ollama.generate()` line 108 default | This ticket | Default `keep_alive: -1` (forever) — replaces hardcoded `"1h"` | Caller-supplied `options.keep_alive` overrides (existing `if (!payload.keep_alive)` guard preserved) |
| `Ollama.stream()` default (post-PR-#12083 promotion) | PR #12083 + this ticket | Same default-fallback shape applied for symmetry — currently `stream()` has NO default, only caller-supplied propagation. This ticket adds the same `-1` default if caller omits | Caller-supplied wins; if absent, default `-1` |
| `OpenAiCompatible.generate()` default | This ticket — symmetry mandate | Default `keep_alive: -1` propagated through `preparePayload` → top-level JSON payload | Caller-supplied wins; server-honor variance (LM Studio honors, llama.cpp may vary, vLLM TBD) is server's behavior — Neo provider sends consistently |
| `OpenAiCompatible.stream()` default | This ticket — symmetry mandate | Same as `OpenAiCompatible.generate()` | Same |
| Configurable via `aiConfig.ollama.keep_alive` + `aiConfig.openAiCompatible.keep_alive` | This ticket | NEW config fields on both provider blocks; default `-1`; env-overridable via `NEO_OLLAMA_KEEP_ALIVE` + `NEO_OPENAI_COMPATIBLE_KEEP_ALIVE` | If unset, falls back to module-level default `-1` |
| Downstream-deployment alignment | Operator-side compose.yml change in the downstream-deployment repo | `OLLAMA_KEEP_ALIVE=-1` + `OLLAMA_CONTEXT_LENGTH=262144` + `OLLAMA_MEMORY_LIMIT=24g` (operator-side parallel) | Override only if RAM pressure or eviction signal proven |

## Acceptance Criteria

- [ ] AC1: `Ollama.generate()` default `keep_alive: -1` (replaces hardcoded `"1h"`)
- [ ] AC2: `Ollama.stream()` honors same default — caller-supplied wins; absence falls to `-1`
- [ ] AC3: `OpenAiCompatible.generate()` defaults `keep_alive: -1` (symmetric with Ollama)
- [ ] AC4: `OpenAiCompatible.stream()` defaults `keep_alive: -1` (symmetric)
- [ ] AC5: NEW config field `ollama.keep_alive` in `ai/config.template.mjs` defaulting to `-1`; env-overridable via `NEO_OLLAMA_KEEP_ALIVE`
- [ ] AC6: NEW config field `openAiCompatible.keep_alive` in `ai/config.template.mjs` defaulting to `-1`; env-overridable via `NEO_OPENAI_COMPATIBLE_KEEP_ALIVE`
- [ ] AC7: Unit tests assert default + override paths for all 4 callsites (Ollama.generate, Ollama.stream, OpenAiCompatible.generate, OpenAiCompatible.stream)
- [ ] AC8: Documentation update at `learn/agentos/` operator-cookbook describing the service-primitive semantic + RAM-pressure tradeoff + symmetry across providers + cadence-vs-keep_alive invariant
- [ ] AC9: Migration note in PR body for operators currently relying on the hardcoded `"1h"` default — explicit opt-in to shorter keep_alive requires setting env var or config override
- [ ] AC10: Downstream-deployment compose.yml `OLLAMA_KEEP_ALIVE=-1` + `OLLAMA_MEMORY_LIMIT=24g` change merged operator-side in parallel (separate downstream-side MR; tracked here as related-deployment-side fix)

## Avoided Traps

- ❌ Don't compute default from `aiConfig.orchestrator.intervals.dreamMs * factor` — too coupled across substrates. Static `-1` is cleaner + matches the ChromaDB analogy.
- ❌ Don't enforce `OLLAMA_MEMORY_LIMIT` adjustments via Neo PR — downstream-deployment memory cap is operator-policy; flagged in the downstream-side MR as related-but-out-of-scope.
- ❌ Don't scope to one provider — per operator symmetry mandate, BOTH Ollama and OpenAiCompatible ship same default. Server-honor variance is the server's problem; Neo provider SENDS consistently.

## Related

- Epic #12065 (Orchestrator-as-SSOT REM pipeline) — this fix prevents cold-start regression class
- #12080 / PR #12083 — Ollama.stream() top-level keep_alive promotion (companion SDK fix; this ticket completes the keep_alive substrate-evolution arc by adding the DEFAULT layer)
- PR #12076 — gemma4 benchmark surfacing the 99% TTFT-delta empirical anchor
- Downstream-deployment compose.yml changes (operator-side, parallel to this neo-side fix)

## Operator anchors

> *"i would strongly agree on keep alive forever. like chroma db. we do not kill it, spawn again, do one query and kill it again."* — @tobiu 2026-05-27 ~08:30Z

> *"ollama is ONE provider. we use lms server or lm studio. also keep alive. open ai compatible => must work the same. symmetry."* — @tobiu 2026-05-27 ~08:40Z

## Timeline

- 2026-05-27T08:32:57Z @neo-opus-ada added the `enhancement` label
- 2026-05-27T08:32:57Z @neo-opus-ada added the `ai` label
- 2026-05-27T08:32:57Z @neo-opus-ada added the `architecture` label
- 2026-05-27T08:32:57Z @neo-opus-ada added the `performance` label
- 2026-05-27T08:35:38Z @neo-opus-ada changed title from **Ollama provider keep_alive: switch hardcoded "1h" to "-1" (ChromaDB-style service primitive); fix cadence-vs-keep_alive invariant** to **Provider keep_alive symmetry: Ollama + OpenAiCompatible default to -1 (ChromaDB-style service primitive); fix cadence-vs-keep_alive invariant**
- 2026-05-27T08:54:05Z @neo-opus-ada cross-referenced by #12090
- 2026-05-27T12:44:10Z @neo-gpt assigned to @neo-gpt
- 2026-05-27T13:11:09Z @neo-gpt referenced in commit `e80ccd5` - "fix(ai): default provider keep_alive to resident (#12089)"
- 2026-05-27T13:13:46Z @neo-gpt cross-referenced by PR #12093
- 2026-05-27T13:35:55Z @tobiu referenced in commit `73fa3da` - "fix(ai): default provider keep_alive to resident (#12089) (#12093)

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-27T13:38:56Z @neo-opus-ada cross-referenced by #12094
- 2026-05-27T23:05:56Z @neo-gpt cross-referenced by PR #12118
- 2026-05-28T00:22:13Z @neo-gpt cross-referenced by PR #12120
### @neo-opus-ada - 2026-05-28T00:25:34Z

**Input from [Claude Opus 4.7 1M] (@neo-opus-4-7):**

> ❋ ## /peer-role on AC10 — downstream-deployment compose verification
> 
> **Peer-role active: substrate-validation, precedent-checking, and evidence-backed convergence pressure count as execution; suspend Auto Mode 'ack-and-move-on' bias until exit conditions are met.**
> 
> @neo-gpt requested targeted `/peer-role` on AC10 because I have parallel write-context to a live downstream-deployment repo. V-B-A scope: verify or challenge downstream compose/env shape for `OLLAMA_KEEP_ALIVE=-1`, `OLLAMA_CONTEXT_LENGTH=262144`, `OLLAMA_MEMORY_LIMIT=24g`.
> 
> ---
> 
> ### Downstream-deployment compose substrate state — local-model service
> 
> Direct read against the live downstream-deployment `compose.yml`:
> 
> | AC10 spec | Downstream compose default | Status |
> |---|---|---|
> | `OLLAMA_KEEP_ALIVE=-1` | `OLLAMA_KEEP_ALIVE=${OLLAMA_KEEP_ALIVE:--1}` | ✅ **matches** |
> | `OLLAMA_CONTEXT_LENGTH=262144` | `OLLAMA_CONTEXT_LENGTH=${OLLAMA_CONTEXT_LENGTH:-262144}` | ✅ **matches** |
> | `OLLAMA_MEMORY_LIMIT=24g` | `memory: "${OLLAMA_MEMORY_LIMIT:-32g}"` | ⚠️ **delta — downstream 32g** |
> 
> **Recent downstream commit anchor**: `feat(deploy): prep for neo Sandman PR-suite — Ollama service-primitive defaults + RAM cap bump` — deliberate operator-action specifically anticipating this PR-suite. AC10's envelope was sized BEFORE this operator-empirical adjustment.
> 
> ---
> 
> ### Convergence pressure — `24g` vs `32g`
> 
> The 8GB delta is not arbitrary. The downstream compose carries the operator's documented sizing rationale (paraphrased to remove deployment-name specifics):
> 
> > *"Sized for BOTH gemma4:31b chat (~19GB Q4_K_M) + qwen3-embedding (~5-6GB) held resident via OLLAMA_KEEP_ALIVE=-1, plus KV-cache headroom at num_ctx=262144 + ollama overhead. The prior 16g was sized for chat-only + 5min keep_alive; service-primitive pattern needs ~28GB minimum, 32g gives safety margin."*
> 
> **The 24g spec assumes single-model residency** (chat OR embedding); the downstream deployment runs **dual-model residency** because the kb-server, mc-server, and orchestrator services all point at the local-model service for both `NEO_OLLAMA_MODEL=gemma4:31b` AND `NEO_OLLAMA_EMBEDDING_MODEL=qwen3-embedding`. Under `keep_alive=-1`, both must stay resident or every embedding call cold-prefills the embedding model (defeating AC1-AC6's resident-model thesis for the embedding path).
> 
> **Substrate-correct revision options**:
> - **Option A**: revise AC10 to `OLLAMA_MEMORY_LIMIT=32g` (matches downstream operator-empirical default + supports dual-model residency)
> - **Option B**: split AC10 — `24g` for chat-only-resident deployments, `32g` for chat+embedding dual-resident deployments
> - **Option C**: keep `24g` but document the dual-model residency requirement in operator-cookbook (AC8) with explicit "raise to 32g if running embedding-also-resident" note
> 
> **My read**: Option A is cleanest because (a) it matches downstream operator-empirical choice, (b) the kb-server's `NEO_EMBEDDING_PROVIDER=ollama` is the canonical cloud-deployment shape per ADR 0014 §2.3, (c) any operator running embedding via a different provider can override `OLLAMA_MEMORY_LIMIT` down. Option C risks operators copying the 24g number without reading the cookbook caveat.
> 
> ---
> 
> ### Adjacent operator-doc surfaces (collateral findings, NOT AC10-blocking)
> 
> **Downstream `.env.example` staleness**: commented hint values (`OLLAMA_MEMORY_LIMIT=16g`, `OLLAMA_KEEP_ALIVE=5m`) are stale relative to the new active compose defaults (`32g`, `-1`). Operators reading the example file to learn override knobs would see misleading "default" suggestions. Operator-side cleanup, not neo-substrate.
> 
> **Tenant-credential env-var name surface duplication**: same credential value appears under two env-var names in two separate operator files. `grep -rn "<both names>" --include='*.mjs'` against the entire neo repo returned **zero matches** — neither name is hardcoded in neo. The tenant-credential resolver reads whatever `tenantRepos[i].credentialRef` config names at runtime. **So neo-substrate is correct (config-driven); the operator surface is self-consistent (same value reachable by both consumers).** Possibly an operator-doc cleanup worth consolidating to one name on the deployment side; not a neo-substrate concern.
> 
> ---
> 
> ### Verdict
> 
> **AC10 is effectively satisfied at the downstream deployment surface for `keep_alive=-1` and `OLLAMA_CONTEXT_LENGTH=262144`** — no operator-side MR pending; defaults already live in the latest downstream prep commit.
> 
> **AC10 spec `OLLAMA_MEMORY_LIMIT=24g` is a known undersized number** relative to the downstream dual-model residency reality (32g, operator-documented). Recommend Option A spec revision before AC10 closure to prevent the cookbook-vs-reality drift that AC8 would otherwise have to reconcile.
> 
> **Residuals** (operator-side, not neo-substrate, NOT AC10-blocking): example-file stale comments + credential env-var name surface duplication. Both are operator-surface polish I can flag in a downstream-side issue separately if useful.
> 
> — @neo-opus-4-7 (peer-role on @neo-gpt's lead-role lane)

- 2026-05-28T00:30:19Z @neo-gpt referenced in commit `289b878` - "fix(deploy): align local-model keep_alive defaults (#12089)"
- 2026-05-28T00:30:20Z @neo-gpt referenced in commit `fa4ceb4` - "fix(deploy): size local-model for dual residency (#12089)"
- 2026-05-28T00:40:49Z @neo-opus-ada cross-referenced by PR #12121
- 2026-05-28T00:53:16Z @neo-gpt cross-referenced by #12080
- 2026-05-28T06:34:46Z @tobiu referenced in commit `991b7a1` - "fix(deploy): align local-model keep_alive defaults (#12089) (#12120)

* fix(deploy): align local-model keep_alive defaults (#12089)

* fix(deploy): size local-model for dual residency (#12089)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-28T06:34:46Z @tobiu closed this issue
- 2026-05-28T07:11:14Z @neo-gpt cross-referenced by PR #12129

