---
id: 17421
title: 'Remove apps/devindex, its guides, SCSS and tests from the neo repo'
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - tech-debt
assignees:
  - neo-opus-grace
createdAt: '2026-08-20T15:07:12Z'
updatedAt: '2026-08-20T17:41:55Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17421'
author: neo-opus-grace
commentsCount: 3
parentIssue: 17238
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Remove apps/devindex, its guides, SCSS and tests from the neo repo

## Context

Operator, 2026-08-20, after testing the app from `dev`: *"tested the devindex app after pulling from dev => works fine. first real friction item with the new repo => it will stay broken there, until the next neo release. on the bright side: we can now remove the app, its learn items, scss, and tests from the neo repo (vba if all tests (not just unit) are there)."*

This is the terminal step of neomjs/neo#17238. `neomjs/devindex` owns the app, its Data Factory and its own tickets and PRs; what remains here is a copy that is **behind** the destination (`config.publishedWorkingSet` exists only there — see neomjs/neo#17238's revision comment).

**The requested V-B-A came back negative.** Recorded here as the ticket's own precondition rather than discovered mid-removal.

Live latest-open sweep: latest 20 open issues at 2026-08-20T14:45Z, no equivalent. A2A in-flight claim sweep over the last 30 messages: no overlapping `[lane-claim]`.

## The Problem — the tests are NOT all there

| suite | in neo | in `neomjs/devindex` | gap |
|---|---|---|---|
| unit (`unit/app/devindex/`) | 9 | 9 | **not the same 9** |
| e2e | 5 | **0** | **no e2e harness exists there at all** |

**Unit — one spec exists only here.** `StoragePublishedIndex.spec.mjs` (8 tests) covers neomjs/neo#17374's published-artifact **read** path: fetched-vs-checkout record parity, the single declared config URL, fallback-with-disclosure on failure, non-2xx handling, an unparseable body on the bootstrap run, digest mismatch, absence-of-provenance, and curated files untouched.

`neomjs/devindex` has a spec neo does not — `StorageWriteDirectory.spec.mjs` — but it is a **different subject** (atomic write, parent-dir creation, temp-file cleanup, the empty-index door). The two do not overlap. Deleting neo's copy drops the read-path coverage entirely, and that path is exactly the mechanism neomjs/neo#17394 is about.

**E2E — none of the five have a home.** `neomjs/devindex` has one playwright config (`playwright.config.unit.mjs`) and one script (`test-unit`). There is no e2e config, no e2e directory, no `test-e2e`.

All five drive `/apps/devindex/` **exclusively** — not one has an alternative target:

| spec | devindex-specific dependency |
|---|---|
| `grid/ThumbDragDevIndex.spec.mjs` | the app itself; named for it |
| `grid/ThumbDrag.spec.mjs` | `devindex-stop-stream-button` |
| `grid/ThumbDragPause.spec.mjs` | 50,000 rows |
| `benchmarks/GridScrollBenchmark.spec.mjs` | `devindex-stop-stream-button` |
| `benchmarks/GridProfile.spec.mjs` | `devindex-stop-stream-button` |

So this is not a `goto` rewrite. Three of the five click a control that only that app has, and `examples/grid/bigData` (20,000 rows / 50 columns) is not row-comparable with DevIndex's 50,000.

## The Architectural Reality

Removal surface, measured on `origin/dev`:

| area | count | note |
|---|---|---|
| `apps/devindex/**` | 49 files, **27 MB** | incl. the 23 MB still-tracked `users.jsonl` |
| `resources/scss/**/devindex/**` | 14 | 8 `src` + 3 dark + 3 light |
| `learn/guides/devindex/**` | 27 | plus **30** `learn/tree.json` entries |
| tests | 14 | 9 unit + 5 e2e |
| `apps/portal` registration | 6 | 4 `examples_*.json` + `sitemap.xml` + `llms.txt` |
| `buildScripts` + `ai/scripts` | 6 | webpack appworker dev/prod, esmodules, dataSyncPipeline, check-package-contents, retry-bound-registry |
| `package.json` | 6 scripts | `devindex:add|cleanup|optin|optout|spider|update` |

`resources/content/**` carries **0** devindex entries, so the generated mirror needs no prune.

## The Fix

Remove in one PR, with the four decisions below resolved rather than discovered.

**D1 — port `StoragePublishedIndex.spec.mjs` to `neomjs/devindex` first.** The code it covers lives only there. Order matters: port and prove green in the destination **before** deleting here, so the coverage is never absent from both.

**D2 — `ThumbDragDevIndex.spec.mjs` follows the app.** It is devindex-specific by construction. That requires standing up an e2e harness in `neomjs/devindex` (config + script + fixture), which is the larger half of this ticket and may deserve its own leaf.

**D3 — the three stream-dependent specs retarget to a neo-owned fixture.** `bigData` loads synchronously and has no stream, so the `devindex-stop-stream-button` step is not replaced — it is *unnecessary* there and drops out. **The benchmark series re-baselines at the cut** (50k→20k rows, 37→50 columns): the numbers before and after are not comparable and must not be read as a regression. That discontinuity is the real cost of the removal and is recorded here deliberately rather than left to be discovered as a mystery drop in a future comparison.

**D4 — the public URL is an operator call.** `apps/devindex` is a registered portal example with its own `sitemap.xml` entry at `https://neomjs.com/apps/devindex/index.html` and an `llms.txt` listing. Deregistering removes a live public URL; a redirect to the app's new home is the obvious disposition, but changing a published URL is not mine to decide unilaterally.

## Acceptance Criteria

- [ ] `StoragePublishedIndex.spec.mjs` runs green in `neomjs/devindex` **before** its removal here (D1)
- [ ] `apps/devindex/**`, its 14 SCSS files, its 27 learn guides and the 30 `learn/tree.json` entries are gone, and `npm run build-themes` plus the learn-tree lint both pass
- [ ] The three retargeted specs pass against a neo-owned fixture with no `devindex-*` selector remaining anywhere in `test/**`
- [ ] No `apps/devindex` reference survives outside deliberately-retained history — `git grep -l 'apps/devindex'` returns only files this ticket consciously keeps
- [ ] The six `devindex:*` npm scripts are gone and `npm run` still resolves
- [ ] The portal no longer registers the app, and D4's redirect disposition is recorded on this ticket before merge
- [ ] Full unit + e2e suites are re-run and every delta against the pre-removal baseline is accounted for

## Out of Scope

- Reclaiming the 23 MB from **history** — that is neomjs/neo#17376 and needs the one-shot rewrite; this ticket only stops the working tree carrying it
- `neomjs/devindex#1` (column resize) — that app stays broken until a `neo.mjs` release, which the operator has explicitly accepted as the trade for removing now
- Standing up the devindex e2e harness, if D2 is split into its own leaf

## Avoided Traps

**Do not delete the tests and "port them later."** The unit gap above is exactly how that ends: `StoragePublishedIndex` would be the only record of neomjs/neo#17374's read-path contract, and it would be deleted from the one repo that has it while the code it covers lives in the other.

**Do not silently retarget the benchmarks.** A benchmark whose fixture changed is a new series. Renaming the target without recording the discontinuity produces a future "performance regression" that is really a fixture change.

## Related

- neomjs/neo#17238 — the epic this closes out
- neomjs/neo#17376 — history reclaim (the other half of the size problem)
- neomjs/neo#17394 — the read-path defect `StoragePublishedIndex` covers
- `neomjs/devindex#1` — why the destination app is currently broken

Origin Session ID: 3e4f33e0-fb23-4a61-a2a0-7f396950f3d6

Handoff Retrieval Hints: `query_raw_memories("remove apps/devindex from neo test parity e2e harness")`. Anchors: `StoragePublishedIndex.spec.mjs`, `devindex-stop-stream-button`, `learn/tree.json`.


## Timeline

- 2026-08-20T15:07:14Z @neo-opus-grace added the `enhancement` label
- 2026-08-20T15:07:14Z @neo-opus-grace added the `ai` label
- 2026-08-20T15:07:14Z @neo-opus-grace added the `architecture` label
- 2026-08-20T15:07:14Z @neo-opus-grace added the `tech-debt` label
- 2026-08-20T15:07:22Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-20T15:07:34Z @neo-opus-grace added parent issue #17238
### @neo-opus-grace - 2026-08-20T15:10:50Z

## D1 is not a port — the destination's newer implementation has **zero** coverage

Started D1 (port `StoragePublishedIndex.spec.mjs` to `neomjs/devindex`, prove it green there, then delete here) and it does not survive contact. Correcting the body's D1 rather than discovering this mid-removal.

### The contract changed shape in the destination

| symbol | `neomjs/neo` | `neomjs/devindex` |
|---|---|---|
| `config.publishedIndex` | 2 | **0** |
| `config.publishedWorkingSet` | 0 | **4** |
| `config.paths.indexProvenance` | 3 | **0** |
| `Storage.digestOf` / `getBlocklist` / `getUsers` / `readJson` | present | present |

neo's spec reads `config.publishedIndex.url` — a single deployed file. devindex superseded that with `publishedWorkingSet`: a base URL plus **three** derived members (`users.jsonl`, `tracker.json`, `visited.json`) fetched through a manifest in `Storage#fetchAndAdoptWorkingSet()` (`Storage.mjs:394`). And `indexProvenance`, the file AC-3's digest-mismatch arm asserts against, **does not exist there at all**.

So a copy-with-rewritten-imports would fail on symbols that are gone, for the right reason.

### The part that actually matters

**No devindex spec covers the read path.** Measured across all nine, counting `publishedWorkingSet` / `baseUrl` / `fetch(`:

```
CleanupReconciliation      0     StoreFilterProfile        0
GitHubService              0     StoreRankCalculation      0
GridScrollProfile          0     UpdaterGraphqlBudget      0
OptInPhaseIsolation        0     UpdaterRenameRecovery     0
StorageWriteDirectory      0
```

`StorageWriteDirectory` — the spec devindex has and neo does not — covers the **write** side: atomic write, parent-dir creation, temp-file cleanup, the empty-index door. It never touches hydration.

**So the state is the inverse of what "the tests are already there" implies:**

- the **simpler, superseded** single-file implementation lives in neo with **8 tests**
- the **more complex** three-file manifest implementation lives in devindex with **none**

Deleting neo's copy removes the only tests that have ever existed for this contract family, and it removes them from the repo that no longer runs the code.

### Consequence for neomjs/neo#17394

That ticket — a completed opt-out silently reversed, reaching the published index — is a defect **in this hydration path**. It is currently sitting on code with no test coverage in the repository that owns it. That raises its risk materially and is worth knowing before it is picked up.

### Revised D1

Not a port. **Write read-path coverage in `neomjs/devindex` against `publishedWorkingSet`, using neo's spec as the specification of intent rather than as the artifact to move.** The eight properties it pins are all still meaningful, and four of them get *harder* with three members instead of one:

1. fetched and tree paths agree byte-for-byte — now per member
2. the request goes to the single declared base URL, never a reassembled one
3. a fetch failure falls back to the tree **and says so** — now: does one member failing fall back for that member, or for the whole set?
4. a non-2xx is a fallback, not a body to parse
5. an unparseable body falls back on the bootstrap run rather than throwing
6. a digest mismatch falls back rather than adopting an artifact we did not write — **needs a home now that `indexProvenance` is gone**
7. absence of provenance is not mismatch — the first run must proceed
8. curated files still come from the checkout, untouched

Items 3 and 6 are open questions about the new shape, not transcription. Item 6 in particular: if provenance verification was dropped rather than relocated, that is its own finding and belongs on neomjs/neo#17394 rather than here.

**This is a leaf, not a line item.** Splitting it out rather than letting the removal ticket carry unbounded test-authoring work.

Origin Session ID: 3e4f33e0-fb23-4a61-a2a0-7f396950f3d6


- 2026-08-20T15:10:52Z @neo-opus-grace cross-referenced by #17394
- 2026-08-20T15:24:17Z @neo-opus-grace cross-referenced by #17422
### @neo-opus-grace - 2026-08-20T15:24:40Z

## D2/D3 superseded — the tests MOVE, they do not retarget

Operator, 2026-08-20: *"we need to ensure ALL tests (not just unit ones) from neo that relate to devindex get moved over. once this is done, we can remove it from the neo repo. when that is done, we can finally reduce the bloated 5GB neo history."*

That fixes the ordering for the whole line — **move tests → remove app (this ticket) → reclaim history (neomjs/neo#17376)** — and it retires my D3.

**I withdraw the retarget proposal.** D3 had the three shared e2e specs re-pointed at `examples/grid/bigData` with the benchmark series re-baselining at the cut, and I wrote that discontinuity up as an accepted cost. Moving them is simply better: the specs keep driving the same app in its new home, so the benchmarks stay continuous and there is no discontinuity to remember. The cost I was about to accept was avoidable.

D2 folds into the same work — `ThumbDragDevIndex` was always going to follow the app; it just does so alongside four siblings and a harness rather than alone.

**Filed neomjs/neo#17422 as the executable leaf**, linked under the epic and assigned to me. This ticket is now gated on it.

One measurement that shrinks that leaf a lot, recorded here because it is the kind of thing that gets scoped off a directory listing: **none of the five e2e specs import `fixtures.mjs`.** They use plain `@playwright/test`; only `GridScrollBenchmark` pulls a single helper. So the harness port is ≈920 lines across seven dependency-light files, not the 682-line Neural Link fixture and everything behind it — and porting that fixture would hand devindex the exact whole-Brain import cost neomjs/neo#17369 exists to remove.

Revised gate on this ticket: **do not delete anything here until neomjs/neo#17422 is green in the destination.**

Origin Session ID: 3e4f33e0-fb23-4a61-a2a0-7f396950f3d6

- 2026-08-20T15:30:24Z @neo-opus-grace cross-referenced by PR #4
- 2026-08-20T17:40:38Z @neo-opus-grace cross-referenced by PR #17429
- 2026-08-20T17:41:29Z @neo-opus-grace cross-referenced by #17430
### @neo-opus-grace - 2026-08-20T17:41:55Z

PR open: #17429. Recording the four decisions' actual dispositions, since two resolved differently from how this ticket specified them, plus one the ticket did not have.

**D1 — satisfied by re-derivation, not the port specified here.** `StoragePublishedIndex.spec.mjs` pins the superseded single-file contract; `config.publishedIndex` and `config.paths.indexProvenance` do not exist in the destination, so porting it would have carried a spec that cannot run. `neomjs/devindex` instead carries `StorageWorkingSetHydration.spec.mjs` — 9 cases over the same read path, re-derived against the shape that runs, where one file became nine members and provenance moved into a fetched manifest. Mutation-verified twice. The ordering this ticket insisted on held: the coverage was green in the destination before it left here.

**D2 — done.** `neomjs/devindex#4` stood up the e2e harness (config + globalSetup + 5 specs, 11 passed).

**D3 — partly moot, and the ticket over-estimated the loss.** The plan was to retarget three specs to a neo-owned fixture. Measuring first showed `RowPinning.spec.mjs` already carries the *identical* "Scroll Telemetry: Visual Blanking and Jitter Detector" test against BigData, and `LockedCellHorizontalStability` / `ColumnOverdragScroll` cover the horizontal-scroll and overdrag halves on lockedColumns. So most of D3 was pre-satisfied and no retarget was needed.

Two properties did genuinely leave, and I filed them rather than write them into a PR paragraph: **#17430** — a paused thumb drag has no remaining coverage (`RowPinning` has exactly one test, the telemetry one), and neo now has no grid scroll benchmark (`HarnessEnduranceBenchmark` measures event-loop lag at `/ai/examples/harnessEndurance/`, a different subject).

**D4 — still yours, and NOT decided in the PR.** `https://neomjs.com/apps/devindex/index.html` plus ~20 `learn/guides/devindex/*` URLs stop resolving once this deploys. `sitemap.xml` and `llms.txt` are regenerated so the tree does not *advertise* dead URLs, but that is not the same as a redirect. A redirect to the app's new home is the obvious disposition and I have not made it — changing a published URL is not mine to decide unilaterally. This is the one open item on the PR.

**D5 — a blocker this ticket did not have.** `AGENTS_STARTUP.md` **Step 1** made `learn/guides/devindex/frontend/Architecture.md` a mandatory read for every agent, every session. Deleting it breaks every boot, and reverting Step 1 to `CodebaseOverview.md` regresses the quality that guide was introduced to fix.

My first instinct was to relocate and de-app the guide. Checking first showed that was wrong: both halves already exist as engine-canonical guides — `fundamentals/WorkerArchitecture.md` for the worker mechanics and `datahandling/StateProviders.md` for the hierarchical MVVM. The devindex guide was a **duplicate** of documentation this repo already had. Step 1 now points at the canonical pair and no new file was written. Had I not checked, this PR would have added a third copy of the same content.

**On the AC "no `apps/devindex` reference survives outside deliberately-retained history":** met, with the retained set named explicitly in the PR body. Worth flagging that a filename census finds one e2e spec and a content census finds nine — neither is the answer. The census that decided the migration was by **navigation target**: exactly five specs call `goto('/apps/devindex/…')`. Four others mention DevIndex in prose while driving `/examples/grid/lockedColumns/`; their comments were already stale before this change and are corrected rather than carried.

#17376 is now unblocked.


- 2026-08-20T17:45:14Z @neo-opus-grace referenced in commit `07abb97` - "chore(docs): regenerate the tracked class hierarchy after the devindex removal (#17421)

`extends` is hashed into every Knowledge Base chunk id, so committing the 49
deleted app classes without regenerating this file would make the next ingest
re-identify the affected chunks and mark the existing corpus stale.

Caught by `check-freshness` on PR #17429 rather than by me — the file is
generated but tracked, so "generated" was the wrong reason to leave it alone."

