---
id: 17285
title: mc-server reaches into a host-edge config for its lane-landscape census
state: CLOSED
labels:
  - bug
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-ada
createdAt: '2026-08-17T08:43:07Z'
updatedAt: '2026-08-18T09:32:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17285'
author: neo-opus-ada
commentsCount: 7
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-18T09:32:25Z'
---
# mc-server reaches into a host-edge config for its lane-landscape census

## Context

Raised by @tobiu against PR #17280: *"why are there 'assigned' and 'unassigned' flags inside the memory core? this is NOT the github workflow server."*

The question is about `explore_lane_landscape`'s response schema, but the schema is downstream of a placement decision.

## The Problem

`explore_lane_landscape` is registered on the **Memory Core** MCP server (`ai/mcp/server/memory-core/toolService.mjs:374`). To serve it, that file imports:

| import | from | what it is |
|---|---|---|
| `GraphqlService` | `services/github-workflow/` | the GitHub transport |
| `PullRequestHistoryService` | `services/github-workflow/` | PR history reads |
| `makeOpenWorkCensusReader` | `services/github-workflow/` | the open-issue / open-PR page readers |
| **`GitHubWorkflowConfig`** | **`../github-workflow/config.mjs`** | **the sibling MCP server's config module** |

The last row is the one that is structurally distinct from the others. A service import is ordinary reuse; reaching into another *server's* config is not.

**Measured, not asserted:** grepping every `ai/mcp/server/*/toolService.mjs` for a sibling-server config import (`from '../<server>/config.mjs'`) returns **exactly one hit** — this one. It is not an established pattern in the tree. It is used at `:111-114` for `owner`, `repo`, `maxAssigneesPerIssue`, `maxLabelsPerIssue`.

The published response schema then carries the consequence: `authorityCoverage.assignedCount` / `unassignedCount` / `unassignedIds` are **pure GitHub issue-assignee facts** with no Memory Core content in them. Memory Core's genuine contribution to the landscape is the other half — the Native Edge Graph relation edges.

## The crossing is deliberate and documented — which is why this is a design question, not a bug report

`toolService.mjs` states the split explicitly:

> *"GitHub repository/query fan-out belongs to the GitHub Workflow child; census traversal bounds belong to the Memory Core child. Reading both at call time preserves reactive overlay/env resolution and fails loud when either provider is not wired."*

and frames the tool as *"the CURRENT-STATE Bird View, the third self-awareness context slot beside the two historical views."*

That reasoning is sound **about config resolution** — each leaf is read from the child Provider that owns it, at call time, per ADR-0019. It does not address **where the tool should live**. "This tool is about agent self-awareness" is a claim about purpose; "this tool's data is GitHub's" is a claim about domain. The current placement optimises the first and pays for it in the second.

No ADR governs MCP tool domain ownership. The two decision records touching `github-workflow` — `0004-github-content-architecture` and `0039-two-plane-sdk-barrel-boundary` — settle neither.

## The Fork

This ticket does not presume the answer; the tool genuinely joins two domains and reasonable people place it differently.

1. **Move the tool to the GitHub Workflow server.** The census, the readers and the config already live there. Cost: it loses cheap access to the Native Edge Graph edges, which is the half that makes it a *landscape* rather than a list.
2. **Keep it on Memory Core, but consume the sibling as a service rather than importing its config.** The self-awareness framing survives; the cross-server config reach — the part with no precedent — does not. Requires deciding how one server asks another for `owner`/`repo`/fan-out bounds.
3. **Split the composition.** The GitHub Workflow server owns and exposes the open-work census; Memory Core joins it with graph edges. Two tools, each in its own domain, at the cost of a round trip.

Whichever is chosen, the sibling-config import at `:33` should not survive as the mechanism — it is the concrete anomaly regardless of where the tool lands.

> [!IMPORTANT]
> **Scope narrowed to region A. Everything below the Context was written before the fork was resolved and is RETAINED, not deleted** — the reasoning is why the current scope is what it is, and deleting it would send the next reader to re-derive it.
>
> **What this ticket is now:** the three `github-workflow` imports serving `explore_lane_landscape`'s open-work census (`GitHubWorkflowConfig`, `makeOpenWorkCensusReader`, `GraphqlService`). Nothing else.
>
> **What left scope:** `PullRequestHistoryService` — the fourth import. Its replacement is the credentialed, provider-agnostic conversation source shaped in Discussion #17301, which is architecture rather than a deletion.
>
> **The fork below is resolved and it was neither branch** — operator direction: conversations reach the container through multi-tenant ingestion using the tenant's own credentials, so no plane-neutral extraction is needed and no capability is surrendered. See the resolution comment for the full disposition.

## Acceptance Criteria (current — region A)

- [ ] `ai/mcp/server/memory-core/toolService.mjs` imports no `github-workflow` module on the census path: `GitHubWorkflowConfig`, `makeOpenWorkCensusReader` and `GraphqlService` are gone.
- [ ] `explore_lane_landscape` still resolves rather than throwing — the census source is fail-closed on its injections, so removing the readers would take the tool down along with the violation.
- [ ] The census reports the count as **unknown**, never zero: an unreachable source yields `exhausted: false` with a reason, and the landscape withholds its narrative with the cause attached. A stand-in returning an empty page would report proven exhaustion and assert zero open work.
- [ ] The refusal names the boundary (host-edge capability) rather than a generic degradation, and reaches an operator through the walk's reason.
- [ ] Unit coverage includes a positive control proving an empty reader really does report `exhausted: true` — without it, the refusal assertion proves nothing.

## Acceptance Criteria (SUPERSEDED — written against the unresolved fork; retained for provenance)

- [ ] The fork is decided and recorded where a reader of either server will find it — the absence of a rule is why this drifted.
- [ ] `ai/mcp/server/memory-core/toolService.mjs` no longer imports `../github-workflow/config.mjs`; whatever replaces it is a mechanism the tree can have more than one of.
- [ ] The response schema's GitHub-domain fields are served by whichever server the fork assigns the domain to — a consumer should not have to know that assignee facts arrive from the memory server.
- [ ] If the tool stays on Memory Core: the reason is written at the tool boundary as a domain decision, not only as a config-resolution note.

## Out of Scope

- The degraded-count honesty fix (PR #17280 / #17278). It adds no import and creates none of this coupling; it stops an already-published schema from declaring `integer` for a field that returns `null`, and that correction travels with the tool wherever it lands.
- `explore_pull_request_history` and the other historical views, unless the fork's answer implicates them — `PullRequestHistoryService` is imported on the same line family and may share the question. Worth checking once the rule exists rather than assuming it does.

## Avoided Traps

- **Reading the documented rationale as a decision.** The comment explains config resolution correctly and was never a placement ruling; treating it as one is how the anomaly kept its cover.
- **Moving the tool to make the import go away.** The import is the symptom that made this visible; the domain question is the thing to settle, and a relocation that leaves the domains still mixed has spent the churn for nothing.

## Related

`ai/mcp/server/memory-core/toolService.mjs:33, :111-114, :374` · `ai/services/github-workflow/openWorkCensusReader.mjs` · `ai/services/graph/laneLandscapeProjection.mjs` · `ai/mcp/server/memory-core/openapi.yaml` `/lanes/landscape` · #17278 / PR #17280 (the schema-honesty fix) · #17283 (the credential fork on the same tool) · ADR 0004, ADR 0039

Live latest-open sweep: open queue checked 2026-08-17T08:3xZ; #17283 is the nearest neighbour (same tool, different question — that one is the plane's outbound credential posture, this one is domain ownership). Not a duplicate.

Origin Session ID: 80b326bf-b37a-4efd-8313-1a9eae09e9c4

Retrieval Hint: `query_raw_memories("explore_lane_landscape memory-core hosts github census sibling server config import assignedCount")`


## Timeline

- 2026-08-17T08:43:09Z @neo-opus-ada added the `bug` label
- 2026-08-17T08:43:09Z @neo-opus-ada added the `ai` label
- 2026-08-17T08:43:09Z @neo-opus-ada added the `architecture` label
- 2026-08-17T08:43:09Z @neo-opus-ada added the `agent-os` label
### @neo-opus-ada - 2026-08-17T08:48:23Z

## ⚠️ Correction — this is a PLANE violation, not a domain-ownership question

**@tobiu, escalating the original finding:** *"ADA! MC => INSIDE DOCKER, cloud plane. github and gitlab mcp servers => HOST EDGE. local."*

He is right, and it invalidates the framing this ticket opened with. I filed it as "which server should own this domain" — a design preference with three defensible shapes. It is not that. The two servers **run in different runtime planes by construction**, so the import is not a boundary someone chose loosely; it is a boundary that cannot hold.

### Verified mechanically

`ai/deploy/docker-compose*.yml` service list: `chroma`, `kb-server`, **`mc-server`**, `orchestrator`, `fleet-server`, `ingress`, `local-model`.

| MCP server | compose files | plane |
|---|---|---|
| `memory-core` | 3 | **cloud — containerized as `mc-server`** |
| `knowledge-base` | 4 | cloud (`kb-server`) |
| **`github-workflow`** | **0** | **host edge** |
| **`gitlab-workflow`** | **0** | **host edge** |
| `file-system` | 0 | host edge |

So `ai/mcp/server/memory-core/toolService.mjs:33` —

```js
import GitHubWorkflowConfig from '../github-workflow/config.mjs';   // module scope
```

— is a **cloud-plane container reaching into a host-edge server's config module, at module load**. Not a service it could call over a boundary: a filesystem import across a plane split.

### What this explains that the original framing missed

**#17283 is not a fork, and I filed it as one.** I wrote that ticket as *"the plane may be deliberately credential-free — both readings are defensible"* and asked someone to decide. The topology already decided: the container holds no GitHub credential **because git-provider access is a host-edge concern by design**. That ticket's option 2 is not one candidate among two — it is the answer, and I did not know the topology well enough to see it. Correcting it there.

**The census is permanently degraded in the cloud plane, by construction.** Not a transient GitHub outage — the steady state for a containerized MC. That makes the honesty fix in PR #17280 load-bearing rather than cosmetic: without it, every containerized Memory Core reports `totalOpenItems: 0` forever and every seat reading it concludes the backlog is empty.

**Why I could not see it, which is the part worth keeping.** I measured everything in a single dev checkout, where the cloud plane and the host edge sit in one filesystem and every cross-plane import resolves cleanly. A dev checkout **collapses the plane distinction**, so a plane violation is indistinguishable from an ordinary relative import — it type-checks, it lints, it boots, and the specifier looks like every other sibling reference in the file. The empirical test I ran (remove the file → `ERR_MODULE_NOT_FOUND`) proved the coupling was hard, but it could not tell me the two ends live in different runtimes.

### The fork is narrower than this ticket first claimed

Option 1 as originally written — *"move the tool to the GitHub Workflow server"* — now reads differently: it means moving a self-awareness tool out of the cloud plane and onto the host edge, which is a topology decision about where lane-awareness lives, not a file relocation.

What survives unchanged: **the `:33` import cannot be the mechanism**. A cloud-plane service that needs host-edge facts asks for them across a boundary or does not have them.

### Unchanged

PR #17280 stays merge-eligible and is now *more* clearly correct: it adds no import, creates none of this coupling, and it is what stops a permanently-degraded cloud-plane census from reading as an empty backlog.


- 2026-08-17T08:48:24Z @neo-opus-ada changed title from **The Memory Core server hosts a GitHub open-work census and imports the sibling MCP server's config — the only cross-server config reach in the tree** to **Cloud-plane `mc-server` imports a HOST-EDGE server's config at module scope — a plane violation, not a domain preference**
- 2026-08-17T08:48:49Z @neo-opus-ada cross-referenced by #17283
### @neo-opus-ada - 2026-08-17T09:21:32Z

## Plane-crossing census — measured, whole tree. The mixture is **one line**.

@tobiu asked whether to dive into the cloud/host-edge mixture ahead of a refactor. Measured first, and the answer changes the plan: **there is no mixture to refactor.** There is one import.

### The oracle, read from deployment rather than from directory names

A server is container-plane iff a `docker-compose*.yml` builds it via `TARGET_SERVER: <name>`. Grepping every compose file yields exactly two:

| plane | servers |
|---|---|
| **container** | `memory-core`, `knowledge-base` |
| **host edge** | `github-workflow`, `gitlab-workflow`, `file-system`, `neural-link` |

`neural-link`'s compose mentions are config plane-member references, not a build — so it is host-edge, which fits a browser driver.

### Result: 748 tracked `.mjs` under `ai/`, 1 crossing

AST-based (static + dynamic imports), whole tree rather than entrypoint closures:

```
scanned 748 tracked .mjs files under ai/
server-to-server PLANE CROSSINGS: 1
  [container -> host-edge] ai/mcp/server/memory-core/toolService.mjs:33
      -> ai/mcp/server/github-workflow/config.mjs
```

That is the line this ticket already names. Nothing else in `ai/` crosses.

### What the surrounding structure actually looks like

Because "one crossing" invites the question *"then why did it feel systemic?"*:

| pattern | count | verdict |
|---|---|---|
| Service reads **its own** domain's server config (`ai/services/github-workflow/*` → `github-workflow/config.mjs`) | **58 files** | **cohesion, not mixture** |
| **Barrels** wiring several domains | 4 sites | **already plane-correct** — see below |
| Cross-domain config bindings, **container → container** | 6 | domain coupling, **no plane issue** |
| Cross-**plane** server-to-server | **1** | the defect |

**The barrels are the finding I did not expect.** `ai/services.mjs` reads only `knowledge-base/config.mjs` and `memory-core/config.mjs` — both container. `ai/services.host.mjs` reads only `github-workflow/config.mjs` and `neural-link/config.mjs` — both host-edge. The plane split is already encoded in *which barrel* a service is published through, and it holds perfectly. Whoever drew that line drew it correctly; `toolService.mjs:33` is the single place that bypasses it.

The 6 remaining cross-domain bindings are `knowledge-base` ×3, `graph` ×2 and `shared/vector` ×1 all reading `memory-core/config.mjs` — container→container, so plane-clean. Worth knowing for a domain refactor, irrelevant to this one.

### Correcting my own numbers, because the first one would have justified the wrong plan

This census read **182 → 134 → 68 → 10 → 1** as I fixed the unit three times:

1. **182** — I keyed the crossing's plane on *the entrypoint the walk started from* instead of *the importing file*, so `memory-core/logger.mjs → memory-core/config.mjs` counted as a crossing whenever the walk had entered via a host-edge server. That measured **reachability**, not adjacency.
2. **134** — plane-neutral shared code importing a server's private surface, which is a different smell entirely and had no business in a plane count.
3. **68** — config-only, still counting `ai/services/github-workflow/*` reading its own `github-workflow/config.mjs`, which is cohesion.
4. **10** → **1** — separating barrels and same-plane domain coupling from actual cross-plane adjacency.

**182 would have justified a refactor. 1 justifies a three-line change.** Recording the sequence because the first number was the one that felt actionable, and it was measuring something else — the same wrong-unit failure I hit twice earlier today on this branch.

### What this means for the fork above

The three options in this ticket were sized for a systemic problem. At n=1 the question narrows to: **what should `toolService.mjs:111-114` do instead**, given it needs `owner` / `repo` / `maxAssigneesPerIssue` / `maxLabelsPerIssue` for the lane-landscape census?

And #17283's correction answers it — the container should not be running that census at all, because git-provider access is host-edge by construction. So the likely shape is *not* "get the config another way", it is "the container-side path reports the capability unavailable and needs no provider config", which removes the import by removing the reason for it.

That is a behaviour change to a published tool, so it stays a decision rather than something I take unilaterally. But it is a small one, and it is the same disposition #17283 already landed on.

**Bound on this census, stated:** it covers static and dynamic relative imports across tracked `.mjs` under `ai/`. It does not cover runtime-assembled paths (`path.join(root, 'ai/mcp/server/...')`), which is the variant that bit me twice today and which no import-graph sweep can see. A literal-string grep is a separate pass if someone wants that closed.


- 2026-08-17T09:28:03Z @neo-opus-ada assigned to @neo-opus-ada
### @neo-opus-ada - 2026-08-17T09:42:37Z

## ⚠️ Third correction — do NOT strip the tools. The logic belongs in the cloud plane; the *import* was the miss.

**@tobiu, reframing again:** *"PullRequestHistoryService in general sounds useful, for github workflow mcp server tool… we can keep logic inside the cloud plane, just the github workflow service import was a miss."* Plus: this is the **Bird Views** topic, it wants to **embed PR conversation bodies**, and that connects to **multi-tenant repository ingestion**.

My previous disposition — remove `explore_pull_request_history` from MC, strip `explore_lane_landscape`'s census — was **wrong**, and wrong in the expensive direction: it would have deleted capability to fix an import.

### What the code actually shows

`PullRequestHistoryService.explorePullRequestHistory` has **two data legs**, not one:

| leg | source | works in a container? |
|---|---|---|
| `query` = `GraphqlService.query`, `rest` = `GraphqlService.rest` | **live GitHub API** | **No.** No container holds a git-provider credential (#17283) — this leg is dead in every deployment |
| `scanCorpus` = `scanPullRequestCorpus({pullsDir, archiveRoot})` | **the synced corpus on disk** | **Yes.** The corpus ships in the repo and needs no credential |

So the cloud-plane-legal path **already exists in the service**. The live-API leg is the miss, and the `aiConfig` defaults (`owner`, `repo`, `pullsDir`, `archiveRoot`, `productNameDenylist`) resolve through `github-workflow/config.mjs` — correct for a github-workflow service, wrong the instant `memory-core/toolService.mjs` imports it.

### Nothing operational regresses, which is what makes the narrow fix safe

Both GitHub-reaching legs are **already non-functional in the cloud plane**. The live symptom is the one that opened this whole thread: `explore_lane_landscape` returning `totalOpenItems: 0, degraded: true` with `Could not authenticate with GitHub`. Removing a credential-dependent leg from a container removes code that cannot run there — it is not a capability loss, it is deleting a path that only ever returned a degraded envelope.

### The ingestion connection, and a correction to the recollection

The operator's memory was *"blocked until neo itself becomes an ingestion tenant."* Measured, that is close but not the blocker:

- **Neo is already a tenant.** #16566: all three configured repos `checkpointStatus: complete`, one tenant (`tenantHash cf744f16ee7f`). The live problems there are a **starved lane** (`nextDueAt` ~11h past) and `KB_TENANT_REPO_ACCESS_EVIDENCE_EXPIRED` — not absence.
- **`tenant-repo-mirrors` is mounted by `orchestrator`** — cloud plane. So the ingested material is already on the right side of the boundary.
- **The real blocker is #17260** (unassigned epic): mirrors materialize **one envelope per file**, whole files run past the 14,336-token safe band, and *"the dispatch mechanism already exists end-to-end — what is missing is anything production-grade to dispatch to."*

So embedding PR conversation bodies is blocked on **chunking parsers (#17260)**, not tenant onboarding. Worth having precisely, because "onboard neo as a tenant" is work nobody needs to do.

### Corrected disposition — narrow

1. **Remove the four `github-workflow` imports from `ai/mcp/server/memory-core/toolService.mjs`** (`GraphqlService`, `PullRequestHistoryService`, `makeOpenWorkCensusReader`, `GitHubWorkflowConfig`). That is the whole plane violation and the whole of this ticket.
2. **Both tools stay on MC.** Synthesis stays cloud-plane, where the model is (`local-model`, Ollama on `neo-mcp-network`, allowlisted to `chroma, kb-server, mc-server, orchestrator` — nothing host-edge).
3. **The cloud-plane source is the corpus leg**, already implemented, plus the graph edges MC owns.
4. The live-API legs go host-edge with the service, where a credential exists — the service is already correctly homed in `ai/services/github-workflow/`; only MC's reach into it is wrong.
5. Quality of the corpus path improves via **#17260**; not a blocker for this ticket, and this ticket is not a blocker for it.

### What I got wrong, recorded because the pattern repeated

Three corrections on this ticket, all mine, all the same shape: I kept treating a **narrow defect** as evidence of a **systemic one**, and each proposed remedy was larger than the finding.

- Filed as "which server owns this domain" → it was a plane violation
- Reframed as systemic mixture → the census measured **1** crossing in 748 files
- Then proposed stripping two tools → the fix is deleting four import lines

The census that said 182 and the disposition that said "remove two tools" came from the same reflex: a confident large number arriving before the unit was right.


### @neo-opus-ada - 2026-08-17T10:12:15Z

## ⚠️ Retraction — "neo is already an ingestion tenant" was WRONG. Neo is deliberately excluded, and the operator's original recollection was right.

**@tobiu:** *"i highly doubt that it gets UPDATED inside our docker containers, but this you can easily explore inside our agent os on this machine."*

The doubt was correct and my claim does not survive it. I asserted neo was already a tenant from **#16566's body, measured 2026-08-07** — a ten-day-old snapshot cited as current state, which is the exact mistake I flag in other people's reviews.

### What the live plane says

`inspect_deployment`, snapshot age 14s:

```
tenantRepoSync: enabled=true  status=completed  repoCount=3  tenantCount=1
accessReadiness: 3/3 ready
all three repos: checkpointStatus=complete  outstanding=0  consecutiveFailures=0
lastRunAttemptAt 2026-08-17T09:21:59Z  nextDueAt 09:55:05Z
```

So the lane is **healthy and current** — the starvation #16566 described (`nextDueAt` ~11h past) is gone. That part of the old picture is stale in the other direction.

But none of the three `lastIngestedRev` values (`d8ae9ffa41ac`, `b5c9fe7d712d`, `a895dad15de7`) resolve as commits in this repo, and the snapshot identifies repos by hash only. So it **cannot** confirm neo is among them — which is where I should have stopped instead of leaning on the old ticket.

### The config settles it, and it settles it against me

`ai/deploy/kb-config.yaml:16-23`:

> *"The Neo repo itself is **deliberately NOT registered here**… **Neo returns as a tenant once sources and parsers are declarable per tenant**, so it can use our own extractors instead of the raw-file fallback; that is sequencing, not a retreat from the goal."*

The three tenants are `neo-shared/create-app`, `neo-shared/devindex-opt-in`, `neo-shared/devindex-opt-out` — all **external** to the maintainer checkout, which is stated as the lane's actual purpose. Neo is not one of them.

**So the operator's original framing — *"blocked until neo itself becomes an ingestion tenant"* — was accurate, and my "correction" of it was the error.**

### The hazard neither of us had named, and it is the good part

The config explains *why* neo is excluded, and it is sharper than "not done yet":

> *"A pull-mode entry for the same repo declares no parser, so it falls through to `RawRepoSource` and yields untyped raw-file chunks — a second, weaker corpus under the **SAME `{tenantId, repoSlug}` stamp** that `kbSync` resolves to by default. **Each lane then classifies the other's rows as stale and deletes them.**"*

Registering neo as a tenant today does not merely duplicate the corpus — it creates **two lanes that mutually delete each other's rows**. That is a live data-destruction hazard behind a config entry, not a sequencing preference. Anyone reaching for "just add neo to kb-config.yaml" needs that sentence in front of them.

### Net effect on this ticket

**None on the code disposition.** The fix is still the four import lines: the cross-plane reach is wrong regardless of where the corpus eventually comes from.

**Corrected on the blocker, though**, because I published the wrong version above: embedding PR conversation bodies via ingestion is gated on **per-tenant declarable sources AND parsers** — #17260 is the parser half, and the neo-as-tenant half is real and additionally gated on resolving the `{tenantId, repoSlug}` collision with `kbSync`. Not "parsers, not onboarding." Both, in that order.

### Score on this ticket

Four corrections now, all mine: domain-ownership → plane violation → systemic mixture (census said 1 of 748) → strip two tools (fix is four lines) → and this one, where I overwrote a correct operator recollection with a stale ticket body. The first four were over-scoping. This one was worse: I contradicted someone who was right, using evidence I had not freshness-checked.


### @neo-opus-ada - 2026-08-17T12:41:20Z

## ⚠️ Fourth correction — "delete the four imports" does not remove the crossing. It relocates it one level deeper.

Picking this lane up and re-measuring the disposition before writing code. The prescription above — *"Remove the four `github-workflow` imports from `ai/mcp/server/memory-core/toolService.mjs`. That is the whole plane violation and the whole of this ticket"* — does not survive.

### What the corpus leg actually costs to reach

The disposition rests on *"the cloud-plane source is the corpus leg, already implemented."* The logic exists, and it is exported:

```
ai/services/github-workflow/PullRequestHistoryService.mjs:742
    export async function scanPullRequestCorpus({pullsDir, archiveRoot, sources})
```

But its **module** is host-edge by construction:

```
ai/services/github-workflow/PullRequestHistoryService.mjs:6
    import aiConfig       from '../../mcp/server/github-workflow/config.mjs';
:7  import GraphqlService from './GraphqlService.mjs';
```

Both at module scope. So a container importing `scanPullRequestCorpus` still executes `github-workflow/config.mjs` at load. **The crossing does not go away — it stops being adjacent and becomes transitive.** Deleting line 33 while importing the corpus function would make the census in this ticket report `0` while the violation is fully live, which is the same wrong-unit failure the census sequence (182 → 1) already recorded once.

Stating the bound on my own instrument, since it is the thing that misled me: that AST census measured **direct adjacency between server directories**. A transitive edge through a service module is invisible to it, and I described it as "whole tree" — it was whole-tree in *file coverage*, not in *path depth*.

### So the fork is real, and it is not a file relocation

1. **Extract a plane-neutral corpus reader** — `scanPullRequestCorpus` moves to a module importing no `github-workflow` config, taking `pullsDir` / `archiveRoot` as arguments. Precedent exists: `ai/services/graph/issueFocusSections.mjs` already reads `pullsDir` from the graph plane. Cost: touches a host-edge service's internals; the caller must then supply the paths, which means deciding which config owns them on the container side.
2. **MC does not run the corpus leg** — report the capability unavailable, per the disposition two comments up. Cost: contradicts *"both tools stay on MC"*, so `explore_pull_request_history` loses its only container-legal source.

Option 1 preserves the capability the operator explicitly asked to keep (*"we can keep logic inside the cloud plane, just the github workflow service import was a miss"*), so it is the one that matches the stated intent. It is also strictly more work than this ticket claims.

### Region A is genuinely small, and it can go first

The `explore_lane_landscape` half needs no extraction. `laneLandscapeCensusWalk.mjs:17` records that a bound returns `exhausted: false` with a reason and *"callers derive `degraded` from THAT"* — and `exploreLaneLandscape.mjs:57-60` already turns `coverage.degraded` into a withheld narrative with `unavailableReason`. A census source that is structurally unreachable in-container flows through the path #17280 built, including its `unknown`-not-zero counts. That removes `GitHubWorkflowConfig`, `makeOpenWorkCensusReader` and `GraphqlService` — three of the four — with no new module.

`PullRequestHistoryService` is the fourth and the only one needing the fork above.

### Recorded because it is the fourth correction on this ticket, all mine

Filed as domain ownership → it was a plane violation. Reframed as systemic → the census measured 1. Then proposed stripping two tools → the operator's reframe kept them. Now: the narrow fix I landed on relocates the defect instead of removing it. The consistent error is not the direction — it is that each disposition was written from a measurement whose unit I had not yet checked.

Splitting this is the honest next step rather than one PR claiming to close it: region A is one PR against this ticket; the corpus extraction is its own scoped work. Not filing that split until the operator weighs option 1 against option 2, since it decides whether a published tool keeps a source.


### @neo-opus-ada - 2026-08-17T13:17:46Z

## Fork resolved by operator direction — it is neither option. The import goes away by removing the reason for it.

**@tobiu:** *"multi-tenant ingestion => we get credentials for ingested repos, which enables us to get pr bodies (github or gitlab) as needed. the old kbSync logic will get replaced by a multi-tenant version. github workflow host-edge services are not the way for the MC docker container."*

That answers the fork I posted above, and it rejects both branches of it:

- **Option 1 — extract a plane-neutral corpus reader out of the host-edge service.** Declined. It keeps the container's conversation data flowing out of a github-workflow module, just through a cleaner door. It is also provider-locked: the extracted reader would still read a GitHub-shaped corpus, so a GitLab tenant gets nothing.
- **Option 2 — MC reports the capability unavailable.** Declined. It gives up the capability rather than sourcing it correctly.

**The actual shape:** conversations reach the container through **multi-tenant ingestion**, using the tenant's own credentials, behind a provider-agnostic source contract — the same declared-capability shape `customParsers` now has after #17294. No github-workflow module is imported, transitively or otherwise, because nothing provider-specific belongs in the container at all.

That also disposes of the transitive-crossing problem I raised in the previous comment: there is no need to reach `scanPullRequestCorpus`, so its module-scope `import aiConfig from '../../mcp/server/github-workflow/config.mjs'` stops being something this ticket has to work around.

### What stays in scope here, and it is now genuinely small

Region A only — the three imports serving `explore_lane_landscape`'s census (`GitHubWorkflowConfig`, `makeOpenWorkCensusReader`, `GraphqlService`). The seam already exists and needs no new module: `laneLandscapeCensusWalk.mjs:17` records that a bound returns `exhausted: false` with a reason and *"callers derive `degraded` from THAT"*, and `exploreLaneLandscape.mjs:57-60` already turns `coverage.degraded` into a withheld narrative with an `unavailableReason`. A census source that is structurally unreachable in-container flows through the path #17280 built, including its `unknown`-rather-than-zero counts. This is also what #17283 already landed on: git-provider reads are host-edge by design, so the container should report *unavailable on this plane* rather than *degraded*.

### What leaves scope

`PullRequestHistoryService` — the fourth import. Its replacement is the credentialed, provider-agnostic conversation source, which is architecture rather than a deletion, and it is now shaped in **Discussion #17301** (*multi-tenant ingestion becomes the only ingestion path*). Not filing a ticket for it: that Discussion is pre-quorum, cross-family signal is unavailable today, and a ticket now would create the premature center-of-gravity §1d exists to prevent.

Retitling is warranted once region A is the whole of this ticket, and I will do that rather than let the title keep promising the larger fix. Keeping the ticket open and narrow instead of closing and refiling, so the four corrections above stay attached to the work they corrected.


- 2026-08-17T16:21:40Z @neo-fable-clio cross-referenced by #17304
- 2026-08-17T19:57:43Z @neo-opus-ada changed title from **Cloud-plane `mc-server` imports a HOST-EDGE server's config at module scope — a plane violation, not a domain preference** to **mc-server reaches into a host-edge config for its lane-landscape census**
### @neo-opus-ada - 2026-08-17T19:59:46Z

## Retitled, and one refinement to region A's shape before it gets built

Title is now scoped to what this ticket actually is. It had been promising the larger fix that left scope into D#17301, per the disposition above.

### The refinement: "remove the three imports" is not sufficient, and the naive version throws

I wrote above that region A *"needs no new module"* and flows through the seam #17280 built. The no-new-module half holds. The implied *"so just delete the wiring"* half does not:

```
ai/services/graph/laneLandscapeCensusSource.mjs:63
    throw new Error('makeLandscapeCensusSource: an injected `fetchIssuesPage` is required')
:66 throw new Error('makeLandscapeCensusSource: an injected `fetchPullRequestsPage` is required')
```

`makeLandscapeCensusSource` is fail-closed on its injected fetchers. Deleting the `makeOpenWorkCensusReader` / `GraphqlService` wiring at `toolService.mjs:128-137` therefore does not produce a degraded census — it produces a **throwing tool**. The plane violation would be gone and `explore_lane_landscape` would be broken, which is not a trade this ticket is entitled to make.

### And the obvious repair is the defect #17278 just removed

The tempting fix is a stub fetcher returning `{items: [], hasNextPage: false}`. That reports `exhausted: true` with **zero** open issues and zero open PRs — a confident, wrong, current-state answer. It is precisely the zero-instead-of-unknown failure `#17278` landed two commits to remove (`3645966047`, `9e8f6f0bf6`).

### The shape that is actually correct

Inject a fetcher that **refuses** rather than one that returns nothing. Per `laneLandscapeCensusWalk.mjs:14-21`, a failed page yields `exhausted: false` with a reason, and *"callers derive `degraded` from THAT"* — which is the existing path to `exploreLaneLandscape.mjs:57-60`'s withheld narrative and `unavailableReason`. Counts stay `unknown`, the narrative is withheld with a stated cause, and no `github-workflow` module is imported.

So region A is still small and still adds no module, but it is a **substitution** rather than a deletion: the host-edge census reader is replaced by a plane-explicit refusing source, not removed.

### Vocabulary note — deliberately NOT fixed here

The refusal will surface as `coverage-degraded`, and `#17283` is open on exactly the argument that a container should report *unavailable on this plane* rather than *degraded*, because a git-provider read is host-edge by design rather than a partial failure. That rename is a vocabulary change across the whole seam, not a property of these three imports, and `#17283` is unassigned. Pre-empting it inside this PR would widen a narrow ticket into someone else's. This ticket uses the seam as it exists today; `#17283` renames it everywhere at once.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


- 2026-08-17T20:52:54Z @neo-opus-ada cross-referenced by PR #17324
- 2026-08-17T21:27:58Z @neo-opus-ada referenced in commit `7c57bff` - "test(ai): the lane-landscape config seam pins ONE owning domain, and says the absence is the contract (#17285)

Caught by @neo-opus-grace on the PR: `unit` was red and deterministically so --
it failed on two retries, and retries get fresh workers, so the cross-file-leak
family chewing on [unit-brain] this evening would have healed. This did not.

McpServerToolLimits.spec.mjs pinned readLaneLandscapeConfig() returning BOTH child-config
domains. That two-domain contract is exactly what the census fix retires: the second
domain came from the GitHub Workflow child, a host-edge provider this cloud-plane server
must not read. The spec was not in the changed-file set, so the older contract stood
where the boundary had moved.

DELETING THE ASSERTION WOULD HAVE GONE GREEN AND COST THE GUARD. Nothing at that seam
would then pin that the host-edge child is NOT read from here, and the plane violation
could walk back in with the spec still green. So the assertion is retargeted rather than
removed, and the test is RENAMED -- its old title asserted the superseded contract to
every future reader while the body asserted the new one.

On the added key-set assertion: `toEqual` already fails on a re-added domain, so that
line is not what CATCHES a regression. It states that the absence is the contract, and
it earns its place against a future loosening to `toMatchObject`, which would leave the
assertion looking intact while silently permitting a second domain back in.

METHOD CORRECTION, which is the transferable part. My earlier verification ran the
lane-landscape specs -- scoped by my own model of relatedness, which never reaches a file
called McpServerToolLimits. Re-swept by CHANGED SYMBOL instead of by feature name
(readLaneLandscapeConfig, makeOpenWorkCensusReader, makeLandscapeCensusSource,
makeRefusingCensusPageReader) and ran every spec it found: 112 pass."
- 2026-08-17T21:46:56Z @neo-opus-ada referenced in commit `68be9ae` - "fix(ai): a source that is out of reach by design is typed, not just worded (#17285)

Caught by @neo-opus-grace in review, and it is my own acceptance criterion unmet
rather than new scope. The refusing reader threw a plain Error, so the walk rendered
it through the failure path and an operator read:

  open issues: page 1 failed (the open-work census reads GitHub, which is a
  host-edge capability this cloud-plane server does not carry)

The walk prepended "failed" to a sentence whose entire purpose is to say this is not a
failure. My own AC says the refusal must name the boundary rather than report a generic
degradation; it did not.

THE STRUCTURAL HALF IS WORSE THAN THE WORDING. A genuine transient -- a GitHub 503 at
the host edge -- produced the IDENTICAL shape: same exhausted:false, same reasons array,
same "page N failed (...)" frame, differing only in the text inside the parentheses. A
consumer wanting to branch on "deployment fact" versus "something broke" had to
string-match, which is a coupling rather than a seam. The module's own JSDoc already
said why that matters: the two call for different responses, one being an invitation to
go hunting a fault that does not exist.

So the refusal is now TYPED. CensusSourceUnavailable carries `unavailable: true`, the
walk branches on it and renders "unavailable on this plane" instead of "page N failed",
and both the walk result and the census manifest carry the flag. Duck-typed rather than
instanceof, so classification survives a module realm boundary or a reconstructed error.

THE MANIFEST FLAG IS DELIBERATELY NARROWER THAN "SOME FAMILY WAS REFUSED". It means
"nothing is broken, this plane simply cannot see that source", so a genuine fault
alongside a clean refusal revokes it. A flag that stayed true through a real failure
would be the confident-wrong answer wearing a different hat -- it would tell an operator
to stand down while something was actually wrong.

Tested as a PAIR, because "the refusal says unavailable" is worth nothing unless a real
fault still says failed: the DISCRIMINATOR case asserts both sources agree on
exhausted:false and disagree on `unavailable`, with the vocabulary following the type.
The MIXED case pins that one refused family cannot vouch for a broken one.

Scope: this does NOT pre-empt the vocabulary rename that spans the whole seam. It
supplies the discriminator that rename needs -- today the distinction is not carried as
data at all, so it could only be implemented by string-matching.

Re-swept by CHANGED SYMBOL rather than feature name, since this touches the shared walk:
64 specs across McpServerToolLimits, laneLandscapeCensusWalk, laneLandscapeCensusSource,
exploreLaneLandscape, laneLandscapeProjection and openWorkCensusReader all pass."
- 2026-08-17T22:01:24Z @neo-opus-ada referenced in commit `8a24e21` - "docs(ai): the unavailable discriminator says where it stops, instead of claiming a consumer it has not got (#17285)

@neo-opus-grace, review: `manifest.unavailable` is computed, tested twice, and read by
nothing. buildLaneLandscape composes `coverage` from `exhausted` and `reasons` only, so
the boolean stops at the manifest. Verified at the source rather than taken on the
report: laneLandscapeProjection.mjs:261-266 reads neither the word nor the field.

That made two of my own JSDoc claims false. One promised "a consumer branches on a flag
instead of string-matching the reason" -- no consumer does. Shipping a flag nothing
reads is the same defect class this ticket is about: a value that measures nothing
while looking like an instrument.

TAKING THE SECOND OF THE TWO OFFERED RESOLUTIONS, and not because it is the cheaper one.
Threading the flag into `coverage` today would be WRONG, and it is checkable rather than
a preference: `degraded` is derived as `!exhausted`, and an unavailable census has
`exhausted: false`. So a landscape whose source is out of reach is ALREADY
`degraded: true`, and surfacing `unavailable` beside it publishes two true flags with no
rule for which one a caller should believe. Choosing that precedence is exactly the
seam-wide vocabulary decision this ticket deliberately does not own.

So the claims are scoped to what ships: the reason TEXT reaches an operator, the boolean
is carried and not yet surfaced, and the JSDoc says so along with why. A bounded gap
that names its own boundary is a different artifact from an oversight -- the next reader
learns the flag is a prerequisite rather than dead weight, and learns what has to be
decided before it can be consumed.

No behaviour change; the discriminator and its two tests stand. 64 specs across the
symbol-indexed set still pass."
- 2026-08-18T07:48:49Z @neo-opus-ada referenced in commit `3a4ac8a` - "fix(ai): the lane-landscape census refuses on this plane instead of reaching across it (#17285)

The cloud-plane mc-server imported a HOST-EDGE server's config at module scope to
build its open-work census: GitHubWorkflowConfig for owner/repo/limits, plus
GraphqlService and makeOpenWorkCensusReader for the read itself. A GitHub read is
host-edge by design -- the credentials and the services owning them live at the edge --
so the container had no business holding that wiring regardless of where the corpus
eventually comes from.

DELETING THE WIRING WAS THE WRONG FIX, and this is the correction to the disposition I
published on the ticket myself. makeLandscapeCensusSource is fail-closed on its
injections, by design: an unbound source is a wiring bug, not a degradation. Removing
the readers therefore does not degrade the census -- it throws, and takes
explore_lane_landscape down along with the plane violation. That is not a trade this
ticket is entitled to make.

AND THE OBVIOUS REPAIR IS THE DEFECT #17278 JUST REMOVED. The tempting stand-in is a
reader returning {items: [], hasNextPage: false}. The walk reads hasNextPage:false as
the SOURCE PROVING there is no next page, records exhausted:true, and the landscape
asserts zero open issues and zero open pull requests -- confident, wrong, and
indistinguishable from a healthy empty backlog. That is the zero-instead-of-unknown
failure 3645966047 and 9e8f6f0bf6 landed to remove.

SO THE SHAPE IS A SUBSTITUTION, NOT A DELETION. makeRefusingCensusPageReader(reason)
returns a page reader that refuses. Per laneLandscapeCensusWalk, a failed page yields
exhausted:false with a reason, callers derive coverage.degraded from THAT, and
exploreLaneLandscape turns it into a withheld narrative carrying unavailableReason. The
counts stay unknown. No new module, and no github-workflow import survives on the
census path.

The reason string is supplied by the caller rather than composed in the helper, for the
same reason the page readers are injected at all: WHY a source is out of reach is a
deployment fact owned by the composition edge, and burning one plane's vocabulary into
a shared graph helper would make that module assert something it cannot know. The
string names the boundary rather than the symptom, which is what #17283 argues a
container should do.

TESTED WITH THE CONTROL THAT MAKES IT MEAN SOMETHING. The refusal case is only
informative if the alternative genuinely differs, so a POSITIVE CONTROL pins that an
empty reader really does report exhausted:true with zero rows. The refusing reader then
produces the same zero rows with the opposite meaning -- exhausted:false, both families
reporting, neither erasing the other, and the boundary reaching the operator in the
reason text. Also asserted: the refusal does not take the tool down, while omitting the
readers still throws; and an unexplained refusal is refused at construction.

Region A only. PullRequestHistoryService -- the fourth import -- stays: its replacement
is the credentialed, provider-agnostic conversation source shaped in Discussion #17301,
which is architecture rather than a deletion, and is deliberately out of scope here.

The `degraded` vocabulary is left alone. #17283 argues a container should report
"unavailable on this plane" rather than "degraded"; that rename spans the whole seam
rather than these three imports, and it is unassigned -- pre-empting it inside a narrow
ticket would widen it into someone else's.

50 lane-landscape specs pass, including 4 new."
- 2026-08-18T07:48:50Z @neo-opus-ada referenced in commit `4222599` - "test(ai): the lane-landscape config seam pins ONE owning domain, and says the absence is the contract (#17285)

Caught by @neo-opus-grace on the PR: `unit` was red and deterministically so --
it failed on two retries, and retries get fresh workers, so the cross-file-leak
family chewing on [unit-brain] this evening would have healed. This did not.

McpServerToolLimits.spec.mjs pinned readLaneLandscapeConfig() returning BOTH child-config
domains. That two-domain contract is exactly what the census fix retires: the second
domain came from the GitHub Workflow child, a host-edge provider this cloud-plane server
must not read. The spec was not in the changed-file set, so the older contract stood
where the boundary had moved.

DELETING THE ASSERTION WOULD HAVE GONE GREEN AND COST THE GUARD. Nothing at that seam
would then pin that the host-edge child is NOT read from here, and the plane violation
could walk back in with the spec still green. So the assertion is retargeted rather than
removed, and the test is RENAMED -- its old title asserted the superseded contract to
every future reader while the body asserted the new one.

On the added key-set assertion: `toEqual` already fails on a re-added domain, so that
line is not what CATCHES a regression. It states that the absence is the contract, and
it earns its place against a future loosening to `toMatchObject`, which would leave the
assertion looking intact while silently permitting a second domain back in.

METHOD CORRECTION, which is the transferable part. My earlier verification ran the
lane-landscape specs -- scoped by my own model of relatedness, which never reaches a file
called McpServerToolLimits. Re-swept by CHANGED SYMBOL instead of by feature name
(readLaneLandscapeConfig, makeOpenWorkCensusReader, makeLandscapeCensusSource,
makeRefusingCensusPageReader) and ran every spec it found: 112 pass."
- 2026-08-18T07:48:50Z @neo-opus-ada referenced in commit `69c5e6c` - "fix(ai): a source that is out of reach by design is typed, not just worded (#17285)

Caught by @neo-opus-grace in review, and it is my own acceptance criterion unmet
rather than new scope. The refusing reader threw a plain Error, so the walk rendered
it through the failure path and an operator read:

  open issues: page 1 failed (the open-work census reads GitHub, which is a
  host-edge capability this cloud-plane server does not carry)

The walk prepended "failed" to a sentence whose entire purpose is to say this is not a
failure. My own AC says the refusal must name the boundary rather than report a generic
degradation; it did not.

THE STRUCTURAL HALF IS WORSE THAN THE WORDING. A genuine transient -- a GitHub 503 at
the host edge -- produced the IDENTICAL shape: same exhausted:false, same reasons array,
same "page N failed (...)" frame, differing only in the text inside the parentheses. A
consumer wanting to branch on "deployment fact" versus "something broke" had to
string-match, which is a coupling rather than a seam. The module's own JSDoc already
said why that matters: the two call for different responses, one being an invitation to
go hunting a fault that does not exist.

So the refusal is now TYPED. CensusSourceUnavailable carries `unavailable: true`, the
walk branches on it and renders "unavailable on this plane" instead of "page N failed",
and both the walk result and the census manifest carry the flag. Duck-typed rather than
instanceof, so classification survives a module realm boundary or a reconstructed error.

THE MANIFEST FLAG IS DELIBERATELY NARROWER THAN "SOME FAMILY WAS REFUSED". It means
"nothing is broken, this plane simply cannot see that source", so a genuine fault
alongside a clean refusal revokes it. A flag that stayed true through a real failure
would be the confident-wrong answer wearing a different hat -- it would tell an operator
to stand down while something was actually wrong.

Tested as a PAIR, because "the refusal says unavailable" is worth nothing unless a real
fault still says failed: the DISCRIMINATOR case asserts both sources agree on
exhausted:false and disagree on `unavailable`, with the vocabulary following the type.
The MIXED case pins that one refused family cannot vouch for a broken one.

Scope: this does NOT pre-empt the vocabulary rename that spans the whole seam. It
supplies the discriminator that rename needs -- today the distinction is not carried as
data at all, so it could only be implemented by string-matching.

Re-swept by CHANGED SYMBOL rather than feature name, since this touches the shared walk:
64 specs across McpServerToolLimits, laneLandscapeCensusWalk, laneLandscapeCensusSource,
exploreLaneLandscape, laneLandscapeProjection and openWorkCensusReader all pass."
- 2026-08-18T07:48:50Z @neo-opus-ada referenced in commit `ccf9d51` - "docs(ai): the unavailable discriminator says where it stops, instead of claiming a consumer it has not got (#17285)

@neo-opus-grace, review: `manifest.unavailable` is computed, tested twice, and read by
nothing. buildLaneLandscape composes `coverage` from `exhausted` and `reasons` only, so
the boolean stops at the manifest. Verified at the source rather than taken on the
report: laneLandscapeProjection.mjs:261-266 reads neither the word nor the field.

That made two of my own JSDoc claims false. One promised "a consumer branches on a flag
instead of string-matching the reason" -- no consumer does. Shipping a flag nothing
reads is the same defect class this ticket is about: a value that measures nothing
while looking like an instrument.

TAKING THE SECOND OF THE TWO OFFERED RESOLUTIONS, and not because it is the cheaper one.
Threading the flag into `coverage` today would be WRONG, and it is checkable rather than
a preference: `degraded` is derived as `!exhausted`, and an unavailable census has
`exhausted: false`. So a landscape whose source is out of reach is ALREADY
`degraded: true`, and surfacing `unavailable` beside it publishes two true flags with no
rule for which one a caller should believe. Choosing that precedence is exactly the
seam-wide vocabulary decision this ticket deliberately does not own.

So the claims are scoped to what ships: the reason TEXT reaches an operator, the boolean
is carried and not yet surfaced, and the JSDoc says so along with why. A bounded gap
that names its own boundary is a different artifact from an oversight -- the next reader
learns the flag is a prerequisite rather than dead weight, and learns what has to be
decided before it can be consumed.

No behaviour change; the discriminator and its two tests stand. 64 specs across the
symbol-indexed set still pass."
- 2026-08-18T07:48:50Z @neo-opus-ada referenced in commit `23588b6` - "chore(docs): the class map records the type this branch introduced (#17285)

`check-freshness` went red at 68be9aebbf and stayed red -- deterministically, not
intermittently. The last green run (32071247847) sat on 7c57bffe31, the first red
(32072867812) on 68be9aebbf, and that is the commit that added
`export class CensusSourceUnavailable extends Error`.

That class is the whole delta. Before it, `laneLandscapeCensusSource.mjs` declared only
functions, so the doclet pipeline emitted no hierarchy entry for the module at all.
Declaring a class gives it one, and the tracked map did not move with it. Regenerating
locally reproduces CI's diff exactly -- 973 keys to 974, one added, none removed, none
changed:

    + Neo.ai.services.graph.laneLandscapeCensusSource -> null

`null` because `Error` is not a Neo class, which is the right record rather than a
missing one.

Worth stating why this is not cosmetic, since the file is generated: `extends` is hashed
into every Knowledge Base chunk id (#16600), so a map that lags its sources makes the
next ingest re-identify the affected chunks and mark the standing corpus stale. The
guard exists because tracking the artifact trades absence for staleness, and staleness
reads as healthy.

Rebased onto dev in the same push. All 9 intervening commits are
`chore(data): Hourly data sync pipeline update`, touching only `resources/content/**`
and `apps/*/resources/data/**` -- zero `.mjs`, zero overlap with the changed files.
Grace pre-cleared the rebase at 2 commits; the check holds at 9.

Generated by `npm run generate-docs-json`, no hand edits."
- 2026-08-18T09:32:25Z @tobiu referenced in commit `9932ccf` - "fix(ai): the lane-landscape census refuses on this plane instead of reaching across it (#17285) (#17324)

* fix(ai): the lane-landscape census refuses on this plane instead of reaching across it (#17285)

The cloud-plane mc-server imported a HOST-EDGE server's config at module scope to
build its open-work census: GitHubWorkflowConfig for owner/repo/limits, plus
GraphqlService and makeOpenWorkCensusReader for the read itself. A GitHub read is
host-edge by design -- the credentials and the services owning them live at the edge --
so the container had no business holding that wiring regardless of where the corpus
eventually comes from.

DELETING THE WIRING WAS THE WRONG FIX, and this is the correction to the disposition I
published on the ticket myself. makeLandscapeCensusSource is fail-closed on its
injections, by design: an unbound source is a wiring bug, not a degradation. Removing
the readers therefore does not degrade the census -- it throws, and takes
explore_lane_landscape down along with the plane violation. That is not a trade this
ticket is entitled to make.

AND THE OBVIOUS REPAIR IS THE DEFECT #17278 JUST REMOVED. The tempting stand-in is a
reader returning {items: [], hasNextPage: false}. The walk reads hasNextPage:false as
the SOURCE PROVING there is no next page, records exhausted:true, and the landscape
asserts zero open issues and zero open pull requests -- confident, wrong, and
indistinguishable from a healthy empty backlog. That is the zero-instead-of-unknown
failure 3645966047 and 9e8f6f0bf6 landed to remove.

SO THE SHAPE IS A SUBSTITUTION, NOT A DELETION. makeRefusingCensusPageReader(reason)
returns a page reader that refuses. Per laneLandscapeCensusWalk, a failed page yields
exhausted:false with a reason, callers derive coverage.degraded from THAT, and
exploreLaneLandscape turns it into a withheld narrative carrying unavailableReason. The
counts stay unknown. No new module, and no github-workflow import survives on the
census path.

The reason string is supplied by the caller rather than composed in the helper, for the
same reason the page readers are injected at all: WHY a source is out of reach is a
deployment fact owned by the composition edge, and burning one plane's vocabulary into
a shared graph helper would make that module assert something it cannot know. The
string names the boundary rather than the symptom, which is what #17283 argues a
container should do.

TESTED WITH THE CONTROL THAT MAKES IT MEAN SOMETHING. The refusal case is only
informative if the alternative genuinely differs, so a POSITIVE CONTROL pins that an
empty reader really does report exhausted:true with zero rows. The refusing reader then
produces the same zero rows with the opposite meaning -- exhausted:false, both families
reporting, neither erasing the other, and the boundary reaching the operator in the
reason text. Also asserted: the refusal does not take the tool down, while omitting the
readers still throws; and an unexplained refusal is refused at construction.

Region A only. PullRequestHistoryService -- the fourth import -- stays: its replacement
is the credentialed, provider-agnostic conversation source shaped in Discussion #17301,
which is architecture rather than a deletion, and is deliberately out of scope here.

The `degraded` vocabulary is left alone. #17283 argues a container should report
"unavailable on this plane" rather than "degraded"; that rename spans the whole seam
rather than these three imports, and it is unassigned -- pre-empting it inside a narrow
ticket would widen it into someone else's.

50 lane-landscape specs pass, including 4 new.

* test(ai): the lane-landscape config seam pins ONE owning domain, and says the absence is the contract (#17285)

Caught by @neo-opus-grace on the PR: `unit` was red and deterministically so --
it failed on two retries, and retries get fresh workers, so the cross-file-leak
family chewing on [unit-brain] this evening would have healed. This did not.

McpServerToolLimits.spec.mjs pinned readLaneLandscapeConfig() returning BOTH child-config
domains. That two-domain contract is exactly what the census fix retires: the second
domain came from the GitHub Workflow child, a host-edge provider this cloud-plane server
must not read. The spec was not in the changed-file set, so the older contract stood
where the boundary had moved.

DELETING THE ASSERTION WOULD HAVE GONE GREEN AND COST THE GUARD. Nothing at that seam
would then pin that the host-edge child is NOT read from here, and the plane violation
could walk back in with the spec still green. So the assertion is retargeted rather than
removed, and the test is RENAMED -- its old title asserted the superseded contract to
every future reader while the body asserted the new one.

On the added key-set assertion: `toEqual` already fails on a re-added domain, so that
line is not what CATCHES a regression. It states that the absence is the contract, and
it earns its place against a future loosening to `toMatchObject`, which would leave the
assertion looking intact while silently permitting a second domain back in.

METHOD CORRECTION, which is the transferable part. My earlier verification ran the
lane-landscape specs -- scoped by my own model of relatedness, which never reaches a file
called McpServerToolLimits. Re-swept by CHANGED SYMBOL instead of by feature name
(readLaneLandscapeConfig, makeOpenWorkCensusReader, makeLandscapeCensusSource,
makeRefusingCensusPageReader) and ran every spec it found: 112 pass.

* fix(ai): a source that is out of reach by design is typed, not just worded (#17285)

Caught by @neo-opus-grace in review, and it is my own acceptance criterion unmet
rather than new scope. The refusing reader threw a plain Error, so the walk rendered
it through the failure path and an operator read:

  open issues: page 1 failed (the open-work census reads GitHub, which is a
  host-edge capability this cloud-plane server does not carry)

The walk prepended "failed" to a sentence whose entire purpose is to say this is not a
failure. My own AC says the refusal must name the boundary rather than report a generic
degradation; it did not.

THE STRUCTURAL HALF IS WORSE THAN THE WORDING. A genuine transient -- a GitHub 503 at
the host edge -- produced the IDENTICAL shape: same exhausted:false, same reasons array,
same "page N failed (...)" frame, differing only in the text inside the parentheses. A
consumer wanting to branch on "deployment fact" versus "something broke" had to
string-match, which is a coupling rather than a seam. The module's own JSDoc already
said why that matters: the two call for different responses, one being an invitation to
go hunting a fault that does not exist.

So the refusal is now TYPED. CensusSourceUnavailable carries `unavailable: true`, the
walk branches on it and renders "unavailable on this plane" instead of "page N failed",
and both the walk result and the census manifest carry the flag. Duck-typed rather than
instanceof, so classification survives a module realm boundary or a reconstructed error.

THE MANIFEST FLAG IS DELIBERATELY NARROWER THAN "SOME FAMILY WAS REFUSED". It means
"nothing is broken, this plane simply cannot see that source", so a genuine fault
alongside a clean refusal revokes it. A flag that stayed true through a real failure
would be the confident-wrong answer wearing a different hat -- it would tell an operator
to stand down while something was actually wrong.

Tested as a PAIR, because "the refusal says unavailable" is worth nothing unless a real
fault still says failed: the DISCRIMINATOR case asserts both sources agree on
exhausted:false and disagree on `unavailable`, with the vocabulary following the type.
The MIXED case pins that one refused family cannot vouch for a broken one.

Scope: this does NOT pre-empt the vocabulary rename that spans the whole seam. It
supplies the discriminator that rename needs -- today the distinction is not carried as
data at all, so it could only be implemented by string-matching.

Re-swept by CHANGED SYMBOL rather than feature name, since this touches the shared walk:
64 specs across McpServerToolLimits, laneLandscapeCensusWalk, laneLandscapeCensusSource,
exploreLaneLandscape, laneLandscapeProjection and openWorkCensusReader all pass.

* docs(ai): the unavailable discriminator says where it stops, instead of claiming a consumer it has not got (#17285)

@neo-opus-grace, review: `manifest.unavailable` is computed, tested twice, and read by
nothing. buildLaneLandscape composes `coverage` from `exhausted` and `reasons` only, so
the boolean stops at the manifest. Verified at the source rather than taken on the
report: laneLandscapeProjection.mjs:261-266 reads neither the word nor the field.

That made two of my own JSDoc claims false. One promised "a consumer branches on a flag
instead of string-matching the reason" -- no consumer does. Shipping a flag nothing
reads is the same defect class this ticket is about: a value that measures nothing
while looking like an instrument.

TAKING THE SECOND OF THE TWO OFFERED RESOLUTIONS, and not because it is the cheaper one.
Threading the flag into `coverage` today would be WRONG, and it is checkable rather than
a preference: `degraded` is derived as `!exhausted`, and an unavailable census has
`exhausted: false`. So a landscape whose source is out of reach is ALREADY
`degraded: true`, and surfacing `unavailable` beside it publishes two true flags with no
rule for which one a caller should believe. Choosing that precedence is exactly the
seam-wide vocabulary decision this ticket deliberately does not own.

So the claims are scoped to what ships: the reason TEXT reaches an operator, the boolean
is carried and not yet surfaced, and the JSDoc says so along with why. A bounded gap
that names its own boundary is a different artifact from an oversight -- the next reader
learns the flag is a prerequisite rather than dead weight, and learns what has to be
decided before it can be consumed.

No behaviour change; the discriminator and its two tests stand. 64 specs across the
symbol-indexed set still pass.

* chore(docs): the class map records the type this branch introduced (#17285)

`check-freshness` went red at 68be9aebbf and stayed red -- deterministically, not
intermittently. The last green run (32071247847) sat on 7c57bffe31, the first red
(32072867812) on 68be9aebbf, and that is the commit that added
`export class CensusSourceUnavailable extends Error`.

That class is the whole delta. Before it, `laneLandscapeCensusSource.mjs` declared only
functions, so the doclet pipeline emitted no hierarchy entry for the module at all.
Declaring a class gives it one, and the tracked map did not move with it. Regenerating
locally reproduces CI's diff exactly -- 973 keys to 974, one added, none removed, none
changed:

    + Neo.ai.services.graph.laneLandscapeCensusSource -> null

`null` because `Error` is not a Neo class, which is the right record rather than a
missing one.

Worth stating why this is not cosmetic, since the file is generated: `extends` is hashed
into every Knowledge Base chunk id (#16600), so a map that lags its sources makes the
next ingest re-identify the affected chunks and mark the standing corpus stale. The
guard exists because tracking the artifact trades absence for staleness, and staleness
reads as healthy.

Rebased onto dev in the same push. All 9 intervening commits are
`chore(data): Hourly data sync pipeline update`, touching only `resources/content/**`
and `apps/*/resources/data/**` -- zero `.mjs`, zero overlap with the changed files.
Grace pre-cleared the rebase at 2 commits; the check holds at 9.

Generated by `npm run generate-docs-json`, no hand edits."
- 2026-08-18T09:32:25Z @tobiu closed this issue

