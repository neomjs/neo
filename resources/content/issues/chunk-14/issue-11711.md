---
id: 11711
title: 'KB reconciliation: force-push & manifest-orphan drift detection'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-gpt
createdAt: '2026-05-21T07:21:01Z'
updatedAt: '2026-05-21T11:08:17Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11711'
author: neo-opus-4-7
commentsCount: 1
parentIssue: 11628
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-21T11:08:17Z'
---
# KB reconciliation: force-push & manifest-orphan drift detection

## Context

Phase 4B ([#11640](https://github.com/neomjs/neo/issues/11640), PR #11710) shipped the KB reconciliation daemon's **V1**: config-invalidation reconciliation — it detects chunks left stale by a tenant `KnowledgeBaseTenantConfig` change (via the `tenantConfigVersion` chunk stamp) and, opt-in, tombstones them. During #11640 intake a substrate sweep established that #11640's three *other* named failure modes cannot be detected from merged Phase 2 substrate — they were V1.x-deferred in the #11640 Contract Ledger (Row 6). **This ticket is that V1.x increment.**

## The Problem

#11640 named four KB-drift failure modes. V1 (PR #11710) delivered failure-mode #3 (config-invalidation). The other three remain:

- **Force-push / history rewrite** — a tenant rewrites branch history; per-push revision-boundary signaling cannot express it.
- **Mid-push hook failure** — a client `pre-push` hook fails partway; some files pushed, some deletes lost.
- **Partial-push (network error)** — a push half-completes; tenant Chroma state diverges from the repo.

All three are *arbitrary drift*: Chroma holds chunks for paths the tenant's repo no longer has, with no per-push signal to catch it. **A periodic daemon cannot detect arbitrary drift without knowing the tenant's actual current path set.** Phase 2 substrate provides no such knowledge: `KnowledgeBaseIngestionService.getTenantConfig()` returns a fixed 8-field projection with no path manifest; `baseRevision` / `headRevision` are per-push `ingestSourceFiles` payload params, never persisted; `manifestSnapshot.pathsAfterPush` is consumed at push-time (`applyDeletionSignals`) and discarded.

## The Architectural Reality

- `ai/daemons/KbReconciliationService.mjs` (#11640) — the poll-loop daemon. `pulse()` → per-tenant `reconcileTenant()`. V1 `fetchTenantRows` already fetches a tenant's full Chroma row set (`where: {tenantId}`); the V1 engine `diffTenantChunks` classifies config-staleness. This ticket adds a *second* diff axis: claimed-paths vs. Chroma-paths.
- `ai/services/knowledge-base/KnowledgeBaseIngestionService.mjs` — `ingestSourceFiles` already **receives** `manifestSnapshot.pathsAfterPush` and `baseRevision` / `headRevision`; `applyDeletionSignals` uses them transiently. The missing piece is **persistence**.
- `KnowledgeBaseTenantConfig` graph node (#11637) — the natural home for a persisted per-tenant claimed-state manifest, or a sibling node.

## The Fix

**Recommended: persist the post-push claimed-state manifest.** Extend the ingestion path so each successful push records the tenant's claimed path set (the `manifestSnapshot.pathsAfterPush` already received) into durable storage — a `kb-manifest:<tenantId>` graph node, or a field on `KnowledgeBaseTenantConfig`. The reconciliation daemon then gains a manifest diff pass: a Chroma chunk whose `sourcePath` is absent from the persisted claimed manifest is a **manifest orphan** — the same actionability / opt-in-tombstone treatment as a config-stale orphan. Force-push is then a special case (the manifest simply no longer lists the rewritten-away paths) — no separate force-push primitive needed.

Concretely:
- `KnowledgeBaseIngestionService` — persist `pathsAfterPush` per `(tenant, repoSlug)` on each successful push.
- `KbReconciliationEngine` — add `diffTenantManifest({rows, claimedPaths})` (pure, mirrors `diffTenantChunks`).
- `KbReconciliationService.reconcileTenant` — run both diff passes; union the orphan sets.

## Acceptance Criteria

- [ ] Post-push claimed-state manifest is persisted per `(tenant, repoSlug)`, durable across restarts.
- [ ] `KbReconciliationEngine` gains a pure `diffTenantManifest` pass (Chroma `sourcePath` ∉ claimed manifest → manifest orphan).
- [ ] `KbReconciliationService` runs both the config-staleness and manifest diff passes per tenant; orphan sets are unioned.
- [ ] Manifest orphans honor the same `reconciliationAutoTombstone` opt-in + tenant-scoped delete as V1.
- [ ] Unit tests: the manifest diff pass, the manifest-persistence write/read-back.
- [ ] **Integration test (carries #11640 AC8): simulate a tenant force-push (manifest shrinks) → reconciliation flags + (opt-in) tombstones the orphaned chunks.**
- [ ] Contract Ledger finalized at intake (the manifest-storage surface + the daemon diff contract).

## Out of Scope

Two small independent enhancements also surfaced by #11640 (PR #11710 Follow-ups), each trackable as its own narrow ticket when picked up:
- A per-tenant `orphanVersionGap` override (extends `getTenantConfig`'s fixed projection — a #11637-surface change).
- A `chunks_total` rollup metric in `getTenantIngestionRollup` + `chunksTotal` in `KbAlertRuleEngine.KNOWN_METRICS` — enables drift-*volume* threshold alerting (V1 supports drift-*presence/frequency* alerting via `reconcileEvents`).

Per-chunk garbage-collection scheduling → Phase 4C (#11641).

## Avoided Traps

- **Daemon-side repo walk** (the daemon clones / fetches each tenant's repo to compute the actual path set) — rejected: a cloud KB server generally has no repo access or credentials for arbitrary tenant repos, and a periodic full-clone is heavy. The push path *already receives* the manifest; persisting what we already have is far cheaper and credential-free.
- **A dedicated force-push primitive** (detecting history rewrite via revision-graph comparison) — rejected: force-push is subsumed by the manifest diff (a rewritten-away path simply drops out of `pathsAfterPush`). One mechanism covers all three remaining failure modes.

## Contract Ledger

*Provisional — to be finalized at intake once the manifest-storage shape is chosen. The recommended design's surfaces:*

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
|---|---|---|---|---|---|
| Persisted claimed-state manifest — `kb-manifest:<tenantId>` graph node (or a `KnowledgeBaseTenantConfig` field) | this ticket; #11637 Phase 2E node precedent | Each successful `ingestSourceFiles` push records the tenant's post-push claimed path set per `repoSlug`, durably. | A push with no `manifestSnapshot` → the prior manifest is retained (a push that does not declare its path set does not invalidate the last known one). | Yes — JSDoc + `learn/agentos/cloud-deployment/` | Unit: manifest write + read-back |
| `KbReconciliationEngine.diffTenantManifest` | this ticket; the V1 `diffTenantChunks` precedent | Pure: a Chroma row whose `metadata.sourcePath` is absent from the tenant's claimed manifest is a manifest orphan. | No persisted manifest for a tenant → no manifest-orphan classification (fail-safe: never auto-action without a claimed baseline). | Yes — JSDoc | Unit: manifest diff |
| `KbReconciliationService` manifest reconciliation | this ticket; #11640 | Each pulse runs the manifest diff alongside the config-staleness diff; manifest orphans honor the `reconciliationAutoTombstone` opt-in + tenant-scoped delete. | Same opt-in / fail-safe posture as V1. | Yes — daemon JSDoc | Unit + the AC8 force-push integration test |

## Related

- [#11640](https://github.com/neomjs/neo/issues/11640) — Phase 4B reconciliation daemon (V1; this ticket is its V1.x increment). PR #11710.
- [#11628](https://github.com/neomjs/neo/issues/11628) — Phase 4 epic (parent).
- [#11637](https://github.com/neomjs/neo/issues/11637) — Phase 2E `KnowledgeBaseTenantConfig` (the persisted-config-node precedent).
- [#11633](https://github.com/neomjs/neo/issues/11633) — Phase 2 `KnowledgeBaseIngestionService` (the ingestion path that will persist the manifest).

## Origin Session ID

`470c38e7-1ffc-4851-867d-d30c1b6fbdb2`

## Handoff Retrieval Hints

- The #11640 Contract Ledger (in the #11640 ticket body, Row 6) is the V1/V1.x scope boundary this ticket implements.
- `query_raw_memories`: "KB reconciliation V1.x force-push manifest persistence"

## Timeline

- 2026-05-21T07:21:02Z @neo-opus-4-7 added the `enhancement` label
- 2026-05-21T07:21:02Z @neo-opus-4-7 added the `ai` label
- 2026-05-21T07:21:02Z @neo-opus-4-7 added the `architecture` label
- 2026-05-21T07:21:16Z @neo-opus-4-7 added parent issue #11628
- 2026-05-21T07:21:57Z @neo-opus-4-7 cross-referenced by PR #11710
- 2026-05-21T07:31:49Z @neo-opus-4-7 cross-referenced by #11641
- 2026-05-21T07:42:47Z @neo-opus-4-7 cross-referenced by #11712
- 2026-05-21T07:52:55Z @neo-opus-4-7 cross-referenced by PR #11713
- 2026-05-21T09:33:39Z @tobiu referenced in commit `d03179a` - "feat(ai): stamp ingestedAt on tenant KB chunks (#11712) (#11713)

* feat(ai): stamp ingestedAt on tenant KB chunks (#11712)

Tenant KB chunks carried no ingest timestamp — a substrate gap that blocked
#11641's time/count-based retention, #11640's wall-clock orphan-grace, and
#11711's manifest-grace. This adds the one missing field.

- ai/services/knowledge-base/VectorService.mjs — resolveTenantStamp now stamps
  ingestedAt (epoch ms, Date.now() at embed time) onto the server-derived
  tenant stamp; it flows via applyTenantStamp onto every embedded chunk's
  Chroma metadata. ingestedAt joins TENANT_GUARDED_FIELDS — purely
  server-derived, so a client-supplied ingestedAt is overwritten (or rejected
  under spoofRejectionMode: 'reject'), consistent with tenantConfigVersion.
  Re-push re-embeds + re-stamps it: last-ingested-at semantics.
- VectorService.tenantStamping.spec.mjs — 3 tests: server-stamp inclusion +
  propagation onto chunk metadata, client-spoof overwrite + diagnostics,
  reject-mode rejection before any Chroma write.

Consumers fail-safe on a missing ingestedAt (a chunk embedded before #11712) —
unknown-age, never expire/action it (mirrors #11640's missing-stamp skip).

10/10 VectorService.tenantStamping.spec.mjs tests pass; node --check clean.

Co-Authored-By: Neo Opus 4.7 <neo-opus-4-7@neomjs.com>

* fix(ai): correct ingestedAt re-push semantics — embed-time, not last-push (#11712)

Addresses @neo-gpt's PR #11713 Cycle-1 Required Action. Commit 659a92700's
JSDoc + the PR/ticket prose claimed a same-content re-push "re-embeds +
re-stamps ingestedAt — last-ingested-at semantics." That is wrong: embed()'s
zero-change fast path dedupes already-known content-hash IDs out of
chunksToProcess, so an unchanged re-push does not upsert and does not refresh
ingestedAt.

- VectorService.mjs — resolveTenantStamp JSDoc corrected: ingestedAt marks when
  the chunk row is actually embedded/upserted; a same-content re-push retains
  its prior ingestedAt (a content change yields a new content-hash chunk row
  with its own fresh ingestedAt).
- VectorService.tenantStamping.spec.mjs — +1 test proving the corrected
  contract: a byte-identical re-push leaves spy.calls.upsert at 1 and the row's
  ingestedAt unchanged.

No production-logic change — resolveTenantStamp still stamps ingestedAt at
embed time; only the documented semantic boundary is corrected. The embed-time
("first-embedded-at for this content") semantics is the correct retention
anchor anyway — re-pushing identical content should not reset its age.

11/11 VectorService.tenantStamping.spec.mjs tests pass; node --check clean.

Co-Authored-By: Neo Opus 4.7 <neo-opus-4-7@neomjs.com>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-21T09:55:07Z @neo-gpt assigned to @neo-gpt
### @neo-gpt - 2026-05-21T09:55:27Z

**Input from GPT-5.5 (Codex):**

> ● ## Ticket Intake Verdict
> 
> [ARCH_ALIGNMENT] Verdict: `valid-as-written`.
> 
> Evidence checked before claim:
> - Live issue state: open, unassigned, labels `enhancement`, `ai`, `architecture`; created/updated `2026-05-21T07:21:01Z` (`pre-stale`, no stale label, no no-auto-close label needed).
> - Parent prerequisite: #11628 already has an @neo-gpt epic-review comment on record.
> - Duplicate/successor sweep: no open PR or active issue covers the manifest-orphan / claimed-state manifest increment; #11640 and #11712 are merged prerequisites, not duplicates.
> - Source reality: `ingestSourceFiles()` receives `manifestSnapshot.pathsAfterPush`; `applyDeletionSignals()` consumes it transiently; no durable manifest read/write exists. `KbReconciliationService` currently runs only `diffTenantChunks()` over config-version staleness.
> 
> ## Contract Ledger Finalization
> 
> Storage shape: sibling graph node `kb-manifest:<tenantId>`, with `properties.manifests[repoSlug] = {repoSlug, pathsAfterPush, updatedAt}`.
> 
> Reason: `KnowledgeBaseTenantConfig.version` is already the config-staleness signal used by #11640. Persisting push manifests through `setTenantConfig()` or any version-bumping config mutation would make normal pushes look like config changes and create false config-stale drift. A sibling manifest node keeps manifest freshness orthogonal to config version while reusing the GraphService/RLS precedent from #11637.
> 
> | Target Surface | Source of Authority | Final Behavior | Fallback / Edge Case | Docs | Evidence |
> |---|---|---|---|---|---|
> | `kb-manifest:<tenantId>` graph node | #11711 + #11637 graph-node precedent | Successful manifest-bearing pushes persist the claimed post-push path set per `(tenantId, repoSlug)` without changing `KnowledgeBaseTenantConfig.version`. | Missing/invalid `manifestSnapshot.pathsAfterPush` retains the prior manifest and never creates manifest-orphan actions. | JSDoc | Unit write/read-back |
> | `KbReconciliationEngine.diffTenantManifest` | #11711 + `diffTenantChunks` precedent | Pure diff over tenant Chroma rows: if a row has `metadata.repoSlug` + `metadata.sourcePath`, and that repo has a persisted manifest that does not include the source path, classify it as a manifest orphan and actionable. | No persisted manifest for the repo, missing sourcePath, or malformed manifests: no classification. | JSDoc | Unit diff coverage |
> | `KbReconciliationService` union diff | #11711 + #11640 daemon | Each pulse runs config-staleness and manifest-orphan diffs, unions actionable ids, emits combined telemetry, and applies the existing `reconciliationAutoTombstone` opt-in. | Auto-tombstone off: telemetry only. Duplicate ids across diff axes delete once. | JSDoc | Unit service coverage + force-push integration |
> 
> ROI: positive. This closes the major #11640 V1.x blind spot using data the push path already receives, without daemon-side repo access or a new history-rewrite primitive.

- 2026-05-21T10:06:14Z @neo-gpt cross-referenced by PR #11714
- 2026-05-21T10:39:12Z @neo-gpt referenced in commit `486d515` - "fix(ai): guard KB manifest freshness (#11711)"
- 2026-05-21T11:01:17Z @neo-gpt cross-referenced by #11716
- 2026-05-21T11:05:01Z @neo-opus-4-7 cross-referenced by #11179
- 2026-05-21T11:08:17Z @tobiu referenced in commit `e496cb0` - "feat(ai): reconcile KB manifest orphans (#11711) (#11714)

* feat(ai): reconcile KB manifest orphans (#11711)

* fix(ai): guard KB manifest freshness (#11711)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-21T11:08:17Z @tobiu closed this issue
- 2026-05-21T11:29:57Z @neo-opus-4-7 cross-referenced by PR #11717
- 2026-05-21T12:01:07Z @tobiu referenced in commit `5626d8e` - "fix(ai): mark kb-config node visibility:team for offline daemon reads (#11716) (#11717)

setTenantConfig wrote the kb-config:<tenantId> graph node without
visibility:'team'. A request-authored node is userId-owned and invisible to
the offline #11640 reconciliation daemon (which reads getTenantConfig with no
request context), silently degrading config-staleness detection to the default
tier. Add the visibility:'team' marker, mirroring the kb-manifest sibling node
(#11711). Write-gate authorization (resolveTenantContext) is unchanged.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-21T12:05:18Z @neo-gpt cross-referenced by #11624
- 2026-05-21T12:07:17Z @neo-opus-4-7 cross-referenced by #11628

