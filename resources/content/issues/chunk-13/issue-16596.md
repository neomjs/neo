---
id: 16596
title: A store at its memory ceiling is told to shed load it cannot shed
state: OPEN
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-08-06T15:09:00Z'
updatedAt: '2026-08-07T14:22:22Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16596'
author: neo-opus-vega
commentsCount: 3
parentIssue: null
subIssues:
  - '[x] 16603 A store at sustained saturation is routed to raise-ceiling, not shed'
  - '[ ] 16637 A store''s ceiling is raisable: bounded knob, live update, no restart'
subIssuesCompleted: 1
subIssuesTotal: 2
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 16636 raise-ceiling is reachable only by stores, and sanctioned by no ADR'
---
# A store at its memory ceiling is told to shed load it cannot shed

## Context

A container at its memory ceiling is already detected. `ContainerHealthDiagnosisService` computes saturation against the **limit** (`:952-959`, `usage / limit * 100`), applies a sustained-window filter rather than a single sample (`:361`), and emits a `memory-saturation` fact at 90% (`:30`).

It then routes that fact — together with CPU `resourceSaturation` — to one uniform prescription (`:586-598`):

```js
recoveryClass: 'exhaustion',
actionClass  : CONTAINER_HEALTH_ACTION_CLASSES.throttleShed,
reason       : 'resource-exhaustion'
```

## The Problem

**`throttle-shed` is the wrong heal for a data store, and it is the only one on offer.**

The available action classes are `record`, `restart`, `throttleShed`, `warmProvider` (`:21-26`). For a process holding transient work, shedding load is correct — pressure comes from arrival rate, so reducing arrivals relieves it. For a vector store, **the corpus is the workload**: memory is a near-deterministic function of rows already stored. There is nothing to shed, restarting frees nothing durable, and the pressure returns the moment the corpus is reloaded.

Measured on the live plane 2026-08-06: chroma at 4096-dim float32 needs `rows x 16 KiB x ~1.38`. At ~96,000 rows that is **~2.02 GiB against a 2.00 GiB cap**. Because the store **exits cleanly** rather than being OOM-killed (`OOMKilled=false ExitCode=0 RestartCount=13`), each attempt to reach a complete corpus stops near completion and reads as a graceful restart. A restore of 59,754 chunks died at 24,000 this way.

So the diagnosis layer can *see* the condition and has **no vocabulary to express the only heal that works**: raise the ceiling.

**Second-order:** because growth is monotonic and predictable, 90% is late for a store. By the time saturation is sustained at 90%, the remaining headroom is smaller than one ingestion batch.

## The Architectural Reality

Three pieces exist and are not connected:

1. **Detection** — `ContainerHealthDiagnosisService`, per-service, sustained-window, measured against the limit.
2. **A bounded actuator for config intent** — `RecoveryActuatorService` `DEFAULT_ACTIONS` includes `reconfigure` (`:23`), dispatching to `reconfigureComposeService({knob, knobValues, reason, target})` (`:582`). `escalate` and `page` are already deleted from that set, consistent with the autonomous-heal directive.
3. **An intent-based knob registry** — `RECOVERY_KNOBS` (`recoveryKnobRegistry.mjs:39`). Its docblock states the property that makes it the right home: *"A controller asks to widen the mini-summary window; it never names leaves."* Bounds are enforced in the registry (`:190-191`), so a controller cannot request an unbounded change.

**What blocks the connection:** chroma's limit is hardcoded `memory: 2g` (`docker-compose.yml:47`), so no knob can reach it. `local-model` already demonstrates the controllable form in the same file — `memory: "${NEO_LOCAL_MODEL_MEMORY_LIMIT:-32g}"`. The store is the one component whose ceiling is both load-bearing and unreachable.

**And a live raise is possible without recreation.** Verified against a disposable target (ingress, 256m → 300m → restored): `docker update --memory` is accepted and takes effect on a running container. So the heal can be immediate and autonomous; the compose/env value is what makes it durable across the next recreate.

## The Fix

- **Parameterise chroma's ceiling** as `${NEO_CHROMA_MEMORY_LIMIT:-8g}`, following `local-model`'s existing precedent. The default is derived, not preferred: `rows x 4096 x 4B x 1.38` puts a complete corpus at ~2.02 GiB, and 8g admits ~370k rows — about 4x — at ~17% of the docker VM.
- **Add a bounded `container-memory-ceiling` knob** to `RECOVERY_KNOBS`, expressing the intent *"raise this service's memory ceiling"* with registry-enforced min/max so a controller can never request an unbounded value.
- **Classify services** as store-backed or transient, and route `memory-saturation` on a store to the ceiling-raise intent rather than `throttleShed`. Transient services keep `throttleShed` unchanged.
- **Lower the store threshold** to 80%, because monotonic growth makes 90% later than one ingestion batch.

**Deliberately not in this ticket:** the vector dimension. Reducing 4096 would cut resident memory proportionally and is rejected on operator direction — below ~4k, `query_documents` retrieval quality degrades materially, and it would require re-embedding the whole corpus. Recorded so it is not re-proposed as a cheaper fix.

## Decision Record impact

`amends ADR-0025` (Orchestrator Container Health Self-Healing) and `amends ADR-0026` (Orchestrator Recovery Actuator, `Proposed`).

ADR-0025's action-class taxonomy has no ceiling-raise class; ADR-0026's actuator matrix has no row for it. Both need the store-versus-transient distinction, because the same fact warrants opposite heals depending on whether the pressure comes from arrival rate or from stored data. ADR-0026 being pre-merge makes extending it cheaper than amending later; @neo-opus-grace authored both and holds the deepest context on the un-healable class, so she is the right reviewer.

## Handover to @neo-fable-clio — 2026-08-07, and three ACs were stale

Reassigned at @tobiu's request. Before handing it over I verified every AC against the tree rather than
trusting its checkbox, and **three of eight had shipped with PR #16597 and were never ticked** — corrected
below with the delivering file and line. Left unticked they would have cost the next owner a cycle
reimplementing a code path that already has a passing spec citing this ticket number.

**Genuinely open:** the healthy-state classification projection · a `container-memory-ceiling` knob with
registry-enforced bounds (searched: nothing exists, not even partial) · the bounded/anti-thrash raise ·
the ADR-0025/0026 rows · the post-merge observability check.

**Sequencing.** Draft PR #16634 modifies the same `collectStatsFacts` function in the same file — it adds
an effective-heap-ceiling denominator for NON-STORE services. The projection AC moves classification fields
out of `if (memoryWindow.sustained)` in that same function. Different concerns, real textual conflict risk;
branching after #16634 merges avoids it.

**Adjacent but deliberately separate:** #16630 leaves *service*-class exhaustion unrouted to `raiseCeiling`
on purpose. This ticket's store-side routing is delivered. The two were split, not duplicated.

## Acceptance Criteria

- [ ] **A healthy-state classification projection** — `DeploymentStateBridge` emits `serviceClass`, `serviceClassDeclared`, the applied threshold, and `observedWindowMs` beside `requiredWindowMs` for every store-classified service on **every** snapshot, independent of load. *Moved here from #16603 on 2026-08-07 (`@neo-gpt` cycle-4).* Today those fields exist only inside `if (memoryWindow.sustained)`, so a healthy store at the raised `8g` ceiling exposes none of them — which makes any load-independent observability claim unverifiable and is why three successive post-merge formulations on #16603 were each unobservable. It is **new behaviour**, not a wording fix, so it belongs on this ticket beside the other undelivered half rather than blocking the leaf that delivered detection.
- [x] chroma's compose limit is env-parameterised with a derived default, and the arithmetic is recorded inline. **Delivered by PR #16597** — `memory: "${NEO_CHROMA_MEMORY_LIMIT:-8g}"` with the reachability rationale and the resident-vector arithmetic recorded above it. *(Ticked 2026-08-07 on handover; it had shipped and gone unchecked.)*
- [ ] A `container-memory-ceiling` knob exists with registry-enforced bounds; a request outside them is rejected with a violation rather than clamped silently.
- [x] A store-class service at sustained saturation yields a ceiling-raise action class, **not** `throttle-shed`. Spec asserts the action class, not merely that a fact was emitted. **Delivered by PR #16597** — `ContainerHealthDiagnosisService.mjs:726-732` returns `actionClass: raiseCeiling` with `reason: 'store-ceiling-exhaustion'`, and `ContainerHealthDiagnosisService.spec.mjs:650` asserts `decision.actionClass` directly, exactly as this AC demands. *(Ticked 2026-08-07 on handover.)*
- [x] A transient-class service at sustained saturation still yields `throttle-shed` — a negative control, so the change is proven narrow rather than global. **Delivered by PR #16597** — `ContainerHealthDiagnosisService.spec.mjs:700`, whose comment reads *"Negative control for the ROUTING."* *(Ticked 2026-08-07 on handover.)*
- [ ] The raise is bounded and anti-thrash: repeated saturation cannot ratchet the ceiling indefinitely, and the bound is asserted by spec. *(Restated 2026-08-07 per comment 2, folded on pickup: without a restart there is no restart-storm, so the **ratchet is the only thrash vector left** — the bound lives in the registry, where an out-of-band request is a thrown violation rather than a silent clamp.)*
- [ ] A store-class ceiling raise does **not** restart its target. Spec asserts the restart path is not called on the store path — a negative assertion, since the defect is an extra call rather than a wrong value. *(Folded from comment 2's forced AC revisions on 2026-08-07 pickup — `@neo-opus-vega`'s finding that `reconfigure` couples the mutation to a restart, and for a store mid-ingestion that restart is the harm itself.)*
- [ ] ADR-0025 and ADR-0026 carry the new action class, the actuator row, and the store/transient rationale.
- [ ] *(post-merge)* A store crossing 80% produces a raise attempt with a recorded reason, observable without reading logs.

## Out of Scope

- ~~Applying a live `docker update` raise. Verified feasible, but wiring the runtime call belongs with the actuator's ops layer; this ticket delivers the detection, vocabulary, and bounded intent.~~ *(Superseded 2026-08-07 by comment 2, recorded on pickup: reading "the actuator's ops layer" as reuse of `reconfigure` would have carried the restart in with it, and the restart is the harm. The live raise IS the store path's activation step and ships with the actuator half — the no-restart AC above is its guard.)*
- The chroma recreate that activates a new default on the running plane — operator-owned.
- Any other service's ceiling, and revisiting `local-model`'s 32g.
- Chunk-identity/tenant-slug divergence, a separate deletion-class failure — not this ticket.
- #16563's false-success receipts; #16561's backup starvation.

## Avoided Traps

**Adding a watchdog.** The obvious reading of "we have no pre-cliff signal" is to build a monitor. The monitor exists, measures the right ratio, and applies a sustained window. Building a second one would have duplicated working code and left the actual defect — a store being told to shed a corpus — in place. The gap is *vocabulary and routing*, not observation.

**Raising the number alone.** A fixed cap on a store whose size grows with the repo re-creates the cliff further out. The ceiling has to be reachable by a controller, or the next corpus doubling repeats this incident.

## Related

- #16595 — the measurement that produced the sizing; this supersedes its config-only shape.
- ADR-0025 · ADR-0026 · #16463 (ceiling-sizing lane, @neo-opus-grace).
- #16452 — activation kernel as the only mutation path; a durable ceiling change is a config mutation, so the knob-override path must compose with it rather than bypass it.
- #16549 (the losses) · #16563 · #16561 · #16566.

Origin Session ID: 6004a4aa-2089-4b14-b73f-b58c08cf53d9

Retrieval Hint: `query_raw_memories("store memory saturation throttle-shed wrong heal raise ceiling knob")` · `ContainerHealthDiagnosisService.mjs:586-598`

Authored by @neo-opus-vega (Claude Opus 5).


## Timeline

- 2026-08-06T15:09:02Z @neo-opus-vega added the `bug` label
- 2026-08-06T15:09:02Z @neo-opus-vega added the `ai` label
- 2026-08-06T15:09:02Z @neo-opus-vega added the `architecture` label
- 2026-08-06T15:09:25Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-06T15:19:36Z @neo-opus-vega cross-referenced by PR #16597
### @neo-opus-vega - 2026-08-06T18:07:25Z

## Handover — @neo-opus-vega, session sunset (scope: convergent)

**PR #16597** open on `agent/16596-store-ceiling-heal` @ `6711b40935`. CI was re-running after an **infrastructure** failure in *AiConfig Antipattern Lint*'s "Set up job" step — the check itself passes locally, and `dev` was 0 commits ahead so the rerun's merge ref was current. **Verify CI before assuming it needs work.**

### What is delivered vs claimed

Delivered: `raise-ceiling` action class, frozen `STORE_BACKED_SERVICE_KEYS`, store memory-saturation routed to the raise **ahead of** `hasAuthoritativeEvidence`, 80% store threshold, the fact reporting the threshold actually applied, and chroma's compose limit parameterised `${NEO_CHROMA_MEMORY_LIMIT:-8g}`.

**Not delivered, and the PR says so:** the bounded `container-memory-ceiling` knob in `RECOVERY_KNOBS`, the runtime `docker update` call, and the ADR-0025/0026 amendments. Those ACs on this ticket are **unmet** — do not close it on the PR merging.

### The two facts worth not re-deriving

1. **Why nothing ever fired.** `hasAuthoritativeEvidence` needs `minAuthoritativeFacts` (2). A store at its ceiling saturates memory with **idle CPU** — measured live `mem=81.40% cpu=0.00%` — so it emits exactly ONE authoritative fact, permanently. The floor discarded the whole diagnosis. It was never mis-healed; it was never diagnosed.
2. **`docker update --memory` works live.** Verified on a throwaway target (ingress 256m → 300m → restored), so an autonomous raise needs no recreate. But it is **ephemeral** — lost on the next `up` — so the durable half is a config mutation, and #16452 holds the activation kernel as the only mutation path. Detect-not-actuate + kernel-only-mutation compose to *"the daemon escalates a bounded request, the kernel applies it."*

### Test posture

Five tests, three of them controls. **Mutation-tested:** keying the branch on service class instead of on a memory fact **passes the positive test** and is caught only by the CPU control. A plain revert is NOT a usable RED here — removing the export breaks the spec at import, so nothing runs, which proves dependency rather than discrimination.

### Pickup

Reviewer is @neo-opus-grace (authored ADR-0025/0026, holds #16463). @neo-gpt was asked for an A2A-only review read, to be sent to her rather than to me. Next lane is the actuator half.

Authored by @neo-opus-vega (Claude Opus 5).


### @neo-opus-vega - 2026-08-06T18:38:33Z

## Actuator half — the deferred design question was already answered in code, and the real defect is a coupling I was about to reuse

Entering the lane my own handover named. Two findings, and they point opposite ways from what the deferral assumed.

### 1. The #16452 tension does not exist — `writeKnobOverride` already is the kernel-mediated path

I deferred the knob on the grounds that *"detect-not-actuate plus kernel-only-mutation compose to 'the daemon escalates a bounded request, the kernel applies it' — that reconciliation is design content, not a patch."*

`RecoveryActuatorService.mjs:549-565` already implements exactly that:

```js
const {applied, path: overridePath, violations} = await writeKnobOverride({context, knob, overrideDir, values});
if (!applied) {
    throw new Error(`Knob transaction refused for '${knob}': ${violations.join('; ')}`);
}
```

Bounds are registry-enforced, a violation is **thrown before any mutation reaches the plane**, and the comment at `:558-564` records why it throws rather than returning a soft `{ok:false}`. There is no reconciliation to design — the composition ships. **Adding `container-memory-ceiling` to `RECOVERY_KNOBS` is a registry entry, not an architecture.** I over-costed the deferral, and the honest reason is that I reasoned about #16452 as a gate rather than reading the actuator that already satisfies it.

### 2. The real defect: `reconfigure` couples the mutation to a **restart**, and for a store that restart is the harm

`reconfigureComposeService` ends here (`:567`):

```js
const restart = await this.restartComposeService({target, reason});   // :627 -> operation: 'restart'
```

For a **transient** service that is coherent — the restart *is* the heal, and the new limit needs a recreate to take effect anyway.

For a **store-backed** service it is the exact failure this ticket exists to fix. A store crosses 80% *while ingesting*. Raising its ceiling via `reconfigure` would write the override and then **restart the store mid-ingestion** — destroying the operation the raise was meant to rescue. That is not hypothetical: a chroma restart at `13:44:41` mid-import is what killed the 59,754-row restore at 24,000 rows, the incident recorded in this ticket's Context.

**And the restart is not even necessary.** Measured tonight on the live plane:

```
docker update --memory 8g --memory-swap 8g neo-local-agent-os-chroma-1
RestartCount 13 -> 13     StartedAt unchanged     cgroup memory.max = 8589934592
```

The restore then resumed 24,000 → **59,754** and carried the store **through 2.153 GiB, past the old 2.00 GiB cap**, finishing at 112,808 rows / 2.359 GiB with `RestartCount` still 13.

So the store path is **not** a reuse of `reconfigure`. It is:

| step | mechanism | purpose |
|---|---|---|
| bounded intent + violation gate | `writeKnobOverride` (existing) | durability across the next `up`, kernel-mediated |
| activation | live `docker update --memory` | takes effect **now**, no restart |
| **explicitly omitted** | `restartComposeService` | a restart is the harm for a store |

**I was one step from shipping the coupling.** The plan of record in this ticket's Out of Scope — *"wiring the runtime call belongs with the actuator's ops layer"* — reads as reuse of the existing `reconfigure` action, and reuse would have carried the restart in with it. Reusing an existing channel is not the conservative choice when the channel's semantics are wrong for the new caller.

### AC revisions this forces

- **AC-2** (bounded knob) is *smaller* than filed — a `RECOVERY_KNOBS` entry plus bounds, no new mutation path.
- **A new AC is required and was missing:** a store-class ceiling raise must **not** restart the target. Spec asserts `restartComposeService` is not called on the store path — a negative assertion, since the defect is an extra call rather than a wrong value.
- **AC-5** (anti-thrash) needs restating. Without a restart there is no restart-storm, so the ratchet is the only thrash vector left: repeated saturation must not walk the ceiling up indefinitely, and the bound belongs in the registry where a violation is thrown rather than clamped.
- **ADR-0026's actuator matrix needs two rows, not one** — `reconfigure` (mutate + restart, transient) and the store variant (mutate + live activate, no restart). Collapsing them into one row is what produced this defect in the design.

@neo-opus-grace — ADR-0026 is yours and still `Proposed`, so this is cheaper to fold in now than to amend later. The two-row split is the part I would want you to push on: I am claiming the same fact (`memory-saturation`) warrants not just a different *action class* but a different *actuator envelope*, and that is a bigger claim than the one #16597 makes.

Not claimed: none of this is implemented yet. #16597 delivers detection, vocabulary and routing only; the ACs above remain **unmet** and this ticket stays open on its merge.

Authored by @neo-opus-vega (Claude Opus 5).


- 2026-08-06T19:08:45Z @neo-opus-vega cross-referenced by #16600
- 2026-08-06T21:13:17Z @neo-opus-vega referenced in commit `99b3738` - "fix(orchestrator): the sustained window is measured from sample timestamps, not asserted from config (#16596)

`summarizeSustainedWindow` took no timestamps: `sustained` was every-value-over-threshold
gated only on sample COUNT, while the emitted fact stamped `sampleWindowMs` from config as
though it had been observed. Two samples collected 10ms apart therefore satisfied a
"sustained 30 second window" and the evidence said so.

That is load-bearing rather than cosmetic. Single-fact sufficiency for a store's memory
saturation is justified by the window supplying the corroboration a second fact would
otherwise provide -- so an unenforced window cannot discharge the argument. The reasoning
was sound; the implementation had not earned it.

- rememberStatsSample stamps each sample with its observation time. The value was already
  in scope at the call site and simply dropped.
- summarizeSustainedWindow measures the first-to-last span and requires it to meet the
  configured minimum. Unstamped or single-stamp windows yield a zero span and fail closed
  rather than inheriting a window they never demonstrated.
- The fact reports observedWindowMs beside requiredWindowMs. Reporting only the configured
  value put an unobserved claim inside the evidence a heal decision reads -- the same defect
  as reporting the wrong threshold, one field over in the same object.

Five controls: back-to-back, identical-timestamp, unstamped, and just-short spans all
refuse; the same samples spanning the requirement qualify and report a measured 45000 against
a required 30000. Mutation-verified -- dropping the span condition fails exactly the four
refusals and leaves the positive control passing.

Six pre-existing fixtures asserted a sustained window from unstamped samples. They now carry
timestamps, which is what they were always claiming.

Found by @neo-gpt in review 4877846070: labeled, not enforced."
- 2026-08-06T22:04:37Z @neo-opus-vega referenced in commit `4e542f5` - "fix(orchestrator): service classification is exhaustive and genuinely immutable (#16596)

The classification was a frozen Set of store keys where ABSENCE meant transient, which made
two different things indistinguishable: "declared transient" and "nobody classified this".

Not total: only `chroma` and a non-existent `'model'` were ever exercised, while `kb-server`
and `mc-server` were unclassified. `validateServiceKey` accepts any safe string rather than a
roster member, so "unlisted = transient" applied to an unbounded key space and a future
store-backed service would silently inherit the 90% transient threshold it cannot survive.

Not immutable: `Object.freeze` on a Set locks own properties while `add`/`delete` mutate an
internal slot, so `Object.isFrozen` returned true on a set whose membership was still
writable. Verified in node: add succeeds, and delete('chroma') succeeds. A false-assurance
guarantee is worse than none.

- SERVICE_CLASS_BY_KEY declares all four `deploymentRuntimeAccess.allowedServices` keys, the
  roster the bridge actually iterates. A spec asserts coverage BOTH ways, so adding a service
  without classifying it -- or leaving a stale entry for a removed one -- fails a test.
- classifyServiceKey reports `declared`. An unrostered key still defaults to transient, since
  refusing would break unrecognised deployments, but the guess now travels into the emitted
  fact instead of disappearing.
- Immutability is asserted by ATTEMPTING write, inject and delete and checking the values,
  not by asserting `Object.isFrozen`, which is the predicate that lied on the Set.
- The old control asserted `.has('model') === false`; `'model'` is not a production service
  key at all -- `local-model` is -- so it could not fail. Replaced with roster keys.

Mutation-verified: removing `kb-server` from the map fails the totality audit with
"unclassified roster services: kb-server".

Non-totality and the Set immutability defect both found by @neo-gpt in review 4877846070."
- 2026-08-06T22:54:47Z @neo-opus-vega cross-referenced by #16603
- 2026-08-06T22:55:00Z @neo-opus-vega added sub-issue #16603
- 2026-08-07T00:04:21Z @neo-opus-vega referenced in commit `1d430c0` - "fix(orchestrator): the roster totality guard reads the committed config, not the overlay (#16596)

CI's unit job was red on two config-authority violations this branch introduced: the
two totality tests dynamically imported the overlay-resolving config entrypoint to
read allowedServices. Tests resolve committed config templates instead, so a
repo-local ignored overlay cannot decide whether the guard passes — a green there
would describe one machine rather than the shipped deployment.

Static import of ai/config.template.mjs, which subclasses the configBase leaf that
declares the roster. Both tests drop async; the non-empty roster assertion stays,
so an empty read still fails rather than passing vacuously."
- 2026-08-07T00:26:53Z @neo-opus-grace cross-referenced by #16594
- 2026-08-07T00:27:08Z @neo-opus-grace cross-referenced by PR #16606
- 2026-08-07T00:48:42Z @neo-opus-vega referenced in commit `729e5c3` - "fix(orchestrator): a sustained window must be witnessed by every sample, not merely two (#16596)

@neo-gpt's cycle-3 falsifier: filtering non-finite stamps narrowed the claim without
narrowing the verdict. Three samples carrying two stamps 30s apart produced
observedWindowMs = 30000, passed a 30s floor, and asserted the span over a third
sample no clock had witnessed - the same defect the window was added to close, one
level in.

- A positive floor now requires FULL stamp coverage. A partial-coverage span is
  unknown rather than shorter, and an unknown span cannot satisfy a floor.
- minWindowMs <= 0 keeps count-only semantics, so the requirement binds exactly where
  a temporal claim is made and unstamped callers are unaffected.
- stampCoverage is reported, so a fact can distinguish an under-length span from a
  partially stamped one instead of collapsing two different conditions.
- Mutation-proven: dropping the coverage term makes the partial-stamp case diagnose
  again, and a fully-stamped three-sample control still qualifies."
- 2026-08-07T06:42:28Z @tobiu referenced in commit `ea5b305` - "A store at its memory ceiling is diagnosed and told to raise it (#16596) (#16597)

* fix(orchestrator): a store at its memory ceiling is diagnosed and told to raise it (#16596)

Container memory saturation was already detected -- measured against the limit, over a
sustained window -- and routed uniformly to `throttle-shed`. Two defects followed, and the
second is why the condition never surfaced at all.

`throttle-shed` is incoherent for a store. Its footprint tracks rows already persisted, so
there is no arrival rate to reduce and a restart frees nothing durable; the pressure
returns as soon as the data is reloaded. It was the one exhaustion case for which no
available action class could have worked.

Worse, it was never even diagnosed. `hasAuthoritativeEvidence` requires
`minAuthoritativeFacts` (2) corroborating facts, but a store crossing its ceiling saturates
memory while its CPU sits idle -- measured live at `mem=81.40% cpu=0.00%` -- so it produces
exactly ONE authoritative fact, permanently. The corroboration floor suppressed the whole
diagnosis, which is why a store at its limit produced no signal across repeated incidents.

Adds a `raise-ceiling` action class and a declared `STORE_BACKED_SERVICE_KEYS` set, and
routes an authoritative memory-saturation fact on a store to the raise, evaluated before
the corroboration floor. Single-fact sufficiency is scoped to that branch: the floor exists
to prevent acting on one noisy signal, and a sustained-window ratio against a hard limit is
not noisy -- the window is already the corroboration the second fact stood in for.

Stores also get an earlier threshold (80% vs 90%). They cross a ceiling by growing,
monotonically, so at sustained 90% the remaining headroom is smaller than one ingestion
batch. The emitted fact now reports the threshold ACTUALLY applied rather than the
transient default, which would otherwise place a falsehood inside the evidence a heal is
chosen from.

Chroma's compose limit is parameterised as `${NEO_CHROMA_MEMORY_LIMIT:-8g}`, matching the
form `local-model` already uses. A hardcoded literal cannot be raised by any controller,
which is the mechanical reason the store's ceiling was unreachable. The default is derived:
vectors are resident, so `rows x 16 KiB x ~1.38` puts a complete store at ~2.03 GiB against
the previous 2g -- over by about one percent, which is why failures clustered near
completion and left no crash signature. Reducing the 4096 dimension would cut this
proportionally and is rejected: retrieval quality degrades materially below ~4k.

Scoped narrowly, with three controls proving it: a transient service at the same load stays
healthy (the earlier threshold did not leak globally), a transient genuinely over its
ceiling still sheds (routing unchanged), and a store saturating CPU does NOT raise (only a
memory fact may justify it). Mutation-tested -- keying the branch on service class instead
of on a memory fact passes the positive test and is caught only by the third control.

* fix(orchestrator): the sustained window is measured from sample timestamps, not asserted from config (#16596)

`summarizeSustainedWindow` took no timestamps: `sustained` was every-value-over-threshold
gated only on sample COUNT, while the emitted fact stamped `sampleWindowMs` from config as
though it had been observed. Two samples collected 10ms apart therefore satisfied a
"sustained 30 second window" and the evidence said so.

That is load-bearing rather than cosmetic. Single-fact sufficiency for a store's memory
saturation is justified by the window supplying the corroboration a second fact would
otherwise provide -- so an unenforced window cannot discharge the argument. The reasoning
was sound; the implementation had not earned it.

- rememberStatsSample stamps each sample with its observation time. The value was already
  in scope at the call site and simply dropped.
- summarizeSustainedWindow measures the first-to-last span and requires it to meet the
  configured minimum. Unstamped or single-stamp windows yield a zero span and fail closed
  rather than inheriting a window they never demonstrated.
- The fact reports observedWindowMs beside requiredWindowMs. Reporting only the configured
  value put an unobserved claim inside the evidence a heal decision reads -- the same defect
  as reporting the wrong threshold, one field over in the same object.

Five controls: back-to-back, identical-timestamp, unstamped, and just-short spans all
refuse; the same samples spanning the requirement qualify and report a measured 45000 against
a required 30000. Mutation-verified -- dropping the span condition fails exactly the four
refusals and leaves the positive control passing.

Six pre-existing fixtures asserted a sustained window from unstamped samples. They now carry
timestamps, which is what they were always claiming.

Found by @neo-gpt in review 4877846070: labeled, not enforced.

* fix(orchestrator): service classification is exhaustive and genuinely immutable (#16596)

The classification was a frozen Set of store keys where ABSENCE meant transient, which made
two different things indistinguishable: "declared transient" and "nobody classified this".

Not total: only `chroma` and a non-existent `'model'` were ever exercised, while `kb-server`
and `mc-server` were unclassified. `validateServiceKey` accepts any safe string rather than a
roster member, so "unlisted = transient" applied to an unbounded key space and a future
store-backed service would silently inherit the 90% transient threshold it cannot survive.

Not immutable: `Object.freeze` on a Set locks own properties while `add`/`delete` mutate an
internal slot, so `Object.isFrozen` returned true on a set whose membership was still
writable. Verified in node: add succeeds, and delete('chroma') succeeds. A false-assurance
guarantee is worse than none.

- SERVICE_CLASS_BY_KEY declares all four `deploymentRuntimeAccess.allowedServices` keys, the
  roster the bridge actually iterates. A spec asserts coverage BOTH ways, so adding a service
  without classifying it -- or leaving a stale entry for a removed one -- fails a test.
- classifyServiceKey reports `declared`. An unrostered key still defaults to transient, since
  refusing would break unrecognised deployments, but the guess now travels into the emitted
  fact instead of disappearing.
- Immutability is asserted by ATTEMPTING write, inject and delete and checking the values,
  not by asserting `Object.isFrozen`, which is the predicate that lied on the Set.
- The old control asserted `.has('model') === false`; `'model'` is not a production service
  key at all -- `local-model` is -- so it could not fail. Replaced with roster keys.

Mutation-verified: removing `kb-server` from the map fails the totality audit with
"unclassified roster services: kb-server".

Non-totality and the Set immutability defect both found by @neo-gpt in review 4877846070.

* fix(orchestrator): the roster totality guard reads the committed config, not the overlay (#16596)

CI's unit job was red on two config-authority violations this branch introduced: the
two totality tests dynamically imported the overlay-resolving config entrypoint to
read allowedServices. Tests resolve committed config templates instead, so a
repo-local ignored overlay cannot decide whether the guard passes — a green there
would describe one machine rather than the shipped deployment.

Static import of ai/config.template.mjs, which subclasses the configBase leaf that
declares the roster. Both tests drop async; the non-empty roster assertion stays,
so an empty read still fails rather than passing vacuously.

* fix(orchestrator): a sustained window must be witnessed by every sample, not merely two (#16596)

@neo-gpt's cycle-3 falsifier: filtering non-finite stamps narrowed the claim without
narrowing the verdict. Three samples carrying two stamps 30s apart produced
observedWindowMs = 30000, passed a 30s floor, and asserted the span over a third
sample no clock had witnessed - the same defect the window was added to close, one
level in.

- A positive floor now requires FULL stamp coverage. A partial-coverage span is
  unknown rather than shorter, and an unknown span cannot satisfy a floor.
- minWindowMs <= 0 keeps count-only semantics, so the requirement binds exactly where
  a temporal claim is made and unstamped callers are unaffected.
- stampCoverage is reported, so a fact can distinguish an under-length span from a
  partially stamped one instead of collapsing two different conditions.
- Mutation-proven: dropping the coverage term makes the partial-stamp case diagnose
  again, and a fully-stamped three-sample control still qualifies."
- 2026-08-07T12:59:27Z @neo-opus-vega cross-referenced by #16630
- 2026-08-07T13:46:47Z @neo-opus-vega assigned to @neo-fable-clio
- 2026-08-07T13:46:47Z @neo-opus-vega unassigned from @neo-opus-vega
### @neo-opus-vega - 2026-08-07T13:46:49Z

**`[lane-override]` reassignment audit-trail** (#11537 §AC8)

**Previous assignees:** `@neo-opus-vega`
**New assignees:** `neo-fable-clio`
**Reason:** @tobiu asked whether @neo-fable-clio could work on #16596 and whether she needed input from me. Handing the lane over with a high-priority A2A covering the three ACs already shipped-but-unchecked, the collectStatsFacts conflict with my in-flight PR #16634, and the projection AC's provenance. I remain available for the actuator half if she prefers a detect/act split.

*Audit-trail per AGENTS.md §6.5 — `acknowledgedReassign` reason persistence. Graph-ingested via Retrospective daemon comment-scan path.*

- 2026-08-07T14:11:50Z @neo-opus-vega cross-referenced by #16636
- 2026-08-07T14:12:48Z @neo-opus-vega marked this issue as blocking #16636
- 2026-08-07T14:14:35Z @neo-opus-vega cross-referenced by PR #16634
- 2026-08-07T14:26:38Z @neo-fable-clio cross-referenced by #16637
- 2026-08-07T14:26:51Z @neo-fable-clio added sub-issue #16637
- 2026-08-07T14:27:46Z @neo-fable-clio referenced in commit `0f85aed` - "docs(orchestrator): the action matrix discloses throttle-shed's cross-world state (#16596)

Mid-flight peer probe (@neo-opus-vega, #16636): the diagnosis layer emits a
throttle-shed action class for transient exhaustion whose only shipped
implementation is collection-keyed in ADR-0027's data-recovery world — the
lifecycle actuator this matrix governs has no throttle/shed operation. The
section was amended by #16374 specifically so a reader can tell which actions
they can actually call; one disclosure sentence keeps it honest for that class
and points reconciliation at #16636, which is sequenced blocked_by #16596 so
the sanction never travels separately from the sanctioned code."
- 2026-08-07T14:27:58Z @neo-fable-clio cross-referenced by PR #16638
- 2026-08-07T15:14:45Z @neo-opus-vega cross-referenced by PR #16640
- 2026-08-07T15:18:33Z @neo-fable-clio cross-referenced by #16431
- 2026-08-07T15:19:50Z @neo-opus-vega cross-referenced by #16642
- 2026-08-07T15:30:54Z @neo-fable-clio cross-referenced by PR #16643

