---
id: 16499
title: Re-entry paints the pane in both embodiments and stacks the landing region
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-fable
createdAt: '2026-08-04T12:14:14Z'
updatedAt: '2026-08-07T15:00:44Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16499'
author: neo-fable
commentsCount: 8
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-07T15:00:44Z'
---
# Re-entry paints the pane in both embodiments and stacks the landing region

## Context

Two operator-captured stills from the take-18 WIP film cut at t≈29–31 (cut-relative; raw = +11.3s; dev head `2cd55fdd7d`; capture sha256 `3dfc1d13bd13d93077de8eb4ec8b1491582135d97c05999ae6f807bbb2ef6495`), corroborated by 6fps frame bursts. During the native vessel's return drag and drop: (a) the `System Metrics` pane content is visible **simultaneously** in the native vessel AND inside the main window's right column, with a floating `LIVE METRICS` label over the matrix area and the `Evidence Audit` card rendering ghosted label-over-label; (b) one frame later, the landing region shows `Evidence Chain` AND the squeezed `Live Metrics` card **stacked at once** — where tab semantics should render exactly one active pane — plus a truncated `Me…` tab header painted PAST the main window's right edge mid-relayout. Sweeps: live latest-20 open (2026-08-04 ~12:15Z) no equivalent; A2A last-30 no claim; the label-over-label paint class is `#16406`'s family (tab-drag label paint, open, distinct scope — referenced, not duplicated).

## The Problem

The re-entry chain (native vessel → claim → drop into `right-top-tabs`) produces a window of visibly wrong render truth: double embodiment of one pane, ghosted label compositing, then a post-drop region rendering two panes' content simultaneously with a tab header escaping the container bounds. Part of this MAY be the documented transient proxy embodiment (the dashboard drag proxy carries `computedOpacity 0.7` by design) — but the observed frames show full-opacity content in both places, and the post-drop stacked state persists beyond staging frames.

## The Architectural Reality

- The transfer path publishes preview/placement via the harness dock preview (`neo.harness.dockPreview.v1`, placement kinds like `tab-into`) and the beat receipts carry `targetProxy` + placement — the classification can bind each frame to the receipt's declared state.
- Candidate seams: (1) the projection commit racing the vessel retirement (both embodiments live because the source vessel has not retired when the target renders — adjacent to `#16497`'s conversion boundary but a distinct symptom: RENDER truth, not gesture timing); (2) FLIP staging frames presenting measured-but-unstaged content (`#16356`'s closed class, entering-classification); (3) tab activation rendering the incoming pane before the region's prior active pane deactivates (stacked content); (4) the escaped tab header = mid-relayout header toolbar painting outside its clipped container (overflow/clip seam in `src/tab/` chrome during item insertion).
- Reproduction: film-paced five-beat native-titlebar beat (`NEO_FILM_TAKE=1 … --headed --grep "native titlebar"`), frames at the drop moment.

## The Fix

Classification first, frame-by-frame against the semantic receipts: for each symptom (double embodiment, ghost labels, stacked region, escaped header) bind the frame to the receipt state (`converted`, placement kind, activeItemId) and split {documented-transient | staging-frame | real double-render}. Repair the real ones at their owning seams; explicitly document any accepted transient with its bounded duration so the film QA gate can pin it.

## Acceptance Criteria

- [ ] Each of the four symptoms is classified with a frame+receipt pair naming its owner.
- [ ] After repair: no frame shows one pane's content at full opacity in two embodiments; the landing region renders exactly one active pane's content post-drop; no tab header paints outside its container during relayout.
- [ ] Accepted transients (if any) are documented with bounded frame counts and covered by the film QA oracle.
- [ ] Witness at film pacing, red-proven at the pre-fix head.

## Out of Scope

- The conversion claim-boundary timing itself (`#16497`).
- The tab-drag label paint artifact (`#16406`, Phoebe's — referenced family, separate lane).
- Empty-sibling-pane body (`#16498`).

## Decision Record impact

none.

## Related

`#15252`, `#16497`, `#16498`, `#16406`, closed `#16356` (staging-frame precedent), `#16412` (restore-projection hazard context).

Origin Session ID: 1913de09-6dc0-4d1e-a9a3-b51c33b46cdc

Retrieval Hint: `query_raw_memories("re-entry double embodiment stacked landing region escaped tab header ghost labels take-18")`

## Timeline

- 2026-08-04T12:14:15Z @neo-fable added the `bug` label
- 2026-08-04T12:14:15Z @neo-fable added the `ai` label
- 2026-08-07T08:01:27Z @neo-fable assigned to @neo-fable
### @neo-fable - 2026-08-07T08:09:51Z

## Classification r1 — reproduction at head `ea5b3058ff` (CDP path, film pace, 2 runs)

**Method:** scene 4 (reintegration) — the CDP re-entry vehicle for the ticket's chain (native vessel → claim → drop into `right-top-tabs`) — run `NEO_FILM_TAKE=1 … -g "scene 4" --headed` ×2 at the current dev head. Film mode records per-page Playwright video (25fps); analysis = per-frame `signalstats` YAVG series + full-res frame extraction at the events. Beat receipts from the spec's own phase contract bind the frames: `documents-adopted → main-projected → close-dispatched → topology-exited` ([WorkstationFiveBeatNL.spec.mjs:2198](https://github.com/neomjs/neo/blob/dev/test/playwright/e2e/workstation/WorkstationFiveBeatNL.spec.mjs#L2198)).

**Per-symptom disposition at this head:**

| # | symptom (ticket) | runs | disposition |
|---|---|---|---|
| S-a | full-opacity double embodiment (content in vessel AND main simultaneously) | **not reproduced ×2** | On the CDP path the vessel body clears at `documents-adopted` BEFORE `main-projected` renders — no simultaneous full-opacity window exists. The originating stills predate the `#16497` fix and came from the native-input reel → the native-titlebar beat (staged run, operator window) is the remaining discriminator. |
| S-1 *(sharpened)* | **whole-workspace blank at adoption** | camera-caught 1/2 | REAL: for ≥1 frame (~40ms when caught) the ENTIRE workspace body un-renders during `main-projected` — all panes gone, header + rail survive (full-res frame receipt, run 1 t=6.56; YAVG 48.2 → 36.1 → 48.0). 1/2 on camera = 25fps sampling of a ~40ms event, not nondeterminism evidence — the repair witness needs a paint-level oracle (the `#16500` entropy-instrument class), not video sampling. Presentation family = `#16500`'s cleared-body, **distinct trigger** (re-entry adoption vs film-paced resize). |
| S-2 | stacked/ghosted landing region | 1/1 examined | Transitional: the post-blank recovery frame composites the audit pane's label AND a ghosted mis-positioned card (run 1 t=6.60), settling clean ≤0.4s later (t=7.00). Staging-frame class (`#16356`'s closed cousin, visible echo). Repair fork: eliminate via render continuity (don't re-stage what didn't move) vs bound+document for the film QA oracle. |
| S-3 *(sharpened)* | **emptied-vessel residue + surviving drag affordance** | **2/2** | REAL: vessel body clears to black at `documents-adopted` (YAVG step −4.0 both runs: t=5.20/t=5.80) but the window persists **~1.0s both runs** until `topology-exited`/OS close — a dead black window with the grouped-drag "…" affordance floating at FULL opacity on the emptied body (full-res receipt t=5.60). Spec tolerates up to 15s here. Two sub-defects: (i) unmanaged terminal presentation of the closing vessel, (ii) the gesture affordance is not retired with the content it decorated. |
| S-d | tab header escapes container bounds mid-relayout | not reproduced ×2 (800×600 sampling) | Pending the native-path run + higher-res frames; possibly `#16497`-fixed or input-path-specific. |

**Mechanism frame:** main-side S-1/S-2 live in `main-projected`'s whole-surface re-projection (the landing should re-stage incrementally — only the receiving region changed); vessel-side S-3 lives in the adoption→close gap. S-a's absence on this path is itself a receipt: the phase order already serializes the embodiments — the film-visible defects are the *presentation* of each phase, not the ordering.

**Repair plan (this lane):**
1. **S-1/S-2:** incremental adoption projection (only the landing region re-stages) or painted-readiness gating on the swap; witness = paint/entropy oracle red-proven at the pre-fix head.
2. **S-3:** retire the drag affordance with the gesture; manage the closing vessel's terminal second (hide/fade at `close-dispatched` rather than a 1s dead window).
3. **S-a/S-d discriminator:** one staged native-titlebar run inside the operator's hands-off window (doubles as take-19 prep).

AC mapping unchanged: S-1/S-3 → the "real" bucket; S-2 → candidate documented-transient with bounded frame count; S-a/S-d → pending the native discriminator.

*Receipts local to the film host: `scratchpad/16499-frames/` (contact sheets, full-res frames, YAVG tables); videos in `test/playwright/test-results/e2e/artifacts/`.*

- 2026-08-07T08:10:29Z @neo-fable cross-referenced by #16500
### @neo-fable - 2026-08-07T08:38:45Z

## Classification r2 — seam map (post app-work-gate contract reads; head `ea5b3058ff`, branch `agent/16499-reentry-render-truth`)

**S-1/S-2 candidate root — a preservation asymmetry in the transfer projection path.** The cross-window transfer handler serializes phases at [Workspace.mjs:1720-1760](https://github.com/neomjs/neo/blob/dev/apps/workstation/view/Workspace.mjs#L1720-L1760):

- **main→vessel** direction projects with `preserveItemIds: Object.keys(targetState.document?.items || {})` (:1744-1746) — the reconciler keeps existing panes alive and re-stages only what changed.
- **vessel→main return** (this ticket's direction) calls `refreshDockWorkspace()` **with no arguments** (:1750) → `preserveItemIds=[]` → `DockProjectionReconciler.reconcileProjection` receives an empty preservation set and re-stages the ENTIRE workspace.

The r1 frames match this exactly: the whole-workspace blank (S-1, every pane un-renders for ~40ms) and the post-blank ghost composite (S-2, the audit pane's content re-staging under FLIP) are what a full re-stage looks like at film pace. The asymmetry also explains why only the return direction is film-dirty. **Repair candidate (small):** pass the unmoved main items as the preservation set at :1750 — pending a contract read of `reconcileProjection` (semantics of preserved-vs-incoming ids, interplay with `DockFlip.captureFirst`/`#16356`'s zero-rect precedent) before any edit.

**S-3a (surviving drag chip) — proxy lifecycle map.** The blue "…" chip is the stack-drag proxy owned by [`src/dashboard/DockTabSortZone.mjs`](https://github.com/neomjs/neo/blob/dev/src/dashboard/DockTabSortZone.mjs): armed at :949 (`stackDragActive=true` → `dragStart` creates the proxy), retired in `processDragEnd`'s finally block :1173-1176 (`dragEnd` → proxy teardown). The gesture's mouseup IS simulated on the source window ([Workspace.mjs:4668-4671](https://github.com/neomjs/neo/blob/dev/apps/workstation/view/Workspace.mjs#L4668-L4671)), so source-side drag-end should run — yet the proxy demonstrably outlives the content by ~1.0s (r1, 2/2). Drag proxies are main-thread DOM managed by the DragDrop addon (outside vdom), so the removal rides a main-thread call that can be skipped or starved in the closing window. **Probe next:** timestamp source-side `processDragEnd`/`dragEnd` vs proxy-DOM disappearance vs `documents-adopted` in one instrumented scene-4 run — discriminates {end-never-fires | end-fires-removal-starved | end-fires-late}. NOTE: this seam is ENGINE-shared (every dock consumer — Body/engine layer), not app-local — repair lands in `src/dashboard/`, blast radius accordingly.

**S-3b (dead-window second) — presentation seam.** Phase receipts: `documents-adopted` :1724 (source un-projection = the content clear) → `main-projected` :1747/:1751 → `close-dispatched` :1861 → `topology-exited` :3635. The phase ORDER is contract (scene-4 pins model-before-close; not a repair surface). The emptied second is presentation: the source vessel needs a deliberate terminal state applied WITH the un-projection (departing class / fast fade of the vessel root) so the closing window reads as exit, not corpse.

**Witness plan:** S-1 needs a paint-level oracle (the `#16500` entropy-instrument class — 25fps video sampling caught it 1/2 in r1); S-3a probe doubles as its regression witness; S-3b witnesses via frame QA at film pace. All red-proven at the pre-fix head before any repair merges.


### @neo-fable - 2026-08-07T08:51:35Z

## Classification r3 — r2's candidate mechanism FALSIFIED by the contract read; the real seam is one level up (and the repair got smaller)

**Correction of my own r2:** `preserveItemIds` does not mean "keep these panes alive during re-stage." Its contract ([DockProjectionReconciler.mjs:288-289](https://github.com/neomjs/neo/blob/dev/src/dashboard/DockProjectionReconciler.mjs#L288)): *"Owner-held panes which are absent from this projection but must survive without their obsolete tab buttons"* — i.e., panes that **departed** for another workspace and must not be destroyed. The `:1744` vs `:1750` asymmetry I flagged is therefore semantically **correct as written** (main→vessel: main loses panes → they need survival; vessel→main return: panes arrive, none depart → empty set is right).

**The real S-1 mechanism:** `reconcileProjection` has two paths — a strict **stable-topology fast path** (in-place reconciliation on the retained shell) and a **full path** (stage a hidden new shell, move chrome, swap). The fast path with `reconcileItems: true` explicitly admits *"tab item/active deltas while retaining the proven-identical structural shell"* (:123-124) and runs the same `reconcileTabChrome` that materializes arriving pane/button pairs. But `refreshDockWorkspace` keys that admission to **`operation === 'detachItem'` only** (:2115) — and the return-transfer call site passes **no operation** (:1750). So a stack-return adoption — which is topologically stable (structural node set identical; one tabs node's items grow) — is forced through the full path: hidden shell staged, whole workspace re-materialized, swap. The measured ~40ms whole-workspace blank (S-1) and the FLIP re-stage ghost (S-2) are the swap's paint signature.

**Repair A (small, safe-by-construction):** thread the operation through the return call site (`refreshDockWorkspace({operation: 'transferNode'})` at :1750) and widen the `retainTopology` admission to include it. The stable-topology validator remains the gate: any transfer that genuinely changes the structural node set (e.g. a split-creating placement) fails its identity checks, returns `null`, and takes today's full path unchanged. The fast path only engages when provably stable — the strictness check IS the falsifier.

**Witness (discriminating, deterministic):** worker-side pane identity survives BOTH paths (`resolveItem` reuses live instances — which is why scene-4's existing `getPaneIdentity` pins pass today). The discriminator is **DOM node identity** of an unmoved pane across the return transfer: tag the live DOM node pre-return (JS property, not attribute), assert the same node post-settle. Full path re-materializes the subtree → tag gone → RED at pre-fix head; stable path retains the shell → GREEN. Extends scene 4; red-proof via src-stash run before the fix lands.

**Noted, not yet in scope:** the outbound main→vessel transfer (:1744) also passes no operation and takes the full path — same defect class, symmetric one-line fix, but its witness rides a different scene; decision at PR time whether it lands here or as an immediate follow-up.


- 2026-08-07T09:29:34Z @neo-fable cross-referenced by #16620
- 2026-08-07T09:30:51Z @neo-fable cross-referenced by #16621
- 2026-08-07T09:32:35Z @neo-fable cross-referenced by PR #16622
### @neo-fable - 2026-08-07T09:32:51Z

S-1/S-2 delivered: the split-leaf #16621 carries the repair (stable-topology fast path for stack-return adoption) — PR https://github.com/neomjs/neo/pull/16622, red-proven shell-identity witness + film-truth YAVG receipt (no body-clear frame at the fixed head; pre-fix dip 36.1 vs post-fix minimum 68.4). This ticket stays OPEN for: S-3 (emptied-vessel residue + surviving drag affordance — probe designed in r2, seam `src/dashboard/DockTabSortZone`), the native-input discriminator for S-a/S-d (rides the next staged take), and the outbound main→vessel admission twin (decision after #16621 lands). Suite-table byproduct: the ambient gesture instability is now #16620 (pool).

### @neo-fable - 2026-08-07T10:10:33Z

## Probe r4 — S-3a discriminator resolved: the removal is STARVED, not skipped

**Method:** throwaway working-tree instrumentation on a probe branch off dev (event pushes to a worker-global at stack-arm / dragEnd-enter / dragEnd-done / the transfer phases + a temp worker-side reader; scene-4 film-paced run; everything reverted, zero commits — receipts below are the full yield).

**Timeline (ms relative to the return gesture's `stack-arm`):**

| t | event |
|---|---|
| +209 | `dragEnd-enter` (source-side mouseup processed, `cancelled: false`) |
| +210 | `documents-adopted` (return commit) **and** `dragEnd-done` — the proxy teardown call completed |
| +719 | `main-projected` (509ms of staged full-path re-stage at the pre-fix head) |
| +719 | `close-dispatched` (immediately after projection) |
| ~+1200 | OS window close (from the r1 video: chip visible to the end) |

**Verdicts on r2's three-way split:** `end-never-fires` FALSIFIED · `end-fires-late` FALSIFIED · **`end-fires-removal-starved` CONFIRMED** — `dragEnd` runs to completion within ~2ms of the drop, yet the grouped-drag chip persists on the emptied vessel until OS close. The teardown's DOM effect never lands in the dying window; suspect seam = the removal riding an ordinary vdom/update flush against a window whose content was just un-projected (next read: base DragZone/SortZone `dragEnd` → proxy destroy path, to establish vdom-routed vs addon-direct removal).

**S-3b measured + partially resolved upstream:** `close-dispatched` WAITS on `main-projected` (phase contract), and at the pre-fix head the staged full path spent 509ms re-staging main *inside the corpse window* — so the #16621 fix (merged-pending) already compresses the dead-vessel second materially. Residual = the adoption→close gap that remains under the fast path.

**Repair plan (fresh branch, post-#16622-merge):**
1. **Engine seam (S-3a):** dying-window-safe proxy teardown — either addon-direct removal at `dragEnd` or a workspace-level gesture-overlay sweep at vessel retirement (belt-and-braces: covers any overlay class, not just this proxy).
2. **App seam (S-3b):** departing terminal presentation applied WITH the source un-projection at `documents-adopted` (the closing vessel reads as a deliberate exit, not a corpse), sized against the post-fix residual gap.

Phase order stays untouched — model-before-close is scene-4-pinned contract; both repairs are presentation/teardown-robustness only.

- 2026-08-07T10:13:18Z @neo-fable cross-referenced by #16623
- 2026-08-07T10:16:00Z @neo-fable cross-referenced by PR #16624
- 2026-08-07T10:53:19Z @neo-fable cross-referenced by #16626
- 2026-08-07T10:57:02Z @neo-fable cross-referenced by PR #16627
### @neo-fable - 2026-08-07T10:57:16Z

S-3b delivered: #16626 / PR https://github.com/neomjs/neo/pull/16627 — the departing theme-ground overlay engages at `documents-adopted` (during-step witness red-proven; the green run's vessel tail frame is a uniform theme surface vs r1's black-body-plus-chip). With #16621 MERGED (the fast path), #16623/PR #16624 in review (proxy root-retirement), and this leaf open, the parent's remaining scope is exactly: the S-a/S-d native-input discriminator + the whole-take filmed AC receipts at the next staged capture. This ticket closes on those receipts.

### @neo-fable - 2026-08-07T15:00:43Z

## Filmed receipts landed — the ticket's remaining ACs close (take-19b, head `fed9bf6e7d`, operator-granted hands-off window)

The five-beat reel rolled on the staged display at the exact head carrying all four merged fixes (#16621 · #16623 · #16506 · #16626). Receipts (production-record hashes: d2 `836ce63a83b292b0…`, spec log `05018df171b076f9…`):

- **Scene 4 GREEN in-take (8.7s)** with every witness inside the filmed run: shell identity retained (the #16621 fast path), DOM continuity, and the departing overlay's own sampler — **19 samples, `background === LIVE ground` at the flipped LIGHT theme, opacity 0 → 1.0** (spec log line 1186). The retheme→return sequence is visible on camera (t≈104.4-105.4 of the take), with metrics+commits verified home in the light-themed main column at full-res.
- **The native-titlebar beat GREEN (7.1s, real physical input under the granted window)** — the S-a/S-d discriminator from r1: **S-a (full-opacity double embodiment) is RESOLVED** — not reproduced on the native path post-#16497, matching the r1 CDP result; **S-d (escaped header) not reproduced anywhere post-#16497** (r1 ×2 + this run). Both classified with the take + r1 receipts as owners: the originating stills predate Ada's fix.
- Scene-5 full journey on camera; no black yank frame at sampling; the whole-workspace blank and ghost re-stage are gone from the filmed record (the #16621/#16622 fix observed at film pace).
- Scene-1 red = the #16500 entropy oracle firing on its KNOWN open defect (Vega's lane; entropy 0.409 vs ≥3.48) — correctly excluded from film use, exactly as F1 documented. Not this ticket's scope.

**AC audit:** four symptoms classified with frame+receipt owners ✓ (S-1/S-2/S-3 → the three merged repair leaves with red-proven witnesses; S-a/S-d → resolved/not-reproduced post-#16497 with take receipts) · no full-opacity double embodiment, landing region renders one active pane, no header overflow ✓ (witnessed in-take) · accepted transients documented with bounded counts ✓ (r1's S-2 ≤400ms staging composite; the departing overlay is the managed terminal state, motion-vocabulary bounded) · witnesses at film pacing red-proven at pre-fix heads ✓ (each leaf's PR carries its pair).

Closing as delivered. The dense-tour half (take-19, ACCEPTED) completed the WIP pair in the same window; composition follows on the production record.

### @neo-fable - 2026-08-07T15:00:44Z

All ACs delivered per the audit above — classification r1-r4, four merged repair leaves, red-proven witnesses, and the filmed receipts from the operator-granted staged take.

- 2026-08-07T15:00:45Z @neo-fable closed this issue

