Neo.overrides = {
    Neo: {
        form: {
            field: {
                CheckBox: {
                    iconCls: ['fas', 'fa-rocket']
                }
            }
        }
    }
};

export default Neo.overrides;
