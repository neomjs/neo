---
number: 10992
title: 'feat(agentos): codify post-review pickup discipline (#10970)'
author: neo-gpt
state: OPEN
createdAt: '2026-05-08T20:49:19Z'
updatedAt: '2026-05-08T20:49:40Z'
closedAt: null
mergedAt: null
head: codex/10970-post-review-pickup
base: dev
url: 'https://github.com/neomjs/neo/pull/10992'
---
Resolves #10970

Authored by GPT-5.5 (Codex Desktop). Session 1ed5570e-a33b-4a48-b05b-cda820c16bbb.

Codifies post-review-cycle pickup discipline in the public PR workflow manuals: reviewers and authors now have explicit next-lane expectations after review handoff, plus legitimate halt states when continuation is blocked. The top-level workflow trigger points agents at those sections without adding a new always-loaded payload.

Evidence: L1 (static skill-contract diff + diff hygiene checks) -> L1 required (workflow contract only). No residuals.

## Slot Rationale
- Added `AGENTS.md` `post-review-pickup` trigger row: disposition `compress-to-trigger`; 3-axis rating high trigger-frequency after PR/review handoffs x high failure-severity for swarm idle x discipline-only enforceability. Rationale: one routing row exposes the trap without always loading another workflow payload.
- Added `.agents/skills/pr-review/references/pr-review-guide.md` §11: disposition `keep` inside triggered skill payload; 3-axis rating high reviewer trigger-frequency x high post-approval stall severity x discipline-only enforceability. Decay mitigation: retire or compress if runtime task-state automation mechanically enforces next-lane pickup.
- Added `.agents/skills/pull-request/references/pull-request-workflow.md` §6.3: disposition `keep` inside triggered skill payload; 3-axis rating high author trigger-frequency x high post-response stall severity x discipline-only enforceability. Decay mitigation matches reviewer side: retire or compress once automation replaces manual discipline.

## Deltas from ticket
- Implemented the optional top-level `AGENTS.md` routing pointer because the failure mode needs discoverability at the per-turn skill table.
- Did not create a standalone `post-review-pickup` skill. The smaller public-surface fix is to extend the existing author/reviewer lifecycle manuals and point the trigger there.
- Preserved the private `feedback_peer_not_assistant_mode` lineage as provenance only; the load-bearing contract now lives in committed repo paths.
- Noted during intake that parent epic #10960 still has stale label-to-Project wording after the operator correction. That is out of scope for this PR.

## Test Evidence
- `git diff --check`
- `git diff --cached --check`
- No Playwright tests run; this is a docs / workflow-skill substrate change with no runtime surface.

## Post-Merge Validation
- [ ] In the next PR review or review-response cycle, the acting agent either picks the next ready lifecycle lane or states an explicit halt reason.

## Commits
- `6560fd450` - `feat(agentos): codify post-review pickup discipline (#10970)`
