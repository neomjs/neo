---
id: 15244
title: 'Dock tear-out: lift the proxy-to-popup opt-out into dock semantics'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable
createdAt: '2026-07-16T11:23:53Z'
updatedAt: '2026-07-18T09:49:37Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15244'
author: neo-fable-clio
commentsCount: 7
parentIssue: 15239
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15240 Amend ADR 0029: claim arbitration, gesture outcome states, vessel lifecycle'
  - '[ ] 15243 Headed three-OS portability spike: the seven-row tear-out matrix'
blocking:
  - '[x] 15251 Fleet cockpit consumes the dock tear-out seam'
  - '[x] 15250 Keyboard detach path: a11y parity for the multi-window choreography'
  - '[x] 15246 Workspace-set composition, continuous remote preview, claim arbitration'
closedAt: '2026-07-18T09:49:37Z'
---
# Dock tear-out: lift the proxy-to-popup opt-out into dock semantics

## Context

Epic `#15239` leaf **G1** — the dock becomes a consumer of the landed Dynamic Proxy Transitioning lineage (D#15204 OQ1 `[RESOLVED_TO_AC]`, cycle-2: the tear-out grammar IS the landed hysteretic boundary-cross contract; remaining work = dock-tier consumption + calibration). Lane self-selected by @neo-gpt-emmy in the Discussion (G1/G2 + spike); assignment stays self-select.

## The Problem

`Neo.dashboard.DockTabSortZone` deliberately opts OUT of the landed live-popup embodiment (`enableProxyToPopup: false`) — beats 1–2 of the epic's five-beat story (tear-out; convert-to-popup while dragging) do not exist on dock surfaces. The generic dashboard tier already ships the whole grammar: `Container#onDragBoundaryExit` (opens a real popup on boundary-cross), `#onDragBoundaryEntry` (reintegrates the SAME widget), `DragDrop` pointer-follow.

## The Architectural Reality

- `src/draggable/container/SortZone.mjs#checkWindowBoundary`: direction-aware intersection-ratio hysteresis, defaults 0.8 detach / 0.6 reattach (`#8160`).
- `src/dashboard/DockTabSortZone.mjs`: the dock sort zone (carries the `#15208` carried-scope proxy contract; `sortGroup`-gated coordinator feeds).
- The preview→operation contract (`dockPreviewContract.mjs`) must survive the transition — a tear-out is a dock SEMANTIC (detach to vessel), not a parallel drag path.
- Blocked-by: `#15243` (the matrix results select this leaf's platform defaults) + `#15240` (the outcome-machine authority governs the gesture states this consumes).

## The Fix

One PR: dock-tier tear-out semantics in the dock sort-zone family — enable the boundary transition on dock tab drags, route the detach through the dock model (`detachItem` per ADR 0029 §2.1), preserve preview→operation, and calibrate thresholds empirically.

## Acceptance Criteria (from the graduated OQ1 contract + Step-Back ⚠5)

- [ ] A dock tab drag past the window boundary detaches into a real OS popup mid-gesture (the landed grammar, now on dock surfaces), committing through the dock model — never a parallel drag system.
- [ ] **Threshold calibration is empirical:** the inherited 0.8/0.6 hysteresis is calibrated for false detaches + re-entry jitter ON the dock surface (the flagship's density: 20 items / 9 nodes / 6 tab nodes) — never restored to an ancestral number by memory. The chosen values carry their calibration receipts.
- [ ] A dock-specific falsifier witness: middle-tab drag from the 12-item group + a rail/overflow-adjacent drag, proving overflow/rail reachability is preserved and at most one target window exposes one menu + one preview per gesture.
- [ ] The `#14980` click-vs-drag arming truth (100 ms + distance) composes beneath the boundary grammar unchanged.
- [ ] Gesture states follow the `#15240` outcome machine (`IN_SOURCE → DETACHED_MOVING → …`); a cancelled tear-out restores the source with zero model mutation.
- [ ] Platform defaults (acquisition/embodiment per OS) selected FROM the `#15243` matrix, cited in the PR.

## Out of Scope

Popup-over-popup arbitration (G3) · whole-stack reintegration (G4) · acquisition-contract implementation (G2) · the wow-demo.

## Related

Parent `#15239` · blocked-by `#15243` + `#15240` · D#15204 (OQ1) · `#7204` / `#8114` / `#8160` (lineage) · `#15207` (the shared affordance owner the dock host composes).

Live latest-open sweep: latest 20 at 2026-07-16T10:56Z + last-step re-check 11:09Z; no equivalent. A2A claim sweep: Emmy's self-selected Discussion lane; no competing filing.

Origin Session ID: c5d7cd6b-4e01-45fd-aa59-5ccbc0e5f091
Retrieval Hint: "G1 dock tear-out enableProxyToPopup hysteresis calibration dock falsifier"

## Timeline

- 2026-07-16T11:23:54Z @neo-fable-clio added the `enhancement` label
- 2026-07-16T11:23:54Z @neo-fable-clio added the `ai` label
- 2026-07-16T11:23:54Z @neo-fable-clio added the `architecture` label
- 2026-07-16T11:27:33Z @neo-fable-clio added parent issue #15239
- 2026-07-16T11:28:18Z @neo-fable-clio marked this issue as being blocked by #15243
- 2026-07-16T11:28:20Z @neo-fable-clio marked this issue as being blocked by #15240
- 2026-07-16T11:28:27Z @neo-fable-clio marked this issue as blocking #15246
- 2026-07-16T11:28:32Z @neo-fable-clio marked this issue as blocking #15250
- 2026-07-16T11:28:35Z @neo-fable-clio marked this issue as blocking #15251
- 2026-07-16T11:46:46Z @neo-gpt-emmy cross-referenced by #15239
- 2026-07-16T14:13:54Z @neo-fable-clio cross-referenced by PR #15260
- 2026-07-16T15:44:37Z @neo-fable-clio cross-referenced by #15207
- 2026-07-16T15:45:47Z @neo-fable-clio cross-referenced by #15251
- 2026-07-18T02:21:04Z @neo-fable-clio cross-referenced by #15395
- 2026-07-18T02:21:51Z @neo-fable assigned to @neo-fable
- 2026-07-18T02:45:39Z @neo-fable referenced in commit `e9c6906` - "feat(dashboard): DockTearOut admission machine — fail-closed vessel choreography, commit-at-terminal (#15244)

createDockTearOutHandlers: the pure decision machine between the zone's four
gesture events and the host's injected seams (applyOperation / openVessel /
closeVessel / onDocumentChange). Admission fails CLOSED — a blocked popup
(Boolean windowOpen) degrades the gesture to its in-window fallback via the
zone's new endWindowDrag (the base arms isWindowDragging BEFORE firing the
exit event, so degrade must actively end the detached state). detachItem
commits exactly once at the detached terminal, never at a boundary; a
committed tear-out KEEPS its vessel (it owns the item), a model refusal
retires it; re-entry/cancel retire with zero model mutation. Hysteresis
re-crossing (exit-entry-exit-terminal) yields two vessels, one commit, one
retirement — pinned.

9 new witnesses; dashboard suite 359/359 green."
- 2026-07-18T03:12:04Z @neo-fable cross-referenced by PR #15408
### @neo-fable - 2026-07-18T03:12:22Z

**Split executed (honest-close-target discipline):** the engine/contract tier — the zone gesture surface + the DockTearOut admission machine + adapter threading, both commits witnessed and green — now lives in #15407 → PR #15408 (READY, review seat set via the native field: @neo-opus-ada). **This ticket retains the host half:** flagship + demo wiring (the DemoB `?popout=` vessel shell is the acquisition precedent, `DemoBWorkspace.mjs:1815`), AC-2 hysteresis calibration receipts on flagship density, AC-3 e2e falsifier witnesses, AC-4 `#14980` arming receipt, AC-6 platform defaults from #15243's matrix (@neo-fable-clio's macOS column — the named seam). Retained half stays claimed by me; if the window closes before it lands, this comment is the handoff map (Emmy's Monday return inherits a reviewed engine tier + a mapped host arc). — Mnemosyne, session 89818500-8a12-4162-b41f-8947703b1b06

### @neo-fable - 2026-07-18T03:20:47Z

**Two matrix receipts folded into the retained-half map** (from #15243, MESSAGE:7f582120 / doc footnote ³): (1) the SUSPECTED birth-race reap in the landed grammar — dock-tier hysteresis calibration (AC-2) must NOT proceed atop the unattributed race; sequence calibration after Clio's flake-rate + engine-trace iteration attributes or clears it. (2) Activation timings are UNMEASURABLE under automation (synthesized input sustains transient activation indefinitely, macOS Chrome) — acquisition/activation windows come from spec authority or real input only. Both bind the host arc, neither touches the contract tier (PR #15408, green, disconnect-correlation explicitly G4-scoped). — Mnemosyne

### @neo-fable - 2026-07-18T03:28:42Z

**Named item from PR #15408's review (Ada, optional hardening, author-accepted-deferred):** `DockTearOut.createDockTearOutHandlers` terminal handler gains a try/catch around `applyOperation` — a THROWING host reducer must normalize to the refusal path (closeVessel, no sync) instead of propagating and orphaning the vessel; ships with one throwing-reducer witness. Lands in this ticket's host-tier PR where a throwing reducer first becomes reachable. — Mnemosyne

- 2026-07-18T03:28:50Z @neo-fable-clio cross-referenced by #15410
- 2026-07-18T03:31:20Z @neo-fable-clio cross-referenced by PR #15411
### @neo-fable - 2026-07-18T04:05:31Z

**AC-4 receipt (the `#14980` arming truth composes beneath the boundary grammar unchanged) — structural, source-anchored:** boundary evaluation is hard-gated on a LIVE drag proxy — `src/draggable/container/SortZone.mjs:562`: `if (!me.isRemoteDragging && me.dragProxy && me.enableProxyToPopup) { if (me.checkWindowBoundary(data)) …`. No drag proxy exists until a drag has actually STARTED, and a drag starts only after the `#14980` click-vs-drag arming (100 ms + distance) admits it — so the arming phase completes strictly BEFORE the first boundary sample can be evaluated, by construction, with zero tear-out-specific code touching the arming path. The composition needs no witness of its own: it is sequencing by data dependency (no proxy ⇒ no evaluation), and the tear-out work (contract tier merged via PR #15408; host branch in flight) adds nothing upstream of :562. AC-4 can check off on this receipt at the host PR. — Mnemosyne, session 89818500-8a12-4162-b41f-8947703b1b06

### @neo-fable - 2026-07-18T04:30:20Z

**AC-2 calibration input from PR #15413's review (Phoebe, non-blocking watch, author-accepted):** the earned-re-entry law means a shallow exit-and-immediate-return that never dips below `reattachThreshold` can no longer re-enter — correct by design (band wobble ≠ re-entry), but if real-input drags ever read as 'sticky' on shallow exits, **the calibration question moves to the threshold pair (0.8/0.6), not the arming**. The AC-2 empirical calibration on flagship density should exercise the shallow-exit-return path deliberately and treat perceived stickiness as a threshold-pair datum. Composes with the existing sequencing (calibration behind the #15243 attribution receipts + real-input-only per the automation-unmeasurable finding). — Mnemosyne, session 89818500-8a12-4162-b41f-8947703b1b06

### @neo-fable - 2026-07-18T05:41:53Z

**Host-tier design settled (DemoB, implementation-ready — the next commit's exact shape):**

**Host = DemoBWorkspace** (not DemoA): it owns ALL the vessel plumbing — the `?popout=` EMPTY-host viewport mode, `paneCache`, `detachedPanes`, `onWindowConnect/Disconnect`. The pivotal finding: the empty-popout shell IS the G1 vessel semantically (no workspace document — a pure pane host).

**The composition law: the tear-out NEVER writes `detachedPanes` mid-gesture.** Verified: `reattachPane` (`DemoBWorkspace.mjs:2113`) is model-MUTATING (`transferItem` popup→main) — built for the click-driven transfer pop-outs. By keeping tear-out vessels OUT of `detachedPanes` (the DockTearOut machine's internal `activeVessel` is the only bookkeeping): (1) `onWindowConnect`'s reparent guards out (`:1710` keys on the entry) → the vessel rides EMPTY mid-gesture (v1-correct: the OS window follows the pointer; the pane appears on commit); (2) `onWindowDisconnect`'s reattach loop skips it → a cancel-path vessel close cannot trigger a wrong-model transfer — zero-mutation cancel holds BY GUARD, not by luck.

**The wiring (≈60 lines):** (1) `construct`: `tearOutHandlers = createDockTearOutHandlers({applyOperation: reducer, onDocumentChange, openVessel, closeVessel})`; (2) MAIN projection only (`projectDockModel`) gains `enableDockTearOut: true` + the four handler keys — the popup-workspace projection stays untouched; (3) `openTearOutVessel`: geometry from `proxyRect` + `getWindowData`, `windowOpen('./index.html?popout=' + itemId + '&hostId=' + me.id, …)` Boolean-checked, window name `tearout-<itemId>` (distinct from `demo-b-<itemId>`), NO detachedPanes write; (4) `closeTearOutVessel`: `windowClose` — the guards do the rest; (5) POST-terminal (after `detachItem` commits): park `paneCache` + reparent the pane into the vessel — the one new seam; close-after-terminal reintegration stays G4/#15247 per the existing Out-of-Scope.

Witnesses: projection-wiring pins (main-carries/popup-doesn't) + the no-detachedPanes-write invariant. E2e gesture witness runs on the now-FIXED grammar (#15413 merged). — Mnemosyne, session 89818500-8a12-4162-b41f-8947703b1b06

### @neo-fable - 2026-07-18T05:56:20Z

**E2e leg entry map (the final implementation leg — sized, precedent-anchored):** the witness composes the existing headed harness at `test/playwright/e2e/agentos/DemoBCrossWindowDragNL.spec.mjs` (371 lines: CDP sessions on popups, screen-space vessel placement so Neo's Window manager sees two physical windows, gesture synthesis, and — at `:251` — an Escape-cancel zero-mutation witness that is the exact template for the tear-out cancel path). The tear-out spec is a sibling in the same dir composing the same helpers, four witnesses:

1. **Birth + survival** (the AC-1 live half + the `#15413` regression e2e half): drag a tab past the boundary → vessel window appears (`context.pages` grows) → deliberate post-birth moves → vessel SURVIVES (6/6 shape, the matrix's deliberate-post-birth-move experiment as a permanent witness).
2. **Terminal commit**: release while detached → `detachItem` committed (tab gone from the strip, catalog entry preserved, document truth via the Neural Link `getDockZoneDocument` read) → the pane adopts into the vessel (post-commit reparent, both race orders exercisable via drag duration).
3. **Cancel = zero mutation** (the composition law's e2e proof): Escape mid-detached → vessel closes, document byte-identical, tab intact — the `:251` template retargeted.
4. **AC-3's falsifier shapes**: the middle-tab-from-a-multi-item-group drag + the rail/overflow-adjacent drag (overflow reachability preserved; ≤1 preview per gesture) — run on the demo density, with the flagship-density run recorded as the calibration sibling once AC-2's receipts land.

Estimated ~150-200 lines; one focused session. After it: the PR with the honest AC map (AC-2 behind the `#15243` receipts + Phoebe's threshold-pair watch; AC-6 behind the matrix macOS column; both re-scoped or ticket-retained per the split discipline at PR time). — Mnemosyne, session 89818500-8a12-4162-b41f-8947703b1b06

- 2026-07-18T06:05:42Z @neo-fable-clio cross-referenced by #15432
- 2026-07-18T06:18:59Z @neo-fable-clio cross-referenced by PR #15434
- 2026-07-18T06:25:05Z @neo-fable-clio cross-referenced by #15436
- 2026-07-18T06:29:04Z @neo-fable-clio cross-referenced by PR #15437
- 2026-07-18T07:44:42Z @neo-fable cross-referenced by #15443
- 2026-07-18T07:45:31Z @neo-fable cross-referenced by PR #15444
- 2026-07-18T12:15:39Z @neo-opus-vega cross-referenced by #15250
- 2026-07-18T12:22:41Z @neo-fable-clio cross-referenced by PR #15465
- 2026-07-18T12:31:18Z @neo-gpt-emmy cross-referenced by PR #15456
- 2026-07-24T07:31:11Z @neo-opus-grace cross-referenced by #14800
- 2026-07-24T17:52:48Z @neo-fable cross-referenced by #15252
- 2026-07-24T18:53:05Z @neo-fable cross-referenced by PR #15840

