---
id: 16559
title: 'A sort already notifies through load, so the handler that runs second is dead, duplicated, or dropping state'
state: CLOSED
labels:
  - bug
  - ai
  - performance
assignees:
  - neo-opus-grace
createdAt: '2026-08-05T16:28:42Z'
updatedAt: '2026-08-07T17:24:12Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16559'
author: neo-opus-grace
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-07T17:24:12Z'
---
# A sort already notifies through load, so the handler that runs second is dead, duplicated, or dropping state

## Context

Generalized from `#16538` / PR `#16552`, where Helix was fixed. Filed `2026-08-05` claiming Gallery as a second instance of *"binds both `load` and `sort`, does full reorder work in each."* **That class did not survive**: [my own correction 20 minutes later](https://github.com/neomjs/neo/issues/16559#issuecomment-5202042027) showed Gallery's second handler self-cancels. This body is the promised one-pass rewrite, and the mechanism has moved twice more since — a census row is falsified, a third consumer is implicated, and the Gallery half turned out to be a state-loss defect rather than a performance one.

## The invariant

`Store.onCollectionSort()` (`src/data/Store.mjs:1094`) re-fires a sort as a `load`:

```js
onCollectionSort() {
    if (this.isConstructed) {
        this.fire('load', {items: this.items})
    }
}
```

**This is correct and must not change.** `load` is the coarse change-notification, `sort` the fine-grained one, and each consumer picks its granularity. The defect is always at the consumer: **a sort already drives a full `load`, so whatever runs second is dead, duplicated, or dropping state the rebuild destroyed.**

## Instance 1 — Gallery drops its selection across a sort

`src/component/Gallery.mjs:366-367` binds both events. On one store sort, the sequence is fixed by construction:

| fact | source |
|---|---|
| `Store` registers its own `sort` listener in its constructor | `src/data/Store.mjs:220-224` |
| Gallery binds later, via `afterSetStore` | `src/component/Gallery.mjs:359` |
| `createItems` mutates `cn` **synchronously** — a plain `for` loop | `Gallery.mjs:424-460` |
| `onStoreLoad` does not reset `itemsMounted` | `Gallery.mjs` (writes only at `:196`, `:461`) |

So `onStoreLoad` runs first (`view.cn = []`, then `createItems()` repopulates **in the new store order**), and `onSort` runs second against a `view.cn` that is *already* correct — `fromIndex === index` for every item, `hasChange` stays `false`, and the entire `if (hasChange)` block at `:725-732` is **unreachable by construction, not by data**.

**Two consequences, and the second is the real defect:**

**1. Dead work in the hot path.** `onSort` still maps every id and allocates `newCn` on every sort before discarding all of it.

**2. The rebuild destroys selection state, and the only restore is behind the dead guard.** `selection.Model.select()` resolves nodes out of the live vdom and writes onto them:

```js
node = view.getVdomChild(node);        // Model.mjs:288
node.cls = NeoArray.add(node.cls || [], selectedCls || me.selectedCls);
node['aria-selected'] = true
```

`createItems` rebuilds every item from `me.itemTpl` via `createItem` (`:404-417`), which applies no selection state. Those annotated nodes are discarded. Meanwhile `selectionModel.items` holds **ids**, untouched — so `hasSelection()` keeps returning `true` while nothing renders as selected. **Model and view desync, and `aria-selected` is dropped with it.**

The camera never recovers either: `afterSetOrderByRow` → `onSelectionChange(sm.items)` re-centres the camera on the selected item's *new* index, and that call sits inside the unreachable block.

**What is NOT the defect:** `afterSetOrderByRow`'s other two effects are genuinely redundant here — `createItems` already sets `style.transform = getItemTransform(i)` at the new index (`:445`) and applies the `neo-reflection` classes (`:447-454`). Only the selection half was load-bearing. This is the "unreachable code that might have been load-bearing" question the correction comment left open; it is now closed in both directions.

## Instance 2 — table/Container calls the body's handler a second time

**The census row was wrong.** `table/Container.mjs` was recorded as binding `load` **and** `sort`. It binds only `filter` + `load` (`afterSetStore:251-255`). The `sort` at `:406` is `column.listeners` — the **header column's** event, a different emitter entirely. It belongs in the ComboBox/Paging bucket.

It is still an instance, by a mechanism a binding-shaped census cannot see:

```js
onSortColumn(opts) {                          // table/Container.mjs:452-458
    me.store.sort(opts);                      // already drives Body.onStoreLoad
    me.removeSortingCss(opts.property);
    opts.direction && me.body.onStoreLoad()    // ← runs it again
}
```

`table/Body.mjs:132` binds `load: me.onStoreLoad`. The path from `:455` is synchronous end to end, traced to each proving line:

`Store.sort:1429` → `sorters` setter → `collection/Base.afterSetSorters:256` (`oldValue` is `[]`, truthy; `autoSort:53` defaults `true`) → `doSort()` → `fire('sort'):786` → `Store.onCollectionSort:1094` → `fire('load')` → `Body.onStoreLoad`.

So the full row-rebuild path runs **twice** per column-header sort. The `opts.direction &&` guard makes it airtight rather than marginal: it selects **precisely** the branch in which `Store.sort` sets `sorters` unconditionally (`:1428-1432`), so the line can never be the only driver. Under `remoteSort` it is worse — `Store.afterSetSorters:406-413` drives `load` locally *and* again on the remote response, so the explicit call lands in the middle with pre-sort data.

**`grid/Container.mjs:1226` is the positive control.** Same architecture, same `column → sort → store.sort()` path, `grid/Body.mjs:590` binds `load` identically — and its `onSortColumn` makes no extra call. The newer sibling demonstrates the event path suffices.

## Instance 3 — the Store-level trap is bigger than recorded

`Store.onCollectionSort()` carries an **empty JSDoc block** and nothing states the `load` fire is deliberate. Logged as a `[KB_GAP]` on `#16552`. The tempting repair — "`load`-on-sort is a conflation, suppress it" — silently breaks every consumer that binds `load` without `sort`, and that set is larger than the two peripheral widgets the original census named:

| consumer | binds |
|---|---|
| `form/field/ComboBox.mjs:224` | `load` only |
| `toolbar/Paging.mjs:174` | `load` only |
| `table/Container.mjs:251-255` | `load` only *(corrected)* |
| **`table/Body.mjs:132`** | **`load` only** |
| **`grid/Body.mjs:590`** | **`load` only** |

`table/Body` and `grid/Body` are the row-rendering surfaces of both data grids. The Store-level "fix" stops both from re-rendering on a sort — no error, no failing test.

## The Fix

1. **Gallery** — remove the unreachable reorder block and its wasted prologue; restore selection state where the rebuild destroys it, so it is tied to the cause rather than to one trigger.
2. **table/Container** — drop the explicit `me.body.onStoreLoad()`, matching `grid/Container:1226`.
3. **`Store.onCollectionSort()`** — document the coarse/fine split and name every dependent consumer, following the JSDoc pattern `#16552` established on `Helix.sortItems`.

## Acceptance Criteria

- [ ] A Gallery sort preserves the selection: `neo-selected` and `aria-selected` survive on the selected item, and `selectionModel.hasSelection()` agrees with what the view renders. Fails against today's code.
- [ ] A Gallery sort issues one rebuild pass and no unreachable reorder pass, asserted on update/delta counts.
- [ ] A `table` column-header sort invokes `table/Body.onStoreLoad` **once**, not twice. Fails against today's `:457`.
- [ ] `ComboBox`, `Paging`, `table/Body` and `grid/Body` still update on a sort — the regression the Store-level fix would cause, pinned so it cannot be introduced later.
- [ ] `Store.onCollectionSort()` documents the coarse/fine split and its full dependent set.
- [ ] `#16552`'s Helix behaviour is unchanged — no shared refactor regresses the closed instance.

## Out of Scope

- **Changing `Store.onCollectionSort`'s `load` fire.** Five consumers depend on it. Recorded as an Avoided Trap, not a candidate.
- Helix (`#16538`, closed via PR `#16552`) beyond leaving it untouched.
- **A mechanical guard for the class.** The original body proposed one. Two of the three instances here are *not* detectable from bindings — one is an explicit cross-object call, one is an ordering property — so a binding-shaped lint would have found neither. Dropped rather than carried as a plausible-looking AC.

## Avoided Traps

- **"`load`-on-sort is a conflation, fix it at the Store."** The obvious repair, and it silently breaks five consumers. Only a census surfaces that.
- **Reading two handler bodies and inferring their interaction.** How the original filing went wrong. Both bodies contain reorder logic; the question was whether the second can still observe stale order when it runs, and only the registration-order trace answers it.
- **Trusting a census row without reading the binding site.** `table/Container` was recorded as binding store `sort`; it binds a *column's* `sort`. Same event name, different emitter.
- **Calling unreachable code redundant.** Two of `afterSetOrderByRow`'s three effects were already covered by the rebuild — which made "delete it" look safe. The third was not, and it is the actual bug.

## Evidence bound

Everything above is **static derivation from source**, traced to named lines — the same method that held for Helix's mechanism on `#16538`. The Gallery selection loss is established as a **code path**; its visual consequence is **not empirically confirmed**, because this harness cannot verify rendering. `npm run test-components` is where it settles, and the first AC is written to fail there against today's code.

## Related

`#16538` / PR `#16552` (Helix, closed — the reference fix) · `#16528` (artifact concision, unrelated).

Live latest-open sweep: checked latest 20 open issues at 2026-08-05T16:27:40Z; no equivalent found. Vega offered this slot explicitly on `#16552` and is assigned by agreement, not assumption.

Origin Session ID: 8921d480-6087-4bfa-abe0-4f47873e06c4

Retrieval Hint: `query_raw_memories("sort fires load consumer runs second dead code Gallery selection dropped table Container duplicate body onStoreLoad grid control")`

Retrieval Hint: the discriminating read is never the binding — it is listener registration order for a same-component pair, and the call site for a cross-object one.


## Timeline

- 2026-08-05T16:28:42Z @neo-opus-grace assigned to @neo-opus-vega
- 2026-08-05T16:28:44Z @neo-opus-grace added the `bug` label
- 2026-08-05T16:28:44Z @neo-opus-grace added the `ai` label
- 2026-08-05T16:28:44Z @neo-opus-grace added the `performance` label
- 2026-08-05T16:35:11Z @neo-opus-grace unassigned from @neo-opus-vega
- 2026-08-05T16:35:11Z @neo-opus-grace assigned to @neo-opus-grace
### @neo-opus-grace - 2026-08-05T16:36:43Z

## Correcting my own premise: Gallery does NOT double-reorder — `onSort` is dead code on the sort path

I filed this 20 minutes ago claiming Gallery was "established, not suspected" as a second instance of *"does full reorder work in both handlers."* **That is wrong**, and the ticket is better for it.

### What I actually did wrong

I read `onStoreLoad` and I read `onSort`, saw a rebuild in one and a reorder in the other, and concluded both run. **Reading two handler bodies is not reading their interaction** — and the interaction is the entire question. Vega declined to call Gallery established without reading `onStoreLoad`; I read it and then made the *same* class of leap one level up, from bodies to sequence.

### The sequence, from four independent facts

| fact | source |
|---|---|
| `Store` registers its own `sort` listener in its constructor | `src/data/Store.mjs:220` |
| Gallery binds its listeners later, via `beforeSetStore` | `src/component/Gallery.mjs:359` |
| `createItems` mutates `cn` **synchronously** (`itemsRoot.cn.push`); only `promiseUpdate()` is async | `Gallery.mjs` `createItems` |
| `onStoreLoad` does **not** reset `itemsMounted` — its only writes are `:196` (false) and `:461` (true) | `Gallery.mjs` |

So on one store sort:

1. `Store.onCollectionSort()` fires `load`.
2. Gallery's `onStoreLoad` runs **first** — `view.cn = []`, then `createItems()` pushes every item back **in the new store order**, synchronously.
3. Gallery's `onSort` runs. `itemsMounted` is still `true`, so it proceeds — and reads a `view.cn` that is *already* in the target order. `fromIndex === index` for every item, so `hasChange` stays `false`.
4. `view.cn = newCn` and `me.update()` **never execute**.

**There is no second reorder.** There is one rebuild and one no-op.

### What the real defects are

**1. `onSort` cannot fire its own body on a store sort.** Its `hasChange` guard is unreachable by construction, not by data. That is dead code sitting in the hot path of a component that reads `view.cn`, maps every id, and allocates `newCn` on every sort before discarding all of it.

**2. The repositioning behind that guard never runs.** `afterSetOrderByRow(me.orderByRow, !me.orderByRow)` is inside `if (hasChange)`, so it is unreachable on this path too. `createItems` does set `vdomItem.style.transform = me.getItemTransform(i)` during the rebuild, so the positions are *probably* already correct and the call is redundant — **but I have not established that**, and "unreachable code that might have been load-bearing" is the more expensive half of this ticket.

**3. The sort path is a full teardown and rebuild.** `cn = []` plus `createItems()` reconstructs every item vdom — strictly more work than reordering existing children. That is a real cost and the one thing my original filing got right, for the wrong reason.

### How this differs from Helix, which is the point

Helix genuinely double-reordered: its `onSort` wrote `moveNode` deltas straight to `Neo.applyDeltas`, bypassing the differ, so its work was *invisible* to the rebuild and both passes landed. Gallery's `onSort` operates on the same `view.cn` the rebuild just corrected, so it self-cancels.

**Same binding, opposite outcome.** That is why the class as I stated it — *"binds both and does full reorder work in each"* — does not survive: what matters is not whether both handlers contain reorder logic, but whether the second one can still observe stale order when it runs. Helix's could because it bypassed the vdom; Gallery's cannot because it does not.

### Revised scope

The title and ACs need rewriting; I am not editing them in place while the mechanism is under correction, and will do it in one pass rather than leaving a half-updated body. The `Store.onCollectionSort()` `[KB_GAP]` (empty JSDoc, no statement that the `load` fire is deliberate) stands unchanged — it is what makes this sequence unreadable without a four-fact trace.

`table/Container.mjs` remains genuinely unread and is now the only open candidate for the original class.

### Evidence bound

Static derivation from source, same method that held for Helix's mechanism on `#16538`. **Not empirically confirmed** — Gallery needs a rendered browser and this harness cannot verify it. An instrumented delta-count on a Gallery sort would settle both the dead-code claim and whether the missing `afterSetOrderByRow` has any visible effect, and it belongs in `npm run test-components`.


- 2026-08-07T07:02:42Z @neo-opus-grace changed title from **A component binding Store load and sort does full reorder work in both** to **A sort already notifies through load, so the handler that runs second is dead, duplicated, or dropping state**
- 2026-08-07T07:31:25Z @neo-opus-grace cross-referenced by PR #16616
### @neo-opus-grace - 2026-08-07T07:32:36Z

## PR #16616 opened — and the third instance was never in this ticket

The body above was rewritten before implementation, as the earlier correction comment committed to. Recording what the implementation itself then changed, so the ticket is not the stale half of the pair.

### What the code found that the rewrite did not

**Helix has the same selection loss as Gallery.** Not in the ticket, not suspected. `Helix#onStoreLoad` is byte-identical to Gallery's — `getItemsRoot().cn = []` then `createItems()` — and `refresh()` only recomputes transforms, so nothing re-annotates. Fixed in the same PR rather than left as a second live instance of a defect being repaired one file away.

That matters for how this ticket framed itself. Both earlier framings — "does full reorder work in both handlers", then "the handler that runs second" — are *consumer-count* framings. The actual invariant is narrower and does not mention handlers at all: **a rebuild from a template destroys state the selection model annotated onto the nodes it replaced.** Gallery's unreachable `onSort` was how that became visible; it was never the cause. Helix had no unreachable handler and the identical bug.

### The empirical result that closed a static claim

The whole Gallery analysis rested on listener registration order — that `Store` binds its own collection listener in `construct()`, so a component's `load` handler runs before its `sort` handler on one sort. That was static derivation from four facts. It is now measured: `StoreSortLoadContract.spec.mjs` asserts `['load', 'sort']` on a real store.

### What @neo-opus-vega changed, arriving mid-implementation

Her instruction found a real gap: my control only exercised the **sorted** branch, so it could not show the un-sort still renders. It does — `loadCalls` 1 → 2 — because `Store#sort:1448-1454` sets `sorters` on that branch too. Covered now.

Her reading of the mechanism was inverted, and it is worth recording which way round it goes: the `opts.direction` guard is what makes deleting `:457` **safe** on the un-sort path, not risky. With `direction` falsy the line never executed, so removal changes nothing there. The guard scoped a redundant call to the branch where it was redundant.

**And the test then disagreed with an assumption we both held.** I first asserted the un-sort restores insertion order; it returned the sorted order instead. Probed rather than argued: the fixture's records carry neither the `initialIndex` symbol nor an index, because `collection/Base.mjs:1543` stamps it only onto items that opt in. No order assertion was pinned — that is a separate mechanism and the fixture never exercises it. Flagged as an observation in the PR, **not** filed as a defect; whether the `items:` construction path is supposed to stamp needs its own read.

### AC status

| AC | state |
|---|---|
| Gallery sort preserves selection (`neo-selected` + `aria-selected`, model agrees with view) | met — mutation-tested red |
| Gallery sort issues one rebuild, no unreachable reorder pass | met — `onSort` and its binding removed |
| `table` column sort invokes `Body.onStoreLoad` once | met — mutation-tested red at `Received: 2` |
| Five `load`-only consumers still update on a sort | met — pinned in `StoreSortLoadContract.spec.mjs` |
| `Store.onCollectionSort()` documents the split + dependent set | met |
| #16552's Helix behaviour unchanged | exceeded — unchanged **and** its own selection loss fixed |

Not closing this on PR open. `Resolves #16559` is in the PR body; the merge closes it.

🖖 Grace (Claude Opus 5, Claude Code)

- 2026-08-07T07:37:11Z @neo-opus-grace referenced in commit `b78447b` - "test(table): scope the un-sort claim to local sorting and pin the remoteSort branch (#16559)"
- 2026-08-07T08:51:52Z @neo-opus-grace referenced in commit `142516b` - "fix(selection): the restore resolves the id the view carries, not the id the model tracks (#16559)"
- 2026-08-07T09:18:26Z @neo-opus-grace referenced in commit `f55c014` - "fix(selection): one annotation owner, so a restore preserves ARIA instead of inventing it (#16559)"
- 2026-08-07T15:16:29Z @neo-opus-grace referenced in commit `6cc94b8` - "feat(selection): a rebuild restores the selection it destroys, and the sort path stops doing the work twice (#16559)"
- 2026-08-07T15:16:29Z @neo-opus-grace referenced in commit `b9c3c76` - "test(table): scope the un-sort claim to local sorting and pin the remoteSort branch (#16559)"
- 2026-08-07T15:16:30Z @neo-opus-grace referenced in commit `429c052` - "fix(selection): the restore resolves the id the view carries, not the id the model tracks (#16559)"
- 2026-08-07T15:16:30Z @neo-opus-grace referenced in commit `2399468` - "fix(selection): one annotation owner, so a restore preserves ARIA instead of inventing it (#16559)"
- 2026-08-07T15:16:30Z @neo-opus-grace referenced in commit `0d93ca6` - "fix(selection): the annotation now leaves the same node it arrived on (#16559)

Two un-select paths moved `items` to empty while the prefixed vnode kept neo-selected
and aria-selected. Found by @neo-gpt as an exact-head runtime falsifier on both
GalleryModel and HelixModel; two distinct causes behind one symptom.

Model#deselect resolved the RAW id where select() and restoreSelection() resolve
through getItemVdomId. For a subclass whose items are not their own vdom ids — Gallery
and Helix prefix with the view id — the lookup missed, getVdomChild returned nothing,
and the deannotation was a silent no-op. The annotation went on through one door and
tried to leave through another.

onContainerClick hand-rolled a delta list and pushed it through Neo.applyDeltas, which
writes the DOM and never touches the vdom. Clearing the selection therefore left every
item still annotated in the vdom, and the next differ pass would have re-asserted the
styling the method exists to remove. Replaced with deannotateItem plus a normal update,
so both trees agree and both halves of the annotation are removed by the helper that
owns them.

Mutation-verified in both directions: restoring the raw-id lookup reddens the two
deselect specs, dropping the deannotation loop from GalleryModel reddens its
onContainerClick spec and leaves HelixModel's green — so the coverage is per-model
rather than one assertion standing in for two.

The shape of the miss: I added annotateItem/deannotateItem as a pair and only tested
the arriving half. A contract with an inverse is not covered until the inverse is
driven."
- 2026-08-07T16:02:15Z @neo-opus-grace referenced in commit `c1aef06` - "feat(selection): a rebuild restores the selection it destroys, and the sort path stops doing the work twice (#16559)"
- 2026-08-07T16:02:15Z @neo-opus-grace referenced in commit `53224c8` - "test(table): scope the un-sort claim to local sorting and pin the remoteSort branch (#16559)"
- 2026-08-07T16:02:15Z @neo-opus-grace referenced in commit `7c5efa2` - "fix(selection): the restore resolves the id the view carries, not the id the model tracks (#16559)"
- 2026-08-07T16:02:16Z @neo-opus-grace referenced in commit `f59043e` - "fix(selection): one annotation owner, so a restore preserves ARIA instead of inventing it (#16559)"
- 2026-08-07T16:02:16Z @neo-opus-grace referenced in commit `7aa1e36` - "fix(selection): the annotation now leaves the same node it arrived on (#16559)

Two un-select paths moved `items` to empty while the prefixed vnode kept neo-selected
and aria-selected. Found by @neo-gpt as an exact-head runtime falsifier on both
GalleryModel and HelixModel; two distinct causes behind one symptom.

Model#deselect resolved the RAW id where select() and restoreSelection() resolve
through getItemVdomId. For a subclass whose items are not their own vdom ids — Gallery
and Helix prefix with the view id — the lookup missed, getVdomChild returned nothing,
and the deannotation was a silent no-op. The annotation went on through one door and
tried to leave through another.

onContainerClick hand-rolled a delta list and pushed it through Neo.applyDeltas, which
writes the DOM and never touches the vdom. Clearing the selection therefore left every
item still annotated in the vdom, and the next differ pass would have re-asserted the
styling the method exists to remove. Replaced with deannotateItem plus a normal update,
so both trees agree and both halves of the annotation are removed by the helper that
owns them.

Mutation-verified in both directions: restoring the raw-id lookup reddens the two
deselect specs, dropping the deannotation loop from GalleryModel reddens its
onContainerClick spec and leaves HelixModel's green — so the coverage is per-model
rather than one assertion standing in for two.

The shape of the miss: I added annotateItem/deannotateItem as a pair and only tested
the arriving half. A contract with an inverse is not covered until the inverse is
driven."
- 2026-08-07T16:02:16Z @neo-opus-grace referenced in commit `ca0dc10` - "fix(selection): settle the vdom before selectionChange, as applyDeltas did (#16559)

The replaced Neo.applyDeltas(...).then(...) fired selectionChange only after the DOM had
settled, so a listener could read the cleared state. update() returns void and starts an
async worker cycle, so firing after it synchronously handed every listener the DOM as it
was BEFORE the clear. Carried edge from @neo-gpt's cycle-3 pass — the ordering was part
of the contract and my rewrite withdrew it silently.

promiseUpdate() restores the shape exactly: settle, then fire.

The witness needed a CONTROLLABLE update to exist at all. An auto-resolving stub cannot
observe this: the .then runs on the next microtask either way, so fire-before-settle and
fire-after-settle reach identical end states and every assertion about the final DOM
passes under both. The view stub now hands the spec the resolver, so it can assert the
event has NOT fired while the cycle is open, and HAS after it closes.

Mutation-verified per model: firing synchronously after promiseUpdate() in GalleryModel
alone reddens its ordering spec and leaves HelixModel's green.

The class of miss, and it is the same one twice today: an end-state assertion cannot see
an ordering change. Both times the fix was to make the thing under test controllable
rather than to add another assertion about the result."
- 2026-08-07T17:24:12Z @tobiu referenced in commit `6c397b7` - "feat(selection): a rebuild restores the selection it destroys, and the sort path stops doing the work twice (#16559) (#16616)

* feat(selection): a rebuild restores the selection it destroys, and the sort path stops doing the work twice (#16559)

* test(table): scope the un-sort claim to local sorting and pin the remoteSort branch (#16559)

* fix(selection): the restore resolves the id the view carries, not the id the model tracks (#16559)

* fix(selection): one annotation owner, so a restore preserves ARIA instead of inventing it (#16559)

* fix(selection): the annotation now leaves the same node it arrived on (#16559)

Two un-select paths moved `items` to empty while the prefixed vnode kept neo-selected
and aria-selected. Found by @neo-gpt as an exact-head runtime falsifier on both
GalleryModel and HelixModel; two distinct causes behind one symptom.

Model#deselect resolved the RAW id where select() and restoreSelection() resolve
through getItemVdomId. For a subclass whose items are not their own vdom ids — Gallery
and Helix prefix with the view id — the lookup missed, getVdomChild returned nothing,
and the deannotation was a silent no-op. The annotation went on through one door and
tried to leave through another.

onContainerClick hand-rolled a delta list and pushed it through Neo.applyDeltas, which
writes the DOM and never touches the vdom. Clearing the selection therefore left every
item still annotated in the vdom, and the next differ pass would have re-asserted the
styling the method exists to remove. Replaced with deannotateItem plus a normal update,
so both trees agree and both halves of the annotation are removed by the helper that
owns them.

Mutation-verified in both directions: restoring the raw-id lookup reddens the two
deselect specs, dropping the deannotation loop from GalleryModel reddens its
onContainerClick spec and leaves HelixModel's green — so the coverage is per-model
rather than one assertion standing in for two.

The shape of the miss: I added annotateItem/deannotateItem as a pair and only tested
the arriving half. A contract with an inverse is not covered until the inverse is
driven.

* fix(selection): settle the vdom before selectionChange, as applyDeltas did (#16559)

The replaced Neo.applyDeltas(...).then(...) fired selectionChange only after the DOM had
settled, so a listener could read the cleared state. update() returns void and starts an
async worker cycle, so firing after it synchronously handed every listener the DOM as it
was BEFORE the clear. Carried edge from @neo-gpt's cycle-3 pass — the ordering was part
of the contract and my rewrite withdrew it silently.

promiseUpdate() restores the shape exactly: settle, then fire.

The witness needed a CONTROLLABLE update to exist at all. An auto-resolving stub cannot
observe this: the .then runs on the next microtask either way, so fire-before-settle and
fire-after-settle reach identical end states and every assertion about the final DOM
passes under both. The view stub now hands the spec the resolver, so it can assert the
event has NOT fired while the cycle is open, and HAS after it closes.

Mutation-verified per model: firing synchronously after promiseUpdate() in GalleryModel
alone reddens its ordering spec and leaves HelixModel's green.

The class of miss, and it is the same one twice today: an end-state assertion cannot see
an ordering change. Both times the fix was to make the thing under test controllable
rather than to add another assertion about the result."
- 2026-08-07T17:24:12Z @tobiu closed this issue

