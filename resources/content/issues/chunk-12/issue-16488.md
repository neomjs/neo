---
id: 16488
title: 'Retire the SDK-boundary exception: the KB service graph imports chromadb at module scope, so every consumer needs a Brain-tier package to merely load'
state: OPEN
labels:
  - bug
  - ai
assignees:
  - neo-opus-ada
createdAt: '2026-08-04T09:28:19Z'
updatedAt: '2026-08-07T18:47:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16488'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Retire the SDK-boundary exception: the KB service graph imports chromadb at module scope, so every consumer needs a Brain-tier package to merely load

> ## Fourth correction (2026-08-07) — the prescribed FIX is wrong, and two ACs do not survive contact
>
> Folded from PR `#16641` while it is still open, because `@neo-gpt`'s review falsified prescriptions that this ticket states as settled. Everything below is retained; the wrong versions stay visible because each one is the intuitive version and the next reader will reach for it too.
>
> ### 1. The ledger prescribes the fix that does not work
>
> The Contract Ledger row **"client construction site → moves from `construct()` to `initAsync()`"** is **wrong**, and the "Why the two managers are the hard part" paragraph that derives it is wrong for the same reason.
>
> **Moving the import into `initAsync()` is not demand-lazy for an eager singleton.** `Neo.setupClass()` instantiates the singleton at module load and `core.Base` schedules `initAsync()` on the very next microtask, so a dynamic `import('chromadb')` there still runs on barrel import. In the Body tier it rejects unhandled and terminates the process. The failure changes **phase**, not ownership. An earlier revision of PR `#16641` implemented exactly what this ledger prescribes, and it was falsified by a runtime probe.
>
> **What actually works:** resolution moves to `ensureChromaReady()` — memoized, on first actual **use**, and `initAsync()` must never call it. Two invariants that each cost a defect:
> - it must **return early when a client already exists**, or the documented mock seam (>14 replacements in one spec alone) breaks;
> - `connect()` stays unguarded, because it runs *from* the resolve path.
>
> ### 2. `ready()` no longer implying a client is the real blast radius — and the AC under-scopes it
>
> The AC *"Every reader of `.client` on BOTH managers is enumerated from source"* is the right instinct with the wrong population. Two things it misses, both of which escaped into review:
>
> - **Test readers are the same population as production readers.** `test/.../memory-core/helpers/restoreTargetSetStorage.spec.mjs` awaited `ready()` and handed `ChromaManager.client` to its fixture; it received null.
> - **The population is not "readers of `.client`" — it is "anyone who awaits `ready()` and then uses the client."** `ai/services/memory-core/HealthService.mjs` never named `.client` at all. It called `ready()` then `connect()`, and `connect()` against a null client returns false, so a healthcheck reported a **live** Chroma server as unreachable. A grep for `.client` cannot see that.
>
> Restated: *every caller that awaits `ready()` and subsequently depends on a client — production or test, whether or not it names `.client` — must be enumerated and routed through `ensureChromaReady()`.*
>
> ### 3. "No consumer of `ai/services.mjs` changes" is NOT met as literally written
>
> `ai/examples/smart-search.mjs` imports the barrel and is changed. Stated plainly rather than reinterpreted after the fact.
>
> The property that actually matters — and that the AC was reaching for — is that the **export surface is unchanged** and no consumer is forced to deep-import past the SDK boundary. That holds. The AC as written does not, and PR `#16641` says so rather than claiming otherwise.
>
> ### 4. A static import walk is not sufficient evidence for this ticket
>
> Two ACs are satisfiable by a static walk. **A static walk reports clean while the runtime property is false** — that is precisely how the retired sunset spec passed against broken code, and full-install CI cannot reproduce the Body tier either. The evidence class this ticket needs is a **runtime** witness: a spawned process with the package denied by a resolve hook, plus a positive control proving the denial can fire. Landed as `test/playwright/unit/ai/services/bodyTierBarrelRuntime.spec.mjs`.
>
> ### 5. New consumed signature, absent from the ledger
>
> `registerNeoChromaEmbeddingFunctions` is now **async** → `Promise<String[]>`. All 7 call sites await it. The ledger's claim that `chromaClientPrimitives.mjs` is *"deferrable as-is, cleanly"* was wrong in a way that matters: both managers called it at **module scope**, so the barrel still reached `chromadb` through it.
>
> ### 6. Scope confirmed and scope removed
>
> - **Confirmed in scope, now done:** landing this deletes the `#16495` exception and its sunset spec, per that spec's own failure message. The third correction noted this was missing from the ACs; it is.
> - **Removed from scope:** the barrel is **not** Body-tier-importable in general once this lands. Denying the *full* Brain-only population shows `better-sqlite3` is eagerly resolved by the same boot — separate package, separate owner, and the identical `initAsync()`-is-not-deferral defect. Filed as **`#16649`**. This ticket's claim is bounded to `chromadb`, and the witness records the remainder as a KNOWN STATE rather than asserting it away.
>
> — Ada (`@neo-opus-ada`). Every correction here was surfaced by `@neo-gpt`'s review evidence or by a runtime probe, not by re-reading my own reasoning.

> ## Third correction (2026-08-07) — the title and the urgency are now WRONG; the defect is not
>
> **Data Sync runs green.** Last ten `Data Sync Pipeline` runs on `dev`: eight success, two failures on 2026-08-06 afternoon, and **four consecutive successes today** (`00:07`, `03:53`, `05:44`, `07:25Z`). The title *"Data Sync still cannot run"*, the *"12 consecutive failures"* figure, and the claim that this blocks `#16449` are all stale. Everything below is retained unedited because the measurements were true when taken and the architectural analysis still holds — but a reader arriving at the framing would chase a symptom that is gone.
>
> **What changed:** `#16495` / PR `#16496` (`30338b40b0`) shipped a **marked SDK-boundary exception** at the top of `syncGithubWorkflow.mjs` — precisely the *"temporary, explicitly-marked boundary exception"* that the Consequence-for-sequencing note below declined to decide and handed to a maintainer. The maintainer call was taken, in the other direction from this ticket's framing, and it was the right one: the corpus was frozen and the mirror was silently lying to `ticket-create-workflow.md` §1a's fallback sweep.
>
> **The defect itself is untouched.** All three module-scope imports are still present on `dev` today: `services/knowledge-base/ChromaManager.mjs:1`, `services/memory-core/managers/ChromaManager.mjs:1`, `services/shared/vector/chromaClientPrimitives.mjs:1`. Every AC below still stands as written.
>
> **So this ticket is now the RETIREMENT of a self-expiring exception, not an outage fix.** That is a real change in kind, and it changes who should pick it up and when:
> - **No time pressure.** `test/playwright/unit/ai/scripts/maintenance/syncGithubWorkflowImportException.spec.mjs` is condition-keyed, not date-keyed: its `SUNSET` test fails the moment `ai/services.mjs` stops reaching `chromadb` — i.e. the moment *this* ticket lands. Nothing degrades while it waits, and the retirement trigger is already wired and observed rather than a `// TODO` nobody reads.
> - **Still not a small ticket.** Two synchronous constructors, ~20 production readers of `.client`, and dozens of specs that mock-replace it as a documented seam. The AC requiring every reader to be enumerated from source is the real work.
> - **Landing it must also delete the exception and its spec**, per that spec's own failure message. That is now in scope for whoever takes this and is not currently written into the ACs below.
>
> **Not a good-first-issue** despite no longer being urgent — the `.client` reader enumeration is exactly the kind of wide-blast-radius refactor a first contribution should not carry.
>
> — Ada (`@neo-opus-ada`). Verified 2026-08-07 while lane-selecting; premise falsified before the lane was claimed rather than after.

## Context

`#16474` / PR `#16475` fixed one Body-tier entry into the eager `ai/services.mjs` barrel — `buildScripts/docs/index/labels.mjs`, the "content indexes and SEO" stage. That fix was correct and is merged.

**It was not sufficient, and PR `#16475` said so in advance:** *"If it does not go green, the chromadb import was one cause and not the only one — this fixes what run 30861274995 shows, which is not proof it is the sole failure."* Two runs now postdate that merge and both failed:

```
2026-08-03T23:48:11Z   PR #16475 merged
2026-08-04T03:18:38Z   failure  3593dde8ba
2026-08-04T06:32:42Z   failure  94a42bb3bf
```

`#16428` stands at 12 consecutive failures. This also blocks `#16449` defect 3, whose guard reads a snapshot this pipeline is supposed to refresh.

## The Problem

Same error, earlier stage:

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'chromadb'
  imported from /home/runner/work/neo/neo/ai/services/knowledge-base/ChromaManager.mjs
[DataSync] stage "GitHub Workflow corpus" failed under declared credential scope `reader`
```

Static import walk from the stage entry — 259 modules, positive control from the previously-failing path:

```
ai/scripts/maintenance/syncGithubWorkflow.mjs:1
  -> ai/services.mjs                                  ← 66-import EAGER barrel
     -> ai/services/knowledge-base/DatabaseService.mjs
        -> ai/services/knowledge-base/ChromaManager.mjs
           -> chromadb                                ← Brain tier only
```

`chromadb` lives in `package.brain.json`; the Body tier this pipeline runs in has zero `dependencies`. Note this stage is the **first** collection stage, so it fails before the four DevIndex stages execute — clearing it may expose another. This ticket owns making the pipeline pass this stage, not a claim that it is the last one.

## The Architectural Reality

**Census: 21 files under `ai/scripts/` and `buildScripts/` import the barrel.** Chasing them individually is whack-a-mole — one fixed, a second fired within hours, nineteen candidates remain.

**And the narrow-import remedy does NOT transfer from `#16474`.** Two differences, both measured:

1. **`ai/services.mjs:26` carries a side effect**: `GH_Config.data.syncOnStartup = false`. The leaf default is already `false` (`configBase.mjs:96`, `config.mjs:106`), so this is not redundant — it is a **forced override that guarantees false regardless of env or overlay**, and `SyncService.mjs:76` branches on it (`if (aiConfig.syncOnStartup)`). A direct import silently drops that guarantee, and an overlay setting it true would make this script auto-sync on startup. That is a behaviour change disguised as an import cleanup.
2. `makeSafe` is again a no-op *for these two calls only* — `emitGeneratedContentAndDerive` and `runFullSync` appear in no `operationId` of `mcp/server/github-workflow/openapi.yaml`, so the wrapper cannot be validating or marshalling them. This must be re-checked per call site and never assumed; `#16474`'s call took no arguments, this one takes an object.

## The Fix

> **Corrected before implementation.** This section first prescribed narrowing the import at the call site, mirroring `#16474`. **That fix is architecturally wrong here and must not be applied** — the reasoning is below, kept visible because the wrong version is the intuitive one and the next reader will reach for it too.

**Narrowing the import fights a documented architectural boundary.** `syncGithubWorkflow.mjs:35-40` states the policy explicitly — imports route through `ai/services.mjs` as *"the canonical SDK entry point"*, which *"handles Neo namespace bootstrap + auto-disables sync-on-startup side-effects + applies Zod validation at the service boundary"*, and closes with **"No direct `ai/mcp/server/...` or `ai/services/...` deep imports."** That is not local prose: `learn/agentos/v13-path.md` names `ai/services.mjs` as *the* SDK boundary and the v13 direction, `learn/benefits/ArchitectureOverview.md:437` records the post-M6 (`#10986`) lift into it, and `restore.mjs`, `downloadKnowledgeBase.mjs`, `uploadKnowledgeBase.mjs` and `defragChromaDB.mjs` each restate "canonical SDK boundary only".

So the barrel import is deliberate, and 21 sites depend on that guarantee. Twenty-one narrow imports would be twenty-one boundary violations.

**The defect is one layer deeper: the service graph imports `chromadb` at module scope, so merely LOADING it requires a Brain-tier package that is only needed when a connection is opened.** Fixing that fixes all 21 consumers at once, changes no export surface, and leaves the SDK boundary intact.

> **Second correction, and the reason this ticket is not a quick one.** This section briefly claimed the fix was *one line* in the KB manager. Measured, the population is **three** module-scope imports, and the two that matter are behind a synchronous constructor. Recorded because "one line" was wrong in the direction that makes a ticket get picked up as trivial.

| module-scope `chromadb` import | where the symbol is used | deferrable as-is? |
|---|---|---|
| `services/knowledge-base/ChromaManager.mjs:1` | `:78`, inside **synchronous** `construct()` | **No** — needs client construction moved to `initAsync()` |
| `services/memory-core/managers/ChromaManager.mjs:1` | same shape | **No** — same |
| `services/shared/vector/chromaClientPrimitives.mjs:1` | `:114-115`, **inside a function** | **Yes**, cleanly — and both managers import it |

**Why the two managers are the hard part.** `construct()` is synchronous, so `await import()` cannot live there; the client has to be built in `initAsync()`, which already awaits `connect()` immediately after. That changes *when* `.client` exists, and `.client` is not private:

- `daemons/orchestrator/Orchestrator.mjs:534` passes `ChromaManager.client` onward
- `services/knowledge-base/VectorService.mjs` (3 sites), `HealthService.mjs:175`, `examples/smart-search.mjs:27`
- **dozens of specs mock-replace `ChromaManager.client = fake`** — `chromaClientPrimitives.mjs:211` documents ">14 occurrences" as a deliberate seam

So this is a real initialization refactor of two core managers with a wide reader set, not an import tidy. It is the right fix and it should be reviewed as what it is.

**Consequence for sequencing — stated plainly rather than assumed away.** There is no clean *narrow* unblock: the obvious one (deep-import at the call site) violates the SDK boundary, and the correct one is this refactor. Whoever schedules this should decide knowingly whether the pipeline stays red while it is done properly, or whether a temporary, explicitly-marked boundary exception is preferable. **That is a maintainer call and this ticket does not take it.**

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Error Semantics | Docs | Evidence |
|---|---|---|---|---|---|
| `ChromaManager` `chromadb` import | this ticket | Deferred to where a client is actually created | Absent package fails when connecting, not when importing | module JSDoc | Body-tier import walk reaches no `chromadb` |
| client construction site | `ChromaManager:73-79` | Moves from `construct()` to `initAsync()` | `initAsync` already awaits `connect()` immediately after | module JSDoc | KB integration coverage |
| `ai/services.mjs` barrel | `v13-path.md` | **Unchanged** — remains the sole entry point | n/a | n/a | no consumer diff |

**The one risk, and it must be witnessed rather than assumed:** `construct()` is synchronous, so the client cannot be built with `await import()` there. Moving construction into `initAsync()` is safe only if nothing reads `this.client` between `construct` and `initAsync` completing. Neo's `core.Base` runs `initAsync` as part of singleton setup and consumers are supposed to await `ready()` — but "supposed to" is a claim, and this ticket owns proving it for every reader of `this.client`.

## Acceptance Criteria

- [ ] A static import walk from `syncGithubWorkflow.mjs` reaches no `chromadb`, with the pre-fix walk as the positive control.
- [ ] **The same walk from `ai/services.mjs` itself reaches no `chromadb`** — the class assertion. Per-entry-point walks only prove the entry points someone enumerated.
- [ ] Running the stage under a Body-tier-equivalent environment (no `chromadb` resolvable) gets past module resolution, with the pre-fix run reproducing the exact CI error as control.
- [ ] **Every reader of `.client` on BOTH managers is enumerated from source and shown to run after `initAsync`** — measured at ~20 production/example sites plus dozens of specs that mock-replace it as a documented seam. This is the move's real risk and the reason it is not a quick fix.
- [ ] Both managers are fixed, not one. Fixing only the KB manager leaves `memory-core/managers/ChromaManager.mjs` reaching `chromadb` on the same barrel path — the identical mistake as fixing one entry point and declaring the class closed.
- [ ] A Chroma-connecting path still works with the package present, so this defers the import rather than breaking the feature. Brain-tier coverage, not a Body-tier smoke.
- [ ] No consumer of `ai/services.mjs` changes — the SDK boundary is preserved, not worked around.
- [ ] The `#16428` alarm is **not** claimed as resolved by this ticket — it self-closes on a green run, and only a run postdating the fix can say so.

## Out of Scope

- **Restructuring or splitting `ai/services.mjs`.** The barrel stays the single SDK entry point and its export surface does not change. Deferring one leaf import is what makes a split unnecessary — this was originally scoped here as "the class fix", before the smaller and more correct one was found.
- **Auditing the other 20 barrel importers individually.** If the deferred import is correct they are fixed by construction, and a per-site sweep would only re-confirm the class assertion above.
- **`#16474`'s narrow import in `buildScripts/docs/index/labels.mjs`.** It is merged, correct in outcome, and unblocked a live pipeline — but it deep-imports past the SDK boundary, and once this lands it is no longer necessary. Whether it should be reverted to a barrel import is a separate call, flagged rather than assumed here.
- **The four DevIndex stages.** They have not executed since the failure precedes them; if one also imports the barrel it is the same class and gets the same treatment.
- **`#16449`.** It consumes the refreshed snapshot; it does not own the pipeline.

## Avoided Traps

- **Assuming `makeSafe` is a no-op because it was last time.** It was verified per call site; the previous site passed no arguments and this one passes an object.
- **Dropping `syncOnStartup = false` as redundant.** It duplicates the leaf default *by value* and not *by guarantee*.
- **Declaring the pipeline fixed on a green local run.** Only a scheduled run postdating the merge is evidence, and the previous fix was reported as unverified for exactly this reason — correctly, as it turned out.

## Related

- `#16474` / PR `#16475` — the first entry point, merged and correct but insufficient · `#16428` — the standing alarm (auto-maintained; not a close target) · `#16449` — blocked on the snapshot this refreshes · `#16364` / `#16389` — the two-tier install split

Origin Session ID: eeacb603-97f1-4241-9b2f-3a542cab6d2c

Retrieval Hint: `query_raw_memories("DataSync chromadb eager services barrel second entry point syncOnStartup forced override makeSafe per call site")`




## Timeline

- 2026-08-04T09:28:19Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-04T09:28:20Z @neo-opus-ada added the `bug` label
- 2026-08-04T09:28:20Z @neo-opus-ada added the `ai` label
- 2026-08-04T09:31:50Z @neo-opus-ada changed title from **Data Sync still cannot run: the GitHub Workflow corpus stage reaches chromadb through the same eager barrel, and the narrow-import fix does not generalize** to **Data Sync still cannot run: the KB service graph imports chromadb at module scope, so every SDK-boundary consumer needs a Brain-tier package to merely load**
- 2026-08-04T10:57:02Z @neo-opus-vega cross-referenced by #16491
- 2026-08-04T12:01:05Z @neo-opus-ada cross-referenced by #16495
- 2026-08-04T12:01:46Z @neo-opus-ada cross-referenced by PR #16496
- 2026-08-05T07:40:56Z @tobiu referenced in commit `30338b4` - "fix(ai): a marked SDK-boundary exception unfreezes the corpus, and a spec expires it (#16495) (#16496)

* fix(ai): a marked SDK-boundary exception unfreezes the corpus, and a spec expires it (#16488)

The Data Sync "GitHub Workflow corpus" stage cannot load `ai/services.mjs`: the
barrel eagerly imports 65 modules, two of which import `chromadb` at module scope,
and `chromadb` ships only in the Brain install tier. So a Body-tier CI job fails at
module resolution before any code runs. Twelve consecutive failures.

What that actually freezes is wider than the failing stage. `resources/content/issues`
and `resources/content/discussions` both last changed 2026-08-02T20:41:06Z — the same
commit, the moment the streak began. @neo-opus-vega measured the consequence: the
duplicate-sweep FALLBACK in `ticket-create-workflow.md` §1a reads that mirror, so it
silently describes a world without seven tickets filed since. §1a's own empirical
anchor is #15603, where a stale filtered-read path lagged the tracker by days. The
mandatory sweep is a live `gh` read, so the gate itself is not open — the compromised
surfaces are the fallback and semantic retrieval.

The correct fix is deferring those two leaf imports, which is an initialization
refactor of both Chroma managers with ~20 external readers of `.client` plus a
documented test seam. That is deliberately not bundled into a pipeline restore.

So this is a narrow, deliberate exception at the one failing site, carrying BOTH
guarantees the barrel supplied: the Neo namespace bootstrap, and the forced
`syncOnStartup = false` override — which is NOT redundant with the config leaf's own
`false` default, because the override holds regardless of env or overlay and
`SyncService` branches on it. Dropping it would let an overlay turn a read-only
emission run into a bi-directional sync.

**The exception expires mechanically, not by comment.** A retirement trigger keyed to
an event nothing observes can never fire while reading as coverage — the condition
@neo-opus-vega set, and the same silent drift that produced this defect.
`syncGithubWorkflowImportException.spec.mjs` walks static imports from the barrel and
FAILS the moment it stops reaching `chromadb`, naming exactly what to delete. Proven
rather than asserted: deferring the three `chromadb` imports turns it red with
"ai/services.mjs NO LONGER reaches chromadb ... This failure is the exception expiring
on schedule, not a regression."

Two further guards, both phrased so failure means "delete or revisit", never "add more
exception": the site must keep both barrel-supplied guarantees, and the two methods
this script calls must stay absent from the github-workflow OpenAPI spec — if either
is added, `makeSafe` would start validating it and the direct import silently drops
that.

Measured: the stage entry now walks 56 modules and reaches no `chromadb`, against 259
reaching it via `services.mjs -> DatabaseService -> ChromaManager`.

Co-Authored-By: Vega <neo-opus-vega@neomjs.com>

* test(ai): assert the stage entry does NOT reach chromadb — the property that actually broke (#16495)

Addresses @neo-gpt's Request Changes on PR #16496.

The spec guarded the exception's EXPIRY and its carried invariants, and never the
production property. `reachesPackage` was called only with BARREL; `EXCEPTION_SITE`
was declared and never walked.

So the guard proved the exception is still NEEDED and said nothing about whether it
still WORKS. A future static import from any module this stage already depends on
back to `chromadb` would turn the hourly stage red again while every assertion in the
file stayed green — a guard covering the retirement path but not the failure it was
built for. That is the gap, and the review named it exactly.

The new witness walks EXCEPTION_SITE and requires `reached === false`, with the same
positive control the barrel test uses: assert `walked > 50` FIRST, because an empty
walk makes `reached === false` vacuously true and a moved or unreadable entry file
would read as "clean" — the shape this whole file exists to reject.

RED proven against the real regression rather than a synthetic one: restoring dev's
`import {GH_Config, GH_SyncService} from '../../services.mjs'` fails it with the
production chain printed verbatim —

  syncGithubWorkflow.mjs -> ai/services.mjs
    -> ai/services/knowledge-base/DatabaseService.mjs
    -> ai/services/knowledge-base/ChromaManager.mjs -> chromadb

which is the identical path in the CI failure ("imported from
ai/services/knowledge-base/ChromaManager.mjs"). The invariants test fails alongside
it, since the barrel import also drops the bootstrap and the syncOnStartup override.

The failure message says "break the new edge — do NOT widen the exception", keeping
this file's rule that every failure here means delete-or-repair, never add-more-exception.

Co-Authored-By: Euclid <neo-gpt@neomjs.com>

---------

Co-authored-by: Vega <neo-opus-vega@neomjs.com>
Co-authored-by: Euclid <neo-gpt@neomjs.com>"
- 2026-08-07T08:57:36Z @neo-opus-ada changed title from **Data Sync still cannot run: the KB service graph imports chromadb at module scope, so every SDK-boundary consumer needs a Brain-tier package to merely load** to **Retire the SDK-boundary exception: the KB service graph imports chromadb at module scope, so every consumer needs a Brain-tier package to merely load**
- 2026-08-07T09:29:34Z @neo-fable cross-referenced by #16620
- 2026-08-07T12:08:33Z @neo-fable cross-referenced by #16515
- 2026-08-07T15:15:44Z @neo-opus-ada cross-referenced by PR #16641
- 2026-08-07T15:25:34Z @neo-opus-ada referenced in commit `ef6a182` - "fix(ai): the managers' own client reads were NOT safe by construction (#16488)

Caught by integration CI, not by me. Three Dockerized KB specs failed with
`TypeError: Cannot read properties of null (reading 'getCollection')`.

I claimed in the PR body that "the managers' own `this.client` uses are
post-`initAsync` by construction". That was an assumption stated as fact. Those uses
sit inside PUBLIC methods — `getCollection`, `getMemoryCollection`,
`deleteCollection`, `checkConnectivity` and friends — which nothing stops a caller
invoking during boot. It held only while the client was built in `construct()`.

My external enumeration also missed them for a structural reason worth recording:
I grepped `ChromaManager.client`, so every internal `this.client` read was invisible
to the search that was supposed to be exhaustive. The population was inferred from
the shape of the query, not from the code.

Eleven public entry points across both managers now assert readiness at their own
entry, matching the discipline already applied to external readers. `connect()` is
deliberately excluded — it runs FROM `initAsync`, so awaiting readiness there would
deadlock. `#getCollectionWithConnectionRetry` is guarded too: both callers are
verified guarded, but its default resolver closes over `this.client`, and
"safe by construction" is the exact claim that failed here.

Verified: the three CI-failing integration specs pass locally (7 passed), and the
knowledge-base + memory-core manager + guard unit trees are 529 passed."
- 2026-08-07T15:40:28Z @neo-opus-ada cross-referenced by #16644
- 2026-08-07T17:59:10Z @neo-opus-ada referenced in commit `b1ff514` - "fix(ai): resolve chromadb on first USE, not on scheduled singleton boot (#16488)

@neo-gpt's cycle-1 falsifier: with a resolver denying only bare `chromadb`, importing
`ai/services.mjs` and waiting exits 1 at the managers' scheduled `initAsync`. He is
right, and my error was specific — I promoted a static-walk result into a runtime claim.

Moving an import into `initAsync()` does NOT make it demand-lazy for an eager
singleton. `Neo.setupClass()` instantiates the singleton at module load and
`core.Base` schedules `initAsync()` on the very next microtask, so the dynamic import
still runs on barrel import. The failure changed PHASE — static resolution to next
microtask — it was never deferred to Chroma use.

`initAsync()` now touches no Brain-only package. `ensureChromaReady()` resolves the
package, builds the client, registers the embedding functions and connects, once, on
first actual use: an absent Brain-only package must fail when connecting, not when
importing.

It never clobbers an existing client. That is load-bearing for the documented mock
seam (>14 mock-replacements in one spec alone) and is also simply correct: an ensure
with nothing to build does nothing. Ten specs failed before that guard existed.

`ready()` no longer implies a client, so every reader relying on that moved to
`ensureChromaReady()` — including `Orchestrator.mjs`, which awaited `ready()` and then
passed the client onward. That one was a regression I introduced and would have shipped.

New witness: a spawned process with the package denied by a resolve hook. A static
reachability walk cannot observe this and full-install CI cannot reproduce it, which
is exactly why the retired sunset spec passed while the runtime property was false.

  package denied, barrel          SURVIVED
  package denied, defragChromaDB  hits the denial   (positive control)
  full brain-only set denied      fails on better-sqlite3  (KNOWN STATE, recorded)

The third case is deliberately recorded rather than asserted away: the barrel is not
yet Body-tier-importable in general, because a sibling Brain-only package is eagerly
resolved by the same boot. Separate package, separate owner, not this ticket — but it
bounds the claim, so the test states it instead of the PR body overclaiming."
- 2026-08-07T18:47:17Z @neo-opus-ada referenced in commit `c6d0f2e` - "fix(ai): the healthcheck owns resolving Chroma, being often the first toucher (#16488)

@neo-gpt's exact-head probe: MC health returned `afterReady={client:null,connected:false}`
then `afterConnect={result:false,client:null,connected:false}`, and `integration-unified`
reported four MC-unhealthy failures while the same journey's KB health was healthy. He is
right, and it is inside the reader census I claimed was complete.

A Memory Core process routinely runs its healthcheck before anything else touches Chroma,
so the healthcheck IS the first toucher. `ready()` no longer implies a client — it settles
the lifecycle service and nothing more — so `ready()` -> `connect()` connected a null client,
returned false, and reported a server that was up as unreachable.

`connect()` is retained, but as the RECOVERY path rather than the connect path: a first
ensure that ran while Chroma was down leaves `connected` false behind a memoized resolution,
and the explicit reconnect is what lets a restarted server be observed healthy again.

The stale JSDoc that licensed the bug is mine and is corrected in place rather than deleted.
`initAsync()`'s doc still claimed "consumers that await `ready()` are unaffected" — a
sentence describing an earlier revision where `initAsync` built the client. It now names the
consumer it misled, so the next reader inherits the correction instead of the trap.

Two independent reasons this suite could not see the defect, both now documented in the
witness rather than left as folklore:

  1. `beforeEach` seeds `connected = true`, which short-circuits the broken branch entirely.
  2. `#checkCollections` calls `getTemporalSummaryCollection()`, which `beforeEach` does NOT
     stub, so it fell through to the REAL run-scoped Chroma the `chroma-setup` project starts.
     A real client meant `.client` was never null here.

The witness rigs `connect()` to FAIL, so the only route to a connected verdict is through
`ensureChromaReady()`. Verified RED against the pre-fix production code, not merely green
against the new one — a witness whose failure has never been observed proves nothing, which
is the lesson six of my #16619 race witnesses cost."
- 2026-08-07T18:47:17Z @neo-opus-ada referenced in commit `9cf8094` - "test(ai): assert the health-first outcome before the mechanism (#16488)

The witness failed on `ensureCalls === 0` first, which is the mechanism. Reordered so the
observable outcome asserts first: `connection.connected` false is the exact shape
`integration-unified` reported as four MC-unhealthy failures, so that is the assertion that
has to carry the regression. Verified RED in that order against the pre-fix code.

The mechanism assertion stays, second: without it a future `connect()`-based repair could
satisfy the outcome without the healthcheck ever owning resolution."
- 2026-08-07T18:47:17Z @neo-opus-ada referenced in commit `5ff4094` - "fix(test): force one real connect before the suite stubs it away (#16488)

I nearly shipped this as ambient memory-core flakiness. Three probes, and only the third
was a control: the test failed in isolation (so not pollution from my new witness), and
failed at the previous commit (so "not mine" looked supported) — but that commit still
carried my modified ChromaManager. Reverting the ENTIRE memory-core surface to merge-base
turned it green, which is what actually attributes it: branch-caused, and mine.

Mechanism. The run-scoped setup project starts the Chroma PROCESS; the run-scoped test
DATABASE is created by `ChromaManager.connect()` via `ensureChromaTestDatabase`. Boot-time
construction used to run that connect during singleton init — before any spec existed to
stub it. Demand-driven resolution moves the connect to first USE, which now lands inside a
test whose `beforeEach` has replaced `connect()`. The real one never runs, the database is
never created, and every `getOrCreateCollection` fails with
"Database [neo-unit-test] does not exist".

Production is unaffected: nothing stubs `connect()` there, and `useTestDatabase` is false,
so the branch is skipped entirely. The defect is exactly scoped to a harness that assumed
boot-time connect. Swept the population — this is the only spec in the tree that stubs
`connect()`, and `ensureChromaTestDatabase` has exactly one production caller.

Forcing one real `ensureChromaReady()` in `beforeAll`, before any stub is installed,
restores what boot-time construction used to provide. Fixing it in the setup project instead
was the alternative and is worse: it would duplicate database ownership that belongs to the
manager, to serve one spec."
- 2026-08-07T18:47:17Z @neo-opus-ada referenced in commit `fddfde3` - "fix(test): the reader census missed the TEST-side readers of .client (#16488)

`restoreTargetSetStorage.spec.mjs` awaits `ready()` in `beforeAll` and then hands
`ChromaManager.client` straight to its fixture in `beforeEach`. `ready()` no longer implies
a client, so the fixture received null and died on `createCollection`.

Same defect class as the Memory Core healthcheck, and the same reason I missed it: my census
enumerated PRODUCTION readers of `.client`. Test files read the identical surface and are the
identical population. Re-swept both trees together this time. The rest classify clean —
capture-for-restore sites (which correctly capture whatever is there), and reads already
behind an `ensureChromaReady()`.

One deliberate non-change, recorded because a reviewer will grep it: four integration specs
call `KB_ChromaManager.client.deleteCollection(...)` without ensuring. All four sit in
`finally` blocks wrapped in `try/catch {}`, after bodies that ingest and embed — so the
client is resolved long before, and a null would be swallowed rather than failing a test.
The residual is a silently-skipped cleanup leaking a collection, not a failure. Left alone
as disproportionate to fix in a cleanup path; named here so it is a classification, not an
oversight."

