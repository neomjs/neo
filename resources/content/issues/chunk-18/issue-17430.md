---
id: 17430
title: 'Two grid properties lost coverage when devindex left: a paused thumb drag, and scroll cost'
state: CLOSED
labels:
  - architecture
  - tech-debt
assignees:
  - neo-opus-grace
createdAt: '2026-08-20T17:41:28Z'
updatedAt: '2026-08-25T19:26:07Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17430'
author: neo-opus-grace
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-25T19:26:07Z'
---
# Two grid properties lost coverage when devindex left: a paused thumb drag, and scroll cost

## Context

#17421 removes `apps/devindex` and the five e2e specs that navigated to it. Those specs were the app's coverage, and they moved to `neomjs/devindex` with it — correct disposition, and the operator explicitly accepted removing now.

But three of the five were also **engine** coverage that merely used the app as a fixture. Most of that survives; two properties do not, and this is the account of which — filed at removal time rather than discovered later as a silent hole.

## What survives (measured, not assumed)

| property | still covered by | fixture |
|---|---|---|
| Visual blanking + jitter under scroll | `grid/RowPinning.spec.mjs` — the **identical** test title | BigData |
| Optical + recycling horizontal scroll | `grid/LockedCellHorizontalStability.spec.mjs` | lockedColumns |
| Scrollbar-routed overdrag, locked region frozen | `grid/ColumnOverdragScroll.spec.mjs` | lockedColumns |

`RowPinning.spec.mjs` already carried *"Scroll Telemetry: Visual Blanking and Jitter Detector"* against BigData — the same title the departing spec used — so the jitter half needed no retargeting. Worth stating plainly, because the obvious assumption on reading the removal diff is that it left with the app.

## The Problem — two properties genuinely left

**1. Pausing mid-thumb-drag no longer has any coverage.**

`ThumbDragPause.spec.mjs` asserted *"Pausing during thumb drag keeps pinning active"* — hold the scrollbar thumb, stop moving, and pinned rows must stay pinned. `RowPinning.spec.mjs` contains exactly **one** test and it is the telemetry one, so nothing remaining exercises a paused drag. The pause is the interesting part: it is the state where a drag is live but no events are arriving, which is exactly when a stale-render bug is invisible to a moving-drag test.

**2. neo has no grid scroll benchmark.**

`GridProfile` and `GridScrollBenchmark` were the only grid wall-clock measurements here. The sole remaining file under `e2e/benchmarks/` is `HarnessEnduranceBenchmark.spec.mjs`, which drives `/ai/examples/harnessEndurance/neo/` and measures event-loop lag and heap under a streamed load — a different subject entirely. Nothing in this repository now measures grid scroll cost.

This one is **not** simply "re-point the old spec". #17421 recorded why: the benchmarks re-baseline at any fixture change (50k rows / 37 columns → 20k / 50 for bigData), so numbers before and after are not comparable and must not be read as a regression. A new series is the honest framing, not a continued one.

## Acceptance Criteria

- [ ] A neo-fixture spec asserts pinning survives a **paused** thumb drag — thumb held, no movement for a measurable interval, pinned rows still pinned and still painted
- [ ] That spec fails when the pause handling is removed or short-circuited — a moving-drag-only assertion must not be able to satisfy it
- [ ] Either a grid scroll benchmark exists against a neo-owned fixture, **or** this ticket records the decision not to have one and says what covers scroll-cost regressions instead
- [ ] If a benchmark lands, its first run is labelled as a new series with its fixture named in the annotation, so a future reader cannot compare it against the pre-removal numbers

## Deliberately not urgent

Neither gap is a live defect and neither blocks #17421 or #17376. This exists so the reduction is **observed** rather than remembered — a coverage hole with no ticket is indistinguishable from a coverage hole nobody noticed, and #17421's own diff reads as if the coverage simply moved.

## Out of Scope

- Restoring the deleted specs here; they navigate to an app this repository no longer contains
- `GridScrollBenchmark`'s dormant jank instrument and its scroll target, both marked `fixme` and now `neomjs/devindex`'s to repair

## Related

- #17421 — the removal that produced this reduction
- #17422 — the test migration that preceded it
- #17427 — the grid silent-update defect a paused-drag probe would plausibly exercise

Origin Session ID: 3e4f33e0-fb23-4a61-a2a0-7f396950f3d6

Handoff Retrieval Hints: `query_raw_memories("devindex removal grid coverage gap thumb drag pause benchmark")`. Anchors: `ThumbDragPause.spec.mjs`, `RowPinning.spec.mjs`, `HarnessEnduranceBenchmark.spec.mjs`.


## Timeline

- 2026-08-20T17:41:29Z @neo-opus-grace added the `architecture` label
- 2026-08-20T17:41:29Z @neo-opus-grace added the `tech-debt` label
- 2026-08-20T17:41:56Z @neo-opus-grace cross-referenced by #17421
- 2026-08-20T18:03:16Z @neo-opus-grace cross-referenced by PR #17429
- 2026-08-24T17:17:15Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-24T17:21:48Z @neo-opus-grace cross-referenced by #17596
### @neo-opus-grace - 2026-08-24T17:22:12Z

## Prior-art sweep before writing the spec — AC-1 and AC-3 are different classes, and only one is local-only

I had the fixture read, the deleted `ThumbDragPause.spec.mjs` recovered from `715d48d101`, and the pinning mechanism traced (`GridRowScrollPinning.applyPinning` engages on `isThumbDragging` and must stay engaged until the worker catches up). I was one step from writing the spec when the operator pointed at prior art. It changed the shape, so I am recording it before implementing rather than after.

**A downstream app in our own org runs whitebox e2e in its CI**, in a plain `node:24` container, keeping neo's full GPU-intent flag list. The delta is `channel: process.env.CI ? 'chromium' : 'chrome'` plus `npx playwright install --with-deps chromium`. Full finding, with the neo-side blocker, on #17596: https://github.com/neomjs/neo/issues/17596#issuecomment-5398811294

### What that means for this ticket's two ACs

They are **not the same class**, and this ticket currently treats them as one:

- **AC-1/AC-2 (pinning survives a paused thumb drag)** asserts *behaviour*: thumb held, no movement, rows still pinned and painted. It makes no FPS claim. Under that pattern this is a candidate for CI rather than a local-only spec — which is a materially better home for it, since the coverage was lost precisely because nothing outside a local run was watching.
- **AC-3 (grid scroll benchmark)** is a *measurement*. A software rasterizer makes those numbers lies; `--disable-frame-rate-limit` and the GL boot gate exist so a benchmark cannot quietly report the vsync cap. This one stays local by construction, and this ticket already says a new series is the honest framing.

**The caveat I will not skip:** the paused-drag assertion is behaviour, but its *witness* may not be. The original spec detected blanking, and blanking under software rendering can be real for the rasterizer and false for the engine — the same trap `RowPinning.spec.mjs` sits in. So "CI-able" is a claim about this spec's assertion that has to be earned by construction, not assumed from its category. If the witness cannot survive without hardware GL, it belongs local and this note is the reason recorded rather than a discovery repeated later.

### Verified while scoping (so the next reader does not re-derive it)

- e2e **runs in this environment**: `npm run test-e2e -- test/playwright/e2e/grid/RowPinning.spec.mjs` → 1 passed, 47.6s, real GPU. Local verification of whatever lands here is available.
- `ThumbDragPause.spec.mjs` is recoverable at `git show 715d48d101^:test/playwright/e2e/grid/ThumbDragPause.spec.mjs` — it held the thumb, paused 2500ms "longer than the removed timeout window", then drove a large drag and asserted no blanking.
- The pause is load-bearing for a reason worth keeping in the spec: it is the state where a drag is live but no events arrive, which is exactly when a stale-render bug is invisible to a moving-drag test.
- AC-2's mutation lever is `state.isPinningActive` in `src/main/addon/GridRowScrollPinning.mjs` — reintroducing an idle-based disengage is what the arm must go red on. It is main-thread browser code, so the mutation is a runtime patch rather than an env guard.

Not blocked; this is scoping, and I am carrying it forward.

🖖 Grace (Claude Opus 5, Claude Code) · session 728a756d-71df-48e6-8dad-0bac498ca23e

- 2026-08-25T17:49:55Z @neo-opus-grace cross-referenced by PR #17775
### @neo-opus-grace - 2026-08-25T17:53:30Z

## AC-3 decision: no grid scroll benchmark

AC-3 offers two branches. Taking the second: **no benchmark, and here is what covers scroll-cost regressions instead.**

A wall-clock series here would be a number nobody can read correctly. #17421 already recorded why — `GridProfile` and `GridScrollBenchmark` measured 50k rows × 37 columns, the nearest neo-owned fixture is 20k × 50 — and the hazard is not that the comparison is unavailable. It is that the comparison is *available and wrong*: two series of milliseconds look comparable whether or not they are, and the annotation asking a reader not to compare them is weaker than the instinct to do it.

Covering surfaces today:

- `grid/RowPinning.spec.mjs` — per-frame telemetry across five scroll profiles (native thumb drag and a compositor-saturation flood among them), asserting blank frames and jitter bounces are zero. That is the property a scroll benchmark protects, asserted directly rather than inferred from a duration.
- `grid/LockedCellHorizontalStability.spec.mjs`, `grid/ColumnOverdragScroll.spec.mjs` — recycling and locked-region correctness under scroll.
- `grid/ThumbDragPause.spec.mjs` (PR #17775) — the pinned state across an event-free interval.

**The gap this leaves, stated rather than buried:** nothing above catches a change that keeps every frame correct while making each one twice as expensive. That is a genuine cost gap. Closing it wants a fixture decision plus a fresh baseline series, which is different work from restoring a lost assertion — so it is recorded here as an accepted absence rather than carried as a silent one.

AC-4 is therefore N/A by its own terms: it is conditional on a benchmark landing.

AC-1 and AC-2 are delivered in PR #17775, including the mutation control (an inactivity timeout patched into `GridRowScrollPinning.onMouseDown` turns the new spec red on the pinning assertion itself).

🖖 Grace (Claude Opus 5, Claude Code) · session 8daa7672-824e-4d4a-9283-8a0b908180c8


- 2026-08-25T18:29:12Z @neo-opus-grace referenced in commit `a6c5ebc` - "test(grid): the paused-drag probe asserts after the pin latch clears (#17430)

@neo-gpt-emmy's RA-1: the pause landed before pinning was ever engaged, so the spec
demonstrated only that a fresh scroll engages after an idle hold. Engaging the pin first
exposed a second problem — `isPinningActive` latches, so a jump taken while it is set
re-pins whether or not the held-thumb state survived. The assertion now runs after a
verified worker catch-up, where engagement can only come from a thumb still held.

Refs #17430"
- 2026-08-25T19:26:07Z @tobiu referenced in commit `32cc4b7` - "test(grid): pinning survives a paused thumb drag (#17430) (#17775)

* test(grid): pinning survives a paused thumb drag (#17430)

The departed devindex spec asserted `__BLANK === false` against a flag that starts
false, so a run where nothing scrolled passed having observed nothing. This replaces it
against a neo-owned fixture with positive controls that make that failure mode visible.

Refs #17430

* test(grid): the paused-drag probe asserts after the pin latch clears (#17430)

@neo-gpt-emmy's RA-1: the pause landed before pinning was ever engaged, so the spec
demonstrated only that a fresh scroll engages after an idle hold. Engaging the pin first
exposed a second problem — `isPinningActive` latches, so a jump taken while it is set
re-pins whether or not the held-thumb state survived. The assertion now runs after a
verified worker catch-up, where engagement can only come from a thumb still held.

Refs #17430"
- 2026-08-25T19:26:08Z @tobiu closed this issue

