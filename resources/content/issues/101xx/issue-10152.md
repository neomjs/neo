---
id: 10152
title: 'SemanticGraphExtractor: emit typed edges to Memory + Session nodes'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - core
assignees:
  - tobiu
createdAt: '2026-04-21T09:31:18Z'
updatedAt: '2026-04-21T19:25:20Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10152'
author: tobiu
commentsCount: 0
parentIssue: 10143
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[x] 10151 DreamService: deterministic ingestion phase for Memory + Session graph nodes'
blocking: []
closedAt: '2026-04-21T19:25:20Z'
---
# SemanticGraphExtractor: emit typed edges to Memory + Session nodes

# SemanticGraphExtractor: emit typed edges to Memory + Session nodes

### Context
Parent sub-epic (#10143) phase 2. After Memory + Session are nodes (phase 1), extractor attaches extracted entities (concepts/classes/methods/tickets) to those nodes via typed edges instead of `sessionId`-scalar metadata.

### The Problem
Extractor today produces entity nodes with only `sessionId` scalar metadata — no graph edge to the anchoring Session or Memory. Consumers can't traverse from a concept back to its source memory or from a ticket-reference back to the authoring session via the graph.

### The Architectural Reality
- `ai/daemons/services/SemanticGraphExtractor.mjs` runs Gemma 4-31B (via Ollama) with a Zod-validated tri-vector schema.
- Current emission is node-only; provenance encoded (if at all) as metadata properties.
- Post-phase-1, Memory and Session nodes exist as edge targets.

### The Fix
- Extend Gemma4 extraction prompt to emit provenance edges alongside entities:
  - `MENTIONED_IN(Concept → Memory)`
  - `DISCUSSED_IN(Class | Method → Session)`
  - `REFERENCED_BY(Issue → Memory)`
- Update Zod schema to validate the new edge shapes
- Post-processing maps emitted edges to graph `addEdge` calls
- Backward compat: if Memory/Session nodes don't exist yet (pre-migration), edges queue for lazy back-fill rather than drop

### Acceptance Criteria
- [ ] Gemma4 extraction prompt updated with provenance edge emission examples
- [ ] Zod schema validates new edge types; rejections surface in `tri-vector-errors` telemetry
- [ ] Edges land in graph: integrity check confirms extracted entities have provenance edges to Memory/Session (when source is post-phase-1)
- [ ] Existing extracted-entity emission unchanged (no regression beyond additive edges)
- [ ] Telemetry: REM cycle comparison pre- vs post-shipping shows edge growth proportional to entity count

### Out of Scope
- Changing which entities are extracted
- Retroactively attaching edges to historical nodes (via lazy back-fill)
- Human review of extracted edges for correctness (validation via integrity checks + telemetry)

### Avoided Traps
- **Scalar metadata for provenance instead of edges.** Rejected. Metadata strings can't be traversed; edges can.
- **Hardcoded edge emission in post-extractor code.** Rejected. Gemma4 has per-extraction context to pick which memory sourced which entity; post-extractor would re-derive. Emission-from-LLM is a small prompt change with high semantic fidelity.

### Related
- Parent: #10143
- Blocked by: phase 1 (Memory + Session ingestion)
- Files: `ai/daemons/services/SemanticGraphExtractor.mjs`, extraction prompt templates, Zod schema

Origin Session ID: 71dc3cd8-d39d-48e1-ac62-e240ca67d1a5


## Timeline

- 2026-04-21T09:31:20Z @tobiu added the `enhancement` label
- 2026-04-21T09:31:20Z @tobiu added the `ai` label
- 2026-04-21T09:31:20Z @tobiu added the `architecture` label
- 2026-04-21T09:31:21Z @tobiu added the `core` label
- 2026-04-21T09:33:08Z @tobiu added parent issue #10143
- 2026-04-21T09:33:10Z @tobiu marked this issue as being blocked by #10151
- 2026-04-21T13:06:54Z @tobiu cross-referenced by #10143
- 2026-04-21T13:23:14Z @tobiu cross-referenced by #10158
- 2026-04-21T14:36:20Z @tobiu cross-referenced by PR #10161
- 2026-04-21T17:03:02Z @tobiu cross-referenced by #9999
- 2026-04-21T18:19:18Z @tobiu assigned to @tobiu
- 2026-04-21T18:27:01Z @tobiu cross-referenced by PR #10165
- 2026-04-21T19:25:20Z @tobiu referenced in commit `aafc8bc` - "feat(ai): extract Gemma4 provenance edges (#10152) (#10165)

* test(ai): formalize Gemma4 provenance extraction tests (#10152)

- Added SemanticGraphExtractor.spec.mjs to verify provenance edge validation and lazy backfill.
- Fixed async race condition in SemanticGraphExtractor lazy edge serialization.
- Fixed GraphService.linkNodes test leak in DreamService.spec.mjs.

* chore(ai): address PR review polish items for provenance lazy queue"
- 2026-04-21T19:25:20Z @tobiu closed this issue
- 2026-04-21T21:18:27Z @tobiu cross-referenced by PR #10167
- 2026-04-21T22:47:30Z @tobiu cross-referenced by PR #10171
- 2026-04-21T23:19:24Z @tobiu cross-referenced by #10172
- 2026-04-21T23:21:05Z @tobiu referenced in commit `3dbea00` - "feat(ai): lazy back-fill for Memory + Session graph nodes (#10153) (#10171)

* feat(ai): lazy back-fill for Memory + Session graph nodes (#10153)

Closes the producer/consumer contract opened by Gemini's #10152 / PR #10165:
missing MEMORY: / SESSION: edge targets are now materialized from their Chroma
source rows on-demand rather than silently culled. Together with the eager
priorityBackfill CLI, this removes the forward-only-ingestion asymmetry that
would otherwise leave ~9040 pre-migration Chroma rows permanently unreachable
by graph edges.

Pipeline:
- MemorySessionIngestor.ingestSingleRow(graphNodeId) — per-row analog of the
  existing syncSessionToGraph batch path. Case-insensitive on MEMORY:/SESSION:
  prefix so it consumes Gemini's uppercase-prefix queue entries without a
  canonical-format migration. Recursive parent-Session creation when a Memory
  is back-filled. Minimal-session fallback for never-summarized history.
  Tags backfilled: true to distinguish from live-ingested nodes.
- GraphService.linkNodesAsync + ensureNodeExists + normalizeGraphNodeId — async
  edge-creation path that attempts back-fill before falling through to the
  existing cull behavior. Sync linkNodes unchanged; zero blast radius on
  existing callers. Dynamic import of MemorySessionIngestor breaks the
  module-load cycle.
- LazyEdgeDrainer.drainQueue — JSONL queue consumer for ai/data/memory-core/
  lazy-edges.jsonl (producer: #10165). Atomic rename-then-drain pattern handles
  concurrent producer appends. Failures retained for retry; malformed lines
  discarded (never succeed on retry).
- ai/scripts/priorityBackfill.mjs — operator CLI. Two-phase: ingest sessions
  in a last-N-days window (default 30) + drain the lazy queue. --dry-run and
  --skip-drain flags.
- liveIngested: true marker added to syncSessionToGraph upsert paths — mutually
  exclusive with backfilled: true for provenance queries.

Design doc: learn/agentos/tooling/GraphBackfill.md registered in tree.json.

Test coverage: 35 tests across 3 specs (13 MemorySessionIngestor + 15
GraphService + 7 LazyEdgeDrainer) — all green in 845ms combined.

Coordination note: node-ID case convention (memory: vs MEMORY:) is a known
seam between the ingestor's lowercase and the extractor's uppercase. Both
forms resolve via normalization; settling on a canonical direction is
deferred to a follow-up.

* fix(ai): recover orphaned .draining file from SIGKILL edge case (#10153)

Gemini 3.1 Pro's Depth Floor §7.1 review on PR #10171 surfaced a legitimate
edge case: if the Node process is terminated via SIGKILL (OOM, hardware
failure) between `rename(queuePath, drainingPath)` and the try/catch restore,
the `.draining` file is orphaned and those queued edges wait for manual
recovery. The catch-block restore doesn't execute under signal-based
termination.

Fix: `LazyEdgeDrainer.recoverOrphanedDraining(queuePath, drainingPath)`
runs at the start of every `drainQueue` invocation. If a `.draining` file
exists (from a prior crashed run), its content is merged into the live
queue and the orphan deleted. Idempotent — subsequent recovery-on-boot
finds no orphan and no-ops.

Stats shape extended with `orphanRecovered: Boolean` so operators can
observe recovery events via the drain-cycle logs.

Test coverage: +1 Playwright test in LazyEdgeDrainer.spec covering the
orphan-recovery path (synthesizes a .draining file + fresh queue, asserts
both drain in a single run and the orphan is cleaned up). 8/8 green."

