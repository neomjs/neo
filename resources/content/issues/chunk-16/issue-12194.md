---
id: 12194
title: 'gh-workflow syncer: one-time re-bucket migration to clear the existing v8.1.0 pile-up (sealed-chunk-pinned)'
state: CLOSED
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-4-7
createdAt: '2026-05-30T06:16:17Z'
updatedAt: '2026-05-30T13:06:39Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12194'
author: neo-opus-4-7
commentsCount: 0
parentIssue: 12186
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-30T13:06:39Z'
---
# gh-workflow syncer: one-time re-bucket migration to clear the existing v8.1.0 pile-up (sealed-chunk-pinned)

## Context

Sub of #12186. The bucketing logic fixes — #12188 (fetch-all-releases) + #12184 (milestone semver-guard), merged via #12192 — are **forward-only**: they correct how *new* closures bucket, but do **not** move the existing pile-up.

## The Problem (verified)

`IssueSyncer.mjs:586-606` (sealed-chunk enforcement) forces any already-archived issue whose recomputed bucket differs from its current path to stay put:

```js
} else if (wasArchived && targetPath !== oldAbsolutePath) {
    logger.warn(`[SEALED CHUNK ENFORCEMENT] ... Forcing retention at ${oldAbsolutePath}.`);
    targetPath = oldAbsolutePath;
}
```

A clean-slate re-sync (`lastSync === null`) still seeds from existing `metadata.issues` (`:562 {...metadata.issues}`), so `oldIssue` exists → sealed. **Result: a re-sync will NOT move the existing 4,133 v8.1.0 issues** (56.9% of the archive). The portal tickets-view stays garbage until a deliberate one-time migration runs. (The epic's "after re-sync, no v8.1.0 catch-all" AC silently assumed a re-sync clears it — it doesn't.)

## Approaches

- **A — Regenerate (ADR-canonical):** clear `metadata.issues` + delete `resources/content/archive/{issues,pulls,discussions}/` → clean-slate re-sync re-fetches + buckets fresh (no sealed conflict). Matches ADR-0004's regeneratable-cache model. Destructive (deletes committed content) + heavy full GitHub re-fetch (now rate-limit-hardened by #12193).
- **B — Non-destructive migration:** a one-time sealed-chunk override that re-buckets the existing archive into correct folders, ideally **metadata-driven** (local file moves using the new bucketing logic + full `sortedReleases`; no full GitHub re-fetch). No delete; controlled + reviewable; more code than A.

## Operational notes

- `sync_all` runs on `dev` only.
- Either approach produces a **large content diff** (4,133+ files move buckets).
- The follow-on portal regen needs a **concurrency cap** first — the un-capped SSR fan-out melted the host at load 117 on 18 cores (middleware-v2 `build.mjs`).

## Status

**Deferred** (operator 2026-05-30: finish #12191 first; schedule this migration deliberately for the large diff + regen).

## Acceptance Criteria

- [ ] After the migration, `resources/content/archive/issues/` has no v8.1.0 catch-all; the 4,133 mis-bucketed issues distribute to their true historical releases.
- [ ] No data loss — issue content preserved; only bucket paths change.
- [ ] Portal News → Tickets shows clean release groups.
- [ ] `_index.json` + `tickets.json` regenerated consistently.

## Related

- #12186 (epic), #12188 / #12184 (forward-only logic fixes), #12193 (rate-limit hardening — de-risks approach A's re-fetch).

Retrieval Hint: grep `SEALED CHUNK` in `ai/services/github-workflow/sync/IssueSyncer.mjs`.

## Timeline

- 2026-05-30T06:16:18Z @neo-opus-4-7 added the `bug` label
- 2026-05-30T06:16:18Z @neo-opus-4-7 added the `ai` label
- 2026-05-30T06:16:18Z @neo-opus-4-7 added the `architecture` label
- 2026-05-30T06:16:44Z @neo-opus-4-7 added parent issue #12186
- 2026-05-30T06:36:18Z @neo-opus-4-7 cross-referenced by #12186
- 2026-05-30T06:40:33Z @neo-opus-4-7 assigned to @neo-opus-4-7
- 2026-05-30T07:01:39Z @neo-opus-4-7 cross-referenced by PR #12196
- 2026-05-30T13:06:39Z @tobiu referenced in commit `b9e2d48` - "feat(github-workflow): one-time non-destructive archive re-bucket migration (#12194) (#12196)

The merged bucketing fixes (#12188/#12184) are forward-only: sealed-chunk + oldVersion precedence pin the existing ~4133 v8.1.0 issues, so a re-sync will not clear the tickets-view pile-up. This adds the tool to do it: IssueSyncer.migrateArchiveBuckets recomputes every issue version from the full release history with oldVersion ignored (new #planBuckets ignoreOldVersion option), relocates mis-bucketed .md files via a two-phase staged move (no delete, no GitHub re-fetch), rewrites the issues _index.json, and prunes emptied dirs. A SyncService facade + ai/scripts/migrations/rebucketArchive.mjs (with --dry-run) provide the operator-run entry. Fails loud if no releases load. Scope: issues (the dominant pile-up). Execution is operator-gated.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-30T13:06:39Z @tobiu closed this issue
- 2026-05-30T15:15:00Z @neo-opus-4-7 referenced in commit `615e91b` - "fix(github-workflow): re-bucket 4144 archived issues to correct release folders (#12194)

Executes the #12196 migration tool against the live archive. Relocates 4144 mis-bucketed
closed-issue markdown files out of the v8.1.0 catch-all - a floor artifact from before
full-release-history fetching, where pre-window closed issues collapsed into the oldest
in-window release - into their correct release folders, resolved from the full release
history with oldVersion ignored.

Result: v8.1.0 drops from 4133 to its true 24; the largest resulting bucket is v12.0.0 at
413 (no catch-all remains); 4628 issues unchanged. Git detected all 4144 moves as renames
(content byte-preserved). Issue metadata paths and the issues _index.json updated to match.

Co-Authored-By: neo-opus-4-7 <neo-opus-4-7@neomjs.com>"
- 2026-05-30T15:15:02Z @neo-opus-4-7 cross-referenced by PR #12201
- 2026-05-30T15:22:02Z @tobiu referenced in commit `e20c9b5` - "fix(github-workflow): re-bucket 4144 archived issues to correct release folders (#12194) (#12201)

Executes the #12196 migration tool against the live archive. Relocates 4144 mis-bucketed
closed-issue markdown files out of the v8.1.0 catch-all - a floor artifact from before
full-release-history fetching, where pre-window closed issues collapsed into the oldest
in-window release - into their correct release folders, resolved from the full release
history with oldVersion ignored.

Result: v8.1.0 drops from 4133 to its true 24; the largest resulting bucket is v12.0.0 at
413 (no catch-all remains); 4628 issues unchanged. Git detected all 4144 moves as renames
(content byte-preserved). Issue metadata paths and the issues _index.json updated to match.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"

