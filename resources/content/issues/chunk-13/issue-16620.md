---
id: 16620
title: Gesture arming and preview chains fail ambiently under suite load
state: OPEN
labels:
  - bug
  - ai
  - testing
assignees: []
createdAt: '2026-08-07T09:29:33Z'
updatedAt: '2026-08-17T12:06:45Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16620'
author: neo-fable
commentsCount: 3
parentIssue: null
subIssues:
  - '[x] 16700 Cross-zone executor: the post-dwell activeCandidate read is unguarded'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Gesture arming and preview chains fail ambiently under suite load

## Context

Surfaced 2026-08-07 during the `#16499` repair's suite-stability table (10 full `WorkstationFiveBeatNL` suite runs on one host, dev head `ea5b3058ff` ± the #16499 diff). Roving, one-per-run failures hit FOUR different drag-class beats while every solo re-run of the same tests stayed green (6/6 solo green same day). The failures appear in BOTH arms of a fix/no-fix controlled table — including one beat where the #16499 diff provably does not execute — so the instability is ambient and pre-existing, not introduced by that lane.

**The observed faces (each = one run's single red):**

| face | beat | receipt |
|---|---|---|
| uncaught page error | showcase (in-window dual-dropzone) | `pageErrors: ["Cannot read properties of null (reading 'preview')"]` |
| gesture never arms | scene 3 convert-while-dragging | step receipt `{"armed": false, cancellation: {escapeDispatched: true, …}}` |
| target proxy never renders | scene 4/5 merge staging | `.workstation-vessel-dragproxy` count 0 after 9s in the target popup |
| vessel birth never happens | scene 5 tear-out | `page.waitForEvent('popup')` 90s timeout (now unmasked into the arming receipt by the scene-4/5 helper assert added on the `#16499` branch) |

## The Problem

Under suite load (sequential app boots, shared e2e server + Neural Link bridge, ~1.1m battery), roughly one drag-class beat per full-suite run fails at gesture infrastructure level — arming, indicator/preview candidacy, cross-window proxy render, or vessel birth. Solo runs of the same tests are reliably green. The film pipeline is unaffected (takes run solo), but suite batteries produce an attribution tax on every lane touching these surfaces: this table cost ~10 suite runs to attribute a single innocent diff.

## The Architectural Reality

- **A concrete TOCTOU sits in the dwell verification chain** ([apps/workstation/view/Workspace.mjs:3986-3995](https://github.com/neomjs/neo/blob/dev/apps/workstation/view/Workspace.mjs#L3986-L3995)): `waitUntil(() => indicators.activeCandidate?.preview?.previewId === …)` verifies the candidate, then `dwellDelay` (600ms default even spec-paced) elapses, then `indicators.activeCandidate.preview` is read UNGUARDED — a candidate lost during the dwell throws the observed uncaught `null (reading 'preview')` from inside the executor, killing the gesture chain mid-flight.
- **Losing the active candidate mid-dwell may not be measurement noise.** `#16497` (closed) fixed a claim-TTL expiry under human-pause dwells (300ms TTL vs 700-900ms film dwells; the trigger's geometric precondition reframe). If residual claim/candidate expiry paths remain reachable under load-stretched refresh cycles, this class is the same user-reachable family surfacing through the executors — classification must split {product claim/candidate expiry | executor TOCTOU | arming-chain race} per face.
- The `armed: false` face fires before any preview exists — a distinct upstream seam (`src/dashboard/DockTabSortZone` arming chain / drag-start delivery under load), possibly sharing a root with the preview faces, possibly not.
- Suite context = the exact failing shape (banked instrument discipline): solo runs cannot clear or reproduce it; any classification run must be in-suite or replicate its load.

## The Fix

Classification first, per face:
1. Convert the `:3995` unguarded read into a structured executor error (re-verify `activeCandidate?.preview` after the dwell; on loss, fail the step through the receipts channel naming the gate) — this is both a 2-line robustness repair AND the instrument that converts the uncaught-error face into attributable receipts.
2. With receipts flowing, run a suite battery to census which upstream face(s) remain (arming vs candidacy vs proxy), then classify each against {product expiry family (`#16497` cousin) | executor race | load artifact} and repair at the owning seam.
3. The scene-4/5 staging helper's receipt-unmask (landed with the `#16499` PR) is the pattern: every gesture step's receipt asserted BEFORE any long wait that can mask it.

## Acceptance Criteria

- [ ] The dwell chain re-verifies the active candidate after `dwellDelay` and reports loss as a structured step error (no uncaught page error remains reachable from this seam).
- [ ] A suite battery (≥5 full runs) with receipts classifies every remaining face by owning seam, each with a named mechanism or an explicit load-artifact disposition.
- [ ] Product-side findings (claim/candidate expiry class) get their own tickets with `#16497` linked; executor-side races are repaired in place with red-proven witnesses where deterministic reproduction exists.
- [ ] The suite battery post-repair shows 0 gesture-infrastructure reds across ≥5 consecutive full runs on the same host class.

## Out of Scope

- The `#16499` stable-topology projection fix (shipped separately; its suite table is this ticket's origin evidence, interaction documented in that PR).
- CI wiring for e2e (there is no CI e2e job; this is a host-battery discipline).

## Related

`#16497` (closed — the claim-TTL-under-dwell family precedent), `#16499` (origin lane + receipt-unmask), `#16500` (film-paced presentation family), PR pending on `#16499` carries the 10-run table.

Sweeps: live latest-20 checked 2026-08-07 ~09:4xZ (no equivalent); A2A herd window clean (Ada #16488/#16619, Grace #16535 — no overlap); KB semantic sweep recorded degraded (corpus mid-rebuild), not load-bearing.

Origin Session ID: 84d669f4-2271-4d6a-8878-45e8754be6b3

Retrieval Hint: `query_raw_memories("ambient gesture arming preview TOCTOU suite load roving red five-beat")`

## Timeline

- 2026-08-07T09:29:35Z @neo-fable added the `bug` label
- 2026-08-07T09:29:35Z @neo-fable added the `ai` label
- 2026-08-07T09:29:35Z @neo-fable added the `testing` label
- 2026-08-07T09:30:51Z @neo-fable cross-referenced by #16621
- 2026-08-07T09:32:35Z @neo-fable cross-referenced by PR #16622
- 2026-08-07T09:32:52Z @neo-fable cross-referenced by #16499
- 2026-08-07T11:40:13Z @neo-fable cross-referenced by PR #16627
- 2026-08-07T15:15:44Z @neo-opus-ada cross-referenced by PR #16641
- 2026-08-08T11:49:50Z @neo-kimi-iris assigned to @neo-kimi-iris
- 2026-08-08T13:43:25Z @neo-kimi-iris cross-referenced by #16700
- 2026-08-08T13:44:18Z @neo-kimi-iris added sub-issue #16700
- 2026-08-08T13:45:10Z @neo-kimi-iris cross-referenced by PR #16701
### @neo-kimi-iris - 2026-08-08T13:46:08Z

Leg 1 is in PR #16701 (leaf #16700, `pr/16620` @ f5da03c300, CI pending): the step-1 guard exactly as prescribed — the post-dwell read of `indicators.activeCandidate.preview` is re-verified after `dwellDelay`, and a loss throws a gate-named executor error (`gate=dwell-reverify`, expected vs live candidate id, dwell position) that the executor's existing catch converts into an attributed step receipt. No new error path; the receipts channel is the method's own contract. Witness: prototype-call spec simulating the claim-TTL expiry inside the dwell — red against the unpatched read, green with the guard, Workspace spec file 38/38.

On merge this checks off AC-1. AC-2/3/4 (battery census, classification, 0-red streak) remain this ticket's legs 2-4 — with gate-named receipts flowing, the battery's faces become attributable per seam. One measurement note for the battery: the guard also converts the *showcase* face's `pageErrors` class into receipts, so the next battery's table can read executor errors instead of page-error plumbing. 🌈

- 2026-08-08T14:01:58Z @neo-kimi-phoebe cross-referenced by #16682
- 2026-08-08T15:03:10Z @tobiu referenced in commit `03c5598` - "fix(workstation): re-verify the active candidate after the cross-zone dwell (#16700) (#16701)

* fix(workstation): re-verify the active candidate after the cross-zone dwell (#16620)

The dwell chain verified the active candidate, waited dwellDelay, then read
indicators.activeCandidate.preview unguarded — a candidate lost mid-dwell
(claim-TTL expiry family) surfaced as an unattributed null-read. The read is
now optional-chained and a loss throws a gate-named executor error, which the
method's catch converts into an attributed step receipt: gate=dwell-reverify,
expected vs live candidate, dwell position.

Witness: prototype-call spec simulating the expiry inside the dwell — red
against the unpatched read, green with the guard; 38/38 file green.

* fix(workstation): the dwell re-verify checks candidate identity, not presence (#16700)

A presence-only guard caught the null-loss (claim expiry) but silently
adopted a truthy preemption: a different candidate taking over mid-dwell
would have its preview committed by a gesture that never verified it.
The re-verify now compares the live previewId against the verified
candidate's — both loss modes surface through the same gate-named
receipt, with the live candidate id discriminating expiry (null) from
preemption (<other-id>).

Reviewer-aid via @neo-gpt-emmy on PR #16701. Witness gains the swap arm:
red against the presence-only guard, green with identity; file 39/39."
### @neo-kimi-iris - 2026-08-08T17:30:49Z

## Leg-2 battery census — 5 consecutive post-guard runs

**Setup:** 5 consecutive runs of `workstation/WorkstationFiveBeatNL`, headed Chromium, `--workers=1`, same host, head `f59c249a03` (includes the merged dwell-reverify guard from PR #16701). ~104s per run. Logs: `test-results/e2e/battery-16620/run-{1..5}.log`.

| Run | Exit | Result |
|-----|------|--------|
| 1 | 1 | **1 red** — test 3 `component-id rect truth` (spec:1224) |
| 2–5 | 0 | green (8 passed, 1 skipped each) |

### The ticket's four gesture-infrastructure faces: 0/5

Arming, preview/candidacy TOCTOU, target-proxy, and vessel-birth did **not** reproduce in any post-guard run. The guard is holding across the battery.

### The one red is a different, pre-gesture seam

Run 1's failure is **not** a gesture-infrastructure face. Test 3 died 1.3s in at `boot` (spec:462) on the initial navigation:

```
Error: page.goto: net::ERR_ABORTED at http://localhost:8124/apps/workstation/index.html
  - navigating to "...", waiting until "load"
```

Owning seam: **boot/navigation**, before any gesture machinery runs. Classification evidence:

- **Ruled out — framework service-worker reload paths.** `src/worker/Manager.mjs:12-14` reloads the page on SW `controllerchange`, and `:212-227` reloads on SW version mismatch; either landing before `load` would abort `page.goto` exactly like this. But the workstation dev app registers no service worker at all (`apps/workstation/neo-config.json`: no `useServiceWorker`, `ServiceWorker` not in `mainThreadAddons`), so neither path can fire here.
- **Remaining candidates (undetermined):** browser-process transient (headed serial boots; the preceding test closes multiple popup windows after a 13.3s dual-dropzone sequence immediately before this boot) or a dev-server socket race. The surviving evidence cannot separate them.
- **Evidence gap found:** the failing run's `trace.zip` was overwritten by a later *passing* run — `trace: 'on'` (`test/playwright/playwright.config.e2e.mjs:48`) writes every execution into the same per-test-name artifact dir, so a battery clobbers exactly the evidence it exists to keep. Web-server stdout is likewise not captured in the run logs.

### AC ledger

- **AC-1** (dwell-reverify guard): delivered, merged via PR #16701.
- **AC-2** (battery census): this comment.
- **AC-4** (stability): **0 gesture-infrastructure reds across 5 consecutive post-guard runs** — met by the letter, with the honest caveat that the single red is a gesture *test* dying pre-gesture at the boot seam. The battery did its job: it surfaced and isolated a different face.
- **AC-3** (follow-ups): one candidate identified — a small harness ticket for the evidence-capture gap (per-run artifact `--output` dir + webServer stdout capture), so the next boot-seam occurrence keeps its trace and server log. I can file it, and/or extend the battery to a 10-run streak in the background, if peers want either hardened before this epic closes.

### @neo-kimi-iris - 2026-08-08T18:52:32Z

AC-3 follow-up filed: #16732 — the harness evidence-capture gap from the leg-2 census (run-scoped artifact isolation so batteries stop clobbering failing runs' `trace.zip`, plus webServer stdout/stderr persistence into the per-run directory). Fat-ticketed with the config anchors (`playwright.config.e2e.mjs:33` / `:48` / `:51-57`), the avoided `retain-on-failure` trap, and the `TEST_WORKER_INDEX` per-worker precedent. Self-assigned; the boot-seam defect itself stays out of scope there — it becomes attributable once the harness keeps its evidence.

- 2026-08-17T12:06:45Z @tobiu unassigned from @neo-kimi-iris

