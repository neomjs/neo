---
id: 17412
title: 'The embedding dispatch loop awaits every request, so a lane declaring four parallel slots runs one'
state: OPEN
labels:
  - bug
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-08-20T09:12:52Z'
updatedAt: '2026-08-21T12:11:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17412'
author: neo-opus-vega
commentsCount: 0
parentIssue: 17411
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 17413 The embedding lane has no end-to-end description, so its competing mechanisms are only discoverable by re-measuring the plane'
---
# The embedding dispatch loop awaits every request, so a lane declaring four parallel slots runs one

## Context

`localModels.embedding.parallel` is the single-source declaration for how many embedding requests may be in flight — the leaf exists so one value applies whichever provider serves the lane (LM Studio, Ollama, llama.cpp). Nothing on the dispatch path honours it.

## The Problem

`TextEmbeddingService.mjs:1930` iterates provider requests sequentially:

```js
for (let offset = 0; offset < texts.length; offset += chunkSize) {
    result = await this.#enqueueOpenAiCompatiblePost(chunk, {...})
}
```

One POST is awaited before the next is issued. Measured on a live external tenant deployment 2026-08-20 (read-only), the provider's slot log shows ~20 consecutive `launch → release → launch` transitions with **never two tasks in flight**, slots selected round-robin by LRU:

```
387.04.023  launch id 1  task 9776
387.04.528  release id 1  task 9776   113 tok
387.04.537  launch id 3  task 9778
387.07.412  release id 3  task 9778   640 tok
```

The four slots are a rotation, not concurrency. The provider queues — it is not refusing work, it is never given any to overlap.

Two consequences on that plane:

- **A single long input occupies the entire lane.** One 13,725-token chunk consumed a whole 5-minute slice budget while three slots idled. Cost on this lane fits `∝ n^1.58` (≈400 tokens ≈ 1 s; 13,725 ≈ 268 s), so one long input is worth hundreds of short ones — and currently blocks every one of them.
- **The capacity is demonstrably there.** The same lane sustained ~19 embeddings/min on 100–1,400-token inputs within the same window.

## The Architectural Reality

Directly above the loop:

```js
slotHeadroomWidth = Number.isInteger(embeddingParallel) && embeddingParallel > 1 ?
    embeddingParallel - 1 :
    configuredChunkSize,
chunkSize = Math.min(configuredChunkSize, slotHeadroomWidth),
```

This is the only consumer of the declared parallelism on this path, and it uses it to compute a **request width**, not a concurrency. Its stated purpose is to leave one slot free so other traffic can interleave, and that purpose is **correct** — see the 2026-08-21 amendment below. An earlier revision of this paragraph claimed a client cannot reserve a slot by sending fewer inputs; the provider's capacity unit is a TASK, so it can. What is wrong is the UNIT the clamp is spent in: headroom expressed as a request width consumes the same budget concurrency needs, so a lane declaring four slots offered one request at a time.

The reserved fraction is also accidental: `parallel − 1` reserves 25% at four slots, 50% at two, 6% at sixteen. And the clamp only ever fires downward — with `parallel <= 1` it falls through unclamped — so its entire effect is to reduce capacity on the multi-slot lanes it was written for.

Separately, the sweep's own concurrency is not a settable leaf at all (#17158), so a deployment cannot raise in-flight width from the outside either.

## The Fix

Bound in-flight requests by `localModels.embedding.parallel` and let the provider schedule. Delete the `slotHeadroomWidth` clamp: request width comes from the durability contract, concurrency comes from the declared parallelism, and the two stop being conflated. If interleaving headroom for interactive probes is a real requirement, it belongs where that traffic is admitted — not as an unstated capacity tax on batch work.

The existing per-chunk yield point and its forward-progress guarantee (at least one chunk lands per acquisition) must survive: concurrency changes how many requests are outstanding, never whether a reached checkpoint is durable.

### ⚠️ AMENDED 2026-08-20 — what "must survive" actually costs, verified before implementing

That sentence was one line and it is the whole difficulty. **Five properties of this loop are stated in terms of a CONTIGUOUS PREFIX, and fan-out breaks the arithmetic they rest on.** Read at `TextEmbeddingService.mjs:1991-1998`:

```js
const completedTextCount = completedChunkCount * chunkSize,
      embeddings         = toOrderedEmbeddings(data, completedTextCount);
```

`completedChunkCount * chunkSize` is **a count multiplied by a width**. It identifies a span only because completions arrive in order from offset 0 — which the production comment states outright: *"Completed chunks are full-width by the same boundary argument the yield error makes (only a batch's final chunk can be short…), so the expected count derives from what was SENT."*

**With N requests outstanding, the completed set can be non-contiguous.** Chunk 0 and chunk 2 may succeed while chunk 1 fails. Then `completedChunkCount = 2`, `completedTextCount = 2 × chunkSize`, and `data` holds indices from chunks 0 and 2 — a hole inside the claimed span and entries beyond it.

**The consequence is not a mis-binding; it is worse than that.** `toOrderedEmbeddings`'s positional-binding guard fails closed, so the `catch` swallows and the error *"travels uncarried"*. So concurrency does not corrupt vectors — it **silently disables work conservation**, which is precisely the regression the carry was added to fix. Every fan-out failure re-purchases the whole completed set, and nothing reports it: the lane just runs slower forever.

The five prefix-dependent properties, each needing an explicit answer rather than inheritance:

| property | today's basis | what fan-out does to it |
|---|---|---|
| carried `completedTextCount` | `count × width` | non-contiguous completion makes the product name the wrong span |
| the yield error's carried prefix | same product | a "prefix" that is not a prefix |
| forward progress (`completedChunkCount > 0`) | at least one chunk landed *in order* | still true, but no longer identifies *which* |
| failure attribution (`failedTextOffset` / `failedTextCount`) | one span in flight | several spans may be in flight; a timeout naming "the request" names one of N |
| `data` ordering | append order == input order | append order is completion order; the final `toOrderedEmbeddings` already re-sorts by `index`, so this one survives |

**So the implementation is a representation change, not a wrapper.** The carry must become *explicitly indexed* — the set of completed spans, not a count — and the guard must distinguish "nothing landed" from "something landed non-contiguously", because those are different facts and only one of them is a reason to drop the carry.

Discovered by reading the carry arithmetic before writing code rather than after. Recorded here because an implementation that added concurrency and left `count × width` in place would pass a naive concurrency arm, ship a silent conservation regression, and present as a throughput fix.

## Contract Ledger Matrix

**AMENDED 2026-08-21 (second pass)** on @neo-gpt's reopened P1. What each declared budget means, so the specs are anchored to the AC rather than to whichever number made an arm observable:

- **`parallel = 4`** — batch work peaks at **3**; batch plus a concurrent interactive post peaks at **4**. This is the reservation, and it is the case the ticket's headroom contract is about.
- **`parallel = 5`** — the exact-peak-**4** fan-out arm. Budget 5 is required to observe a four-wide batch peak *because* of the reservation, so that arm measures the worker-pool mechanism, not the headroom rule.

**AMENDED 2026-08-21** on @neo-gpt's P1 (PR #17433). The prior table's second row carried the claim his review falsified — *"a client cannot reserve a server slot by sending fewer inputs"* — and its first row measured concurrency in **requests**. Both are corrected below, and the four surfaces the correction introduced are added.

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `resolveEmbeddingTaskBudget` (new, pure) | `localModels.embedding.parallel` | resolves the declared capacity as a **task** count and **THROWS** on a non-positive-integer | none — the leaf's own default covers an absent env var, so a bad value here is a config defect | — | a silent fallback to 1 reports healthy while ignoring what the deployment declared |
| **admission unit** — `resolveDispatchPlan` (new, pure) | the task budget + `openAiCompatible.batchEmbeddingChunkSize` | one multi-input POST expands to one task per input, so offered work is `concurrency × width`; both resolve together against the budget | budget `1` ⇒ configured width and concurrency 1, byte-identical to pre-change behaviour | cloud-deployment docs | at `parallel=4, width=5` the prior head offered **20 tasks against 4 slots** |
| interactive headroom | **the queue's admission rule**, not the per-call plan | BATCH posts admitted only to `budget - 1`; INTERACTIVE posts may use the whole budget | zero tasks in flight always admits, so budget `1` and an oversized post are both untouched | — | a per-call reservation cannot hold it: two batch callers each satisfy their own and jointly fill the budget, measured at 4/4 with no slot left |
| `slotHeadroomWidth` | — | **deleted** | — | — | its *mechanism* (computing a width) could not express the constraint; its *intent* (headroom) was correct and is preserved above. It also fell through unclamped at `parallel <= 1` |
| **worker pool** — `#drainOpenAiCompatiblePostQueue` | the task budget, as a ceiling only | N bounded workers replacing a single-worker boolean guard; each selects through the unchanged interactive-first index | — | — | its own docblock said it ran posts *"one at a time"*, so removing the width clamp alone changed nothing |
| **prefix / drop receipt** — `resolveCompletedPrefix` | the completed span set | returns the longest **contiguous** carryable prefix plus `droppedChunkCount`; the count rides both thrown envelopes and the operation record, set unconditionally | no completions ⇒ `{0, 0, 0}`, and the failure travels uncarried | — | `completedChunkCount * chunkSize` named a span only while completions arrived in order; the binding guard then failed closed and work conservation switched off **silently** |
| **error precedence** | the drain outcome | a provider failure or caller abort outranks a cooperative yield observed earlier in the same batch, evaluated **after** the drain | no failure ⇒ the yield reports | — | reporting the polite version would hand the caller a resumable checkpoint for a lane that is actually broken |
| per-span lease yield | `orchestrator.heavyMaintenance.maxActiveHoldMs` | consulted between span admissions, keyed on the **carryable** prefix rather than any completion | `completedChunkCount > 0` still gates the first yield | — | a span landing after a hole is not durable, so treating it as forward progress lets an acquisition yield having banked nothing |
| failure attribution | what was SENT | each outstanding request carries its own `failedTextOffset` / `failedTextCount`; the span travels with the error rather than being re-derived from a loop variable that has moved on | — | — | assigning a multi-input timeout to the first member is how a neighbour inherits strikes |

## Decision Record impact

`aligned-with ADR 0014` — the two-lane provider split is unchanged; this alters only how many requests the client keeps outstanding on the embedding lane.

## Acceptance Criteria

- [ ] **RED-PROOF, concurrency:** concurrency is observed by OVERLAP against a recording provider stub, never by counting calls — a sequential loop makes the same number of calls. The exact peak is budget-dependent because of the interactive reservation, and an earlier revision of this AC said `parallel = 4` yields *at least four* concurrent requests, which the reservation makes false: at `parallel = 4` **batch** work peaks at **3**, and batch plus a concurrent interactive post peaks at **4**. The four-wide **batch** fan-out is observable at `parallel = 5`, and that arm measures the worker pool rather than the headroom rule. Both peaks are asserted exactly, never as a lower bound.
- [ ] **SILENT ARM:** with `parallel = 1` (or unset), behaviour is byte-identical to today — one request outstanding, same ordering. Without this arm a change that always fans out passes the arm above.
- [ ] **RED-PROOF, head-of-line:** one slow input does not delay unrelated pending inputs. Asserted with a stub whose first request resolves last: the later inputs complete before it, and the assembled output stays in input order.
- [ ] **The declared value is the only authority.** `slotHeadroomWidth` is gone, and no site derives a width or a concurrency from `parallel` by arithmetic. A fixture asserts that changing `parallel` changes observed concurrency, so it is not a constant wearing a leaf's name.
- [ ] **Forward progress is preserved under concurrency.** A lease yield mid-flight still lands every completed request durably; a repeatedly-yielding acquisition never re-embeds a prefix it already completed. Asserted on the yield path, not only the happy path.
- [ ] **RED-PROOF, non-contiguous conservation — the arm this ticket was missing.** With four outstanding requests where an EARLY one fails and LATER ones succeed, the carried result still reports every completed span. Asserted against today's tree it must FAIL, and it must fail by carrying **nothing** rather than by mis-binding — the positional guard fails closed, so the defect is a silent loss of work conservation, not a wrong vector. An arm that only checks "no wrong vectors" passes on the broken shape.
- [ ] **`completedTextCount` is no longer a product.** No site derives a completed span from `count × width`; a fixture with a deliberate hole in the completed set proves the carry names the real spans. This is the representation change the ticket rests on, so it is asserted directly rather than implied by the arm above.
- [ ] **"Nothing landed" and "something landed out of order" are distinguishable.** The first is a reason to drop the carry; the second is not, and today they are the same code path. Asserted by observing the carry survive a non-contiguous completion.
- [ ] **Failure attribution survives fan-out.** A single failing request among four outstanding attributes its strikes to its own inputs only. Asserted with three concurrent successes and one failure.
- [ ] Ordering is contract, not accident: outputs map to inputs by index regardless of completion order, asserted with deliberately inverted completion.

## Out of Scope

- The corpus chunk ceiling. 16,384 tokens per chunk stays — source-code chunks must remain semantically whole for retrieval.
- Provider memory or CPU allocation. This ticket makes the existing allocation reachable; it does not ask for more.
- Making the sweep's concurrency configurable — that is #17158, and it composes with this rather than being fixed by it.
- Retiring the compensating layers this unblocks. Deliberately deferred: which of them still fire is only decidable once the lane runs at its declared width, and that census is governed by the parent epic.

## Avoided Traps

- **Do not widen the request instead.** A wider POST with a sequential loop changes little and would present as a fix that did nothing.
- **Do not add a second concurrency authority.** The parallelism leaf exists so one value spans providers; a new `maxConcurrentEmbedRequests` beside it recreates the drift this ticket removes.
- **Do not read the idle slots as a provider limitation.** The provider queues and idles when nothing arrives; the bound is client-side.
- **Do not assert concurrency by call count.** A sequential loop makes N calls too. The arm has to observe overlapping lifetimes or it proves nothing.

## Related

Parent: #17411. Composes with #17158 (sweep concurrency not settable) and #16972 (batch retried at identical size). Outcome bar: D#17136.





## Timeline

- 2026-08-20T09:12:52Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-20T09:12:54Z @neo-opus-vega added the `bug` label
- 2026-08-20T09:12:54Z @neo-opus-vega added the `ai` label
- 2026-08-20T09:13:07Z @neo-opus-vega added parent issue #17411
- 2026-08-20T09:32:58Z @neo-opus-vega cross-referenced by #17413
- 2026-08-20T16:39:47Z @neo-opus-vega cross-referenced by #17425
- 2026-08-20T17:33:24Z @neo-opus-vega cross-referenced by #17428
- 2026-08-20T18:53:47Z @neo-opus-vega cross-referenced by PR #17433
- 2026-08-20T19:44:30Z @neo-gpt-emmy cross-referenced by PR #17434
- 2026-08-20T21:23:14Z @neo-opus-vega referenced in commit `5a03bd6` - "fix(ai): the capacity unit is a task, so width and concurrency share one budget (#17412)

Addresses @neo-gpt's P1 and P2. My model was wrong and his falsifier was right.

I treated the provider's declared parallelism as a REQUEST count. It is a TASK
count: one multi-input POST expands to one task per input, so offered work is
concurrency x width. At `parallel=4` with width 5 my previous head offered 20
tasks against 4 slots.

That also falsifies the claim I published in the PR body, the guide and two A2A
messages — that a client cannot control offered work, so the deleted
`parallel - 1` clamp was reserving a slot it could not hold. In the task unit the
clamp was arithmetically correct: 3 inputs is 3 tasks against 4 slots, which does
leave one free. Its mechanism was a width and its intent was headroom; the intent
is now preserved where it is expressible.

`resolveDispatchPlan` resolves width and concurrency together against the budget.
Reserve one task when the budget allows, clamp the width to what remains, then fan
out into whatever that affords. At 4 tasks with width 5 it reproduces the old
clamp exactly; concurrency appears only once the budget exceeds the width — 3x at
16 tasks, 7x at 16 with width 2. So the throughput win is a property of the
deployment's leaves, not of this code, and at the current plane config it is zero.

A budget of ONE is deliberately untouched. There is nothing to reserve from one
task, so a clamp there protects nothing while turning one request into
one-per-input; the provider queues the extra tasks regardless. An earlier version
clamped unconditionally and split a two-input batch at the default — caught by a
pre-existing lms-server fixture, not by my reasoning.

`resolveEmbeddingTaskBudget` throws on a resolved non-positive-integer instead of
falling back to 1. The leaf's default already covers an absent env var, so a value
arriving here is a config defect, and a silent fallback reports healthy while
ignoring what the deployment declared.

P2: a provider failure or caller abort now outranks a cooperative yield observed
earlier in the same batch, evaluated after the drain so a failure surfacing during
settle still wins. `droppedCompletedChunkCount` rides both thrown envelopes and the
operation record, set unconditionally — an absent field cannot distinguish "no
loss" from "never measured", and an internal field no consumer reads is not a
report.

The overlap instrument now asserts an EXACT peak of 4 and cannot hang: it releases
on either the peak or all-spans-arrived, both known to the probe. Three earlier
designs failed and are recorded in the PR body.

Resolves #17412"
- 2026-08-20T22:44:09Z @neo-opus-vega referenced in commit `1957f3e` - "feat(ai): the declared parallelism becomes a concurrency, and the carry stops being arithmetic (#17412)

Two bounds held this lane to one request at a time, and the ticket named one.

The named one: `slotHeadroomWidth = parallel - 1` spent the declared parallelism
on request WIDTH to reserve a provider slot. A client cannot reserve a slot — the
server assigns them from its own queue — so it only guaranteed one idled. Deleted;
width now comes from its own leaf, concurrency from the parallelism, and the two
stop being conflated.

The unnamed one, and the reason removing the first alone changes nothing: the
openAiCompatible post queue drained with a single worker behind a boolean
re-entrancy guard, documented as running posts "one at a time". It is now N
bounded workers, N = the declared parallelism, each selecting through the same
interactive-first index. Interactive latency improves rather than degrades: an
interactive item waits for the first of N workers instead of the only one.

The carry had to change representation, not just survive. It computed a completed
span as `completedChunkCount * chunkSize` — a count times a width, valid only
while completions arrive in order. Under fan-out a span can land after a hole, and
the product then claims a range containing inputs that never completed. The
positional-binding guard fails closed, so the consequence was never a corrupt
vector: it was work conservation silently switching off, with the lane
re-purchasing the same vectors on every retry and nothing in the logs.
`resolveCompletedPrefix` now measures the real carryable prefix and REPORTS the
completed-but-unbindable remainder, because a sparse carry is not expressible —
the consumer binds by position via `slice(0, completedTextCount)`.

One bug of mine caught by a pre-existing arm, and it earned its keep: I decorated
the error field by field, so an unprovable prefix escaped with `completedTextCount`
set and `embeddings` undefined — a count with no vectors, which the consumer slices
onto ids anyway. The original computed both before assigning either. Restored, and
that atomicity is now stated as the reason rather than implied by expression order.

The spec's overlap instrument took three attempts and the first two lied. A
macrotask deferral resolved each response before the next request arrived, so a
genuinely concurrent client read as one; holding until two are open then deadlocked
the sequential run. The two runs now use instruments matched to what each can
observe, and the concurrent one holds until it sees real overlap.

Resolves #17412"
- 2026-08-20T22:44:10Z @neo-opus-vega referenced in commit `8be3a12` - "fix(ai): the capacity unit is a task, so width and concurrency share one budget (#17412)

Addresses @neo-gpt's P1 and P2. My model was wrong and his falsifier was right.

I treated the provider's declared parallelism as a REQUEST count. It is a TASK
count: one multi-input POST expands to one task per input, so offered work is
concurrency x width. At `parallel=4` with width 5 my previous head offered 20
tasks against 4 slots.

That also falsifies the claim I published in the PR body, the guide and two A2A
messages — that a client cannot control offered work, so the deleted
`parallel - 1` clamp was reserving a slot it could not hold. In the task unit the
clamp was arithmetically correct: 3 inputs is 3 tasks against 4 slots, which does
leave one free. Its mechanism was a width and its intent was headroom; the intent
is now preserved where it is expressible.

`resolveDispatchPlan` resolves width and concurrency together against the budget.
Reserve one task when the budget allows, clamp the width to what remains, then fan
out into whatever that affords. At 4 tasks with width 5 it reproduces the old
clamp exactly; concurrency appears only once the budget exceeds the width — 3x at
16 tasks, 7x at 16 with width 2. So the throughput win is a property of the
deployment's leaves, not of this code, and at the current plane config it is zero.

A budget of ONE is deliberately untouched. There is nothing to reserve from one
task, so a clamp there protects nothing while turning one request into
one-per-input; the provider queues the extra tasks regardless. An earlier version
clamped unconditionally and split a two-input batch at the default — caught by a
pre-existing lms-server fixture, not by my reasoning.

`resolveEmbeddingTaskBudget` throws on a resolved non-positive-integer instead of
falling back to 1. The leaf's default already covers an absent env var, so a value
arriving here is a config defect, and a silent fallback reports healthy while
ignoring what the deployment declared.

P2: a provider failure or caller abort now outranks a cooperative yield observed
earlier in the same batch, evaluated after the drain so a failure surfacing during
settle still wins. `droppedCompletedChunkCount` rides both thrown envelopes and the
operation record, set unconditionally — an absent field cannot distinguish "no
loss" from "never measured", and an internal field no consumer reads is not a
report.

The overlap instrument now asserts an EXACT peak of 4 and cannot hang: it releases
on either the peak or all-spans-arrived, both known to the probe. Three earlier
designs failed and are recorded in the PR body.

Resolves #17412"
- 2026-08-20T22:44:10Z @neo-opus-vega referenced in commit `32bdb8d` - "test(ai): four concurrency scenarios pure prefix arithmetic cannot reach (#17412)

The dispatch plan's arms prove what the plan computes; they cannot prove the
service wires it. These four drive the real loop: a held span that must not
delay its neighbours, an early hole that makes later completions unbindable
and reported, a provider failure surfacing while a yield vote already stands,
and attribution while several spans are outstanding.

A six-mutant kill matrix ran per arm rather than per suite, because the block
is serial and one red arm skips the rest — which had hidden the specificity
the matrix exists to show. The precedence arm's first version was vacuous: at
four spans against four slots every span dispatches in one pass, so the yield
predicate is never consulted and 'failure outranks yield' was asserted where
no yield existed. It now runs six spans against two slots and counts the
consultations it depends on.

The yield error builder stops re-deriving its prefix from a chunk count. The
product's stated justification was false (the final span is short whenever the
count is not a multiple of the width) while its conclusion held for an unstated
reason: the loop consults the predicate only while spans remain undispatched.
No reachable input reddened it, so no arm claims one."
- 2026-08-20T23:05:48Z @neo-opus-vega marked this issue as blocking #17413
- 2026-08-20T23:34:37Z @neo-opus-vega cross-referenced by #17443
- 2026-08-21T00:31:57Z @tobiu referenced in commit `330ae0e` - "feat(ai): the declared parallelism becomes a concurrency, and the carry stops being arithmetic (#17412)

Two bounds held this lane to one request at a time, and the ticket named one.

The named one: `slotHeadroomWidth = parallel - 1` spent the declared parallelism
on request WIDTH to reserve a provider slot. A client cannot reserve a slot — the
server assigns them from its own queue — so it only guaranteed one idled. Deleted;
width now comes from its own leaf, concurrency from the parallelism, and the two
stop being conflated.

The unnamed one, and the reason removing the first alone changes nothing: the
openAiCompatible post queue drained with a single worker behind a boolean
re-entrancy guard, documented as running posts "one at a time". It is now N
bounded workers, N = the declared parallelism, each selecting through the same
interactive-first index. Interactive latency improves rather than degrades: an
interactive item waits for the first of N workers instead of the only one.

The carry had to change representation, not just survive. It computed a completed
span as `completedChunkCount * chunkSize` — a count times a width, valid only
while completions arrive in order. Under fan-out a span can land after a hole, and
the product then claims a range containing inputs that never completed. The
positional-binding guard fails closed, so the consequence was never a corrupt
vector: it was work conservation silently switching off, with the lane
re-purchasing the same vectors on every retry and nothing in the logs.
`resolveCompletedPrefix` now measures the real carryable prefix and REPORTS the
completed-but-unbindable remainder, because a sparse carry is not expressible —
the consumer binds by position via `slice(0, completedTextCount)`.

One bug of mine caught by a pre-existing arm, and it earned its keep: I decorated
the error field by field, so an unprovable prefix escaped with `completedTextCount`
set and `embeddings` undefined — a count with no vectors, which the consumer slices
onto ids anyway. The original computed both before assigning either. Restored, and
that atomicity is now stated as the reason rather than implied by expression order.

The spec's overlap instrument took three attempts and the first two lied. A
macrotask deferral resolved each response before the next request arrived, so a
genuinely concurrent client read as one; holding until two are open then deadlocked
the sequential run. The two runs now use instruments matched to what each can
observe, and the concurrent one holds until it sees real overlap.

Resolves #17412"
- 2026-08-21T00:31:57Z @tobiu referenced in commit `5dd95d4` - "fix(ai): the capacity unit is a task, so width and concurrency share one budget (#17412)

Addresses @neo-gpt's P1 and P2. My model was wrong and his falsifier was right.

I treated the provider's declared parallelism as a REQUEST count. It is a TASK
count: one multi-input POST expands to one task per input, so offered work is
concurrency x width. At `parallel=4` with width 5 my previous head offered 20
tasks against 4 slots.

That also falsifies the claim I published in the PR body, the guide and two A2A
messages — that a client cannot control offered work, so the deleted
`parallel - 1` clamp was reserving a slot it could not hold. In the task unit the
clamp was arithmetically correct: 3 inputs is 3 tasks against 4 slots, which does
leave one free. Its mechanism was a width and its intent was headroom; the intent
is now preserved where it is expressible.

`resolveDispatchPlan` resolves width and concurrency together against the budget.
Reserve one task when the budget allows, clamp the width to what remains, then fan
out into whatever that affords. At 4 tasks with width 5 it reproduces the old
clamp exactly; concurrency appears only once the budget exceeds the width — 3x at
16 tasks, 7x at 16 with width 2. So the throughput win is a property of the
deployment's leaves, not of this code, and at the current plane config it is zero.

A budget of ONE is deliberately untouched. There is nothing to reserve from one
task, so a clamp there protects nothing while turning one request into
one-per-input; the provider queues the extra tasks regardless. An earlier version
clamped unconditionally and split a two-input batch at the default — caught by a
pre-existing lms-server fixture, not by my reasoning.

`resolveEmbeddingTaskBudget` throws on a resolved non-positive-integer instead of
falling back to 1. The leaf's default already covers an absent env var, so a value
arriving here is a config defect, and a silent fallback reports healthy while
ignoring what the deployment declared.

P2: a provider failure or caller abort now outranks a cooperative yield observed
earlier in the same batch, evaluated after the drain so a failure surfacing during
settle still wins. `droppedCompletedChunkCount` rides both thrown envelopes and the
operation record, set unconditionally — an absent field cannot distinguish "no
loss" from "never measured", and an internal field no consumer reads is not a
report.

The overlap instrument now asserts an EXACT peak of 4 and cannot hang: it releases
on either the peak or all-spans-arrived, both known to the probe. Three earlier
designs failed and are recorded in the PR body.

Resolves #17412"
- 2026-08-21T00:31:57Z @tobiu referenced in commit `b7aa271` - "test(ai): four concurrency scenarios pure prefix arithmetic cannot reach (#17412)

The dispatch plan's arms prove what the plan computes; they cannot prove the
service wires it. These four drive the real loop: a held span that must not
delay its neighbours, an early hole that makes later completions unbindable
and reported, a provider failure surfacing while a yield vote already stands,
and attribution while several spans are outstanding.

A six-mutant kill matrix ran per arm rather than per suite, because the block
is serial and one red arm skips the rest — which had hidden the specificity
the matrix exists to show. The precedence arm's first version was vacuous: at
four spans against four slots every span dispatches in one pass, so the yield
predicate is never consulted and 'failure outranks yield' was asserted where
no yield existed. It now runs six spans against two slots and counts the
consultations it depends on.

The yield error builder stops re-deriving its prefix from a chunk count. The
product's stated justification was false (the final span is short whenever the
count is not a multiple of the width) while its conclusion held for an unstated
reason: the loop consults the predicate only while spans remain undispatched.
No reachable input reddened it, so no arm claims one."
- 2026-08-21T10:01:10Z @neo-opus-vega referenced in commit `0b54380` - "test(ai): a deadlocked concurrency probe now fails on the peak it measured (#17412)

Answers P3's instrument half. @neo-gpt's re-review showed the single-worker mutant
still died by SIGKILL rather than by assertion, and I reproduced it: 11.3s against a
1.4s baseline, with the verdict "the process died" instead of "the peak was wrong".

The cause is that BOTH release arms were unreachable in the mutant's world. At one
worker exactly one request is ever open, so `open` cannot reach the target peak, and
requests two onward never dispatch, so the arrival count cannot reach the total. A
predicate built from "values the probe already knows" is undecidable precisely where
the mutant puts it — which is the shape of my own banked lesson, violated inside the
fix that lesson was written for.

The probe now debounces on ARRIVAL and releases what it holds when nothing further
arrives. That is the one fact it cannot otherwise learn; everything else it knows is
what the mutant removes. Armed per arrival rather than once, because at one worker
the stall recurs for every request — a guard armed once rescues exactly one and the
run dies on the second, measured before this shape.

Assertion order matters and the first draft had it wrong. `maxOpen` is asserted
before the stall flag: any concurrency below the target stalls the probe, so both
lines redden together and the first decides the message. The stall wording alone
would have called two-worker behaviour "never overlapped", which is false there.

    mutant maxWorkers=1   was 10.0s SIGKILL   now 1.4s   Expected: 4  Received: 1
    mutant maxWorkers=2   would SIGKILL       now 632ms  Expected: 4  Received: 2

113 specs pass across the three touched files; the green path is unchanged at 114ms
and never reaches the guard."
- 2026-08-21T10:21:13Z @neo-opus-vega referenced in commit `3835698` - "refactor(ai): selecting the next queued post stops being a state change (#17412)

Groundwork for P1, and a defect in its own right. The queue selector returned an
index AND incremented `interactiveBypassCount` on the batch item an interactive post
overtook. Selection was therefore an observation that mutates, so nothing could ask
"what would be dispatched next" without spending a batch item's one fairness bypass.

P1 needs exactly that question. The budget is denominated in TASKS while the queue
admits by POST, so admission has to weigh a candidate BEFORE admitting it — and
weighing means peeking, and peeking cost a bypass whether or not the candidate was
then declined. Weight-based admission cannot be layered on a selector shaped this
way; that is why the first attempt misbehaved, independently of whether its budget
arithmetic was right.

Split into a peek, which is free and reports the bypass it WOULD cause, and a commit,
which applies it only when the dispatch actually happens. Interactive-first fairness
is unchanged: same item, same order, same single bypass — recorded at dispatch rather
than at observation.

The drain comment is corrected too. It claimed the task budget "can never be the
binding constraint … the dispatch loop keeps ITS OWN offered tasks" — sound for one
caller and false for two, which is P1 itself. It now states that a worker is one post
rather than one task, and marks that gap open.

P1's own fix is NOT here. Its red is measured — two callers at width 3 offer six tasks
against a budget of four, by assertion — but the admission change built on this still
deadlocks for a reason I have not isolated, and a concurrency change I cannot explain
is not shippable.

1838 memory-core specs pass. One intermittent failure was observed and is
pre-existing: it reproduces at HEAD without this change, in two of four runs across
both trees."
- 2026-08-21T10:34:51Z @neo-opus-vega referenced in commit `064e1a7` - "fix(ai): the task budget binds across callers, and admission decides before a worker exists (#17412)

P1. `resolveDispatchPlan` reserves headroom per `embedTexts` call; the queue then
admitted by POST while the budget is denominated in TASKS. Two callers each satisfied
their own reservation and jointly offered six tasks against a budget of four.

Admission moves into the drain. A worker cannot enforce it by declining: an async
function that returns before its first `await` runs synchronously to its `finally`,
so the worker count is already back down when control reaches the drain's loop, which
respawns it without bound. That spin is why the first attempt looked like a deadlock
from outside; it is measured, and it is the reason the check has to run before a
worker exists rather than inside one.

`inFlightTasks === 0` always admits, so a post wider than the whole budget still makes
forward progress. A worker looping to a second task re-checks, which is safe because
it has already awaited.

    mutant weight=1 (count posts, the pre-fix unit)   Expected: <= 4  Received: 6
    fixed                                             maxTasks 4, green in 626ms

1839 memory-core specs pass."
- 2026-08-21T10:36:33Z @neo-opus-vega referenced in commit `4fa2647` - "docs(agentos): the width clamp was correct in the task unit, not impossible (#17413)

The guide said `parallel - 1` reserved "a slot the client cannot hold open, since
the server assigns slots from its own queue". That is false, and it is the claim
@neo-gpt falsified during the #17412 review.

The provider's capacity unit is a TASK — one multi-input POST expands to one task
per input — so a client does control offered work, and the clamp reserved real
headroom: three inputs is three tasks against four slots, leaving one free. The
clamp was arithmetically correct and its intent was sound. Its defect was the unit
it was spent in: headroom expressed as a width consumes the budget concurrency
needs, so a lane declaring four slots offered one request at a time.

A ground-truth guide carrying a refuted claim is worse than no guide, which is why
this lands ahead of the #17433 rebase rather than with it.

lint-guides: 0 findings for this file."
- 2026-08-21T11:11:35Z @neo-opus-vega referenced in commit `b9687cf` - "fix(ai): capacity is woken on worker retirement, not after every settle (#17412)

The previous commit re-drained the queue after every completed task. That spawned
workers during container teardown, and the parity stack died on a native
`node::RemoveEnvironmentCleanupHook` assertion — a cleanup hook firing against an
environment that was already gone. Invisible to the unit suite, which never tears a
container down; visible in integration-parity as a 10-minute webServer timeout.

The per-settle drain was redundant anyway. A worker's own loop re-checks admission
and picks up freed capacity itself, so draining after each task only ever spawned
EXTRA workers. Retirement is the only moment another worker is genuinely needed, and
by then this one has awaited, so the count is live and the spawn cannot spin.

CI bisect: parity green at 3835698588, red at 064e1a7519, and the differentiator is
that assertion — `Unterminated string in JSON` and the `defectObservations --digest`
error appear in the passing run too and are pre-existing noise.

1839 memory-core specs pass; the budget arm still reddens under a weight=1 mutant."
- 2026-08-21T11:38:19Z @neo-opus-vega referenced in commit `3c26bcb` - "fix(ai): batch admission leaves the interactive slot open (#17412)

P1, reopened by the reviewer after he retracted his own P1-ADDRESSED disposition.
My cross-caller fix bound offered work to the budget and destroyed the headroom the
budget existed to protect: admission ran to the full budget for every lane, so at
budget 4 two batch callers jointly reach four and a later interactive post finds no
slot.

My arm could not see it. It asserted offered tasks stay <= 4 — the budget noun, not
the headroom noun. Filling all four slots with batch work satisfies that and still
breaks the interactive reservation, so the arm passed on the exact case the Required
Action named.

Admission is now lane-aware in one shared rule: BATCH is admitted to budget - 1,
INTERACTIVE may use the whole budget because the reserved slot exists for it, and
zero tasks in flight always admits, so an oversized post and a single-slot deployment
are both untouched. A per-call reservation cannot express this — two callers each
satisfy their own and jointly fill the budget — which is why it lives where every
caller is visible.

The new arm asserts the contract directly: four single-input batch tasks at budget 4
must peak at three.

    mutant: batch ceiling = full budget    Expected: <= 3  Received: 4
    fixed                                  peaks at 3, green in 627ms

Also P4's orphaned JSDoc: the task-weight helper had been inserted between the
worker's docblock and the worker.

1839 memory-core specs pass, three consecutive clean runs."
- 2026-08-21T12:03:54Z @neo-opus-vega referenced in commit `4d75cd0` - "test(ai): the interactive arm submits real interactive work, and both mutants redden (#17412)

P1, third attempt. The reviewer caught that my headroom arm recorded an
`interactiveAdmitted` flag, never asserted it, and never enqueued interactive work
at all — so a mutant applying the batch ceiling to the interactive lane passed it.

Two further defects surfaced while rebuilding it, both mine and both found only by
mutating:

Firing interactive the moment batch reached three sampled MID-RAMP, before batch
settled, so the no-reservation mutant still had a free slot and passed. The arm now
waits for arrival-quiet steady state, which is the only moment a peak is observable.

Using ONE batch caller made the global ceiling untestable: `resolveDispatchPlan`
already caps a single caller at three, so the reservation this ticket adds was
invisible. Two callers, which is the case the reservation exists for.

And the probe itself held later arrivals forever. `embedTexts` dispatches more spans
than the budget admits, so the remainder arrives AFTER the first release and went
into a `held` list nothing would drain again — the run hung with the contract already
satisfied. A probe defect wearing a product failure's clothes; four rewrites before I
traced it to a file instead of theorising.

Both mutants now fail by assertion, with different messages for different defects:

    interactive gets the batch ceiling   Expected: < 1  Received: 2   (ordering)
    no reservation at all                Expected: 3    Received: 4   (peak)
    baseline                             green in 370ms

P4: two docblocks still said admission volume and headroom are enforced upstream by
`resolveDispatchPlan`. They are enforced here now, and the reason per-call cannot
hold — two callers each satisfy their own reservation and jointly leave nothing free
— is stated where the rule lives.

1840 memory-core specs pass."

