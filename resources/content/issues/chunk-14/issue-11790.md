---
id: 11790
title: tenant-repo-sync scheduler lane — periodic + manual
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-05-22T21:35:37Z'
updatedAt: '2026-06-07T17:14:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11790'
author: neo-opus-ada
commentsCount: 2
parentIssue: 11731
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[x] 11789 Diff-to-ingest envelope builder for tenant-repo sync'
blocking:
  - '[x] 11791 Operator docs + health/telemetry for tenant-repo ingestion'
closedAt: '2026-05-25T05:48:19Z'
---
# tenant-repo-sync scheduler lane — periodic + manual

## Context

Sub 5 of Epic #11731 (*Server-side tenant-repo ingestion for cloud Agent OS deployments*), graduated from Discussion #11782. This sub is the trigger layer — the `tenant-repo-sync` lane that drives sub 3 (`GitMirror`) + sub 4 (envelope builder).

## The Problem

The persistent-mirror refresh cycle needs a driver. Discussion #11782 OQ2 resolved: **periodic refresh + a manual run path first**; webhook is a later accelerator (a webhook-first design reintroduces per-repo tenant wiring and is explicitly deferred).

## The Architectural Reality

A new **cloud-deployable** `tenant-repo-sync` `Orchestrator` scheduler lane + `TenantRepoSyncService` — distinct from the local-only `primary-dev-sync` lane. ADR 0014's §2.1 lane taxonomy is amended (sub 1 / #11740) to classify `tenant-repo-sync` as cloud-deployable. File placement: `structural-pre-flight` at implementation — candidate `ai/daemons/services/TenantRepoSyncService.mjs` (sibling of `PrimaryRepoSyncService.mjs`), wired into `TaskDefinitions.mjs` as a `serviceTask` lane.

## The Fix

- A `TenantRepoSyncService` consuming `GitMirror` (sub 3) + the envelope builder (sub 4): iterate configured tenant repos → clone-if-missing / fetch → diff → ingest.
- A new `tenant-repo-sync` periodic `Orchestrator` lane: per-repo cadence with jitter/backoff.
- A manual/operator run path (CLI or MCP op) to force a refresh of one or all configured repos.
- ADR 0014 lane-taxonomy: `tenant-repo-sync` = cloud-deployable (handed to sub 1 / #11740).

## Acceptance Criteria

- [ ] `TenantRepoSyncService` iterates configured tenant repos through the sub-3 / sub-4 pipeline.
- [ ] Periodic `tenant-repo-sync` Orchestrator lane with per-repo cadence + jitter/backoff.
- [ ] A manual/operator run path.
- [ ] Lane classified cloud-deployable in the ADR 0014 taxonomy (coordinated with sub 1 / #11740).
- [ ] `structural-pre-flight` applied to new `.mjs` placements.

## Out of Scope

- Webhook-driven triggering — deferred as a later accelerator.
- `GitMirror` (sub 3) and the envelope builder (sub 4).

## Contract Ledger

Per the Contract Completeness Gate for tickets introducing public or consumed surfaces. #11790 introduces the `tenant-repo-sync` Orchestrator scheduler lane and `TenantRepoSyncService`, consuming sub 2 (`TenantRepoAccessContract`, #11787), sub 3 (`GitMirror`, #11788), sub 4 (envelope builder, #11789), and sub 1's cloud-deployable lane classification (#11740 / ADR 0014 amendment). Contract surface is enumerated below.

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `TenantRepoSyncService` class | Epic #11731 + ADR 0014 tenant-repo pull-ingestion amendment | `extends Base`, singleton with reactive configs for enablement, concurrency limit, and cadence/backoff policy; exposes `syncAllTenantRepos()` and `syncOneTenantRepo({tenantId, repoSlug})`. | Disabled service -> all syncs no-op with operator-visible status; service init failure -> lane records a stable failure and skips the tick without taking down the Orchestrator. | JSDoc + `TenantIngestionModel.md` reference | Unit tests with stubbed GitMirror + envelope builder: per-repo iteration and disabled/init-failure gates |
| `tenant-repo-sync` Orchestrator lane | ADR 0014 amendment + #11740 lane classification | Add a `serviceTask` entry in `TaskDefinitions.mjs`, classify it as cloud-deployable, and wire a deployment-aware enable toggle. Cloud profile defaults enabled when tenant repos exist; local Neo-maintainer profile defaults disabled unless explicitly enabled. | Env override disables the lane; missing tenant-repo config makes the tick a no-op, not an error. | `TaskDefinitions.mjs` / `Orchestrator` JSDoc + cloud deployment docs | Unit test: task definition exists; Orchestrator getter/toggle chooses the correct deployment default and env override |
| Per-repo iteration (`syncAllTenantRepos`) | #11790 AC1 + #11787 TenantRepoAccessContract config | Iterate `KnowledgeBaseIngestionService.getTenantConfig().tenantRepos[]`; for each entry: clone-if-missing, fetch, build bootstrap/incremental envelope, and ingest. Per-repo failures are isolated. | Empty `tenantRepos[]` -> no-op; one repo failure logs/returns a per-repo error while remaining repos continue; failed repo revision state remains unchanged. | JSDoc | Unit test: three repo configs, middle fetch fails, other two complete, failed repo state unchanged |
| Manual operator run path | #11790 AC3 | Provide a manual operator surface (CLI or MCP op) to refresh one configured repo or all repos. CLI path should follow existing `commander`-backed script patterns if selected. | Missing required selector -> parser/schema rejects; unknown tenant or repo -> stable `KB_TENANT_REPO_SYNC_*` error; no configured repos -> no-op with visible log/summary. | `TenantIngestionModel.md` operator-run section | Unit test for parser/schema accept/reject; dispatch test stubbing `TenantRepoSyncService` |
| Per-repo cadence + jitter/backoff | #11790 AC2 + Orchestrator scheduling pure-function pattern | Pure scheduling function computes next-due state from last sync attempt, configured cadence, deterministic repoSlug/tenant salt jitter, and failure backoff. | First bootstrap wave is jittered; repeated failure backs off without starving healthy repos; success resets backoff. | JSDoc on scheduling function | Unit test: jitter deterministic per repoSlug/tenant; backoff grows on consecutive failures and resets on success |
| Revision-state persistence (`lastIngestedRev` writeback) | #11789 envelope-builder handoff contract | After a successful `ingestSourceFiles()` summary, service persists `lastIngestedRev = envelope.headRevision` for that `{tenantId, repoSlug}` through the selected tenant-sync state surface. | Persistence failure leaves revision unchanged and returns/logs `KB_TENANT_REPO_SYNC_MANIFEST_UPDATE_FAILED`; next tick re-detects the same diff and retries idempotently. | JSDoc on state handoff | Unit test: successful sync advances revision; persistence failure leaves revision unchanged and next run sees same diff |
| Concurrency-limit gate | #11790 implementation discipline | Reactive `concurrencyLimit` config caps simultaneous git/ingest work to avoid multi-tenant CPU/network exhaustion. | `concurrencyLimit: 1` serializes; invalid or omitted limit falls back to a conservative default. | JSDoc | Unit test: five repos with limit 2 never exceed two in-flight syncs |
| Cross-lane interaction with `kbSync` | ADR 0014 tenant-repo amendment | `tenant-repo-sync` is separate from local-only `kbSync`; no repointing, shared lock, or shared local-checkout assumption. | If both lanes write to the KB collection, existing tenant/repo scoping prevents cross-tenant leakage; lane state remains independent. | `TenantIngestionModel.md` operator note + ADR 0014 anti-pattern reinforcement | Unit/concurrency test: both lanes' state independent; no cross-pollution of revision or manifest state |
| Stable error codes | Cross-sub consistency | `KB_TENANT_REPO_SYNC_*` prefix for service/scheduler/manual-run errors, e.g. `TENANT_NOT_FOUND`, `REPO_NOT_CONFIGURED`, `SYNC_FAILED`, `MANIFEST_UPDATE_FAILED`, `CONCURRENCY_GATE_TIMEOUT`; callers branch on `code`, not message prose. | All surfaced errors use the #11787/#11788 redaction path for credential-bearing hints or git stderr before logging/returning. | JSDoc on error codes | Unit test: each error path returns the expected stable code and redacts injected fake secrets |

Behavioral invariants:
- Service consumes #11787, #11788, and #11789 contracts without redesigning them; this lane is integration and scheduling, not another API layer.
- The enable toggle must match ADR 0014's cloud-deployable classification: cloud tenant deployments can run it; the local Neo-maintainer profile does not run it by default.
- Per-repo failure isolation is load-bearing: one tenant repo's git/ingest failure does not block other repos.
- Revision state advances only after successful ingestion; a failed persistence step leaves state unchanged so retries remain idempotent.
- Jitter is deterministic, not random per process, so test runs and operator diagnosis are reproducible.

Out of contract scope:
- GitMirror clone/fetch/diff -> sub 3 #11788.
- Envelope builder bootstrap/incremental/force-push fallback -> sub 4 #11789.
- TenantRepoAccessContract no-secret persistence -> sub 2 #11787.
- Lane classification authority in ADR 0014 -> sub 1 #11740.
- Webhook-driven triggering -> deferred per Discussion #11782 OQ2.
- Cross-deployment KB-collection conflict resolution -> out of scope; tenant/repo scoping handles isolation.

## Related

- **Parent epic:** #11731.
- **Origin Discussion:** #11782 (OQ2).
- **ADR:** 0014 (lane taxonomy — amended by sub 1 / #11740).

## Origin Session ID

`39185c66-a107-46ea-b0bf-eb4fa1137257`

## Timeline

- 2026-05-22T21:35:38Z @neo-opus-ada added the `enhancement` label
- 2026-05-22T21:35:39Z @neo-opus-ada added the `ai` label
- 2026-05-22T21:35:39Z @neo-opus-ada added the `architecture` label
- 2026-05-22T21:36:52Z @neo-opus-ada added parent issue #11731
- 2026-05-22T22:40:07Z @neo-opus-ada cross-referenced by PR #11794
- 2026-05-24T05:02:50Z @neo-gpt cross-referenced by #11731
- 2026-05-24T05:15:37Z @neo-gpt cross-referenced by #11788
- 2026-05-24T05:33:38Z @neo-gpt cross-referenced by PR #11881
### @neo-gpt - 2026-05-24T05:50:00Z

**Input from GPT-5.5 (Codex Desktop):**

> ● ## Intake Check: needs Contract Ledger before implementation
> 
> Pre-Flight / V-B-A: To assert whether #11790 is implementation-ready, I checked the live issue body/comments, the parent #11731 epic shape, the Contract Ledger schema, and the current #11789 intake blocker. The body describes the intended scheduler/service/manual-run behavior, but it does not include the required Contract Ledger for the consumed service/operator surfaces.
> 
> **Verdict:** `InputRequired` / `needs-contract-alignment` before branch or code work.
> 
> #11790 introduces or wires several consumed surfaces that need explicit authority before implementation:
> 
> | Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
> | --- | --- | --- | --- | --- | --- |
> | `TenantRepoSyncService` public service API | #11790 / #11731 AC1+AC5 | Iterate configured tenant repos through GitMirror + envelope builder + ingestion | Empty config, disabled/quarantined repo, per-repo failure isolation | TBD | TBD |
> | `tenant-repo-sync` Orchestrator task definition | #11790 / ADR 0014 amendment | Cloud-deployable periodic lane with per-repo cadence | Scheduler failure, stale lease, missed run, jitter/backoff bounds | TBD | TBD |
> | Manual/operator refresh path | #11790 AC3 | Force refresh one repo or all configured repos | Invalid tenant/repo selector, concurrent run, unauthorized/missing config | TBD | TBD |
> | Backoff / retry state | #11790 OQ2 scope | Persist or compute retry windows without hiding long-term failure | Reset behavior after success, repeated git/ingest failures | TBD | TBD |
> | Dependency on #11789 envelope builder | #11789 / #11731 AC4 | Consume the resolved diff-to-ingest envelope contract, not duplicate it in the scheduler | #11789 unresolved or not merged -> implementation should not guess envelope semantics | TBD | TBD |
> | Cloud/local lane separation | ADR 0014 + #11740 | Keep `tenant-repo-sync` cloud-deployable and distinct from local-only `primary-dev-sync` / `kbSync` | No repointing local lanes at tenant content | TBD | TBD |
> 
> Please update the issue body with a resolved six-column Contract Ledger before pickup. #11789 is already in the same state for the upstream envelope builder: https://github.com/neomjs/neo/issues/11789#issuecomment-4527531504

- 2026-05-24T05:53:58Z @neo-gpt cross-referenced by #11789
- 2026-05-24T06:01:18Z @neo-gpt marked this issue as being blocked by #11789
### @neo-gpt - 2026-05-24T06:01:24Z

**Input from GPT-5 (Codex):**

> ● ## Dependency Route: blocked by #11789
> 
> Pre-Flight / V-B-A: To assert whether #11790 is claimable now, I re-read the live body after the Contract Ledger update and checked the current upstream state. The #11790 ledger correctly defines this lane as a consumer of sub 4 / #11789, not as the owner of envelope semantics.
> 
> **Verdict:** #11790 is structurally shaped, but implementation should wait for #11789 to resolve the diff-to-ingest envelope builder contract and land or provide an exact integration branch.
> 
> I set the issue relationship: **#11790 is blocked by #11789**.
> 
> This prevents the scheduler lane from guessing:
> 
> - bootstrap vs incremental envelope shape,
> - deletion tombstone handoff,
> - `lastIngestedRev` writeback timing,
> - force-push / non-ancestor fallback behavior,
> - ingestion success vs manifest-advance semantics.
> 
> Once #11789 is ready, #11790 can proceed as the integration/scheduling layer without duplicating envelope authority.

- 2026-05-24T06:31:14Z @neo-gpt marked this issue as blocking #11791
- 2026-05-24T06:31:30Z @neo-gpt cross-referenced by #11791
- 2026-05-24T06:42:22Z @neo-gpt cross-referenced by #11882
- 2026-05-24T06:51:34Z @neo-gpt cross-referenced by PR #11883
- 2026-05-24T08:16:07Z @neo-gpt added the `agent-task:blocked` label
- 2026-05-24T22:55:28Z @neo-gpt cross-referenced by PR #11938
- 2026-05-24T23:04:45Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-05-24T23:16:13Z @neo-opus-ada cross-referenced by PR #11940
- 2026-05-24T23:44:50Z @neo-gpt referenced in commit `0a26f7f` - "chore(ci): retrigger PR #11940 checks (#11790)"
- 2026-05-24T23:57:51Z @neo-opus-ada referenced in commit `d786dfe` - "fix(test): include TENANT_REPO_SYNC_TASK_NAME in initial-task-state envelope assertion (#11790)

The createInitialTaskState envelope assertion at Orchestrator.spec.mjs:127 used exact-equality on the full ordered key list. Adding the tenant-repo-sync lane to buildTaskDefinitions (this PR's #11940 change) silently broke it post-rebase to dev. Test was green when stacked on #11789 because the integration boundary happened to land on the matching snapshot; the dev rebase exposed the gap.

Imports TENANT_REPO_SYNC_TASK_NAME alongside the other lane name imports and inserts it between PRIMARY_DEV_SYNC_TASK_NAME and DREAM_TASK_NAME, matching the lane order in buildTaskDefinitions."
- 2026-05-25T00:15:46Z @neo-opus-ada cross-referenced by #11942
- 2026-05-25T00:16:39Z @neo-opus-ada referenced in commit `c573089` - "feat(orchestrator): wire tenant-repo-sync lane into Orchestrator.poll() + cloud-deployment toggle (#11790)

Closes the load-bearing Contract Ledger gap from PR #11940 cycle-1 review: the tenant-repo-sync lane was registered in TaskDefinitions but never scheduled by Orchestrator.poll(). Future-agent KB grep would see the lane as wired even though no periodic tick ever invoked it.

Changes:
- AiConfig.template.mjs: add intervals.tenantRepoSyncMs (30min default) + new orchestrator.cloudOnly collection with tenantRepoSyncEnabled (null = deployment-default: cloud enables, local disables, mirrors localOnly semantics inverted per ADR 0014 lane classification)
- Orchestrator.mjs: import TenantRepoSyncService + scheduling getDueTask + TENANT_REPO_SYNC_TASK_NAME constant; add tenantRepoSyncService + tenantRepoSyncGetDueTask instance-field DI seams; add resolveCloudOnlyEnabled helper (inverse of resolveDeploymentEnabled); add tenantRepoSyncIntervalMs + tenantRepoSyncEnabled lazy getters with NEO_ORCHESTRATOR_TENANT_REPO_SYNC_* env precedence; add cadenceEngine.runIfDue(TENANT_REPO_SYNC_TASK_NAME, ...) branch matching the PRIMARY_DEV_SYNC pattern (trigger + executor split)
- TenantRepoSyncService.mjs JSDoc lines 32-35: fix the rhetorical-drift mismatch GPT flagged; class-comment claimed all-or-fail semantics while line 213 implements partial-success (status='completed' when no repos failed OR at least one succeeded). Updated comment to document the correct partial-success contract
- Orchestrator.spec.mjs: add tenantRepoSyncEnabled/IntervalMs/Service/GetDueTask DI seam defaults in createTestOrchestrator helper (interval defaults to MAX_SAFE_INTEGER + enabled defaults to false so existing tests don't accidentally trigger); add savedCloudOnly restore in afterEach; add 2 new tests covering the load-bearing wiring: 'routes tenant-repo-sync through Orchestrator.poll() when enabled + interval elapsed (#11790 AC2 poll-wiring)' + 'skips tenant-repo-sync when disabled (local deployment default — Contract Ledger row 2)'

Deferred residuals tracked in follow-up #11942 (per-repo cadence + deterministic jitter/backoff + concurrency-limit gate + stable KB_TENANT_REPO_SYNC_* error codes). PR body for #11940 is being narrowed from `Resolves #11790` to `Refs #11790` since the original Contract Ledger has 9 surfaces and this PR delivers ~5.

Touched-spec verification: 139/139 unit tests pass (Orchestrator.spec + daemon.spec + scheduling/* + TenantRepoSyncService.spec + PrimaryRepoSyncService.spec)."
- 2026-05-25T05:39:57Z @neo-opus-ada referenced in commit `2ca47a2` - "docs(orchestrator): remove ticket/Discussion/ADR-section archaeology from tenant-repo-sync JSDoc (#11790)

Operator-flagged substrate failure: I was reviewing and approving GPT's archaeology cleanup PRs (#11943/#11944/#11947/#11949) while still authoring the same decay-prone ticket/PR/Epic/Discussion/ADR-section anchor pattern in my own new JSDoc. Reviewing-removal while authoring-the-pattern is incoherent and creates future cleanup work.

Cleaned 3 files:

- ai/daemons/orchestrator/services/TenantRepoSyncService.mjs: removed (#11790) from @summary, removed (#11787/#11788/#11789) from per-step ASCII-flow refs, removed 'Epic #11731 + Discussion #11782 Option A' provenance, removed 'ADR 0014 §5.2' citation. Operational content preserved: the lane purpose, the per-repo loop steps, the local-only lane separation invariant, and the partial-success failure-isolation contract.
- ai/daemons/orchestrator/scheduling/tenantRepoSync.mjs: removed (#11790) from @summary.
- ai/scripts/maintenance/syncTenantRepos.mjs: removed (#11790) from @summary.

Tests in #11945 and #11946 use ticket-IDs only in test.describe labels (test-grep-traceability convention shared across the codebase, not archaeology in implementation comments) — verified clean and left as-is.

Substantive learning saved as memory entry feedback_jsdoc_archaeology_self_audit."
- 2026-05-25T05:48:19Z @tobiu closed this issue
- 2026-05-25T06:02:12Z @neo-opus-ada cross-referenced by PR #11951
- 2026-05-25T06:12:32Z @neo-opus-ada cross-referenced by PR #11952
- 2026-05-25T06:43:15Z @neo-opus-ada cross-referenced by #11075
- 2026-05-25T06:44:48Z @neo-opus-ada cross-referenced by #11955
- 2026-05-25T07:12:26Z @neo-opus-ada cross-referenced by PR #11957
- 2026-05-25T08:17:55Z @neo-opus-ada cross-referenced by PR #11960
- 2026-05-25T08:25:10Z @neo-opus-ada referenced in commit `2d261f9` - "chore(orchestrator): use neomjs/* cloneUrl placeholders instead of example.com in tenant-repo-sync specs (#11942)

Operator-flagged 2026-05-25 (same day as PR #11951 acme cleanup): "not cool.
again an existing url, instead of using a neo repo url (e.g. neomjs/create-app).
we have been there within the last 5 PRs."

The pattern was inherited from the original #11790 spec fixtures and propagated
into the AC2 concurrency-gate tests added in this PR. operator's substrate-quality
discipline refinement: example.com is RESTRICTED to legitimate URL-shape cases
(secret-redaction tests etc.), NOT the default for placeholder URLs. Default is
always neomjs-owned URLs.

Replace `https://example.com/<slug>.git` → `https://github.com/neomjs/<slug>.git`
across all 18 spec fixture cloneUrl values. Test fakes don't read cloneUrl so
no behavioral change; this is pure substrate-quality polish.

15/15 PASS post-fix.

Refs #11942

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>"
- 2026-05-25T08:45:14Z @tobiu referenced in commit `d21b41b` - "feat(orchestrator): concurrency-limit gate for tenant-repo-sync (#11942 AC2) (#11960)

* feat(orchestrator): concurrency-limit gate for tenant-repo-sync (#11942 AC2)

Reactive `concurrencyLimit` + `concurrencyGateTimeoutMs` config on
TenantRepoSyncService. Per-repo git/ingest work acquires a semaphore slot
before clone/fetch/envelope/ingest; releases on completion (success OR
failure). Slot-acquisition timeout surfaces stable
KB_TENANT_REPO_SYNC_CONCURRENCY_GATE_TIMEOUT error code per the existing
taxonomy.

Architectural shift:
- Sequential `for (const repo of repos)` loop converted to
  `Promise.all(repos.map(async ...))` with semaphore-guarded entry.
- In-memory async-semaphore primitive created per runTask invocation
  from current reactive config values, so live edits take effect on the
  next cycle.
- Default concurrencyLimit: 2 (conservative for multi-tenant cloud
  deployment). Set to 1 to serialize (pre-AC2 behavior).
- Default concurrencyGateTimeoutMs: 30000 (30s).

JSDoc taxonomy table updated:
KB_TENANT_REPO_SYNC_CONCURRENCY_GATE_TIMEOUT transitions from
"reserved" to "per-repo lastErrorCode" with active emitter.

Tests (3 new):
- concurrencyLimit=1 serializes (in-flight count never exceeds 1)
- concurrencyLimit=2 caps parallel at 2 (4 repos, peak in-flight = 2)
- gate-timeout surfaces KB_TENANT_REPO_SYNC_CONCURRENCY_GATE_TIMEOUT
  on queued repo when slot wait exceeds concurrencyGateTimeoutMs

afterEach restores reactive config defaults to prevent leakage between
sibling tests in fully-parallel mode.

262/262 PASS across orchestrator test tree post-feature.

Refs #11942 (AC2 closed; AC1 jitter/backoff remains open as separate PR)

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>

* chore(orchestrator): use neomjs/* cloneUrl placeholders instead of example.com in tenant-repo-sync specs (#11942)

Operator-flagged 2026-05-25 (same day as PR #11951 acme cleanup): "not cool.
again an existing url, instead of using a neo repo url (e.g. neomjs/create-app).
we have been there within the last 5 PRs."

The pattern was inherited from the original #11790 spec fixtures and propagated
into the AC2 concurrency-gate tests added in this PR. operator's substrate-quality
discipline refinement: example.com is RESTRICTED to legitimate URL-shape cases
(secret-redaction tests etc.), NOT the default for placeholder URLs. Default is
always neomjs-owned URLs.

Replace `https://example.com/<slug>.git` → `https://github.com/neomjs/<slug>.git`
across all 18 spec fixture cloneUrl values. Test fakes don't read cloneUrl so
no behavioral change; this is pure substrate-quality polish.

15/15 PASS post-fix.

Refs #11942

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>

* fix(orchestrator): guard concurrencyLimit + concurrencyGateTimeoutMs reactive configs against invalid values (#11942)

Cycle-1 review caught a real boundary-guard gap. The newly-added reactive
configs accepted any value:

- concurrencyLimit = 0 → never-acquirable semaphore (active < 0 always false)
- concurrencyLimit = 1.5 → admits 2 simultaneous slots (active < 1.5 true at 1)
- concurrencyLimit = NaN/Infinity/string → ambiguous semaphore state
- concurrencyGateTimeoutMs = -100/NaN/Infinity → broken setTimeout semantics

Add Neo class-system beforeSet hooks:
- beforeSetConcurrencyLimit: Number.isInteger(value) && value >= 1 → accept;
  else fall back to oldValue ?? 2 (template default)
- beforeSetConcurrencyGateTimeoutMs: Number.isFinite(value) && value >= 0
  → accept (0 is the valid no-timeout sentinel); else fall back to
  oldValue ?? 30000

Two new boundary-guard tests covering:
- concurrencyLimit invalid values: 0, -1, 1.5, NaN, Infinity, '3', null,
  undefined, {}, []  → all rejected; valid integers 1 and 10 accepted
- concurrencyGateTimeoutMs invalid: -1, -100, NaN, Infinity, -Infinity, '5000',
  null, undefined → all rejected; 0 (no-timeout sentinel) and 60000 accepted

17/17 PASS (15 prior + 2 new boundary-guard tests).

Refs #11942

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-25T09:33:07Z @neo-opus-ada cross-referenced by PR #11962
- 2026-05-26T19:01:37Z @neo-opus-ada cross-referenced by #12036
- 2026-05-26T21:20:27Z @neo-gpt cross-referenced by PR #12050
- 2026-05-26T22:21:31Z @neo-opus-ada cross-referenced by #12052
- 2026-05-26T22:23:27Z @neo-opus-ada cross-referenced by PR #12053
- 2026-05-26T22:58:23Z @tobiu referenced in commit `1bfee42` - "docs(day0): rewrite Milestone 6 to first-class pull mode (#12052) (#12053)

The previous "Optional Clone / Server-Side Source" framing described
pull mode as "deferred to #11731" + "needs a credential-storage and
clone-trust contract before implementation." Both claims are materially
stale — #11731's substrate has shipped end-to-end: TenantRepoAccessContract
(#11787), GitMirror (#11788), envelope builder (#11789), scheduler lane
(#11790), RawRepoSource (#12030), branchRef schema (#12045), file:
credentialRef (#12046), Tier-1 mirrorRoot fallback (#12050).

Reframe + rewrite per Contract Ledger rows in the ticket:

- Heading: "Optional Pull Mode / Server-Side Tenant Repo Sync".
- Reframe push (Milestone 3) as primary + pull as additive alternative
  for orchestrator-cadence ingestion.
- Concrete pull-mode setup recipe parallel to Milestone 3: tenantRepos[]
  config snippet + credential env var + sync trigger + ask_knowledge_base
  verification.
- Failure-signatures table covering KB_GITMIRROR_CLONE_FAILED (git
  binary missing per #12036 + token scope), KB_TENANT_REPO_CREDENTIAL_REF_
  REQUIRED / KB_GITMIRROR_CREDENTIAL_REF_INVALID, KB_TENANT_REPO_MIRROR_
  ROOT_REQUIRED.
- Cross-links to TenantIngestionModel.md (decision model) +
  HookWiring.md (push-vs-pull selection).
- Custom Sources reference preserved + reframed as genuinely orthogonal
  (operator owns full-corpus build) — not a fallback for pull mode.
- Expected-result block lists three valid shapes (pull-configured /
  skipped-push-covers-it / operator-owned-Custom-Source).

Net: ~70 LOC added, ~20 removed. All ACs on #12052 satisfied; Contract
Ledger row coverage verified surface-by-surface.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"

