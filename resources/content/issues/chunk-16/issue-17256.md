---
id: 17256
title: The engine→Brain boundary needs an enforceable guard before the nine crossings move
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
  - architecture
  - build
assignees:
  - neo-opus-ada
createdAt: '2026-08-16T20:39:32Z'
updatedAt: '2026-08-17T08:35:06Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17256'
author: neo-opus-ada
commentsCount: 0
parentIssue: 17239
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-17T08:35:06Z'
---
# The engine→Brain boundary needs an enforceable guard before the nine crossings move

## Context

The guard half of #17239, split per its reviewer's fork answer (option 1: guard first, burndown after). Filed as a leaf so the guard has an honest close target while #17239 stays open on the relocation it actually asks for.

Two independent reasons the guard leads rather than trails, both from the parent's own thread:

1. **The count was wrong three times.** #17239 named three crossings. Running its own AC-1 grep found six. Rewriting the detector to read the AST found ten — but one of those ten was my own detector defect, not a real crossing. **The true census is nine crossings across seven files, all under `buildScripts/`.** Every sweep before the last was `from`-anchored, and a dynamic import has no `from` keyword. A baseline turns the count into an artifact that fails when it drifts, instead of a number in a title that outlives the sweep that produced it.
2. **`publish.mjs` is the atomic `dev` → `main` release path.** It is the one relocation where a mid-move regression is expensive and hard to notice, and it should not move without a net.

## The Problem

`neomjs/neo` is the engine; `ai/` is the Brain. The direction is one-way by design — the Brain may consume the engine, never the reverse. Nothing enforces it, so ten crossings accumulated without any of them failing anything.

**`src/**` is clean, and stays clean.** An earlier revision of this ticket claimed otherwise, on the strength of `src/worker/App.mjs:779` doing `import('../ai/Client.mjs')`. That resolves to **`src/ai/Client.mjs`**, not the top-level `ai/` — and `src/ai/**` is the engine's own AI layer, the one sanctioned seam by which the Body connects to the Brain. Engine code, by design.

The claim came from a predicate matching specifier *text* rather than resolving it against the importing file: identical text, opposite meanings by directory. Retracted in full; the guard now resolves before deciding, and the spec asserts both verdicts of that pair.

What the earlier hand-greps genuinely missed is `buildScripts/devCockpit.mjs` — three crossings, all **dynamic imports**, which have no `from` keyword and so were invisible to every `from`-anchored sweep.

## The Architectural Reality

Ten crossings, three classes. A and B are the parent ticket's split; C is one neither census had.

| Class | What it is | Fix shape | Sites |
|---|---|---|---|
| A | engine tooling reaching a Brain service | moves a **concern** | `labels.mjs`, `rebuildContentIndexesAndSeo.mjs`, `publish.mjs` |
| B | agent-serving script misfiled in an engine directory | moves a **file** | `agentCoAuthorEmails.mjs`, `deriveFleetRoster.mjs`, `agent-preflight.mjs`, `devCockpit.mjs` (×3) |

The A/B split is #17239's and it is complete. A "Class C" proposed in an earlier revision of this ticket was an artifact of the predicate defect above and has been withdrawn.

## The Fix

`buildScripts/util/check-engine-brain-boundary.mjs` — reads `ImportDeclaration` / `ImportExpression` nodes from the AST, not text — plus a burndown baseline, a lint-staged entry, and its required CI mirror.

Three properties that are the whole point:

- **Asserts the property, not the list.** The baseline exempts known debt; it is not the assertion. A crossing in a file the baseline does not cover fails, so a new one cannot pass by leaving the listed nine intact.
- **The ratchet fails both ways.** A baselined crossing that no longer violates must also leave the baseline, or the recorded count drifts above the real one and becomes a list of things that used to be true.
- **Rows carry `count`.** `devCockpit.mjs` imports `localBearer.mjs` at two lines; a key of file+specifier alone holds one member for both, so removing one occurrence would leave the key present and the diff silent — the measured shape from a sibling baseline where 83 rows collapsed to 9 keys and deleting 63 of 64 still reported green.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `check-engine-brain-boundary.mjs` | new | fails on an unbaselined crossing, or on baseline drift in either direction | unparseable source → skipped (`check-parse` owns that) | module JSDoc | red-proofs below |
| `check-engine-brain-boundary-baseline.json` | this ticket | ten rows, each with class + `count` + why it is tolerated | absent file → hard failure | inline `note` per row | `git ls-files` census |
| `lint-staged` `{buildScripts,src}/**/*.mjs` | `package.json` | runs the guard pre-commit | `--no-verify` bypass covered by the CI mirror | — | `lint-guard-ci-parity` OK |
| `engine-brain-boundary-lint.yml` | `lint-guard-ci-parity` | CI mirror; resolved by parsed `run:` full path in a `*-lint.yml` | n/a | header comment | lint green |

## Decision Record impact

`none` — restores a boundary no ADR presently defines. Whether the engine↔Brain direction deserves a formal ADR is #17239's stated follow-up, not this leaf's.

## Acceptance Criteria

- [ ] The guard reports zero drift against the shipped baseline on `dev`, and the baseline contains all nine crossings with per-row class and count.
- [ ] `src/ai/**` consumers are NOT convicted — the specifier is resolved against the importing file rather than pattern-matched. Asserted at three import depths, plus the same specifier yielding opposite verdicts from `buildScripts/` and from `src/worker/`.
- [ ] A crossing in a file **absent from the baseline** fails — red-proofed, so the check cannot degrade into a snapshot comparison.
- [ ] Both ratchet directions fail — a new crossing, and a baselined crossing that disappeared without a baseline edit.
- [ ] A **partial** burndown of a multi-occurrence crossing fails (2 → 1 on `devCockpit.mjs`), red-proofed.
- [ ] A dynamic `import('../ai/…')` is detected — the shape every prior sweep missed.
- [ ] The guard does not convict comments, prose strings, `apps/ai/**` lookalikes, or **its own JSDoc**, which quotes specifiers as examples.
- [ ] `lint-guard-ci-parity` passes: the lint-staged guard has a CI mirror that invokes it by full path.

## Out of Scope

- **Relocating any of the ten.** That is #17239, deliberately. This leaf lands the net; the parent does the moving.
- The Class C decision. The guard makes it visible and baselines it; whether `src/worker/App.mjs` keeps its lazy Brain import is a call for #17239 or its own ticket.
- Making the workflow a required status context — #17171 covers that class for all nineteen existing lint workflows.

## Avoided Traps

- **Regex over source text.** The first draft did this and convicted its own JSDoc — invisible until the file became tracked and the guard could scan itself. Masking comments would have fixed that one instance; reading the parse tree removes the class *and* catches the dynamic imports, which is what took the count from six to ten.
- **Baseline-as-assertion.** Flagged by the parent's reviewer before it could ship: a snapshot used as the assertion lets a seventh import in a new file pass while the six stay clean — the exact shape the parent ticket is now a case study in.
- **A key without a count.** Would have let a partial burndown of `devCockpit.mjs` report green.

## Related

- Parent: #17239 (the relocation; stays open)
- #17237 / PR #17243 — the same boundary eroding in the opposite direction
- #17171 — required-status-context gap affecting every lint workflow

Origin Session ID: 3f264a19-c7d4-481e-bc80-5c288bca177f

Retrieval Hint: `query_raw_memories("engine Brain boundary guard dynamic import from-anchored grep missed src/worker/App.mjs")`

Live latest-open sweep: checked latest open issues at 2026-08-16T20:40Z; A2A claim sweep same time — #17239's owner has answered the fork in favour of this split and holds no competing claim.


## Timeline

- 2026-08-16T20:39:33Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-16T20:39:34Z @neo-opus-ada added the `enhancement` label
- 2026-08-16T20:39:34Z @neo-opus-ada added the `ai` label
- 2026-08-16T20:39:35Z @neo-opus-ada added the `testing` label
- 2026-08-16T20:39:35Z @neo-opus-ada added the `architecture` label
- 2026-08-16T20:39:35Z @neo-opus-ada added the `build` label
- 2026-08-16T20:39:45Z @neo-opus-ada added parent issue #17239
- 2026-08-16T20:40:52Z @neo-opus-ada cross-referenced by PR #17257
- 2026-08-16T20:47:26Z @neo-opus-ada referenced in commit `9208672` - "fix(build): the boundary predicate resolved nothing — it matched text shape (#17256)

`../ai/Client.mjs` means different things depending on where it is written. From
`buildScripts/devCockpit.mjs` it resolves to the Brain. From `src/worker/App.mjs` it resolves to
`src/ai/Client.mjs` — the engine's own AI layer, the one sanctioned seam by which the Body connects
to the Brain, and unambiguously engine code.

The predicate matched `/^(?:\.\.\/)+ai\//` on the specifier alone, so it convicted the second case
and I published a confident, wrong claim that the engine runtime crosses the boundary. It does not.
`src/**` is clean and always was — which is exactly what the originating ticket said before I
'corrected' it.

Specifiers are now joined to the importing file's directory and normalised before anything is
decided. A path question answered by a text pattern stays wrong precisely where the answer matters.

Census after the fix: 9 crossings across 7 files, all under buildScripts/, zero in src/**. The one
thing the earlier hand-greps genuinely missed is devCockpit.mjs, whose three crossings are all
dynamic imports.

Specs now assert the pair directly: the same specifier from buildScripts/ fires and from src/worker/
does not, plus three src/ai/** consumers that must stay clean."
- 2026-08-16T20:49:52Z @neo-opus-ada cross-referenced by #17239
- 2026-08-16T20:50:57Z @neo-opus-ada referenced in commit `ba7ccd1` - "fix(ci): the boundary workflow needs acorn — its own comment said otherwise (#17256)

The job skipped `npm ci` and stated the guard was dependency-free. True of the regex draft; false
from the acorn rewrite onward, and the comment outlived the change. CI failed with
ERR_MODULE_NOT_FOUND, which is the honest outcome.

Matches the atomic-write-shape sibling, which installs for the same reason: `npm ci --ignore-scripts`
plus npm caching, skipping the heavy prepare lifecycle.

Third instance this branch of prose outliving the code it describes — the module JSDoc, the success
message, and now a workflow comment. Each was accurate when written."
- 2026-08-16T21:32:59Z @neo-opus-ada referenced in commit `1d89bf9` - "fix(build): register the boundary lint's scan surface — scanned must be a subset of watched (#17256)

lintWorkflowScanRootParity failed: a new path-filtered lint workflow must register its scan surface,
and mine did not. A workflow whose paths filter is narrower than what its lint actually scans runs
late and misattributed — present, correct, and never triggered by the change that broke it.

Registered as imported rather than declared, the shape the spec prefers: the guard exports
SCAN_SURFACE and the registry takes it as SSOT, so the scanned set and the watched set are one
object rather than two lists that agree today. The runtime walk derives from it too, so a root
cannot be added to the walk without the workflow and the spec seeing it."
- 2026-08-17T07:24:47Z @neo-opus-ada referenced in commit `643e435` - "fix(build): the boundary guard's only output named no line (#17256)

`tallyCrossings` collapses occurrences to {file, specifier, count} and drops
`line` on the way. The reporter then asked a tallied row for `entry.line`, so
the guard's single product -- the failure message -- printed the literal
`:undefined` where the location belongs.

Fifteen specs passed over it. Every one asserts the returned arrays, and `line`
IS asserted, on findBrainImports, one layer upstream of where it was lost. The
coverage boundary and the defect boundary were the same line.

Reported from the raw findings rather than by re-attaching one line to the tally:
a crossing with count 2 has two real locations and a tallied row can only name
one, so a developer sent to devCockpit.mjs would have found the second by hand.

Three specs on the reporter, red-proofed against the pre-fix shape. Plus the two
docstrings the census correction missed: both said six, the module docstring
already said nine.

Found by @neo-opus-grace on review, with a planted-crossing probe."
- 2026-08-17T07:24:49Z @neo-opus-ada changed title from **The engine→Brain boundary needs an enforceable guard before the ten crossings move** to **The engine→Brain boundary needs an enforceable guard before the nine crossings move**
- 2026-08-17T07:29:56Z @neo-opus-ada cross-referenced by #17272
- 2026-08-17T08:35:07Z @tobiu closed this issue
- 2026-08-17T08:35:07Z @tobiu referenced in commit `eace3d1` - "feat(build): the engine → Brain boundary becomes enforceable, nine crossings baselined (#17257)

* feat(build): the engine -> Brain boundary becomes enforceable, ten crossings baselined (#17239)

The engine must not consume the Brain. Its build tooling does, and the consequence is concrete:
`buildScripts/release/publish.mjs` performs the atomic dev -> main release commit and cannot run
without `ai/services.host.mjs`, so releasing the framework requires the agent OS to be importable.

The guard, not the relocation. Ten crossings including the release path is multi-step work, and a
gate failing on all ten from day one is a gate that gets disabled. The baseline makes the boundary
enforceable now while they burn down.

## The count went three -> six -> ten, and each step was a tool limitation, not carelessness

The ticket named three. Running its own AC-1 — "grep over the whole directory, not only the three
named files" — found six. Then rewriting the detector to read the AST instead of matching text found
ten, in nine files.

Both greps were `from`-anchored. A DYNAMIC import has no `from` keyword, so neither could see one.
Four crossings are dynamic imports, and one of them is the premise:

    src/worker/App.mjs:779   this.aiClientPromise = import('../ai/Client.mjs')

The ticket states `src/**` is clean with zero crossings. I reproduced that grep independently, got
zero, and asserted it publicly. `src/**` is not clean — the engine RUNTIME reaches the Brain. It is
lazy and gated behind the `useAi` config, so the engine does not unconditionally require `ai/`, but
"never crosses" is false and that row is the one to burn down first.

## Reading declarations from the parse tree, not text

The first draft regex-matched `from '…'` and immediately convicted its own JSDoc, which quotes two
specifiers as examples — invisible until the file became tracked and the guard could scan itself.
Masking comments would have fixed that instance; taking the specifier from an `ImportDeclaration`
removes the class, and catches the dynamic-import shape that text-matching structurally cannot.

## The ratchet fails both ways, and counts occurrences

A baselined crossing that no longer violates must also leave the baseline, or the recorded count
drifts above the real one and becomes a list of things that used to be true.

Rows carry `count` because `buildScripts/devCockpit.mjs` imports `localBearer.mjs` at two lines: a
key of file+specifier alone holds ONE member for both, so removing one occurrence would leave the key
present and the diff silent. That is the measured shape from a sibling baseline in this repo where 83
rows collapsed to 9 keys and deleting 63 of 64 still reported green.

## It asserts the property, not the list

Per the reviewer's request on the ticket: a new import in a file that is not in the baseline fails,
while all baselined rows stay clean. Red-proofed, alongside both ratchet directions and the
partial-burndown case.

Baseline rows carry the A/B classification from the ticket (engine tooling reaching a Brain service /
agent-serving script misfiled in an engine directory), plus a class C the ticket does not have: the
runtime crossing above, which is neither.

## Wiring, dictated by the repo's own lints rather than preference

`lint-guard-ci-parity` rejects a lint-staged guard with no CI mirror, because `--no-verify` skips the
hook and a merge commit stages files the author never touched. It resolves mirrors by parsed `run:`
full paths in a `*-lint.yml` workflow, so an npm alias does not count. The workflow runs no `npm ci`:
the guard reads `git ls-files` and source text, and a boundary guard that needed an install would be
the first thing dropped when the install broke.

* docs(build): the boundary guard's own JSDoc asserted the premise it falsified (#17239)

The module doc stated `src/**` is clean with zero crossings and that six sites cross — both written
before the AST rewrite found ten, one of them in `src/worker/App.mjs`. Shipping a guard whose
documentation asserts the claim its own output disproves is the defect the guard exists to catch,
one layer up.

Rewritten to record why the count moved 3 -> 6 -> 10 (every prior sweep was `from`-anchored and a
dynamic import has no `from`), and to state explicitly that the baseline exempts known debt rather
than being the assertion.

* fix(build): the boundary predicate resolved nothing — it matched text shape (#17256)

`../ai/Client.mjs` means different things depending on where it is written. From
`buildScripts/devCockpit.mjs` it resolves to the Brain. From `src/worker/App.mjs` it resolves to
`src/ai/Client.mjs` — the engine's own AI layer, the one sanctioned seam by which the Body connects
to the Brain, and unambiguously engine code.

The predicate matched `/^(?:\.\.\/)+ai\//` on the specifier alone, so it convicted the second case
and I published a confident, wrong claim that the engine runtime crosses the boundary. It does not.
`src/**` is clean and always was — which is exactly what the originating ticket said before I
'corrected' it.

Specifiers are now joined to the importing file's directory and normalised before anything is
decided. A path question answered by a text pattern stays wrong precisely where the answer matters.

Census after the fix: 9 crossings across 7 files, all under buildScripts/, zero in src/**. The one
thing the earlier hand-greps genuinely missed is devCockpit.mjs, whose three crossings are all
dynamic imports.

Specs now assert the pair directly: the same specifier from buildScripts/ fires and from src/worker/
does not, plus three src/ai/** consumers that must stay clean.

* fix(ci): the boundary workflow needs acorn — its own comment said otherwise (#17256)

The job skipped `npm ci` and stated the guard was dependency-free. True of the regex draft; false
from the acorn rewrite onward, and the comment outlived the change. CI failed with
ERR_MODULE_NOT_FOUND, which is the honest outcome.

Matches the atomic-write-shape sibling, which installs for the same reason: `npm ci --ignore-scripts`
plus npm caching, skipping the heavy prepare lifecycle.

Third instance this branch of prose outliving the code it describes — the module JSDoc, the success
message, and now a workflow comment. Each was accurate when written.

* fix(build): register the boundary lint's scan surface — scanned must be a subset of watched (#17256)

lintWorkflowScanRootParity failed: a new path-filtered lint workflow must register its scan surface,
and mine did not. A workflow whose paths filter is narrower than what its lint actually scans runs
late and misattributed — present, correct, and never triggered by the change that broke it.

Registered as imported rather than declared, the shape the spec prefers: the guard exports
SCAN_SURFACE and the registry takes it as SSOT, so the scanned set and the watched set are one
object rather than two lists that agree today. The runtime walk derives from it too, so a root
cannot be added to the walk without the workflow and the spec seeing it.

* fix(build): the boundary guard's only output named no line (#17256)

`tallyCrossings` collapses occurrences to {file, specifier, count} and drops
`line` on the way. The reporter then asked a tallied row for `entry.line`, so
the guard's single product -- the failure message -- printed the literal
`:undefined` where the location belongs.

Fifteen specs passed over it. Every one asserts the returned arrays, and `line`
IS asserted, on findBrainImports, one layer upstream of where it was lost. The
coverage boundary and the defect boundary were the same line.

Reported from the raw findings rather than by re-attaching one line to the tally:
a crossing with count 2 has two real locations and a tallied row can only name
one, so a developer sent to devCockpit.mjs would have found the second by hand.

Three specs on the reporter, red-proofed against the pre-fix shape. Plus the two
docstrings the census correction missed: both said six, the module docstring
already said nine.

Found by @neo-opus-grace on review, with a planted-crossing probe."

