---
id: 15057
title: 'Computed GP guard-filter ledger recording — the #14454 route-attribution record-seam'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-07-12T01:51:41Z'
updatedAt: '2026-08-10T20:15:00Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15057'
author: neo-opus-ada
commentsCount: 5
parentIssue: null
subIssues:
  - '[x] 15068 Computed GP guard-filter route-attribution telemetry — the live record-seam (AC1+AC2)'
  - '[x] 15076 Computed GP type-gate rejection record-seam — the AC3 evidence-window producer'
subIssuesCompleted: 2
subIssuesTotal: 2
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-10T20:15:00Z'
---
# Computed GP guard-filter ledger recording — the #14454 route-attribution record-seam

## Context

Split from #14609 behavior 2 (guard-filter ledger recording) per that ticket's own AC4 "each behavior lands separately reviewable … split PRs if any grows." #14609's substantive behaviors — behavior 1 (no-survivor focus-as-route, the never-empty floor) + behavior 3 (release-window config leaf, the release-gate-blind fix) — landed in their PR; this diagnostic half grew and separates cleanly.

## The Problem

Behavior 2 as scoped was "blocked candidates land in the #14454 route-attribution ledger (which candidates the guard filters, how often, under which focus reasons)." A V-B-A while implementing #14609 found the blocking reality: **there is no live code record-seam for the #14454 route-attribution ledger.** The only artifact is a static one-time committed dataset — `learn/agentos/measurements/golden-path-route-attribution-2026-07-02.md` (from #14454 / PR #14458) — and nothing in the routing / synthesizer flow *writes* a live route-attribution ledger (grep for `route-attribution` / `rejectionBucket` / `recordRoute` across `ai/` → zero writers). So wiring the guard-filtered candidates into "the ledger" is not a wire — it requires **first building the same-run record mechanism** the one-shot dataset was a manual snapshot of.

## The Architectural Reality

- `computedGoldenPathRouting.mjs` already exposes the exclusion vocabulary for exactly this — `getComputedRecommendationExclusionLabels()` is documented as "so diagnostic ledgers can report the exact rejection bucket." The consumer (a live ledger) is what's missing.
- `findComputedFocusContradiction()` already computes the guard's `blockedNodes`; the synthesizer branch that renders the no-survivor section (behavior 1) is the natural emit point.
- The 42.2% label/actionability disposition (Clio's folded-in residual from #14503) is **gated on this** — "once the route-attribution ledger carries a real window of guard-filtered candidates with focus-reason attribution, the disposition gets its evidence-based decision." Until the record-seam exists, that disposition stays a guess-number.

## The Fix

1. Establish the live same-run route-attribution record-seam (the mechanism the #14454 dataset was a one-shot of) — a real writer, not a committed static file.
2. Record the guard's `blockedNodes` + their focus reasons into it per synthesis run (which candidates the guard filters, how often, under which focus reasons).
3. Feed the accumulated window into the #14503 type-gate disposition decision.

## Acceptance Criteria

- [ ] A live route-attribution record-seam exists (writes per synthesis run, not a static committed dataset).
- [ ] Guard-filtered candidates are recorded with focus-reason attribution, measurably.
- [ ] The 42.2% type-gate disposition (#14503 residual) has an evidence window to decide against; the disposition is recorded (keep / narrow / rank-with-discount).

## Out of Scope

Behaviors 1 + 3 of #14609 (shipped). The release-window `publish.mjs` clearing follow-on (behavior 3's second half, tracked on #14609).

## Related

Parent behavior: #14609 (behaviors 1 + 3 shipped; this is its split behavior-2 half) · #14454 / PR #14458 (the one-time route-attribution dataset) · #14503 (the 42.2% type-gate disposition this unblocks) · #14472 (GP-v2 epic).

Origin Session ID: 01f4cc68-8b8e-43e6-b51c-55b4f421f4e0
Retrieval Hint: "GP guard-filter route-attribution ledger record-seam blocked candidates focus reasons 42.2% type-gate disposition"

## Timeline

- 2026-07-12T01:51:42Z @neo-opus-ada added the `enhancement` label
- 2026-07-12T01:51:42Z @neo-opus-ada added the `ai` label
- 2026-07-12T01:51:42Z @neo-opus-ada added the `architecture` label
- 2026-07-12T01:52:24Z @neo-opus-ada cross-referenced by PR #15058
### @neo-opus-ada - 2026-07-12T02:13:29Z

## Record-seam design resolved — the one open architecture question is answered (V-B-A 2026-07-12)

The body's Fix step 1 ("establish the live same-run record-seam") had one unresolved design question: **where/how does it durably write?** A V-B-A this turn settled it — the ticket is now a line-anchored buildable spec, and it flags the wrong-shape a fresh agent would otherwise reach for.

### Constraint discovered: the synthesizer runs in the orchestrator *daemon*, not a build script
`GoldenPathSynthesizer` is invoked from `ai/daemons/orchestrator/` (DreamService / pipeline / MaintenanceBackpressureService). It is a **runtime service**. Therefore the record-seam must **NOT** append to a git-tracked `learn/agentos/measurements/*.md` file — that is a runtime daemon mutating tracked docs (the anti-pattern). The #14454 dataset (`golden-path-route-attribution-2026-07-02.md`) was a one-shot *diagnostic-script* output; it is not the shape a live writer copies. This is exactly the wrong-shape a fresh implementer would reach for by pattern-matching the existing `.md`, so it is called out here.

### Follow the existing live-ledger primitive — do not invent one
`ai/services/memory-core/helpers/healEventLedgerStore.mjs` (279 lines) is the established live JSONL-ledger pattern to copy:
- `appendHealEvent(entry, {dir, now, triggerBytes, maxEvents})` → `appendFile(path, JSON.stringify(stamped) + '\n')` per event,
- `readHealLedger` / `pruneHealLedger` (retention rewrite) / `queryHealLedger` / `summarizeHealLedger`,
- `getHealLedgerFilePath(dir)` + `validateHealLedgerRetention(maxEvents, triggerBytes)`.

New helper: `ai/services/graph/helpers/routeAttributionLedgerStore.mjs` (or `ai/services/memory-core/helpers/`), mirroring that surface: `appendRouteAttribution(entry, opts)` / `readRouteAttributionLedger` / `pruneRouteAttributionLedger` / `queryRouteAttributionLedger`. Runtime data dir (same base the heal ledger resolves to), **not** git-tracked. Entry shape: `{blockedNodeId, focusReasons: string[], synthesisRunAt, exclusionLabels}` (exclusion labels from the already-exported `getComputedRecommendationExclusionLabels()`).

### Emit point — single, known site (fail-open)
`GoldenPathSynthesizer.mjs:1008` computes `focusContradiction` (via `findComputedFocusContradiction`); `blockedNodes` are in hand by `:1049`, and the no-survivor section renders at `:1110`. The emit call goes there, **fail-open**: a ledger-write failure must never abort a synthesis run (wrap + swallow-log, mirroring how the heal-ledger append is non-fatal). This is the sensitive part (a durable write in the runtime synthesis path) and is the reason AC1+AC2 land as their own reviewed increment rather than riding a large multi-lane turn.

### AC sequencing (unchanged, now with the seam resolved)
- **AC1+AC2** = the store helper (copy the heal-ledger shape, unit-tested in isolation) **plus** the fail-open emit at `GoldenPathSynthesizer.mjs:1110`. These land together so the store is non-speculative (it has its writer from the first commit — no dead infra).
- **AC3** (feed the #14503 42.2% type-gate disposition) genuinely follows once the ledger has accumulated a real window; it is naturally a second PR (there is nothing to decide against until data exists).

Ready to build against this spec — the architecture ambiguity is gone; what remains is the careful fail-open write, which wants its own focused increment.

— Ada (@neo-opus-ada) · Session 01f4cc68-8b8e-43e6-b51c-55b4f421f4e0


- 2026-07-12T02:16:30Z @neo-opus-ada assigned to @neo-opus-ada
### @neo-opus-ada - 2026-07-12T02:27:08Z

## AC1 landed — store primitive built + tested; AC2 (the ADR-0019-gated emit) is the next focused increment

**AC1 done** on `agent/15057-route-attribution-ledger` (commit `e544c59b1d`): `ai/services/graph/routeAttributionLedgerStore.mjs` — the live JSONL record-seam, mirroring `healEventLedgerStore` (pure edge I/O, injected clock, no config-SSOT read, no helper-owned retention default; fail-open reads; self-bounding prune only when the AiConfig boundary supplies the policy). `append` / `read` / `prune` / `summarize` / `query` + **12 hermetic specs green**. Runtime JSONL store, not a git-tracked `measurements/*.md`.

**No PR yet** — deliberately: a store with no writer is speculative until wired, so the PR opens once AC2 (the emit) lands, keeping it non-speculative for review.

**AC2 — the fail-open synthesis emit (exact spec, verified this turn):**
- **Single emit site:** `GoldenPathSynthesizer.mjs:1015`, right after `focusContradiction` / `routedTopNodes` are computed. `if (focusContradiction)` covers **both** guard branches — the partial-block ("Routing Guard: Filtered…" at :1050) and the total-block no-survivor render (:1110) — with one call, not two. The method is already `async`, so `await this.recordRouteAttribution(...)` fits; wrap fail-open (a ledger-write failure must never abort synthesis).
- **Record shape:** map `focusContradiction.blockedNodes` → `{blockedNodeId: item.node.id, focusReasons: <the live focus reasons that armed the guard>, exclusionLabels: getComputedRecommendationExclusionLabels(item.node), at: now}`.
- **AiConfig leaves (§critical_gate #10 — read ADR-0019 §3 first; do NOT rush):** a new leaf group (e.g. `orchestrator.goldenPath.routeAttributionLedger`) in `config.template.mjs`: `{dir: leaf(path.join(<daemonDataDir>, 'route-attribution'), 'NEO_…_DIR', 'string'), maxEvents: leaf(N, 'NEO_…_MAX_EVENTS', 'number'), pruneTriggerBytes: leaf(N, 'NEO_…_PRUNE_BYTES', 'number')}`. Read resolved leaves at the use site (aiConfig is in scope at the emit), pass into `appendRouteAttribution`; validate via `validateRouteAttributionRetention` at the boundary. Then re-materialize the gitignored `config.mjs` (`npm run prepare -- --migrate-config`). ADR-0019 traps to avoid: no module-load capture (read at use site), no defensive `?.`/`??`-default fail-open, no helper-local default.
- **Emit tests:** verify records are written per contradiction (both branches) with the right focus-reason attribution; verify fail-open (a throwing append does not break synthesis output).

**AC3** (feed the #14503 42.2% type-gate disposition) stays a separate PR — nothing to decide against until a window accumulates.

— Ada (@neo-opus-ada) · Session 01f4cc68-8b8e-43e6-b51c-55b4f421f4e0


- 2026-07-12T02:44:25Z @neo-opus-ada referenced in commit `a8f3126` - "feat(graph): wire the route-attribution record-seam emit — fail-open, both guard branches (#15057)

AC2 of the record-seam. GoldenPathSynthesizer now records which computed candidates
the routing contradiction guard filtered (BOTH the partial-block and no-survivor
branches) into the route-attribution ledger, per synthesis run:

- buildRouteAttributionRecords (pure): blockedNodes -> records tagged with the
  focus-reason union across the armed Current Focus candidates + the per-node
  exclusion bucket.
- recordRouteAttribution (fail-open boundary): reads the retention/dir leaves at
  the use site (read-at-use-site, no defensive optional-chaining, no threading),
  validates, passes them EXPLICITLY into the pure store helper; a write failure is
  swallow-logged, never aborts synthesis.
- New AiConfig leaves in the memory-core goldenPath* set: LedgerDir/MaxEvents/
  PruneTriggerBytes; config.mjs re-materialized (overlay, gitignored).

3 mapping/fail-safe specs green; 59 existing synthesizer specs unbroken; 12 store
specs green. Makes the seam non-speculative; the disposition-feed follows a window."
- 2026-07-12T02:44:27Z @neo-opus-ada cross-referenced by PR #15060
- 2026-07-12T02:55:48Z @neo-opus-ada referenced in commit `27e5319` - "fix(graph): restore destructuring block-align --fix mangled into a syntax error (#15057)

check-block-alignment --fix corrupted
  const {blockedNodes = [], focusCandidates = []} = focusContradiction, focusReasons = ...
— it choked on the = defaults INSIDE the destructuring pattern, ate '[], focusCandidates = []} ='
and produced 'const {blockedNodes = focusContradiction, ...' (SyntaxError 387:139). It shipped
because --fix runs in lint-staged AFTER the local test pass, so the aligned version was never
run locally. Rewritten as block-align-stable plain consts (Array.isArray guards replace the
destructuring defaults). Verified: node --check + 15 specs pass on the post-hook file."
- 2026-07-12T03:15:08Z @neo-opus-ada referenced in commit `f2bb6fe` - "fix(test): restore Synthesizer binding a replace_all clobbered from the main describe (#15057)

The AC2 commit removed 3 redundant per-test 'const Synthesizer = GoldenPathSynthesizer.constructor'
lines via replace_all — but that exact form also existed in the main describe's EXISTING tests, so
it deleted their bindings too. GoldenPathSynthesizer.spec.mjs:163 (the first user) threw
ReferenceError; mode:'serial' then SKIPPED the rest, surfacing in CI as a lone '1 failed' that masked
the cascade. Fixed by binding Synthesizer once in the main describe's beforeAll (mirrors the
hasCrossFamilyReview describe). Full spec: 50 passed / 0 failed / 0 skipped."
- 2026-07-12T03:49:40Z @neo-opus-ada cross-referenced by #15066
- 2026-07-12T04:05:40Z @neo-opus-ada referenced in commit `2fe9202` - "fix(graph): attribute only guard causes — armingReasons vs candidateReasons (#15057)

Cycle-2 RA-2 (@neo-gpt review on #15060). buildRouteAttributionRecords recorded the
UNION of all focus-candidate reasons — mis-attributing incidental co-reasons
(fresh-updated, agent-os) as guard triggers. Now it derives armingReasons from the SAME
getRoutingConflictReasons authority isRoutingConflictFocusCandidate predicates on
(incident/prio-zero), keeping the full diagnostic set separately as candidateReasons.
The store folds byArmingReason and queries by armingReasons. Exact incidental-reason
case: {incident, fresh-updated} -> armingReasons [incident], candidateReasons both. 15 passed."
- 2026-07-12T04:06:13Z @neo-opus-ada cross-referenced by #15068
- 2026-07-12T04:06:22Z @neo-opus-ada added sub-issue #15068
- 2026-07-12T04:11:20Z @neo-opus-ada referenced in commit `8423ec2` - "test(graph): hermetic route-attribution emit via injected ledger dir (#15057)

Cycle-2 RA-3 (@neo-gpt review on #15060). recordRouteAttribution now takes the ledger
dir + retention as params (the synthesis boundary reads the resolved AiConfig leaves at
its use site and passes them), making the real emit a testable seam without an aiConfig
read of its own — validation + fail-open stay inside the method. New synthesis specs pin
it hermetically with a per-test temp dir: non-empty writes for partial-block + no-survivor
(with correct armingReasons attribution), a real write failure (mkdir under a file) does
not throw so synthesis is unaffected, and nothing touches the production .neo-ai-data
default (records provably land in the injected dir). 5 passed."
### @neo-opus-ada - 2026-07-12T04:23:05Z

## AC3 (the #14503 type-gate disposition) — seam located, sequenced after #15060 merges

Per @neo-gpt's #15060 review: the guard-filter record-seam (AC1+AC2, PR #15060 → #15068) records the routing-**contradiction guard**'s filtering, NOT the **actionability type-gate**'s rejections. AC3 needs the type-gate rejections recorded at their real filter point, then an accumulated window before the 42.2% disposition (keep / narrow / rank-with-discount) can be decided. V-B-A this turn located the seam and the sequencing constraint:

### The type-gate rejection filter point (production-reachable)
`GoldenPathSynthesizer.mjs:1042` — inside the scoring loop that builds `scoredNodes`:
```
if (!this.constructor.isActionableComputedRecommendation(nodeData || {id: issueId})) {
    logger.debug(`[GoldenPathSynthesizer] Skipping non-actionable computed recommendation: ${issueId}`);
    continue;   // <-- the type-gate rejection: THIS is where AC3's evidence is produced
}
```
The rejection bucket is already available from the shared authority: `getComputedRecommendationExclusionLabels(nodeData)` (the exact `epic` / `not-code-ready` / … labels that made it visibility-only). So the record shape is `{nodeId: issueId, rejectionBucket: <exclusion labels>, stage: 'actionability-type-gate', at}`.

### Buildable shape (once unblocked)
- Reuse the `routeAttributionLedgerStore` primitive with a `stage` field on the record (`'contradiction-guard'` vs `'actionability-type-gate'`) so the two filter points share one store but stay queryable-apart — OR a sibling `typeGateRejectionLedgerStore` if separation is cleaner at review. `summarize`/`query` gain a `stage` dimension.
- Fail-open emit at `:1042` (mirror `recordRouteAttribution`'s injected-dir seam so it stays hermetically testable), config leaves via the same ADR-0019 pattern.
- Then the accumulated window (post-deploy) feeds the #14503 keep/narrow/rank-with-discount disposition.

### Sequencing (the real dependency — why AC3 is not built now)
AC3's producer builds directly on the `routeAttributionLedgerStore` + the `recordRouteAttribution` injected-dir seam that **PR #15060 introduces and is still in cycle-2 review**. Building AC3 now would stack on an unmerged PR whose surface may still move in cycle-3. So AC3 is sequenced **after #15060 merges** — at which point it is a small, well-specified increment against this spec (record at `:1042`, `stage` field, fail-open emit, config leaves). #15057 stays open as its tracking contract.

— Ada (@neo-opus-ada) · Session 01f4cc68-8b8e-43e6-b51c-55b4f421f4e0


- 2026-07-12T04:50:05Z @neo-opus-ada referenced in commit `7eb9c8e` - "feat(graph): route-attribution ledger store — the live #14454 record-seam primitive (#15057)

The #14454 route-attribution ledger had no live writer: its only artifact was a
one-shot committed measurement dataset. This adds the durable append-only JSONL
store that the computed-GP contradiction guard's blocked-candidate stream writes
to per synthesis run — mirroring the healEventLedgerStore primitive (pure edge
I/O, injected clock, no config-SSOT read, no helper-owned retention default;
fail-open reads, self-bounding prune only when the AiConfig boundary supplies the
policy). summarize/query fold the stream into the evidence surface the #14503
type-gate disposition reads (blocked-node counts, by focus reason, by exclusion
bucket). GoldenPathSynthesizer runs in the orchestrator daemon (runtime), so this
is a JSONL store in a runtime state dir, never a git-tracked measurements/*.md.

Store primitive + 12 hermetic specs; the fail-open synthesis emit lands next."
- 2026-07-12T04:50:05Z @neo-opus-ada referenced in commit `ce4d2e1` - "feat(graph): wire the route-attribution record-seam emit — fail-open, both guard branches (#15057)

AC2 of the record-seam. GoldenPathSynthesizer now records which computed candidates
the routing contradiction guard filtered (BOTH the partial-block and no-survivor
branches) into the route-attribution ledger, per synthesis run:

- buildRouteAttributionRecords (pure): blockedNodes -> records tagged with the
  focus-reason union across the armed Current Focus candidates + the per-node
  exclusion bucket.
- recordRouteAttribution (fail-open boundary): reads the retention/dir leaves at
  the use site (read-at-use-site, no defensive optional-chaining, no threading),
  validates, passes them EXPLICITLY into the pure store helper; a write failure is
  swallow-logged, never aborts synthesis.
- New AiConfig leaves in the memory-core goldenPath* set: LedgerDir/MaxEvents/
  PruneTriggerBytes; config.mjs re-materialized (overlay, gitignored).

3 mapping/fail-safe specs green; 59 existing synthesizer specs unbroken; 12 store
specs green. Makes the seam non-speculative; the disposition-feed follows a window."
- 2026-07-12T04:50:05Z @neo-opus-ada referenced in commit `a680544` - "fix(graph): restore destructuring block-align --fix mangled into a syntax error (#15057)

check-block-alignment --fix corrupted
  const {blockedNodes = [], focusCandidates = []} = focusContradiction, focusReasons = ...
— it choked on the = defaults INSIDE the destructuring pattern, ate '[], focusCandidates = []} ='
and produced 'const {blockedNodes = focusContradiction, ...' (SyntaxError 387:139). It shipped
because --fix runs in lint-staged AFTER the local test pass, so the aligned version was never
run locally. Rewritten as block-align-stable plain consts (Array.isArray guards replace the
destructuring defaults). Verified: node --check + 15 specs pass on the post-hook file."
- 2026-07-12T04:50:05Z @neo-opus-ada referenced in commit `45402ef` - "fix(test): restore Synthesizer binding a replace_all clobbered from the main describe (#15057)

The AC2 commit removed 3 redundant per-test 'const Synthesizer = GoldenPathSynthesizer.constructor'
lines via replace_all — but that exact form also existed in the main describe's EXISTING tests, so
it deleted their bindings too. GoldenPathSynthesizer.spec.mjs:163 (the first user) threw
ReferenceError; mode:'serial' then SKIPPED the rest, surfacing in CI as a lone '1 failed' that masked
the cascade. Fixed by binding Synthesizer once in the main describe's beforeAll (mirrors the
hasCrossFamilyReview describe). Full spec: 50 passed / 0 failed / 0 skipped."
- 2026-07-12T04:50:05Z @neo-opus-ada referenced in commit `679e670` - "fix(graph): attribute only guard causes — armingReasons vs candidateReasons (#15057)

Cycle-2 RA-2 (@neo-gpt review on #15060). buildRouteAttributionRecords recorded the
UNION of all focus-candidate reasons — mis-attributing incidental co-reasons
(fresh-updated, agent-os) as guard triggers. Now it derives armingReasons from the SAME
getRoutingConflictReasons authority isRoutingConflictFocusCandidate predicates on
(incident/prio-zero), keeping the full diagnostic set separately as candidateReasons.
The store folds byArmingReason and queries by armingReasons. Exact incidental-reason
case: {incident, fresh-updated} -> armingReasons [incident], candidateReasons both. 15 passed."
- 2026-07-12T04:50:06Z @neo-opus-ada referenced in commit `2c3b5c9` - "test(graph): hermetic route-attribution emit via injected ledger dir (#15057)

Cycle-2 RA-3 (@neo-gpt review on #15060). recordRouteAttribution now takes the ledger
dir + retention as params (the synthesis boundary reads the resolved AiConfig leaves at
its use site and passes them), making the real emit a testable seam without an aiConfig
read of its own — validation + fail-open stay inside the method. New synthesis specs pin
it hermetically with a per-test temp dir: non-empty writes for partial-block + no-survivor
(with correct armingReasons attribution), a real write failure (mkdir under a file) does
not throw so synthesis is unaffected, and nothing touches the production .neo-ai-data
default (records provably land in the injected dir). 5 passed."
- 2026-07-12T04:50:06Z @neo-opus-ada referenced in commit `0335a6f` - "fix(graph): cycle-3 — drop phantom exclusion-label dimension + test-isolate synthesis ledger (#15057)

Cycle-2 RC (@neo-gpt on #15060).

RA-1 (attribute only populated dimensions): every guard-blocked node ALREADY passed the
actionability type-gate (predicate: getComputedRecommendationExclusionLabels().length === 0),
so its exclusion set is empty by construction — exclusionLabels was a phantom dimension (exact
runtime row: exclusionLabels []). Removed it from the record + byExclusionLabel from summarize +
the exclusionLabels query filter; the guard-filter producer now describes only what it populates
(armingReasons, blockedNodeId, candidateReasons). The rejection-bucket belongs to the type-gate producer.

RA-3 (hermetic synthesis emit): the direct recordRouteAttribution seam tests used temp dirs but
did not drive synthesizeGoldenPath, so the synthesis specs still wrote the prod .neo-ai-data ledger.
Fixed by construction: goldenPathRouteAttributionLedgerDir is now a Prod/Test formula gated on
UNIT_TEST_MODE (mirrors storagePaths.graph / handoffFilePath), so synthesis-triggered emits resolve
to an OS-temp dir under test. Proof test asserts the resolved dir is the test path, not .neo-ai-data.

Rebased onto dev (merged #15058 touched the same synthesizer/routing/spec surfaces). 18 specs green."
- 2026-07-12T05:17:15Z @neo-opus-ada cross-referenced by #15072
- 2026-07-12T05:19:23Z @tobiu referenced in commit `539c900` - "feat(graph): computed-GP guard-filter route-attribution telemetry — record-seam (#15068) (#15060)

* feat(graph): route-attribution ledger store — the live #14454 record-seam primitive (#15057)

The #14454 route-attribution ledger had no live writer: its only artifact was a
one-shot committed measurement dataset. This adds the durable append-only JSONL
store that the computed-GP contradiction guard's blocked-candidate stream writes
to per synthesis run — mirroring the healEventLedgerStore primitive (pure edge
I/O, injected clock, no config-SSOT read, no helper-owned retention default;
fail-open reads, self-bounding prune only when the AiConfig boundary supplies the
policy). summarize/query fold the stream into the evidence surface the #14503
type-gate disposition reads (blocked-node counts, by focus reason, by exclusion
bucket). GoldenPathSynthesizer runs in the orchestrator daemon (runtime), so this
is a JSONL store in a runtime state dir, never a git-tracked measurements/*.md.

Store primitive + 12 hermetic specs; the fail-open synthesis emit lands next.

* feat(graph): wire the route-attribution record-seam emit — fail-open, both guard branches (#15057)

AC2 of the record-seam. GoldenPathSynthesizer now records which computed candidates
the routing contradiction guard filtered (BOTH the partial-block and no-survivor
branches) into the route-attribution ledger, per synthesis run:

- buildRouteAttributionRecords (pure): blockedNodes -> records tagged with the
  focus-reason union across the armed Current Focus candidates + the per-node
  exclusion bucket.
- recordRouteAttribution (fail-open boundary): reads the retention/dir leaves at
  the use site (read-at-use-site, no defensive optional-chaining, no threading),
  validates, passes them EXPLICITLY into the pure store helper; a write failure is
  swallow-logged, never aborts synthesis.
- New AiConfig leaves in the memory-core goldenPath* set: LedgerDir/MaxEvents/
  PruneTriggerBytes; config.mjs re-materialized (overlay, gitignored).

3 mapping/fail-safe specs green; 59 existing synthesizer specs unbroken; 12 store
specs green. Makes the seam non-speculative; the disposition-feed follows a window.

* fix(graph): restore destructuring block-align --fix mangled into a syntax error (#15057)

check-block-alignment --fix corrupted
  const {blockedNodes = [], focusCandidates = []} = focusContradiction, focusReasons = ...
— it choked on the = defaults INSIDE the destructuring pattern, ate '[], focusCandidates = []} ='
and produced 'const {blockedNodes = focusContradiction, ...' (SyntaxError 387:139). It shipped
because --fix runs in lint-staged AFTER the local test pass, so the aligned version was never
run locally. Rewritten as block-align-stable plain consts (Array.isArray guards replace the
destructuring defaults). Verified: node --check + 15 specs pass on the post-hook file.

* fix(test): restore Synthesizer binding a replace_all clobbered from the main describe (#15057)

The AC2 commit removed 3 redundant per-test 'const Synthesizer = GoldenPathSynthesizer.constructor'
lines via replace_all — but that exact form also existed in the main describe's EXISTING tests, so
it deleted their bindings too. GoldenPathSynthesizer.spec.mjs:163 (the first user) threw
ReferenceError; mode:'serial' then SKIPPED the rest, surfacing in CI as a lone '1 failed' that masked
the cascade. Fixed by binding Synthesizer once in the main describe's beforeAll (mirrors the
hasCrossFamilyReview describe). Full spec: 50 passed / 0 failed / 0 skipped.

* fix(graph): attribute only guard causes — armingReasons vs candidateReasons (#15057)

Cycle-2 RA-2 (@neo-gpt review on #15060). buildRouteAttributionRecords recorded the
UNION of all focus-candidate reasons — mis-attributing incidental co-reasons
(fresh-updated, agent-os) as guard triggers. Now it derives armingReasons from the SAME
getRoutingConflictReasons authority isRoutingConflictFocusCandidate predicates on
(incident/prio-zero), keeping the full diagnostic set separately as candidateReasons.
The store folds byArmingReason and queries by armingReasons. Exact incidental-reason
case: {incident, fresh-updated} -> armingReasons [incident], candidateReasons both. 15 passed.

* test(graph): hermetic route-attribution emit via injected ledger dir (#15057)

Cycle-2 RA-3 (@neo-gpt review on #15060). recordRouteAttribution now takes the ledger
dir + retention as params (the synthesis boundary reads the resolved AiConfig leaves at
its use site and passes them), making the real emit a testable seam without an aiConfig
read of its own — validation + fail-open stay inside the method. New synthesis specs pin
it hermetically with a per-test temp dir: non-empty writes for partial-block + no-survivor
(with correct armingReasons attribution), a real write failure (mkdir under a file) does
not throw so synthesis is unaffected, and nothing touches the production .neo-ai-data
default (records provably land in the injected dir). 5 passed.

* fix(graph): cycle-3 — drop phantom exclusion-label dimension + test-isolate synthesis ledger (#15057)

Cycle-2 RC (@neo-gpt on #15060).

RA-1 (attribute only populated dimensions): every guard-blocked node ALREADY passed the
actionability type-gate (predicate: getComputedRecommendationExclusionLabels().length === 0),
so its exclusion set is empty by construction — exclusionLabels was a phantom dimension (exact
runtime row: exclusionLabels []). Removed it from the record + byExclusionLabel from summarize +
the exclusionLabels query filter; the guard-filter producer now describes only what it populates
(armingReasons, blockedNodeId, candidateReasons). The rejection-bucket belongs to the type-gate producer.

RA-3 (hermetic synthesis emit): the direct recordRouteAttribution seam tests used temp dirs but
did not drive synthesizeGoldenPath, so the synthesis specs still wrote the prod .neo-ai-data ledger.
Fixed by construction: goldenPathRouteAttributionLedgerDir is now a Prod/Test formula gated on
UNIT_TEST_MODE (mirrors storagePaths.graph / handoffFilePath), so synthesis-triggered emits resolve
to an OS-temp dir under test. Proof test asserts the resolved dir is the test path, not .neo-ai-data.

Rebased onto dev (merged #15058 touched the same synthesizer/routing/spec surfaces). 18 specs green.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-07-12T05:20:27Z @neo-opus-ada referenced in commit `f93a6d4` - "fix(build): block-alignment --fix corrupts destructuring-with-defaults (#15072)

The aligner located the assignment `=` with `line.indexOf('=')` (the first `=`),
which for a defaulted destructuring LHS (`{blockedNodes = []}`) is the default
inside the pattern, not the assignment operator — so `--fix` sliced mid-pattern
and emitted a SyntaxError. It shipped green locally (lint-staged runs the
check-only `--staged` mode; an author runs `--fix` manually after local tests)
and red only in CI, where every importing spec threw (PR #15060 / #15057).

Two-part fix:
1. DECL_BINDING excludes `=` from its {…}/[…] branches, so a defaulted
   destructuring LHS no longer matches as a declaration — it breaks the run and
   is left byte-for-byte untouched. Default-free `{record}` still aligns (the
   #13908 behaviour is preserved).
2. New buildScripts/util/check-parse.mjs, wired last in the *.mjs lint-staged
   chain: node --check every staged .mjs, fail the commit if any no longer
   parses — a durable backstop for any mechanical fixer's future edge cases,
   at the exact point (commit) where the wasted CI cycle was incurred.

Verified: the exact repro survives --fix byte-identical and node --check-clean;
check mode reports no drift on it; #13908 preserved; 30 unit tests green
(check-block-alignment.spec + new check-parse.spec); lint-staged dry-run blocks
a corrupted staged file at check-parse. All 2587 tracked .mjs pass node --check."
- 2026-07-12T05:22:43Z @neo-opus-ada cross-referenced by PR #15073
- 2026-07-12T05:35:53Z @tobiu referenced in commit `bf19e22` - "fix(build): block-alignment --fix corrupts destructuring-with-defaults (#15072) (#15073)

The aligner located the assignment `=` with `line.indexOf('=')` (the first `=`),
which for a defaulted destructuring LHS (`{blockedNodes = []}`) is the default
inside the pattern, not the assignment operator — so `--fix` sliced mid-pattern
and emitted a SyntaxError. It shipped green locally (lint-staged runs the
check-only `--staged` mode; an author runs `--fix` manually after local tests)
and red only in CI, where every importing spec threw (PR #15060 / #15057).

Two-part fix:
1. DECL_BINDING excludes `=` from its {…}/[…] branches, so a defaulted
   destructuring LHS no longer matches as a declaration — it breaks the run and
   is left byte-for-byte untouched. Default-free `{record}` still aligns (the
   #13908 behaviour is preserved).
2. New buildScripts/util/check-parse.mjs, wired last in the *.mjs lint-staged
   chain: node --check every staged .mjs, fail the commit if any no longer
   parses — a durable backstop for any mechanical fixer's future edge cases,
   at the exact point (commit) where the wasted CI cycle was incurred.

Verified: the exact repro survives --fix byte-identical and node --check-clean;
check mode reports no drift on it; #13908 preserved; 30 unit tests green
(check-block-alignment.spec + new check-parse.spec); lint-staged dry-run blocks
a corrupted staged file at check-parse. All 2587 tracked .mjs pass node --check.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-07-12T05:49:59Z @neo-opus-ada cross-referenced by #14477
- 2026-07-12T06:11:39Z @neo-opus-ada cross-referenced by #15076
- 2026-07-12T06:11:47Z @neo-opus-ada added sub-issue #15076
- 2026-07-12T06:12:30Z @neo-opus-ada cross-referenced by PR #15077
- 2026-07-12T09:15:57Z @tobiu referenced in commit `65233c6` - "feat(graph): type-gate rejection producer — the AC3 record-seam (#15057) (#15077)

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
### @neo-opus-ada - 2026-07-12T12:09:03Z

## State verification (@neo-opus-ada) — producer COMPLETE + merged; the sole open item is the AC3 post-deploy disposition (stays here, do NOT close)

V-B-A'd the full state this turn so the ticket's true status is unambiguous (the 2/2 sub-count alone would mis-signal "done"):

**Producer goal — DONE, merged to dev:**
- **AC1 + AC2** (live record-seam + fail-open synthesis emit, both guard branches) → sub-issue #15068, PR #15060, merged (commit `539c900`). `routeAttributionLedgerStore.mjs` + `recordRouteAttribution`, cycle-3 RC-clean (phantom exclusion-label dimension dropped, armingReasons-vs-candidateReasons attribution, hermetic UNIT_TEST_MODE-gated dir).
- **AC3 producer** (type-gate rejection record-seam, the `:1042` evidence-window emit) → sub-issue #15076, PR #15077, merged (commit `65233c6`).

**Sole remaining item — AC3 *disposition* (post-deploy, data-dependent):** "the #14503 42.2% type-gate disposition is recorded (keep / narrow / rank-with-discount)." This **cannot be decided now** — it needs an accumulated production window of route-attribution + type-gate-rejection data from the now-live seams. It is genuinely open.

**Why this ticket stays open (do not close on the 2/2 sub-count):** the disposition residual was re-sited #14503 → #14609 → here. **#14503 is CLOSED** (superseded 2026-07-04, Clio's fold-in), so there is NO upstream ticket to hand the disposition to — it lives on #15057 as its tracking contract. Closing now would orphan the post-deploy AC3 decision.

**Watch-note / next action (post-deploy):** once the ledger has a real window, decide the #14503 42.2% disposition (keep/narrow/rank-with-discount) from the measured data and record it here; that closes AC3 and the ticket. No code lane is drivable until the window accumulates.

— Ada (@neo-opus-ada, Claude Opus 4.8). Origin session `01f4cc68-8b8e-43e6-b51c-55b4f421f4e0`.

- 2026-07-12T19:55:37Z @neo-gpt cross-referenced by #15105
- 2026-07-13T00:35:23Z @tobiu referenced in commit `f8c0401` - "feat(memory-core): register explore_memory_history — the Memory/session temporal Bird View runtime tool (#14435) (#15096)

* feat(memory-core): temporal-pyramid window resolver — the pure half-open synthesis front (#14435)

First increment of the L3-L5 dynamic-synthesis path (Leaf C of the temporal pyramid).
resolveTemporalWindow turns a (partition, preset | explicit windowStart/windowEnd) request
into a validated half-open [windowStart, windowEnd) interval with a declared filter-set:

- named grains (weekly/monthly/quarterly = L3/L4/L5) + finer arbitrary grains (daily/3-day)
  + explicit windows all resolve through ONE path; an arbitrary (windowStart, windowEnd) is
  first-class, not a retrofit.
- half-open boundaries keep adjacent windows non-overlapping (an event at windowEnd opens
  the next window, never both).
- the reference clock is INJECTED (now), never read internally — a run is deterministic and
  replayable from its resolved window.
- filterSet (grain + partition) rides every result: cross-window comparison is defined only
  within an identical set.
- fails loud on every ambiguous/invalid request — never a silently-wrong window.

Pure + storage/inference-free; writes nothing (the no-durable-above-L2 contract). 13 hermetic
specs green. The retrieval + cited-synthesis + notAuthority-envelope increments follow; the PR
stays un-merge-ready until the temporal-pyramid ADR is Accepted at the human merge gate.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): temporal Bird View envelope — the notAuthority coverage/citation discipline (#14435)

Second increment of the L3-L5 dynamic-synthesis path. buildTemporalBirdViewEnvelope wraps a
resolved window + retrieved sources into the honest, non-authoritative result:

- completeness is PROVEN, never assumed: synthesisAvailable is true ONLY when totalResolved is
  finite, excluded===0, nothing truncated, not flagged degraded, AND a narrative exists. Any gap
  (unknown total → coverage-unknown, truncation → source-truncated, excluded remainder →
  incomplete-inclusion, explicit flag → flagged-degraded) WITHHOLDS the narrative and records the
  exact degradedReason — the 'no confident narrative without complete coverage' contract.
- notAuthority:true stamped on every envelope; per-source citations (type/id/ref) preserved for
  get_conversation-style drill-down; deterministic order-independent FNV-1a sourceManifestHash for
  provenance/change-detection.
- generatedAt is injected (never a clock read); pure, writes nothing (no-durable-above-L2).

10 hermetic specs green. Retrieval orchestration + cited synthesis + the MC query entry follow;
PR stays un-merge-ready until the temporal-pyramid ADR is Accepted at the human merge gate.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): temporal Bird View synthesizer — fail-open composition, no inference over incomplete coverage (#14435)

Third increment: the composition core tying resolve -> retrieve -> synthesize -> envelope, with the
two impure legs (retrieve, synthesize) as INJECTED seams (the #15057 injected-dependency pattern) so
it is deterministic + hermetically testable while the real Memory Core / inference adapters plug in.

Two disciplines enforced here, not trusted to callers:
- fail-OPEN: a retrieval or synthesis error degrades THIS Bird View (a coverage-bearing degraded
  envelope with the failure reason), never throws out of the synthesis. An invalid WINDOW request is
  the one exception — a caller error with no honest window to report against — and propagates.
- no inference over incomplete coverage: the LLM synthesis leg is SKIPPED when retrieval reports
  degraded/incomplete coverage (the envelope withholds the narrative anyway, so a partial read must
  not incur or pay for a synthesis call). Verified by a spy: synthesize is never called on a gap.

Matches @neo-gpt's #14435 peer-role convergence gate: the deterministic envelope is built BEFORE any
model synthesis; the coverage manifest survives an inference failure. Retrieve stays the injected seam
where the chronological-session-exhaustion spine (not query_raw_memories) will plug in.

7 hermetic specs green (33 across the 3 increments). Pure composition; writes nothing.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): chronological window-source spine — recency-exhaustion completeness backbone (#14435)

Fourth increment: enumerateChronologicalWindowSources — the completeness SPINE of the retrieve leg
per @neo-gpt's #14435 convergence gate (chronological exhaustion, NOT a semantic sample). Walks the
recency cursor to exhaustion across the partition, keeps only the half-open-window items, cross-agent
de-duplicates, and reports honest coverage:

- completeness is EARNED by exhaustion: a partition is complete only when every walked identity's
  cursor reached its end (or paged strictly past the window start) within the runaway cap, with no
  fetch failure. A cap hit or a fetch error → coverage.degraded with a reason, so the envelope
  withholds the narrative — a partial recency walk never masquerades as 'everything that happened'.
- early-stop: once a page's OLDEST item predates windowStart, no older page can re-enter the window,
  so the walk stops (no over-paging) — verified by a fetch counter.
- half-open filter (windowStart inclusive, windowEnd exclusive); cross-agent de-dup by id for unified.
- fail-open per identity: a lost cursor degrades the manifest, never throws — sources already
  enumerated survive.

The page fetch is INJECTED (fetchPage), so the pagination/filter/de-dup logic is pure + hermetically
tested (8 specs) while the real query_recent_turns adapter plugs into the same seam. This feeds the
synthesizer's retrieve; query_raw_memories enrichment + the explore_memory_history MCP entry follow.
41 specs across the 5 modules; writes nothing.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): citation prominence — high-impact direct-cite fidelity, prominence never admission (#14435)

Fifth increment: the temporal-pyramid fidelity layer per @neo-gpt's #14435 convergence #2 (prominence,
NOT admission). annotateCitationProminence / partitionByProminence mark which enumerated sources the
concise narrative foregrounds + direct-cites, WITHOUT changing which are admitted:

- high-impact sessions (>= the direct-cite threshold) and accepted ADRs cite directly.
- a PR earns prominence only through a NAMED fidelity marker (accepted-ADR link, epic label, or
  high-impact review metadata) — never an invented universal PR impact score (a raw numeric impact on
  a PR confers nothing, asserted).
- the load-bearing invariant: prominent + context === the full admitted manifest — nothing is dropped,
  so a low-impact session is never silently removed from the window it belongs to (admission != emphasis).

Pure; 5 hermetic specs. Full temporal suite now 43 specs green across the 5 pure/composition modules
(resolver, envelope, synthesizer, chronological spine, prominence). The impure adapters — the real
recency fetchPage, semantic enrichment, the LLM synthesize, and the explore_memory_history MCP entry
composing them — follow, then no-durable-write + cost tests + cross-family review.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): recent-turns fetchPage adapter — the query_recent_turns recency-leg seam (#14435)

Sixth increment, the first impure adapter: makeRecentTurnsFetchPage binds Memory Core's queryRecentTurns
behind the fetchPage({identity, cursor}) closure the chronological spine walks.

- maps reverse-chronological turns -> {id, timestamp, type:'turn'} items; passes the compound {timestamp,id}
  cursor back as 'before'; nextCursor passthrough (null at exhaustion).
- queryRecentTurns signals failure by RETURNING {error, message} (it never throws — a WAL read must not
  veto a turn save); the adapter converts that to a THROW so the spine's per-identity catch degrades the
  coverage manifest rather than treating an errored page as exhausted.
- injected queryRecentTurns keeps the mapping hermetically testable; an integration spec drives the real
  spine (enumerateChronologicalWindowSources) to exhaustion over two paged turns through this adapter.

5 specs (incl. the adapter+spine integration). The real caller passes MemoryService.queryRecentTurns.
Remaining: query_raw_memories enrichment, the LLM synthesize (partitionByProminence prompt), and the
explore_memory_history MCP entry composing resolver->spine+enrichment->synthesize->envelope, then
no-durable-write + cost tests + cross-family review.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): semantic enrichment adapter — best-effort themes, never the coverage backbone (#14435)

Seventh increment: makeSemanticEnrichment binds queryMemories (relevance-ranked semantic search) into a
best-effort enrich({query}) that recovers what a window was ABOUT (themes to foreground), never what it
CONTAINED. Per @neo-gpt's #14435 convergence #3 (query_raw_memories = enrichment, never completeness):

- capped + relevance-ranked, so it can never prove window completeness. Its degraded flag is ENTIRELY
  SEPARATE from the chronological coverage spine — an enrichment failure means fewer themes, and must
  never withhold the narrative, degrade coverage, or make a complete window look incomplete.
- best-effort throughout: a thrown search, an {error} envelope, or a missing query each yield empty themes
  with their own reason, never a throw. An empty result set is a clean (non-degraded) enrichment.
- passes the raw relevance-ranked records through tagged type:'memory' (no assumption about record shape;
  the synthesis prompt foregrounds fields).

6 specs. 54 across the 7 modules. The LLM synthesize adapter (partitionByProminence prompt over the spine
sources + these themes) and the explore_memory_history MCP entry composing it all are the remaining
integration; then no-durable-write + cost tests + cross-family review.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): temporal synthesis prompt + generate seam — fidelity baked in, not model discretion (#14435)

Eighth increment: buildTemporalSynthesisPrompt (pure) + makeTemporalSynthesize (injected-generate adapter).
The fidelity rules are in the PROMPT, not left to the model:

- prominent sources (high-impact sessions / accepted ADRs / named-marker PRs, via partitionByProminence)
  are foregrounded and must be cited directly by id; context sources are given as a bounded COUNT so the
  prompt caps density without dropping admission (every source stays in the coverage manifest elsewhere).
- the model may use ONLY the provided sources + best-effort themes — no invented events, no outside
  knowledge; thin evidence must be stated plainly, not embellished.
- half-open window + partition stated verbatim; prominent/theme lists bounded (40/20).
- a generation yielding no narrative THROWS, so the orchestrator degrades the envelope rather than
  returning an empty 'what happened'.

Prompt builder is hermetically testable; generate stays a seam (real caller passes the provider). 5 specs.
59 across 8 modules. Remaining: the explore_memory_history MCP entry composing resolver -> spine
(recentTurnsFetchPage) + enrichment -> synthesize -> envelope, its identity roster, and MC-server
registration; then no-durable-write + cost tests + cross-family review.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): exploreMemoryHistory composition — the full Memory/session Bird View, 8 primitives end-to-end (#14435)

Ninth increment, the composition keystone: exploreMemoryHistory wires all eight tested primitives —
resolver -> recency spine (recentTurnsFetchPage) -> best-effort enrichment -> fidelity synthesis -> the
notAuthority envelope — into the logic behind the explore_memory_history runtime operation, kept separate
from its MCP-server registration so the whole path is dependency-injected and hermetically testable.

- all Memory Core / inference touchpoints injected via deps (queryRecentTurns, queryMemories, generate,
  listIdentities) — no global reach-in.
- unified walks the live identity roster + cross-agent de-dups; @<identity> walks exactly one and never
  consults the roster (asserted).
- coverage is the recency spine's ALONE: a spine failure degrades the envelope and the LLM is never called
  (asserted); an enrichment failure does NOT block synthesis (themes are best-effort, coverage intact —
  asserted).
- fails loud when a dep is missing.

5 end-to-end specs (the composition proof). 64 across 9 modules; writes nothing. The ONLY remaining piece
is the impure MCP-server registration (openapi + toolService dispatch + Server.mjs wiring binding the real
MemoryService.queryRecentTurns/queryMemories + provider generate + roster), then no-durable-write + cost
tests + cross-family review. @neo-gpt's #14435 contract is fully converged.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): chat-model generate adapter — the buildChatModel -> synthesis seam (#14435)

Tenth increment: makeChatModelGenerate binds the deployment-agnostic model builder into the
generate({prompt}) => string seam the temporal synthesis adapter calls. The model BUILDER is injected so
provider selection (local gemma via openAiCompatible/ollama, or remote gemini) stays the modelProvider
reactive-Provider SSOT's job and this stays hermetically testable.

- calls model.generateContent(prompt, {timeoutMs, operationLabel, priority}) and extracts result.response.text()
  (the uniform buildChatModel model contract, confirmed against MemoryService.buildMiniSummary).
- fails LOUD where a write-path summary fails soft: a null model (no configured provider, e.g. gemini
  without an API key) or an empty/absent completion throws, so the Bird View orchestrator degrades the
  envelope rather than emitting an empty 'what happened'.

5 specs. 69 across 10 modules. The registration handler wires this via buildModel: () =>
buildChatModel({modelProvider: aiConfig.modelProvider, ...}). Remaining: a public identity roster method
+ the explore_memory_history serviceMapping handler + openapi schema + no-durable-write/cost tests + PR.

Co-Authored-By: Ada <ada@neo-opus-ada.ai>

* feat(memory-core): register explore_memory_history MCP tool — wire the Bird View composition (#14435)

The temporal Bird View logic (exploreMemoryHistory — 10 pure/injected increments, 69 specs) binds its
impure edges here at the MC server. The openapi /memories/history operation (operationId
explore_memory_history, read tier; params partition/preset/windowStart/windowEnd) is what ToolService
iterates to register + list the tool; the serviceMapping handler wires the deps: recency spine + semantic
enrichment via MemoryService, synthesis via buildChatModel over the modelProvider reactive-Provider SSOT
(read at use-site), and the unified roster as the union of the who-is-online buckets. Zero durable output
by construction — no write dep is injected; the injected real clock keeps the composition deterministic.
Verified: openapi parses + operationId parity; toolService parses + block-aligned; dep signatures
(buildChatModel, whoIsOnline terse shape, AiConfig leaves) confirmed against source.

* fix(memory-core): preserve impact/summary in the recency fetch-page adapter (#14435)

Addresses @neo-gpt's #15096 truth-path RC item 1: the adapter mapped turns to a minimal {id, timestamp, type}, dropping the impact/summary the downstream synthesis + citation-prominence read — forcing the narrative to reconstruct from ids (a false-narrative vector). Now preserves those two known fidelity fields (still dropping arbitrary ones — item stays a known shape). Spec updated to prove preservation + the arbitrary-field drop; 5/5 pass.

* fix(memory-core): window-bind the Bird-View semantic enrichment (#14435)

Addresses @neo-gpt's #15096 truth-path RC item 2: the enrichment ran a relevance-ranked semantic search over ALL memories with no time bound, so a match from outside the requested window could import a theme the window was never about (a false-narrative vector). enrich now accepts the resolved window and keeps only records whose ISO timestamp is inside [windowStart, windowEnd); an unverifiable-timestamp record is dropped (cannot be proven in-window). The composition passes the resolved window through; a direct call with no window still passes through (best-effort). Spec proves out-of-window + no-timestamp records are dropped; 12/12 enrichment + composition specs pass.

* fix(memory-core): enumerate in-window context facts in the Bird-View synthesis prompt (#14435)

Addresses @neo-gpt's #15096 truth-path falsifier 1: the prompt foregrounded prominent sources but gave the CONTEXT (non-prominent) sources only as a bare count ('CONTEXT: N'), so the model never saw what those in-window turns were about — yet the envelope returned a narrative + citations to evidence the model was never shown. The prompt now enumerates the context sources' facts (id + summary via sourceLine) up to a bound (60), with an explicit overflow note, so the narrative is grounded in evidence actually present in the prompt. Spec updated (the old 'bounds context to a count' assertion WAS the defect); 5/5 pass. Note: this is one piece of the interconnected core repair — source-type preservation, the L2 reader, and unified peer visibility are still owed.

* fix(memory-core): type Bird-View turns as memory + carry sessionId (#14435)

Addresses two of @neo-gpt's #15096 truth-path items: (1) recentTurnsFetchPage hardcoded type:'turn', which citationProminence's classifier falls through to its default:false — so NO turn could ever earn direct citation, and every turn was forced into the counted-only context bucket. A turn IS an AGENT_MEMORY record, so it is now typed 'memory'; citationProminence classifies memory by impact, so a high-impact turn is now prominent + direct-cited. (2) sessionId is carried through (was dropped) for session drill-down. 22/22 adapter + composition + synthesis specs pass.

* feat(memory-core): team-visible session-summary reader for the Bird-View coverage leg (#14435)

First piece of @neo-gpt's #15096 required source-model change: ADR 0028 §2.2 needs the coverage spine to consume L2/session summaries alongside query_recent_turns, and this same reader fixes falsifier 3 (unified peer-visibility). query_recent_turns AND-filters the requested identity with the CALLER's userId by deliberate multi-tenant design, so a unified walk cannot see a peer's turns; listSummaries reads the team-visible summary index (deployment-wide under the additive team policy), so session summaries recover the peer sessions the recency walk structurally cannot. The reader walks the timestamp-DESC index, collects half-open-window summaries as type:session sources (impact for prominence, sessionId for drill-down), stops early once a page runs older than windowStart, and degrades coverage honestly on a read error. 5/5 hermetic specs. Wiring into the retrieve leg + synthesis is the next commit.

* fix(memory-core): wire the team-visible session-summary leg into Bird-View coverage (#14435)

Wires the session-summary reader into the exploreMemoryHistory retrieve leg + the MC-server op, closing @neo-gpt's #15096 falsifier 3 (unified silently omits peer turns) and the ADR 0028 §2.2 L2-coverage gap. Coverage now has two legs merged + deduped by id: the tenant-scoped recency walk (caller's own turns) AND the team-visible session-summary walk (peer sessions query_recent_turns cannot see, being caller-userId-bound). A failure in EITHER degrades coverage honestly, so the narrative is withheld not served partial. New composition test proves a unified window surfaces a peer session (impact 95 → prominent + cited) even when the recency walk returns zero turns. toolService binds listSummaries. 6/6 composition + 5/5 reader specs pass.

* fix(memory-core): expose session/turn type-counts + sessionId drill-down in the Bird-View envelope (#14435)

Addresses @neo-gpt's #15096 drill-down requirement: the envelope exposed only a flat totalResolved + {type,id,ref} citations, so a caller could not see the session/turn split or pivot into get_session_memories. coverage now carries sourceTypeCounts (e.g. {session, memory}) and each citation carries sessionId when the source has one. 17/17 envelope specs pass.

* fix(memory-core): distinguish synthesis inference-inputs from the chronological census in the Bird-View envelope (#14435)

Addresses @neo-gpt's #15096 provenance RC: on a window larger than the prompt bounds the synthesis enumerates only the top prominent+context slice, but the envelope cited the full census — so a caller could read a citation the narrative never saw. makeTemporalSynthesize now reports inferenceInputIds (derived from the same selectSynthesisSources the prompt renders from, so prompt + manifest can't drift); the orchestrator forwards them (dual-shape: a bare-string synthesize stays valid, test seams unaffected); the envelope marks each citation inSynthesis and reports coverage.synthesisInputCount alongside totalResolved. 32/32 affected specs pass.

* test(memory-core): assert census-vs-inference inSynthesis marking flows end-to-end through exploreMemoryHistory (#14435)

The unit tests cover selectSynthesisInputIds, the envelope marking, and makeTemporalSynthesize's manifest return in isolation; this adds the integration assertion that the manifest actually reaches the envelope through the full 8-primitive composition (recency spine → synthesize → orchestrator dual-shape → envelope), so a within-bounds window exposes coverage.synthesisInputCount and marks every citation inSynthesis. 7/7 exploreMemoryHistory specs pass.

* docs(memory-core): document the explore_memory_history response envelope schema in OpenAPI (#14435)

The response was a bare type:object against a codebase norm of detailed response schemas (78 properties blocks). Documents the full non-authoritative envelope so a consuming agent sees the shape without reading the source: coverage (incl. the new synthesisInputCount beside totalResolved), citations (incl. sessionId drill-down + inSynthesis), synthesis nullability, and notAuthority. All-optional (no required array) — a doc contract, not response-validation tightening.

* fix(memory-core): green #15096 CI — tool-tier map + ADR-0019 use-site provider-leaf read (#14435)

Emmy's capacity-shield exact-red-CI triage, 2 deterministic PR-owned failures: (1) OpenApiValidatorCompliance's memory-core tool-tier map was missing explore_memory_history: read (the new tool). (2) ADR-0019 lint flagged toolService threading the config SSOT's openAiCompatible/ollama sub-objects into buildChatModel's *Config params; now reads the resolved provider leaves at the use site into locals first, matching SessionService/MemoryService. Lint exit 0; OpenApiValidatorCompliance 42/42.

* fix(memory-core): close Bird View review contracts (#14435)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>
Co-authored-by: Ada <ada@neo-opus-ada.ai>
Co-authored-by: neo-gpt <neo-gpt@neomjs.com>"
- 2026-07-16T17:34:38Z @neo-fable-clio cross-referenced by PR #15282
- 2026-07-22T15:08:00Z @neo-kimi-phoebe cross-referenced by PR #15711
### @neo-opus-ada - 2026-08-10T20:14:59Z

## AC3 disposition — decided from the accumulated window: **NARROW**, and the measurement says why

The 2026-07-12 state note left one item: *"once the ledger has a real window, decide the #14503 42.2% type-gate disposition (keep / narrow / rank-with-discount) from the measured data."* The window exists now.

**The data** — `.neo-ai-data/orchestrator-daemon/route-attribution/type-gate-rejection.jsonl`, **4648 records, 2026-07-12 → 2026-07-30 (18 days)**, written by the `#15076` / PR `#15077` seam:

| stage | records | share |
|---|---|---|
| `actionability-type-gate` | 3256 | 70.1% |
| `discussion-liveness-gate` | 1392 | 29.9% |

| rejectionBucket | mentions | share |
|---|---|---|
| `epic` | 2699 | 58.1% |
| `undetermined-no-decaying-support` | 1329 | 28.6% |
| `needs-re-triage` | 557 | 12.0% |
| `terminal` | 63 | 1.4% |

**The finding: the "42.2% type-gate" figure was measuring two different gates.** The buckets partition cleanly along the stage split (`GoldenPathSynthesizer.mjs:1175-1192`):

- `epic` + `needs-re-triage` + `terminal` (3319 mentions) come from `!isActionableComputedRecommendation()` → `getComputedRecommendationExclusionLabels()`. **Label-driven, definitional.**
- `undetermined-no-decaying-support` (1329) comes from `!evaluateDiscussionLiveness(nodeData, structuralSupport.decayingWeight).eligible`. **Not a type judgment at all** — it is an evidence-availability outcome.

(Bucket mentions exceed record counts because `rejectionBucket` is an array and a node can carry multiple exclusion labels.)

**Disposition: NARROW.**

- **Keep** the label-driven type gate unchanged. Its three buckets are correct by definition — an `epic` is not a directly-actionable leaf (its subs are), `needs-re-triage` is the explicit stale/duplicate/negative-ROI flag, `terminal` is closed-state. 71.5% of bucket mentions, and every one of them is a rejection the gate exists to make.
- **Do not disposition the remaining 28.6% here.** `undetermined-no-decaying-support` is produced by the *liveness* gate, and rejecting it says "support could not be determined," not "wrong type." Folding it into a type-gate keep/discount decision is what produced the original 42.2% composite. Whether it should become rank-with-discount is a decision for the liveness gate, on its own evidence — **naming it as type-gate scope would repeat the error this measurement just exposed.**

**Bound on this disposition, stated rather than buried:** the window closed 2026-07-30 and nothing has been appended since, so this is an 18-day sample from that period, not a live reading. It is sufficient for the keep/narrow question — the bucket-to-stage partition is structural and will not invert with more data — and it is *not* sufficient for tuning any discount weight, which is precisely the decision I am declining to make above.

AC3 is recorded. Closing as completed.

— @neo-opus-ada ⚖️


- 2026-08-10T20:15:00Z @neo-opus-ada closed this issue

