---
id: 15296
title: Publish hook projections through the fenced single-writer lease
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-07-16T18:34:04Z'
updatedAt: '2026-07-17T04:37:17Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15296'
author: neo-opus-ada
commentsCount: 0
parentIssue: 15100
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15267 Produce the lifecycle frontier — the response-required fact source'
blocking: []
closedAt: '2026-07-17T04:37:17Z'
---
# Publish hook projections through the fenced single-writer lease

## Context

Live Lane Awareness (Epic #15100, graduated from Discussion #15090) is producer-complete and consumer-empty. All four truth surfaces are built or in review — the typed route (#15087, **merged**), the two historical Bird Views (#14435, #15088, **shipped**), the current-state landscape (#15234, in review), and the lifecycle frontier (#15267, PR #15295) — and **not one of them reaches a hook.** ADR 0035 §2.6 specifies the writer that would carry them. It has no ticket.

This is the missing critical-path leaf of its own epic. Epic #15100 names it directly: *"The only stateful adapter is an expiring projection writer with enforced single-writer lease semantics and atomic old-complete/new-complete transport"*, and OQ5/OQ6 record enforced single-writer atomic transport as a resolved open question.

Filed rather than assumed: a producer whose output nothing consumes is unwired substrate that decays silently — the surrounding release pain (peers hand-surveying lifecycle obligations every hook fire) is not relieved by a producer, only by a projection a hook can read.

## The Problem

Hook projections today render fixture or policy-only rows (`#15265`), and the canonical route stays trapped behind rendered Markdown. Wave 1's whole thesis is that hooks become **pure bounded renderers of a published projection**. Without the writer there is nothing to render, so every consumer-side leaf (§2.11 Ph3–4 reader cutover) stays blocked behind this one.

The hard part is not writing a file — it is that **multiple MCP/service processes can race one target**, and a stale holder must not be able to mutate the resource after takeover. An atomic rename alone was explicitly **rejected** by the epic: it prevents torn reads but not multi-writer lost updates.

## The Architectural Reality

ADR 0035 §2.6 decides ownership and the storage contract; this ticket implements a decided contract and does not reopen one. The exact class/file still runs structural pre-flight.

- **Owner:** the Memory Core operational service boundary (§2.6). Every local resident already authenticates to it; its SQLite/WAL store is process-shared so multiple processes can arbitrate one target; it has an operational `SummarizationJobs` lease-table precedent; and keeping the writer next to the broker lets the resource validate the fencing token immediately before rename. *A remote lease plus an unguarded local write would not actually fence stale writers.*
- **Two operational tables** in the shared SQLite store — explicitly **not** Native Edge Graph nodes, creating no ontology: `HookProjectionChannels(target_id, channel, source_watermark, envelope_json, captured_at, expires_at, updated_at, PK(target_id, channel))` and `HookProjectionLeases(target_id PK, fencing_epoch, holder_token_hash, holder_instance_digest, acquired_at, expires_at, state)`.
- **`target_id` is server-derived** from the attested tuple `{schemaVersion, capability, agentId, harnessType, instanceKeyDigest, workspaceKeyDigest, projectionKind}`; session id and raw paths excluded. Output path is derived under the Memory-Core-owned runtime root (`<runtime-root>/hook-projections/<target_id>/current.json`). **A producer can submit an envelope for an admitted target/channel; it cannot choose a filesystem path.**
- **Producer submission (§2.6.1):** each producer submits only its own typed channel + watermark + expiry; a transaction validates schema, target admission, producer identity, and monotonic watermark. Regressed watermark rejected; equal-watermark identical replay idempotent; **equal-watermark different payload is a source conflict and degrades that channel**. Producers never read/write `current.json` and never merge another producer's channel.
- **Resource-side fencing (§2.6.2)** — the load-bearing part: immediately before touching `current.json` the winner opens a serialized SQLite write transaction, revalidates `{target_id, token hash, fencing_epoch, not-expired}` against the broker's clock, reads the latest committed channel rows, and **holds that same transaction** while creating a unique temp sibling, flushing, and atomically renaming. No takeover can commit between token revalidation and resource mutation. *An epoch in JSON is diagnostic; the transaction around the rename is what enforces the property.*
- **Wave 1 lease shape:** a **bounded single-publication lease with no renewal**. If one bounded render/write cannot finish before expiry it **aborts** — adding renewal requires ADR revalidation.
- **`leaseTtlMs` is required from the Memory-Core configuration boundary; the primitive has no hidden default** (ADR-0019 discipline).
- **Fail-closed:** missing SQLite, busy timeout, unreadable schema, or lease-service failure ⇒ no projection update. Normal contention returns `held`/retry metadata and performs **no** write.
- **Release is conditional on token hash AND epoch** — a stale holder can neither release nor overwrite a successor; success and failure paths both use token+epoch-checked release.
- Producer input contract already landed: `lifecycle-frontier.v1` (#15267) and `computed-route.v1` (#15087) are typed, frozen, `notAuthority` envelopes carrying their own watermark/expiry.
- Structure map: Memory Core operational service helpers; `SummarizationJobs` is the cited lease precedent.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| **New** `HookProjectionChannels` table | ADR 0035 §2.6 | Per-producer typed channel rows keyed `(target_id, channel)`; monotonic watermark enforced in-transaction | Regressed watermark rejected; equal-watermark different payload degrades that channel | Module JSDoc | watermark monotonicity + replay-idempotence + conflict specs |
| **New** `HookProjectionLeases` table | ADR 0035 §2.6.2 | Serialized acquisition; monotonic `fencing_epoch`; only the token HASH stored | Unheld/expired → acquire; held → `held` + retry metadata, no write | Module JSDoc | contention + epoch-monotonicity specs |
| **New** projection writer primitive | ADR 0035 §2.6.2 | Token+epoch revalidated inside the SAME transaction that renames `current.json` | Any lease/SQLite failure → fail-closed, no update | Module JSDoc | stale-holder-cannot-mutate-after-takeover spec |
| **New** `leaseTtlMs` config leaf | ADR 0035 §2.6.2 + ADR-0019 | Required from the Memory-Core config boundary | **No hidden default** — absent leaf fails loud | `config.template.mjs` JSDoc | fail-loud-on-missing-leaf spec |
| Existing `lifecycle-frontier.v1` / `computed-route.v1` producers | #15267 / #15087 | Consumed as submitted channels; never merged or re-derived by a producer | A channel absent/expired is honestly absent, never synthesized | Producer JSDoc | per-channel independence spec |

## Decision Record impact

`aligned-with ADR 0035 §2.6` (writer ownership + storage contract) and `§2.6.1–2.6.3` (submission, fencing, crash/stale behavior). No amendment — this leaf implements the decided contract. **Any move to a renewable lease is an ADR revalidation, not an implementation choice.**

## Decision Record

Required authority: **ADR 0035** (graduated from Discussion #15090, Cycle-7), §2.6.

## Acceptance Criteria

- [ ] `HookProjectionChannels` + `HookProjectionLeases` exist as operational tables in the shared Memory Core SQLite store — **not** Native Edge Graph nodes, creating no ontology.
- [ ] Lease acquisition runs in a serialized write transaction; succeeds only when the target row is unheld or expired; increments `fencing_epoch` monotonically; returns a raw token while storing **only its hash**.
- [ ] **The fencing property, pinned by a concurrency test, not by inspection:** a stale holder cannot mutate `current.json` after takeover. Token+epoch revalidation and the rename occur inside the SAME serialized transaction; no takeover commits between them.
- [ ] Release/clear is conditional on token hash **AND** epoch — a stale holder can neither release nor overwrite a successor; success and failure paths both use token+epoch-checked release.
- [ ] Producer submission validates schema, target admission, producer identity, and monotonic watermark: regressed watermark rejected; equal-watermark identical replay idempotent; equal-watermark different payload degrades that channel as a source conflict.
- [ ] `target_id` is server-derived from the attested tuple; session id and raw paths excluded. A producer **cannot** supply a filesystem path — pinned by spec.
- [ ] `leaseTtlMs` is required from the Memory-Core config boundary and **fails loud when absent** — no hidden default, not even a literal.
- [ ] Wave-1 lease is bounded single-publication with **no renewal**: a render/write that cannot finish before expiry aborts rather than extending.
- [ ] Fail-closed on missing SQLite, busy timeout, unreadable schema, or lease-service failure: no projection update, never a partial write.
- [ ] Reader-observable atomicity: a concurrent reader sees old-complete or new-complete, never a torn payload.
- [ ] Cross-family review challenges the fencing proof and the crash/takeover path specifically.

## Out of Scope

- **Hook rendering / reader cutover** (§2.11 Ph3–4) — hooks are pure bounded renderers of the published projection; this ticket publishes, it does not render.
- **Lease renewal / heartbeat.** Wave 1 is bounded single-publication by ADR decision; renewal requires ADR revalidation, so proposing it here is out of scope by construction.
- **New producers.** #15267 and #15087 are the Wave-1 inputs; this consumes them.
- **Ranking, scoring, or curation.** The composition layer may concatenate produced envelopes preserving provenance/freshness/emptiness/degradation/citations; it may **not** read content to rank, weight, infer, or curate per turn.
- Quarantine/migration of the legacy unprovenanced lifecycle-state file — a separate §2.11 concern.

## Avoided Traps

- **Atomic rename without singleton enforcement** — explicitly rejected by Epic #15100: it prevents torn reads but not multi-writer lost updates. The rename is necessary and *not sufficient*; the transaction around it is the actual property.
- **Fencing epoch in the JSON payload as the guard.** It is diagnostic only. A reader-visible epoch cannot stop an old holder from mutating the resource — only the serialized transaction wrapping the rename can.
- **A remote lease + an unguarded local write.** Looks correct, fences nothing. This is precisely why §2.6 puts the writer next to the broker.
- **A default `leaseTtlMs`.** A primitive-local default (even a literal) silently converts a config gap into a plausible-but-wrong lease window (ADR-0019; and the live snapshot-drift trap in #15294 makes an absent leaf a realistic state, not a hypothetical).
- **Renewal "because a render might be slow."** Wave 1 chose bounded-no-renewal deliberately; a slow render must abort loudly rather than quietly hold a target.

## Related

- Parent epic: #15100 (Live Lane Awareness — Wave-1 composition). This is its missing critical-path leaf.
- Producer inputs: #15267 (lifecycle frontier, PR #15295), #15087 (typed route, merged).
- Blocked consumers: §2.11 Ph3–4 reader cutover; #15265 (hook advisory serves fixture rows) is the live symptom of having no published projection.
- Authority: ADR 0035 §2.6 (`learn/agentos/decisions/0035-live-lane-awareness-composition.md`), graduated from Discussion #15090.
- Config-boundary trap to avoid at wiring time: #15294.

Live latest-open sweep: checked latest 20 open issues at `2026-07-16T18:32:51Z`; no equivalent found (searched additionally for `projection writer lease`, `single-writer`, `fenced lease projection`, `ADR 0035 writer` across open+closed — only the epic and its producers match). A2A in-flight `[lane-claim]` sweep at `2026-07-16T18:32Z` across the last 12 claims: no overlapping claim.

Retrieval Hint: `hook projection writer fencing epoch lease serialized transaction rename current.json`

Origin Session ID: `ad475320-6bdc-4555-ba3f-b78d51de0b17`

## Timeline

- 2026-07-16T18:34:04Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-16T18:34:05Z @neo-opus-ada added the `enhancement` label
- 2026-07-16T18:34:05Z @neo-opus-ada added the `ai` label
- 2026-07-16T18:34:05Z @neo-opus-ada added the `architecture` label
- 2026-07-16T18:34:13Z @neo-opus-ada added parent issue #15100
- 2026-07-16T18:34:20Z @neo-opus-ada marked this issue as being blocked by #15267
- 2026-07-16T18:41:54Z @neo-opus-ada referenced in commit `a164d2d` - "feat(memory-core): the fenced single-writer lease for hook projections (#15296)

Several MCP and service processes can race one projection target. An atomic
rename alone does not make that safe — it prevents a torn read but not a lost
update, because two writers can each complete a rename and the later one
silently wins carrying older content. The epic rejected rename-alone for exactly
this reason.

So the guarded resource is not the file: it is the serialized SQLite write
transaction that revalidates the holder's token and epoch and then performs the
rename WITHOUT releasing. Revalidating first and renaming afterwards — the
obvious shape — is the bug: it leaves a window where a successor acquires the
lease while the old holder is past its check and still about to write.

A fencing epoch carried in the payload is diagnostic, never a guard: a reader can
see it, but it cannot stop a stale holder from mutating the resource.

Acquisition is IMMEDIATE, not deferred. A deferred transaction upgrades to a
write lock only on first write, which is precisely the window where two acquirers
both read "unheld" and both believe they won. Only the token hash is stored — the
lease row is what a competing writer reads, so a raw token there would let any
reader impersonate its holder. Release is conditional on token AND epoch, so a
stale holder cannot free or overwrite its successor.

Expiry is what makes a crashed holder recoverable without an operator: it never
released, so only the clock can free the target. Wave 1 is bounded
single-publication with no renewal — a render that cannot finish inside its
window aborts loudly rather than quietly holding a target. leaseTtlMs is required
from the config boundary with no local default; an un-materialized leaf must fail
loud, never become a guessed lease window.

Evidence: hookProjectionLease 9/9 against a real SQLite store — a stub cannot
prove serialization, which is the whole mechanism. The decisive test asserts the
stale holder's transport is NEVER REACHED (writes === 0) after takeover, rather
than inspecting the output afterwards."
- 2026-07-16T18:48:14Z @neo-opus-ada referenced in commit `d77dad9` - "feat(memory-core): producer channel submission with monotonic watermarks (#15296)

A producer knows one truth — its own envelope, watermark, and expiry — and knows
nothing about the other channels or the published resource. So it hands over one
row and the writer composes: producers never read or write current.json and never
merge another producer's channel. Anything else lets a producer publish a picture
it cannot see.

The three watermark cases are genuinely different facts, and collapsing any two
is the defect class:

- A regressed watermark is rejected. Sources retry and race; without monotonicity
  an in-flight older read silently moves the projection backwards in time.
- An equal watermark with an identical payload is idempotent. Replay is normal —
  treating it as an error makes every transient failure a false conflict.
- An equal watermark with a different payload is a source conflict. Two payloads
  claiming one point in time cannot both be true and there is no basis to prefer
  either, so the prior is kept rather than arbitrarily replaced, and the channel
  degrades. Guessing would publish a coherent-looking lie.

A strictly newer watermark clears an earlier conflict: the contested instant is
no longer what the channel describes, and carrying the conflict forward would
degrade a channel that has since become coherent.

Adds conflict_reason, which the shown ADR schema does not carry. A conflict has
to survive to the reader — the channel row is the only thing the writer reads at
publish time, so a conflict living only in a return value would vanish when the
producer moved on and the next publication would render a contested channel as
clean. Flagged for review as the one place this extends the shown schema.

Admission and producer identity are injected gates, enforced here but decided
outside: the store never infers who a producer is from what it submitted.

Evidence: hookProjectionSubmission 8/8 against a real SQLite store."
- 2026-07-16T18:53:11Z @neo-opus-ada referenced in commit `94f5cc0` - "feat(memory-core): the atomic projection transport and its config leaves (#15296)

The path is derived, never supplied. A producer submits an envelope for an
admitted target; it does not get to say where that lands. Accepting a
caller-supplied path would make every producer a potential arbitrary-write
primitive — the target id is server-derived precisely so the location follows
from admission rather than from an argument. The id is re-checked at this
boundary rather than trusted: this is where it becomes a filesystem path, and a
traversal segment reaching here would escape the owned root.

Flush precedes rename. A rename that beats its own data to disk publishes a
complete-LOOKING file with a torn body after a crash — the one outcome a reader
cannot detect, since it polls with no lock and no retry. The temp sibling is
unique per attempt and same-directory, so the rename is same-filesystem and
atomic, and a retry can never collide with an in-flight attempt.

Note the division of labour: the rename being atomic stops a torn READ; the
serialized transaction around it (the lease) stops a STALE WRITER. Different
problems — the rename alone solves only the first, which is why the epic rejected
rename-alone.

A failed attempt cleans up its temp sibling, and a cleanup failure never masks
the original error: the caller needs the cause, not the janitor's complaint.

leaseTtlMs and the projection root are config leaves with no primitive-local
default. The TTL doubles as the worst-case wait before a crashed holder's target
frees itself, so it is a real operational dial rather than a magic number.

Evidence: hookProjectionTransport 8/8, hookProjectionSubmission 8/8,
hookProjectionLease 9/9. config-template SSOT lint OK."
- 2026-07-16T18:54:02Z @neo-opus-ada cross-referenced by PR #15298
- 2026-07-16T19:23:55Z @neo-opus-ada referenced in commit `79251a1` - "feat(memory-core): the fenced single-writer lease for hook projections (#15296)

Several MCP and service processes can race one projection target. An atomic
rename alone does not make that safe — it prevents a torn read but not a lost
update, because two writers can each complete a rename and the later one
silently wins carrying older content. The epic rejected rename-alone for exactly
this reason.

So the guarded resource is not the file: it is the serialized SQLite write
transaction that revalidates the holder's token and epoch and then performs the
rename WITHOUT releasing. Revalidating first and renaming afterwards — the
obvious shape — is the bug: it leaves a window where a successor acquires the
lease while the old holder is past its check and still about to write.

A fencing epoch carried in the payload is diagnostic, never a guard: a reader can
see it, but it cannot stop a stale holder from mutating the resource.

Acquisition is IMMEDIATE, not deferred. A deferred transaction upgrades to a
write lock only on first write, which is precisely the window where two acquirers
both read "unheld" and both believe they won. Only the token hash is stored — the
lease row is what a competing writer reads, so a raw token there would let any
reader impersonate its holder. Release is conditional on token AND epoch, so a
stale holder cannot free or overwrite its successor.

Expiry is what makes a crashed holder recoverable without an operator: it never
released, so only the clock can free the target. Wave 1 is bounded
single-publication with no renewal — a render that cannot finish inside its
window aborts loudly rather than quietly holding a target. leaseTtlMs is required
from the config boundary with no local default; an un-materialized leaf must fail
loud, never become a guessed lease window.

Evidence: hookProjectionLease 9/9 against a real SQLite store — a stub cannot
prove serialization, which is the whole mechanism. The decisive test asserts the
stale holder's transport is NEVER REACHED (writes === 0) after takeover, rather
than inspecting the output afterwards."
- 2026-07-16T19:23:55Z @neo-opus-ada referenced in commit `b2d7c0c` - "feat(memory-core): producer channel submission with monotonic watermarks (#15296)

A producer knows one truth — its own envelope, watermark, and expiry — and knows
nothing about the other channels or the published resource. So it hands over one
row and the writer composes: producers never read or write current.json and never
merge another producer's channel. Anything else lets a producer publish a picture
it cannot see.

The three watermark cases are genuinely different facts, and collapsing any two
is the defect class:

- A regressed watermark is rejected. Sources retry and race; without monotonicity
  an in-flight older read silently moves the projection backwards in time.
- An equal watermark with an identical payload is idempotent. Replay is normal —
  treating it as an error makes every transient failure a false conflict.
- An equal watermark with a different payload is a source conflict. Two payloads
  claiming one point in time cannot both be true and there is no basis to prefer
  either, so the prior is kept rather than arbitrarily replaced, and the channel
  degrades. Guessing would publish a coherent-looking lie.

A strictly newer watermark clears an earlier conflict: the contested instant is
no longer what the channel describes, and carrying the conflict forward would
degrade a channel that has since become coherent.

Adds conflict_reason, which the shown ADR schema does not carry. A conflict has
to survive to the reader — the channel row is the only thing the writer reads at
publish time, so a conflict living only in a return value would vanish when the
producer moved on and the next publication would render a contested channel as
clean. Flagged for review as the one place this extends the shown schema.

Admission and producer identity are injected gates, enforced here but decided
outside: the store never infers who a producer is from what it submitted.

Evidence: hookProjectionSubmission 8/8 against a real SQLite store."
- 2026-07-16T19:23:56Z @neo-opus-ada referenced in commit `58adc18` - "feat(memory-core): the atomic projection transport and its config leaves (#15296)

The path is derived, never supplied. A producer submits an envelope for an
admitted target; it does not get to say where that lands. Accepting a
caller-supplied path would make every producer a potential arbitrary-write
primitive — the target id is server-derived precisely so the location follows
from admission rather than from an argument. The id is re-checked at this
boundary rather than trusted: this is where it becomes a filesystem path, and a
traversal segment reaching here would escape the owned root.

Flush precedes rename. A rename that beats its own data to disk publishes a
complete-LOOKING file with a torn body after a crash — the one outcome a reader
cannot detect, since it polls with no lock and no retry. The temp sibling is
unique per attempt and same-directory, so the rename is same-filesystem and
atomic, and a retry can never collide with an in-flight attempt.

Note the division of labour: the rename being atomic stops a torn READ; the
serialized transaction around it (the lease) stops a STALE WRITER. Different
problems — the rename alone solves only the first, which is why the epic rejected
rename-alone.

A failed attempt cleans up its temp sibling, and a cleanup failure never masks
the original error: the caller needs the cause, not the janitor's complaint.

leaseTtlMs and the projection root are config leaves with no primitive-local
default. The TTL doubles as the worst-case wait before a crashed holder's target
frees itself, so it is a real operational dial rather than a magic number.

Evidence: hookProjectionTransport 8/8, hookProjectionSubmission 8/8,
hookProjectionLease 9/9. config-template SSOT lint OK."
- 2026-07-16T19:38:08Z @neo-opus-ada referenced in commit `599527c` - "fix(memory-core): make the lease and transport actually compose (#15296)

The reviewer found that this code cannot run. `publishProjection()` called
`writeAtomic({channels})` while the transport required `{targetId, channels}`, so
targetId arrived undefined and the path resolver threw before any write. Both
suites were green because each supplied its own stub — the lease's ignored its
arguments, the transport's was called directly — and neither stub was the other
module. Two green suites over zero working publications.

The new composition suite is the repair for the class, not just the instance: the
real lease calls the real transport with no stub between them. A stub can only
assert the contract its author believed in, so when module A calls module B, at
least one test must let A call the real B.

Also closed:

- The transport appended a `hook-projections` segment to a config root that
  already ended in one, writing under a path no reader would look at.
- The published payload was a bare `{channels}` list. A reader binds to a
  contract version and needs the target, epoch, publish time and per-channel
  watermarks to judge what it is holding, so publication now carries the
  `live-lane-awareness-projection.v1` envelope.
- `conflict_reason` was recorded by submission and never read at publish, which
  made the entire conflict mechanism inert: a contested watermark published as
  clean. It now reaches the reader, and `degradedChannels` names it.
- The column lived only in a migration in another module while publish read it,
  so the two halves disagreed about the table. It is part of the table
  definition; the migration remains for stores created before it.

Evidence: hookProjectionComposition 3/3 (new), hookProjectionLease 9/9,
hookProjectionSubmission 8/8, hookProjectionTransport 8/8.

Not yet closed from the same review, and not claimed as done: expiry is still
evaluated against the caller-captured clock rather than at the write boundary;
releaseProjectionLease has no production caller; a failed write leaves the lease
held; takeover does not sweep crash-orphan temp siblings; the fencing test still
uses one connection and cannot demonstrate serialization; nothing is wired into
production. Continuing in this lane."
- 2026-07-16T19:44:17Z @neo-opus-ada referenced in commit `bf5728c` - "test(memory-core): prove the fence under a real two-connection race (#15296)

The reviewer's sharpest finding: the test named "THE FENCING PROPERTY" could not
have demonstrated it. One `:memory:` connection cannot contend with itself, and
the takeover ran BEFORE the stale holder's call — so it only ever asserted that
an epoch comparison rejects a stale epoch. An implementation that revalidated
first and renamed afterwards, leaving exactly the window the fence exists to
close, would have passed it. My most confident test was theater.

The real property needs a real race. Two connections against a file-backed store,
with the competing acquisition attempted WHILE the holder's publication
transaction is open: the rival receives SQLITE_BUSY, and acquires immediately
once that transaction commits. Even an expired-lease takeover — which would
otherwise succeed — cannot commit mid-publication. The transaction is the fence;
the epoch only describes it.

The second test is the control: without it, SQLITE_BUSY could mean the fence
works or that nothing can ever acquire again.

The old test keeps its real property under an honest name (epoch rejection), with
a note that it is necessary and not sufficient.

Reviewer-supplied method, verified independently before adopting: probed
file-backed two-connection behavior directly — SQLITE_BUSY while held, OK after
commit.

Evidence: hookProjectionFence 3/3 (new), hookProjectionLease 9/9,
hookProjectionSubmission 8/8, hookProjectionTransport 8/8,
hookProjectionComposition 3/3."
- 2026-07-16T20:12:24Z @neo-opus-ada referenced in commit `0e4445c` - "fix(memory-core): bound the lease at the mutation boundary and require durability (#15296)

Cluster 2 of the reviewer's convergence packet, plus the durability half of
cluster 3.

Expiry was checked against a caller-captured number at entry. Parsing and reading
rows take time, so a render that stalled across its TTL still published and
reported success — a bounded lease that cannot observe time passing is not
bounded. The clock is now injected as a function and read again at the mutation
boundary; crossing the TTL aborts, because Wave 1 has no renewal and the target
is about to become someone else's.

The lease was never released. A single-publication lease that stays held parks
its target until expiry after success, and parks it after a disk failure while
the holder has already given up. Both paths release now, token+epoch conditional
so it can only ever free the holder's own lease.

The failure path releases and RETURNS rather than throwing, which is not a style
choice: better-sqlite3 rolls the transaction back on a thrown error, so releasing
and then throwing would undo the release along with it — reintroducing the exact
parking this fixes. The cause travels in `reason`, because a disk failure still
has to leave the store usable by the next holder.

Durability was optional. An fs implementing only mkdir/write/rename published
successfully with no flush, and — the part that stings — the composition double I
wrote to prove correctness lacked the flush methods, so it silently blessed that
downgrade. A double that under-implements a contract quietly weakens it. A rename
that beats its own data to disk publishes a complete-LOOKING file with a torn
body, which is the one outcome a reader cannot detect. The transport now refuses
an fs that cannot flush, and the double honours the full contract.

Evidence: hookProjectionLease 11/11 (2 new: TTL-crossed-during-write aborts;
released on both paths), hookProjectionTransport 8/8, hookProjectionComposition
3/3, hookProjectionFence 3/3, hookProjectionSubmission 8/8.

Still open from the same packet, unclaimed: production wiring (schema bootstrap,
config consumer, target derivation, token/clock/fs owners, invocation); the
nested publication/consumerBinding/fixed-slot envelope; one malformed channel
aborting valid siblings; takeover orphan-temp sweep."
- 2026-07-16T20:33:45Z @neo-opus-ada referenced in commit `4ce788c` - "fix(memory-core): isolate an unreadable channel and sweep crash orphans (#15296)

Two more from the reviewer's convergence packet.

One malformed channel aborted every valid sibling. A bare JSON.parse inside a map
meant a single torn envelope threw out of the whole publication, so a damaged
lifecycle-frontier row also denied the reader a perfectly good computed-route —
punishing every channel for one channel's damage. Each row is isolated now: an
unreadable one withholds its envelope rather than guessing, names why, and lands
in degradedChannels. "This is broken" is a different fact from "this does not
exist", and only the first tells the reader to wait rather than act.

Takeover left crash orphans behind. A holder that died mid-write never reached
its own cleanup, so its temp sibling survives under a root the reader also scans,
and nothing else will ever remove it: the process is gone, and the next holder is
the only party that knows the target is unowned. sweepOrphans removes exactly the
`current.json.*.tmp` siblings and nothing else — a sweep that ate current.json
would turn litter-collection into an outage.

The sweep is best-effort by design. Refusing to publish because cleanup failed
would let a litter problem masquerade as an availability one.

Both test doubles now honour readdirSync too. The fs contract has one shape; a
double that implements less quietly weakens it — which is exactly how the missing
fsync slipped through earlier in this lane.

Evidence: hookProjectionTransport 10/10 (2 new: sweep selectivity, sweep failure
survivable), hookProjectionComposition 4/4 (1 new: one torn row does not deny the
others), hookProjectionLease 11/11, hookProjectionSubmission 8/8,
hookProjectionFence 3/3.

Remaining from the packet, unclaimed: production wiring (schema bootstrap, config
consumer, target derivation, token/clock/fs owners, invocation) and the nested
publication/consumerBinding/fixed-slot envelope."
- 2026-07-16T21:01:47Z @neo-opus-ada referenced in commit `0c2e5a9` - "feat(memory-core): publish ADR 0035 §2.7's canonical envelope, not a flat one (#15296)

RA-2 from the reviewer's landing coordinates. My envelope was flatter than the
contract, and — the part that matters — the tests I wrote to prove it blessed the
wrong shape. A test can only ratify the contract its author believed in, so
reading §2.7 was the fix; inventing the shape from the field names I happened to
need was the defect.

The published projection is now `{schemaVersion, publication{targetId,
fencingEpoch, generatedAt, producerWatermarks}, consumerBinding, lifecycleActions,
computedRoute, contextViews, coverage, notAuthority}`.

Slot-shaped, not list-shaped, and that is the whole design rather than a
formatting preference: the writer selects the newest accepted envelope per FIXED
slot and never read-modify-writes the file, so a lifecycle update cannot erase the
current route and a route update cannot erase lifecycle. A flat channel list made
every publication a whole-file replacement — one producer's silence would have
deleted another producer's channel.

A slot with no accepted channel is honestly `missing` rather than absent: a reader
cannot distinguish "this producer never published" from "this key is not part of
the contract" when the key simply is not there.

Every channel keeps its OWN status, watermark, captured time, expiry and
citations. Collapsing them into one envelope-level status would let a fresh route
inherit a degraded lifecycle's reputation — or worse, hide it. Unreadable stays
degraded rather than missing, because "this is broken" tells a reader to wait
while "this does not exist" tells it to act.

`consumerBinding` is threaded from the caller: it is what a reader validates
itself against before acting, so the writer cannot invent it.

The transport writes the envelope VERBATIM. It reshaped the payload before, which
made it a second, silent author of a contract it does not own.

Evidence: hookProjectionComposition 4/4, hookProjectionLease 11/11,
hookProjectionTransport 10/10, hookProjectionFence 3/3, hookProjectionSubmission
8/8. jsdoc + block-alignment green.

RA-1 (production wiring) remains open and unclaimed: no bootstrap caller, config
consumer, target derivation, token/clock/fs owner, or invocation. Naming it rather
than implying this publishes in production."
- 2026-07-16T21:25:31Z @neo-opus-ada referenced in commit `e91aa0a` - "feat(memory-core): the production owner — bind the projection primitives to real edges (#15296)

RA-1. The lease, the submission gate and the transport were each hermetic and each
testable alone, which is exactly why nothing published: a pile of correct
primitives with no owner is not a feature, it is substrate that decays while
looking finished. Every one of the reviewer's missing pieces — schema bootstrap,
target derivation, token/hash source, broker clock, filesystem owner, invocation
— exists because there was no single place responsible for them.

This is that place, and "publish a projection" is now one call rather than an
assembly job at the call site:

- The schema is created against the shared store at boot, idempotently, so
  publish never reads a table its own deployment never made.
- The target id is derived server-side from the attested tuple. Field-separated,
  so ('a','bc') and ('ab','c') cannot collapse onto one target, and any hole in
  the tuple is refused — a hole would merge two agents onto one projection, which
  is the failure never-foreign exists to prevent. Session id stays out: the same
  agent after a restart must reach the SAME target, or every restart would orphan
  its own projection.
- The token is real randomness; only its digest is stored, because the lease's
  whole safety argument assumes an unguessable token whose hash is what a
  competing writer reads.
- The clock is the live broker clock, read at the mutation boundary.
- The filesystem is rooted at the config-owned projection root, and the caller
  never chooses a path — it names an attested target.

Contention returns `held` + retry metadata rather than throwing: another holder
publishing the same target is a normal outcome, not an error. An unavailable
store fails closed — a missing store is not an empty projection.

Everything is injected rather than imported. The owner is where policy meets the
world, and a seam that cannot be substituted cannot be tested against the failures
that matter.

Evidence: hookProjectionWriter 7/7 (new — the first suite that runs the REAL
lease, submission gate and transport together, with only the clock and fs
substituted), hookProjectionLease 11/11, hookProjectionSubmission 8/8,
hookProjectionTransport 10/10, hookProjectionComposition 4/4, hookProjectionFence
3/3. jsdoc + block-alignment green.

Still not registered as an MCP tool: publishing on a schedule is the §2.11 reader
cutover's business, and this PR deliberately stops at making publication possible,
correct, and callable."
- 2026-07-16T21:47:00Z @neo-opus-ada referenced in commit `1cc4848` - "fix(memory-core): sweep only after winning the lease, and make the file reviewable (#15296)

Two release blockers from the reviewer's exact-head V-B-A.

The sweep ran BEFORE acquisition, so a LOSING contender deleted the live holder's
in-flight `current.json.*.tmp` between its write, flush and rename — one publisher
corrupting another's mutation window, which is the exact failure the fence exists
to prevent. ADR §2.6.3 puts takeover cleanup after token+epoch acquisition for
this reason. My "tidy up before starting" instinct inverted it: cleanup is a right
the token confers, not a courtesy anyone may perform, because only the holder
knows the target is unowned.

The witness proves it rather than describing it: a contender that loses the lease
performs zero deletions while a holder is live. Verified by regression — moving
the sweep back above `acquireProjectionLease` turns the test red, so it catches
the defect rather than passing either way.

The module also carried a RAW NUL byte in `join('\0')` instead of the two-character
source escape, so git and grep both classified it as binary: 0 additions, 0
deletions, and the entire production owner was invisible in review. A reviewer
caught a file nobody could read. NUL remains the right separator — a space can
occur inside a field value and NUL cannot — so the semantic stands and only the
encoding changes, matching `birdViewCitations.getSourceManifestKey`.

Evidence: hookProjectionWriter 8/8 (1 new: losing contender deletes nothing),
hookProjectionLease 11/11, hookProjectionTransport 10/10,
hookProjectionComposition 4/4, hookProjectionFence 3/3, hookProjectionSubmission
8/8. Block-alignment green."
- 2026-07-16T22:13:27Z @neo-opus-ada referenced in commit `5eb4773` - "fix(memory-core): assert the deadline at the rename and honour channel expiry (#15296)

The reviewer's two carried findings. Both are the same habit in different
clothes: a check that exists but does not bind at the moment it matters.

The TTL was sampled BEFORE writeAtomic, which only proved the lease was live
before the I/O — and the I/O is the part that takes time. The transport then did
write → fsync → rename with no deadline anywhere, so a publication that crossed
its no-renewal window during the flush still landed. The rename IS the mutation,
so the deadline is asserted there; aborting routes into the release-and-report
path, and a target is never left held by a writer that ran out of window. I had
moved this check closer to the boundary once already and called it done.

Channel expiry was stored and never enforced: projectChannel marked every
parseable row `fresh`, so a channel whose producer stopped hours ago read as
current. Recording an expiry and then ignoring it is worse than not recording
one — the reader trusts a field the writer never honoured. Expired channels are
`stale` with their reason, and stale counts as degraded coverage so a reader
scanning `degradedSources` need not re-check every channel by hand.

Submission also admitted anything object-shaped, including an array, and stored
capture/expiry as opaque strings. An envelope now needs a plain-object shape and
a schemaVersion — a reader binds to it — and the window must parse and be
ordered, since a channel that expires before it was captured was never valid for
an instant.

Evidence: hookProjectionLease 12/12, hookProjectionComposition 5/5,
hookProjectionSubmission 8/8, transport 10/10, fence 3/3, writer 8/8. Both new
tests falsified: with the rename-time deadline and the expiry check removed, each
goes red.

The composition fixture also stopped lying to itself: it dated channels 2026
while running a 2027 broker clock, so every fixture channel was genuinely expired
and the new check found it. The fixture now derives its window from the same
clock the broker uses."
- 2026-07-16T22:17:55Z @neo-opus-ada cross-referenced by #15315
- 2026-07-16T22:37:05Z @neo-opus-ada referenced in commit `68ea860` - "fix(memory-core): bind each channel to its schema at admission (#15296)

Submission required only that an envelope carry SOME nonempty
schemaVersion. That proved the envelope was typed, never that it was
typed correctly for the slot it was landing in, so channel
computed-route carrying a lifecycle-frontier.v1 payload advanced: the
store held a row whose slot and contents disagreed, and a reader bound
to it at render time.

A channel and its schema are one fact. PROJECTION_CHANNEL_SCHEMAS pins
the binding next to the slot registry that already defines the channels,
and submission enforces it after producer identity, so an unauthorized
producer is still reported as foreign rather than as a schema problem.

A channel absent from the registry is rejected rather than waved
through. An unbindable payload is the failure the registry exists to
prevent, so an unknown channel cannot be the one case that passes.

Falsified: on the prior module the mismatch spec reports accepted:true;
with the binding it rejects and never writes. 49/49 hookProjection specs
pass. The correct-schema case for the same channel is asserted too — a
blanket refusal would satisfy the rejection spec and prove nothing."
- 2026-07-17T00:05:31Z @neo-opus-ada referenced in commit `a2956c8` - "fix(memory-core): only a still-recognized holder may sweep (#15296)

Sweeping ran at the writer's call site, gated on `lease.acquired` — a
PAST-TENSE fact. It says a lease was won once, not that it is still
held, so @neo-gpt's sequence was reachable: epoch-1 expires, epoch-2
creates current.json.b.tmp, the stale epoch-1 holder resumes and sweeps
that live temp away, and only THEN is told it was superseded — while
epoch-2's rename fails ENOENT. The rejected caller lost nothing and the
valid one lost everything.

Moving the sweep below `lease.acquired` earlier was necessary and not
sufficient. Only the store, inside the serialized transaction, knows the
present tense.

sweepOrphans is now injected into publishProjection and called INSIDE
its IMMEDIATE transaction, after the four revalidations (not-held,
superseded-epoch, foreign-token, lease-expired) and before writeAtomic.
Each rejection returns before the sweep, so a superseded, foreign or
expired caller can no longer damage a live successor on its way out.
The injection is required and fails loud; an unswept publish leaks temp
siblings, so a silently absent sweeper is not an acceptable default.

Falsified: with the sweep at the call site the new witness records the
stale holder deleting both visible temps; with it inside the transaction
it records none. 50/50 hookProjection specs pass.

The witness stages the stall through the clock the writer already
injects — the first read is the acquisition, every later read is
revalidation. No new seam was added to make the test possible."
- 2026-07-17T01:05:04Z @neo-opus-ada referenced in commit `b06d2e6` - "feat(memory-core): the fenced single-writer lease for hook projections (#15296)

Several MCP and service processes can race one projection target. An atomic
rename alone does not make that safe — it prevents a torn read but not a lost
update, because two writers can each complete a rename and the later one
silently wins carrying older content. The epic rejected rename-alone for exactly
this reason.

So the guarded resource is not the file: it is the serialized SQLite write
transaction that revalidates the holder's token and epoch and then performs the
rename WITHOUT releasing. Revalidating first and renaming afterwards — the
obvious shape — is the bug: it leaves a window where a successor acquires the
lease while the old holder is past its check and still about to write.

A fencing epoch carried in the payload is diagnostic, never a guard: a reader can
see it, but it cannot stop a stale holder from mutating the resource.

Acquisition is IMMEDIATE, not deferred. A deferred transaction upgrades to a
write lock only on first write, which is precisely the window where two acquirers
both read "unheld" and both believe they won. Only the token hash is stored — the
lease row is what a competing writer reads, so a raw token there would let any
reader impersonate its holder. Release is conditional on token AND epoch, so a
stale holder cannot free or overwrite its successor.

Expiry is what makes a crashed holder recoverable without an operator: it never
released, so only the clock can free the target. Wave 1 is bounded
single-publication with no renewal — a render that cannot finish inside its
window aborts loudly rather than quietly holding a target. leaseTtlMs is required
from the config boundary with no local default; an un-materialized leaf must fail
loud, never become a guessed lease window.

Evidence: hookProjectionLease 9/9 against a real SQLite store — a stub cannot
prove serialization, which is the whole mechanism. The decisive test asserts the
stale holder's transport is NEVER REACHED (writes === 0) after takeover, rather
than inspecting the output afterwards."
- 2026-07-17T01:05:05Z @neo-opus-ada referenced in commit `9689313` - "feat(memory-core): producer channel submission with monotonic watermarks (#15296)

A producer knows one truth — its own envelope, watermark, and expiry — and knows
nothing about the other channels or the published resource. So it hands over one
row and the writer composes: producers never read or write current.json and never
merge another producer's channel. Anything else lets a producer publish a picture
it cannot see.

The three watermark cases are genuinely different facts, and collapsing any two
is the defect class:

- A regressed watermark is rejected. Sources retry and race; without monotonicity
  an in-flight older read silently moves the projection backwards in time.
- An equal watermark with an identical payload is idempotent. Replay is normal —
  treating it as an error makes every transient failure a false conflict.
- An equal watermark with a different payload is a source conflict. Two payloads
  claiming one point in time cannot both be true and there is no basis to prefer
  either, so the prior is kept rather than arbitrarily replaced, and the channel
  degrades. Guessing would publish a coherent-looking lie.

A strictly newer watermark clears an earlier conflict: the contested instant is
no longer what the channel describes, and carrying the conflict forward would
degrade a channel that has since become coherent.

Adds conflict_reason, which the shown ADR schema does not carry. A conflict has
to survive to the reader — the channel row is the only thing the writer reads at
publish time, so a conflict living only in a return value would vanish when the
producer moved on and the next publication would render a contested channel as
clean. Flagged for review as the one place this extends the shown schema.

Admission and producer identity are injected gates, enforced here but decided
outside: the store never infers who a producer is from what it submitted.

Evidence: hookProjectionSubmission 8/8 against a real SQLite store."
- 2026-07-17T01:05:05Z @neo-opus-ada referenced in commit `af9e3c6` - "feat(memory-core): the atomic projection transport and its config leaves (#15296)

The path is derived, never supplied. A producer submits an envelope for an
admitted target; it does not get to say where that lands. Accepting a
caller-supplied path would make every producer a potential arbitrary-write
primitive — the target id is server-derived precisely so the location follows
from admission rather than from an argument. The id is re-checked at this
boundary rather than trusted: this is where it becomes a filesystem path, and a
traversal segment reaching here would escape the owned root.

Flush precedes rename. A rename that beats its own data to disk publishes a
complete-LOOKING file with a torn body after a crash — the one outcome a reader
cannot detect, since it polls with no lock and no retry. The temp sibling is
unique per attempt and same-directory, so the rename is same-filesystem and
atomic, and a retry can never collide with an in-flight attempt.

Note the division of labour: the rename being atomic stops a torn READ; the
serialized transaction around it (the lease) stops a STALE WRITER. Different
problems — the rename alone solves only the first, which is why the epic rejected
rename-alone.

A failed attempt cleans up its temp sibling, and a cleanup failure never masks
the original error: the caller needs the cause, not the janitor's complaint.

leaseTtlMs and the projection root are config leaves with no primitive-local
default. The TTL doubles as the worst-case wait before a crashed holder's target
frees itself, so it is a real operational dial rather than a magic number.

Evidence: hookProjectionTransport 8/8, hookProjectionSubmission 8/8,
hookProjectionLease 9/9. config-template SSOT lint OK."
- 2026-07-17T01:05:05Z @neo-opus-ada referenced in commit `427d924` - "fix(memory-core): make the lease and transport actually compose (#15296)

The reviewer found that this code cannot run. `publishProjection()` called
`writeAtomic({channels})` while the transport required `{targetId, channels}`, so
targetId arrived undefined and the path resolver threw before any write. Both
suites were green because each supplied its own stub — the lease's ignored its
arguments, the transport's was called directly — and neither stub was the other
module. Two green suites over zero working publications.

The new composition suite is the repair for the class, not just the instance: the
real lease calls the real transport with no stub between them. A stub can only
assert the contract its author believed in, so when module A calls module B, at
least one test must let A call the real B.

Also closed:

- The transport appended a `hook-projections` segment to a config root that
  already ended in one, writing under a path no reader would look at.
- The published payload was a bare `{channels}` list. A reader binds to a
  contract version and needs the target, epoch, publish time and per-channel
  watermarks to judge what it is holding, so publication now carries the
  `live-lane-awareness-projection.v1` envelope.
- `conflict_reason` was recorded by submission and never read at publish, which
  made the entire conflict mechanism inert: a contested watermark published as
  clean. It now reaches the reader, and `degradedChannels` names it.
- The column lived only in a migration in another module while publish read it,
  so the two halves disagreed about the table. It is part of the table
  definition; the migration remains for stores created before it.

Evidence: hookProjectionComposition 3/3 (new), hookProjectionLease 9/9,
hookProjectionSubmission 8/8, hookProjectionTransport 8/8.

Not yet closed from the same review, and not claimed as done: expiry is still
evaluated against the caller-captured clock rather than at the write boundary;
releaseProjectionLease has no production caller; a failed write leaves the lease
held; takeover does not sweep crash-orphan temp siblings; the fencing test still
uses one connection and cannot demonstrate serialization; nothing is wired into
production. Continuing in this lane."
- 2026-07-17T01:05:05Z @neo-opus-ada referenced in commit `e798ca6` - "test(memory-core): prove the fence under a real two-connection race (#15296)

The reviewer's sharpest finding: the test named "THE FENCING PROPERTY" could not
have demonstrated it. One `:memory:` connection cannot contend with itself, and
the takeover ran BEFORE the stale holder's call — so it only ever asserted that
an epoch comparison rejects a stale epoch. An implementation that revalidated
first and renamed afterwards, leaving exactly the window the fence exists to
close, would have passed it. My most confident test was theater.

The real property needs a real race. Two connections against a file-backed store,
with the competing acquisition attempted WHILE the holder's publication
transaction is open: the rival receives SQLITE_BUSY, and acquires immediately
once that transaction commits. Even an expired-lease takeover — which would
otherwise succeed — cannot commit mid-publication. The transaction is the fence;
the epoch only describes it.

The second test is the control: without it, SQLITE_BUSY could mean the fence
works or that nothing can ever acquire again.

The old test keeps its real property under an honest name (epoch rejection), with
a note that it is necessary and not sufficient.

Reviewer-supplied method, verified independently before adopting: probed
file-backed two-connection behavior directly — SQLITE_BUSY while held, OK after
commit.

Evidence: hookProjectionFence 3/3 (new), hookProjectionLease 9/9,
hookProjectionSubmission 8/8, hookProjectionTransport 8/8,
hookProjectionComposition 3/3."
- 2026-07-17T01:05:05Z @neo-opus-ada referenced in commit `0ccea3b` - "fix(memory-core): bound the lease at the mutation boundary and require durability (#15296)

Cluster 2 of the reviewer's convergence packet, plus the durability half of
cluster 3.

Expiry was checked against a caller-captured number at entry. Parsing and reading
rows take time, so a render that stalled across its TTL still published and
reported success — a bounded lease that cannot observe time passing is not
bounded. The clock is now injected as a function and read again at the mutation
boundary; crossing the TTL aborts, because Wave 1 has no renewal and the target
is about to become someone else's.

The lease was never released. A single-publication lease that stays held parks
its target until expiry after success, and parks it after a disk failure while
the holder has already given up. Both paths release now, token+epoch conditional
so it can only ever free the holder's own lease.

The failure path releases and RETURNS rather than throwing, which is not a style
choice: better-sqlite3 rolls the transaction back on a thrown error, so releasing
and then throwing would undo the release along with it — reintroducing the exact
parking this fixes. The cause travels in `reason`, because a disk failure still
has to leave the store usable by the next holder.

Durability was optional. An fs implementing only mkdir/write/rename published
successfully with no flush, and — the part that stings — the composition double I
wrote to prove correctness lacked the flush methods, so it silently blessed that
downgrade. A double that under-implements a contract quietly weakens it. A rename
that beats its own data to disk publishes a complete-LOOKING file with a torn
body, which is the one outcome a reader cannot detect. The transport now refuses
an fs that cannot flush, and the double honours the full contract.

Evidence: hookProjectionLease 11/11 (2 new: TTL-crossed-during-write aborts;
released on both paths), hookProjectionTransport 8/8, hookProjectionComposition
3/3, hookProjectionFence 3/3, hookProjectionSubmission 8/8.

Still open from the same packet, unclaimed: production wiring (schema bootstrap,
config consumer, target derivation, token/clock/fs owners, invocation); the
nested publication/consumerBinding/fixed-slot envelope; one malformed channel
aborting valid siblings; takeover orphan-temp sweep."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `57c482d` - "fix(memory-core): isolate an unreadable channel and sweep crash orphans (#15296)

Two more from the reviewer's convergence packet.

One malformed channel aborted every valid sibling. A bare JSON.parse inside a map
meant a single torn envelope threw out of the whole publication, so a damaged
lifecycle-frontier row also denied the reader a perfectly good computed-route —
punishing every channel for one channel's damage. Each row is isolated now: an
unreadable one withholds its envelope rather than guessing, names why, and lands
in degradedChannels. "This is broken" is a different fact from "this does not
exist", and only the first tells the reader to wait rather than act.

Takeover left crash orphans behind. A holder that died mid-write never reached
its own cleanup, so its temp sibling survives under a root the reader also scans,
and nothing else will ever remove it: the process is gone, and the next holder is
the only party that knows the target is unowned. sweepOrphans removes exactly the
`current.json.*.tmp` siblings and nothing else — a sweep that ate current.json
would turn litter-collection into an outage.

The sweep is best-effort by design. Refusing to publish because cleanup failed
would let a litter problem masquerade as an availability one.

Both test doubles now honour readdirSync too. The fs contract has one shape; a
double that implements less quietly weakens it — which is exactly how the missing
fsync slipped through earlier in this lane.

Evidence: hookProjectionTransport 10/10 (2 new: sweep selectivity, sweep failure
survivable), hookProjectionComposition 4/4 (1 new: one torn row does not deny the
others), hookProjectionLease 11/11, hookProjectionSubmission 8/8,
hookProjectionFence 3/3.

Remaining from the packet, unclaimed: production wiring (schema bootstrap, config
consumer, target derivation, token/clock/fs owners, invocation) and the nested
publication/consumerBinding/fixed-slot envelope."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `77d02b6` - "feat(memory-core): publish ADR 0035 §2.7's canonical envelope, not a flat one (#15296)

RA-2 from the reviewer's landing coordinates. My envelope was flatter than the
contract, and — the part that matters — the tests I wrote to prove it blessed the
wrong shape. A test can only ratify the contract its author believed in, so
reading §2.7 was the fix; inventing the shape from the field names I happened to
need was the defect.

The published projection is now `{schemaVersion, publication{targetId,
fencingEpoch, generatedAt, producerWatermarks}, consumerBinding, lifecycleActions,
computedRoute, contextViews, coverage, notAuthority}`.

Slot-shaped, not list-shaped, and that is the whole design rather than a
formatting preference: the writer selects the newest accepted envelope per FIXED
slot and never read-modify-writes the file, so a lifecycle update cannot erase the
current route and a route update cannot erase lifecycle. A flat channel list made
every publication a whole-file replacement — one producer's silence would have
deleted another producer's channel.

A slot with no accepted channel is honestly `missing` rather than absent: a reader
cannot distinguish "this producer never published" from "this key is not part of
the contract" when the key simply is not there.

Every channel keeps its OWN status, watermark, captured time, expiry and
citations. Collapsing them into one envelope-level status would let a fresh route
inherit a degraded lifecycle's reputation — or worse, hide it. Unreadable stays
degraded rather than missing, because "this is broken" tells a reader to wait
while "this does not exist" tells it to act.

`consumerBinding` is threaded from the caller: it is what a reader validates
itself against before acting, so the writer cannot invent it.

The transport writes the envelope VERBATIM. It reshaped the payload before, which
made it a second, silent author of a contract it does not own.

Evidence: hookProjectionComposition 4/4, hookProjectionLease 11/11,
hookProjectionTransport 10/10, hookProjectionFence 3/3, hookProjectionSubmission
8/8. jsdoc + block-alignment green.

RA-1 (production wiring) remains open and unclaimed: no bootstrap caller, config
consumer, target derivation, token/clock/fs owner, or invocation. Naming it rather
than implying this publishes in production."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `8aec5c3` - "feat(memory-core): the production owner — bind the projection primitives to real edges (#15296)

RA-1. The lease, the submission gate and the transport were each hermetic and each
testable alone, which is exactly why nothing published: a pile of correct
primitives with no owner is not a feature, it is substrate that decays while
looking finished. Every one of the reviewer's missing pieces — schema bootstrap,
target derivation, token/hash source, broker clock, filesystem owner, invocation
— exists because there was no single place responsible for them.

This is that place, and "publish a projection" is now one call rather than an
assembly job at the call site:

- The schema is created against the shared store at boot, idempotently, so
  publish never reads a table its own deployment never made.
- The target id is derived server-side from the attested tuple. Field-separated,
  so ('a','bc') and ('ab','c') cannot collapse onto one target, and any hole in
  the tuple is refused — a hole would merge two agents onto one projection, which
  is the failure never-foreign exists to prevent. Session id stays out: the same
  agent after a restart must reach the SAME target, or every restart would orphan
  its own projection.
- The token is real randomness; only its digest is stored, because the lease's
  whole safety argument assumes an unguessable token whose hash is what a
  competing writer reads.
- The clock is the live broker clock, read at the mutation boundary.
- The filesystem is rooted at the config-owned projection root, and the caller
  never chooses a path — it names an attested target.

Contention returns `held` + retry metadata rather than throwing: another holder
publishing the same target is a normal outcome, not an error. An unavailable
store fails closed — a missing store is not an empty projection.

Everything is injected rather than imported. The owner is where policy meets the
world, and a seam that cannot be substituted cannot be tested against the failures
that matter.

Evidence: hookProjectionWriter 7/7 (new — the first suite that runs the REAL
lease, submission gate and transport together, with only the clock and fs
substituted), hookProjectionLease 11/11, hookProjectionSubmission 8/8,
hookProjectionTransport 10/10, hookProjectionComposition 4/4, hookProjectionFence
3/3. jsdoc + block-alignment green.

Still not registered as an MCP tool: publishing on a schedule is the §2.11 reader
cutover's business, and this PR deliberately stops at making publication possible,
correct, and callable."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `293c8cb` - "fix(memory-core): sweep only after winning the lease, and make the file reviewable (#15296)

Two release blockers from the reviewer's exact-head V-B-A.

The sweep ran BEFORE acquisition, so a LOSING contender deleted the live holder's
in-flight `current.json.*.tmp` between its write, flush and rename — one publisher
corrupting another's mutation window, which is the exact failure the fence exists
to prevent. ADR §2.6.3 puts takeover cleanup after token+epoch acquisition for
this reason. My "tidy up before starting" instinct inverted it: cleanup is a right
the token confers, not a courtesy anyone may perform, because only the holder
knows the target is unowned.

The witness proves it rather than describing it: a contender that loses the lease
performs zero deletions while a holder is live. Verified by regression — moving
the sweep back above `acquireProjectionLease` turns the test red, so it catches
the defect rather than passing either way.

The module also carried a RAW NUL byte in `join('\0')` instead of the two-character
source escape, so git and grep both classified it as binary: 0 additions, 0
deletions, and the entire production owner was invisible in review. A reviewer
caught a file nobody could read. NUL remains the right separator — a space can
occur inside a field value and NUL cannot — so the semantic stands and only the
encoding changes, matching `birdViewCitations.getSourceManifestKey`.

Evidence: hookProjectionWriter 8/8 (1 new: losing contender deletes nothing),
hookProjectionLease 11/11, hookProjectionTransport 10/10,
hookProjectionComposition 4/4, hookProjectionFence 3/3, hookProjectionSubmission
8/8. Block-alignment green."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `33216d6` - "fix(memory-core): assert the deadline at the rename and honour channel expiry (#15296)

The reviewer's two carried findings. Both are the same habit in different
clothes: a check that exists but does not bind at the moment it matters.

The TTL was sampled BEFORE writeAtomic, which only proved the lease was live
before the I/O — and the I/O is the part that takes time. The transport then did
write → fsync → rename with no deadline anywhere, so a publication that crossed
its no-renewal window during the flush still landed. The rename IS the mutation,
so the deadline is asserted there; aborting routes into the release-and-report
path, and a target is never left held by a writer that ran out of window. I had
moved this check closer to the boundary once already and called it done.

Channel expiry was stored and never enforced: projectChannel marked every
parseable row `fresh`, so a channel whose producer stopped hours ago read as
current. Recording an expiry and then ignoring it is worse than not recording
one — the reader trusts a field the writer never honoured. Expired channels are
`stale` with their reason, and stale counts as degraded coverage so a reader
scanning `degradedSources` need not re-check every channel by hand.

Submission also admitted anything object-shaped, including an array, and stored
capture/expiry as opaque strings. An envelope now needs a plain-object shape and
a schemaVersion — a reader binds to it — and the window must parse and be
ordered, since a channel that expires before it was captured was never valid for
an instant.

Evidence: hookProjectionLease 12/12, hookProjectionComposition 5/5,
hookProjectionSubmission 8/8, transport 10/10, fence 3/3, writer 8/8. Both new
tests falsified: with the rename-time deadline and the expiry check removed, each
goes red.

The composition fixture also stopped lying to itself: it dated channels 2026
while running a 2027 broker clock, so every fixture channel was genuinely expired
and the new check found it. The fixture now derives its window from the same
clock the broker uses."
- 2026-07-17T01:05:06Z @neo-opus-ada referenced in commit `636a08a` - "fix(memory-core): bind each channel to its schema at admission (#15296)

Submission required only that an envelope carry SOME nonempty
schemaVersion. That proved the envelope was typed, never that it was
typed correctly for the slot it was landing in, so channel
computed-route carrying a lifecycle-frontier.v1 payload advanced: the
store held a row whose slot and contents disagreed, and a reader bound
to it at render time.

A channel and its schema are one fact. PROJECTION_CHANNEL_SCHEMAS pins
the binding next to the slot registry that already defines the channels,
and submission enforces it after producer identity, so an unauthorized
producer is still reported as foreign rather than as a schema problem.

A channel absent from the registry is rejected rather than waved
through. An unbindable payload is the failure the registry exists to
prevent, so an unknown channel cannot be the one case that passes.

Falsified: on the prior module the mismatch spec reports accepted:true;
with the binding it rejects and never writes. 49/49 hookProjection specs
pass. The correct-schema case for the same channel is asserted too — a
blanket refusal would satisfy the rejection spec and prove nothing."
- 2026-07-17T01:05:07Z @neo-opus-ada referenced in commit `1ae816f` - "fix(memory-core): only a still-recognized holder may sweep (#15296)

Sweeping ran at the writer's call site, gated on `lease.acquired` — a
PAST-TENSE fact. It says a lease was won once, not that it is still
held, so @neo-gpt's sequence was reachable: epoch-1 expires, epoch-2
creates current.json.b.tmp, the stale epoch-1 holder resumes and sweeps
that live temp away, and only THEN is told it was superseded — while
epoch-2's rename fails ENOENT. The rejected caller lost nothing and the
valid one lost everything.

Moving the sweep below `lease.acquired` earlier was necessary and not
sufficient. Only the store, inside the serialized transaction, knows the
present tense.

sweepOrphans is now injected into publishProjection and called INSIDE
its IMMEDIATE transaction, after the four revalidations (not-held,
superseded-epoch, foreign-token, lease-expired) and before writeAtomic.
Each rejection returns before the sweep, so a superseded, foreign or
expired caller can no longer damage a live successor on its way out.
The injection is required and fails loud; an unswept publish leaks temp
siblings, so a silently absent sweeper is not an acceptable default.

Falsified: with the sweep at the call site the new witness records the
stale holder deleting both visible temps; with it inside the transaction
it records none. 50/50 hookProjection specs pass.

The witness stages the stall through the clock the writer already
injects — the first read is the acquisition, every later read is
revalidation. No new seam was added to make the test possible."

