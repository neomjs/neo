---
id: 13074
title: 'Relocate MC healthcheck migration-census to an on-demand surface (compute slice of #12768)'
state: CLOSED
labels:
  - enhancement
  - ai
assignees:
  - neo-opus-grace
createdAt: '2026-06-13T07:29:57Z'
updatedAt: '2026-06-21T00:17:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13074'
author: neo-opus-grace
commentsCount: 0
parentIssue: 12768
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-13T14:20:01Z'
---
# Relocate MC healthcheck migration-census to an on-demand surface (compute slice of #12768)

## Context

The **compute** slice of #12768 (*"Trim the Memory Core healthcheck to a lean liveness probe"*), companion to the context-tax slice #13069 (PR #13071). Where #13069 removed two static/redundant fields, this removes the healthcheck's per-call **batch-scan**.

## The Problem

The healthcheck payload runs the migration census on **every** call: `#checkChromaMigrationState` → `#scanChromaMetadata` **batch-reads the full memory + summary Chroma collections** (`batchSize 2000`, ~17.9k records), an `O(records)` cost on a probe that fires at boot, self-repair, and monitoring intervals. The census is **operator-scraped** — no programmatic consumer reads `healthcheck.migration` — so it does not belong on the hot path.

## The Fix

Mirror the established on-demand pattern (`graphLifecycleReport.mjs` + `GraphService.getLifecycleCensus`):

- Add `HealthService.getMigrationCensus({includeChroma})` — cheap SQLite untagged-userId counts always; the `O(records)` ChromaDB scan only when `includeChroma` is set.
- Add `ai/scripts/maintenance/migrationCensusReport.mjs` (+ `npm run ai:migration-census-report`) — a thin operator entrypoint; `--chroma` opts into the scan, `--json` emits raw.
- **Remove** the `migration` field + the Step-1.5 `payload.migration.chromadb` mutation from `#performHealthCheck`. The private census methods + `buildChromaMigrationStats` remain (now reached via `getMigrationCensus`).

## Contract Ledger

| Field | Value |
|---|---|
| **Target Surface** | `ai/services/memory-core/HealthService.mjs` (`getMigrationCensus`), `ai/scripts/maintenance/migrationCensusReport.mjs` (NEW), `package.json` |
| **Source of Authority** | #12768 (parent trim ticket — AC: "migration census moved to an on-demand surface; healthcheck no longer batch-scans collections per call") |
| **Proposed Behavior** | Healthcheck no longer runs the census/batch-scan; operators get it on demand via the script/method; cheap SQLite default, Chroma scan opt-in |
| **Fallback** | Graph not mounted / Chroma unreachable → `available: false` (substrate-readiness, not error), surfaced by the report |
| **Evidence** | New `migrationCensusReport.spec.mjs` green; `HealthService.spec.mjs` green (no `payload.migration` assertions existed) |

## Acceptance Criteria

- [ ] `getMigrationCensus({includeChroma})` on `HealthService` — cheap SQLite always, Chroma scan opt-in.
- [ ] `migrationCensusReport.mjs` + `ai:migration-census-report` npm script, mirroring `graphLifecycleReport`.
- [ ] `migration` field + Step-1.5 batch-scan removed from the healthcheck; the private methods + `buildChromaMigrationStats` retained.
- [ ] Unit specs: new script spec (parseArgs / formatCensus / runReport) + `HealthService.spec.mjs` green.

## Out of Scope (remains in #12768)

- **`features.wake.gateReason` trim** — a different block (`buildWakeFeaturesBlock`), debatable (a tripped-gate reason is arguably wanted in-band), with a test asserting current behavior. Left for a separate decision in #12768.

## Related

`Refs #12768` (parent trim umbrella; delivers its migration-census AC). Mirrors `graphLifecycleReport` / `GraphService.getLifecycleCensus`.

Release classification: **post-release** (token/compute-cost observability hygiene; nothing v13.x-blocking).

Authored by Claude Opus 4.8 (Claude Code, @neo-opus-grace / Grace).

## Timeline

- 2026-06-13T07:29:58Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-06-13T07:29:59Z @neo-opus-grace added the `enhancement` label
- 2026-06-13T07:29:59Z @neo-opus-grace added the `ai` label
- 2026-06-13T07:31:01Z @neo-opus-grace cross-referenced by PR #13075
- 2026-06-13T07:31:51Z @neo-opus-grace added parent issue #12768
- 2026-06-13T08:40:56Z @neo-opus-grace referenced in commit `df4b15b` - "docs(memory-core): point migration-census docs at the on-demand surface (#13074)

Addresses @neo-opus-vega's #13075 review (code approved; docs were the only blocker). The healthcheck.migration field was relocated to the on-demand getMigrationCensus() / ai:migration-census-report surface; the docs still taught the removed field:

- MultiTenantMigrationGuide.md §5 + runbook: restate the migration-census observability + verification steps to run 'npm run ai:migration-census-report' (getMigrationCensus({includeChroma})) and read graph.total, instead of scraping healthcheck.migration.untaggedCount.
- HealthService.mjs #checkMigrationState JSDoc: point at getMigrationCensus / the script, noting the census is off the healthcheck hot path.

Note: vega's cited MemoryCoreMcpAuth.md:407 cross-ref has no migration reference (verified) — nothing to sweep there."
- 2026-06-13T08:42:01Z @neo-opus-grace cross-referenced by #12768
- 2026-06-13T08:54:58Z @neo-opus-grace referenced in commit `e02f50c` - "docs(memory-core): repoint the last MemoryCoreMcpAuth cross-ref to the on-demand migration-census (#13074)

@neo-opus-vega's Cycle-2 #13075 catch: MemoryCoreMcpAuth.md:407's cross-ref still described the removed 'healthcheck.migration.untaggedCount' operator guidance. (My earlier 'no ref there' was a wrong-path grep — the file is at learn/agentos/tooling/, not learn/agentos/ — V-B-A miss; vega had it right.) Repointed to the on-demand migration-census (ai:migration-census-report)."
- 2026-06-13T14:20:02Z @tobiu closed this issue
- 2026-06-13T14:20:02Z @tobiu referenced in commit `3a12797` - "refactor(memory-core): relocate healthcheck migration-census to an on-demand surface (#13074) (#13075)

* refactor(memory-core): relocate healthcheck migration-census to an on-demand surface (#13074)

The healthcheck ran the migration census on every call: #checkChromaMigrationState -> #scanChromaMetadata batch-reads the full memory + summary Chroma collections (batchSize 2000, ~17.9k records), an O(records) cost on a probe fired at boot/self-repair/monitoring. The census is operator-scraped (no programmatic consumer of healthcheck.migration), so it does not belong on the hot path.

Mirrors the graphLifecycleReport / GraphService.getLifecycleCensus precedent: new HealthService.getMigrationCensus({includeChroma}) (cheap SQLite always, Chroma scan opt-in) + ai/scripts/maintenance/migrationCensusReport.mjs (ai:migration-census-report; --chroma opts into the scan). Removes the migration field + Step-1.5 batch-scan from the healthcheck; the private methods + buildChromaMigrationStats remain (reached via getMigrationCensus).

migrationCensusReport.spec.mjs green; HealthService.spec.mjs green (no payload.migration assertions existed). Compute slice of #12768.

* docs(memory-core): point migration-census docs at the on-demand surface (#13074)

Addresses @neo-opus-vega's #13075 review (code approved; docs were the only blocker). The healthcheck.migration field was relocated to the on-demand getMigrationCensus() / ai:migration-census-report surface; the docs still taught the removed field:

- MultiTenantMigrationGuide.md §5 + runbook: restate the migration-census observability + verification steps to run 'npm run ai:migration-census-report' (getMigrationCensus({includeChroma})) and read graph.total, instead of scraping healthcheck.migration.untaggedCount.
- HealthService.mjs #checkMigrationState JSDoc: point at getMigrationCensus / the script, noting the census is off the healthcheck hot path.

Note: vega's cited MemoryCoreMcpAuth.md:407 cross-ref has no migration reference (verified) — nothing to sweep there.

* docs(memory-core): repoint the last MemoryCoreMcpAuth cross-ref to the on-demand migration-census (#13074)

@neo-opus-vega's Cycle-2 #13075 catch: MemoryCoreMcpAuth.md:407's cross-ref still described the removed 'healthcheck.migration.untaggedCount' operator guidance. (My earlier 'no ref there' was a wrong-path grep — the file is at learn/agentos/tooling/, not learn/agentos/ — V-B-A miss; vega had it right.) Repointed to the on-demand migration-census (ai:migration-census-report)."

