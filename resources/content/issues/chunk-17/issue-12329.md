---
id: 12329
title: 'GraphLog grows unbounded: no compaction past consumer watermark'
state: CLOSED
labels:
  - bug
  - ai
  - architecture
  - performance
assignees:
  - neo-gpt
createdAt: '2026-06-01T22:22:55Z'
updatedAt: '2026-06-07T17:18:03Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12329'
author: neo-opus-ada
commentsCount: 6
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking:
  - '[x] 12394 Schedule GraphLog compaction as a recurring Orchestrator maintenance lane'
closedAt: '2026-06-02T19:34:05Z'
---
# GraphLog grows unbounded: no compaction past consumer watermark

## Context

Investigating the operator's flag that the graph at `.neo-ai-data/sqlite` is "way more worrying" than the chroma test-collection leak (#12143). Empirical read of the live production graph db (`memory-core-graph.sqlite`, 2026-06-02):

- DB size: **441 MB** (112,951 pages × 4096; `freelist=0` → not un-vacuumed dead space, it is live rows).
- `Nodes`: 74,101 rows (~35 MB) · `Edges`: 53,307 rows (~25 MB) — the actual Native Edge Graph, healthy.
- **`GraphLog`: 7,328,097 rows (~365 MB) — 83% of the db.**
- GraphLog composition by `entity_type`: `edges` 7,166,812 (98%) · `nodes` 160,573 · `heartbeat_pulse` 712.

## The Problem

`GraphLog` is a **change-data-capture (CDC) log**: triggers append a `(log_id, entity_id, entity_type)` row on every `Nodes`/`Edges` INSERT/UPDATE/DELETE so remote workers can sync graph diffs incrementally. It is **append-only and never compacted** → it grows monotonically and unbounded (7.3M rows / 365 MB and counting; the `latest GraphLog: NNNN` in every heartbeat pulse is this autoincrement id).

The 98%-`edges` composition reflects heavy edge churn: only ~53K edges currently exist, but 7.17M edge-mutations have been logged (~134 logged mutations per surviving edge), consistent with Hebbian decay/reinforcement updating edge weights frequently. Each mutation is logged forever.

This is the graph-store analog of the chroma test-collection leak (#12143 / #12180), but more serious: it is the **core Native Edge Graph store**, it grows during normal operation (not just tests), and every one of the ~22 GB of daily `.neo-ai-data/backups` bundles carries the full 365 MB GraphLog — so GraphLog's unbounded growth inflates each (retention-swept) backup bundle too.

## The Architectural Reality

- **Producers** — `ai/graph/storage/SQLite.mjs:130,135` (+ the INSERT/UPDATE triggers): `CREATE TRIGGER ... AFTER DELETE ON Nodes/Edges BEGIN INSERT INTO GraphLog(entity_id, entity_type) VALUES (OLD.id, 'nodes'/'edges'); END;`
- **Consumers — all strictly watermark-based, never full-replay:**
  - `ai/daemons/bridge/queries.mjs:110` — `SELECT log_id, entity_id, entity_type FROM GraphLog WHERE log_id > ? ORDER BY log_id ASC` (`lastSyncId`)
  - `ai/graph/storage/SQLite.mjs:341` — same `WHERE log_id > ?` (`sinceId`)
  - `ai/services/memory-core/WakeSubscriptionService.mjs:736` — same `WHERE log_id > ?`
  - Watermark reads: `queries.mjs:22`, `SQLite.mjs:325/439`, `WakeSubscriptionService.mjs:106` (`SELECT MAX(log_id)`).
- **No compaction exists.** `ai/scripts/maintenance/defragSQLiteDB.mjs` is a VACUUM (useless here — `freelist=0`), and `recreateGraphDb.mjs` is a full rebuild. Nothing prunes GraphLog.

Because every consumer reads strictly `WHERE log_id > <its last-seen watermark>`, **rows with `log_id <= min(all live consumers' watermarks)` are dead and safe to delete** — standard CDC-log truncation.

## The Fix

A GraphLog compaction step that deletes rows at or below the minimum live-consumer watermark, run periodically (a maintenance script invoked by the orchestrator/cron, or a low-frequency daemon task):

1. Determine the **min consumer watermark** across all live consumers (bridge daemon, WakeSubscriptionService, any remote workers). If consumer positions are already tracked durably, use their min; otherwise persist them, or fall back to a conservative retention (never prune above `MAX(log_id) - SAFETY_MARGIN`).
2. `DELETE FROM GraphLog WHERE log_id <= :minWatermark;` then checkpoint the WAL so the on-disk file actually shrinks.
3. One-time compaction of the existing 7.3M-row backlog (operator-gated — live store).

**Invariant (safety contract):** never delete a row any live consumer has not yet synced — prune strictly at or below the min watermark.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `GraphLog` row lifecycle (consumed via `WHERE log_id > watermark`) | `ai/graph/storage/SQLite.mjs` + `ai/daemons/bridge/queries.mjs:110` | Rows compacted once `log_id <= min(live-consumer watermarks)`; incremental-sync semantics for `log_id > watermark` unchanged | If a consumer's watermark is unknown/unpersisted, do NOT prune above the most-conservative known position; never prune above `MAX(log_id) - SAFETY_MARGIN` | new maintenance-script JSDoc (sibling to `defragSQLiteDB.mjs`) | Integration test: producer appends N, consumer syncs to K, compaction at K leaves rows `> K` intact + removes `<= K` |
| Consumer watermark durability (bridge daemon, WakeSubscriptionService) | `ai/daemons/bridge/daemon.mjs`, `WakeSubscriptionService.mjs` | Each live consumer's last-synced `log_id` is durably readable so compaction can compute the min | A consumer with no persisted watermark blocks pruning above the conservative floor (fail-safe) | consumer JSDoc | Unit: min-watermark computation across ≥2 consumers |

## Decision Record impact

`none` — no existing ADR governs GraphLog retention; this ticket establishes the compaction policy. Sibling to ADR 0003's unified-store topology (which #12143 cites for chroma); GraphLog retention is the graph-store equivalent gap.

## Acceptance Criteria

- [ ] AC1 — Measure + report current GraphLog row count + byte size (replaces the ~7.3M / 365 MB snapshot).
- [ ] AC2 — A compaction mechanism deletes GraphLog rows with `log_id <= min(live-consumer watermarks)`, runnable periodically; statically verifiable that it never prunes above the min watermark.
- [ ] AC3 — No live consumer (bridge daemon, WakeSubscriptionService, remote workers) loses an unsynced entry (integration test: append → partial sync → compact → unsynced rows intact).
- [ ] AC4 — Post-compaction the graph db size drops toward `Nodes`+`Edges` (~60 MB) + a bounded GraphLog; verified by a follow-up `page_count` read.
- [ ] AC5 — WAL checkpointed after compaction so the on-disk db shrinks (post-merge / operator-gated for the one-time backlog purge).

## Out of Scope

- `Nodes`/`Edges` garbage collection / Hebbian fade (#9945) — this ticket prunes the CHANGE-LOG, not the graph itself.
- `.neo-ai-data/backups` size (~22 GB) — **NOT** a separate retention bug. `ai/scripts/maintenance/backup.mjs` already runs a 30-day retention sweep (`cleanOldBackups`, `keepMinimum=3` / `maxDays=30`, config-driven via `aiConfig.maintenance.backup.retention`). The ~22 GB is ~30 days of large daily bundles, each large because it carries the 365 MB GraphLog; compacting GraphLog (this ticket) shrinks every future bundle proportionally. If still too large afterward, that is a `maxDays`/`keepMinimum` config tune, not code.
- chroma test-collection leak + in-store `backup-<ts>` folders (#12143 / #12180).
- `test-daemon-*.sqlite` files leaking into the graph sqlite dir (SQLite analog of #12143; separate sibling).

## Related

- #9945 (graph Hebbian decay + GC — the Nodes/Edges side)
- #10158 (Memory/Session graph nodes: telemetry + retention policy)
- #12143 / #12180 (chroma test-collection leak — sibling data-hygiene)

Origin Session ID: da9a6007-1250-4363-8c15-dff69eccb3be
Retrieval Hint: "GraphLog CDC log unbounded compaction watermark"

## Timeline

- 2026-06-01T22:22:56Z @neo-opus-ada added the `bug` label
- 2026-06-01T22:22:57Z @neo-opus-ada added the `ai` label
- 2026-06-01T22:22:57Z @neo-opus-ada added the `architecture` label
- 2026-06-01T22:22:57Z @neo-opus-ada added the `performance` label
- 2026-06-01T22:24:45Z @neo-opus-ada cross-referenced by #12143
### @neo-opus-ada - 2026-06-01T22:27:38Z

## Watermark durability — compaction can prune to a real min, not a blind floor

Verified how consumers track their GraphLog position (the AC2/AC3 safety crux):

- **Bridge daemon** — durably persists its watermark: `ai/daemons/bridge/daemon.mjs:53` `STATE_FILE = <DAEMON_DATA_DIR>/lastSyncId`; after each sync it `fs.writeFileSync(STATE_FILE, lastSyncId)` (`:305-306`) and reads it on boot (`:791`). So the bridge's last-synced `log_id` is readable even when the daemon is down.
- **WakeSubscriptionService** — tracks a `liveCursor` (in-memory) plus per-subscription `lastLogId` watermarks (`:140/152/170/704`); subscriptions carry a `sinceLogId` replay anchor (`:677/696`). Active subscriptions' watermarks (and any intended replay-from point) bound how far back the log must be retained.

**Implication for the fix:** the min live-consumer watermark is computable from durable state — `min(bridge lastSyncId, min(active-subscription lastLogId / replay anchors))` — so compaction can prune to a real min watermark rather than a blind keep-last-N retention. A small `SAFETY_MARGIN` below the min is still prudent (guards an in-flight sync between the watermark read and the DELETE).

Refines AC2/AC3: compute the min across BOTH consumer classes (bridge file + active subscriptions); any consumer whose watermark can't be resolved (e.g. a removed-but-uncleaned subscription) must fail safe — block pruning above the conservative floor rather than assume it's caught up.

— @neo-opus-4-7


### @neo-opus-ada - 2026-06-01T22:32:44Z

## Correction (V-B-A): backups DO have retention — the ~22 GB is GraphLog-driven, not missing retention

I asserted in this ticket's Out-of-Scope + the `[ticket-created]` A2A that `.neo-ai-data/backups` is "unbounded / no retention." **That was wrong** — I checked `ai/scripts/maintenance/backup.mjs` after filing:

- `cleanOldBackups(backupRoot, logger, retention)` (`:384`) runs a **retention sweep** on every backup (step `[7/7]`, `:209`): `keepMinimum=3` (newest 3 retained unconditionally) + `maxDays=30` (bundles older than 30 days eligible for deletion). Config-driven via `aiConfig.maintenance.backup.retention` (`:139`); defaults `K=3 / N_DAYS=30`.
- The observed ~22 GB is **~30 days of large daily bundles** (~1–2 GB each), not missing retention. Each bundle is large because it carries the 365 MB GraphLog (+ chroma).

So backup retention is **working as designed** — there is no separate retention bug to file. The lever is THIS ticket: compacting GraphLog shrinks every future bundle (and the graph db) proportionally. If ~22 GB is still too much after compaction, that's a config tune of `maxDays`/`keepMinimum`, not a code fix.

(The `chroma/unified/backup-<numeric-ts>` folders flagged on #12143 are a *separate* source — `backup.mjs` writes to `.neo-ai-data/backups/`, not inside the chroma persist dir — likely a chroma defrag/compaction pre-backup; source still to confirm on #12143.)

— @neo-opus-4-7


### @neo-opus-ada - 2026-06-01T22:44:03Z

## Root-cause context (operator-directed graph V-B-A): the churn is FileSystemIngestor re-ingestion

Inspecting the live graph's *contents* reframes this ticket's churn source. By node label:

| label | count | nature |
| :--- | ---: | :--- |
| FILE | 31,184 | regenerable file-tree (FileSystemIngestor) |
| DIRECTORY | 5,771 | regenerable file-tree |
| CLASS | 1,644 | auto-extracted code nodes |
| KB_GAP | 784 | **garbage** (per-file hallucination, #11141) |
| TOOLING_GAP | 751 | **garbage** (per-file hallucination, #11141) |
| ISSUE / CONCEPT / AGENT_MEMORY / PR / MESSAGE / RETROSPECTIVE / … | ~33K | legitimate memory |

So **~52% of the graph is a regenerable filesystem tree** (`FILE`+`DIRECTORY`+`CLASS` ≈ 38.6K nodes + `CONTAINS` 36.8K edges), built by `ai/services/memory-core/FileSystemIngestor.mjs` (walks the repo: files→`FILE`, dirs→`DIRECTORY`, hierarchy→`CONTAINS`). Per the **#11141 incident** (documented at `ai/scripts/maintenance/restore.mjs:118-121`): `FILE`/`DIRECTORY`/`CONTAINS` are **regenerable** (deterministically rebuilt by FileSystemIngestor post-restore), and `KB_GAP`/`TOOLING_GAP` are *"operator-classified garbage from per-file hallucination."*

GraphLog's 7.17M edge-mutations are therefore dominated by **re-ingestion of the 36.8K `CONTAINS` edges**. The compaction in this ticket bounds the LOG (downstream); the upstream root is that **a regenerable, ephemeral substrate lives in the durable + backed-up + CDC-logged graph**.

**Recast:** #12329 (compaction) stays valid as the log-bounding mechanism, but the higher-leverage fix is upstream — keep the regenerable file-tree *out* of the persisted/CDC path (a separate derived store, or idempotent ingestion that doesn't churn unchanged edges), which shrinks BOTH the graph and the GraphLog growth at the source. That's a separate architectural concern (operator/ideation call), flagged here for linkage.

Memory-mining for the #11141 history was a **clear miss** in session-memory; the authoritative record is the `restore.mjs` #11141 JSDoc (the `--filter-labels=FILE,DIRECTORY,KB_GAP,TOOLING_GAP --filter-edge-types=CONTAINS,DISCOVERED_IN,EVALUATED_BY` + post-restore regenerate pattern). The recurrence (FILE back to 31K, KB_GAP/TOOLING_GAP back to ~1.5K) confirms #11141 was a one-time restore-time janitorial, not a standing fix.

Related sweep findings: `KB_GAP`/`TOOLING_GAP` (1,535 garbage nodes) to purge + stop the hallucination source; `test-daemon-*.sqlite` (12 tiny 3-node files) = minor test-isolation.

— @neo-opus-4-7


### @neo-opus-ada - 2026-06-01T22:56:54Z

## Correction (V-B-A of the DreamService pipeline): the file-tree is intended gap-inference substrate

Retracting the "keep the regenerable file-tree out of the persisted/CDC graph" framing in my earlier root-cause comment — it was asserted before V-B-A'ing the **DreamService** pipeline that governs this graph, and it's wrong.

The graph is the working substrate of the **DreamService REM cycle** (run by `ai/scripts/runners/runSandman.mjs` → `DreamService.executeRemCycle()`). Phase order in `processUndigestedSessions()`: `conceptIngest` → **`workspaceIngest`** (`FileSystemIngestor.syncWorkspaceToGraph` — ingested explicitly *"so downstream gap inference can traverse concept-graph relationships"*) → `memorySessionIngest` → `triVector` → `topology` → **`gapSession`** (`GapInferenceEngine` — derives `KB_GAP`/`TOOLING_GAP` from FILE nodes) → `conceptGap` → **`garbageCollection`** (`GraphMaintenanceService` Vector Apoptosis — removes *orphaned* nodes + their vectors). The **Golden Path** (`GoldenPathSynthesizer`) is the distilled, operator-facing actionable topology.

So:
- `FILE`/`DIRECTORY`/`CONTAINS` are **deliberate gap-inference substrate**, not removable junk.
- `FileSystemIngestor` is **differential** (caches mtime+hash, skips unchanged files) — so it is **NOT** the GraphLog churn source I previously claimed.

**What still stands:** #12329 (GraphLog CDC-log compaction past the min consumer watermark) remains valid as the LOG-bound, independent of what produces the mutations. The 7.3M log is dominated by the (now-fixed) 33M-item explosion residue + ongoing REM inference mutations — not file-tree re-walks. `KB_GAP`/`TOOLING_GAP` over-production is a `GapInferenceEngine` tuning concern (#11141 class), not wholesale-removable substrate.

— @neo-opus-4-7


- 2026-06-01T23:29:12Z @neo-opus-ada cross-referenced by #12331
- 2026-06-02T00:32:21Z @neo-opus-ada cross-referenced by PR #12336
- 2026-06-02T01:16:43Z @neo-gpt assigned to @neo-gpt
- 2026-06-02T01:27:25Z @neo-gpt cross-referenced by PR #12337
- 2026-06-02T01:59:46Z @neo-gpt referenced in commit `35a7b2d` - "fix(memory-core): source graphlog paths from config (#12329)"
- 2026-06-02T02:11:26Z @neo-gpt referenced in commit `90f95b3` - "test(ai): isolate graphlog config import (#12329)"
- 2026-06-02T07:42:05Z @tobiu referenced in commit `85082da` - "feat(ai): add graphlog compaction maintenance (#12329) (#12337)

* feat(ai): add graphlog compaction maintenance (#12329)

* fix(memory-core): source graphlog paths from config (#12329)

* test(ai): isolate graphlog config import (#12329)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
### @neo-gpt - 2026-06-02T08:02:54Z

**Input from GPT-5.5 (Codex Desktop):**

> ● ## Post-merge validation result — default dry-run currently fails before compaction planning
> 
> After PR #12337 merged, I ran the post-merge dry-run validation from the PR body:
> 
> - `npm run ai:compact-graphlog` → failed before planning with `ReferenceError: Neo is not defined` from `src/core/Compare.mjs` while importing Memory Core config.
> - `node -e "await import('./ai/mcp/server/memory-core/config.template.mjs')"` → same failure.
> - `node -e "await import('./ai/mcp/server/memory-core/config.mjs')"` → same failure.
> - `node -e "await import('./src/Neo.mjs'); await import('./ai/mcp/server/memory-core/config.template.mjs')"` → passed.
> - `node -e "await import('./src/Neo.mjs'); await import('./ai/mcp/server/memory-core/config.mjs')"` → passed.
> 
> Watermark state observed before the dry-run:
> 
> - `.neo-ai-data/wake-daemon/lastSyncId` exists and currently contained `7572981`.
> - `.neo-ai-data/wake-daemon/wakeSubscriptionLiveCursor` was missing, so after the CLI bootstrapping issue is fixed the expected safe behavior is still fail-closed until Memory Core is restarted/reloaded into the merged code path and writes the new live cursor.
> 
> I am **not** opening a second PR for #12329 because the operator clarified the non-negotiable granularity model: one PR per ticket/sub, no extra PR fragments. The ticket remains open because AC4/AC5 and even the default dry-run post-merge validation are not complete yet.
> 
> Likely implementation fix, if an exception or reshaped follow-up is authorized: bootstrap `src/Neo.mjs` before the compaction CLI imports Memory Core config, so the CLI can load `BaseConfig`/Neo classes in a standalone Node process.

### @neo-gpt - 2026-06-02T18:04:37Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Post-merge validation blocker: default dry-run fails before planning
> 
> V-B-A performed on 2026-06-02 after PR #12337 merged (`90f95b37448c4b196fd9aca2ba20ad6af7d78927`). The default non-destructive validation command does not currently reach compaction planning:
> 
> ```text
> npm run ai:compact-graphlog
> ReferenceError: Neo is not defined
>     at file:///Users/Shared/codex/neomjs/neo/src/core/Compare.mjs:166:16
>     at async loadMemoryCoreConfig (.../ai/scripts/maintenance/compactGraphLog.mjs:33:13)
> ```
> 
> Evidence checked:
> - `gh pr view 12337` reports `state=MERGED`, `mergedAt=2026-06-02T07:42:04Z`, `headRefOid=90f95b37448c4b196fd9aca2ba20ad6af7d78927`.
> - `npm run ai:compact-graphlog` is the default dry-run path; no `--apply` / `--vacuum` was attempted.
> - The failure happens while importing runtime Memory Core config, before any GraphLog read/delete plan.
> - Sibling maintenance scripts already use the Neo bootstrap pattern before importing modules that transitively call `Neo.gatekeep()`.
> 
> Blocker summary: #12329 cannot complete its own post-merge dry-run validation until `compactGraphLog.mjs` gets the same plain-Node Neo bootstrap discipline as sibling maintenance CLIs.
> 
> Policy note: I am not opening a second #12329 PR from this thread without explicit reshaping because the operator corrected us to one PR per ticket/sub and this ticket already had PR #12337. Candidate route is either an operator-approved fix PR for this post-merge blocker, or folding the bootstrap fix into a broader coherent maintenance follow-up rather than creating another tiny fragment PR.
> 
> lane-state: blocked (#12329 post-merge validation blocked on CLI bootstrap failure; awaiting policy-shaped fix route)

- 2026-06-02T18:24:33Z @neo-gpt cross-referenced by PR #12389
- 2026-06-02T19:17:17Z @neo-opus-ada cross-referenced by #12394
- 2026-06-02T19:18:19Z @neo-opus-ada marked this issue as blocking #12394
- 2026-06-02T19:34:05Z @tobiu referenced in commit `23f3ab8` - "fix(ai): bootstrap graphlog compactor cli (#12329) (#12389)

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-02T19:34:06Z @tobiu closed this issue
- 2026-06-02T19:56:20Z @neo-gpt cross-referenced by PR #12395

