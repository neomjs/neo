---
number: 17713
title: 'fix(agentos): scope split proof to module surfaces (#17707)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-24T15:45:56Z'
updatedAt: '2026-08-24T15:53:42Z'
closedAt: null
mergedAt: null
head: codex/17707-full-module-placement-authority
base: dev
url: 'https://github.com/neomjs/neo/pull/17713'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17707

The paired plane-boundary proof now preserves inventory surface identity when it consumes module custody. Only an explicit `script-module` row can classify a reached module; a same-path plane opener, config, workflow, or other surface cannot impersonate custody, and an ordinary reached dependency no longer manufactures a missing-inventory blocker. Proof 1 remains the sole membership/residue authority, while explicit Cloud module reach and the independent package, escape, unresolved-edge, runtime-denial, and computed-edge hazards remain intact.

Related: #17500 · #17525 · #17533 · #17645 · #16202 · #17627

Decision Record impact: `aligned-with ADR 0040` §§2.2, 2.4, 2.6, and 2.7; `aligned-with ADR 0039`. No amendment.

Evidence: L3 (clean archive receipt at `21da3a2591`: source-bound, zero dirty paths, zero instrument errors, zero `topology-edge-closure-unregistered-module` findings; 26 surviving findings = 17 blockers + 9 non-blockers across five exact classes; focused guard 35/35 after a 2-red/33-green mutation baseline) → L3 required (all close-target ACs are repository-local and executable). No residuals in this leaf.

## AC Evidence

| AC-1 | Production builds `dispositionBySurfaceIdentity` with the inventory's `rowKey(row.surface, row.identity)` and queries module custody only through `rowKey(SURFACE.scriptModule, identity)`. |
| AC-2 | The cross-surface collision arm gives `ai/svc.mjs` a Cloud `plane-opener` row and proves the reached module remains unclassified rather than falsely Cloud. |
| AC-3 | The ungoverned-dependency arm reaches two `ai/**` modules without `script-module` rows and proves neither an instrument error nor a topology finding is emitted; proof 1 retains residue ownership. |
| AC-4 | The explicit Cloud `script-module` arm still emits `topology-edge-closure-reaches-cloud-module` with the exact identity and blocker flag. |
| AC-5 | The focused 35-arm suite retains Cloud-package, plane-root escape, unresolved/unreadable edge, runtime-denial, and computed-edge controls. |
| AC-6 | The clean exact-head receipt reports zero instrument errors and no unregistered-module class; its 26 surviving findings retain exact identities, details, successor owners, and blocker classifications. |
| AC-7 | Focused mutations cover the same-path surface collision, ungoverned dependency reach, explicit Cloud module reach, and every retained independent detector layer. |
| AC-8 | Existing deterministic-ordering and dirty-binding arms remain green; the production receipt binds to `21da3a2591ed42894d9a0ee4939b10d87b12f6ae` with `dirtyPaths: []`. |

## Deltas from ticket

None after intake reshaped #17707 to the Accepted ADR 0040 authority. The earlier full-`ai/**` custody-ledger premise was withdrawn before implementation; this diff implements the corrected surface-aware contract only.

## Test Evidence

All coverage runs in CI. Exact local falsifiers used before opening:

- `npx playwright test -c test/playwright/playwright.config.unit.mjs test/playwright/unit/ai/services/agentOsPlaneBoundary.spec.mjs` — 35/35 green.
- `node ai/scripts/diagnostics/agentOsPlaneBoundaryProof.mjs --json` in a clean `git archive` tree with the exact ignored operator overlays — source-bound receipt above; exit 1 is the expected topology-red result, with zero instrument errors.

## Post-Merge Validation

None. The committed focused guard and source-bound diagnostic are the standing `dev`-branch proof.

## Commits

- `21da3a2591` — scope split-proof module custody to the `script-module` surface.

## Evolution

Vega's ADR challenge exposed two coupled inference errors: reach was being promoted into inventory authority, while a bare path let unrelated surfaces answer module custody. The repair consumes the inventory's existing composite identity instead of growing a 766-module ledger or suppressing the remaining split blockers.

## Signal Ledger

- `gpt` author signal: Emmy's source-Discussion signal, carried by Epic #17500 at the corrected final body anchor.
- `claude` non-author signal: Vega's `[GRADUATION_APPROVED]`, revalidated in the independent Epic Review at https://github.com/neomjs/neo/issues/17500#issuecomment-5376063521.
- Family-keyed quorum remains current for this proof-authority leaf.

## Unresolved Dissent

None at the corrected Discussion/Epic authority anchor.

## Unresolved Liveness

Kimi is benched/unhosted and Gemini is operator-benched; neither absence is counted as consent, and neither is a hold gate under the Epic's recorded liveness disposition.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session cad88c79-073f-4816-aaa7-e779224f2af3.
