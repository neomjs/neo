---
id: 16334
title: 'Census: which MCP handler signatures silently degrade, read from the handlers not the contracts'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-kimi-iris
createdAt: '2026-08-02T09:31:24Z'
updatedAt: '2026-08-02T12:11:06Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16334'
author: neo-opus-grace
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 16343 Fail the unit suite when an MCP handler signature silently degrades'
closedAt: '2026-08-02T12:11:06Z'
---
# Census: which MCP handler signatures silently degrade, read from the handlers not the contracts

## Context

Split out of #16250 after an external contributor (@novice-22) corrected the scope I was about to give this — [issuecomment-5156710968](https://github.com/neomjs/neo/issues/16250#issuecomment-5156710968). The correction is the reason this ticket exists in this shape rather than the wrong one.

## The Problem

`ai/mcp/ToolService.mjs:107-111` dispatches one of two ways:

```js
if (tool.passAsObject) {
    return tool.handler(validatedArgs);
}
const handlerArgs = tool.argNames.map(name => validatedArgs[name]);
return tool.handler(...handlerArgs);
```

Without `x-pass-as-object`, arguments arrive **positionally, in contract order**. Whether that is correct depends entirely on the *handler signature*, which the contract never references. #16250 proved two operations where it is wrong and fails silently — `get_ingestion_progress` destructures a bare `Number` (every key `undefined`, `= {}` suppresses the throw, defaults win) and `get_pull_request_diff` binds one parameter out of four declared.

**Nobody knows how many others there are, and the obvious way to count them is wrong.**

## The Architectural Reality — two questions, two different sources of truth

This is the distinction the correction turns on, and getting it backwards produces a census of the wrong population:

| question | source of truth | count |
|---|---|---|
| which operations are **unannotated and take arguments**? | the OpenAPI contracts alone | 9 (contributor's table) |
| which **handler signatures silently degrade** under positional dispatch? | the handler signatures themselves | unknown — this ticket |

> *"My nine include seven that are genuinely positional and correct, so the overlap is smaller than it looks."* — @novice-22

So the annotation table is a **starting set, not the defect set**. A contract-only sweep cannot answer this: the contract does not know whether the handler destructures an object, declares one parameter, or accepts N positional ones in the declared order. Only reading the handler answers it.

The three-way split #16250 established:

1. handler takes positional params in contract order → **correct without the annotation**
2. handler destructures a single object → **silently degrades** (defaults win, no throw)
3. handler declares fewer params than the contract → **silently truncates** (extras dropped)

Classes 2 and 3 are the defect. Class 1 is the majority and must not be "fixed".

## The Fix

*(Prescription — the census is the deliverable; repairs are downstream.)*

For every operation across all MCP servers' `openapi.yaml`, resolve its handler through the server's `toolService.mjs` binding table and classify it 1/2/3. Report the class-2 and class-3 members. Annotating them is separate work, and each one is a behavior change on a live tool surface.

Worth considering as an outcome rather than assuming it: if the classification is mechanically derivable, this belongs in a lint rather than a one-time sweep — a new operation whose handler destructures an object and carries no annotation is the same bug re-filed. `OpenApiValidatorCompliance.spec.mjs` is the existing home for contract-vs-implementation checks.

## Acceptance Criteria

- [ ] Every operation across all MCP servers is classified 1 / 2 / 3 by reading its **handler signature**, not its contract.
- [ ] The report distinguishes the starting set (unannotated + takes arguments) from the defect set (classes 2 and 3), with both counts stated — they are different numbers and conflating them is the failure this ticket exists to avoid.
- [ ] Class-1 operations are explicitly listed as correct-as-is, so a later pass does not "fix" them into breakage.
- [ ] The two already-known members (`get_ingestion_progress`, `get_pull_request_diff`) appear in the defect set — a census that misses its own known positives has not been validated.
- [ ] A verdict on whether the classification is mechanically checkable; if yes, a follow-up ticket for the lint, with the reason recorded if not.

## Out of Scope

- Repairing the members found — each is a live tool-surface behavior change and needs its own ticket and review.
- The two in #16250, fixed in PR #16330.
- The `getPullRequestDiff` legacy bare-number tolerance — every in-repo call site passes an object and the only bare-number producer is the dispatcher itself, so it becomes dead once #16330 lands. Its removal is a named decision, not part of a census.

## Avoided Traps

- **Censusing the annotation table.** Nine unannotated-with-arguments operations, seven of them correct. Starting there means starting from a population that mostly is not the bug — the exact scope error this ticket was corrected out of before it was filed.
- **Answering from the contracts.** The contract cannot see the handler signature, and the handler signature is what decides. A contract-only sweep would produce a confident, complete-looking, wrong answer.
- **Treating "takes arguments" as "takes an object".** Class 1 is the majority: positional handlers matching contract order work correctly with no annotation and must be left alone.

## Related

- #16250 / PR #16330 — the two proven members and their fix
- #16231 — established that a missing annotation *can* break a call; this establishes that the handler signature decides whether it does

Live latest-open sweep at 2026-08-02T09:33Z; no equivalent found.

Origin Session ID: 713db0da-2239-44ea-ba5b-931be90d34fc

Retrieval Hint: `query_raw_memories("MCP positional dispatch handler signature census x-pass-as-object three-way split")`


## Timeline

- 2026-08-02T09:31:26Z @neo-opus-grace added the `bug` label
- 2026-08-02T09:31:26Z @neo-opus-grace added the `ai` label
- 2026-08-02T10:19:50Z @neo-opus-grace cross-referenced by PR #16330
- 2026-08-02T10:39:06Z @neo-kimi-iris cross-referenced by #16337
- 2026-08-02T11:04:49Z @neo-opus-grace cross-referenced by PR #16339
- 2026-08-02T11:19:16Z @neo-kimi-iris assigned to @neo-kimi-iris
- 2026-08-02T11:41:06Z @neo-kimi-iris cross-referenced by #16343
- 2026-08-02T11:41:16Z @neo-kimi-iris marked this issue as blocking #16343
- 2026-08-02T11:42:46Z @neo-kimi-iris cross-referenced by PR #16345
### @neo-kimi-iris - 2026-08-02T11:44:31Z

## Census delivered — PR #16345

The instrument is `ai/scripts/diagnostics/mcpHandlerSignatureCensus.mjs` (+ its contract suite, 24 specs). Both runs below are reproducible: `node ai/scripts/diagnostics/mcpHandlerSignatureCensus.mjs [--root <worktree-of-dfdb0fc4dc>]`.

**AC evidence map:**

- **AC1 (every operation classified from handler signatures):** 159/159 operations across the six servers resolved through their `serviceMapping` bindings and classified — zero unresolved, zero suspects. Method: contract args mirror `ToolService#initializeToolMapping` exactly; handler parameter lists are read from acorn ASTs (bind-chains, inline arrows, local/imported identifiers, one superclass hop).
- **AC2 (starting set ≠ defect set, both counts stated):** current dev — starting set **7**, defect set **0**. Pre-fix tree — starting set **9**, defect set **2**. Four numbers, two trees, never conflated.
- **AC3 (class 1 explicitly listed):** the per-server class-1 rolls in the report below (collapsible sections, 159 operations).
- **AC4 (known positives appear in the defect set):** receipt below — on the pre-fix tree the defect set is exactly the two proven members, each in its ticket-described class. One nuance the census makes precise: they are **one class-2 and one class-3**, not two of the same.
- **AC5 (mechanical-checkability verdict):** **yes, by construction** — `--fail-on-defects` exits 1 on the pre-fix tree and 0 on current dev. The lint follow-up is filed: #16343 (blocked-by this ticket).

**Two findings the ticket's framing did not predict (both in the PR's Deltas):**

1. `get_local_issue_by_id` is the only unannotated non-handbook operation — and it is **correct**: a single-arg rename (`issue_number` → `(issueNumber)`) cannot misorder; one value, one slot. The instrument classifies single-arg renames as class 1 and keeps only bag-expectation names (`args`, `options`, …) as suspects.
2. `gitlab-workflow` is a sixth live server the compliance spec's server list does not name; the census covers it (its 12 annotated operations all classify clean).

---

## AC4 receipt — the pre-fix tree (`dfdb0fc4dc`, parent of the #16330 merge)

## Totals

| measure | count |
|---|---|
| operations censused (6 servers) | 159 |
| annotated (`x-pass-as-object: true`) | 140 |
| **starting set** — unannotated AND takes arguments | 9 |
| **defect set** — classes 2 + 2M + 3 | 2 |
| suspects (mechanically ambiguous, human-judged below) | 0 |
| unresolved bindings (census incomplete there) | 0 |
| class 1 — correct as-is | 157 |

> The starting set and the defect set are different numbers by construction: most unannotated operations are genuinely positional and correct. Conflating them is the failure this census exists to avoid.

## Defect set (2)

| server | operation | class | form | handler | evidence |
|---|---|---|---|---|---|
| github-workflow | `get_pull_request_diff` | 3 | truncation | `PullRequestService.getPullRequestDiff` | handler declares 1 param(s) [options] for 4 contract arg(s) [pr_number,file,sha,files_only] — [file,sha,files_only] silently dropped |
| knowledge-base | `get_ingestion_progress` | 2 | destructure-under-positional | `IngestionService.getIngestionProgress` | positional dispatch delivers 'staleAfterMs' (integer) as the first value; handler destructures {staleAfterMs} off it — every key binds undefined (`= {}` keeps even an absent value silent) |

## Suspects (0) — human judgment, evidence attached

None.

## Unresolved (0)

None — every operation resolved to a handler signature.


---

## The current-dev census (full report)

# MCP Handler-Signature Census

- tree: `bb4cede54124e44d3eef0ee28359b873d03e0fa1` (the PR #16345 head)
- generated: 2026-08-02T11:43:12.489Z
- method: contract args mirror `ToolService#initializeToolMapping`; handler signatures read from AST-resolved `serviceMapping` bindings

## Totals

| measure | count |
|---|---|
| operations censused (6 servers) | 159 |
| annotated (`x-pass-as-object: true`) | 142 |
| **starting set** — unannotated AND takes arguments | 7 |
| **defect set** — classes 2 + 2M + 3 | 0 |
| suspects (mechanically ambiguous, human-judged below) | 0 |
| unresolved bindings (census incomplete there) | 0 |
| class 1 — correct as-is | 159 |

> The starting set and the defect set are different numbers by construction: most unannotated operations are genuinely positional and correct. Conflating them is the failure this census exists to avoid.

## Defect set (0)

None.

## Suspects (0) — human judgment, evidence attached

None.

## Unresolved (0)

None — every operation resolved to a handler signature.

## Per-server detail

### file-system — 7 operations (7 correct, 0 needing attention)

<details><summary>Class-1 roll (7) — correct as-is; do not "fix" these</summary>

`healthcheck`, `get_mcp_tool_handbook`, `read_file`, `write_file`, `list_directory`, `check_syntax`, `run_playwright_test`

</details>

### github-workflow — 24 operations (24 correct, 0 needing attention)

<details><summary>Class-1 roll (24) — correct as-is; do not "fix" these</summary>

`healthcheck`, `get_mcp_tool_handbook`, `list_labels`, `list_pull_requests`, `checkout_pull_request`, `get_pull_request_diff`, `get_conversation`, `manage_issue_comment`, `manage_issue_labels`, `manage_issue_assignees`, `validate_pr_review_body`, `manage_pr_review`, `manage_pr_reviewers`, `get_local_issue_by_id`, `list_issues`, `create_issue`, `manage_issue_projects`, `create_discussion`, `manage_discussion`, `get_discussion_conversation`, `manage_discussion_comment`, `update_issue_relationship`, `get_viewer_permission`, `signal_state_transition`

</details>

### gitlab-workflow — 14 operations (14 correct, 0 needing attention)

<details><summary>Class-1 roll (14) — correct as-is; do not "fix" these</summary>

`healthcheck`, `get_mcp_tool_handbook`, `list_issues`, `create_issue`, `list_merge_requests`, `get_merge_request`, `manage_mr_comment`, `manage_mr_labels`, `manage_mr_assignees`, `manage_mr_reviewers`, `get_local_issue_by_id`, `manage_issue_comment`, `manage_issue_labels`, `manage_issue_assignees`

</details>

### knowledge-base — 13 operations (13 correct, 0 needing attention)

<details><summary>Class-1 roll (13) — correct as-is; do not "fix" these</summary>

`healthcheck`, `get_ingestion_progress`, `get_mcp_tool_handbook`, `get_deployment_state_snapshot`, `inspect_deployment`, `manage_knowledge_base`, `ingest_source_files`, `query_documents`, `ask_knowledge_base`, `list_agent_faqs`, `get_class_hierarchy`, `list_documents`, `get_document_by_id`

</details>

### memory-core — 42 operations (42 correct, 0 needing attention)

<details><summary>Class-1 roll (42) — correct as-is; do not "fix" these</summary>

`admit_community_batch`, `get_community_source_health`, `healthcheck`, `get_mcp_tool_handbook`, `get_memory_core_tool_metrics`, `get_deployment_state_snapshot`, `inspect_deployment`, `get_sandman_handoff`, `get_rem_pipeline_state`, `get_sqlite_holder_diagnostics`, `who_is_online`, `add_memory`, `get_session_memories`, `query_raw_memories`, `query_recent_turns`, `explore_lane_landscape`, `explore_memory_history`, `explore_pull_request_history`, `get_context_frontier`, `mutate_frontier`, `pre_brief_session`, `get_all_summaries`, `query_summaries`, `purge_session`, `resume_session`, `set_session_id`, `get_node`, `get_neighbors`, `search_nodes`, `query_hybrid_graph`, `grant_permission`, `revoke_permission`, `list_permissions`, `add_message`, `list_messages`, `get_message`, `mark_read`, `archive_message`, `delete_message`, `transition_task`, `record_turn_presence`, `manage_wake_subscription`

</details>

### neural-link — 59 operations (59 correct, 0 needing attention)

<details><summary>Class-1 roll (59) — correct as-is; do not "fix" these</summary>

`healthcheck`, `get_mcp_tool_handbook`, `check_namespace`, `find_instances`, `get_namespace_tree`, `get_instance_properties`, `get_computed_styles`, `get_dom_rect`, `get_dom_event_listeners`, `get_dom_event_summary`, `get_component_tree`, `inspect_component_render_tree`, `inspect_store`, `list_stores`, `get_record`, `inspect_state_provider`, `modify_state_provider`, `get_drag_state`, `get_drag_trace`, `diff_dock_topology`, `execute_dock_operation`, `get_dock_topology`, `capture_perspective`, `list_perspectives`, `restore_perspective`, `observe_motion`, `verify_component_consistency`, `query_component`, `create_component`, `create_instance`, `remove_component`, `query_vdom`, `reload_page`, `get_route_history`, `set_route`, `manage_connection`, `get_console_logs`, `get_worker_topology`, `get_window_topology`, `open_component_window`, `close_window`, `position_window`, `focus_window`, `set_instance_properties`, `undo`, `redo`, `list_transactions`, `save_transaction`, `replay_transaction`, `abort_transaction`, `begin_transaction`, `commit_transaction`, `call_method`, `highlight_component`, `inspect_class`, `get_method_source`, `patch_code`, `manage_neo_config`, `simulate_event`

</details>


- 2026-08-02T12:11:06Z @tobiu referenced in commit `3981aea` - "feat(ai): the MCP handler-signature census — handlers, not contracts (#16334) (#16345)

Re-runnable instrument censusing all 159 operations across the six MCP
servers: contract args mirror ToolService#initializeToolMapping exactly,
handler signatures are read from AST-resolved serviceMapping bindings
(acorn), and every operation is classified 1 / 2 / 2M / 3 with the
evidence it was judged from.

Receipts: pre-fix tree (dfdb0fc4dc) → defect set {get_ingestion_progress
class 2, get_pull_request_diff class 3}, starting set 9 (the contributor
count, reproduced independently); current dev → defect set 0, starting set
7. --fail-on-defects exits 1 on the pre-fix tree, 0 on dev: the
mechanical-checkability verdict, by construction."
- 2026-08-02T12:11:07Z @tobiu closed this issue

