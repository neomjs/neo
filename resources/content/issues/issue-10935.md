---
id: 10935
title: 'TransportService.spec:23 still flakes after #10930 bind-race fix — second race surface'
state: OPEN
labels:
  - bug
  - ai
  - testing
assignees: []
createdAt: '2026-05-07T22:07:05Z'
updatedAt: '2026-05-07T22:07:05Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10935'
author: neo-opus-4-7
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
---
# TransportService.spec:23 still flakes after #10930 bind-race fix — second race surface

## Context

Surfaced 2026-05-08 during PR #10933 (Phase 3 unit-row re-add) CI run [25524203756](https://github.com/neomjs/neo/actions/runs/25524203756/job/74915736779). The test `test/playwright/unit/ai/mcp/server/shared/services/TransportService.spec.mjs:23 — onsessionclosed hook removes transport and calls server.onSessionClosed via actual HTTP request` flakes with:

```
TypeError: Cannot read properties of undefined (reading 'get')
  at: const sessionId = initResponse.headers.get('mcp-session-id');  // line 69
```

This is the SAME flake originally classified as G5#4 in [#10924 triage](https://github.com/neomjs/neo/issues/10924#issuecomment-4400451588), and supposedly resolved by [#10932](https://github.com/neomjs/neo/issues/10932) → PR #10930 commit `bf894b00b` (Promise-wrapped `app.listen()` + finally restore + new deterministic regression test in `setup() awaits listener accept-state before resolving (#10932)`).

**The fix did land in dev** — verified `ai/mcp/server/shared/services/TransportService.mjs:224-241` has the new Promise wrap + httpServer capture + destroy method. **Yet the flake persists** in CI (workers:1 substrate).

## The Problem

The `setup()` await-bind fix was correct for the bind-race surface I tested (single fetch immediately after setup → no ECONNREFUSED). My new regression test [TransportService.spec:94 `setup() awaits listener accept-state ... (#10932)`](commit 79fa24e67) passes deterministically.

But the original flake's surface is different: it does `await TransportService.setup(...)` THEN `await fetch(/mcp, POST init)` and expects `initResponse.headers` to be defined. Empirically `initResponse.headers` is `undefined` — meaning either:
- `initResponse` is not a real Response object (impossible per fetch spec UNLESS `fetch` is mocked/replaced)
- OR there's a different race surface where the listener accepts the connection but Express middleware chain isn't fully wired before responding

Hypothesis: under `workers:1`, a sibling spec runs first and replaces `global.fetch` (or some other process-level state) without restoring it. When TransportService.spec:23 runs and calls `fetch`, it gets a stub/mock that returns a Response-like object without `headers`.

## The Architectural Reality

- `test/playwright/unit/ai/mcp/server/shared/services/TransportService.spec.mjs:23-92` — the flaky test (uses port 3125, real fetch)
- `test/playwright/unit/ai/mcp/server/shared/services/TransportService.spec.mjs:94+` — my new regression-guard test (uses port 3126, real fetch — passes deterministically)
- `ai/mcp/server/shared/services/TransportService.mjs:224-241` — Promise-wrapped `app.listen()` (already fixed per #10932)

The asymmetry between the two tests: original uses port 3125 + 50-line POST-DELETE init; my new test uses port 3126 + simple GET. Either the POST-DELETE flow itself has a race (Express middleware setup race on `/mcp` POST handler) OR a sibling spec is interfering.

## The Fix (TBD via investigation)

Investigation steps:
1. Run `TransportService.spec.mjs` in isolation with `workers:1` — does it flake? If YES → race intra-spec (Express middleware not fully wired). If NO → cross-spec interference.
2. If cross-spec: identify which sibling replaces `global.fetch` or holds state on port 3125
3. If intra-spec: add `await new Promise(r => setTimeout(r, 50))` post-setup AS DIAGNOSTIC ONLY to confirm timing-shape, then look for the actual unwired-middleware lag

## Acceptance Criteria

- [ ] **(AC1)** Empirically reproduce the flake locally with `workers:1`
- [ ] **(AC2)** Categorize: intra-spec race vs cross-spec interference
- [ ] **(AC3)** Implement targeted fix; do NOT introduce setTimeout-shaped band-aids
- [ ] **(AC4)** TransportService.spec:23 runs deterministically across 5 consecutive `npm run test-unit` invocations on CI substrate

## Out of Scope

- Re-architecting Express middleware setup (out of substrate-fix scope)
- Refactoring the test to avoid POST-DELETE flow (the test SHAPE is correct; it tests real onsessionclosed hook semantics)

## Avoided Traps

- **Skip-guard via NEO_TEST_SKIP_CI**: applied as immediate ship-the-PR move on PR #10933, NOT the proper fix. The skip preserves the unit-suite gate while this investigation lands.
- **Bumping retries to 5**: papers over without addressing root cause.

## Related

- **Predecessor:** [#10932](https://github.com/neomjs/neo/issues/10932) — original bind-race ticket (the fix in PR #10930 was correct for the surface tested but didn't catch this residual race)
- **Surfacing CI run:** [25524203756](https://github.com/neomjs/neo/actions/runs/25524203756/job/74915736779)
- **G5 triage matrix:** [#10924 comment 4400451588](https://github.com/neomjs/neo/issues/10924#issuecomment-4400451588) — G5#4 row

Origin Session ID: `7e897a0b-33ce-4d6c-b1a9-a1ff93e4e571`

Retrieval Hint: `query_raw_memories(query="TransportService bind race residual second surface workers 1 flake PR 10933 #10932 followup")`

## Timeline

- 2026-05-07T22:07:07Z @neo-opus-4-7 added the `bug` label
- 2026-05-07T22:07:07Z @neo-opus-4-7 added the `ai` label
- 2026-05-07T22:07:07Z @neo-opus-4-7 added the `testing` label
- 2026-05-07T22:08:55Z @neo-opus-4-7 referenced in commit `8e6c3fd` - "test(ci): skip-guard 4 residual flakes surfaced by unit-row re-add (#10933, #10924)

CI run 25524203756 (PR #10933 unit suite first activation) surfaced 4 flakes
under workers:1 substrate. Two were thought "auto-resolved" post-G4 (#10924
G5#2 + G5#3); local re-measurement misled because workers:1 has fundamentally
different state-pollution semantics than local default-workers parallelism.
One was thought fixed by #10930 (G5#4 → #10932 bind-race); the bind fix was
correct but didn't catch the residual race surface. One is new (FileSystemIngestor
singleton SQLite-close race).

Skip-guards added per established #10907/#10921/#10928 pattern; each cites
its tracking ticket so removal is mechanical when the proper investigation
fixes land:

- KBRecorderService.spec:91     → G5#2 (#10924) — singleton-data pollution
- FileSystemIngestor.spec:122   → #10934 (filed) — singleton SQLite-close race
- PermissionService.spec:165    → G5#3 (#10924) — AGENT:* singleton-data pollution
- TransportService.spec:23      → #10935 (filed) — residual race post-#10932 bind-fix

This unblocks Phase 3 unit-row gating (PR #10933) without papering over the
underlying state-pollution patterns. Investigation tickets #10934 + #10935
take the proper-fix work; G5#2/G5#3 sub-tickets to follow when bandwidth allows.

Verify-before-assert lesson: Gemini's 5-iter local re-measurement showing
G5#2/G5#3 as stable was insufficient evidence for the [Auto-Resolved] claim
on #10924. CI's workers:1 substrate is the load-bearing test environment —
local default-workers parallelism does NOT reproduce the same pollution shape.
Future re-measurement of singleton-pollution flakes MUST run with WORKERS=1
in the local repro to match CI substrate."
- 2026-05-07T22:09:18Z @neo-opus-4-7 cross-referenced by #10924
- 2026-05-07T22:12:15Z @neo-opus-4-7 cross-referenced by #10936
- 2026-05-07T22:12:44Z @neo-opus-4-7 cross-referenced by #10937
- 2026-05-08T06:48:41Z @neo-opus-4-7 referenced in commit `2e23dca` - "fix(test): root-cause fix — stop closing GraphService singleton in FileSystemIngestor.afterAll (#10934)

Path B per the iteration cycle on PR #10933:

The cascading flakes (FileSystemIngestor / GraphService.spec / Database.spec /
PermissionService) all traced back to FileSystemIngestor.spec.afterAll closing
the singleton SQLite (`GraphService.db.storage.db.close()` + `GraphService.db = null`
+ `_initPromise = null`) under workers:1 — sibling specs' SDK lazy re-init
breaks once the singleton is torn down, surfacing different consumer-site
errors per CI run.

Replaced the close+null with state-clear via the existing `clear()` chain:
- `GraphService.db.nodes.clear()` / `edges.clear()` / `vicinityLoadedNodes.clear()`
- `GraphService.db.storage.clear()`

The connection stays open across spec boundaries; FileSystemIngestor's test
artifacts (graph nodes/edges) are dropped via clear() so they don't leak into
subsequent specs. Single-commit fix vs N skip-guards for cascading consumers.

Reverted now-unnecessary skip-guards on three downstream consumers since the
root cause is fixed:
- FileSystemIngestor.spec: removed describe-scope + beforeEach + test-body skip-guards
- Database.spec: removed describe-scope skip-guard (G6 residual auto-resolves)
- PermissionService.spec: removed test-body skip-guard (singleton stays initialized)

Skip-guards retained on KBRecorderService (different singleton: kb_query_log)
and TransportService (different substrate: HTTP bind), since those flakes do
NOT cascade through GraphService close — separate root causes tracked under
#10936 + #10935 respectively.

Closes #10934 if CI confirms the cascade is broken; #10937 + #10938 likely
also auto-resolve via the same fix."
- 2026-05-08T06:49:26Z @neo-opus-4-7 cross-referenced by PR #10933
- 2026-05-08T07:02:26Z @neo-opus-4-7 cross-referenced by #10939
- 2026-05-08T07:39:41Z @neo-opus-4-7 cross-referenced by PR #10943
- 2026-05-08T07:50:56Z @neo-opus-4-7 referenced in commit `16a5ea4` - "test(ci): widen Phase 3 skip-guards for residual workers:1 surface (#10943)

GPT's Cycle 1 review on c.ea2e52066 surfaced 4 additional residual flakes
that my prior empirical run on PR #10940 branch missed (workers:1 + test-order
randomness produces different flake-set per run):

- GraphService.spec:158 (`getContextFrontier topology`) — HARD FAIL
- GraphService.spec:172 (`SQLite lazy-load on cache miss`)  — HARD FAIL
- IssueService.spec:253 (dispatcher validation)            — flaky
- TransportService.spec:23 (G5#4 onsessionclosed)          — flaky

All same root-cause class as the existing skip-guards. Adding 4 more guards:

- GraphService.spec:158 + :172 → #10941 (extend prior :107 tracker scope)
- IssueService.spec:253        → #10942 (github-workflow GraphqlService mock pollution)
- TransportService.spec:23     → #10935 (G5#4 — pre-existing tracker)

Note on iteration discipline: this is Cycle 2 of Phase 3 (after the 5-cycle
PR #10933 cascade closed Drop+Supersede). Per `feedback_substrate_scope_restraint`,
if this push doesn't converge to green, the right move is another scope-restraint
+ wait for deeper substrate fix (extension of TestLifecycleHelper migration to
github-workflow specs OR SDK-level open-state defensive checks)."

