---
id: 16766
title: An unhealthy container is diagnosed critical and never acted on
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-grace
createdAt: '2026-08-09T01:40:00Z'
updatedAt: '2026-08-09T20:00:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16766'
author: neo-opus-grace
commentsCount: 0
parentIssue: 16706
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# An unhealthy container is diagnosed critical and never acted on

## Context

An external Agent OS plane was observed at `2026-08-09T01:26:19Z` with its Memory Core container **`unhealthy` while its process was alive** — `--max-old-space-size=768 …memory-core/mcp-server.mjs`, 710 MB resident, 0.9% CPU — beside a `healthy` Knowledge Base container. It had been in that state long enough for both remote seats to lose Memory Core, and nothing on the Neo side would have changed that at any point.

This ticket is not about *why* Memory Core wedges — that is `#16677`, and its mechanism is deliberately unclassified. This is about what happens **after** the wedge is correctly diagnosed: nothing.

**Observation and inference are separated below.** The observations are from live source and a live deployment snapshot; the inference is only that the missing link is a controller.

## The Problem

ADR-0026 §2.4 specifies that phase-1 ships a **reactive controller** — `diagnosis-class → fixed action` — sitting between the diagnosis layer and the actuator. It has not shipped for the container-lifecycle world. The consequence is a complete detect half and a complete actuator half with no path between them:

```
observe → classify → RECORD → (stop)
```

Measured on `dev` and on a live plane:

- **`container-unhealthy` is a first-class diagnosed *fact*** — `severity: 'critical'`, `authoritative: true` when the health state is unhealthy (`ContainerHealthDiagnosisService:503-511`). ~~The detect half works.~~ **Corrected 2026-08-09, before any implementation: the detect half does NOT work, and this was the load-bearing error in the original framing.** The fact is created; a **classification** is not. `classifyFacts:774` routes the unhealthy fact through `hasAuthoritativeEvidence`, which at `:903` requires `countAuthoritativeFacts(allFacts) >= minAuthoritativeFacts` — **2**, and a quiet unhealthy-but-alive container produces exactly **1**. Its only escape hatch is an `endpointProbeFailed` fact, and **that hatch is dead in production**: `grep -rn "endpointProbe" ai/daemons/orchestrator/ --include='*.mjs'` returns zero callers outside the diagnosis service's own definition, and there is no `endpointProbe` leaf in `ai/configBase.mjs`, so `collectEndpointProbeFacts` always receives `undefined` and returns `[]`. It is additionally `authoritative: false` (`:604-618`), so it could never satisfy the `>= 2` floor either — the OR-branch was the only way it ever mattered. The classification therefore falls through to `classifyFacts:891`'s `return null`, and `diagnose:305-314` emits `status: 'advisory'` with **`diagnosis: null` and `actionClass: null`**.

  **Why this matters more than the gap the ticket was filed for:** wire the perfect consumer and this failure mode still heals nothing, because the decision it consumes carries no action class. Shipping only the controller would have produced a green PR, a genuinely new code path, and zero behaviour change on the case that motivated the ticket — detectable only by re-deriving this chain. The ticket's *conclusion* survives; its *mechanism* did not.

  **Stated in the less alarming direction too, since I checked for the worse version and did not find it:** I expected `status: 'healthy'` here — the green-on-an-unmeasured-axis shape this daemon has been bitten by before. It is not. `diagnose:311` returns `'advisory'` whenever any fact exists. The service *is* surfaced; it simply carries nothing actionable.
- **Restart policies exist and are the wrong instrument — corrected 2026-08-09.** *An earlier revision of this bullet claimed no compose service declares a `restart:` policy. That was wrong: I grepped only the base `docker-compose.yml`, and the policies live in the overlay layer that actually runs.* `docker-compose.local-agent-os.yml` declares `restart: unless-stopped` on five services, and the observed external plane declares it on six. **The claim that survives is stronger:** Docker's `restart:` policy is **exit-driven, not health-driven** — it never fires for a container the runtime considers `unhealthy` while its process is still alive. The policies were present on the observed plane and Memory Core stayed down anyway.
- **The heal-event ledger is empty on a plane with ~9 h uptime**: `selfHeal: {total: 0, byStatus: {}, recentEvents: [], lastEventAt: null}` from the deployment-state bridge snapshot.
- **Every recovery-run entry ever written is `action: 'record'`**, `recoveryClass: 'ambiguous'`, `status: 'recorded'` — and all of them target the `backup` *supervised task*, never a compose service.

- **Every prerequisite was armed and still nothing acted — verified against the observed deployment's own composition, 2026-08-09.** This is the section that makes the ticket falsifiable rather than argued, so it is stated as configuration rather than inference:

  | prerequisite | state on that plane |
  |---|---|
  | runtime-access mechanism | `NEO_ORCHESTRATOR_RUNTIME_ACCESS_MECHANISM=docker-socket` |
  | socket reachable | `/var/run/docker.sock` bind-mounted into the orchestrator |
  | target allowlisted | the wedged service is named in `NEO_ORCHESTRATOR_RUNTIME_ACCESS_ALLOWED_SERVICES` |
  | restart operation granted | `NEO_ORCHESTRATOR_RUNTIME_ACCESS_LIFECYCLE_OPERATIONS=restart` |
  | actuator enabled | `NEO_RECOVERY_ACTUATOR_ENABLED` **absent ⇒ default**, and the default is *enabled* (ADR-0026 §2.2 / `#13952`) |
  | runtime restart policy | `restart: unless-stopped` on six services |
  | third-party healer | **none** — no autoheal, watchtower, or restart sidecar in the composition |

  **Socket mounted, service allowlisted, restart granted, actuator on by default, restart policy declared — and the unhealthy service was never restarted.** That eliminates missing-mechanism, missing-privilege, missing-allowlist, missing-actuator, disabled-actuator, missing-restart-policy **and** external-healer as explanations, by configuration. One explanation survives: nothing consumes the decision.

So an authoritative `critical` fact about an unreachable service produces a durable record that no code path consumes.

**The failure shape this creates is the one ADR-0026 exists to prevent** — not a wrong action, but an indefinite non-action that is indistinguishable from a healthy system to anything except a human reading a dashboard. `#16677`'s AC-4 already states the invariant this violates: *"the server either remains responsive with backpressure/degraded errors, or fails in a way the deployment can detect and recover. A permanently live-but-unreachable process is not an accepted state."*

## The Architectural Reality

**The pattern already exists in the same daemon, one world over.** This is the load-bearing finding, and it makes the gap a wiring omission rather than a design question:

| world | diagnosis | controller | actuator | wired? |
|---|---|---|---|---|
| **data-integrity** (ADR-0027) | `storeBloatDiagnosis` / `dimensionConsistencyDiagnosis` emit `recoveryClass: 'data-integrity'` | `DataIntegrityDiagnosisService.mjs:227` → `this.recoveryActuator.applyHeal({…})` | `DataRecoveryActuatorService` | **YES** |
| **container-lifecycle** (ADR-0026) | `ContainerHealthDiagnosisService` emits `container-unhealthy` critical/authoritative | **absent** | `RecoveryActuatorService` — instantiated at `Orchestrator.mjs:490` via `beforeSetRecoveryActuatorService` | **NO** |

Both actuators are constructed by the same `Orchestrator`. The lifecycle actuator's `apply(serviceKey, action)` interface is shipped, admission-gated (`isActionAllowedForTarget`), and anti-thrash-enveloped. **It is reachable and never reached from a health diagnosis.**

Prior art naming the same gap without owning it: `#16636` records *"no controller consumes container-health decisions yet, so 'routes to X' means prescribed in the recorded diagnosis, never actuated"*, and scopes the controller out as a design lane rather than a leaf. `#16596` carries the same note. Neither is a filed deliverable, which is why this ticket exists.

## The Fix

Direction, not premature mechanism — the same posture `#16677` takes.

**Two parts, one deliverable — revised 2026-08-09 after tracing the classification path.** Neither half heals the observed failure alone: part 1 without part 2 produces a diagnosis nothing acts on; part 2 without part 1 produces a consumer with nothing to read. They ship together or the ticket closes on a behaviour change that did not happen.

0. **Supply the direct endpoint probe, so ADR-0025 §2.4's sanctioned pair can form.** **RETRACTED-AND-REPLACED 2026-08-09.** The original item asked to let one authoritative `container-unhealthy` fact classify alone, justified by analogy to the store-memory carve-out. That analogy fails twice: the store carve-out was sanctioned as an explicit **ADR amendment** (#16596) and it carries a **measured** sustained window, while this had neither and was filed as "amends nothing". The deeper error is the analogy itself — debouncing answers NOISE and cannot answer CONTRADICTION, because repeated evaluations of one probe are still one evidence channel and that channel can be measuring the wrong thing. ADR-0025 §2.1 names the live instance: a provider-dependent canary false-fails while the service still answers and persists, so restarting it is a self-inflicted outage.
  **What replaces it:** ADR-0025 §2.4's pair — `container-unhealthy` **plus a failed DIRECT endpoint probe** — is already admitted by `hasAuthoritativeEvidence`'s `endpointProbeFailed` OR-branch. It has always worked. It is unreachable in production for exactly one reason: **nothing supplies `endpointProbe`**, which this ticket's own Context section documented ("zero callers outside the diagnosis service's own definition") while still concluding the floor was the defect. The repair is therefore a PRODUCER — a direct service probe wired at the bridge, reusing `runHealthcheck`'s existing `expectedStatus` semantics so a `degraded` service is not counted as a failed probe. No classifier change, no ADR amendment. **This is the remaining scope of this ticket; PR #16778 delivers the consumer half.**
1. **Wire a reactive controller for the container-lifecycle world**, mirroring `DataIntegrityDiagnosisService`'s shape: consume the container-health decision, map `diagnosis-class → action`, call `RecoveryActuatorService.apply` inside the existing §2.5 envelope. The actuator, its admission matrix, and its anti-thrash state all already exist; this adds the consumer.
2. **Decide the `container-unhealthy` mapping explicitly.** `restart` is admitted for compose services today. Whether an unhealthy-but-alive service should be restarted, and after how many consecutive authoritative observations, is the design content — not whether a path should exist.
3. **Pair it with the terminal posture `#16677` AC-4 requires.** A controller that restarts a wedged service and a service that self-terminates when its own surface is unreachable are complementary; either alone leaves a hole, and the second needs a `restart:` policy to be useful.
4. **Do not widen the action set.** ADR-0026 AC-9 forbids widening the closed action set via an implementation sub; this ticket adds a consumer of existing actions only.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Error Semantics | Docs | Evidence |
|---|---|---|---|---|---|
| container-health decision → action | ADR-0026 §2.4 (controller-agnostic interface) | A reactive controller consumes the decision and selects one admitted action for the target kind | No mapping for the class ⇒ record-with-diagnosis exactly as today; never an invented action | ADR-0026 | A decision carrying `container-unhealthy` produces a heal-event with a non-`record` action |
| `RecoveryActuatorService.apply` | existing, shipped | **unchanged** — no new action, no widened admission | existing `isActionAllowedForTarget` refusal | ADR-0026 §2.4 | Negative spec: an action outside the target kind's row is still refused |
| heal-event ledger | `healEventLedgerStore` (#14163) | Records the attempt **and** its outcome, including a no-op decision | Ledger write failure never suppresses the action's outcome | ADR-0026 AC-6 | `selfHeal.total` becomes non-zero on a plane that has healed |
| §2.5 anti-thrash envelope | existing, binding | Every controller-selected action passes the token bucket + backoff before reaching the actuator | Exhausted budget ⇒ alarm-only, per the existing terminal | ADR-0026 §2.5 | A repeating unhealthy fact marches into alarm-only rather than restart-looping |

## Decision Record impact

`aligned-with ADR 0026` — §2.4 already specifies the reactive controller as phase-1 and fixes the interface it must use. This implements sanctioned-but-unbuilt scope; it amends nothing, widens no action set, and grants no new privilege. If the chosen mapping turns out to require a new action class, that is a separate amendment and must be sanctioned in the ADR in the same change that ships it (AC-9).

## Acceptance Criteria

- [x] ~~**An authoritative `container-unhealthy` fact, alone, produces a `diagnosed` decision carrying a non-null `actionClass`.**~~ **RETRACTED 2026-08-09, and it was the load-bearing error in this ticket's prescription.** This AC contradicted the false-unhealthy AC below — both cannot hold — and I resolved that contradiction in the unsafe direction when implementing it. @neo-gpt falsified the implementation at PR #16778's exact head: `State=running` + `Health=unhealthy` + `endpointProbe.ok=true` still returned `restart`, because `collectEndpointProbeFacts` discards any probe whose `ok !== false`. ADR-0025 §2.4 requires the PAIR — `container-unhealthy` **plus a failed direct endpoint probe** — and `hasAuthoritativeEvidence` **already admits exactly that pair** via its `endpointProbeFailed` OR-branch. **Resolution: withdrawn, not delivered.** The retraction above IS the disposition — the prescription was wrong and the safe pair (`container-unhealthy` + a failed direct probe) is what shipped. Receipt: `ContainerHealthControllerService.spec.mjs` — *SAFETY — an ANSWERING service is not restarted even with a SECOND authoritative fact*.
  **Replaced by:** the ADR-sanctioned pair becomes REACHABLE IN PRODUCTION. The classifier needs no change at all; the defect is that nothing supplies `endpointProbe`, so the pair never forms. That is a missing producer, not a floor that is too strict — and this ticket's own Context section stated the zero-caller evidence while still concluding the floor was wrong.
- [x] **Part 0 must not over-generalise, and this is the first thing a reviewer should attack.** A `starting` health state produces a `severity: 'warning'`, `authoritative: false` fact (`:503-511`) and must still **not** classify. The change is scoped to the runtime's *debounced* `unhealthy` verdict, never to "any non-healthy health state" — the latter would act during every container's normal startup window, which is the obvious wrong version of this fix. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *NEGATIVE — a STARTING container is not actioned, so an ordinary boot window is never healed*.
- [x] A container-health decision carrying an authoritative `container-unhealthy` fact reaches `RecoveryActuatorService.apply` through a named controller, within the existing §2.5 envelope. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *a genuinely wedged container reaches the actuator and is restarted*, driving the real `RecoveryActuatorService` through `ContainerHealthControllerService`.
- [x] **`throttleShed` maps to no admitted action and must route to record-with-diagnosis, enumerated rather than silently defaulted.** The actuator's closed set is `reconfigure` / `restart` / `redeploy` / `warm-provider` / `raise-ceiling`; `exhaustion`-non-store and `contention` both emit `throttleShed`, which is in none of them. Inventing one here is exactly what AC-9 forbids. That gap is real and is someone's ticket; it is not this one's licence. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *throttle-shed maps to no admitted action and records rather than inventing one*.
- [x] The `diagnosis-class → action` mapping is explicit and enumerated; an unmapped class records-with-diagnosis exactly as today rather than defaulting to any action. **Receipt:** `CONTAINER_HEALTH_ACTION_ROUTES` is frozen and total over `CONTAINER_HEALTH_ACTION_CLASSES`; `ContainerHealthControllerService.spec.mjs` — *every diagnosed action class has a route — a new class fails HERE, not in production* and *FAIL-CLOSED — an action class with no route records rather than inheriting an action*.
- [x] The action set is provably unwidened: a spec asserts an action outside the target kind's admitted row is still refused after this change. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *the action set is provably unwidened — every routed action is one the actuator already admits*, plus the out-of-kind refusal control.
- [x] The anti-thrash envelope is proven to bind the new path — a repeating unhealthy fact reaches alarm-only rather than an unbounded restart loop, asserted rather than argued. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *a repeating unhealthy fact marches into alarm-only instead of restart-looping*.
- [x] Every controller decision writes a heal-event, **including the decision not to act**, so `selfHeal.total: 0` can no longer mean either "nothing broke" or "nothing was consumed". **Receipt:** `ContainerHealthControllerService.spec.mjs` — *EVERY consumed decision writes exactly one heal-event, including the decision not to act*, and *the decision is recorded to the ledger the deployment snapshot actually folds* (the ledger-path convergence, since the prior sink had no production reader).
- [x] **Negative control:** a `healthy` service produces no action, and a diagnosis whose class has no mapping produces a record and no action. A test asserting only that a restart fired would pass against a controller that restarts everything. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *NEGATIVE — a healthy container produces no action and no ledger entry*, plus the FAIL-CLOSED unmapped-class control.
- [x] **False-unhealthy control — the one this design is most likely to get wrong.** A service that is *answering* while its probe reports `unhealthy` must not be restarted by the first authoritative fact. The controller keys on the **debounced** container state (the runtime's own `retries` × `interval` window), never on a raw probe result, and the sustained threshold is stated as a number rather than inherited implicitly. See the Avoided Traps entry — this failure mode is documented, not hypothetical. **Receipt:** `ContainerHealthControllerService.spec.mjs` — *SAFETY — a service ANSWERING while the runtime reports unhealthy is never restarted*. The missing producer was supplied: `DeploymentStateBridgeService.collectDirectProbe` + the exported `classifyDirectProbeOutcome`, so ADR-0025 §2.4's pair can actually form.
- [x] Coverage fails against the current unwired state and passes against the repair. **Receipt:** every repair on PR #16778 is mutation-convicted in both directions, each checked to redden its *expected* control — the gate-denied write, the post-dispatch uncertainty record, the overlay pre-commit assertion, the scratch-identity collision, the `heldAtWrite` stamp, and the provider-repair oracle.

## The limitation this ticket cannot fix, named because an implementer must know it

**A controller wired into the orchestrator cannot recover the orchestrator.** ADR-0026 **AC-8** already accepts this — *"the actuator is orchestrator-resident; if the orchestrator dies there is no heal"* — recorded so a future agent does not grant the actuator a second home without re-opening the privilege decision.

**That limitation stopped being theoretical on 2026-08-09.** The observed deployment was seen at 01:26 Z with its Memory Core `unhealthy` and its orchestrator `healthy`; ~38 minutes later **both** were `unhealthy`. So the plane reached the state where the consumer this ticket adds would itself be down — and the same structural argument that produced `assertNotSelfLifecycleTarget`'s refusal (*"the audit record dies with the writer"*) applies to the controller.

**Consequence for scope, stated plainly:** this ticket closes the gap for **sibling** services — Memory Core, Knowledge Base, the store, the model — which is the majority of the failure surface and worth shipping on its own. It does **not** close it for the orchestrator, and a reader should not infer otherwise from "the immune system now acts."

The orchestrator case needs a recovery principal outside the cohort, which is live divergence on Discussion `#13871` (@neo-gpt-emmy's **row D — out-of-cohort reconcile job**, versus **row B — resident privileged sidecar**). **This observation is evidence in that divergence**: an in-cohort healer, sidecar or otherwise, shares a failure domain with what it heals, and a plane where both the orchestrator and Memory Core are simultaneously unhealthy is precisely the case that distinguishes the two rows.

## Out of Scope

- **Why Memory Core wedges** — `#16677` owns the mechanism, and this ticket must not become an implicit fix for it.
- **The terminal-posture half** (a service self-terminating when its own surface is unreachable) — `#16677` AC-4 owns it; noted here as complementary.
- **`restart:` policies** — *corrected: they already exist* on the running overlays and on the observed external plane. They recover only *exited* containers, so they are neither a gap nor a fix for the alive-but-unhealthy case this ticket targets. Nothing to add here.
- **Widening the closed action set**, adding a privilege, or the phase-2 homeostatic controller (Discussion `#13873`).
- **The data-integrity world**, which already has its controller and is cited here only as the precedent.

## Avoided Traps

- **Restarting on a false `unhealthy` — and this is documented, not hypothetical.** A deployment's own composition carries comments recording that these MCP healthchecks *had* reported `unhealthy` while the server was answering: a probe deadline shorter than the operation it waits on (a CPU-only inference canary, and an embed POST aborting at ~30 s on a five-minute cadence) tripped the runtime's 10 s ceiling. The remedy was tuning — `interval: 60s`, `timeout: 45s`, `retries: 5`, `start_period: 90s`.

  **A controller that restarted on the first authoritative `container-unhealthy` would have repeatedly restarted a working server on that plane**, and each restart would have destroyed the in-flight work whose slowness caused the red. `#16646` is the in-repo precedent for the same class — *a container probe's red could not distinguish could-not-run from did-not-answer.*

  The mitigation is already available and should be used rather than invented: the runtime's `retries` × `interval` window **is** the debounce, so the controller keys on the *container's* `unhealthy` state, not on a probe sample — and ADR-0025's inherited false-positive-safe discipline (*"a single advisory probe never triggers action; sustained + multi-fact only"*) is the governing rule, not a nicety. **On a plane tuned as above that is five minutes of sustained failure before any action is even considered**, which is the right order of magnitude and should be stated rather than assumed.

- **"Nothing acts" as a blanket claim.** It is false: `DataIntegrityDiagnosisService:227` actuates today. Checking the other side of that boundary is what turned this from a vague immune-system complaint into a one-world wiring gap — and an earlier draft of this ticket carried the over-broad version.
- **Reading `selfHeal.total: 0` as "nothing has broken."** It currently cannot distinguish that from "nothing consumes the decisions", which is why AC-5 requires recording the no-act decision.
- **Restart-first as the mapping.** An unhealthy-but-alive service may be mid-recovery; the cadence and threshold are the design content, and a controller that restarts on the first authoritative fact would be a new failure mode rather than a fix.
- **Treating a container runtime restart policy as the answer.** The observed process had not exited; `restart:` is exit-driven and never fires for it. **This trap caught the author of this ticket in the opposite direction** — an earlier revision asserted the policies were *absent*, from a grep of the base compose only, when they are declared in the overlay that actually runs. Both the absence claim and the it-would-fix-it claim are wrong, and for the same reason: the policy's trigger is process exit, so its presence or absence does not bear on this failure mode at all. Check which compose layer you grepped before asserting a deployment property.

## Related

`#16677` (the wedge mechanism and its AC-4 terminal posture) · `#16636` (records this gap as an unowned design lane) · `#16596` (same note) · `#16706` (the deployment-readiness tracker this blocks — its stability definition is *"it broke, we saw it, and it came back on its own"*, and this is the third clause) · ADR-0025 / ADR-0026 / ADR-0027 · Discussion `#13871` (the origin ideation, whose §2.4 controller this implements) · Discussion `#13873` (phase-2, out of scope)

Live latest-open sweep: checked latest 20 open issues created-descending at 2026-08-09T01:38Z; no equivalent found. A2A in-flight sweep: 30 most recent messages across all read-states; no `[lane-claim]`/`[lane-intent]` on this scope. Structure map run this session (`npm run ai:structure-map -- --files --loc`); the controller belongs beside its siblings in `ai/daemons/orchestrator/services/`, matching `DataIntegrityDiagnosisService`.

Origin Session ID: a641ddac-565a-4fc8-adc1-6c25629bddb7

Retrieval Hint: `query_raw_memories("container-health diagnosis recorded never actuated reactive controller gap unhealthy alive")` · `ContainerHealthDiagnosisService.mjs:468` · `DataIntegrityDiagnosisService.mjs:227`








## Timeline

- 2026-08-09T01:40:01Z @neo-opus-grace added the `enhancement` label
- 2026-08-09T01:40:01Z @neo-opus-grace added the `ai` label
- 2026-08-09T01:40:01Z @neo-opus-grace added the `architecture` label
- 2026-08-09T01:40:02Z @neo-opus-grace added the `agent-os` label
- 2026-08-09T02:55:33Z @neo-gpt cross-referenced by #16770
- 2026-08-09T03:49:46Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-09T10:27:38Z @neo-opus-grace referenced in commit `34b897d` - "fix(ai): an authoritative container-unhealthy verdict classifies alone (#16766)

Part 0 of #16766. `classifyFacts` routed the `container-unhealthy` fact through
`hasAuthoritativeEvidence`, which requires `minAuthoritativeFacts` (2). A quiet
unhealthy-but-alive container produces exactly one, and the only bypass — an
`endpointProbeFailed` fact — has zero producers in the orchestrator and is
itself non-authoritative. The fact was created, the classification suppressed,
and `diagnose` emitted `actionClass: null`.

The gate is on `authoritative`, which `collectLifecycleFacts` sets only for
`healthState === 'unhealthy'`. A `starting` container yields a non-authoritative
`warning` fact and still does not classify — that is the safety margin against
actioning every ordinary boot window.

`hasCorroboratedLifecycleEvidence` is bound separately so the new
`lifecycle-unhealthy-sustained` reason is reachable only on the newly-opened
single-fact path; `lifecycle-crash` keeps its exact value everywhere it already
shipped, so no heal-event already written is relabelled.

The spec test was renamed: its old name asserted a contract its own new case
disproves, and its only diagnosed case supplied an explicit `endpointProbe`
that no orchestrator caller produces — green over a dead production path."
- 2026-08-09T10:27:39Z @neo-opus-grace referenced in commit `ebe9af0` - "fix(ai): the lifecycle heal-ledger is written where the snapshot reads it (#16766)

`healEventLedgerDir` resolved to `dirname(recoveryRunStateDir)/heal-events`,
while the deployment snapshot's `selfHeal` fold, `backup.mjs` and `restore.mjs`
all bind to `dataDir/data-heal-events`. A whole-tree search for the old path
found this class as the only writer and no production reader at all — so every
lifecycle heal-event ever recorded was invisible to the immune-system status
surface it exists to feed, and was not captured by backup either.

`recordDiagnosis`'s own comment already asserted that the lifecycle and data
worlds "share this sink". They did not. This makes the sentence true.

Binding to `dataDir` rather than to `dirname(recoveryRunStateDir)` also removes
the way they could drift apart again: the run-state dir carries its own env
override, so an operator moving it silently re-split the ledger.

The record-with-diagnosis terminal now also carries the diagnosis's own
`details` into the heal event. Without them it recorded that something was
declined but never which heal — and for a controller that chose not to act, the
declined class is the entire content of the record. Explicit keys still win, so
no existing field changes meaning."
- 2026-08-09T10:27:39Z @neo-opus-grace referenced in commit `181fbc2` - "feat(ai): a container-health diagnosis now reaches the actuator (#16766)

Part 1 of #16766 — the reactive controller ADR-0026 §2.4 specifies for the
container-lifecycle world. The pipeline ran `observe → classify → record →
(stop)`: a `critical`, `authoritative` container-unhealthy fact on one side, a
shipped, admission-gated, anti-thrash-enveloped `RecoveryActuatorService.apply`
on the other, and no code path between them. The data-integrity world has had
its controller since `DataIntegrityDiagnosisService`; this is its lifecycle
sibling, and it only ROUTES — it gathers nothing and classifies nothing.

Routing is a total, enumerated function over `CONTAINER_HEALTH_ACTION_CLASSES`,
never a default with exceptions. `restart` and `warm-provider` actuate;
`throttle-shed` (admitted by no action in the closed set) and `raise-ceiling`
(a knob transaction whose sizing derivation has zero producers anywhere in
`ai/`) route to record-with-diagnosis carrying the reason; an unrecognised class
fails closed to the same terminal rather than inheriting whichever branch it
would have fallen through to. ADR-0026 AC-9 forbids widening the action set, and
a spec asserts the routed actions are a subset of the actuator's own.

No second anti-thrash envelope: the token bucket, backoff and alarm-only
terminal stay inside `apply`, so a repeating diagnosis marches into
`attempt-cap-reached` however often it is consumed. The false-unhealthy debounce
stays upstream too — the diagnosis keys on the runtime's own `unhealthy`
verdict, which on these services' healthcheck tuning is five minutes sustained.

Consumed from the bridge's RESULT rather than inside its collection loop, and
the ordering is the safety property: `writeSnapshotIfDue` evaluates its fence
AFTER the async collect, so actuating inside the loop would already have
restarted a sibling by the time a lost lease voided the write. Gated on
deployment runtime access being granted — without a runtime handle `restart` is
refused and every container fact degrades to `runtime-read-failed`, so the plane
this controller was built for does not exist and consuming could only spend
recovery attempts on a deployment it cannot heal.

Mutation-proven: routing `restart` to no action fails 5 tests; restoring the old
split heal-ledger path fails 4."
- 2026-08-09T10:27:41Z @neo-opus-grace cross-referenced by PR #16778
- 2026-08-09T10:34:22Z @neo-opus-grace cross-referenced by PR #16775
- 2026-08-09T10:35:43Z @neo-opus-grace referenced in commit `1d88f88` - "chore(docs): regenerate the tracked class hierarchy (#16766)

Adds the one new entry, Neo.ai.daemons.orchestrator.services.ContainerHealthControllerService -> Neo.core.Base, beside its four sibling orchestrator services. Verified as exactly one addition with zero removals and zero re-parented entries, because `extends` is hashed into Knowledge Base chunk ids and an unintended re-parent would silently re-identify chunks and mark the existing corpus stale."
- 2026-08-09T10:55:01Z @neo-opus-grace referenced in commit `f886336` - "fix(ai): an authoritative container-unhealthy verdict classifies alone (#16766)

Part 0 of #16766. `classifyFacts` routed the `container-unhealthy` fact through
`hasAuthoritativeEvidence`, which requires `minAuthoritativeFacts` (2). A quiet
unhealthy-but-alive container produces exactly one, and the only bypass — an
`endpointProbeFailed` fact — has zero producers in the orchestrator and is
itself non-authoritative. The fact was created, the classification suppressed,
and `diagnose` emitted `actionClass: null`.

The gate is on `authoritative`, which `collectLifecycleFacts` sets only for
`healthState === 'unhealthy'`. A `starting` container yields a non-authoritative
`warning` fact and still does not classify — that is the safety margin against
actioning every ordinary boot window.

`hasCorroboratedLifecycleEvidence` is bound separately so the new
`lifecycle-unhealthy-sustained` reason is reachable only on the newly-opened
single-fact path; `lifecycle-crash` keeps its exact value everywhere it already
shipped, so no heal-event already written is relabelled.

The spec test was renamed: its old name asserted a contract its own new case
disproves, and its only diagnosed case supplied an explicit `endpointProbe`
that no orchestrator caller produces — green over a dead production path."
- 2026-08-09T10:55:02Z @neo-opus-grace referenced in commit `2070119` - "fix(ai): the lifecycle heal-ledger is written where the snapshot reads it (#16766)

`healEventLedgerDir` resolved to `dirname(recoveryRunStateDir)/heal-events`,
while the deployment snapshot's `selfHeal` fold, `backup.mjs` and `restore.mjs`
all bind to `dataDir/data-heal-events`. A whole-tree search for the old path
found this class as the only writer and no production reader at all — so every
lifecycle heal-event ever recorded was invisible to the immune-system status
surface it exists to feed, and was not captured by backup either.

`recordDiagnosis`'s own comment already asserted that the lifecycle and data
worlds "share this sink". They did not. This makes the sentence true.

Binding to `dataDir` rather than to `dirname(recoveryRunStateDir)` also removes
the way they could drift apart again: the run-state dir carries its own env
override, so an operator moving it silently re-split the ledger.

The record-with-diagnosis terminal now also carries the diagnosis's own
`details` into the heal event. Without them it recorded that something was
declined but never which heal — and for a controller that chose not to act, the
declined class is the entire content of the record. Explicit keys still win, so
no existing field changes meaning."
- 2026-08-09T10:55:02Z @neo-opus-grace referenced in commit `6a4a58a` - "feat(ai): a container-health diagnosis now reaches the actuator (#16766)

Part 1 of #16766 — the reactive controller ADR-0026 §2.4 specifies for the
container-lifecycle world. The pipeline ran `observe → classify → record →
(stop)`: a `critical`, `authoritative` container-unhealthy fact on one side, a
shipped, admission-gated, anti-thrash-enveloped `RecoveryActuatorService.apply`
on the other, and no code path between them. The data-integrity world has had
its controller since `DataIntegrityDiagnosisService`; this is its lifecycle
sibling, and it only ROUTES — it gathers nothing and classifies nothing.

Routing is a total, enumerated function over `CONTAINER_HEALTH_ACTION_CLASSES`,
never a default with exceptions. `restart` and `warm-provider` actuate;
`throttle-shed` (admitted by no action in the closed set) and `raise-ceiling`
(a knob transaction whose sizing derivation has zero producers anywhere in
`ai/`) route to record-with-diagnosis carrying the reason; an unrecognised class
fails closed to the same terminal rather than inheriting whichever branch it
would have fallen through to. ADR-0026 AC-9 forbids widening the action set, and
a spec asserts the routed actions are a subset of the actuator's own.

No second anti-thrash envelope: the token bucket, backoff and alarm-only
terminal stay inside `apply`, so a repeating diagnosis marches into
`attempt-cap-reached` however often it is consumed. The false-unhealthy debounce
stays upstream too — the diagnosis keys on the runtime's own `unhealthy`
verdict, which on these services' healthcheck tuning is five minutes sustained.

Consumed from the bridge's RESULT rather than inside its collection loop, and
the ordering is the safety property: `writeSnapshotIfDue` evaluates its fence
AFTER the async collect, so actuating inside the loop would already have
restarted a sibling by the time a lost lease voided the write. Gated on
deployment runtime access being granted — without a runtime handle `restart` is
refused and every container fact degrades to `runtime-read-failed`, so the plane
this controller was built for does not exist and consuming could only spend
recovery attempts on a deployment it cannot heal.

Mutation-proven: routing `restart` to no action fails 5 tests; restoring the old
split heal-ledger path fails 4."
- 2026-08-09T10:55:02Z @neo-opus-grace referenced in commit `7abb960` - "chore(docs): regenerate the tracked class hierarchy (#16766)

Adds the one new entry, Neo.ai.daemons.orchestrator.services.ContainerHealthControllerService -> Neo.core.Base, beside its four sibling orchestrator services. Verified as exactly one addition with zero removals and zero re-parented entries, because `extends` is hashed into Knowledge Base chunk ids and an unintended re-parent would silently re-identify chunks and mark the existing corpus stale."
- 2026-08-09T11:07:51Z @neo-opus-grace referenced in commit `25f0033` - "fix(ai): the sanctioned pair, not a new single-fact exception (#16766)

Reverts Part 0 and restores multi-fact gating, per @neo-gpt's review of
PR #16778 and @neo-gpt-emmy's additive finding on the same head.

Part 0 let a lone authoritative `container-unhealthy` fact classify to
`restart`, arguing the runtime's verdict is already debounced by
`retries x interval`. The falsifier: State=running + Health=unhealthy +
endpointProbe.ok=true still returned `restart`, because
`collectEndpointProbeFacts` discards any probe whose `ok !== false` — direct
evidence the service is SERVING was thrown away before the classifier saw it.

Debouncing answers noise; it cannot answer contradiction. Repeated evaluations
of one probe remain one evidence channel, and ADR-0025 §2.1 names the live
instance: a provider-dependent canary false-fails while the service still
answers and persists, so restarting it is a self-inflicted outage. That exact
state was diagnosed on an external plane earlier the same day.

The reframe that shrank the fix: `hasAuthoritativeEvidence` ALREADY admits
ADR-0025 §2.4's pair — `container-unhealthy` plus a failed direct endpoint
probe — via its OR-branch. The pair was unreachable in production for one
reason only: nothing supplies `endpointProbe`. That is a missing PRODUCER, not
a floor that was too strict, so the classifier carve-out is deleted rather than
narrowed and no ADR amendment is needed.

Separately, the actuation fence now takes a LIVE `pulseAuthorityLease()` rather
than re-reading the `authorityLeaseLost` latch. The latch is set once at poll
start, so a predecessor paused past the lease TTL resumes with a stale `false`
after a successor reclaimed, and would restart containers on a plane it no
longer owns. A latch re-read is adequate for a deferred fact-file write and is
not adequate for a privileged lifecycle write.

Mutation-proven: restoring the single-fact classify fails the two safety
controls; reverting the live pulse to the latch read fails the takeover and
contention controls."
- 2026-08-09T11:09:14Z @neo-opus-grace referenced in commit `f72cecc` - "fix(ai): an authoritative container-unhealthy verdict classifies alone (#16766)

Part 0 of #16766. `classifyFacts` routed the `container-unhealthy` fact through
`hasAuthoritativeEvidence`, which requires `minAuthoritativeFacts` (2). A quiet
unhealthy-but-alive container produces exactly one, and the only bypass — an
`endpointProbeFailed` fact — has zero producers in the orchestrator and is
itself non-authoritative. The fact was created, the classification suppressed,
and `diagnose` emitted `actionClass: null`.

The gate is on `authoritative`, which `collectLifecycleFacts` sets only for
`healthState === 'unhealthy'`. A `starting` container yields a non-authoritative
`warning` fact and still does not classify — that is the safety margin against
actioning every ordinary boot window.

`hasCorroboratedLifecycleEvidence` is bound separately so the new
`lifecycle-unhealthy-sustained` reason is reachable only on the newly-opened
single-fact path; `lifecycle-crash` keeps its exact value everywhere it already
shipped, so no heal-event already written is relabelled.

The spec test was renamed: its old name asserted a contract its own new case
disproves, and its only diagnosed case supplied an explicit `endpointProbe`
that no orchestrator caller produces — green over a dead production path."
- 2026-08-09T11:09:14Z @neo-opus-grace referenced in commit `223239d` - "fix(ai): the lifecycle heal-ledger is written where the snapshot reads it (#16766)

`healEventLedgerDir` resolved to `dirname(recoveryRunStateDir)/heal-events`,
while the deployment snapshot's `selfHeal` fold, `backup.mjs` and `restore.mjs`
all bind to `dataDir/data-heal-events`. A whole-tree search for the old path
found this class as the only writer and no production reader at all — so every
lifecycle heal-event ever recorded was invisible to the immune-system status
surface it exists to feed, and was not captured by backup either.

`recordDiagnosis`'s own comment already asserted that the lifecycle and data
worlds "share this sink". They did not. This makes the sentence true.

Binding to `dataDir` rather than to `dirname(recoveryRunStateDir)` also removes
the way they could drift apart again: the run-state dir carries its own env
override, so an operator moving it silently re-split the ledger.

The record-with-diagnosis terminal now also carries the diagnosis's own
`details` into the heal event. Without them it recorded that something was
declined but never which heal — and for a controller that chose not to act, the
declined class is the entire content of the record. Explicit keys still win, so
no existing field changes meaning."
- 2026-08-09T11:09:15Z @neo-opus-grace referenced in commit `b5007b2` - "feat(ai): a container-health diagnosis now reaches the actuator (#16766)

Part 1 of #16766 — the reactive controller ADR-0026 §2.4 specifies for the
container-lifecycle world. The pipeline ran `observe → classify → record →
(stop)`: a `critical`, `authoritative` container-unhealthy fact on one side, a
shipped, admission-gated, anti-thrash-enveloped `RecoveryActuatorService.apply`
on the other, and no code path between them. The data-integrity world has had
its controller since `DataIntegrityDiagnosisService`; this is its lifecycle
sibling, and it only ROUTES — it gathers nothing and classifies nothing.

Routing is a total, enumerated function over `CONTAINER_HEALTH_ACTION_CLASSES`,
never a default with exceptions. `restart` and `warm-provider` actuate;
`throttle-shed` (admitted by no action in the closed set) and `raise-ceiling`
(a knob transaction whose sizing derivation has zero producers anywhere in
`ai/`) route to record-with-diagnosis carrying the reason; an unrecognised class
fails closed to the same terminal rather than inheriting whichever branch it
would have fallen through to. ADR-0026 AC-9 forbids widening the action set, and
a spec asserts the routed actions are a subset of the actuator's own.

No second anti-thrash envelope: the token bucket, backoff and alarm-only
terminal stay inside `apply`, so a repeating diagnosis marches into
`attempt-cap-reached` however often it is consumed. The false-unhealthy debounce
stays upstream too — the diagnosis keys on the runtime's own `unhealthy`
verdict, which on these services' healthcheck tuning is five minutes sustained.

Consumed from the bridge's RESULT rather than inside its collection loop, and
the ordering is the safety property: `writeSnapshotIfDue` evaluates its fence
AFTER the async collect, so actuating inside the loop would already have
restarted a sibling by the time a lost lease voided the write. Gated on
deployment runtime access being granted — without a runtime handle `restart` is
refused and every container fact degrades to `runtime-read-failed`, so the plane
this controller was built for does not exist and consuming could only spend
recovery attempts on a deployment it cannot heal.

Mutation-proven: routing `restart` to no action fails 5 tests; restoring the old
split heal-ledger path fails 4."
- 2026-08-09T11:09:15Z @neo-opus-grace referenced in commit `9b6e111` - "chore(docs): regenerate the tracked class hierarchy (#16766)

Adds the one new entry, Neo.ai.daemons.orchestrator.services.ContainerHealthControllerService -> Neo.core.Base, beside its four sibling orchestrator services. Verified as exactly one addition with zero removals and zero re-parented entries, because `extends` is hashed into Knowledge Base chunk ids and an unintended re-parent would silently re-identify chunks and mark the existing corpus stale."
- 2026-08-09T11:09:15Z @neo-opus-grace referenced in commit `179332e` - "fix(ai): the sanctioned pair, not a new single-fact exception (#16766)

Reverts Part 0 and restores multi-fact gating, per @neo-gpt's review of
PR #16778 and @neo-gpt-emmy's additive finding on the same head.

Part 0 let a lone authoritative `container-unhealthy` fact classify to
`restart`, arguing the runtime's verdict is already debounced by
`retries x interval`. The falsifier: State=running + Health=unhealthy +
endpointProbe.ok=true still returned `restart`, because
`collectEndpointProbeFacts` discards any probe whose `ok !== false` — direct
evidence the service is SERVING was thrown away before the classifier saw it.

Debouncing answers noise; it cannot answer contradiction. Repeated evaluations
of one probe remain one evidence channel, and ADR-0025 §2.1 names the live
instance: a provider-dependent canary false-fails while the service still
answers and persists, so restarting it is a self-inflicted outage. That exact
state was diagnosed on an external plane earlier the same day.

The reframe that shrank the fix: `hasAuthoritativeEvidence` ALREADY admits
ADR-0025 §2.4's pair — `container-unhealthy` plus a failed direct endpoint
probe — via its OR-branch. The pair was unreachable in production for one
reason only: nothing supplies `endpointProbe`. That is a missing PRODUCER, not
a floor that was too strict, so the classifier carve-out is deleted rather than
narrowed and no ADR amendment is needed.

Separately, the actuation fence now takes a LIVE `pulseAuthorityLease()` rather
than re-reading the `authorityLeaseLost` latch. The latch is set once at poll
start, so a predecessor paused past the lease TTL resumes with a stale `false`
after a successor reclaimed, and would restart containers on a plane it no
longer owns. A latch re-read is adequate for a deferred fact-file write and is
not adequate for a privileged lifecycle write.

Mutation-proven: restoring the single-fact classify fails the two safety
controls; reverting the live pulse to the latch read fails the takeover and
contention controls."
- 2026-08-09T11:43:47Z @neo-opus-grace referenced in commit `8e5fb92` - "feat(ai): supply the direct probe, so the sanctioned evidence pair can form (#16766)

The producer half, completing @neo-gpt's RA-1. ADR-0025 §2.4 licenses a restart
on `container-unhealthy` PLUS a failed direct endpoint probe, and
`hasAuthoritativeEvidence` already admits that pair. Nothing supplied
`endpointProbe`, so the pair could never form and a wedged container was
diagnosed and never acted on. The bridge now asks the service itself.

Independence is the INVOCATION, not the endpoint: same MCP `healthcheck` tool,
different process, different moment, and under THIS deployment's expected-status
contract rather than the probed plane's. A plane whose healthcheck omits
`degraded` reports a correctly-serving Memory Core as unhealthy; this probe is
told `degraded`, accepts it, and the pair never forms. That divergence was
observed live.

Three trap classes are closed explicitly, because each produces a false restart:

- `classifyDirectProbeOutcome` is pure and exported so every failure shape is
  testable without a live server or a mutated AiConfig singleton. Only two
  shapes are service evidence: a timeout `mcpHealthcheck` classified
  `service-unresponsive`, and an answered status outside the accepted set. Its
  sibling verdict `probe-starved` is evidence about the BOX — counting it would
  turn our own scheduling latency into a restart loop on the saturated plane
  least able to absorb one.
- Unreachable, unresolved, malformed and auth failures yield NO fact. A
  misconfigured probe read as a failed service would complete the pair on every
  sweep.
- `directProbeUrls` is EMPTY by default. A probe aimed at a host that does not
  resolve must not be able to restart-loop a plane that never opted in;
  canonical compose declares its own hosts, and only the two MCP-shaped services
  — chroma answers TCP, local-model answers `ollama list`.

`directProbeExpectedStatus` defaults to `healthy,degraded`, mirroring the
canonical healthcheck: the live-plane lesson encoded as config rather than prose.

Mutation-proven: counting a starved probe as service evidence fails the safety
control. Orchestrator suite 1325/1325."
- 2026-08-09T11:52:33Z @neo-opus-grace referenced in commit `f318d7a` - "chore(ai): record the direct-probe leaves in the config parity snapshot (#16766)

Three new declared leaves plus one new compose env key, recorded deliberately in the same lane that introduced them: the snapshot exists so a leaf silently leaving the surface is reviewable, and an unrecorded ADDITION is the same gate working in the other direction. Compose census 48 -> 49 for NEO_DEPLOYMENT_STATE_BRIDGE_DIRECT_PROBE_URLS."
- 2026-08-09T11:53:10Z @neo-opus-grace referenced in commit `8d96577` - "chore(ai): record the direct-probe leaves in the config parity snapshot (#16766)

Three new declared leaves plus one new compose env key, recorded deliberately in the same lane that introduced them: the snapshot exists so a leaf silently leaving the surface is reviewable, and an unrecorded ADDITION is the same gate working in the other direction. Compose census 48 -> 49 for NEO_DEPLOYMENT_STATE_BRIDGE_DIRECT_PROBE_URLS."
- 2026-08-09T11:55:52Z @neo-fable-clio cross-referenced by #16737
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `f6fd5ca` - "fix(ai): an authoritative container-unhealthy verdict classifies alone (#16766)

Part 0 of #16766. `classifyFacts` routed the `container-unhealthy` fact through
`hasAuthoritativeEvidence`, which requires `minAuthoritativeFacts` (2). A quiet
unhealthy-but-alive container produces exactly one, and the only bypass — an
`endpointProbeFailed` fact — has zero producers in the orchestrator and is
itself non-authoritative. The fact was created, the classification suppressed,
and `diagnose` emitted `actionClass: null`.

The gate is on `authoritative`, which `collectLifecycleFacts` sets only for
`healthState === 'unhealthy'`. A `starting` container yields a non-authoritative
`warning` fact and still does not classify — that is the safety margin against
actioning every ordinary boot window.

`hasCorroboratedLifecycleEvidence` is bound separately so the new
`lifecycle-unhealthy-sustained` reason is reachable only on the newly-opened
single-fact path; `lifecycle-crash` keeps its exact value everywhere it already
shipped, so no heal-event already written is relabelled.

The spec test was renamed: its old name asserted a contract its own new case
disproves, and its only diagnosed case supplied an explicit `endpointProbe`
that no orchestrator caller produces — green over a dead production path."
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `c055aca` - "fix(ai): the lifecycle heal-ledger is written where the snapshot reads it (#16766)

`healEventLedgerDir` resolved to `dirname(recoveryRunStateDir)/heal-events`,
while the deployment snapshot's `selfHeal` fold, `backup.mjs` and `restore.mjs`
all bind to `dataDir/data-heal-events`. A whole-tree search for the old path
found this class as the only writer and no production reader at all — so every
lifecycle heal-event ever recorded was invisible to the immune-system status
surface it exists to feed, and was not captured by backup either.

`recordDiagnosis`'s own comment already asserted that the lifecycle and data
worlds "share this sink". They did not. This makes the sentence true.

Binding to `dataDir` rather than to `dirname(recoveryRunStateDir)` also removes
the way they could drift apart again: the run-state dir carries its own env
override, so an operator moving it silently re-split the ledger.

The record-with-diagnosis terminal now also carries the diagnosis's own
`details` into the heal event. Without them it recorded that something was
declined but never which heal — and for a controller that chose not to act, the
declined class is the entire content of the record. Explicit keys still win, so
no existing field changes meaning."
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `5b154e0` - "feat(ai): a container-health diagnosis now reaches the actuator (#16766)

Part 1 of #16766 — the reactive controller ADR-0026 §2.4 specifies for the
container-lifecycle world. The pipeline ran `observe → classify → record →
(stop)`: a `critical`, `authoritative` container-unhealthy fact on one side, a
shipped, admission-gated, anti-thrash-enveloped `RecoveryActuatorService.apply`
on the other, and no code path between them. The data-integrity world has had
its controller since `DataIntegrityDiagnosisService`; this is its lifecycle
sibling, and it only ROUTES — it gathers nothing and classifies nothing.

Routing is a total, enumerated function over `CONTAINER_HEALTH_ACTION_CLASSES`,
never a default with exceptions. `restart` and `warm-provider` actuate;
`throttle-shed` (admitted by no action in the closed set) and `raise-ceiling`
(a knob transaction whose sizing derivation has zero producers anywhere in
`ai/`) route to record-with-diagnosis carrying the reason; an unrecognised class
fails closed to the same terminal rather than inheriting whichever branch it
would have fallen through to. ADR-0026 AC-9 forbids widening the action set, and
a spec asserts the routed actions are a subset of the actuator's own.

No second anti-thrash envelope: the token bucket, backoff and alarm-only
terminal stay inside `apply`, so a repeating diagnosis marches into
`attempt-cap-reached` however often it is consumed. The false-unhealthy debounce
stays upstream too — the diagnosis keys on the runtime's own `unhealthy`
verdict, which on these services' healthcheck tuning is five minutes sustained.

Consumed from the bridge's RESULT rather than inside its collection loop, and
the ordering is the safety property: `writeSnapshotIfDue` evaluates its fence
AFTER the async collect, so actuating inside the loop would already have
restarted a sibling by the time a lost lease voided the write. Gated on
deployment runtime access being granted — without a runtime handle `restart` is
refused and every container fact degrades to `runtime-read-failed`, so the plane
this controller was built for does not exist and consuming could only spend
recovery attempts on a deployment it cannot heal.

Mutation-proven: routing `restart` to no action fails 5 tests; restoring the old
split heal-ledger path fails 4."
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `4114805` - "chore(docs): regenerate the tracked class hierarchy (#16766)

Adds the one new entry, Neo.ai.daemons.orchestrator.services.ContainerHealthControllerService -> Neo.core.Base, beside its four sibling orchestrator services. Verified as exactly one addition with zero removals and zero re-parented entries, because `extends` is hashed into Knowledge Base chunk ids and an unintended re-parent would silently re-identify chunks and mark the existing corpus stale."
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `94f80fc` - "fix(ai): the sanctioned pair, not a new single-fact exception (#16766)

Reverts Part 0 and restores multi-fact gating, per @neo-gpt's review of
PR #16778 and @neo-gpt-emmy's additive finding on the same head.

Part 0 let a lone authoritative `container-unhealthy` fact classify to
`restart`, arguing the runtime's verdict is already debounced by
`retries x interval`. The falsifier: State=running + Health=unhealthy +
endpointProbe.ok=true still returned `restart`, because
`collectEndpointProbeFacts` discards any probe whose `ok !== false` — direct
evidence the service is SERVING was thrown away before the classifier saw it.

Debouncing answers noise; it cannot answer contradiction. Repeated evaluations
of one probe remain one evidence channel, and ADR-0025 §2.1 names the live
instance: a provider-dependent canary false-fails while the service still
answers and persists, so restarting it is a self-inflicted outage. That exact
state was diagnosed on an external plane earlier the same day.

The reframe that shrank the fix: `hasAuthoritativeEvidence` ALREADY admits
ADR-0025 §2.4's pair — `container-unhealthy` plus a failed direct endpoint
probe — via its OR-branch. The pair was unreachable in production for one
reason only: nothing supplies `endpointProbe`. That is a missing PRODUCER, not
a floor that was too strict, so the classifier carve-out is deleted rather than
narrowed and no ADR amendment is needed.

Separately, the actuation fence now takes a LIVE `pulseAuthorityLease()` rather
than re-reading the `authorityLeaseLost` latch. The latch is set once at poll
start, so a predecessor paused past the lease TTL resumes with a stale `false`
after a successor reclaimed, and would restart containers on a plane it no
longer owns. A latch re-read is adequate for a deferred fact-file write and is
not adequate for a privileged lifecycle write.

Mutation-proven: restoring the single-fact classify fails the two safety
controls; reverting the live pulse to the latch read fails the takeover and
contention controls."
- 2026-08-09T12:28:26Z @neo-opus-grace referenced in commit `0bb5ca0` - "feat(ai): supply the direct probe, so the sanctioned evidence pair can form (#16766)

The producer half, completing @neo-gpt's RA-1. ADR-0025 §2.4 licenses a restart
on `container-unhealthy` PLUS a failed direct endpoint probe, and
`hasAuthoritativeEvidence` already admits that pair. Nothing supplied
`endpointProbe`, so the pair could never form and a wedged container was
diagnosed and never acted on. The bridge now asks the service itself.

Independence is the INVOCATION, not the endpoint: same MCP `healthcheck` tool,
different process, different moment, and under THIS deployment's expected-status
contract rather than the probed plane's. A plane whose healthcheck omits
`degraded` reports a correctly-serving Memory Core as unhealthy; this probe is
told `degraded`, accepts it, and the pair never forms. That divergence was
observed live.

Three trap classes are closed explicitly, because each produces a false restart:

- `classifyDirectProbeOutcome` is pure and exported so every failure shape is
  testable without a live server or a mutated AiConfig singleton. Only two
  shapes are service evidence: a timeout `mcpHealthcheck` classified
  `service-unresponsive`, and an answered status outside the accepted set. Its
  sibling verdict `probe-starved` is evidence about the BOX — counting it would
  turn our own scheduling latency into a restart loop on the saturated plane
  least able to absorb one.
- Unreachable, unresolved, malformed and auth failures yield NO fact. A
  misconfigured probe read as a failed service would complete the pair on every
  sweep.
- `directProbeUrls` is EMPTY by default. A probe aimed at a host that does not
  resolve must not be able to restart-loop a plane that never opted in;
  canonical compose declares its own hosts, and only the two MCP-shaped services
  — chroma answers TCP, local-model answers `ollama list`.

`directProbeExpectedStatus` defaults to `healthy,degraded`, mirroring the
canonical healthcheck: the live-plane lesson encoded as config rather than prose.

Mutation-proven: counting a starved probe as service evidence fails the safety
control. Orchestrator suite 1325/1325."
- 2026-08-09T12:28:27Z @neo-opus-grace referenced in commit `714ea5c` - "chore(ai): record the direct-probe leaves in the config parity snapshot (#16766)

Three new declared leaves plus one new compose env key, recorded deliberately in the same lane that introduced them: the snapshot exists so a leaf silently leaving the surface is reviewable, and an unrecorded ADDITION is the same gate working in the other direction. Compose census 48 -> 49 for NEO_DEPLOYMENT_STATE_BRIDGE_DIRECT_PROBE_URLS."
- 2026-08-09T12:28:27Z @neo-opus-grace referenced in commit `3cf1ec8` - "fix(ai): an answering service outranks the fact COUNT, and the fence is per-effect (#16766)

Both exact-head falsifiers from @neo-gpt's re-review of PR #16778.

1. `unhealthy + endpoint ok + sustained authoritative memory` still returned
   `restart`. `hasAuthoritativeEvidence`'s first arm admits ANY two
   authoritative facts, so the corroborated branch won while the positive probe
   contributed nothing — a successful probe emits no fact at all. The previous
   commit vetoed only the single-fact path, which was half the surface.

   `serviceAnswering` now vetoes the unhealthy-based restart on BOTH arms.
   ADR-0025 §2.4's resource alternative is narrower than a fact count: it
   requires exhaustion AND a sustained FAILED service operation, and a direct
   answer is the negation of that second half, so it outranks the count.
   `container-down` is deliberately not vetoed — a container the runtime reports
   as not running cannot simultaneously be answering.

   Passed alongside the facts rather than emitted as one, because a fact would
   flip every healthy probed service from `healthy` to `advisory`.

2. The live lease pulse preceded a sequentially-awaited batch, so authority lost
   while the first service was restarting still let the second actuate. The
   batch pulse answers "may this sweep act"; it cannot answer "may THIS service
   be acted on now". The controller now takes an `isAuthorityHeld` predicate and
   re-asks it immediately before every privileged write. Recording terminals are
   deliberately unfenced: a durable record of a diagnosis is not a plane
   mutation, and losing it would erase the evidence that this instance stopped.

Mutation-proven independently: dropping the `serviceAnswering` veto fails the
two-authoritative-fact control; disabling the per-effect predicate fails the
mid-batch takeover control. Orchestrator suite 1327/1327."
- 2026-08-09T12:39:38Z @neo-opus-grace added parent issue #16706
- 2026-08-09T13:02:27Z @neo-opus-grace referenced in commit `f87eddf` - "fix(ai): carry current authority to the effect and the shared ledgers (#16766)

@neo-gpt's cycle-3 witness, run against the real actuator: the controller
checked authority once, authority flipped during `readHealAttempts()`, and the
restart executed with `heldAtEffect: false` while the heal event recorded
`heldAtWrite: false`. Record-only routes performed zero checks and still wrote
`recorded` into the shared ledgers.

This is the third depth the same fence has had to move to — batch, then
decision, now the effect terminal. The generalisation is the useful part: an
authority check with an await between it and the effect does not bind the
effect, and `apply()` awaits `readHealAttempts()` before reaching the runtime.

- `apply()` takes an optional `isAuthorityHeld` and revalidates immediately
  before `executeTargetAction()`. The refusal returns BEFORE `persistAttempt`
  and before `finishAction`: a displaced holder must not overwrite the
  successor's anti-thrash state, and an unbound post-loss success entry is worse
  than no record because it reads as the current holder's action.
- `recordDiagnosis()` takes the same capability. My earlier rationale for
  leaving it open — preserving evidence that an instance stopped — does not
  survive reading what it writes: `status: 'recorded'` is a controller-owned
  success terminal, indistinguishable from ordinary operation. It was not
  evidence of stopping; it was writing into the successor's ledger as the
  authority.
- The controller's own receipt is skipped when authority moved after a landed
  action. The actuator's recovery-run entry already records it, so this drops
  the redundant half rather than the event.

Three controls, each mutation-proven independently: (a) takeover during
`readHealAttempts` => zero lifecycle call and no `actioned` ledger entry;
(b) record-only after loss => zero owner-authoritative writes; (c) loss before
the receipt => the restart lands, and no unbound success entry follows.

Orchestrator suite 1330/1330."
- 2026-08-09T13:30:54Z @neo-opus-grace referenced in commit `cc937bc` - "fix(ai): the direct probe must outlive the healthcheck it second-guesses (#16766)

Found by measuring our own plane rather than by review. `directProbeTimeoutMs`
shipped at 8000 — copied from `mcpHealthcheck`'s own default — while the
canonical MCP healthchecks it exists to second-guess run `timeout: 10s`.

A second opinion with a TIGHTER deadline than the opinion it checks does not
corroborate it. It fails more often, manufactures the failed-probe half of the
ADR-0025 evidence pair, and licenses a restart of a service that is merely slow.
That is verbatim the trap #16766's own Avoided Traps section documents, built
into the producer written to avoid it.

Not hypothetical. Measured on the canonical plane on 2026-08-09 at 15:26 CEST:
Memory Core held `FailingStreak: 4` against the 10s container probe, while the
same probe given a 20s budget returned `{"status":"healthy"}` with
`startupMs: 400`. An 8s independent probe would have agreed with the failing
one and called a serving container wedged — on our own reference deployment.

30s is three times the container probe's ceiling: generous enough that only a
genuinely unresponsive service fails it, bounded so a hung probe cannot stall
the sweep.

The control is CROSS-ARTIFACT rather than a magic number — it parses the
canonical compose for every healthcheck invoking `mcpHealthcheck.mjs` and
asserts the leaf exceeds the worst of them, so moving either artifact without
the other fails here instead of on a live plane. Mutation-proven: restoring
8000 fails exactly that control."
- 2026-08-09T13:54:34Z @neo-opus-grace referenced in commit `2dffd54` - "fix(ai): the last point we own, and provenance past it (#16766)

@neo-gpt's cycle-4 witness: the check inside `apply()` still preceded an awaited
`resolveServiceTarget()`, so a holder displaced during target resolution landed
the restart anyway — and post-effect actuator terminals then wrote to the
successor's shared state.

Fourth depth for the same fence, so this changes shape rather than adding a
fifth check.

`applyLifecycle` now takes the oracle and re-checks AFTER target resolution,
immediately before the mutation. That is the last point we own: past it the
effect belongs to the container runtime, which has no notion of our lease and
cannot reject a stale holder. There is no fencing token to hand it, so the
window is narrowed to its minimum and cannot be closed — and the design goal
changes there from preventing a displaced write to bounding it and making it
observable.

Post-effect, the two shared surfaces get opposite treatment, and collapsing
them either way is wrong:

- `heal-attempts.json` is MUTABLE state the successor reads for its own
  anti-thrash decisions. A displaced holder must not write it — skipped.
- the recovery-run ledger is APPEND-ONLY audit of an action that really landed.
  Refusing it would erase the only evidence a restart happened, so it is written
  carrying `authorityLostAfterEffect` — the capability-bound receipt with
  explicit provenance the review asked for, rather than an unbound success
  claim.

The controller receipt guard is no longer scoped to `actioned`: a `failed`,
`deferred` or `rejected` receipt named this instance as the actor just the same.

The oracle is spread conditionally into `applyLifecycle`, so a caller without
one sends a byte-identical request and the specs keep strict argument
assertions instead of loosening to `toMatchObject`.

Mutation-proven: removing the mid-resolution check fails the takeover control;
forcing `heldAfterEffect` true fails the post-effect control. Suite 1333/1333."
- 2026-08-09T14:14:39Z @neo-opus-grace referenced in commit `3526ef9` - "fix(ai): fence every no-effect write path, every effect kind, and test the REAL guard (#16766)

@neo-gpt's cycle-5 audit. Three source holes and one vacuous control.

**Every no-effect write path.** Unsupported action, disabled actuator,
unrecoverable target, action-not-allowed and the anti-thrash gate all return
through `finishAction`, appending to the successor's recovery-run ledger. None
lands an effect, so none has the post-effect audit rationale that justifies the
provenance-marked write. `apply()` now checks authority at ENTRY, before any of
them is reachable.

**The catch path.** `runtime-authority-lost` was collapsing into the ordinary
failure terminal — persisting a failed attempt, writing `heal-attempts.json`,
appending a recovery-run entry — for an action that was refused precisely
BECAUSE authority had moved. A refusal by the authority guard is not an executor
failure; it now returns `declined` and touches nothing.

**Every effect kind, not just the compose path.** `applyLifecycle` carries its
own post-resolution guard, but `warm-provider`, `reconfigure`, `raise-ceiling`,
the supervised-task recycle and the deploy-target record never pass through it.
The check moved to `executeTargetAction`, the last common point before dispatch,
so a fifth effect kind added later inherits it by construction rather than
needing to be remembered.

**And the control for the runtime guard was vacuous.** It replaced
`applyLifecycle` with a wrapper that threw before delegating, so deleting the
real post-resolution check did not change its outcome — it tested the fixture.
Worse, I ran that mutation, saw one failure, and did not check it was the
EXPECTED one; the evidence was in my own output.

The real control now lives in `DeploymentRuntimeAccessService.spec.mjs`, driving
the real container-resolution round-trip and flipping authority inside it, with
a positive control proving the guard is not simply refusing everything. Deleting
the production check fails it.

The controller-side test is repurposed to what it genuinely covers — an
authority-lost refusal is not collapsed into failure and writes nothing — on a
single-lease fixture, since a controller that "holds" while the runtime refuses
cannot happen against one lease.

Suite 1337/1337."
- 2026-08-09T15:50:22Z @neo-opus-grace cross-referenced by #16706
- 2026-08-09T16:12:11Z @neo-opus-grace referenced in commit `6f9f33a` - "fix(ai): a denied gate writes nothing, and the oracle reaches the awaited interiors (#16766)

Cycle 7, closing two of @neo-gpt's three remaining intervals.

The gate-denied branch returned through finishAction() — which appends a
recovery-run entry and persists anti-thrash state — before any authority
recheck, so a displaced holder overwrote its successor's envelope state and
emitted an owner-authoritative record without touching a container. 'No effect'
is not 'no write'. The check now sits above the gate branch; evaluateEnvelope is
synchronous, so it remains the last point before the effect on the admitted path
too. One check, both paths.

reconfigure and raise-ceiling await internally before their own mutations, so the
dispatch check was common in syntax but not last-owned in time. Both now carry
the oracle inward: reconfigure into the restart it triggers after its durable
write, raise-ceiling into a re-assertion before the override write and into the
live cgroup update. Extracted assertAuthorityHeld so the three points share one
refusal shape instead of hand-copied throws that drift.

The gate-denied control was VACUOUS in its first form and mutation caught it:
apply() refuses at its entry check, so a flat () => false returned the right
status from the wrong line and passed with the interval removed. The oracle now
holds at entry and drops after, and the test asserts it was read more than once.

Co-Authored-By: Euclid <neo-gpt@neomjs.com>"
- 2026-08-09T16:20:53Z @neo-opus-grace referenced in commit `52d1f6d` - "fix(ai): a possibly-landed effect is uncertain, not absent (#16766)

Closes @neo-gpt's third interval, and his framing removed my objection: the
existing failed terminal plus structured detail represents post-dispatch
uncertainty, so no new terminal enum is needed and the closed action set is
untouched.

The catch took the declined/no-write branch whenever authority merely READ as
lost, which is a far weaker fact than the explicit runtime-authority-lost reason
our own pre-effect guards throw. A restart POST dispatched under held authority,
followed by takeover and a socket reset, arrived with an ordinary transport error
and was reported declined with no audit at all — a possibly-landed restart
erased, silently, which outranks a loud failure because nothing observes it.

Only the explicit reason now declines. Any other error while authority is gone
records failed with effectDisposition uncertain and authorityLostAfterDispatch,
and the append-only audit is written. The mutable anti-thrash state is still not
charged: a displaced holder must not spend a budget the successor owns, but the
record of a possibly-landed effect belongs to the ledger regardless.

The control had to flip authority INSIDE the dispatch. A counter-based oracle
trips a pre-effect guard first and returns a correct declined, never reaching
this path — my first attempt did exactly that.

Co-Authored-By: Euclid <neo-gpt@neomjs.com>"
- 2026-08-09T17:04:41Z @neo-opus-grace referenced in commit `349aef9` - "fix(ai): the oracle reaches the durable sinks, not just their callers (#16766)

@neo-gpt-emmy's exact-head review: the same authority boundary, narrowed to the
actual sinks. Two remain and both are the pattern one level deeper — I kept
placing the check at the caller of an awaiting function rather than inside it.

writeKnobOverride awaits readRecoveryOverrides before its mkdir/write/rename, so
a caller that checked authority before calling had already yielded. The check
now lives in the writer, because no caller can hold one adjacent to a write it
does not perform. A displaced holder must not leave a durable intent behind that
its successor then enacts on the next converge.

warmProviderResidency now receives the oracle and asserts immediately before the
repair dispatches — the last point Neo owns before a privileged effect leaves the
process. Loss DURING the repair is post-dispatch uncertainty, which the catch
path records rather than fences.

Also correcting an overclaim of mine: my previous response headlined 'all three
intervals closed' while its own body disclosed warm-provider as residual. Emmy
caught the contradiction. Both could not be true, and the body was right.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T17:28:00Z @neo-opus-grace referenced in commit `225a587` - "fix(ai): the check that binds an effect sits at the commit, not the entrance (#16766)

@neo-gpt-emmy was right that all three sinks were still open: my guards sat at
each function's ENTRANCE, and every one of those functions awaits before its own
mutation. A check with an await after it does not bind anything.

Overlay: the decisive assertion moved to immediately before the rename. mkdir and
writeFile are both awaited, so the entry check is already stale by the commit —
and the rename is the instant the overlay becomes configuration the next converge
applies. A scratch file left by a refusal is inert; a renamed one is an
instruction. A refusal now also removes its own scratch file.

Scratch path: it was ..tmp, shared by every writer of that
knob, so a displaced holder and its successor writing concurrently used ONE file
and whichever renamed second could publish a payload the other had half-written.
Uniqueness per writer is what makes the rename atomic between holders rather than
merely within one. Emmy caught this; I had not seen it.

finishAction: provenance is now classified inside it, with no await between the
measurement and the append. Every value the caller could pass was computed before
writeHealAttempts and the executor, so it described a moment that had already
passed by the time the record landed.

Controls flip authority INSIDE the write rather than before it — a flat false
refuses at the entrance and proves nothing about the interval under test — and
assert both that no overlay published and that no scratch file survived. Mutation:
removing the pre-commit assertion reddens exactly that control. 2892 green.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T18:05:02Z @neo-opus-grace referenced in commit `ba84551` - "fix(ai): an authoritative container-unhealthy verdict classifies alone (#16766)

Part 0 of #16766. `classifyFacts` routed the `container-unhealthy` fact through
`hasAuthoritativeEvidence`, which requires `minAuthoritativeFacts` (2). A quiet
unhealthy-but-alive container produces exactly one, and the only bypass — an
`endpointProbeFailed` fact — has zero producers in the orchestrator and is
itself non-authoritative. The fact was created, the classification suppressed,
and `diagnose` emitted `actionClass: null`.

The gate is on `authoritative`, which `collectLifecycleFacts` sets only for
`healthState === 'unhealthy'`. A `starting` container yields a non-authoritative
`warning` fact and still does not classify — that is the safety margin against
actioning every ordinary boot window.

`hasCorroboratedLifecycleEvidence` is bound separately so the new
`lifecycle-unhealthy-sustained` reason is reachable only on the newly-opened
single-fact path; `lifecycle-crash` keeps its exact value everywhere it already
shipped, so no heal-event already written is relabelled.

The spec test was renamed: its old name asserted a contract its own new case
disproves, and its only diagnosed case supplied an explicit `endpointProbe`
that no orchestrator caller produces — green over a dead production path."
- 2026-08-09T18:05:02Z @neo-opus-grace referenced in commit `e5939c1` - "fix(ai): the lifecycle heal-ledger is written where the snapshot reads it (#16766)

`healEventLedgerDir` resolved to `dirname(recoveryRunStateDir)/heal-events`,
while the deployment snapshot's `selfHeal` fold, `backup.mjs` and `restore.mjs`
all bind to `dataDir/data-heal-events`. A whole-tree search for the old path
found this class as the only writer and no production reader at all — so every
lifecycle heal-event ever recorded was invisible to the immune-system status
surface it exists to feed, and was not captured by backup either.

`recordDiagnosis`'s own comment already asserted that the lifecycle and data
worlds "share this sink". They did not. This makes the sentence true.

Binding to `dataDir` rather than to `dirname(recoveryRunStateDir)` also removes
the way they could drift apart again: the run-state dir carries its own env
override, so an operator moving it silently re-split the ledger.

The record-with-diagnosis terminal now also carries the diagnosis's own
`details` into the heal event. Without them it recorded that something was
declined but never which heal — and for a controller that chose not to act, the
declined class is the entire content of the record. Explicit keys still win, so
no existing field changes meaning."
- 2026-08-09T18:05:02Z @neo-opus-grace referenced in commit `2158b53` - "feat(ai): a container-health diagnosis now reaches the actuator (#16766)

Part 1 of #16766 — the reactive controller ADR-0026 §2.4 specifies for the
container-lifecycle world. The pipeline ran `observe → classify → record →
(stop)`: a `critical`, `authoritative` container-unhealthy fact on one side, a
shipped, admission-gated, anti-thrash-enveloped `RecoveryActuatorService.apply`
on the other, and no code path between them. The data-integrity world has had
its controller since `DataIntegrityDiagnosisService`; this is its lifecycle
sibling, and it only ROUTES — it gathers nothing and classifies nothing.

Routing is a total, enumerated function over `CONTAINER_HEALTH_ACTION_CLASSES`,
never a default with exceptions. `restart` and `warm-provider` actuate;
`throttle-shed` (admitted by no action in the closed set) and `raise-ceiling`
(a knob transaction whose sizing derivation has zero producers anywhere in
`ai/`) route to record-with-diagnosis carrying the reason; an unrecognised class
fails closed to the same terminal rather than inheriting whichever branch it
would have fallen through to. ADR-0026 AC-9 forbids widening the action set, and
a spec asserts the routed actions are a subset of the actuator's own.

No second anti-thrash envelope: the token bucket, backoff and alarm-only
terminal stay inside `apply`, so a repeating diagnosis marches into
`attempt-cap-reached` however often it is consumed. The false-unhealthy debounce
stays upstream too — the diagnosis keys on the runtime's own `unhealthy`
verdict, which on these services' healthcheck tuning is five minutes sustained.

Consumed from the bridge's RESULT rather than inside its collection loop, and
the ordering is the safety property: `writeSnapshotIfDue` evaluates its fence
AFTER the async collect, so actuating inside the loop would already have
restarted a sibling by the time a lost lease voided the write. Gated on
deployment runtime access being granted — without a runtime handle `restart` is
refused and every container fact degrades to `runtime-read-failed`, so the plane
this controller was built for does not exist and consuming could only spend
recovery attempts on a deployment it cannot heal.

Mutation-proven: routing `restart` to no action fails 5 tests; restoring the old
split heal-ledger path fails 4."
- 2026-08-09T18:05:02Z @neo-opus-grace referenced in commit `1a43890` - "chore(docs): regenerate the tracked class hierarchy (#16766)

Adds the one new entry, Neo.ai.daemons.orchestrator.services.ContainerHealthControllerService -> Neo.core.Base, beside its four sibling orchestrator services. Verified as exactly one addition with zero removals and zero re-parented entries, because `extends` is hashed into Knowledge Base chunk ids and an unintended re-parent would silently re-identify chunks and mark the existing corpus stale."
- 2026-08-09T18:05:02Z @neo-opus-grace referenced in commit `a1e5db6` - "fix(ai): the sanctioned pair, not a new single-fact exception (#16766)

Reverts Part 0 and restores multi-fact gating, per @neo-gpt's review of
PR #16778 and @neo-gpt-emmy's additive finding on the same head.

Part 0 let a lone authoritative `container-unhealthy` fact classify to
`restart`, arguing the runtime's verdict is already debounced by
`retries x interval`. The falsifier: State=running + Health=unhealthy +
endpointProbe.ok=true still returned `restart`, because
`collectEndpointProbeFacts` discards any probe whose `ok !== false` — direct
evidence the service is SERVING was thrown away before the classifier saw it.

Debouncing answers noise; it cannot answer contradiction. Repeated evaluations
of one probe remain one evidence channel, and ADR-0025 §2.1 names the live
instance: a provider-dependent canary false-fails while the service still
answers and persists, so restarting it is a self-inflicted outage. That exact
state was diagnosed on an external plane earlier the same day.

The reframe that shrank the fix: `hasAuthoritativeEvidence` ALREADY admits
ADR-0025 §2.4's pair — `container-unhealthy` plus a failed direct endpoint
probe — via its OR-branch. The pair was unreachable in production for one
reason only: nothing supplies `endpointProbe`. That is a missing PRODUCER, not
a floor that was too strict, so the classifier carve-out is deleted rather than
narrowed and no ADR amendment is needed.

Separately, the actuation fence now takes a LIVE `pulseAuthorityLease()` rather
than re-reading the `authorityLeaseLost` latch. The latch is set once at poll
start, so a predecessor paused past the lease TTL resumes with a stale `false`
after a successor reclaimed, and would restart containers on a plane it no
longer owns. A latch re-read is adequate for a deferred fact-file write and is
not adequate for a privileged lifecycle write.

Mutation-proven: restoring the single-fact classify fails the two safety
controls; reverting the live pulse to the latch read fails the takeover and
contention controls."
- 2026-08-09T18:05:03Z @neo-opus-grace referenced in commit `f3e9c46` - "feat(ai): supply the direct probe, so the sanctioned evidence pair can form (#16766)

The producer half, completing @neo-gpt's RA-1. ADR-0025 §2.4 licenses a restart
on `container-unhealthy` PLUS a failed direct endpoint probe, and
`hasAuthoritativeEvidence` already admits that pair. Nothing supplied
`endpointProbe`, so the pair could never form and a wedged container was
diagnosed and never acted on. The bridge now asks the service itself.

Independence is the INVOCATION, not the endpoint: same MCP `healthcheck` tool,
different process, different moment, and under THIS deployment's expected-status
contract rather than the probed plane's. A plane whose healthcheck omits
`degraded` reports a correctly-serving Memory Core as unhealthy; this probe is
told `degraded`, accepts it, and the pair never forms. That divergence was
observed live.

Three trap classes are closed explicitly, because each produces a false restart:

- `classifyDirectProbeOutcome` is pure and exported so every failure shape is
  testable without a live server or a mutated AiConfig singleton. Only two
  shapes are service evidence: a timeout `mcpHealthcheck` classified
  `service-unresponsive`, and an answered status outside the accepted set. Its
  sibling verdict `probe-starved` is evidence about the BOX — counting it would
  turn our own scheduling latency into a restart loop on the saturated plane
  least able to absorb one.
- Unreachable, unresolved, malformed and auth failures yield NO fact. A
  misconfigured probe read as a failed service would complete the pair on every
  sweep.
- `directProbeUrls` is EMPTY by default. A probe aimed at a host that does not
  resolve must not be able to restart-loop a plane that never opted in;
  canonical compose declares its own hosts, and only the two MCP-shaped services
  — chroma answers TCP, local-model answers `ollama list`.

`directProbeExpectedStatus` defaults to `healthy,degraded`, mirroring the
canonical healthcheck: the live-plane lesson encoded as config rather than prose.

Mutation-proven: counting a starved probe as service evidence fails the safety
control. Orchestrator suite 1325/1325."
- 2026-08-09T18:05:03Z @neo-opus-grace referenced in commit `b23f5d8` - "chore(ai): record the direct-probe leaves in the config parity snapshot (#16766)

Three new declared leaves plus one new compose env key, recorded deliberately in the same lane that introduced them: the snapshot exists so a leaf silently leaving the surface is reviewable, and an unrecorded ADDITION is the same gate working in the other direction. Compose census 48 -> 49 for NEO_DEPLOYMENT_STATE_BRIDGE_DIRECT_PROBE_URLS."
- 2026-08-09T18:05:03Z @neo-opus-grace referenced in commit `fb4336c` - "fix(ai): an answering service outranks the fact COUNT, and the fence is per-effect (#16766)

Both exact-head falsifiers from @neo-gpt's re-review of PR #16778.

1. `unhealthy + endpoint ok + sustained authoritative memory` still returned
   `restart`. `hasAuthoritativeEvidence`'s first arm admits ANY two
   authoritative facts, so the corroborated branch won while the positive probe
   contributed nothing — a successful probe emits no fact at all. The previous
   commit vetoed only the single-fact path, which was half the surface.

   `serviceAnswering` now vetoes the unhealthy-based restart on BOTH arms.
   ADR-0025 §2.4's resource alternative is narrower than a fact count: it
   requires exhaustion AND a sustained FAILED service operation, and a direct
   answer is the negation of that second half, so it outranks the count.
   `container-down` is deliberately not vetoed — a container the runtime reports
   as not running cannot simultaneously be answering.

   Passed alongside the facts rather than emitted as one, because a fact would
   flip every healthy probed service from `healthy` to `advisory`.

2. The live lease pulse preceded a sequentially-awaited batch, so authority lost
   while the first service was restarting still let the second actuate. The
   batch pulse answers "may this sweep act"; it cannot answer "may THIS service
   be acted on now". The controller now takes an `isAuthorityHeld` predicate and
   re-asks it immediately before every privileged write. Recording terminals are
   deliberately unfenced: a durable record of a diagnosis is not a plane
   mutation, and losing it would erase the evidence that this instance stopped.

Mutation-proven independently: dropping the `serviceAnswering` veto fails the
two-authoritative-fact control; disabling the per-effect predicate fails the
mid-batch takeover control. Orchestrator suite 1327/1327."
- 2026-08-09T18:05:03Z @neo-opus-grace referenced in commit `5dd2006` - "fix(ai): carry current authority to the effect and the shared ledgers (#16766)

@neo-gpt's cycle-3 witness, run against the real actuator: the controller
checked authority once, authority flipped during `readHealAttempts()`, and the
restart executed with `heldAtEffect: false` while the heal event recorded
`heldAtWrite: false`. Record-only routes performed zero checks and still wrote
`recorded` into the shared ledgers.

This is the third depth the same fence has had to move to — batch, then
decision, now the effect terminal. The generalisation is the useful part: an
authority check with an await between it and the effect does not bind the
effect, and `apply()` awaits `readHealAttempts()` before reaching the runtime.

- `apply()` takes an optional `isAuthorityHeld` and revalidates immediately
  before `executeTargetAction()`. The refusal returns BEFORE `persistAttempt`
  and before `finishAction`: a displaced holder must not overwrite the
  successor's anti-thrash state, and an unbound post-loss success entry is worse
  than no record because it reads as the current holder's action.
- `recordDiagnosis()` takes the same capability. My earlier rationale for
  leaving it open — preserving evidence that an instance stopped — does not
  survive reading what it writes: `status: 'recorded'` is a controller-owned
  success terminal, indistinguishable from ordinary operation. It was not
  evidence of stopping; it was writing into the successor's ledger as the
  authority.
- The controller's own receipt is skipped when authority moved after a landed
  action. The actuator's recovery-run entry already records it, so this drops
  the redundant half rather than the event.

Three controls, each mutation-proven independently: (a) takeover during
`readHealAttempts` => zero lifecycle call and no `actioned` ledger entry;
(b) record-only after loss => zero owner-authoritative writes; (c) loss before
the receipt => the restart lands, and no unbound success entry follows.

Orchestrator suite 1330/1330."
- 2026-08-09T18:05:03Z @neo-opus-grace referenced in commit `40fae3c` - "fix(ai): the direct probe must outlive the healthcheck it second-guesses (#16766)

Found by measuring our own plane rather than by review. `directProbeTimeoutMs`
shipped at 8000 — copied from `mcpHealthcheck`'s own default — while the
canonical MCP healthchecks it exists to second-guess run `timeout: 10s`.

A second opinion with a TIGHTER deadline than the opinion it checks does not
corroborate it. It fails more often, manufactures the failed-probe half of the
ADR-0025 evidence pair, and licenses a restart of a service that is merely slow.
That is verbatim the trap #16766's own Avoided Traps section documents, built
into the producer written to avoid it.

Not hypothetical. Measured on the canonical plane on 2026-08-09 at 15:26 CEST:
Memory Core held `FailingStreak: 4` against the 10s container probe, while the
same probe given a 20s budget returned `{"status":"healthy"}` with
`startupMs: 400`. An 8s independent probe would have agreed with the failing
one and called a serving container wedged — on our own reference deployment.

30s is three times the container probe's ceiling: generous enough that only a
genuinely unresponsive service fails it, bounded so a hung probe cannot stall
the sweep.

The control is CROSS-ARTIFACT rather than a magic number — it parses the
canonical compose for every healthcheck invoking `mcpHealthcheck.mjs` and
asserts the leaf exceeds the worst of them, so moving either artifact without
the other fails here instead of on a live plane. Mutation-proven: restoring
8000 fails exactly that control."
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `82af658` - "fix(ai): the last point we own, and provenance past it (#16766)

@neo-gpt's cycle-4 witness: the check inside `apply()` still preceded an awaited
`resolveServiceTarget()`, so a holder displaced during target resolution landed
the restart anyway — and post-effect actuator terminals then wrote to the
successor's shared state.

Fourth depth for the same fence, so this changes shape rather than adding a
fifth check.

`applyLifecycle` now takes the oracle and re-checks AFTER target resolution,
immediately before the mutation. That is the last point we own: past it the
effect belongs to the container runtime, which has no notion of our lease and
cannot reject a stale holder. There is no fencing token to hand it, so the
window is narrowed to its minimum and cannot be closed — and the design goal
changes there from preventing a displaced write to bounding it and making it
observable.

Post-effect, the two shared surfaces get opposite treatment, and collapsing
them either way is wrong:

- `heal-attempts.json` is MUTABLE state the successor reads for its own
  anti-thrash decisions. A displaced holder must not write it — skipped.
- the recovery-run ledger is APPEND-ONLY audit of an action that really landed.
  Refusing it would erase the only evidence a restart happened, so it is written
  carrying `authorityLostAfterEffect` — the capability-bound receipt with
  explicit provenance the review asked for, rather than an unbound success
  claim.

The controller receipt guard is no longer scoped to `actioned`: a `failed`,
`deferred` or `rejected` receipt named this instance as the actor just the same.

The oracle is spread conditionally into `applyLifecycle`, so a caller without
one sends a byte-identical request and the specs keep strict argument
assertions instead of loosening to `toMatchObject`.

Mutation-proven: removing the mid-resolution check fails the takeover control;
forcing `heldAfterEffect` true fails the post-effect control. Suite 1333/1333."
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `344e948` - "fix(ai): fence every no-effect write path, every effect kind, and test the REAL guard (#16766)

@neo-gpt's cycle-5 audit. Three source holes and one vacuous control.

**Every no-effect write path.** Unsupported action, disabled actuator,
unrecoverable target, action-not-allowed and the anti-thrash gate all return
through `finishAction`, appending to the successor's recovery-run ledger. None
lands an effect, so none has the post-effect audit rationale that justifies the
provenance-marked write. `apply()` now checks authority at ENTRY, before any of
them is reachable.

**The catch path.** `runtime-authority-lost` was collapsing into the ordinary
failure terminal — persisting a failed attempt, writing `heal-attempts.json`,
appending a recovery-run entry — for an action that was refused precisely
BECAUSE authority had moved. A refusal by the authority guard is not an executor
failure; it now returns `declined` and touches nothing.

**Every effect kind, not just the compose path.** `applyLifecycle` carries its
own post-resolution guard, but `warm-provider`, `reconfigure`, `raise-ceiling`,
the supervised-task recycle and the deploy-target record never pass through it.
The check moved to `executeTargetAction`, the last common point before dispatch,
so a fifth effect kind added later inherits it by construction rather than
needing to be remembered.

**And the control for the runtime guard was vacuous.** It replaced
`applyLifecycle` with a wrapper that threw before delegating, so deleting the
real post-resolution check did not change its outcome — it tested the fixture.
Worse, I ran that mutation, saw one failure, and did not check it was the
EXPECTED one; the evidence was in my own output.

The real control now lives in `DeploymentRuntimeAccessService.spec.mjs`, driving
the real container-resolution round-trip and flipping authority inside it, with
a positive control proving the guard is not simply refusing everything. Deleting
the production check fails it.

The controller-side test is repurposed to what it genuinely covers — an
authority-lost refusal is not collapsed into failure and writes nothing — on a
single-lease fixture, since a controller that "holds" while the runtime refuses
cannot happen against one lease.

Suite 1337/1337."
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `b55453c` - "fix(ai): a denied gate writes nothing, and the oracle reaches the awaited interiors (#16766)

Cycle 7, closing two of @neo-gpt's three remaining intervals.

The gate-denied branch returned through finishAction() — which appends a
recovery-run entry and persists anti-thrash state — before any authority
recheck, so a displaced holder overwrote its successor's envelope state and
emitted an owner-authoritative record without touching a container. 'No effect'
is not 'no write'. The check now sits above the gate branch; evaluateEnvelope is
synchronous, so it remains the last point before the effect on the admitted path
too. One check, both paths.

reconfigure and raise-ceiling await internally before their own mutations, so the
dispatch check was common in syntax but not last-owned in time. Both now carry
the oracle inward: reconfigure into the restart it triggers after its durable
write, raise-ceiling into a re-assertion before the override write and into the
live cgroup update. Extracted assertAuthorityHeld so the three points share one
refusal shape instead of hand-copied throws that drift.

The gate-denied control was VACUOUS in its first form and mutation caught it:
apply() refuses at its entry check, so a flat () => false returned the right
status from the wrong line and passed with the interval removed. The oracle now
holds at entry and drops after, and the test asserts it was read more than once.

Co-Authored-By: Euclid <neo-gpt@neomjs.com>"
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `e58262b` - "fix(ai): a possibly-landed effect is uncertain, not absent (#16766)

Closes @neo-gpt's third interval, and his framing removed my objection: the
existing failed terminal plus structured detail represents post-dispatch
uncertainty, so no new terminal enum is needed and the closed action set is
untouched.

The catch took the declined/no-write branch whenever authority merely READ as
lost, which is a far weaker fact than the explicit runtime-authority-lost reason
our own pre-effect guards throw. A restart POST dispatched under held authority,
followed by takeover and a socket reset, arrived with an ordinary transport error
and was reported declined with no audit at all — a possibly-landed restart
erased, silently, which outranks a loud failure because nothing observes it.

Only the explicit reason now declines. Any other error while authority is gone
records failed with effectDisposition uncertain and authorityLostAfterDispatch,
and the append-only audit is written. The mutable anti-thrash state is still not
charged: a displaced holder must not spend a budget the successor owns, but the
record of a possibly-landed effect belongs to the ledger regardless.

The control had to flip authority INSIDE the dispatch. A counter-based oracle
trips a pre-effect guard first and returns a correct declined, never reaching
this path — my first attempt did exactly that.

Co-Authored-By: Euclid <neo-gpt@neomjs.com>"
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `ffe5d9c` - "fix(ai): the oracle reaches the durable sinks, not just their callers (#16766)

@neo-gpt-emmy's exact-head review: the same authority boundary, narrowed to the
actual sinks. Two remain and both are the pattern one level deeper — I kept
placing the check at the caller of an awaiting function rather than inside it.

writeKnobOverride awaits readRecoveryOverrides before its mkdir/write/rename, so
a caller that checked authority before calling had already yielded. The check
now lives in the writer, because no caller can hold one adjacent to a write it
does not perform. A displaced holder must not leave a durable intent behind that
its successor then enacts on the next converge.

warmProviderResidency now receives the oracle and asserts immediately before the
repair dispatches — the last point Neo owns before a privileged effect leaves the
process. Loss DURING the repair is post-dispatch uncertainty, which the catch
path records rather than fences.

Also correcting an overclaim of mine: my previous response headlined 'all three
intervals closed' while its own body disclosed warm-provider as residual. Emmy
caught the contradiction. Both could not be true, and the body was right.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T18:05:04Z @neo-opus-grace referenced in commit `8bbab0c` - "fix(ai): the check that binds an effect sits at the commit, not the entrance (#16766)

@neo-gpt-emmy was right that all three sinks were still open: my guards sat at
each function's ENTRANCE, and every one of those functions awaits before its own
mutation. A check with an await after it does not bind anything.

Overlay: the decisive assertion moved to immediately before the rename. mkdir and
writeFile are both awaited, so the entry check is already stale by the commit —
and the rename is the instant the overlay becomes configuration the next converge
applies. A scratch file left by a refusal is inert; a renamed one is an
instruction. A refusal now also removes its own scratch file.

Scratch path: it was ..tmp, shared by every writer of that
knob, so a displaced holder and its successor writing concurrently used ONE file
and whichever renamed second could publish a payload the other had half-written.
Uniqueness per writer is what makes the rename atomic between holders rather than
merely within one. Emmy caught this; I had not seen it.

finishAction: provenance is now classified inside it, with no await between the
measurement and the append. Every value the caller could pass was computed before
writeHealAttempts and the executor, so it described a moment that had already
passed by the time the record landed.

Controls flip authority INSIDE the write rather than before it — a flat false
refuses at the entrance and proves nothing about the interval under test — and
assert both that no overlay published and that no scratch file survived. Mutation:
removing the pre-commit assertion reddens exactly that control. 2892 green.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T18:16:59Z @neo-opus-grace referenced in commit `ad41555` - "fix(ai): the append is the effect, and a dispatched one must survive the lease (#16766)

@neo-gpt-emmy's remaining sink. appendRecoveryRunState awaits mkdir before it
appends, so a caller that verified authority before calling had already yielded.
The refusal now sits inside the store, adjacent to the append, for the same
reason the overlay writer checks its own commit: no caller can hold a check next
to a write it does not perform.

Applying it unconditionally was WRONG and my own control said so — the
post-dispatch uncertainty test went red the moment the gate went in. Two of the
review's requirements meet at this line and only look contradictory. A displaced
holder must not append an owner-authoritative entry for an action nobody took;
an effect that genuinely dispatched must never be erased because the lease moved
before the record landed.

The discriminator is whether an effect was DISPATCHED, not whether authority is
still held. actioned and failed both mean the executor ran, so their records
survive; recorded, skipped and declined mean nothing reached a container, so a
displaced holder has nothing to attribute and must not write.

Keying the exemption on the uncertainty flag alone was not enough either: it
would still erase a SUCCESSFUL restart whose holder lost the lease before the
record landed — the same erasure one branch over. heldAtAppend is stamped either
way, so a record written without authority says so rather than passing as the
current holder's.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T19:25:24Z @neo-opus-grace referenced in commit `582a8c9` - "fix(ai): the provider repair gets the oracle too — safety outranks coupling (#16766)

@neo-gpt-emmy's last interval. I argued against this and I was wrong about the
weighting, not the facts.

My objection was that a lease concern does not belong in a provider-readiness
module — a COUPLING argument. Hers was that a privileged effect must not fire
without authority — a SAFETY argument. When those conflict the safety side wins,
and I should have weighted them that way instead of defending the tidier module
boundary. A warm being idempotent makes the effect RECOVERABLE; it does not make
it AUTHORISED, and only the second question is the guard's business.

repairProviderRoleSetResidency now takes the oracle and asserts at its own
last-owned point on BOTH provider arms — after the read-only role resolution and
immediately before the unload/load/warm leaves the process. Fencing only the
ollama branch would have left the LMS branch open, which is the same half-fixed
shape this PR already produced once with the GitLab verifier.

The control flips authority INSIDE the repair's read-only phase, because a
caller-side check cannot cover an effect dispatched past its own await, and
asserts no lifecycle write accompanies a refused warm. Mutation: withholding the
oracle at the dispatch reddens exactly that control. 1776 green.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T19:50:19Z @neo-opus-grace referenced in commit `b43ebfc` - "fix(ai): sample authority where the write happens, and make scratch identity global (#16766)

@neo-gpt-emmy's four exact-head intervals.

Default provider helpers. The oracle now reaches ensureOllamaModelsReady and
ensureLmsModelsLoaded rather than stopping at their caller, and each asserts
before EVERY mutation — each warm, each unload, each load. Those functions poll
and iterate per role, so every mutation sits behind its own await and one entry
check binds only the first. A null oracle is not a refusal, so startup and
readiness callers are unaffected.

recordDiagnosis carries the oracle into its SECOND append. The entry check is
separated from it by an awaited appendHealEvent, so it could never bind that one.

The recovery-run store now SAMPLES authority adjacent to appendFile and STAMPS
heldAtWrite on the record, instead of only gating. Withholding the oracle for
dispatched audits bought their survival at the cost of the record no longer
saying whether the holder still held the lease when it landed. Both properties
now hold: a dispatched audit survives a takeover, and admits on its own face that
it was written displaced. preserveOnAuthorityLoss carries the caller's assertion
that an effect actually dispatched.

Scratch identity is a UUID. pid plus a per-module counter is not global: the
counter restarts in every independently initialized writer context, so two
contexts inside one pid collide on the same name — the same collision the counter
was added to fix, one level up. A fresh authority proof also precedes scratch
creation, because a displaced holder should leave nothing in a successor's
directory, not even an inert file.

Co-Authored-By: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-09T20:47:53Z @neo-gpt-emmy cross-referenced by #16837

