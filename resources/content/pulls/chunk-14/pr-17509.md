---
number: 17509
title: >-
  feat(fleet): compose recipients become removable — toggle + chips; priority
  becomes a radio group (#17311)
author: neo-fable-clio
state: OPEN
createdAt: '2026-08-21T21:36:18Z'
updatedAt: '2026-08-21T21:44:46Z'
closedAt: null
mergedAt: null
head: feature/17311-compose-controls
base: dev
url: 'https://github.com/neomjs/neo/pull/17509'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17311

The operator's first-send friction, fixed at both controls: a selected recipient is now REMOVABLE — click the row again (a new opt-in `toggleOnClick` on `Neo.selection.ListModel`, default false, so no existing consumer changes), or hit the × on its chip. The current selection renders as a chip row of real Buttons on the `.fm-chip` affordance family, rebuilt from `selectionChange` — presentation over the one selection truth, never a second one. The `AGENT:*` broadcast sentinel is now DEFINED as exclusive: it and named picks never co-exist; whichever side was newly picked wins. Priority drops the three-item combo for a three-radio group (the whole decision space visible), keeping the shipped `high` default — the ticket's original `normal` line is amended in-body with the AC-7 steering rationale.

Evidence: L3 achieved (the full journey runs as a real-click e2e: select→chip, toggle-deselect, chip-×, both exclusivity directions, radio move — `OperatorComposeControlsNL.spec.mjs`, green) → L3 required (every AC names click-observable behavior). Residual: none.

## Deltas from ticket

1. Engine half taken as the ticket's sanctioned option A: `toggleOnClick` config on `selection.ListModel` (opt-in; the click path maps the record through `getSelectionItemId` before the `isSelected` check — the same entry conversion `select()`/`deselect()` apply).
2. Chips are real `Button` instances, not DOM-delegated spans: click wiring, focus and a11y come free; the chip row costs zero pixels while empty.
3. Priority default `high`, not the ticket's original `normal` — in-body amendment with rationale (shipped AC-7 decision + today's wake-priority incident as its living proof).
4. Radio-group theme gap: none encountered — `form.field.Radio` renders acceptably in both skins; nothing filed.
5. Environmental find during verification, NOT this diff (stash-falsified, defect-note broadcast, repro offered to @neo-fable whose dock-layout look it touches): the cockpit south tab-set intermittently projects only Activity on bridge-connected boots — the e2e instances (bridge deferred) render all four reliably.

## Test Evidence

- `OperatorComposeControlsNL.spec.mjs` (new): 1/1 green — the four control contracts through real clicks with worker-injected recipients.
- `unit/selection` + `unit/component` + `unit/list` owning trees: 79/79.
- Full agentos e2e: same capability-gated seat state as PR #17491/#17505 (popup/wire fails pre-exist, disjoint); the compose journey + pins green.
- `check-theme-surfaces` ✓; themes build clean.

## Post-Merge Validation

- Live-roster recipients (named peers) re-verified against a bridge-connected instance once the south-tab restore defect clears — the injected-options journey covers the contract meanwhile.

## Commits (if multi-commit)

- single commit — engine config + form controls + skin + journey spec.

Authored by Clio (Claude Fable 5, Claude Code). Session 8947f450-e0c3-424b-8aa1-1e52ea33c03f.


## Comments

### `@neo-fable-clio` commented on 2026-08-21T21:44:46Z

Head 7a0dfa3e68 → b9b698ddb0: the pre-existing compose unit spec batched `['@neo-opus-ada','AGENT:*']` as a legal pair — superseded by the now-DEFINED exclusivity (AC-3). The test adopts the contract: the batch settles to the broadcast alone, `to === ['AGENT:*']`, pending count 1. Full owning tree `unit/apps/agentos` 781/781. (My earlier owning-tree run missed this tree — the scoped-green trap, caught by CI as designed.) 📜

---

