---
id: 16207
title: 'Neural Link''s RecorderService opens the canonical graph read/write with no opt-out, making every host seat a plane writer'
state: OPEN
labels:
  - bug
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-07-31T01:26:02Z'
updatedAt: '2026-07-31T03:42:58Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16207'
author: neo-opus-vega
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Neural Link's RecorderService opens the canonical graph read/write with no opt-out, making every host seat a plane writer

## Context

Surfaced on 2026-07-31 during the #16167 cutover of this machine to the container topology. Memory Core and Knowledge Base are now containerized behind a loopback ingress; Neural Link stays host-side stdio and is expected to (it possesses the browser, so it is host-edge by nature — see #15173 for the streamable-HTTP direction).

With MC containerized against a bind-mounted canonical plane, every host-side Neural Link becomes a **concurrent writer on the same SQLite file** as the containerized Memory Core.

Live evidence during the cutover — two NL processes from one seat, holding the canonical graph with write handles (`u` = read/write):

```
pid 36168  ai/mcp/server/neural-link/mcp-server.mjs   fd=14u
pid 36315  ai/mcp/server/neural-link/mcp-server.mjs   fd=14u
  -> /Users/Shared/github/neomjs/neo/.neo-ai-data/sqlite/memory-core-graph.sqlite
```

They respawned on harness restart. Nine seats means nine potential NL writers against one containerized reader/writer.

Live latest-open sweep at 2026-07-31T01:20Z: no open issue covers an NL recorder opt-out or its role as a plane writer.

## The Problem

**1. There is no opt-out.** A repo-wide sweep for a gate (`NEO_NL_*RECORD*`, `recorderEnabled`, `disableRecorder`, and neighbours) returns nothing. The only way to stop NL from writing the canonical graph today is to unset or blank `NEO_MEMORY_DB_PATH`, which works only because `RecorderService.mjs:87` degrades on a falsy path:

```
logger.warn('[RecorderService] memoryCoreDbPath not configured. Disabling logging.');
```

Disabling a writer by emptying a *path* is a side effect, not an interface. It cannot be expressed per-seat without also breaking anything else that reads the same config leaf, and it reads as a misconfiguration rather than an intent.

**2. The runbook's writer census cannot see it.** #16167's §2 census pattern is:

```
[a]i/(mcp/server/(memory-core|knowledge-base)/mcp-server|daemons/.+/daemon|scripts/(lifecycle|maintenance)/.+)\.mjs
```

`neural-link` is not in that alternation. During the cutover the census reported **empty** while two NL processes still held write handles on the graph — caught only because `lsof` on the file itself disagreed with the process census. The file is the truth; the pattern is a proxy, and the proxy was wrong.

**3. The default is deliberate, which is why this needs care rather than a revert.** `configBase.mjs` documents the reason the recorder is plane-anchored:

> The previous homedir default therefore split writers from readers on any seat with the env unset — the recorders wrote to a file the consumers never open, and gap inference silently produced no edges. Converging the default is the repair.

So NL *should* write where `GapInferenceEngine` and `DreamService` read. The defect is the absence of an opt-out, not the convergence.

## The Architectural Reality

- Neural Link has **no service in `ai/deploy/docker-compose.yml`** — not an omission; it drives a host browser. Host-edge is correct for it.
- Therefore host NL + container MC on one bind-mounted plane is the **steady state** under the current topology, not a transient cutover condition. It does not resolve itself when the cutover finishes.
- SQLite in WAL mode tolerates multiple writers, so this is a correctness-under-contention concern rather than guaranteed corruption. But it is exactly the condition §2's census exists to exclude before the plane moves, which makes an invisible writer worse than a known one.
- **Two adjacent paths already isolate correctly and need no change** — worth recording so nobody "fixes" them:
  - `ai/scripts/diagnostics/genesisProbe.mjs:259` sets `NEO_MEMORY_DB_PATH` into its own `mkdtemp('neo-genesis-probe-')` root, so the #15173 probe's untouched-default-paths guarantee holds by construction.
  - Playwright isolates via `NEO_TELEMETRY_DB_PATH_TEST` (`test/playwright/configTemplateResolver.mjs:140`), selected by `memoryCoreDbUseTestDatabase` / `memoryCoreDbUseTestHarness` at `configBase.mjs:144`.

## The Fix

> **Superseded 2026-07-31 (items 1–2):** the prescription-change comment below elects **default OFF** with an explicit enable, after measuring zero attributed consumption. Items 3–4 stand as written. The Contract Ledger and Acceptance Criteria below are reconciled to the superseding prescription per PR #16209 review cycle 1.

1. ~~Add an explicit opt-out for NL action logging~~ → **Default the recorder OFF**; a named config leaf with an env binding, so enabling is per-seat declared intent (see prescription-change comment).
2. When disabled, degrade **silently and completely** — no partial writes, no per-call warning spam, and no implication that the graph is being recorded when it is not. **The gate must sit at the telemetry capability boundary, not above the service:** `RecorderService` also hosts the independent `nl_transaction_archive` save/replay contract (#14829), which stays available under either gate value.
3. Widen #16167's §2 census pattern to include `neural-link` (and audit the alternation for any other MCP server that opens a plane artifact), so the census stops reporting empty while writers are live.
4. Document the steady-state expectation: with the container topology, host-edge NL and containerized MC share the graph by design. State whether that is accepted, or whether NL should route writes through MC's remote API instead of opening SQLite directly. That second option is the real architectural answer and belongs in #15173's scope, not here.

## Contract Ledger

*(Reconciled 2026-07-31 — supersedes the original ledger, which predated the default-OFF prescription change and did not name the archive contract.)*

| Target Surface | Source of Authority | Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `actionLoggingEnabled` leaf (`NEO_NL_ACTION_LOGGING`) | prescription-change comment (2026-07-31) + ADR 0019 | **default OFF**; enabling is per-seat declared intent | unset = OFF: no telemetry write, no boot connection, no plane handle | NL config JSDoc | fresh-child behavioral specs, both arms (`RecorderServiceDefaultOff.spec.mjs`) |
| `nl_transaction_archive` save/replay | #14829 / PR #14836 (shipped product contract) | **independent of the gate** — the store opens lazily on first archive use under either gate value | a valid save never returns `archive-store-unavailable` under the default | `RecorderService` JSDoc | default-config round-trip through the real recorder + server-boundary replay specs |
| `RecorderService.log()` / `querySequences()` / `pruneOlderThan()` | the leaf | no-op unless the leaf is enabled — an open archive store alone is NOT telemetry consent | disabled = silent no-op, exactly one boot line | `RecorderService` JSDoc | unit specs + parity topology marker |
| Genesis probe telemetry oracle | blind-probe commitment (#15291 / D#15173) | the probe opts IN explicitly inside its disposable root | — | probe env-builder comment | invoked `createProbeEnvironments()` assertion (`genesisProbe.spec.mjs`); end-to-end run = L4 residual below |
| #16167 §2 census pattern | observed live NL write handles | matches every MCP server that opens a plane artifact, including `neural-link` | a census miss fails the runbook step rather than passing silently | runbook §2 | live non-empty census receipt (PR #16209) + structural tripwire |

## Acceptance Criteria

*(Reconciled 2026-07-31 to the prescription-change comment — default OFF — per PR #16209 review cycle 1. State is current at PR head `c0ba51a9d8`.)*

- [x] A named config leaf with an env binding gates NL action logging, **default OFF** (`actionLoggingEnabled` / `NEO_NL_ACTION_LOGGING`; declarative, ADR-0019-aligned).
- [x] With no configuration, the real `RecorderService.initAsync()` opens **no connection and creates no database artifact** — asserted by booting the service in a fresh child process and checking connection state plus the config-resolved path. `[L3-deferred — operator handoff needed]` Server-level twin: after the next harness restart, `lsof` the canonical graph and confirm no `neural-link` pid holds a `…u` descriptor.
- [x] With the enable set, a connection opens and `nl_action_log` is created and populated (enabled arm of the same child-process spec).
- [x] **The independent `nl_transaction_archive` save/replay contract (#14829) keeps working under the disabled default** — witnessed by a default-config child-process round-trip through the real recorder (save → read-back → replay mark) plus the server-boundary replay specs. *(Added in review cycle 1 — the first head gated the whole service and regressed this; the reviewer's falsifier is now a passing spec.)*
- [x] Disabled mode emits exactly one startup line (`Action logging disabled; transaction archive available on demand.`), never a per-call warning; the parity topology spec pins the marker.
- [x] Writes still land where `GapInferenceEngine` / `DreamService` read when enabled — the plane-anchored path default is untouched. `GapInferenceEngine` additionally **degrades cleanly when the table is absent**, witnessed by the pre-existing `DreamService.spec.mjs` test "executeNLActionDigest skips cleanly when nl_action_log is absent", green at this head.
- [ ] `genesisProbe.mjs` still produces **non-empty per-tool telemetry end to end**. `[L4-deferred — operator handoff needed]` Run the probe end to end post-merge. Shipped now: the probe's rendered child env carries the explicit opt-in, asserted on the **invoked** `createProbeEnvironments()` output, plus a structural tripwire on the source.
- [x] #16167 §2's census pattern matches `neural-link` — verified **live** 2026-07-31T03:35Z on the cutover host: the widened runbook command returned five NL server processes (non-empty positive control; receipt in PR #16209 Test Evidence). A structural tripwire pins the alternation.
- [x] Playwright isolation untouched: suites keep writing to the test destination; the child-process spec explicitly clears the harness markers to reach the production leaf, documenting the isolation chain.
- [x] The steady-state host-NL / container-MC sharing expectation is documented (NL config JSDoc + runbook §2 census note), with the route-through-MC alternative delegated to #15173.

## Out of Scope

- Routing NL writes through Memory Core's remote API instead of direct SQLite. That is the architectural fix and belongs to #15173's streamable-HTTP direction.
- Containerizing Neural Link. It possesses a host browser; host-edge is correct.
- `genesisProbe.mjs` and Playwright isolation — already correct, and this ticket must not disturb them.
- The Chroma version stopgap and the compose layering (#16206), both separate.

## Avoided Traps

- **Reverting the plane-anchored default.** It was the repair for recorders writing where consumers never read, which made gap inference silently produce zero edges. Un-converging it recreates a silent-empty-output bug — strictly worse than a known shared writer.
- **Treating "disable by blanking the path" as the interface.** It works, but it is indistinguishable from a misconfiguration and cannot express per-seat intent.
- **Fixing the census pattern without a positive control.** The original failure *was* a pattern that matched nothing while writers existed; asserting non-empty against a live NL is the only check that catches a recurrence.
- **Calling this cutover-transient.** Host-edge NL plus containerized MC is the steady state; it does not resolve when the cutover completes.
- **A source-text test for the gate.** Asserting the flag appears in the file proves nothing about whether the handle is open. The AC requires invocation.
- **Gating the service instead of the capability** *(added in review cycle 1)*. A gate named for one telemetry table must not sit above initialization of a multi-contract persistence service — that silently retires the archive/replay product contract along with the telemetry. Zero observed rows measures usage, not whether a shipped contract may be retired.

## Decision Record impact

`aligned-with ADR 0019` (the new leaf is a declared AiConfig leaf with an env binding, not a re-derived env read). No ADR amended.

## Related

- #16167 — the cutover that surfaced it; owns the §2 census pattern this widens
- #15173 — Genesis ↔ Neural Link streamable-HTTP PoC; owns the route-through-MC alternative
- #14829 — the transaction archive/replay contract the gate must not touch (via PR #14836)
- #16206 — `ai/deploy` compose layering, including host assumptions in shipped files
- #16202 — orchestrator lanes scheduled without regard to the authority profile; same "configuration says one thing, runtime does another" family

Origin Session ID: 0a7f5f1d-cf12-4698-984c-17b64eea5178

Retrieval Hint: `neural link RecorderService nl_action_log canonical graph write handle no opt-out census pattern misses neural-link host-edge container MC dual writer memoryCoreDbPath plane anchored transaction archive lazy ensureStore`


## Timeline

- 2026-07-31T01:26:03Z @neo-opus-vega added the `bug` label
- 2026-07-31T01:26:04Z @neo-opus-vega added the `ai` label
- 2026-07-31T01:27:28Z @neo-opus-vega cross-referenced by #15291
- 2026-07-31T01:32:06Z @neo-opus-vega cross-referenced by #16208
### @neo-opus-vega - 2026-07-31T01:36:06Z

## Prescription change — default OFF for seats, not "add an opt-out"

Operator recommendation (2026-07-31): disable the NL writer, on the grounds that the data is not meaningfully consumed and that Playwright writing too would have produced vast redundancy. I measured both claims before changing this ticket's prescription, and both hold — with one constraint that makes a *blanket* disable wrong.

### The data is written and not consumed

| Measure | Value |
|---|---|
| `nl_action_log` rows in the canonical graph | **188** (~2 days) |
| `nl_transaction_archive` rows | **0** |
| `edges` of type `NL_ACTION_SEQUENCE` | **0** |

`ai/mcp/server/memory-core/configBase.mjs:726` states that gap inference "sequences and emits weak `NL_ACTION_SEQUENCE -> VALIDATES`" edges, and `GapInferenceEngine.mjs:431-455` does read the table. But the NL-attributed edge type has produced **zero** edges. So the writer runs on every seat, holds a write handle on the shared plane, and yields nothing downstream.

Scope note on that measurement: the graph also holds 179 `VALIDATES` edges. I did **not** verify their provenance, so I am not claiming they are NL-derived or that they are not. The load-bearing number is `NL_ACTION_SEQUENCE = 0` — the edge type the config explicitly attributes to this data.

### The Playwright point is right, and the isolation is what saved us

Playwright never wrote to the canonical graph: `test/playwright/configTemplateResolver.mjs:140` sets `NEO_TELEMETRY_DB_PATH_TEST`, selected by `memoryCoreDbUseTestDatabase` / `memoryCoreDbUseTestHarness` at `configBase.mjs:144`. That isolation is the only reason this table holds 188 rows rather than millions — a single whitebox-e2e suite issues far more Neural Link tool calls than two days of real agent work. Worth stating explicitly so nobody later "simplifies" that indirection: it is not redundant plumbing, it is the thing preventing test telemetry from burying real telemetry in a shared production artifact.

### The constraint: a blanket disable breaks the Genesis probe

`ai/scripts/diagnostics/genesisProbe.mjs:944-953` reads `nl_action_log` directly:

```sql
SELECT tool, COUNT(*) AS count,
       SUM(CASE WHEN success = 1 THEN 1 ELSE 0 END) AS successCount,
       COALESCE(SUM(duration_ms), 0) AS durationMs
FROM nl_action_log GROUP BY tool
```

That query **is** the probe's raw Neural Link telemetry — the artifact compared against its independent component-tree oracle in the blind commitment/reveal run for #15291 / Discussion #15173. Removing the writer, or defaulting it off with no way back on, leaves the probe with nothing to compare and silently guts a merged, externally-committed proof.

The probe already isolates its destination (`NEO_MEMORY_DB_PATH` under its own `mkdtemp('neo-genesis-probe-')` root, line 259), so it needs the writer *enabled within its own root* — which is exactly the shape the fix must permit.

### Revised fix

1. **Default the recorder OFF** for ordinary seats. Not an opt-out bolted onto an on-by-default writer — off is the correct default, since the consumer produces nothing and the cost is a write handle on the shared plane from every host seat.
2. **Explicit enable**, set by `genesisProbe.mjs` alongside the `NEO_MEMORY_DB_PATH` override it already performs.
3. Keep the plane-anchored *path* default unchanged. The #12420-era convergence (recorders writing where `GapInferenceEngine` / `DreamService` read) stays correct — we are changing whether it writes, not where.
4. Widen #16167's §2 census pattern to match `neural-link` regardless, since a disabled-by-default writer that any seat can enable still needs to be visible to the census.

### Revised acceptance criteria

- [ ] With no configuration, an NL server holds **no write handle** on the canonical graph — asserted by invoking the server and inspecting open handles, not by grepping for a flag.
- [ ] With the enable set, writes land in the configured path and `nl_action_log` is created and populated.
- [ ] **`genesisProbe.mjs` still produces non-empty per-tool telemetry** — asserted by running the probe end to end, not by asserting the flag is present in its env block. This is the AC that protects the external commitment, and a source-text check would pass while the probe returned an empty oracle.
- [ ] Playwright isolation is untouched: suites continue writing to `NEO_TELEMETRY_DB_PATH_TEST` and never to the canonical graph.
- [ ] `GapInferenceEngine` degrades cleanly when the table is absent — it already probes `sqlite_master` for it at line 431, so this should hold; assert it rather than assume it.
- [ ] #16167 §2's census matches `neural-link`, verified with a live NL asserted **non-empty** (positive control — the original bug was a census reading empty while writers held handles).

Supersedes the original **The Fix** items 1–2 and their ACs; items 3–4 of the original stand.

— Vega (@neo-opus-vega) · Anthropic Claude Opus 5


- 2026-07-31T01:37:31Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-07-31T01:48:04Z @neo-opus-vega cross-referenced by PR #16209
- 2026-07-31T02:00:29Z @neo-opus-vega referenced in commit `a9af795` - "fix(ai): keep the parity sink marker positive on the disabled path (#16207)

Defaulting action logging off removed the log line the parity topology suite uses
as its positive witness. That marker is a deliberate anti-vacuous guard: the suite
asserts the Neural Link logger initializes without sink degradation in mc-server
and orchestrator, and its two negative assertions would both pass on an empty log
stream. Deleting the positive arm would have made a green test meaningless.

The risk it guards still exists after the gate. RecorderService imports the
Neural Link logger at module scope into processes where the Neural Link never
runs, so a misconfigured sink still degrades there silently.

So the disabled path now emits one boot line through that same logger, and the
suite accepts either arm -- connected when logging is on, disabled when it is not.
The guard stays intact without pinning parity to a non-default setting, and the
assertion keeps testing what it was written to test: that RecorderService reported
through the Neural Link logger at all."
- 2026-07-31T02:28:26Z @neo-opus-vega cross-referenced by #16210
- 2026-07-31T03:40:31Z @neo-opus-vega referenced in commit `c0ba51a` - "fix(ai): gate only nl_action_log, keep the transaction archive on demand (#16207)"
- 2026-07-31T04:23:38Z @neo-opus-vega referenced in commit `0ed9ed0` - "fix(ai): single-flight the lazy store opener (#16207)"

