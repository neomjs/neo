---
id: 17353
title: 'SortZone divides a truthy flex string, so widget sizes become NaN'
state: CLOSED
labels:
  - bug
  - ai
  - core
assignees:
  - neo-opus-vega
createdAt: '2026-08-18T13:20:50Z'
updatedAt: '2026-08-24T13:42:34Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17353'
author: neo-opus-vega
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-24T13:42:34Z'
---
# SortZone divides a truthy flex string, so widget sizes become NaN

## Context

Found while verifying @neo-opus-grace's record correction on #17327 (`issuecomment-5328617338`), which established that #9457 wrote a correct `item.flex` truthiness test and #9621 inverted it 164 tickets later by giving `grid.header.Button` a `flex: 'none'` default. Her sequence is correct — verified independently on all three legs.

A tree-wide census of `.flex` readers found a second site with the same shape, still live, doing arithmetic instead of branching.

> **⚠️ This body was rewritten after AC-1 refuted its first framing.** The original filing claimed a framework-internal reachability chain through dock edge rails. **That chain is false** and the correction is recorded below under *Reachability: what was wrong*. The defect is real; its trigger is app-supplied, not framework-internal. Anyone who read the first version or the `AGENT:*` broadcast of 2026-08-18T13:21Z should re-read the reachability section.

## The Problem

`Neo.draggable.dashboard.SortZone` distributes space across sortable items by summing flex values and dividing:

- `src/draggable/dashboard/SortZone.mjs:185-186` — `if (item.flex) { totalFlex += item.flex }`
- `src/draggable/dashboard/SortZone.mjs:203-204` — `if (item.flex) { itemSize = (item.flex / totalFlex) * availableSpace }`

Both guards are truthiness tests. `flex: 'none'` is a **truthy string**, so with one such item:

```
totalFlex = 0 + 'none'                            // => '0none'  (string concatenation)
itemSize  = ('none' / '0none') * availableSpace    // => NaN
```

`itemSize` is written into `style.width` / `style.height`. The failure is silent — no throw, no warning, just NaN geometry.

## Reachability: what was wrong, and what is actually true

**The refuted claim (mine, in the original body and in an `AGENT:*` broadcast).** I claimed `Neo.dashboard.DockLayoutAdapter` reaches this loop, because it gives edge rails (`:521`) and edge bands (`:703`) `flex: 'none'`, and `SortZone.mjs:465` filters only destroyed items while `:177` skips only `dragPlaceholder`/`dragComponent`.

**Why it is false.** Two distinct drag subsystems share the word *dashboard*, and I conflated them:

- `src/dashboard/DockTabSortZone.mjs:46` — `extends TabHeaderSortZone`, i.e. `Neo.draggable.tab.header.toolbar.SortZone`. **Not this file.** The dock projection's sort path never enters the loop above.
- Rails and bands are not direct items of anything that owns this SortZone either: `DockLayoutAdapter:626-676` pushes them into `middleItems` / `rows`, which become the `items` of returned plain `ntype: 'container'` configs (`edge-zone`, `edge-zone-row`). They are nested children, not siblings of sortable widgets.

**What is true.** `src/dashboard/Container.mjs:146` (`loadSortZoneModule()`) is the **sole importer** of `draggable/dashboard/SortZone.mjs`, verified by tree-wide grep across `src/`, `apps/` and `examples/`. So the loop is reached only through `Neo.dashboard.Container`'s widget sorting (`dragResortable: true`, `:59-62`), whose items are **supplied by the application**, not by the framework.

**So the trigger is a public-API hazard, not a framework path**, and that is a weaker but still real claim:

- `Neo.dashboard.Container` is a public class accepting arbitrary component configs as sortable items.
- `flex: 'none'` is the **dominant** `flex` value in this repository — most of `src/calendar/`, `src/dashboard/`, `src/tab/`, `src/dialog/`, `src/grid/`. An app author copying the surrounding idiom into a dashboard widget writes the exact value that breaks it.
- Nothing warns them: `flex` is declared as a config in only two files (see *the generator* below), so its value domain is documented nowhere.

**Honest status:** the arithmetic defect is confirmed by construction. A *framework* caller that triggers it does **not** exist. An *application* caller that triggers it is permitted by the public API and has not been observed in this repo — `apps/workstation` carries `flex: 'none'` values, but whether any sits on a `dashboard.Container` sortable item is unverified.

## The Architectural Reality

`:177` — `item === me.dragPlaceholder || item === me.dragComponent` — is why SortZone's *own* `flex: 'none'` placeholder (`:576`) is harmless. It excludes by **object identity**, so it defends against the one instance its author knew about and cannot defend against a value. The value domain is the defect; identity is not.

**The correct idiom already exists in-tree**, one directory over: `src/dashboard/DockSplitter.mjs:221` and `:388` use `Number(item.flex) || 1`. `Number('none')` is `NaN`, which is falsy, so the fallback takes over. Established prior art, not an invention.

**The census, which is the durable value here** — why no review of the original diffs could have caught either instance:

| Class | Sites | With `flex: 'none'` |
|---|---|---|
| Forwarding a value | `layout/Flexbox.mjs:152,238`, `layout/Form.mjs:78`, `draggable/DragZone.mjs:343` | Correct — `'none'` is a legal CSS value to pass through |
| Coercing then testing | `dashboard/DockSplitter.mjs:221,388` | Correct — `Number('none')` → `NaN` → falsy |
| Testing truthiness as a predicate | `grid/header/Toolbar.mjs:359`, `table/Body.mjs:322`, `draggable/dashboard/SortZone.mjs:185,203` | The vulnerable class |

Of the three predicate sites: one was broken (fixed by PR #17332), one is clean because nothing under `src/table/` defaults `flex`, one is clean only for the placeholder. **Every reason is non-local** — none is visible at the site that depends on it.

**The generator, recorded but not fixed here.** `flex` is declared as a config in exactly two files — `src/grid/header/Button.mjs:64` and `src/grid/header/Wrapper.mjs:42`, both defaulting `'none'`. The other ~90 sites pass it ad-hoc at construction, and `src/component/Base.mjs` declares nothing. No declaration site means no documented value domain: nothing tells an author that `'none'` is legal *and* reads truthy. @neo-opus-grace's formulation is the one to keep — the fact needed to see the bug was **unlocatable, not unforeseeable**.

## The Fix

Coerce before predicating, matching `DockSplitter`'s idiom, in `src/draggable/dashboard/SortZone.mjs`:

- `:185-186` — resolve the item's flex once via `Number(item.flex)`; add to `totalFlex` only when finite and positive, else fall to the measured-rect branch.
- `:203-204` — reuse that same resolved value so the two guards cannot disagree.

The two guards must derive from one resolution. Fixing only `:185` leaves `:203` free to re-admit the string and divide by a `totalFlex` that never counted it.

## Acceptance Criteria

- [ ] **AC-1 — CLOSED, and it refuted the original framing.** Probe run: `DockTabSortZone` extends the tab-header SortZone, not this one; `dashboard/Container.mjs:146` is the sole importer; rails/bands nest inside plain `edge-zone` containers. **No framework caller reaches the defect.** Recorded above; no further work owed on this AC.
- [ ] **AC-2:** a red-first unit spec fails on current `dev` for a sortable item whose `flex` is `'none'`, asserting the produced size is **finite**. A spec asserting only "does not throw" does not satisfy this — the defect produces a value, so the value is what must be asserted.
- [ ] **AC-3:** both `:185` and `:203` read one resolved flex value.
- [ ] **AC-4:** positive control — a fixture where every item carries numeric flex distributes identically before and after, so the fix is not silently disabling the flex path.
- [ ] **AC-5:** the JSDoc of the changed method states that `flex` is app-supplied here and that `'none'` is a legal truthy value, so the next reader learns the non-local dependency at the site.

## Out of Scope

- **Declaring `flex` on `src/component/Base.mjs`.** This is the generator and deserves its own ticket, but its blast radius across ~90 ad-hoc call sites is unmeasured and a config declaration changes reactive-config resolution order. Not bundled into a defect fix.
- `src/table/Body.mjs:322` — same predicate shape, clean today; no `flex:` default exists under `src/table/` (verified). Touching it would be a fix without a defect.
- `src/grid/header/Toolbar.mjs:359` — fixed by PR #17332. The `layoutFinished` JSDoc line that PR retired without naming is a named carry owned by @neo-opus-grace on her next grid touch.
- The dock projection's sort path (`DockTabSortZone` → `draggable/tab/header/toolbar/SortZone`). It does not share this loop. Whether *that* SortZone has the same predicate shape is a separate question this ticket does not answer.

## Avoided Traps

- **Keeping the original reachability framing because the fix is unchanged.** The fix would have been identical either way, which is exactly what makes a false premise easy to leave standing. A ticket whose stated trigger does not exist teaches the next reader a wrong map of the subsystem.
- **Fixing all three predicate sites at once.** Two of the three are correct today; rewriting a correct site to look safer produces a diff whose test cannot fail.
- **Guarding by identity, the way `:177` does.** Adding item types to an exclusion list fixes one instance and leaves the next `flex: 'none'` item to rediscover it.
- **Asserting the fix with a no-throw test.** `NaN` propagates silently into style strings; a no-throw spec passes against the broken code.

## Decision Record impact

`none` — a defect fix adopting an idiom already established in the same subsystem.

## Related

- #17327 — @neo-opus-grace's corrected #9457 → #9621 → #12883 → #17291 → #17332 sequence, and her addendum carrying this census.
- PR #17332 — fixed the `grid/header/Toolbar.mjs` predicate site.
- `#9621` — introduced the `flex: 'none'` class default that makes the predicate class hazardous.

## Handoff Retrieval Hints

- Retrieval Hint: `"flex none truthy string predicate versus coerce"`
- Retrieval Hint: `"SortZone totalFlex string concatenation NaN itemSize"`
- The idiom to copy: `src/dashboard/DockSplitter.mjs:221`.
- ⚠️ Two subsystems share the name *dashboard*: `Neo.dashboard.Container` (widget sorting, uses `draggable/dashboard/SortZone`) and the Dock projection (`DockLayoutAdapter` / `DockTabSortZone`, uses `draggable/tab/header/toolbar/SortZone`). Conflating them is what produced this ticket's first, false reachability chain.
- Live latest-open sweep: checked latest 20 open issues at 2026-08-18T13:2xZ; keyword sweeps for `SortZone flex`, `flex none truthy`, `dashboard drag NaN`, `flex config declaration`; no equivalent found. (First three sweeps initially returned empty from an invalid `--state all` flag on `gh search issues` — instrument failure, not absence; re-run with valid flags.)

Origin Session ID: 9ccc2fa1-8843-4796-8e85-5e151c0392d2

— Vega (Claude Opus 5, Claude Code) 🌿


## Timeline

- 2026-08-18T13:20:50Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-18T13:20:51Z @neo-opus-vega added the `bug` label
- 2026-08-18T13:20:52Z @neo-opus-vega added the `ai` label
- 2026-08-18T13:20:52Z @neo-opus-vega added the `core` label
### @neo-opus-vega - 2026-08-18T13:22:13Z

## Narrowing AC-1 to a single probe

Static follow-up while filing, recorded so AC-1 is one call rather than a hunt.

The body's reachability chain has exactly **one** inferred link, and I have now located it. `src/dashboard/DockLayoutAdapter.mjs:772-791` assembles a **split node's** items — split children (`flex: flexValues[index]`, numeric) plus splitter affordances. That array is *not* obviously the same collection the rail config from `:515-521` lands in; the rail is returned as a config carrying `dockNodeType: 'edge-rail'` and `railItems`, which may live in a separate dock-edge structure rather than as a sibling of sortable items.

Everything else in the chain is read, not inferred:

- `src/dashboard/Container.mjs:146` loads this SortZone — verified
- `SortZone.mjs:465` filters only `isDestroyed` — verified
- `SortZone.mjs:177` skips only `dragPlaceholder` / `dragComponent` — verified
- `DockLayoutAdapter.mjs:521` / `:703` emit configs carrying `flex: 'none'` — verified
- the string arithmetic itself — verified by construction

So **AC-1 reduces to:** during a dock drag-sort, does `me.owner.items[mappedIndex]` ever resolve to an edge rail or edge band? If no, this closes as unreachable and the census in the body is the whole value of the ticket. If yes, the fix is `DockSplitter`'s existing `Number(item.flex) || 1`.

Stating it this way because the honest version of the body's premise is weaker than "confirmed by code path" reads — the phrase covers a chain with one unread link, and naming which link is the difference between a gate and a formality.

— Vega (Claude Opus 5, Claude Code) 🌿

- 2026-08-18T13:30:22Z @neo-opus-vega changed title from **Drag-sorting a dock rail divides a string, so item sizes become NaN** to **SortZone divides a truthy flex string, so widget sizes become NaN**
### @neo-opus-vega - 2026-08-18T13:31:40Z

## The open question in *Out of Scope* is now closed — negatively, which narrows this ticket further

The rewritten body left one question open: whether the dock's actual sort path (`draggable/tab/header/toolbar/SortZone`, which `DockTabSortZone` extends) carries the same predicate shape. If it did, edge rails and bands *would* reach a flex predicate after all, just in a different file.

It does not. Neither that class nor the shared base `src/draggable/container/SortZone.mjs` contains the identifier `flex` at all.

So `src/draggable/dashboard/SortZone.mjs` is the **only** SortZone in the tree that does flex arithmetic — it added proportional distribution that its siblings never had. That makes the defect narrower than the original filing claimed and more isolated than the rewrite claimed: one method, in one subclass, on a path fed exclusively by application-supplied widget configs.

Two consequences for whoever implements:

- **AC-4's positive control matters more than it looks.** Since no sibling SortZone distributes by flex, there is no in-family reference behaviour to compare against. The numeric-flex fixture is the only thing standing between this fix and silently disabling a distribution path nothing else implements.
- **The `Number(item.flex) || 1` idiom is still the right one**, but it is borrowed from `dashboard/DockSplitter.mjs:221` — a different subsystem — rather than from a sibling. Worth a JSDoc note saying so, or the next reader will look for the house pattern in the wrong family, which is the error that produced this ticket's first framing.

— Vega (Claude Opus 5, Claude Code) 🌿

- 2026-08-24T10:00:00Z @neo-opus-vega cross-referenced by PR #17695
- 2026-08-24T10:25:05Z @neo-opus-vega referenced in commit `91a52e4` - "fix(draggable): the flex resolver reads the shorthand's grow factor, not its first number (#17353)

Review RA from @neo-gpt-emmy, and it holds. A leading-number scan makes
CSS-equivalent spellings disagree: `auto` and `1 1 auto` are the same
declaration and resolved to null and 1. The same scan reads `100px` — a basis,
whose grow defaults to 1 — as weight 100, and invents weight 1 out of `1oops`.

Her prescription named two options, full-value Number or a real grammar. Number
was measured against the tree first and rejected: `'0 1 auto'`, `'1 1 auto'`,
`'1 1 600px'` and `'1 1 100%'` appear across src, apps and examples, and Number
resolves every one of them to not-flexible. That is a behaviour change for live
call sites, so the grammar is the option that fixes the RA without introducing a
second defect.

The grammar expands the keywords as CSS defines them — none is 0 0 auto,
initial is 0 1 auto, auto is 1 1 auto — takes grow from the first of up to three
tokens, treats a lone length or percentage as a basis whose grow is 1, and
resolves everything else to not-flexible. Unparseable never invents a weight.

This reverses my own earlier position that `auto` should be fixed rather than
"guessed at 1". The argument was that no caller uses it, which missed that its
equivalent spelling is used widely, so the pair diverging was the real defect.

Three arms fail against the previous resolver, one of them reporting 100 where
CSS says 1 — finite, plausible, and a hundredfold wrong."
- 2026-08-24T10:41:50Z @neo-opus-vega referenced in commit `ab1c3e4` - "fix(draggable): a bad token anywhere invalidates the declaration, not just a bad first one (#17353)

Round-2 RA from @neo-gpt-emmy. The grammar validated the grow position and
trusted the rest, so `1 oops`, `1 -1 auto`, `1 2 3` and `1 1 none` all resolved
to weight 1. A browser drops the entire declaration on one invalid token, which
makes every one of those items not flexible at all. Reading grow off token 0 and
trusting positions 1 and 2 was the same invent-a-weight defect this method exists
to remove, one position further along.

Grow and shrink now go through a shared non-negative-factor read, and the basis
position is validated against the length set — which also gains `auto`,
`content` and unitless zero, all of which are legal bases the first regex
rejected. The valid neighbours are asserted beside the invalid ones so the
validation cannot tighten into a rule that refuses real CSS.

Also retracts a claim of my own. I argued the grammar over strict Number on the
strength of a tree-wide shorthand census presented as caller reachability. It is
not: the sole entry to this loop is the dashboard container's item list, and
`container/Panel.mjs:77` — my leading example — sits on a panel HEADER child,
not on a dashboard item. Emmy caught it with an independent path sweep. The
grammar is still the right branch, because flex is app-supplied and a shorthand
arriving tomorrow should grow rather than silently freeze, but that is a
different argument than the one I made and a weaker one."
- 2026-08-24T13:42:34Z @tobiu referenced in commit `01dc586` - "fix(draggable): a truthy flex string no longer divides into NaN geometry (#17695)

* fix(draggable): a truthy flex string no longer divides into NaN geometry (#17353)

The expanded-layout distribution identified flex items with a truthiness test and
then did arithmetic on the raw config. `flex` is app-supplied on this path — the
sole caller is the dashboard container's widget sorting — so `'none'` arrives,
passes the truthiness test, concatenates into `'0none'`, and the division writes
`NaNpx` into style.width. Nothing throws.

Finiteness was never the whole property. A numeric string mixed with a number
concatenates the same way without producing NaN: two items carrying 2 and '3'
distributed 17.4px and 26.1px of a 200px span, silently, because the total came
out as the string '23'. Any assertion that only checked for NaN would pass that,
so the specs assert the shares and their sum.

One resolver now yields the weight both sites read. A value that cannot be a
positive finite weight falls to the fixed branch, which is also what CSS means
by `flex: none` — `0 0 auto` keeps the measured rect. `'auto'` is deliberately
fixed rather than guessed at 1: no caller uses it, and a measured rect is a safe
wrong answer where an invented weight is not.

The positive control is the only arm that survives reverting the fix: purely
numeric flex distributes identically before and after, so the hazard is removed
without disabling the path.

* fix(draggable): the flex resolver reads the shorthand's grow factor, not its first number (#17353)

Review RA from @neo-gpt-emmy, and it holds. A leading-number scan makes
CSS-equivalent spellings disagree: `auto` and `1 1 auto` are the same
declaration and resolved to null and 1. The same scan reads `100px` — a basis,
whose grow defaults to 1 — as weight 100, and invents weight 1 out of `1oops`.

Her prescription named two options, full-value Number or a real grammar. Number
was measured against the tree first and rejected: `'0 1 auto'`, `'1 1 auto'`,
`'1 1 600px'` and `'1 1 100%'` appear across src, apps and examples, and Number
resolves every one of them to not-flexible. That is a behaviour change for live
call sites, so the grammar is the option that fixes the RA without introducing a
second defect.

The grammar expands the keywords as CSS defines them — none is 0 0 auto,
initial is 0 1 auto, auto is 1 1 auto — takes grow from the first of up to three
tokens, treats a lone length or percentage as a basis whose grow is 1, and
resolves everything else to not-flexible. Unparseable never invents a weight.

This reverses my own earlier position that `auto` should be fixed rather than
"guessed at 1". The argument was that no caller uses it, which missed that its
equivalent spelling is used widely, so the pair diverging was the real defect.

Three arms fail against the previous resolver, one of them reporting 100 where
CSS says 1 — finite, plausible, and a hundredfold wrong.

* fix(draggable): a bad token anywhere invalidates the declaration, not just a bad first one (#17353)

Round-2 RA from @neo-gpt-emmy. The grammar validated the grow position and
trusted the rest, so `1 oops`, `1 -1 auto`, `1 2 3` and `1 1 none` all resolved
to weight 1. A browser drops the entire declaration on one invalid token, which
makes every one of those items not flexible at all. Reading grow off token 0 and
trusting positions 1 and 2 was the same invent-a-weight defect this method exists
to remove, one position further along.

Grow and shrink now go through a shared non-negative-factor read, and the basis
position is validated against the length set — which also gains `auto`,
`content` and unitless zero, all of which are legal bases the first regex
rejected. The valid neighbours are asserted beside the invalid ones so the
validation cannot tighten into a rule that refuses real CSS.

Also retracts a claim of my own. I argued the grammar over strict Number on the
strength of a tree-wide shorthand census presented as caller reachability. It is
not: the sole entry to this loop is the dashboard container's item list, and
`container/Panel.mjs:77` — my leading example — sits on a panel HEADER child,
not on a dashboard item. Emmy caught it with an independent path sweep. The
grammar is still the right branch, because flex is app-supplied and a shorthand
arriving tomorrow should grow rather than silently freeze, but that is a
different argument than the one I made and a weaker one."
- 2026-08-24T13:42:34Z @tobiu closed this issue

