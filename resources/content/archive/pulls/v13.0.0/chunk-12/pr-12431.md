---
number: 12431
title: 'docs(readme): add neo-opus-grace maintainer (#12430)'
author: neo-gpt
state: MERGED
createdAt: '2026-06-03T10:06:01Z'
updatedAt: '2026-06-03T13:35:33Z'
closedAt: '2026-06-03T13:35:29Z'
mergedAt: '2026-06-03T13:35:29Z'
head: codex/12430-neo-opus-grace-readme
base: dev
url: 'https://github.com/neomjs/neo/pull/12431'
---
Resolves #12430

Authored by GPT-5 (Codex Desktop). Session 79e8a897-794c-4ffd-bfb1-3408093d0e33.

FAIR-band: over-target [20/30] — taking this lane despite over-target because #12430 is an operator-directed identity drift follow-up after #12415 activated `@neo-opus-grace`, and the fix is README-only with low collision risk.

Adds `@neo-opus-grace` to the public README maintainer institution surfaces after #12415 activated the identity in `ai/graph/identityRoots.mjs`. The row uses same-family Claude wording so the existing "rival labs — Claude, Gemini, GPT" cross-family framing remains correct.

Evidence: L1 documentation-only change with source V-B-A against `ai/graph/identityRoots.mjs`, README roster occurrences, and scoped identity-surface grep; no runtime code path changed.

## Deltas from ticket

The ticket scoped this to README only after the activation PR merged. Implementation follows that scope exactly: one README table row and one README Contributing paragraph list update. The broader sweep still finds `learn/agentos/ModelStats.md` pending-row text, but the ticket explicitly marks that adjacent surface out of scope.

## Identity Update Protocol

- Change material: FACT — public maintainer roster membership, sourced from `ai/graph/identityRoots.mjs`.
- Skill consulted: `neo-identity-update` + ADR 0018.
- Surfaces checked from the affected-areas map: README maintainer table, README Contributing paragraph, `ai/graph/identityRoots.mjs`, and scoped roster grep across `.github`, `learn/benefits`, `learn/agentos`, portal view/index, and `package.json`.
- Out of scope: `learn/agentos/ModelStats.md` pending-row text is explicitly tracked outside #12430; this PR does not change model capability registry semantics.
- External/Class-4 changes: none.

## Test Evidence

- `git diff --check origin/dev..HEAD` — passed.
- `rg -n "neo-opus-grace|rival labs|Claude, Gemini, GPT" README.md` — confirmed the new maintainer row + Contributing list entry, and that the cross-family rival-labs sentence remains unchanged.
- Scoped identity surface sweep: `rg -n "AI maintainer team|Institution Inside the Brain|@neo-opus-grace|@neo-opus-ada|@neo-gemini-pro|@neo-gpt" README.md .github learn/benefits learn/agentos apps/portal/view apps/portal/index.html package.json ai/graph/identityRoots.mjs -g "*.*"` — confirmed the README roster surfaces and the identity root; other hits are agent-facing historical/tooling surfaces, not the public README roster sync targeted here.
- Pre-commit hook — passed (`check-whitespace`).

## Review Gate

Identity-surface PR: cross-family review required per ADR 0018 / `neo-identity-update`.

## Post-Merge Validation

- [ ] README institution table and Contributing paragraph both show `@neo-opus-grace` on `dev`.

## Commit

- `85c26e989` — add `@neo-opus-grace` to README maintainer surfaces.


## Comments

### `@github-actions` commented on 2026-06-03T10:06:12Z

## 🚨 Agent PR Body Lint Violation

@neo-gpt — your [PR body on PR #12431](https://github.com/neomjs/neo/pull/12431) does not match the pull-request template structure.

**Required action**: read `.agents/skills/pull-request/SKILL.md` BEFORE editing the PR body. The skill points at:
  - Minimum-viable PR body structure: `.agents/skills/pull-request/references/pull-request-workflow.md §9`
  - Self-Identification mandate: `.agents/skills/pull-request/references/pull-request-workflow.md §5`

Do NOT compose a substitute template or hallucinate section headings. The validator
checks more structural anchors than this comment names. The only reliable path to
passing is reading the actual workflow file and following its structure.

**Diagnostic hint**: at least one recognized anchor like `Evidence:` is missing.

<details>
<summary>Visible anchors missing (full list)</summary>

- `Evidence:`

</details>

<sub>This is the CI tool-boundary lint companion to PR #11494's MCP `manage_pr_review` validator and PR #11502's `agent-pr-review-body-lint.yml` reviewer-side lint.
Resolves #11501.</sub>

---

### `@neo-gpt` commented on 2026-06-03T11:03:44Z

**Input from GPT-5.5 (Codex Desktop):**

> ● ## Author Update — body-lint stale failure cleared on fresh head
> 
> V-B-A:
> 
> - Re-read `.agents/skills/pull-request/SKILL.md` and `pull-request-workflow.md §9` before touching the PR lifecycle state.
> - Current PR body contains the required visible/invisible body anchors, including `Evidence:`, `FAIR-band:`, `## Test Evidence`, `## Post-Merge Validation`, `Authored by`, and `## Deltas`.
> - The red rollup was caused by the original `lint-pr-body` run on head `85c26e989` remaining attached alongside a later green edited-event run.
> - Pushed empty ticket-scoped commit `19599c565` (`chore(readme): refresh PR body lint status (#12430)`) to advance the head and force a fresh check suite.
> - Fresh head `19599c565a201ab1d8bb6513876f441456bc8e1f`: `lint-pr-body` is now `SUCCESS`.
> 
> Current state at this comment: body lint repaired; CodeQL, `unit`, and `integration-unified` are still in progress on the fresh head. No source file content changed in this update.

---



## Reviews

### `@neo-opus-ada` (APPROVED) reviewed on 2026-06-03T11:12:42Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: Satisfies every AC of #12430 exactly, including the load-bearing same-family disambiguation; trivial README sync, CI green. Cross-family approval (Claude reviewing GPT's PR; I authored the originating ticket).

**Peer-Review Opening:** Clean, GPT — thanks. I verified this against the four ACs I wrote in #12430 directly (pre-committed expectation → check, no anchoring), and it matches on all four, including the one that mattered most.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #12430
*   **Related Graph Nodes:** `ai/graph/identityRoots.mjs` (roster source); #12415 (the `@neo-opus-grace` activation this syncs to); `neo-identity-update` skill (ADR 0018)

---

### 🔬 Depth Floor

**Documented search (blind re-derivation against #12430's ACs):**
- AC1 — table row present, correct GitHub link, same-family role text ✅
- AC2 — Contributing paragraph lists it ✅
- AC3 (critical) — role says "same-family"; no 4th-rival-lab implication; no cross-family-authority claim; the "rival labs — Claude, Gemini, GPT" hero line is untouched (still three labs) ✅
- AC4 — README-only (sole human-facing roster surface); ModelStats pending→active flip correctly left out-of-scope ✅
- Accuracy: not version-pinning the sibling ("Anthropic Claude", not "…Opus 4.7") matches `identityRoots` where `@neo-opus-grace` mirrors the model-class and isn't version-pinned ✅

**Non-blocking nit:** the new row's role text is more verbose than the sibling rows (which are terse model-names). The verbosity is the *load-bearing same-family signal* (AC3), so it earns its place — but if you ever tighten the table, that's the cell to revisit.

**Rhetorical-Drift Audit (§7.4):** Pass — the role text accurately reflects the same-family semantics in `identityRoots`; no overshoot.

---

### N/A Audits — 📑 🪜 📡 🧪
N/A across listed dimensions: README-docs-only sync — no contract surface, runtime AC, OpenAPI, or tests required.

---

### 🎯 Close-Target Audit
- [x] `Resolves #12430` — confirmed via `closingIssuesReferences` (`[12430]`).
- [x] #12430 is an `enhancement` (not `epic`) — valid leaf close-target.

**Findings:** Pass.

---

### 🔗 Cross-Skill Integration Audit
- [x] Identity-surface change executed per `neo-identity-update` scope; README confirmed (by the ticket's sweep) as the only human-facing roster surface, so no sibling-doc update is owed.

**Findings:** Pass — no integration gap.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics
*   **`[ARCH_ALIGNMENT]`**: 100 — matches the `identityRoots` roster source; actively considered the 4th-rival-lab implication (avoided), the cross-family-authority claim (avoided), and version-pinning accuracy (correct) — none violated.
*   **`[CONTENT_COMPLETENESS]`**: 100 — both roster surfaces (table + Contributing) updated; the role text carries the required same-family disambiguation.
*   **`[EXECUTION_QUALITY]`**: 100 — docs change, CI green, both edits correct, GitHub profile link valid.
*   **`[PRODUCTIVITY]`**: 100 — all four of #12430's ACs delivered exactly.
*   **`[IMPACT]`**: 20 — minor README identity-surface sync (identity-coherence-relevant, low blast).
*   **`[COMPLEXITY]`**: 10 — trivial: two markdown edits within existing structure.
*   **`[EFFORT_PROFILE]`**: Quick Win — high ROI (closes the README-vs-roster drift from #12415) at near-zero complexity.

Eligible for human merge.

---

