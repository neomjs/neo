---
number: 17110
title: 'docs(learn): FM guide D1 — the wake daemon lives host-side (#17104)'
author: neo-fable-clio
state: OPEN
createdAt: '2026-08-14T12:03:02Z'
updatedAt: '2026-08-14T12:03:02Z'
closedAt: null
mergedAt: null
head: agent/17104-d1-wake-daemon-homing
base: dev
url: 'https://github.com/neomjs/neo/pull/17110'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17104

The guide's D1 no longer homes the wake daemon inside the plane. The wake corner now draws the two-actor truth the operator flagged and the ticket source-anchored: the **plane's signed dispatcher** (`WebhookDeliveryService`, in the Memory Core container) POSTs class-6-signed Shape-B digests at seat receivers, while the **host-side wake daemon** — the launchd mini-orchestrator pair on client machines — owns Shape-C harness injection, with the reason stated where it teaches: `osascript` prompt delivery can only execute at the host, never from inside a container. The class-6 arrow re-anchored to its true producer (MC → seat receivers); a new labeled Shape-C edge carries the host daemon's actual job; the caption gains the two-actor paragraph.

The same edit delivers the cosmetic half: every D1 label shortened (the densest were three-line), so the diagram renders tighter at the same information content — all six ledger classes remain placed at their boundaries.

D5 stays untouched, deliberately: its "Wake daemon (delivery attempt)" is an abstract delivery actor homed in NO subgraph, so it asserts no false home — the falsehood lived only in D1's placement. (The ticket body's rationale clause for this was imprecise and is corrected in place.)

Evidence: L2 (docs-only; all six diagrams re-rendered clean under repository `mermaid@11.16.0` via the playwright harness; `ai:lint-tree-json` OK, 223 nodes; content verified against the source anchors in #17104 — `CoalescingEngineService.mjs:680/:739`, `Server.mjs:300`, the daemon's Shape-C adapters) → L2 sufficient (no runtime surface).

## Deltas from ticket

None substantive — the layout polish landed as label-shortening within D1 rather than edge regrouping; edge count rose by one (the Shape-C edge is the taught content) while total label mass shrank.

## Test Evidence

- Playwright render harness (repo `mermaid@11.16.0`): all 6 diagrams parse and render clean at head.
- `npm run ai:lint-tree-json` → OK (223 nodes).
- learn surface: docs-only — no unit suite applies.

## Post-Merge Validation

- [x] Diagram render verified pre-merge (harness receipt above); KB re-ingestion of the updated guide rides the standing pipeline and the #17098 verification lane.

## Commits (if multi-commit)

Single commit.

Review note: docs-only with no runtime impact — the §6.1 pure-documentation exception applies; stated here for the record rather than claiming a seat from the review board mid-release-focus.

Related: `#16798` (the guide) · PR #16936 (the carrier) · #17100 / PR #17103 (where the two-actor split is source-anchored in shipping code).

Authored by Clio (Fable 5, Claude Code). Session c4996813-01b9-4234-8bdd-ed3bf22c0970.
