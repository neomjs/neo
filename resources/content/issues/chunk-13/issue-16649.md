---
id: 16649
title: 'The SDK barrel still resolves better-sqlite3 at eager singleton boot, because an initAsync dynamic import is not demand-lazy'
state: OPEN
labels:
  - bug
  - ai
assignees: []
createdAt: '2026-08-07T18:46:16Z'
updatedAt: '2026-08-11T02:41:14Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16649'
author: neo-opus-ada
commentsCount: 3
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# The SDK barrel still resolves better-sqlite3 at eager singleton boot, because an initAsync dynamic import is not demand-lazy

## Context

> ## ⚠ PREMISE CORRECTION — 2026-08-08, by the author (@neo-opus-ada)
>
> **`PR #16641` was CLOSED, never merged** (`merged=NEVER`, closed `2026-08-08T03:14:09Z`). The struck sentence below is retained because it is the intuitive reading and the next reader will reach for it too.
>
> **Neither the chromadb work nor its witness is on `dev`.** `bodyTierBarrelRuntime.spec.mjs` was added in `b1ff51434a` on branch `ada/16488-defer-chromadb-import`; `git merge-base --is-ancestor b1ff51434a origin/dev` returns **false** and the path is **absent** at `origin/dev`. Any AC here that assumes that witness exists must build it, not extend it.
>
> **How this survived a correction sweep, recorded because it is the reusable part:** I broadcast a `[premise correction]` at `16:05Z` today and reported #16649 as corrected. **It was not.** What I had actually corrected was the *description of* this ticket's error inside `#16710` — never this body. A recalled list of "tickets I fixed" is not a sweep; only a mechanical query over every artifact citing the dead ref is. Found on the re-sweep from `#16644`. Sibling instances: `#16644`, and `#16710`'s Related line, both corrected the same hour.

~~`#16488` / PR `#16641` made `chromadb` genuinely demand-driven so `ai/services.mjs` could be imported in the Body install tier. That work landed a runtime witness — `test/playwright/unit/ai/services/bodyTierBarrelRuntime.spec.mjs` — which spawns a process with Brain-only packages denied by a resolve hook.~~ **[CORRECTED — see above. `#16641` closed unmerged; the witness does not exist at `origin/dev`.]**

That witness records a **KNOWN STATE** it deliberately did not fix: with the full Brain-only population denied, the barrel still fails, on `better-sqlite3`. `#16488` bounded its claim to `chromadb` rather than overclaiming Body-tier importability. **This ticket owns the remaining package.**

## The Problem

**`ai/graph/storage/SQLite.mjs:37-49` resolves `better-sqlite3` inside `initAsync()`:**

```js
async initAsync() {
    await super.initAsync();
    ...
    // Use dynamic imports to prevent native Node module evaluation crashes inside browser/test runtimes
    const Database = (await import('better-sqlite3')).default;
```

**A dynamic import inside `initAsync()` is not demand-lazy for an eager singleton.** `Neo.setupClass()` instantiates the singleton at module load and `core.Base` schedules `initAsync()` on the very next microtask, so the import runs on barrel import. In the Body tier the rejection is unhandled and terminates the process. The comment above the import states the deferral intent, and the intent is not achieved — the failure changed *phase*, not *ownership*.

This is the identical defect `@neo-gpt` falsified on PR `#16641` cycle 1, in a different module. It is worth stating plainly because the wrong version is the intuitive one: moving a `require`/static import into `initAsync()` *looks* like deferral and is not.

### Measured, not inferred

Probe: resolve hook denying only `better-sqlite3`, import target, wait through singleton init.

| target | result |
|---|---|
| `ai/services.mjs` | **resolves `better-sqlite3`** |
| `ai/graph/storage/SQLite.mjs` (alone) | **resolves `better-sqlite3`** |
| `ai/services/memory-core/SourceRegistryService.mjs` | resolves |
| `ai/services/memory-core/MemoryCoreRecorderService.mjs` | resolves |
| `ai/services/knowledge-base/KBRecorderService.mjs` | resolves |
| `ai/services/neural-link/RecorderService.mjs` | resolves |
| `ai/services/memory-core/CommunityBatchAdmissionService.mjs` | resolves |

**Boundary of this measurement, stated because it changes the fix.** Each of those five services *also* contains its own `await import('better-sqlite3')`, but most sit inside a `try/catch` that logs a warning and degrades (e.g. `SourceRegistryService.mjs:117`, whose catch emits *"Failed to initialize SQLite connection"*). A caught denial is not a fatal one. I have **not** isolated which reach is fatal versus which are absorbed — the shared `ai/graph/storage/SQLite.mjs` resolving on its own import makes it the prime candidate, and the per-service imports may be entirely survivable. **Whoever takes this should measure that before deciding the blast radius**, rather than assuming all seven sites need changing.

There is **no static `better-sqlite3` import anywhere under `ai/services/`** — every site is already `await import(...)`. This is exclusively a *when-does-it-run* defect, not an import-shape one.

## The Fix

The shape is established by `#16488` and should be reused rather than re-invented: resolution moves to **first actual use**, memoized, behind an `ensure…Ready()`-style entry point that `initAsync()` never calls.

The relevant precedent, including the traps, is in `ai/services/knowledge-base/ChromaManager.mjs` and `ai/services/memory-core/managers/ChromaManager.mjs`:

- `initAsync()` must touch no Brain-only package at all.
- The ensure must **return early if the handle already exists**, or documented mock seams break.
- Readiness that no longer implies a handle must be swept for **every** reader — production *and* test. On `#16488` two escaped a census that only enumerated production readers.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Error Semantics | Evidence |
|---|---|---|---|---|
| `ai/graph/storage/SQLite.mjs` `initAsync()` | this ticket | Touches no Brain-only package | unchanged lifecycle otherwise | barrel survives full denial |
| `better-sqlite3` resolution | this ticket | Resolved on first actual DB use, memoized | Absent package fails at that call, named | runtime denial witness |
| `ai/services.mjs` export surface | existing | **unchanged** | n/a | no export diff |
| `bodyTierBarrelRuntime.spec.mjs` KNOWN STATE test | `#16488` | **Flips** to asserting survival | n/a | the test is the AC |

## Acceptance Criteria

- [ ] With the **full** Brain-only package population denied, importing `ai/services.mjs` and waiting through singleton init survives — the class assertion, not a per-module one.
- [ ] The existing KNOWN STATE test in `bodyTierBarrelRuntime.spec.mjs` is **rewritten to assert survival — not deleted.** Its comment already prescribes this. Deleting it would remove the only guard for the property it was written to bound.
- [ ] The positive control still fails: a module that genuinely needs the package still reaches the denial, so a passing barrel probe cannot be a loader that stopped denying.
- [ ] The fatal reach is **isolated by measurement** before the fix is scoped — shared storage layer versus per-service imports — and the finding is recorded, including which sites were found to absorb the denial harmlessly.
- [ ] Every reader of the SQLite handle is enumerated across **production and test** trees, since deferral changes when the handle exists.
- [ ] A Brain-tier path that actually uses SQLite still works with the package present. This defers resolution; it must not break the feature.
- [ ] **Inherited witness polish (carried, not forgotten).** `@neo-gpt` raised this as non-blocking on PR `#16641` after approving it: `chromaEnsureConcurrency.spec.mjs` asserts the held public collection entry is *not* the escape-rejection, where it should assert the promise is **exactly pending before release and resolved after**. It was deliberately not pushed there — a new commit would have moved the head off a cross-family approval for a non-blocking item. It lands here because this ticket already edits the sibling spec in the same directory, so it costs one file-touch instead of a review cycle.

## Out of Scope

- **`chromadb`.** Handled by `#16488` / PR `#16641`.
- **`#16523`** (36 modules opening their own sqlite handle). Adjacent and genuinely related, but that is about *handle ownership*; this is about *when the package resolves*. Fixing this one does not close that one, and a shared-primitive refactor there could make this trivial — worth checking before starting.
- **Splitting `ai/services.mjs`.** The barrel stays the single SDK entry point; its export surface does not change.

## Avoided Traps

- **Assuming `initAsync()` is deferral.** It is not, for an eager singleton. This is the whole defect.
- **Trusting a static import walk.** It reports clean here and the runtime property is false — that exact combination is why `#16488`'s original guard passed against broken code.
- **Deleting the KNOWN STATE test when it turns green.** It must flip to asserting survival; it is the only witness for this property.
- **Assuming all seven reach sites are fatal.** Several are inside `try/catch` blocks that degrade. Measure before scoping.
- **Full-install CI as evidence.** It cannot reproduce this — CI has the Brain tier installed.

## Related

`#16488` / PR `#16641` — the `chromadb` half, and the source of the witness and the fix pattern · `#16523` — sqlite handle ownership, adjacent · `#16364` / `#16389` — the two-tier install split

Filed by Ada (`@neo-opus-ada`), unassigned — the measurement above is the hard part and it is already done.

Retrieval Hint: `query_raw_memories("better-sqlite3 eager singleton boot barrel body tier initAsync not demand lazy")`




## Timeline

- 2026-08-07T18:46:17Z @neo-opus-ada added the `bug` label
- 2026-08-07T18:46:17Z @neo-opus-ada added the `ai` label
- 2026-08-07T18:48:31Z @neo-opus-ada cross-referenced by PR #16641
### @neo-opus-ada - 2026-08-08T03:14:56Z

## Reframed — do NOT take this as a lazy-import repair

This ticket prescribes making `better-sqlite3` demand-driven, mirroring the fix in PR #16641. **That PR is closed** — terminal Drop+Supersede — and its approach is superseded. Anyone picking this up on the body as written would rebuild the thing we just discarded.

**Why the prescription is wrong, not just superseded.** Lazy resolution is an unbounded series: `chromadb` was fixed, this ticket appeared before that fix merged, and `ai/services.mjs` statically requires `fs-extra` as well. Each package is a separate PR against a structure that keeps producing them.

**The root cause** is in D[#16652](https://github.com/orgs/neomjs/discussions/16652): the barrel predates the dockerization split and unifies services that cannot execute in one process. Measured since filing:

```
ai/scripts entrypoints: 139   store-touching 56 · ambiguous 11 · host-safe 72
every activity-shaped directory except `lint` is MIXED — lifecycle splits 8/12
```

So this ticket's real content is **evidence for the split**, not a work item on its own terms.

**What stays valid here and should not be lost:**

- The measurement — `ai/graph/storage/SQLite.mjs:37-49` resolves `better-sqlite3` inside `initAsync()`, which is **not demand-lazy for an eager singleton**. That finding stands and is the same design error @neo-gpt caught on #16641 cycle 1.
- The boundary I did not close: several of the five reaching services wrap their import in `try/catch` that degrades. **Which reach is fatal was never isolated.**
- The inherited witness-polish note from @neo-gpt.

**Disposition:** left OPEN and unassigned, reframed as a symptom. If the split lands, this closes without its own repair. If the split is rejected, this ticket becomes live again **and its prescription needs rewriting first** — not implementing as written.

**Do not delete the `KNOWN STATE` test** in `bodyTierBarrelRuntime.spec.mjs` on the assumption this ticket is dead; that spec died with #16641 and its property is now owned by the successor's host-entrypoint guard.

— Ada (`@neo-opus-ada`)


### @neo-opus-ada - 2026-08-08T16:04:44Z

**This ticket's premise is falsified, by me, and I am the author.** Correcting rather than letting the next person inherit it.

The body opens: *"`#16488` / PR `#16641` made `chromadb` genuinely demand-driven so `ai/services.mjs` could be imported in the Body install tier. That work landed a runtime witness — `test/playwright/unit/ai/services/bodyTierBarrelRuntime.spec.mjs`."*

**None of that landed.** PR #16641 was **closed, not merged** — I accepted a Drop+Supersede on it this morning when @tobiu falsified its framing. The work went with it. Measured at `origin/dev` just now:

```
ai/services/knowledge-base/ChromaManager.mjs:1
  import {ChromaClient} from 'chromadb';        <- static, eager, top-level

ensureChromaReady                                <- does not exist at origin/dev
bodyTierBarrelRuntime.spec.mjs                   <- absent at origin/dev

git merge-base --is-ancestor b1ff51434a origin/dev   -> false
git branch -a --contains b1ff51434a                  -> ada/16488-defer-chromadb-import only
```

`#16488` is still **OPEN**, which is the consistent state — it was never delivered.

## What that changes

1. **`better-sqlite3` is not "the remaining package."** It is one of at least two. The body's framing — *"`#16488` bounded its claim to `chromadb`... This ticket owns the remaining package"* — assumed a predecessor that does not exist.
2. **The runtime witness this ticket leans on has to be built, not extended.** There is no deny-hook spec on `dev`.
3. **The enumeration has not started.** `chromadb` then `better-sqlite3` was never a sequence-in-progress; it was a plan on an abandoned branch.

## Disposition

I am **not** closing this. The defect it names is real and independently verified: `ai/graph/storage/SQLite.mjs` resolves `better-sqlite3` inside `initAsync()`, and a dynamic import inside `initAsync()` is not demand-lazy for an eager singleton — `Neo.setupClass()` constructs at module load and `core.Base` schedules `initAsync()` on the next microtask. That reasoning stands on its own and does not depend on the false predecessor.

But its **scope** should now be read through **#16710**, the barrel split graduated from D#16652 today. #16710's AC4 already anticipates this: *"`#16649` closes as a consequence, or is explicitly carried with its own falsifier if the split leaves it standing."* With the chromadb work gone too, fixing packages one at a time is even less defensible than when I filed this — a third package appearing is evidence the split did not happen, which is exactly what #16710 exists to terminate.

I have corrected #16710's Context, AC1, and Contract Ledger in place: they carried the same false citation, because I composed them from this ticket's prose instead of resolving the path.

**How I caught it:** at the first implementation step on #16710 I went to read the witness and `find` returned nothing. I hold a rule about exactly this — *an ID-shaped citation is the costume of evidence; resolve, never compose* — and I shipped an unresolved path into a graduated ticket anyway. Caught at implementation rather than at review, which is the right place, but it was still mine to catch at filing.

Authored by Ada (Claude Opus 5, Claude Code). Session 9b08b9e4-6181-416b-ac68-e9d16636cff0.


- 2026-08-08T16:08:17Z @neo-opus-ada cross-referenced by #16710
- 2026-08-08T20:22:04Z @neo-gpt-emmy cross-referenced by PR #16728
### @neo-opus-ada - 2026-08-11T02:41:14Z

## #16710 shipped — discharging its AC4. **The split leaves this standing, and here is the falsifier it asked for.**

AC4 of #16710: *"`#16649` closes as a consequence, or is explicitly carried with its own falsifier if the split leaves it standing."* #16710 is now CLOSED, so this is decidable. Measured against `dev` just now, in fresh node processes.

### The measurement

The CJS require-cache is the witness: a dynamic `import()` of a CJS package populates it, so presence means the native module was **actually loaded**, not merely named in source. Import the barrel, drain microtasks plus 400ms of real async time, then read the cache.

| barrel imported | `better-sqlite3` resolved |
|---|---|
| `ai/services.host.mjs` | **no** |
| `ai/services.mjs` | **YES** |

**The split did its job.** A host entrypoint no longer constructs the store — that is exactly what #16710 was for, and it holds. This defect is now *scoped to the SDK barrel* rather than being a property of the whole surface, which is a materially different and much smaller ticket than the one I filed.

### The chain, statically traced

```
ai/services.mjs
  -> ai/services/memory-core/GraphService.mjs
  -> ai/graph/storage/SQLite.mjs
```

Three static edges. `SQLite.mjs` ends in `Neo.setupClass()`, which constructs at module load; `core.Base` then schedules `initAsync()` on the next microtask; and `initAsync()` does `await import('better-sqlite3')` (`SQLite.mjs:49`). So the dynamic import is real but reaches nothing — **an eager singleton makes a dynamic import inside `initAsync()` a deferred load, not a demand-driven one.** That was this ticket's original reasoning and it survives intact; only its scope changed.

### Reproduce

```js
import {createRequire} from 'node:module';
const require = createRequire(process.cwd() + '/'),
      target  = require.resolve('better-sqlite3');
await import('<absolute file:// URL of the barrel>');
await new Promise(r => setTimeout(r, 400));
console.log(Boolean(require.cache[target]));   // host barrel: false · ai/services.mjs: true
```

Note the two traps, both of which cost me a run: a relative specifier passed to `import()` resolves against the *probe script*, not `cwd`; and importing `SQLite.mjs` directly throws `ReferenceError: Neo is not defined`, so the barrel is the only honest entry point for this measurement.

### Disposition — carried, re-scoped, still mine

Not closing. The fix is **not** "make the import lazier" — the import is already dynamic and that is precisely why it does not help. It is that `ai/services.mjs` statically pulls `GraphService`, so the SDK barrel constructs a graph store to re-export it. The candidate shapes are a lazy accessor for the graph surface, or moving it behind the same boundary `services.host.mjs` established. That is design work with a real blast radius, not a line edit, and I am not going to start it half-way at the end of a long session and hand over a partial refactor.

Body's stale framing stands corrected by my earlier comment; this adds the post-split scope and the runnable witness so whoever picks it up starts from measurement rather than from my original prose.

⚖️ Ada


