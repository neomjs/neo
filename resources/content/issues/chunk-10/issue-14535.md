---
id: 14535
title: 'Fleet registryBridge: setRepo verb + registry partial-update (FM Lane C)'
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-07-03T06:24:55Z'
updatedAt: '2026-07-03T07:17:03Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14535'
author: neo-opus-ada
commentsCount: 2
parentIssue: 13015
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 13448 Epic: the harness-UI definition — the v13.1 fleet cockpit (keeper views · nav · human/agent caps)'
---
# Fleet registryBridge: setRepo verb + registry partial-update (FM Lane C)

## Context

The operator reframed the Fleet Manager cockpit (2026-07-02, relayed via @neo-opus-grace): *"the team can **control** it and it **enables the team** — session-sunset → restart your harness."* So the cockpit (#13448, Grace's Lane B) is a **control surface**, not observe-only. This leaf delivers the per-agent **repo** control verb — `setRepo` — plus the registry partial-update primitive it needs.

> **Scope refined during implementation (2026-07-03):** this ticket originally paired `setRepo` with `setWakeEnabled`. Deep V-B-A showed the two split by AUTHORITY: `setRepo` is **fleet authority** (own-registry mutation, like `defineAgent`) and ships here; `setWakeEnabled` is **control-plane authorized** (toggling another agent's wake is a privilege boundary via `WakeSubscriptionService`'s context-owner scoping) and moved to **#14537** (`blocked_by` #14477). Ticket re-scoped to `setRepo`.

## The Problem

`FLEET_WIRE_METHODS` (`src/ai/fleet/fleetWireMethods.mjs`) exposed `defineAgent · listAgents · getAgent · startAgent · stopAgent · restartAgent · removeAgent · fleetStatus` — no way for the cockpit to set an agent's working repo. And the registry had `defineAgent` (full create-or-replace upsert) but **no narrow partial-update**, so one facet couldn't be mutated without re-supplying the whole definition.

## The Architectural Reality

- `ai/services/fleet/FleetManager.mjs` is the Brain-side thin facade; each verb composes a collaborator.
- `startAgentProvisioned.mjs:54` **already honors** `agent.metadata.repo = {cloneUrl, repoSlug}` — it clone-or-reuses that repo and pins the harness `cwd` to the checkout. So setting `metadata.repo` is functional end-to-end; no new provisioning consumer is needed.
- The app↔fleet wire (`dispatchFleetRequest.mjs:30`) forwards a **single** `params` argument, so a pane-reachable verb must take ONE payload object — mirroring `defineAgent`.
- `FleetControlBridge` is the pane-reachable capability allowlist; `createFleetRegistryBridge` generates the client bridge from `FLEET_WIRE_METHODS`, and `dispatchFleetRequest` is the server-side choke-point enforcing the same list.

## The Fix

- `FleetRegistryService.updateAgent(id, patch)` — partial-merge (merges `metadata`, preserves other fields + `createdAt` + credential; `null` on unknown id). Non-destructive.
- `FleetManager.setRepo({id, cloneUrl, repoSlug})` — single-payload, wire-compatible; delegates to `updateAgent` setting `metadata.repo = {cloneUrl, repoSlug}`. Non-destructive (mirrors `removeAgent`).
- `FleetControlBridge.setRepo(payload)` + `FLEET_WIRE_METHODS += setRepo` — pane-reachable, both wire ends + the dispatch choke-point updated.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `FLEET_WIRE_METHODS` += `setRepo` | operator control-plane reframe; the app↔fleet wire SSOT | pane-reachable `setRepo` on both wire ends (`createFleetRegistryBridge` gen + `dispatchFleetRequest` enforce) | none — off-list rejected | `fleetWireMethods` JSDoc | `dispatchFleetRequest.spec` allowlist + routing tests |
| `FleetControlBridge.setRepo(payload)` | `FleetControlBridge` capability allowlist | single-`params` payload → `FleetManager.setRepo` | none — fail-closed | JSDoc | `FleetControlBridge.spec` |
| `FleetManager.setRepo({id, cloneUrl, repoSlug})` | `FleetManager` facade; `startAgentProvisioned` `metadata.repo` contract | sets `metadata.repo = {cloneUrl, repoSlug}` via `updateAgent`; provisioner honors it | none — fail-closed; `null` on unknown id | JSDoc | `FleetManager.spec` |
| `FleetRegistryService.updateAgent(id, patch)` | new registry primitive | partial-merge `metadata` + `modelProvider`, preserve rest; `null` on unknown | none | JSDoc | `FleetRegistryService.spec` |

## Decision Record impact

`aligned-with ADR-0026` — a fleet-authority operator-UI control verb on the client bridge, the OTHER side of the control-plane/diagnostics R3 seam from the #14477/#14501 daemon-core actuator. No ADR authority change.

## Acceptance Criteria

- [ ] `FleetRegistryService.updateAgent(id, patch)` partial-merges `metadata`, preserves other fields, returns `null` on unknown id — unit spec.
- [ ] `FleetManager.setRepo({id, cloneUrl, repoSlug})` is **single-payload** (wire-compatible), sets `metadata.repo = {cloneUrl, repoSlug}`, omits unset coords — unit spec.
- [ ] `setRepo` added to `FLEET_WIRE_METHODS`; `dispatchFleetRequest` allowlist + routing tests confirm the choke-point routes it to the injected bridge without opening resolver seams.
- [ ] `FleetControlBridge.setRepo(payload)` exposed on the capability allowlist; the secret-omission boundary still holds.
- [ ] No re-resolution of `managedRoot` / re-wiring of singletons in the new methods (facade discipline).

## Out of Scope

- **`setWakeEnabled`** — moved to #14537 (`blocked_by` #14477; control-plane authorized).
- **`setHookEnabled`** — deferred (Grace's #14439/#14481 stop-hook revert per #14501 OQ2).
- **The #14477/#14501 daemon-core restart actuator** — separate `control-plane/` off-bridge surface.
- **The cockpit UI** — #13448 (Lane B) consumes `registryBridge.setRepo`.
- **`data-dir` per-agent override** — the checkout path is `managedRoot`-derived (not a per-agent `metadata.repo` field); out of scope for this verb.

## Related

#13015 (parent — FM MVP / Lane C) · #13448 (Lane B cockpit — `blocked_by` this) · #14537 (`setWakeEnabled` split-out) · #14477 / #14501 (control-plane authority) · PR #14536 · `ai/services/fleet/FleetManager.mjs` · `startAgentProvisioned.mjs`

Origin Session ID: 29018efb-2edf-4172-902d-0fe42d4b0c11
Retrieval Hint: "Fleet setRepo single payload metadata.repo updateAgent FLEET_WIRE_METHODS wire-compatible"


## Timeline

- 2026-07-03T06:24:56Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-03T06:24:57Z @neo-opus-ada added the `enhancement` label
- 2026-07-03T06:24:58Z @neo-opus-ada added the `ai` label
- 2026-07-03T06:24:58Z @neo-opus-ada added the `architecture` label
- 2026-07-03T06:25:22Z @neo-opus-ada added parent issue #13015
- 2026-07-03T06:25:24Z @neo-opus-ada marked this issue as blocking #13448
### @neo-opus-ada - 2026-07-03T06:30:07Z

## Implementation-scoping V-B-A (author, pre-code) — both verbs need TARGET-owner scoping; not thin wraps

Grounded the two verbs against the live services before implementing, and the "thin facade delegate" framing in the body is optimistic — both underlying surfaces are **self/owner-scoped**, while an FM control verb must act on a **target** agent:

- **`setWakeEnabled`:** `WakeSubscriptionService.manage` (`ai/services/memory-core/WakeSubscriptionService.mjs:296`) dispatches `subscribe`/`unsubscribe`/`update`/`list`/`resync`, but every path is **bound-identity (`owner`) scoped** — `bootstrap`/`subscribe` derive `owner` from the calling AgentIdentity/RequestContext and key the `WAKE_SUBSCRIPTION` node to it (`:346`, `:377`). There is no `targetOwner` parameter. So a naive `setWakeEnabled` delegate would toggle *the FM's own* wake, not the target agent's. **Correct shape:** add a target-owner-parameterized path (an explicit-`owner` variant of `subscribe`/`unsubscribe`, or a Node-side FM helper that updates the target's `WAKE_SUBSCRIPTION` status) FIRST, then wrap it. This is the same class as the `setHookEnabled` "hook has no target-identity awareness" finding.
- **`setRepo`:** already flagged — the `removeAgent` auto-memory-orphan coupling (`FleetManager.mjs:182`) means a live repo/data-dir change needs the Memory-Core reconciliation, not a bare registry write.

**Refined ACs:** each verb's AC now includes the target-owner path as a prerequisite (AC-0: `WakeSubscriptionService` gains an explicit-`owner` subscribe/unsubscribe seam OR an FM-side targeted-subscription helper, unit-proven, before `setWakeEnabled` wraps it). This keeps the facade discipline (FleetManager stays thin) while putting the target-scoping where it belongs (the service, not the facade).

Sequencing note: this pairs with Grace's Lane B (#13448) — the exact verb signatures should confirm against the cockpit's needs before the target-owner service seam hardens. Origin Session ID: 29018efb-2edf-4172-902d-0fe42d4b0c11.

### @neo-opus-ada - 2026-07-03T06:37:11Z

## Architectural gate discovered (author, deeper V-B-A) — `setWakeEnabled` is CONTROL-PLANE-authorized; #14535 depends on #14477/#14501

Going a layer deeper than the prior comment: the "target-owner seam" for `setWakeEnabled` is not a benign param — it's a **privilege boundary**.

`WakeSubscriptionService.subscribe` (`:840`) and `unsubscribe` (`:921`) derive `owner`/`caller` from `RequestContextService.getAgentIdentityNodeId()`. That context-owner scoping **IS the security model**: an agent may manage only its OWN wake subscription. Adding a naive explicit-`owner` param would be a **privilege escalation** — any caller could toggle any agent's wake.

So an FM control verb toggling a **target** agent's wake is inherently a **control-plane-authorized** operation (the FM/operator acting on other agents with elevated authority). That is exactly the authority the #14501 substrate defines and #14477 owns (the ADR-0026 L0 control-capable principal). **`setWakeEnabled` therefore depends on the #14477/#14501 control-plane authority landing first** — it cannot be safely implemented as a fleet-local owner-param.

**Verb split this implies:**
- `setWakeEnabled` → **control-plane-authorized** (targets protected per-agent memory-core state) → blocked_by #14477.
- `setRepo` → **fleet-authority** (the FM legitimately owns the agent-definition registry) + the memory-core auto-memory reconciliation. Implementable in the fleet domain sooner, but still needs the reconciliation coordination — a candidate to split out if #14477 lags.

**Disposition:** linking #14535 `blocked_by` #14477. Correct sequence: the #14501 graduation → #14477 control-plane authority → then `setWakeEnabled` (+ `setRepo` can proceed on fleet authority in parallel with the reconciliation coordinated with @neo-opus-grace). This is the right architectural coupling — the FM cockpit's cross-agent control verbs ARE the client-facing consumers of the #14477 control-plane authority, exactly the #14501 R3 model.

- 2026-07-03T06:37:13Z @neo-opus-ada marked this issue as being blocked by #14477
- 2026-07-03T06:52:23Z @neo-opus-ada changed title from **Fleet registryBridge control verbs: setWakeEnabled + setRepo (FM Lane C)** to **Fleet registryBridge: setRepo verb + registry partial-update (FM Lane C)**
- 2026-07-03T06:53:19Z @neo-opus-ada cross-referenced by PR #14536
- 2026-07-03T06:54:26Z @neo-opus-ada cross-referenced by #14537
- 2026-07-03T06:54:53Z @neo-opus-ada removed the block by #14477
- 2026-07-03T07:03:29Z @neo-opus-ada referenced in commit `8bccd15` - "fix(fleet): setRepo writes metadata.repo {cloneUrl, repoSlug} the provisioner honors (#14535)"
- 2026-07-03T07:07:58Z @neo-opus-ada referenced in commit `ae3d86a` - "test(fleet): add setRepo to the dispatchFleetRequest wire-allowlist assertion (#14535)"
- 2026-07-03T07:16:04Z @neo-opus-ada referenced in commit `fb45cca` - "fix(fleet): setRepo takes a single {id,...} payload for wire-compatibility + dispatch routing test (#14535)"

