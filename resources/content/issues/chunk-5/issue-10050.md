---
id: 10050
title: 'Concept Description Enrichment: Source-Grounded Semantic Anchors'
state: CLOSED
labels:
  - documentation
  - enhancement
  - ai
  - architecture
  - needs-re-triage
assignees: []
createdAt: '2026-04-17T13:44:35Z'
updatedAt: '2026-06-03T05:51:48Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10050'
author: tobiu
commentsCount: 3
parentIssue: 10030
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking:
  - '[x] 10049 Concept Schema Enrichment: Aliases, Analogous Edges & Rich Descriptions'
closedAt: '2026-06-03T05:51:48Z'
---
# Concept Description Enrichment: Source-Grounded Semantic Anchors

## Objective
Rewrite all concept node descriptions in `.neo-ai-data/concepts/nodes.jsonl` to be source-grounded, framework-accurate semantic anchors — 3-5 sentences per concept derived from actual source code and `learn/` guides, not from training data approximations.

## Context
The current 1-2 sentence descriptions were seeded during #10031 and contain approximations that don't accurately reflect Neo.mjs architecture. Example failure:

> ❌ "Unlike React Context (which triggers full subtree re-renders on change), Neo.mjs State Providers only re-render components that bind to a specific state key."

This is wrong. Neo.mjs does not "re-render" components. State Providers use `bind: {}` declarations on reactive configs. When a provider data key changes, it triggers `afterSet` hooks on bound config properties, which surgically update specific VDOM nodes. The delta engine then calculates minimal DOM mutations.

These descriptions will become the input for ChromaDB vector embeddings (#10037). Inaccurate descriptions produce misleading vectors. Each concept must be grounded in the actual implementation.

## Methodology
For each concept node:
1. Read the source implementation via `IMPLEMENTED_BY` edges
2. Read the corresponding guide via `EXPLAINED_BY` edges
3. Query the knowledge base for architectural context
4. Write a 3-5 sentence description covering:
   - What the concept does (grounded in source)
   - How it works mechanically (the Neo.mjs way)
   - What makes it architecturally distinct (without false React/Vue/Angular equivalences)

## Scope
- All **tier-1** concepts (mandatory — these are the architectural pillars)
- All **tier-2** concepts (mandatory — these are the implementation patterns)
- **Tier-3** concepts (stretch — only if time permits)

## Acceptance Criteria
- [ ] All tier-1 and tier-2 descriptions expanded to 3-5 source-grounded sentences
- [ ] Zero React/Vue/Angular terminology applied as if it maps 1:1 to Neo.mjs behavior
- [ ] Each description validated against actual source code or guide content
- [ ] ConceptService unit tests updated to validate enriched descriptions load correctly

## Architectural Note
This is a content-quality prerequisite for #10049 (Schema Enrichment) and #10037 (ChromaDB Embedding). Description quality directly determines vector quality. This ticket requires a dedicated research session — it cannot be rushed.

Origin Session ID: c9bf0d61-a58d-42ba-a030-6426dc5fe270

## Timeline

- 2026-04-17T13:44:37Z @tobiu added the `documentation` label
- 2026-04-17T13:44:37Z @tobiu added the `enhancement` label
- 2026-04-17T13:44:37Z @tobiu added the `ai` label
- 2026-04-17T13:44:43Z @tobiu added parent issue #10030
- 2026-04-17T13:44:44Z @tobiu marked this issue as blocking #10049
- 2026-04-17T13:46:43Z @tobiu cross-referenced by #10049
### @tobiu - 2026-04-17T14:15:41Z

**Input from Claude Opus 4.6 (Antigravity):**

> ❋ ## ⚠️ Human-in-the-Loop Required: Architectural Decision History
> 
> This ticket requires the framework architect's direct input for each concept description. Agent-generated descriptions based on source code or guides will produce **wrong** results due to missing decision history.
> 
> ### Case Study: Data Worker
> 
> The ArchitectureOverview guide (line 84) states the Data Worker handles *"stores, models, sorting, filtering, and grouping"*. An agent reading this would write:
> 
> > ❌ "The Data Worker moves sorting and filtering operations off the App Worker thread to prevent blocking component rendering."
> 
> **This is the abandoned design.** The actual architectural history:
> 
> 1. **Original hypothesis:** Move stores into the Data Worker — sorting/filtering runs off the App Worker thread.
> 2. **Why it was dropped:** Serialization cost kills the idea. A ComboBox with 10 items: `JSON.stringify` → `postMessage` → `JSON.parse` → sort → reverse the entire chain. The overhead dwarfs the compute savings for reasonable dataset sizes.
> 3. **What the Data Worker actually does:** Backend data mutation — incoming XML → JSON transformation, structural normalization. The heavy work isn't sorting, it's I/O format conversion.
> 4. **This spawned `data-pipelines`:** The Connection → Parser → Normalizer pipeline, deployable in either the App Worker or Data Worker depending on workload.
> 
> This decision history exists **only in the creator's memory** — it cannot be derived from source code, guides, or training data. The ArchitectureOverview guide itself is partially misleading on this point and may need correction.
> 
> ### Implication for This Ticket
> 
> Every tier-1 and tier-2 concept description needs the same treatment: validate against the architect's actual design intent, not against what the code or guides superficially suggest. This ticket should be executed as a collaborative session with `@tobiu` driving the architectural rationale.

- 2026-04-17T14:30:24Z @tobiu cross-referenced by PR #10052
- 2026-04-19T08:33:49Z @tobiu cross-referenced by PR #10078
- 2026-04-19T09:16:31Z @tobiu cross-referenced by #10080
- 2026-04-19T10:25:24Z @tobiu cross-referenced by PR #10084
- 2026-04-19T10:31:35Z @tobiu cross-referenced by #10086
- 2026-04-19T10:31:51Z @tobiu cross-referenced by #10087
- 2026-04-19T10:33:36Z @tobiu cross-referenced by #10030
- 2026-04-19T14:17:12Z @tobiu cross-referenced by PR #10100
- 2026-04-19T14:19:33Z @tobiu cross-referenced by #10085
- 2026-04-19T14:58:36Z @tobiu referenced in commit `b91d523` - "feat(ai): ORPHAN_CONCEPT signal + ⚠️ Orphaned Concepts handoff section (#10087)

Promotes orphan concept detection from the ephemeral per-orphan `logger.warn`
in `ConceptIngestor` to the durable `capabilityGap` tag channel + dedicated
`sandman_handoff.md` section — same pattern as `[GUIDE_GAP]` / `[EXAMPLE_GAP]`
established in #10084. Surfaces ontology data-quality issues where humans and
future agents can act on them rather than scroll past in daemon logs.

## Architecture

- `GapInferenceEngine.inferConceptGraphGaps` now emits a third weight-gated
  signal alongside `[GUIDE_GAP]` / `[EXAMPLE_GAP]`: `[ORPHAN_CONCEPT]` when a
  concept has no outbound `IMPLEMENTED_BY` edge. Orthogonal to the other two
  — a concept can be both orphaned and lack a guide. Gates via the shared
  `GUIDE_GAP_WEIGHT_THRESHOLD` so today's guide-title-derived ingestion (thin,
  mostly low-weight concepts) stays silent; the signal auto-surfaces as richer
  ingestion lands (#10036 / #10037 / #10050) without config changes.
- Action-biased entry text: "Concept X has no IMPLEMENTED_BY edge — either
  anchor it to a source file or retire the concept from nodes.jsonl if
  aspirational/stale."
- `ConceptIngestor.syncConceptsToGraph` deletes the per-orphan `logger.warn`
  (one line per orphan × N cycles = log spam growing linearly with ontology
  size). Retains `stats.orphansDetected` for the cycle-summary info line —
  which is the appropriate place for phase-level observability.
- `GoldenPathSynthesizer` parses the new `[ORPHAN_CONCEPT]` tag and renders
  a `### ⚠️ Orphaned Concepts` section with the same 5-item limit + TTL
  pruning as the other tag channels.

## Tests (39/39 stable × 5 runs)

- `DreamService.spec.mjs`: new test for three orphan-detection cases —
  fully-wired concept (no gaps), EXPLAINED_BY + EXEMPLIFIED_BY but no
  IMPLEMENTED_BY (ORPHAN_CONCEPT only, no GUIDE_GAP / EXAMPLE_GAP), and
  low-weight orphan (weight gate blocks emission).
- `DreamService.spec.mjs`: `synthesizeGoldenPath` test extended — planted a
  CONCEPT node with pre-written `[ORPHAN_CONCEPT]` capabilityGap; asserts
  the final handoff content includes the `⚠️ Orphaned Concepts` section.
- `DreamService.spec.mjs`: GUIDE_GAP covered-concept fixture now includes an
  IMPLEMENTED_BY edge (otherwise the fully-covered control concept would
  correctly emit the new ORPHAN_CONCEPT signal, inverting the test's premise).
- `ConceptIngestor.spec.mjs`: orphan test asserts `stats.orphansDetected`
  remains accurate AND no per-orphan logger.warn fires (the AC3 deletion).

## AC status

- [x] ConceptIngestor attaches orphan signal (now via GapInferenceEngine → capabilityGap) — the architectural home for concept-gap emission
- [x] GoldenPathSynthesizer categorizes `[ORPHAN_CONCEPT]` + renders dedicated section
- [x] logger.warn removed entirely (graph persistence is the durable signal)
- [x] Existing tests still pass — 39/39 stable
- [x] New test: ingesting an orphan concept causes `⚠️ Orphaned Concepts` section to appear in handoff

Origin Session ID: 1db25bbe-f39d-4dcd-bb0e-bc125ce91326"
- 2026-04-19T14:59:29Z @tobiu cross-referenced by PR #10101
- 2026-04-19T15:12:45Z @tobiu referenced in commit `9d10d18` - "feat(ai): ORPHAN_CONCEPT signal + ⚠️ Orphaned Concepts handoff section (#10087) (#10101)

* feat(ai): ORPHAN_CONCEPT signal + ⚠️ Orphaned Concepts handoff section (#10087)

Promotes orphan concept detection from the ephemeral per-orphan `logger.warn`
in `ConceptIngestor` to the durable `capabilityGap` tag channel + dedicated
`sandman_handoff.md` section — same pattern as `[GUIDE_GAP]` / `[EXAMPLE_GAP]`
established in #10084. Surfaces ontology data-quality issues where humans and
future agents can act on them rather than scroll past in daemon logs.

## Architecture

- `GapInferenceEngine.inferConceptGraphGaps` now emits a third weight-gated
  signal alongside `[GUIDE_GAP]` / `[EXAMPLE_GAP]`: `[ORPHAN_CONCEPT]` when a
  concept has no outbound `IMPLEMENTED_BY` edge. Orthogonal to the other two
  — a concept can be both orphaned and lack a guide. Gates via the shared
  `GUIDE_GAP_WEIGHT_THRESHOLD` so today's guide-title-derived ingestion (thin,
  mostly low-weight concepts) stays silent; the signal auto-surfaces as richer
  ingestion lands (#10036 / #10037 / #10050) without config changes.
- Action-biased entry text: "Concept X has no IMPLEMENTED_BY edge — either
  anchor it to a source file or retire the concept from nodes.jsonl if
  aspirational/stale."
- `ConceptIngestor.syncConceptsToGraph` deletes the per-orphan `logger.warn`
  (one line per orphan × N cycles = log spam growing linearly with ontology
  size). Retains `stats.orphansDetected` for the cycle-summary info line —
  which is the appropriate place for phase-level observability.
- `GoldenPathSynthesizer` parses the new `[ORPHAN_CONCEPT]` tag and renders
  a `### ⚠️ Orphaned Concepts` section with the same 5-item limit + TTL
  pruning as the other tag channels.

## Tests (39/39 stable × 5 runs)

- `DreamService.spec.mjs`: new test for three orphan-detection cases —
  fully-wired concept (no gaps), EXPLAINED_BY + EXEMPLIFIED_BY but no
  IMPLEMENTED_BY (ORPHAN_CONCEPT only, no GUIDE_GAP / EXAMPLE_GAP), and
  low-weight orphan (weight gate blocks emission).
- `DreamService.spec.mjs`: `synthesizeGoldenPath` test extended — planted a
  CONCEPT node with pre-written `[ORPHAN_CONCEPT]` capabilityGap; asserts
  the final handoff content includes the `⚠️ Orphaned Concepts` section.
- `DreamService.spec.mjs`: GUIDE_GAP covered-concept fixture now includes an
  IMPLEMENTED_BY edge (otherwise the fully-covered control concept would
  correctly emit the new ORPHAN_CONCEPT signal, inverting the test's premise).
- `ConceptIngestor.spec.mjs`: orphan test asserts `stats.orphansDetected`
  remains accurate AND no per-orphan logger.warn fires (the AC3 deletion).

## AC status

- [x] ConceptIngestor attaches orphan signal (now via GapInferenceEngine → capabilityGap) — the architectural home for concept-gap emission
- [x] GoldenPathSynthesizer categorizes `[ORPHAN_CONCEPT]` + renders dedicated section
- [x] logger.warn removed entirely (graph persistence is the durable signal)
- [x] Existing tests still pass — 39/39 stable
- [x] New test: ingesting an orphan concept causes `⚠️ Orphaned Concepts` section to appear in handoff

Origin Session ID: 1db25bbe-f39d-4dcd-bb0e-bc125ce91326

* test(ai): co-emission spec + symmetric DreamService afterEach (#10087)

Addresses the optional follow-ups from #10101's self-review:

- New test for GUIDE_GAP + ORPHAN_CONCEPT co-emission on a concept lacking
  BOTH EXPLAINED_BY and IMPLEMENTED_BY. Locks in the branch orthogonality —
  the GUIDE_GAP / EXAMPLE_GAP pair is mutually exclusive, but ORPHAN_CONCEPT
  is independent. The three-case test (fully-wired / orphan-only / low-weight)
  didn't hit the compound case directly; this does.

- Symmetric `afterEach` in DreamService.spec.mjs mirroring the existing
  `beforeEach` graph-state cleanup. Applies the `feedback_symmetric_spec_cleanup.md`
  discipline established in #10100: `fullyParallel: true` interleaves specs
  in the same worker, and `GraphService.db` is a shared singleton. Without
  `afterEach`, the last test's fixtures (CONCEPT nodes planted by the
  ORPHAN_CONCEPT detection tests) leak to whatever test Playwright queues
  next from another spec. The existing `afterAll` nullifies `GraphService.db`
  so cross-spec leak is bounded at worker-shutdown, but per-test symmetric
  cleanup closes the door at the test-boundary level — belt-and-suspenders,
  aligned with the discipline I just codified.

40/40 tests pass across 5 consecutive runs.

Origin Session ID: 1db25bbe-f39d-4dcd-bb0e-bc125ce91326"
- 2026-04-19T15:20:04Z @tobiu cross-referenced by PR #10102
- 2026-04-20T11:13:56Z @tobiu cross-referenced by #9748
- 2026-05-01T12:22:35Z @neo-gpt cross-referenced by #10574
- 2026-05-18T01:01:53Z @neo-gpt added the `architecture` label
- 2026-05-18T01:01:54Z @neo-gpt added the `needs-re-triage` label
- 2026-05-18T01:01:54Z @neo-gpt added the `agent-task:blocked` label
### @neo-gpt - 2026-05-18T01:02:07Z

**Input from GPT-5.5 (Codex Desktop):**

> ● ## Ticket Intake Classification — blocked / needs re-triage
> 
> [ARCH_ALIGNMENT] I am not picking this up as an autonomous implementation ticket.
> 
> V-B-A evidence checked live before classification:
> 
> - **Age state:** created 2026-04-17, last updated 2026-04-17; with `days-before-issue-stale: 90` + `days-before-issue-close: 14`, this is still **pre-stale**. It has no `stale` or `no auto close` label.
> - **Current issue thread:** @tobiu's 2026-04-17 comment explicitly says agent-generated concept descriptions from source/guides alone will produce wrong architecture because design history is missing; execution requires a collaborative session with the framework architect.
> - **Parent epic context:** #10030 later handoffs still classify #10050 as blocked on human architect time. My prior #10030 epic-review also preserved that classification and recommended autonomous agents avoid generating descriptions from FAQ/query signals.
> - **Successor sweep:** #10574 added non-destructive `verifiedAt` freshness and explicitly left broad description rewriting out of scope; #11373 added ADR/concept KB source types and did not complete description enrichment.
> - **Code/data reality:** `.neo-ai-data/concepts/nodes.jsonl` still has 59 rows with short descriptions and `verifiedAt: null`, so the ticket is not already resolved.
> 
> **Classification:** `needs-narrowing` / human-gated blocked.
> 
> The underlying goal remains valid, but the ticket needs a re-triaged execution shape before implementation. A safer future shape would be a human-guided concept-curation workflow, probably split into small batches with explicit architect-provided rationale and a Contract Ledger for the concept-description surface. Autonomous source/docs rewriting is negative ROI here because it would create false semantic anchors that downstream embeddings and Dream/Sandman paths would trust.
> 
> Labels applied: `architecture`, `needs-re-triage`, `agent-task:blocked`.
> 
> Origin Session ID: 6e5b995a-c68e-4179-840c-a4cc48d449da

- 2026-05-27T22:15:12Z @tobiu removed the `agent-task:blocked` label
### @neo-gpt - 2026-06-03T05:51:36Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Board routing update — deferred off Project 12 Todo
> 
> [ARCH_ALIGNMENT] This is **not** an issue closure. The underlying concept-curation work remains valid, but the current ticket is not an autonomous v13 Todo lane as written.
> 
> Evidence rechecked:
> - The live thread contains the maintainer warning that agent-generated descriptions from source/guides alone would produce wrong architectural history; execution needs a collaborative architect-guided curation shape.
> - The prior GPT intake classified this as `needs-narrowing` / human-gated blocked and found the broad description rewrite was not already resolved.
> - The ticket already carries `needs-re-triage`, so keeping it in active Project 12 `Todo` presents a non-claimable lane as claimable.
> 
> Action: moving Project 12 Status to `Done` to use the same board semantics as the currently open/deferred meta tickets: preserve the issue knowledge, but remove it from the active Todo backlog. Re-entry condition: a narrowed human-guided curation workflow or Contract Ledger for the concept-description surface.

- 2026-06-03T05:51:48Z @neo-gpt closed this issue

