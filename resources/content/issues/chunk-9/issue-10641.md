---
id: 10641
title: checkSunsetted false-positive on stale memory spawns orphan sessions
state: CLOSED
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-4-7
createdAt: '2026-05-03T13:44:21Z'
updatedAt: '2026-05-03T14:16:59Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10641'
author: neo-opus-4-7
commentsCount: 0
parentIssue: 10601
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking:
  - '[x] 10643 checkSunsetted ORDER BY picks legacy rows for originSessionId extraction'
closedAt: '2026-05-03T14:16:59Z'
---
# checkSunsetted false-positive on stale memory spawns orphan sessions

## Context

While recovering from an Anthropic API rate-limit on `@neo-opus-4-7`, the operator observed multiple unsanctioned fresh Claude Desktop sessions spawning during the rate-limit window — without any explicit sunset trigger.

Empirical evidence from `.neo-ai-data/wake-daemon/heartbeat-opus_4_7.log`:

```
[15:12:02 +02:00] Phase 1 Recovery Triggered ... Reason: Last memory is 81m old (>10m threshold)
Successfully resumed @neo-opus-4-7 via osascript (Claude)        ← orphan #1
[15:17:07] ... Cooldown active (300s / 600s)
[15:22:07] ... Last memory is 103m old → Successfully resumed    ← orphan #2
[15:27:11] ... Cooldown active
[15:32:12] ... Last memory is 125m old → Successfully resumed    ← orphan #3
[15:37:16] ... Cooldown active
[15:42:17] ... Last memory is 183m old → ...
```

3 fresh-session-spawns + 3 cooldown-skips in a single rate-limit window. Each successful resume spawned a Claude Desktop chat tab with a `buildBootGroundingPrompt()` payload but NO continuity context — no SUNSET handover read, no consolidated turn save from the original transcript. Pure Zero-State Amnesia per `AGENTS.md` §14.

## The Problem

The substrate-stack Epic #10601 ships fresh-session-spawn (#10619 / #10611 PR-B) as the recovery primitive AFTER explicit sunset. The operator's clarified substrate model:

> the bridge SHOULD spawn new sessions. but only after a sunset. otherwise => resume inside current session.

`checkSunsetted.mjs` is the gating predicate consumed by `swarm-heartbeat.sh` to decide whether to invoke `resumeHarness.mjs` (which sends Cmd+N + paste). It has two branches:

1. **`subs.length === 0` → `isSunsetted = true`** — correct (sunset unsubscribes the WAKE_SUBSCRIPTION node)
2. **`lastMemTime` aged > 10min → `isSunsetted = true`** — false-positive

Branch 2 conflates "stale memory" with "sunsetted." Stale memory has many legitimate causes:

- Anthropic API rate-limit (agent cannot save during throttle)
- Long deep-thinking turn (peer review, complex analysis)
- Memory Core embedding contention (the Memory Core path asymmetry: `add_message` works while `add_memory` blocks on Chroma)
- Tool-call sequences with consolidate-and-save still in flight

None of these are sunsets. The fix isn't a threshold tweak — it's a category error. Memory-staleness is not a sunset signal. The wake substrate already has the correct in-place delivery path via `bridge-daemon.mjs` (Cmd+`<tabShortcut>` + paste); branch 2 short-circuits it.

User-observable harm:
- Each orphan session paste consumes operator attention (visible new tabs in Claude Desktop)
- Orphan sessions have no continuity context — they'd need full SUNSET-DM recovery from scratch if engaged
- Splinters trio coordination — peers may A2A the wrong session

## The Architectural Reality

- **Predicate:** `ai/scripts/checkSunsetted.mjs:86-93` — the staleness branch
- **Caller:** `ai/scripts/swarm-heartbeat.sh:135-156` — Phase 1 Recovery dispatch
- **Spawn adapter:** `ai/scripts/resumeHarness.mjs` — Cmd+N + paste of `buildBootGroundingPrompt()`
- **In-place wake (correct path):** `ai/scripts/bridge-daemon.mjs:560-647` — Cmd+`<tabShortcut>` + paste, no fresh-session-spawn

The boundary is clean: removing branch 2 from `checkSunsetted.mjs` lets the existing in-place wake path own non-sunset wakes (its design contract). Sunset-driven fresh-session-spawn continues to fire correctly via branch 1 because the `session-sunset` workflow unsubscribes the WAKE_SUBSCRIPTION node.

## The Fix

Remove the staleness branch entirely from `checkSunsetted.mjs`. After the change:

```javascript
if (subs.length === 0) {
    isSunsetted = true;
    reason = 'No active WAKE_SUBSCRIPTION (Unsubscribe primitive fired)';
}
// (no else — staleness alone never indicates sunset)
```

Also delete the no-longer-referenced `thresholdMs` / `SUNSET_THRESHOLD_MS` env-var read.

The Anchor & Echo block on `checkSunsetted.mjs` should be extended to capture the decision rationale: memory-staleness has too many legitimate causes (rate-limit, long-think, embedding contention) to serve as a sunset proxy.

## Acceptance Criteria

- [ ] `ai/scripts/checkSunsetted.mjs` no longer reads `lastMemTime` for the sunset decision, no longer references `thresholdMs` or `process.env.SUNSET_THRESHOLD_MS`
- [ ] `checkSunsetted.mjs` returns `{sunsetted: false, originSessionId: ...}` when the agent has an active WAKE_SUBSCRIPTION, regardless of last-memory age
- [ ] Output JSON shape unchanged — `swarm-heartbeat.sh` requires no parser change
- [ ] Test coverage: agent with active WAKE_SUBSCRIPTION + 24h-old AGENT_MEMORY does NOT trigger Phase 1 Recovery
- [ ] Test coverage: agent with NO WAKE_SUBSCRIPTION continues to trigger Phase 1 Recovery (genuine sunset path)
- [ ] Anchor & Echo update on the predicate branch capturing "memory-staleness is not a sunset signal" rationale
- [ ] After merge: heartbeat log shows zero `Last memory is N m old` reasons over a 30-minute window with all three identities idle

## Out of Scope

- Adding a separate "operator warning when memory is unusually stale" signal — staleness has many legitimate causes and a noisy warn would create new operational noise; YAGNI
- Fixing the secondary `Unknown harness target for identity: neo-opus-4-7` (no `@` prefix) bug in `resumeHarness.mjs` `identityMap` — separate ticket
- Closing the 2 orphan Claude Desktop sessions still floating (15:22, 15:32 spawns) — operator-action, not codebase-action
- Cooldown TTL adjustment (currently 600s file-mtime, 300s display in heartbeat) — not load-bearing for this fix

## Avoided Traps

- **Trap: "Tune the threshold higher (30min, 60min)."** — Rejected. Threshold tuning treats the symptom, not the category error. Whatever value is picked, deeper turns will still false-positive eventually (peer reviews routinely cross 30 min; substrate-stack PR cycles can cross 60). The right fix is to stop using staleness as a sunset proxy.
- **Trap: "Demote to warn-only, keep the staleness check."** — Rejected. Adds operational noise without solving the spawn problem. If a future warning-only signal is needed, it can be a separate primitive on a separate substrate (e.g., heartbeat status report).
- **Trap: "Probe Claude Desktop UI state to detect rate-limit before spawning."** — Rejected. Cross-process UI-introspection is brittle and platform-specific. Treating staleness as not-sunset eliminates the need.
- **Trap: "Make the checkSunsetted predicate retain the staleness branch but require BOTH conditions (no subscription AND stale memory)."** — Rejected. The two signals carry different semantics; ANDing them masks the genuine sunset case where a subscription was unsubscribed but the agent hasn't yet saved a memory in the new session.

## Related

- Parent: #10601 (auto-wake substrate Epic — native `SUB_ISSUE` link to follow)
- Sibling: #10619 / #10611 PR-B (fresh-session-spawn substrate — caller of the predicate)
- Sibling: #10626 (cooldown-bounded trio wake — the cooldown that correctly skipped 3 of 6 cycles)
- Sibling: #10633 (cycle_id state-derivation — adjacent substrate-architecture refinement)
- Sibling: #10638 / PR #10639 (bridge-daemon stderr surface — independent diagnostic-gap fix; same operational anchor)

## Origin Session ID

Origin Session ID: 9766f91c-51f8-44fe-ac34-d79f61a0e1bf

## Retrieval Hint

`query_summaries("checkSunsetted memory-staleness false-positive orphan-session-spawn")` or `query_raw_memories("Phase 1 Recovery Triggered Reason Last memory is")`. Empirical anchor: heartbeat-opus_4_7.log entries between 15:12 and 15:42 +02:00 on 2026-05-03.

## Timeline

- 2026-05-03T13:44:23Z @neo-opus-4-7 added the `bug` label
- 2026-05-03T13:44:23Z @neo-opus-4-7 added the `ai` label
- 2026-05-03T13:44:23Z @neo-opus-4-7 added the `architecture` label
- 2026-05-03T13:44:39Z @neo-opus-4-7 assigned to @neo-opus-4-7
- 2026-05-03T13:44:43Z @neo-opus-4-7 added parent issue #10601
- 2026-05-03T13:49:22Z @neo-opus-4-7 cross-referenced by PR #10642
- 2026-05-03T14:10:09Z @neo-opus-4-7 cross-referenced by PR #10639
- 2026-05-03T14:12:59Z @neo-opus-4-7 referenced in commit `f93091c` - "fix(ai): align checkSunsetted file-level JSDoc with new predicate (#10641)

Reviewer (@neo-gpt) flagged that the top-of-file @summary still advertised
the removed inactivity-threshold model — direct rhetorical-drift between
the file-level KB-ingested anchor and the corrected inline Anchor & Echo
block on the predicate.

Rewrites the file-level JSDoc to state the corrected substrate semantics:
the Unsubscribe primitive is the only sunset signal; AGENT_MEMORY is read
solely for origin-session extraction and legacy-row migration. References
the predicate's Anchor & Echo block + issue #10641 for the full rationale.

Pure documentation correction; no code path change. Spec: 5 passed (716ms)."
- 2026-05-03T14:16:54Z @neo-opus-4-7 cross-referenced by #10643
- 2026-05-03T14:16:59Z @tobiu referenced in commit `f75d58d` - "fix(ai): remove memory-staleness branch from checkSunsetted (#10641) (#10642)

* fix(ai): remove memory-staleness branch from checkSunsetted (#10641)

Stale AGENT_MEMORY age was treated as a sunset signal and triggered
Phase 1 Recovery (resumeHarness.mjs Cmd+N + boot-grounding paste)
against agents that still held active WAKE_SUBSCRIPTION nodes. During
an Anthropic API rate-limit window on @neo-opus-4-7, the heuristic
fired four times in 30 minutes and spawned three orphan Claude
Desktop sessions with zero continuity context.

Memory-staleness has too many legitimate causes (rate-limit, long
deep-thinking turns, Memory Core path asymmetry under Chroma
contention, in-flight tool sequences) to serve as a sunset proxy.
The Unsubscribe primitive is the only authoritative sunset signal;
non-sunset wakes belong to the in-place delivery path in
bridge-daemon.mjs (Cmd+<tabShortcut> + paste).

- Removed the lastMemTime > thresholdMs branch and the
  SUNSET_THRESHOLD_MS env-var read from checkSunsetted.mjs
- Anchor & Echo block on the predicate captures the rationale
- New spec covers the false-positive: subscription + 24h-old memory
  must NOT trigger sunsetted=true

* fix(ai): align checkSunsetted file-level JSDoc with new predicate (#10641)

Reviewer (@neo-gpt) flagged that the top-of-file @summary still advertised
the removed inactivity-threshold model — direct rhetorical-drift between
the file-level KB-ingested anchor and the corrected inline Anchor & Echo
block on the predicate.

Rewrites the file-level JSDoc to state the corrected substrate semantics:
the Unsubscribe primitive is the only sunset signal; AGENT_MEMORY is read
solely for origin-session extraction and legacy-row migration. References
the predicate's Anchor & Echo block + issue #10641 for the full rationale.

Pure documentation correction; no code path change. Spec: 5 passed (716ms).

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-03T14:16:59Z @tobiu closed this issue
- 2026-05-03T14:17:03Z @neo-opus-4-7 marked this issue as blocking #10643
- 2026-05-03T14:18:17Z @neo-opus-4-7 cross-referenced by #10644
- 2026-05-03T15:12:04Z @neo-opus-4-7 cross-referenced by #10647
- 2026-05-03T15:24:24Z @neo-opus-4-7 cross-referenced by PR #10653
- 2026-05-03T15:40:38Z @tobiu referenced in commit `8f5bdc4` - "feat(ai): add wake safety gate and circuit breaker (#10648) (#10653)

* feat(ai): add wake safety gate and circuit breaker (#10648)

Sub of #10647 — fail-closed coordination primitive that scheduler
and recovery paths (`swarm-heartbeat.sh`, `resumeHarness.mjs`) MUST
consult before taking any high-authority action (fresh-session-spawn,
trio-wake dispatch). When wake delivery is unsafe or unverified, the
scheduler no-ops and reports — never spawns, steers, or pastes.

The 2026-05-03 regression burst (#10641 orphan-spawn, #10644
Antigravity Cmd+L file-write, #10645 AgentIdentity cache miss)
compounded because no cross-layer envelope stopped the scheduler from
acting while individual layers were broken. This gate IS that
envelope. Per #10647's 8-layer-loop diagnosis, layer-local fixes
without a cross-cutting safety state will keep regressing.

- New `ai/scripts/wakeSafetyGate.mjs` — module + CLI. States:
  `enabled` / `disabled` / `tripped`. Default-tripped when no state
  file present (deny-by-default discipline). Operator override via
  `WAKE_GATE_OVERRIDE=1` env-var; documented as operator-only with
  procedure in #10650.
- `swarm-heartbeat.sh` consults the gate before invoking
  `resumeHarness.mjs` and `trioWakeCooldown.mjs`. Both call sites
  fail-closed; sweep-expired-tasks remains unaffected (read-only).
- `resumeHarness.mjs` consults the gate at the top of `resumeHarness()`
  so direct invocations (e.g. operator-side scripts) also fail-closed,
  not just heartbeat-driven ones.
- New spec `wakeSafetyGate.spec.mjs` covers default-tripped, CLI
  surface (check/reason/show/enable/trip/disable), override sentinel
  semantics, malformed-file fallback, atomic-write round-trip.
- `resumeHarness.spec.mjs` extended with 2 new tests (gate-tripped
  skip, gate-enabled proceeds) + per-test `WAKE_GATE_FILE_PATH`
  isolation so pre-#10648 tests don't gate-block on default-tripped.
- `swarm-heartbeat.spec.mjs` extended with a static-content drift
  guard that verifies the gate check appears textually before each
  high-authority dispatch in the script.

The gate's `tripped` state writer is intentionally out-of-scope for
this PR — bridge-daemon writing on osascript-non-zero, matrix-runner
writing on column-6-fail, etc. follow-ups land via #10649 and a
post-merge bridge integration ticket.

* feat(ai): emit Phase 1 Recovery log only when gate permits spawn (#10648)

Reviewer (@neo-gpt) flagged that `Phase 1 Recovery Triggered` was logged
before the wake safety gate check, so a gate-skip would still emit a
"Triggered" line even though no spawn proceeded. Substrate behavior was
correct (gate still blocked resumeHarness), but the log surface was
misleading and made the PR body's post-merge validation wording
overly literal.

Move the "Triggered" log AFTER the gate check, and enrich the
gate-closed log with the sunset reason so the operator sees both
cause (sunsetted) and gate state (closed) on a single line. Pure
log-surface polish; no behavior change.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-03T16:08:29Z @neo-opus-4-7 cross-referenced by #10601
- 2026-05-03T17:00:24Z @tobiu referenced in commit `7a7d362` - "docs(agentos): add wake substrate incident protocol (#10650) (#10655)

* docs(agentos): add wake substrate incident protocol (#10650)

Sub of #10647 — the operator-facing companion to the wake safety gate
(#10648) and the cross-harness prompt-landing matrix (#10649). Codifies
incident declaration, the freeze rule, the restart preflight checklist
(including @neo-gpt's bridge backlog fence per
MESSAGE:b90e0c31-cdca-4b1e-9da0-2eb69a65df34), reactivation evidence
requirements, coordination mode, ownership split, and the post-incident
retrospective template.

The doc institutionalizes today's 2026-05-03 incident as the founding
empirical anchor: bridge/MCP restart after PR #10632 surfaced four
substrate regressions (#10641, #10644, #10645, #10643) within ~30 min,
three orphan Claude Desktop sessions spawned, and the operator manually
ran `kill 4390 4395 4401 4409 4413 4417` at 14:07Z to halt the cycle.
That manual-kill is the failure mode this protocol replaces with
explicit gate-state semantics + matrix-validated reactivation evidence.

Bridge backlog fence is the load-bearing preflight addition: at
incident declaration time, raw GraphLog had 893 rows pending past
wake-daemon's lastSyncId (3034081 → max 3034974). Restart-as-cleanup
would have flooded the harnesses; the fence requires the operator to
choose one of three documented paths (disable subscriptions / advance
lastSyncId after audit / run targeted test bridge) before the bridge
process starts. Bridge restart as background service action is
prohibited while active subscriptions exist + wake unsafe.

- New doc: learn/agentos/WakeSubstrateIncidentProtocol.md (211 lines)
- Indexed in learn/tree.json under the AgentOS top-level

Per ticket scope (out-of-scope items): does not implement the gate
primitive (lives in #10648 / merged), the matrix runner (#10649), or
fix any of the local regressions (#10643, #10645). Documentation-first
per the ticket's "The Fix" section.

* docs(agentos): relocate to tooling/ + strip incident-specific content (#10650)

Per @tobiu's review: the doc was placed at learn/agentos/ top-level
alongside framework-conceptual guides (StrategicWorkflows, MX,
MemoryCore), but its content was incident-specific operational
discipline — closer in shape to tooling/ (operator runbooks) than to
framework concepts.

- git mv learn/agentos/WakeSubstrateIncidentProtocol.md
       → learn/agentos/tooling/WakeSubstrateIncidentProtocol.md
- learn/tree.json: removed top-level entry, added under AgentOS/Tooling
- Stripped inline 2026-05-03 specifics from header, freeze-rule narrative,
  preflight-checklist anchors, ownership-split ticket list. Replaced the
  "Empirical Anchor: The 2026-05-03 Incident" table-section with a
  one-line pointer to #10647 where retrospectives accumulate using the
  protocol's own template.
- Doc trimmed from 211 to 192 lines; remains framework-portable for any
  downstream wake-substrate operator (npx neo-app deployments, future
  swarm reorganizations) without conflating documentation with session
  history.
- The 2026-05-03 retrospective will land as a comment on #10647 using
  the template defined in this protocol — demonstrates the template by
  example and keeps incident artifacts in the canonical incident issue.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-03T18:35:51Z @neo-gpt cross-referenced by #10658
- 2026-05-03T18:56:59Z @neo-opus-4-7 cross-referenced by PR #10659
- 2026-05-03T21:45:14Z @neo-opus-4-7 cross-referenced by #10650
- 2026-05-04T08:46:55Z @neo-opus-4-7 cross-referenced by #10671
- 2026-05-04T08:49:14Z @neo-opus-4-7 cross-referenced by #10672
- 2026-05-04T08:49:32Z @neo-opus-4-7 cross-referenced by #10673
- 2026-05-04T08:50:44Z @neo-opus-4-7 cross-referenced by #10675
- 2026-05-04T11:43:49Z @neo-opus-4-7 cross-referenced by PR #10688
- 2026-05-04T11:57:08Z @neo-opus-4-7 cross-referenced by PR #10689
- 2026-05-04T13:07:23Z @tobiu referenced in commit `23b2d79` - "feat(ai): two-mode detector contract for checkSunsetted (#10673) (#10689)

Evolves checkSunsetted.mjs to emit BOTH sunset AND idle_out_candidate
signals with explicit evidence fields, supporting the two-mode recovery
substrate from Epic #10671.

Output shape:
  {
    identity, sunset, idle_out_candidate,
    evidence: {
      subscription_active, subscription_status,
      last_memory_age_min, last_sessionId
    },
    recommended_action: 'sunset_restart' | 'idle_out_nudge' | 'no_action',

    // Backward-compat (AC5):
    sunsetted, reason, originSessionId, abandonedCount
  }

Signals are mutually exclusive by construction:
- sunset=true requires subscription_active=false
- idle_out_candidate=true requires subscription_active=true

In-flight lock integration: when a sunset_restart or idle_out_nudge
action is in flight (per inflightLock.mjs from #10683), the corresponding
signal downgrades to false — substrate is mid-recovery, no second
dispatch needed.

#10641 discipline preserved: subscription presence is the authoritative
sunset signal; staleness only surfaces as the lower-authority
idle_out_candidate "candidate in-place nudge."

IDLE_THRESHOLD_MS env var matches checkAllAgentIdle.mjs convention
(default 10 min) so downstream consumers see consistent idle semantics
across detectors.

Backward-compat: all 4 legacy fields (sunsetted/reason/originSessionId/
abandonedCount) preserved exactly as swarm-heartbeat.sh jq-parsing
requires. Verified via grep on consumer scripts.

Tests: 11 passed (5 existing + 6 new). New tests cover all 4 quadrants
of (sunset, idle_out_candidate), the (T,T)-impossible invariant via
8-scenario sweep, subscription_status disambiguation, in-flight lock
downgrade, and backward-compat field shape. Downstream regression check
on resumeHarness.spec.mjs / swarm-heartbeat.spec.mjs / inflightLock.spec.mjs:
21 passed / 2 skipped (live-host opt-in).

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-04T13:16:22Z @neo-opus-4-7 cross-referenced by PR #10690

