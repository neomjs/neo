---
id: 10035
title: Graph Integration & GapInferenceEngine Refactor
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - tobiu
createdAt: '2026-04-15T21:57:45Z'
updatedAt: '2026-04-19T10:26:46Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10035'
author: tobiu
commentsCount: 0
parentIssue: 10030
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[x] 10033 Concept Graph Unit Tests'
blocking:
  - '[x] 10036 Memory Core Concept Discovery & Glossary Enhancement'
closedAt: '2026-04-19T10:26:46Z'
---
# Graph Integration & GapInferenceEngine Refactor

## Objective
Ingest the standalone JSONL concept graph into the Native Edge Graph (SQLite) and refactor GapInferenceEngine to use deterministic concept traversal instead of regex token matching.

## Context
Phase 3, Step 1 of the Concept Ontology epic (#10030). Depends on validated concept graph from Phase 1 (#10031, #10032, #10033) and visualization from Phase 2 (#10034).

## Deliverables

### 1. DreamService Concept Ingestion
Add a new phase to DreamService (or a standalone ingestion script) that:
1. Reads `.neo-ai-data/concepts/nodes.jsonl` → creates/updates CONCEPT nodes in SQLite graph
2. Reads `.neo-ai-data/concepts/edges.jsonl` → creates typed edges (PARENT_CONCEPT, IMPLEMENTED_BY, EXPLAINED_BY, etc.)
3. Runs during DreamService Phase 0 (before gap inference)
4. Differential sync — only update changed concepts (hash comparison)

### 2. GapInferenceEngine Refactor
Replace the existing regex token matching in `executeCapabilityGapInference()` with graph traversal:

```javascript
// OLD (fragile)
const hasGuide = guideFilePaths.some(p => nodeTokens.some(term => regex.test(p)));

// NEW (deterministic)
for (const concept of graphDb.getNodesByLabel('CONCEPT')) {
    const explanations = graphDb.getAdjacentNodes(concept.id, 'outbound', 'EXPLAINED_BY');
    if (explanations.length === 0 && concept.properties.weight >= threshold) {
        gaps.push({ type: 'GUIDE_GAP', concept: concept.name, weight: concept.properties.weight });
    }
}
```

### 3. Gap Types
- `GUIDE_GAP`: Concept has IMPLEMENTED_BY but no EXPLAINED_BY (high priority)
- `EXAMPLE_GAP`: Concept has EXPLAINED_BY but no EXEMPLIFIED_BY (medium priority)
- `ORPHAN_CONCEPT`: Concept has no IMPLEMENTED_BY edges (possible dead concept, review needed)

### 4. Weight-Based Prioritization
Gap priority = `concept.weight × (1 - coverage)`. High-weight concepts missing guides float to the top of Golden Path synthesis.

## Acceptance Criteria
- [ ] CONCEPT nodes visible in SQLite graph after DreamService run
- [ ] All concept edges correctly wired
- [ ] GapInferenceEngine uses graph traversal, not regex
- [ ] Gap report priorities align with concept weights
- [ ] Existing DreamService tests still pass

Origin Session ID: 496c0120-29aa-4b02-a804-d5a32ff6b5eb

## Timeline

- 2026-04-15T21:57:47Z @tobiu added the `enhancement` label
- 2026-04-15T21:57:47Z @tobiu added the `ai` label
- 2026-04-15T21:57:47Z @tobiu added the `architecture` label
- 2026-04-15T21:58:06Z @tobiu cross-referenced by #10036
- 2026-04-15T21:58:21Z @tobiu added parent issue #10030
- 2026-04-15T21:58:35Z @tobiu marked this issue as being blocked by #10033
- 2026-04-15T21:58:36Z @tobiu marked this issue as blocking #10036
- 2026-04-15T22:12:31Z @tobiu cross-referenced by #10037
- 2026-04-19T08:28:02Z @tobiu cross-referenced by PR #10078
- 2026-04-19T09:43:06Z @tobiu cross-referenced by #10079
- 2026-04-19T09:55:42Z @tobiu assigned to @tobiu
- 2026-04-19T10:19:17Z @tobiu referenced in commit `184f3e6` - "feat(ai): add ConceptIngestor for JSONL concept graph ingestion as DreamService Phase 0 (#10035)"
- 2026-04-19T10:19:17Z @tobiu referenced in commit `6d14e44` - "docs(ai): reframe ConceptIngestor differential-sync rationale to I/O throughput (#10035)"
- 2026-04-19T10:19:17Z @tobiu referenced in commit `22d3d0d` - "refactor(ai): GapInferenceEngine replaces regex+LLM guide verification with deterministic concept-graph traversal (#10035)"
- 2026-04-19T10:19:17Z @tobiu referenced in commit `3411bb2` - "feat(ai): add EXAMPLE_GAP display section to sandman_handoff.md generation (#10035)"
- 2026-04-19T10:19:18Z @tobiu referenced in commit `91722d5` - "test(ai): rewrite GUIDE_GAP test for concept-graph traversal + add EXAMPLE_GAP and low-weight-skip coverage; fix SQLite FK by seeding edge-target stub nodes (#10035)"
- 2026-04-19T10:20:30Z @tobiu cross-referenced by PR #10084
- 2026-04-19T10:26:46Z @tobiu referenced in commit `2c949d6` - "feat(ai): refactor GapInferenceEngine to use concept-graph traversal + add ConceptIngestor Phase 0 (#10035) (#10084)

* feat(ai): add ConceptIngestor for JSONL concept graph ingestion as DreamService Phase 0 (#10035)

* docs(ai): reframe ConceptIngestor differential-sync rationale to I/O throughput (#10035)

* refactor(ai): GapInferenceEngine replaces regex+LLM guide verification with deterministic concept-graph traversal (#10035)

* feat(ai): add EXAMPLE_GAP display section to sandman_handoff.md generation (#10035)

* test(ai): rewrite GUIDE_GAP test for concept-graph traversal + add EXAMPLE_GAP and low-weight-skip coverage; fix SQLite FK by seeding edge-target stub nodes (#10035)"
- 2026-04-19T10:26:47Z @tobiu closed this issue
- 2026-04-19T10:31:22Z @tobiu cross-referenced by #10085
- 2026-04-19T10:31:35Z @tobiu cross-referenced by #10086
- 2026-04-19T10:31:51Z @tobiu cross-referenced by #10087
- 2026-04-19T10:33:36Z @tobiu cross-referenced by #10030
- 2026-04-19T14:17:12Z @tobiu cross-referenced by PR #10100
- 2026-04-19T14:59:29Z @tobiu cross-referenced by PR #10101
- 2026-04-19T15:19:01Z @tobiu referenced in commit `db5bdae` - "feat(ai): config-lift guideGapWeightThreshold to aiConfig.data (#10086)

Promotes the previously file-local `GUIDE_GAP_WEIGHT_THRESHOLD = 0.8` constant
in `GapInferenceEngine` to `aiConfig.data.guideGapWeightThreshold`, making the
weight gate curator-tunable without a code change. Default value preserved
(`0.8`); the derivation comment moves from the const JSDoc to the config
template next to the value itself, where tuners will see it.

## Scope

- `ai/mcp/server/memory-core/config.template.mjs`: adds `guideGapWeightThreshold:
  Number(process.env.NEO_GUIDE_GAP_WEIGHT_THRESHOLD) || 0.8` after `decayFactor`
  (both graph-pipeline behavior floats — semantic grouping). JSDoc carries the
  full derivation from ConceptService.calculateWeight plus the post-#10087
  context (threshold now gates three signals, not just GUIDE_GAP — historical
  prefix retained per ticket AC).
- `ai/daemons/services/GapInferenceEngine.mjs`: removes the file-local const,
  imports `Memory_Config as aiConfig` via the services SDK, caches the
  threshold once per cycle at the top of `inferConceptGraphGaps` so the live
  config value is consulted (tests can mutate it; module-load capture would
  miss runtime overrides). Class + method JSDoc updated to reference the
  config path and explain the historical `guideGap*` prefix (introduced in
  #10035 for GUIDE_GAP alone; widened in #10087 to gate all three concept-
  graph signals — rename considered but scoped out to keep this PR tight).

**Parity note:** the gitignored local `config.mjs` must mirror the template
addition for tests to pick up the key. Updated locally in this worktree but
not committed (by design — the template is the source of truth for the
shipped repo; each checkout's `config.mjs` is derived manually or via
`bootstrapWorktree.mjs` on worktree creation).

## Tests (41/41 stable × 5 runs)

New test `threshold override via aiConfig.data.guideGapWeightThreshold
changes emission behavior (#10086)`:
- Plants a tier-2 concept with weight 0.5 (below default 0.8 threshold).
- Overrides `aiConfig.data.guideGapWeightThreshold = 0.3` inside try/finally.
- Asserts the concept now emits both `[GUIDE_GAP]` and `[ORPHAN_CONCEPT]`,
  proving the config is read at gate time (not captured at module load).
- Restores the original value in `finally`.

The existing "skip low-weight concepts" test (weight 0.3 with default 0.8)
remains and now acts as the counterpart — default config path.

## AC checklist

- [x] `aiConfig.data.guideGapWeightThreshold` exists in `config.template.mjs`
      with default 0.8 + derivation comment (moved from GapInferenceEngine)
- [x] `GapInferenceEngine` reads the value from config, not from a file-local
      constant — verified via runtime-override test
- [x] Existing tests pass (40 → 41 total, all green × 5 consecutive runs)
- [x] New test proves threshold override via config produces different
      gap-emission behavior

## Related

- Parent epic: #10030
- Origin PR: #10084 (introduced the const)
- Preceded by: #10100 (scope hoist), #10101 (ORPHAN_CONCEPT — now also gated
  by this threshold, so config-lift value compounded)
- Weight formula precedent: `ConceptService.calculateWeight` (tier + uniqueness + coverage_deficit)

Resolves #10086

Origin Session ID: 1db25bbe-f39d-4dcd-bb0e-bc125ce91326"
- 2026-04-19T15:20:04Z @tobiu cross-referenced by PR #10102
- 2026-04-19T15:33:22Z @tobiu referenced in commit `45b6f63` - "feat(ai): config-lift guideGapWeightThreshold to aiConfig.data (#10086) (#10102)

Promotes the previously file-local `GUIDE_GAP_WEIGHT_THRESHOLD = 0.8` constant
in `GapInferenceEngine` to `aiConfig.data.guideGapWeightThreshold`, making the
weight gate curator-tunable without a code change. Default value preserved
(`0.8`); the derivation comment moves from the const JSDoc to the config
template next to the value itself, where tuners will see it.

## Scope

- `ai/mcp/server/memory-core/config.template.mjs`: adds `guideGapWeightThreshold:
  Number(process.env.NEO_GUIDE_GAP_WEIGHT_THRESHOLD) || 0.8` after `decayFactor`
  (both graph-pipeline behavior floats — semantic grouping). JSDoc carries the
  full derivation from ConceptService.calculateWeight plus the post-#10087
  context (threshold now gates three signals, not just GUIDE_GAP — historical
  prefix retained per ticket AC).
- `ai/daemons/services/GapInferenceEngine.mjs`: removes the file-local const,
  imports `Memory_Config as aiConfig` via the services SDK, caches the
  threshold once per cycle at the top of `inferConceptGraphGaps` so the live
  config value is consulted (tests can mutate it; module-load capture would
  miss runtime overrides). Class + method JSDoc updated to reference the
  config path and explain the historical `guideGap*` prefix (introduced in
  #10035 for GUIDE_GAP alone; widened in #10087 to gate all three concept-
  graph signals — rename considered but scoped out to keep this PR tight).

**Parity note:** the gitignored local `config.mjs` must mirror the template
addition for tests to pick up the key. Updated locally in this worktree but
not committed (by design — the template is the source of truth for the
shipped repo; each checkout's `config.mjs` is derived manually or via
`bootstrapWorktree.mjs` on worktree creation).

## Tests (41/41 stable × 5 runs)

New test `threshold override via aiConfig.data.guideGapWeightThreshold
changes emission behavior (#10086)`:
- Plants a tier-2 concept with weight 0.5 (below default 0.8 threshold).
- Overrides `aiConfig.data.guideGapWeightThreshold = 0.3` inside try/finally.
- Asserts the concept now emits both `[GUIDE_GAP]` and `[ORPHAN_CONCEPT]`,
  proving the config is read at gate time (not captured at module load).
- Restores the original value in `finally`.

The existing "skip low-weight concepts" test (weight 0.3 with default 0.8)
remains and now acts as the counterpart — default config path.

## AC checklist

- [x] `aiConfig.data.guideGapWeightThreshold` exists in `config.template.mjs`
      with default 0.8 + derivation comment (moved from GapInferenceEngine)
- [x] `GapInferenceEngine` reads the value from config, not from a file-local
      constant — verified via runtime-override test
- [x] Existing tests pass (40 → 41 total, all green × 5 consecutive runs)
- [x] New test proves threshold override via config produces different
      gap-emission behavior

## Related

- Parent epic: #10030
- Origin PR: #10084 (introduced the const)
- Preceded by: #10100 (scope hoist), #10101 (ORPHAN_CONCEPT — now also gated
  by this threshold, so config-lift value compounded)
- Weight formula precedent: `ConceptService.calculateWeight` (tier + uniqueness + coverage_deficit)

Resolves #10086

Origin Session ID: 1db25bbe-f39d-4dcd-bb0e-bc125ce91326"
- 2026-04-19T16:05:15Z @tobiu cross-referenced by PR #10105
- 2026-05-01T16:18:14Z @neo-opus-4-7 cross-referenced by PR #10589
- 2026-05-01T18:14:47Z @neo-opus-4-7 cross-referenced by PR #10593

