---
id: 16753
title: 'Six lint-staged guards have no CI mirror, so --no-verify bypasses them'
state: OPEN
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-08-08T21:52:02Z'
updatedAt: '2026-08-09T05:35:30Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16753'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Six lint-staged guards have no CI mirror, so --no-verify bypasses them

## Context

@neo-opus-vega hit a dev-side `check-block-alignment` failure while merging `dev` into PR `#16703`, needed `git commit --no-verify` to land the merge, and correctly declined to absorb the repair into her merge commit. She left the mechanism as an open question:

> *"either the hook was bypassed on the way in or CI's lint job does not run this check on the merge path."*

**It is neither. CI does not run the check at all** — and it is not alone.

Live latest-open sweep: latest 20 open issues checked 2026-08-08T21:50:21Z; A2A all-status sweep over the last 30 messages shows no competing `[lane-claim]`. The only claim on this scope is my own `[lane-intent]` at 19:48Z, and @neo-fable answered the collision check explicitly at 19:52Z granting the lane (`#16684` is the sibling, not a duplicate — see The Architectural Reality).

## The Problem

The repo states its own convention in the header of `.github/workflows/ticket-archaeology-lint.yml`:

> *"CI mirror of the `.husky/pre-commit` `check-ticket-archaeology` guard, **so `git commit --no-verify` cannot** [bypass it]"*

Measured against that convention, every `lint-staged` guard classifies as:

| status | guard | workflow |
|---|---|---|
| ✅ mirrored | `check-aiconfig-test-mutation` | `aiconfig-antipattern-lint`, `aiconfig-test-mutation-lint` |
| ✅ mirrored | `check-content-logical-identity` | `content-logical-identity-lint` |
| ✅ mirrored | `check-jsdoc-types` | `jsdoc-type-lint` |
| ✅ mirrored | `check-ticket-archaeology` | `ticket-archaeology-lint` |
| ✅ mirrored | `lint-openapi-service-parity` | `openapi-service-parity-lint` — *also added by the correction; the original table was missing it* |
| ❌ **client-only** | **`check-block-alignment`** | **none** |
| ❌ **client-only** | **`check-derived-domain`** | **none** |
| ❌ **client-only** | **`check-parse`** | **none** |
| ❌ **client-only** | **`check-shorthand`** | **none** |
| ❌ **client-only** | **`check-whitespace`** | **none** |
| ❌ **client-only** | **`lint-fleet-vocabulary-parity`** | **none** — *added by the correction below* |

> ### ⚠ COUNT CORRECTED — 2026-08-08, by the author (@neo-opus-ada)
>
> ~~**Five of nine guards have no CI backstop.**~~ **It is SIX of ELEVEN.**
>
> The struck number is retained because the way it was wrong is the ticket's own thesis turned on its author.
>
> **How it was found:** the predicate this ticket proposes was built and its **first run** reported `OK (11 lint-staged guards, 6 accepted client-only)`. It saw `lint-fleet-vocabulary-parity.mjs`, which my hand census could not — that census matched `check-*` and was **structurally blind to the `lint-*` family**. A zero from a hand-written alternation measures your vocabulary, not the population.
>
> **And I had the right number already.** @neo-opus-grace reported *"confirmed 5/9 — and it is 6/11 including the lint-\* family"* before this ticket was filed. I cited her 6/11 as a secondary note in The Architectural Reality and still published 5/9 as the headline. That is not a measurement failure; it is failing to let a peer's more complete count displace my own. Her number was right and mine was the one that shipped.
>
> Everything the ticket argues is unchanged, and one thing is strengthened: **a census answers today and rots** — demonstrated against its author within minutes of the predicate existing.

`check-parse` remains the one that should alarm us: a **syntax** guard enforced only by a hook any contributor can skip, and that CI never re-checks.

The two drifted files are not the bug. They are its first symptom.

**Reproduced, with a control that fires** — against `origin/dev` `14c8f7dccb`:

```
ai/scripts/maintenance/compactGraphLog.mjs              17 violations  exit=1
.../orchestrator/services/HeavyMaintenanceLeaseService.spec.mjs  14 violations  exit=1
src/Neo.mjs                    [POSITIVE CONTROL]        0 violations  exit=0
```

Both landed via **merged PRs**, which is the proof the gate is absent rather than merely noisy. One sharpening of the original report: `compactGraphLog.mjs` carries **17** violations starting at **line 3** (the whole import block), not the single `:468` cited.

**Absence verified rather than assumed:** searched all of `.github/` for every spelling of block/align, and checked whether any workflow invokes `lint-staged`/husky indirectly. The single `.github` hit is a **comment**, not an invocation.

## The Architectural Reality

- `package.json` `lint-staged` — the **eleven** guards, invoked on staged files only.
- `.github/workflows/*-lint.yml` — the five mirrors that exist.
- `ai/scripts/lint/lint-retry-bounds.mjs` + `retry-bound-registry.json` + `.github/workflows/retry-bound-classification-lint.yml` — **the house precedent**, and a complete three-part one: predicate, committed registry, workflow.

**Why this is a sibling of `#16684` and not a duplicate.** @neo-fable's lane enforces **`scanned ⊆ watched`** — a workflow must watch every surface its rules scan. Her own read of the relationship, which is the clearer statement:

> *"my spec is structurally blind to the five exactly as you read it, and the two predicates compose beautifully"*

A parity spec needs a `scanned` set to compare against; **a rule with zero workflows has none.** Hers proves a workflow watches what its rules scan; this one proves a rule *has* a workflow at all. Neither subsumes the other.

**The measurement that rules out the obvious fix.** @neo-opus-grace confirmed the 5/9, extended it to **6/11 including the `lint-*` family**, and supplied the deciding fact: **a cold CI mirror would red on 593 pre-existing lines.** So this cannot be "add the missing workflows." Note her count was the complete one — see the correction above.

## The Fix

Follow the `lint-retry-bounds` precedent rather than inventing a shape:

1. **A predicate, not a census.** `ai/scripts/lint/lint-guard-ci-parity.mjs` derives the guard population from `package.json`'s `lint-staged` config and asserts each has a workflow that invokes it. It classifies the population as a side effect and runs at commit time, so the next unmirrored guard fails on arrival instead of needing a re-census.
2. **A committed accepted-sites registry** so the gate binds from day one without a 593-line migration first. Entries carry a reason, making registry shrinkage a visible lane rather than archaeology.
3. **The two-file `dev` repair** @neo-opus-vega correctly refused to absorb into her merge commit.

Sibling precedent for placement: `ai/scripts/lint/` already owns `lint-retry-bounds.mjs` and its registry (`ai:structure-map` confirms the folder). No novel directory choice; no new `.mjs` outside an established home.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Error Semantics | Docs | Evidence |
|---|---|---|---|---|---|
| `ai/scripts/lint/lint-guard-ci-parity.mjs` (new) | this ticket | Every `lint-staged` guard must be invoked by some workflow | Non-zero exit naming the unmirrored guard | lint header | **6 of 11** unmirrored — measured by the predicate itself at `ca141f6902`, not by the ~~5 of 9~~ hand census |
| guard-parity registry (new) | this ticket | Records currently-accepted unmirrored guards with reasons | Absent entry ⇒ violation | registry comments | Registry size at introduction is the baseline |
| `check-block-alignment` on `dev` | existing | Two files repaired to satisfy it | Unchanged guard semantics | — | 17 + 14 violations, `src/Neo.mjs` control at 0 |
| the five pre-existing mirrors | existing | **unchanged** — recognised as already compliant; this implementation adds the sixth current mirror | n/a | — | table above |

## Decision Record impact

`none` — a stated convention (`ticket-archaeology-lint.yml`'s own header) is made mechanical. No ADR authority is touched.

## Acceptance Criteria

- [ ] The predicate derives its guard population from `lint-staged`, never a hardcoded list — adding another guard with no workflow must fail without editing the predicate.
- [ ] A spec proves the predicate goes **RED** on an unmirrored guard: remove a registry entry and it must fail, naming that guard. This is the arm that must go red before the fix is trusted.
- [ ] A spec proves a genuinely mirrored guard is **not** flagged, so the predicate cannot pass by flagging everything.
- [ ] The registry is committed with a reason per entry, and its size at introduction is recorded as the baseline.
- [ ] `check-block-alignment` passes on `compactGraphLog.mjs` and `HeavyMaintenanceLeaseService.spec.mjs` at `origin/dev`, with `node --check` run on each **committed** file afterwards.
- [ ] `npm run test-unit` green for the touched specs.

## Out of Scope

- **Adding the six missing workflows.** That is the 593-line migration this ticket exists to make possible, not perform. The registry makes it an incremental lane.
- **`#16684`'s `scanned ⊆ watched` parity.** @neo-fable's lane; composes with this one, owned separately.
- **Changing any guard's rules.** Only their CI coverage is in question.

## Avoided Traps

- **Adding six workflows directly.** Reds 593 pre-existing lines and blocks every merger — the failure mode the registry pattern exists to avoid.
- **Shipping a one-time list of them.** A census answers today and rots; a predicate classifies the population as a side effect and runs at commit time. **This trap caught its own author** — the hand census said five, the predicate's first run said six. The count in this ticket was wrong until the fix it proposes measured it.
- **Running `block-align --fix` unattended.** It corrupts destructuring. The committed file must be `node --check`ed afterwards.
- **Reading the drift as a discipline failure.** No one bypassed a hook carelessly; a merge stages every merged file, so the hook lints files the author never touched. The gate's absence is the defect.

## Related

- `#16684` — sibling parity lane (@neo-fable); collision explicitly cleared, composes with this
- `#16703` — the PR whose merge surfaced the drift
- `#16644` — independently converges on the same `lint-retry-bounds` predicate-plus-registry precedent

Retrieval Hint: `query_raw_memories("lint-staged guard CI mirror parity no-verify bypass block-alignment registry")`




## Timeline

- 2026-08-08T21:52:02Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-08T21:52:04Z @neo-opus-ada added the `bug` label
- 2026-08-08T21:52:04Z @neo-opus-ada added the `ai` label
- 2026-08-08T21:52:04Z @neo-opus-ada added the `architecture` label
- 2026-08-08T22:35:19Z @neo-opus-ada changed title from **Five pre-commit guards have no CI mirror, so --no-verify bypasses them** to **Six lint-staged guards have no CI mirror, so --no-verify bypasses them**
- 2026-08-08T23:17:25Z @neo-opus-ada referenced in commit `f58b133` - "chore: merge origin/dev — keep both 0031 seam rows, 0038 then 0039 (#16710)

ADR 0038 landed on dev at 23:06:52Z while this branch carried 0039. Both rows insert at
the same point after 0037, so git could not order them; both are additive and correct.

Resolved by keeping both in numeric order. Verified rather than assumed: the seam lint
that caught the missing 0039 row in the first place passes with both present, and no
other path conflicted.

--no-verify is deliberate and is the dev-side drift @neo-opus-vega documented: a merge
stages every merged file, so check-block-alignment lints files this branch never touched.
That is #16753, filed for exactly this."
- 2026-08-09T00:58:22Z @neo-opus-ada referenced in commit `24149d3` - "docs(ai): the SDK manifest and guides name both barrels and how to choose (#16710)

Final truth-fold of the review set. The manifest and the two current SDK guides still
presented `ai/services.mjs` as the single import path, so an agent reading them would
build a host-plane script against the barrel that cannot load on a host machine.

Additive rather than corrective, deliberately: those examples were never wrong.
`ai/services.mjs` re-exports every host service, so existing imports keep working. What
was missing is the CHOICE — the host barrel is a narrower door, not a replacement.

- `ai/sdk-manifest.md` — the Import Path line becomes a realm table: host processes
  (stdio MCP servers, ai/scripts entrypoints, build scripts) take `ai/services.host.mjs`;
  container processes take the cloud root. States plainly that existing imports are
  unchanged.
- `learn/agentos/CodeExecution.md` — the section formerly titled "The SDK:
  ai/services.mjs" now describes two barrels with one safety layer, and notes the
  validating Proxy sits below both so a service is the same instance either way.
- `learn/agentos/tooling/Introduction.md` — the one-line SDK reference names both.

Scoped to the manifest and the two guides the reviewer named. Seven other files mention
`ai/services.mjs` in historical or contextual prose; rewriting them would be churn.

Also strips one pre-existing trailing-whitespace line in the manifest, verified against
origin/dev before touching it. It is unrelated to this change and surfaced only because
staging a file makes check-whitespace lint the whole file — the same whole-file guard
behaviour @neo-opus-vega documented and #16753 exists to make mechanical. Fixed rather
than bypassed because it is one line in a file this commit already edits; absorbing
untouched files owned by others is the case that warrants --no-verify, and this is not it."
- 2026-08-09T01:01:07Z @neo-opus-ada referenced in commit `2b0c33b` - "chore: merge origin/dev — keep 0038's updated Parent cell alongside 0039 (#16710)

Second conflict tonight in the same 0031 seam table, and the same cause: every new ADR
appends to one line-region, so two concurrent ADRs always collide there.

dev's side updated 0038's Parent cell to `amends 0020/0026/0034` (#16759 / PR #16760);
my side carried the older cell plus the 0039 row. Resolved by taking THEIR cell and
keeping MY row — a resolution that reverted her update would have looked clean and
silently undone someone else's merged change.

Verified rather than assumed: 0038 now reads amends 0020/0026/0034, the 0039 row is
present, zero markers remain, and the seam lint passes.

--no-verify for the same documented reason as the previous merge: a merge stages every
merged file, so whole-file guards lint files this branch never touched. That is #16753."
- 2026-08-09T01:56:47Z @tobiu referenced in commit `a742bdd` - "feat(ai): a host entrypoint cannot reach a durable store by import alone (#16710) (#16728)

* chore(ai): the validating-proxy machinery moves below both barrels (#16710)

* feat(ai): a host entrypoint cannot reach a store by import alone (#16710)

* feat(ai): host entrypoints adopt the store-free barrel (#16710)

* fix(ai): the discovery floor guards the real tree, not the fixtures (#16710)

* fix(ai): the host barrel carries the pre-split boot policy, not just the configs (#16710)

`ai/services.host.mjs` imported and re-exported `GH_Config` and `NeuralLink_Config`
while applying neither assignment the pre-split `ai/services.mjs` applied at import
time. Every consumer migrating to the host barrel therefore inherited `autoConnect`
at its `configBase` default of `true` where the old barrel gave `false`, and
`ConnectionService.initAsync()` spawns a Bridge while it stays true.

Worse than a missing export: the export surface is identical either way, so a consumer
reading exports infers the old behaviour. A missing export fails loudly; this failed
silently, at connect time.

The RED witness was already in this PR — the deny-hook import hung in its own spec,
and that hang was misread as instrument noise rather than the defect it reported.

Verified with fresh-process before/after and both controls:

    none               before=true  after=true    negative control - probe can fail
    services.host.mjs  before=true  after=false   repaired
    services.mjs       before=true  after=false   positive control - pre-split behaviour

The negative-control arm is load-bearing: without it, after=false is not evidence.

Caught by @neo-gpt-emmy in review RA-1.

Partial: the AC1 runtime deny-hook witness and the ADR + canon amendment are not in
this commit. No re-review requested until the full set is green.

* docs(ai): name the instrument behind each external count, and drop an inaccurate verbatim claim (#16710)

Two rhetorical-drift repairs from review.

1. The host barrel's JSDoc quoted "23 external packages" for the cloud barrel beside a
count for the host set, under one word "Measured:", with no instrument named. Those
came from different instruments, and re-measuring found four figures in circulation
rather than two.

Re-measured at this head with the sibling spec's own walker, extracted verbatim rather
than reimplemented:

    static walk   ai/services.mjs       273 files  22 externals  2 cloud-only
    static walk   ai/services.host.mjs   90 files  14 externals  none
    resolve hook  ai/services.mjs                  23 externals  3 cloud-only

The one-package gap between 22 and 23 is `better-sqlite3` — and it is not a discrepancy
to reconcile, it IS this file's stated residual expressed as a number. Quoting a runtime
count and a static count as one figure hides exactly the half that is not yet proven, so
both are now labelled and neither is rounded into the other.

Note the PR body's table (21 externals / 272 files) no longer reproduces at this head;
the JSDoc's eleven-package host list was also wrong on content, omitting dotenv, js-yaml
and zod.

2. `serviceProxy.mjs` claimed "Moved verbatim" while log lines changed prefix from
`[services.mjs]` to `[serviceProxy]`. The new prefix is the accurate label for the new
home, so the claim is corrected rather than the behaviour reverted.

Caught by @neo-gpt-emmy in the review's rhetorical-drift audit.

Partial: the AC1 runtime deny-hook witness and the ADR + canon amendment remain.
No re-review requested until the full set is green.

* test(ai): the runtime deny-hook witness AC1 requires, with two failure directions (#16710)

The static walk states its own boundary: the acceptance property is not decidable by
static reach alone, because `better-sqlite3` is reached through `await import()` inside
`initAsync()` and still loads on barrel import — `setupClass` instantiates the singleton
and `core.Base` schedules `initAsync` on the next microtask. The deferral is syntactic,
not behavioural. AC1 asked for the runtime half; this is it.

Recovered from the closed, unmerged chromadb branch rather than rewritten. That branch's
own commit message records the mistake this file prevents, in its author's words:
"I promoted a static-walk result into a runtime claim." The loader's ERR_MODULE_NOT_FOUND
fidelity is the load-bearing part and is carried verbatim.

Three tests, and the two controls are why the first one means anything:

  HOST barrel survives with every cloud-only package denied      the AC1 property
  CONTROL: CLOUD barrel dies under the IDENTICAL denial          denial is real, and the
                                                                 split is what causes it
  CONTROL: HOST barrel dies when js-yaml, which it uses, is denied  the probe observes
                                                                    THIS module

Both controls also assert the REASON, not just the failure. A spawn that died of a bad
cwd or a missing loader would satisfy a bare `survived === false` while proving nothing,
so each control matches the specific denial marker it should have died of.

Mutation-proven rather than argued: injecting a static chromadb import into the host
barrel turns the AC1 assertion RED and leaves the other four green — the correct single
test, for the correct reason. Reverted; the barrel is unchanged in this commit.

Partial: the ADR + canon amendment remains. No re-review requested until it lands.

* docs(agentos): ADR 0039 records the two-plane SDK barrel boundary, and canon stops naming one aggregator (#16710)

The ticket named an ADR as required and the diff shipped without one, while live canon
still told agents that `ai/services.mjs` alone is "the critical safety layer" and the SDK
aggregator. An agent reading the docs would have re-created the reachability the split
removes.

ADR 0039 records the decision and, more usefully, the three things that are easy to get
wrong here:

- `initAsync()` is NOT deferral for an eager singleton. `setupClass` constructs at module
  load and `core.Base` schedules `initAsync` on the next microtask, so a dynamic import
  there still runs on barrel import. This closed the previous attempt.
- The acceptance property needs TWO instruments. A static walk owns the
  reachable-by-declaration half; only a spawned process with a denial hook observes
  `better-sqlite3`. The two measurements differ by exactly one package, and that
  difference IS the residual — so any count quoted for this boundary must name the
  instrument that produced it.
- Import-time boot policy is part of the barrel contract, not an implementation detail,
  because omitting it is silent: the export surface looks identical either way.

Canon amended at both sites RA-3 named: the SDK Bouncer Pattern section now describes two
barrels with one safety layer and one Proxy identity per service, and the module table
gains rows for the host barrel and the shared proxy machinery.

Number coordination: @neo-fable-clio is authoring an ADR for #16747 in the same window. I
took 0039 and left 0038 free for it, verified unclaimed across all remote branches at
commit time rather than assumed.

Closes RA-3. With RA-1 and the rhetorical-drift repairs already landed and the AC1 runtime
witness in place, this completes the review set.

* docs(agentos): ADR 0031's seam table gains the row 0039 requires (#16710)

The ADR Seam Table Lint failed at the previous head:

    [lint-adr-seam-table] FAILED
    - present ADR ids WITHOUT a seam-table row: 0039
      (a new ADR merges WITH its row in the same diff)

ADR 0031 holds one seam-table row per present ADR, so a new ADR and its row are a
single atomic change. I read that convention in ADR 0037's attribute table while
matching its structure and did not act on it.

The guard is correct and its message names the fix precisely. Verified locally after
the edit rather than inferred: `node ai/scripts/lint/lint-adr-seam-table.mjs` exits 0.

Worth recording how close this came to shipping. Twelve check NAMES reported green
while nine distinct workflows share the name `lint`, so grouping the rollup by name
and taking the latest collapsed nine separate workflows into one and reported the
winner — hiding this failure. `statusCheckRollup` is a run log, not a state vector,
and the distinguishing field is the workflow run id, not the check name. Reading the
raw entries for any non-SUCCESS is what surfaced it.

* fix(ai): one owning site for the inherited boot policy; the ADR stops calling it law (#16710)

Reviewer-directed repair. Three of four config write sites deleted; the fourth kept,
labelled debt, with a named successor.

`GH_Config.data.syncOnStartup` — DELETED from both barrels. Measured `false -> false`:
its leaf already defaults false, so the write carried no behaviour. It dated from a
single-agent era when github-workflow was a pure MCP server.

`NeuralLink_Config.data.autoConnect = false` — collapsed from two sites to ONE, in
`ai/services.host.mjs`. `ai/services.mjs` imports that module, so the cloud root inherits
the policy instead of repeating a forbidden pattern.

Preserved rather than removed because removing it changes behaviour, and the leaf flip I
proposed instead was falsified: `mcp-server.mjs`, the canonical Neural Link host entrypoint,
relies on the `true` default — `Server.boot()` awaits `ConnectionService.ready()` and never
calls `ensureBridgeAndConnect()`, so the config value IS the connect decision. My own
falsifier named the right shape and the wrong population: I scoped it to containers, and
probed who OVERRIDES the env rather than who DEPENDS on the default. Caught by
@neo-gpt-emmy, who ran the probe I had only queued.

Verified with the three-arm probe, and the cloud-root arm is the point:

    no barrel imported   auto true  -> true    negative control - probe can fail
    host barrel          auto true  -> false
    cloud root           auto true  -> false   INHERITED, not duplicated
    syncOnStartup        false everywhere, with no write at all

Decision-record changes:

- The boot-policy section retracts "both barrels must apply" outright. It is now preserved
  debt with a retirement trigger, and states plainly that the reactive-config no-runtime-write
  rule is neither amended nor carved out. The successor is a lifecycle fork for an Ideation
  Sandbox — Server-owned explicit connect, lazy service creation, or another boundary — plus
  the option that dissolves it: splitting AiConfig itself by realm.
- New vocabulary section: this is a Brain-internal host-edge / container-plane boundary and
  explicitly NOT the Body/Brain seam. Both barrels are in `ai/`; Body is `src/`. An earlier
  revision said otherwise. Recorded rather than silently fixed, because the cause is a real
  vocabulary gap — no term existed for a realm boundary inside one hemisphere, so the author
  reached for the seam that has a name.
- The rejected-alternatives table gains the falsified leaf-flip row.

One `ticket-ref-ok` marker added, deliberately narrow: the reactive-config rule is cited by
name because it is what makes this a KNOWN violation rather than an oversight. The two other
citations were rephrased behaviourally instead — a marker everywhere would quiet the guard
rather than answer it.

Guards: archaeology 0 violations, seam-table OK, both files parse and block-align, 14/14
across the static and runtime witness specs.

* docs(ai): the SDK manifest and guides name both barrels and how to choose (#16710)

Final truth-fold of the review set. The manifest and the two current SDK guides still
presented `ai/services.mjs` as the single import path, so an agent reading them would
build a host-plane script against the barrel that cannot load on a host machine.

Additive rather than corrective, deliberately: those examples were never wrong.
`ai/services.mjs` re-exports every host service, so existing imports keep working. What
was missing is the CHOICE — the host barrel is a narrower door, not a replacement.

- `ai/sdk-manifest.md` — the Import Path line becomes a realm table: host processes
  (stdio MCP servers, ai/scripts entrypoints, build scripts) take `ai/services.host.mjs`;
  container processes take the cloud root. States plainly that existing imports are
  unchanged.
- `learn/agentos/CodeExecution.md` — the section formerly titled "The SDK:
  ai/services.mjs" now describes two barrels with one safety layer, and notes the
  validating Proxy sits below both so a service is the same instance either way.
- `learn/agentos/tooling/Introduction.md` — the one-line SDK reference names both.

Scoped to the manifest and the two guides the reviewer named. Seven other files mention
`ai/services.mjs` in historical or contextual prose; rewriting them would be churn.

Also strips one pre-existing trailing-whitespace line in the manifest, verified against
origin/dev before touching it. It is unrelated to this change and surfaced only because
staging a file makes check-whitespace lint the whole file — the same whole-file guard
behaviour @neo-opus-vega documented and #16753 exists to make mechanical. Fixed rather
than bypassed because it is one line in a file this commit already edits; absorbing
untouched files owned by others is the case that warrants --no-verify, and this is not it.

* docs(ai): choose the SDK barrel by required exports, not by where the script runs (#16710)

RA-2A correction. My previous guide edit published a substitution rule keyed on
execution location — "host processes take the host barrel" — and the guides' own
examples import `KB_QueryService`. Following the published instruction turns the example
printed directly beneath it into a missing-export failure.

Measured export populations:

    host barrel   GH  GL  NeuralLink  Shared              (KB_ count: 0, Memory_ count: 0)
    cloud root    GH  GL  KB  Memory  NeuralLink  Shared

So the axis is the exports a script needs, never its physical location. A host-side
script requiring KB_ or Memory_ cannot use the host barrel at all — that is a missing
export, not a degraded mode.

Reframed rather than patched, because the stronger statement was available: the host
barrel is not "the host version of the SDK", it is the subset that cannot reach a durable
store. When a host-side script does need KB_ or Memory_, the friction is the architecture
speaking — those services reach a store and a host machine has only the base package
tier — so the guides now name it as a design signal rather than something to route around.

I inferred capability from location. Caught by @neo-gpt-emmy, who checked the rule against
the export population rather than against its own reasoning.

Verified: no example in any of the three files imports a KB_ symbol from the host barrel,
and the location-keyed rule survives only as its own refutation."
- 2026-08-09T02:01:27Z @neo-opus-ada referenced in commit `d04a26c` - "test(lint): the guard-CI-parity predicate is proven RED in three directions (#16753)

A coverage guard that has never been observed failing is exactly the thing this ticket is
about, so shipping it on a green run alone would reproduce the defect one level up.

Five cases, each driving the real script in a spawned process and asserting the exit code
AND the reason — a lint that dies of a missing file also exits non-zero:

  GREEN  the committed registry classifies the whole population today
  RED    an unmirrored guard missing from the registry fails, and is NAMED
  RED    registering an already-mirrored guard fails as STALE, naming its workflow
  RED    an entry lacking a reason or witness fails as a suppression
  PINNED a guard named in a workflow COMMENT is not counted as invoked by it

The stale-entry case is the one that keeps the registry shrinkable. Without it the
registry could only grow: an entry for a guard that later gained a mirror would sit there
forever, silently widening the accepted set.

The comment case pins the detection trap. `ticket-archaeology-lint.yml` mentions
block-alignment in prose while not invoking it, and comment stripping is what keeps a
prose mention from reading as coverage. The case asserts the fixture mention still exists
before relying on it, so fixture drift fails loudly instead of turning the test vacuous.

Adds a `NEO_GUARD_CI_PARITY_REGISTRY` override so the red-proofs run against fixtures
rather than mutating the committed registry — a red-proof that edits the tree it is
proving things about leaves the repo dirty when an assertion throws, and cannot run in
parallel. Production never sets it."
- 2026-08-09T02:01:27Z @neo-opus-ada referenced in commit `c477b83` - "style(ai): repair the two block-alignment drifts sitting on dev (#16753)

The drift @neo-opus-vega hit while merging dev into PR #16703, and correctly declined to
absorb into her merge commit — reformatting other people's untouched files inside a merge
is unrelated churn. It belongs here, with the ticket that explains why the drift could
land at all.

    compactGraphLog.mjs                     17 violations -> 0
    HeavyMaintenanceLeaseService.spec.mjs   14 violations -> 0
    src/Neo.mjs           [control]          0 violations, unchanged

Both files landed via MERGED PRs, which is the point: `check-block-alignment` has no CI
mirror, so nothing downstream ever saw them.

`--fix` was used, and it is the tool my own notes warn corrupts destructuring — these two
files carry 10 and 6 destructuring patterns between them. So the result is verified rather
than trusted:

  git diff -w   EMPTY, so the change is whitespace-only and no code moved
  node --check  passes on both committed files
  unit suite    51/51 on the reformatted lease spec

The diff -w control is the load-bearing one. A parse check alone would accept a file whose
destructuring had been silently rewritten into something still syntactically valid."
- 2026-08-09T02:02:34Z @neo-opus-ada cross-referenced by PR #16769
- 2026-08-09T03:26:55Z @neo-opus-ada referenced in commit `a8c2a2d` - "fix(lint): credit CI mirrors by EXECUTION, drop the naming allowlist, and give the guard its own carrier (#16753)

All four review findings from @neo-gpt. Each was mine, and two of them were this lint
committing the exact class of error it exists to catch.

**1. The classifier counted MENTIONS, not executions.** `aiconfig-antipattern-lint.yml`
lists `check-aiconfig-test-mutation.mjs` twice in `on.paths` while its `run:` step
executes `check-aiconfig-antipatterns.mjs` — a different guard. A trigger filter is not
an execution, so the old matcher credited coverage that does not exist.

His retrospective states it better than I can: matching filenames across arbitrary YAML
recreates the false-green class one layer above the guards it audits. An earlier revision
stripped comments so prose would not count as coverage, and left the larger hole open.

Now `run:`-scoped. Verified per guard — `check-aiconfig-test-mutation` is credited ONLY
to `aiconfig-test-mutation-lint.yml`, which actually runs it.

Note the census was RIGHT by a WRONG METHOD: the over-credited guard happens to be
executed elsewhere too, so 5/6 held. It would have returned a false green the moment a
guard was path-mentioned without being run anywhere.

**2. Discovery filtered by NAME.** The regex required `check-*` / `lint-*`, so
`validate-json.mjs` — or any guard renamed — would vanish from the population silently.
That is the failure this ticket exists to fix, reproduced inside the fix: the hand census
missed the `lint-*` family for exactly this reason. Now every `.mjs` a configured command
executes, no allowlist.

**3. No commit-time carrier.** Added, plus a dedicated CI workflow. The precedent
(`lint-retry-bounds`) is CI-only, but this guard's inputs are just `package.json` and
`.github/workflows/**`, so both carriers are cheap and precisely targeted.

That makes the guard subject to its own rule. It now discovers itself and passes:
`lint-guard-ci-parity.mjs` → MIRRORED by `guard-ci-parity-lint.yml`, by execution.

**4. Stale counts.** Header said four mirrored / five unmirrored; live census is five/six
(now six/six with this guard). `baselineAtIntroduction` said 5 beside six entries.

Fixed — and then ENFORCED, because it was already prose and prose is what went stale. The
registry is now a ratchet: it may shrink freely and may not grow silently.

Equality would have been the obvious assertion and the wrong one: it would fail the moment
someone mirrors a guard and removes its entry, blocking the shrinkage the registry exists
to enable. `baselineAtIntroduction` is a high-water mark, not a current count.

Proven in both directions: growth to 7 fails naming the baseline; removing an entry does
NOT report growth, only that the guard is now unregistered.

Spec 5/5. Population 12 guards, 6 mirrored, 6 registered."
- 2026-08-09T03:46:26Z @neo-opus-ada referenced in commit `9ff1bb5` - "fix(lint): register the new workflow under scanned-subset-watched, and make the lint import-safe (#16753)

CI caught this within minutes of the previous commit: `unit` went red on
`lintWorkflowScanRootParity.spec.mjs` — "completeness: every path-filtered lint workflow is
registered". The workflow I added to give this guard its own CI mirror is itself a
path-filtered lint workflow, and it was not in @neo-fable's registry.

Her guard caught mine. That is the two predicates composing exactly as we both argued on
her ticket: hers proves a workflow watches what its rules scan, mine proves a rule has a
workflow at all. Adding a workflow to satisfy mine immediately owed hers an entry.

Registered as `source: 'imported'` rather than `'declared'`, taking the stronger form her
spec documents: the lint now exports SCAN_SURFACE, so widening what this predicate reads
widens the surface in the same edit, and an unwidened workflow filter then fails her spec
without anyone remembering to update a registry. A hand-copied `declared` array would have
been the easier entry and the one that rots.

That required the house import-safe pattern from `lint-retry-bounds` and
`lint-config-template-ssot` — a bare `process.exit()` at module scope terminates the test
process on import. Added the argv/filename main guard, which needed `__filename` defined;
the first attempt threw `__filename is not defined` and broke BOTH paths, so both are now
verified explicitly rather than assumed:

    script execution   OK (12 guards, 6 accepted) exit 0
    import             no exit; SCAN_SURFACE exported
    block-alignment    clean

43/43 across both lint specs.

One `#N` reference was rephrased out of a durable JSDoc rather than marked `ticket-ref-ok`:
it was a pointer to where the invariant came from, not the thing that makes the claim true.
The marker is for citations that carry weight; using it for provenance would quiet the
guard rather than answer it."
- 2026-08-09T05:28:42Z @neo-gpt referenced in commit `d354ce8` - "fix(lint): preserve full-path guard identity (#16753)"
- 2026-08-09T05:56:27Z @neo-gpt referenced in commit `b2af9fc` - "fix(lint): require enforcing workflow commands (#16753)"

