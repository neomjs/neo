---
id: 14144
title: Maintenance-backpressure can indefinitely defer githubWorkflowSync during long re-embeds
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-06-26T20:46:15Z'
updatedAt: '2026-06-26T20:58:28Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14144'
author: neo-opus-ada
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Maintenance-backpressure can indefinitely defer githubWorkflowSync during long re-embeds

## Context

@tobiu flagged (2026-06-26) that the orchestrator's `githubWorkflowSync` — the GitHub-state mirror sync that feeds the sandman session-handoff — hadn't run in hours, staling the handoff. Root-caused to two blockers. The controllable root (the main repo switched off-dev via `checkout_pull_request`, → `PrimaryRepoSyncService.syncDevRoot` `not-dev-branch` skip) is addressed by a swarm worktree-discipline broadcast. **This ticket is the second, orchestrator-internal blocker.**

## The Problem

`githubWorkflowSync` is in the maintenance-backpressure heavy-task set (`MaintenanceBackpressureService` / `DEFAULT_HEAVY_MAINTENANCE_TASK_NAMES`), so the Orchestrator **defers it whenever any heavy maintenance task is active**. Today a one-time FULL KB re-embed (537 batches ≈ 26,850 chunks — triggered by the 09:38 per-element PR/Discussion chunking #14069/#14071, which re-hashed every chunk) has run ~20h on a slow embedder (~2.2 min/batch). Because that heavy task stays continuously active, `githubWorkflowSync` is deferred at every 2h window with **no max-defer / force-run escape** — so the lightweight GH-state mirror is starved for the entire ~20h, indefinitely staling the handoff.

`githubWorkflowSync` is **lightweight** — it syncs GitHub issues/PRs/discussions → `resources/content/` and commits/pushes; it does NOT contend with the embedder. Blocking it behind an embed-heavy KB-sync is a false coupling.

## The Architectural Reality

`ai/daemons/orchestrator/services/MaintenanceBackpressureService.mjs` (the heavy-task set + the participation check) + `ai/daemons/orchestrator/Orchestrator.mjs` (the `Deferring github workflow sync; heavy maintenance task ... is active` gate). The defer fires BEFORE the `PrimaryRepoSyncService.syncDevRoot` branch-check, so since the 12:42 restart the backpressure-defer is the proximate blocker (the deferral log is dedup'd after the first, masking the recurrence).

## The Fix

A bounded **max-defer escape**: after N continuous hours of deferral (a reactive config leaf, e.g. `githubWorkflowSyncMaxDeferMs`), force-run `githubWorkflowSync` even if a heavy task is active — OR exclude the lightweight `githubWorkflowSync` from the *embed-heavy* backpressure class entirely (it doesn't contend with the embedder). Preserve anti-thrash for genuinely-contending tasks.

## Acceptance Criteria

- [ ] A heavy maintenance task active beyond the max-defer bound no longer starves `githubWorkflowSync` indefinitely — it force-runs (or is excluded from the embed-heavy backpressure class).
- [ ] The max-defer bound is a reactive config leaf, read at the use-site.
- [ ] Anti-thrash preserved for tasks that genuinely contend (no force-run into a real conflict).
- [ ] Test: a simulated long-running heavy task → the lightweight sync force-runs after the bound (and the recurring deferral is observable, not silently dedup'd).

## Out of Scope

- The slow embedder itself (~2.2 min/batch — a separate V-B-A; overlaps the embed-canary work in #14124).
- The off-dev / worktree-discipline root (swarm behavior-broadcast done; a hard guard is a separate ticket).

## Related

#14124 (embedding-write canary), #14069 / #14071 (the per-element chunking that triggered the full re-embed), #14123 (the destructive-guard / sync thread).

Origin Session ID: fe9c04d6-1aae-4017-8d53-19b0e5aaf809

Retrieval Hint: "githubWorkflowSync maintenance-backpressure starvation max-defer escape stale handoff"

Live latest-open sweep: checked open issues at 2026-06-26T20:42Z; no equivalent backpressure-starvation ticket.

Authored by Ada (Claude Opus 4.8, Claude Code).

## Timeline

### @neo-opus-ada - 2026-06-26T20:52:01Z

## Re-scope after reading the backpressure code — the naive max-defer escape is UNSAFE

Self-assigned + V-B-A'd `MaintenanceBackpressureService` before implementing, and this ticket's prescription is risky as written. Re-scoping:

**The mutex is hard-won + fully-serial by design.** `githubWorkflowSync` is in `DEFAULT_HEAVY_MAINTENANCE_TASK_NAMES`, and `DEFAULT_COMPATIBLE_HEAVY_MAINTENANCE_TASK_PAIRS` is **empty** — the heavy-maintenance mutex is *intentionally fully serialized*. The in-code rationale: the prior `['kbSync','memory-summary-backfill']` compatible-pair raced on the inherited-lease-token bypass → `kbSync` SKIPPED its `syncDatabase` → **a silent multi-day embedding stall**. The serialization is a deliberate scar, not an oversight.

**So both naive fixes are unsafe:**
- A **max-defer force-run** (run `githubWorkflowSync` past the mutex after N hours) bypasses the cross-daemon lease → re-introduces exactly the concurrency the mutex prevents.
- **Removing `githubWorkflowSync` from the heavy set** lets its commit/push + "Native Graph Issue Ingestion" run concurrently with `kbSync` → potential git-commit races and/or embedder/Chroma contention.

**The real design question (a contention-analysis, not a bypass):** does `githubWorkflowSync` GENUINELY contend with `kbSync`? `kbSync` is embedder-heavy; `githubWorkflowSync` is git-commit + Native-Graph ingestion. IF that ingestion does NOT hit the embedder/Chroma the way `kbSync` does → a *narrow, contention-aware* exclusion (a documented compatible-pair, with the race-free handshake the empty-pairs comment demands) is safe. IF it does → the fix must instead **checkpoint the long `kbSync`** so it yields the lease periodically, letting the lightweight sync run between checkpoints.

**Disposition:**
1. This touches the hard-won fully-serial mutex (high-blast) → the design belongs in **/ideation-sandbox** (the contention-analysis + the race-free-escape or kbSync-checkpoint design), NOT a rushed implementation.
2. **Pragmatic priority correction:** the 20h starvation is a ONE-TIME chunking-migration cost (the 09:38 per-element re-embed re-hashed every chunk). The *recurring* pain is the slow embedder (~2.2 min/batch — #14124's embedding-write-canary domain). Fixing the embedder dissolves most of this; the backpressure-escape is belt-and-suspenders. So I'd prioritize the embedder V-B-A over this, and keep this as the careful-design follow-up.

Leaving #14144 open + assigned to me as the design-tracking item; not implementing the unsafe naive fix.

— Ada (Claude Opus 4.8, Claude Code)


### @neo-opus-ada - 2026-06-26T20:58:28Z

## Contention-analysis result — rules out a compatible-pair, confirms kbSync-checkpoint

Drove the open contention question from the re-scope. `SyncService.runFullSync` Stage-2 → `IssueIngestor.ingestIssueStates()` (`ai/services/github-workflow/SyncService.mjs:328`) depends on `GraphService` + `StorageRouter` (SQLite/**ChromaDB**) — so `githubWorkflowSync` DOES write to Chroma, the same store `kbSync` embeds into.

So the contention with `kbSync` is real (concurrent Chroma writes), and a naive compatible-pair is **confirmed unsafe** — the empty-`DEFAULT_COMPATIBLE_HEAVY_MAINTENANCE_TASK_PAIRS` invariant holds for this pair too.

**The only safe shape is a race-free `kbSync`-checkpoint:** the long re-embed periodically releases the heavy-maintenance lease so light maintenance (incl. `githubWorkflowSync`) can run between checkpoints, then re-acquires. That re-enters the exact inherited-lease-token handshake whose race caused the prior multi-day stall — so it's a genuine `/ideation-sandbox` design (the race-free release/re-acquire), not a patch.

Net: #14144 is the careful-design tracking item (Ideation when prioritized); the recurring pain is the slow embedder (the 20h is a one-time chunking-migration cost), so that's the higher-leverage fix. Assigned to me as the design lane.

— Ada (Claude Opus 4.8, Claude Code)



