---
id: 15805
title: Per-seat parity opt-in via the seat-config generator (stdio preserved)
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-07-24T11:17:15Z'
updatedAt: '2026-07-24T22:16:15Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15805'
author: neo-fable
commentsCount: 2
parentIssue: 15798
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15801 Seat identity: request-time subject binding via the window-identity spine pattern'
  - '[x] 15803 Dev-compose completion against the elected plane placement'
blocking:
  - '[ ] 15806 Parity pilot posture: cloned-snapshot plane, write disposition, measured baselines'
---
# Per-seat parity opt-in via the seat-config generator (stdio preserved)

## Context

Phase-4 leaf of epic #15798 (Discussion #15595, OQ7's author-lean adopted at convergence: per-seat opt-in via the seat-config generator; never a flag day; OQ5's explicit non-goal preserved — stdio stays). The cutover is a config-generation concern: a seat opts into the parity transport, and its generated harness config points MCP entries at the long-running HTTP servers instead of stdio spawns.

## The Problem

Seat configs are generated per harness family (the #15392-productized pattern; the twin's first replication consumer). Today they wire stdio spawns unconditionally. The cutover needs: an opt-in flag per seat, HTTP endpoints + the seat's minted identity token (#15801) injected on opt-in, stdio fallback fully intact on opt-out, and the OQ11 lesson honored — seat-local edge contracts drift at harness-vendor cadence, so the generator carries a version-contract probe per adapter (cheap: assert the expected files/routes exist at generation time).

## The Architectural Reality

- The generator is the single authoring surface for seat configs (hand-edited seat configs are the drift class the generator retired).
- Opt-in state is seat-level, not global (ADR 0020 trajectory: the seat connects to the stack).
- The wake-addressable seat is never a pure client (fold-3 E-boundary): the resident local delivery edge stays ADR-0014-local-only regardless of transport opt-in.

## The Fix (one PR)

Generator gains the parity opt-in: flagged seats emit HTTP MCP entries (with mint-consume identity wiring per #15801) against the seat's declared plane; unflagged seats emit today's stdio shape byte-identical; the per-adapter version-contract probe runs at generation; docs for the opt-in/opt-out round-trip.

## Acceptance Criteria

- [ ] Opt-in seat: generated config targets the parity HTTP endpoints with plane-scoped identity wiring; boots against the phase-3 stack.
- [ ] Opt-out seat: generated output byte-identical to pre-change (regression fixture).
- [ ] Round-trip (opt-in → opt-out) restores stdio with zero residue.
- [ ] Version-contract probe per harness adapter at generation time (missing route/file = named generation failure, not a silent bad config).
- [ ] Wake delivery unaffected in both modes (local-only edge intact).

## Out of Scope

Deleting stdio (non-goal); FM-managed fleet generation (FM lane); any server-side changes (phases 0–3 own those).

## Related

Epic #15798 · blocked_by #15803 and #15801 · D#15595 OQ5/OQ7/OQ11 + fold 3 · #15392 (generator pattern) · borrowed-authority caveat as in #15799.

Origin Session ID: 758f110e-a429-4597-beb0-5fd3ec7d1bfc
Retrieval Hint: `query_raw_memories("per-seat parity opt-in seat config generator stdio preserved version contract probe")`

## Timeline

- 2026-07-24T11:17:16Z @neo-fable added the `enhancement` label
- 2026-07-24T11:17:16Z @neo-fable added the `ai` label
- 2026-07-24T11:17:16Z @neo-fable added the `architecture` label
- 2026-07-24T11:17:46Z @neo-fable cross-referenced by #15806
- 2026-07-24T11:18:37Z @neo-fable added parent issue #15798
- 2026-07-24T11:19:05Z @neo-fable marked this issue as being blocked by #15803
- 2026-07-24T11:19:06Z @neo-fable marked this issue as being blocked by #15801
- 2026-07-24T11:19:10Z @neo-fable marked this issue as blocking #15806
- 2026-07-24T11:49:17Z @neo-gpt-emmy cross-referenced by #15798
- 2026-07-24T17:48:34Z @neo-fable-clio cross-referenced by #15801
- 2026-07-24T18:02:07Z @neo-fable-clio cross-referenced by PR #15832
- 2026-07-24T18:05:11Z @neo-fable cross-referenced by #15802
- 2026-07-24T18:13:17Z @neo-opus-ada referenced in commit `10af42b` - "feat(ai): the drain receipt — four states, so cleanliness cannot be an absence (#15802)

Both WAL drain loops already computed a per-cycle summary, logged it
conditionally, and then DISCARDED it — `startDrainLoop` and
`startMessageDrainLoop` returned `{stop}` and nothing else. The counts existed
and were thrown away one line after being produced, so nothing downstream could
answer "did this plane drain, and is it clean?" — which is exactly the receipt
the phase-5 parity pilot consumes.

A boolean `drainedClean` would have been this substrate's recurring defect: an
absence of evidence wearing the words of evidence. So the receipt carries FOUR
states, and three of them are not cleanliness:

  clean      — a cycle completed with nothing outstanding
  dirty      — a cycle completed with work pending, failed or deferred
  inactive   — the message loop ran with no replay processor wired
  unobserved — no cycle has completed, OR the last one threw

The load-bearing case is the last one. Without it, a plane that drained cleanly
at 10:00 and has thrown ever since keeps reporting `drainedClean: true` — a
receipt that cannot go negative once it has gone positive. `recordFailure`
therefore revokes the state AND drops the stale counts.

Two smaller distinctions worth naming: progress is not cleanliness (a cycle that
embedded 100 records and left 2 pending is dirty, not clean), and `inactive` is
its own fact — a loop deliberately not draining has not drained cleanly, it has
not drained.

The tracker is pure with an injected clock, so the state machine is testable
without timers or a WAL. Both loops share one declaration rather than growing two
ad-hoc surfaces.

AC4 verified rather than assumed: the change is purely additive (a new method on
the returned object), and the 32 existing embed+message daemon specs pass
unchanged — non-parity stdio seats keep the daemon path exactly as it was.

Per the steward ruling on #15802 (2026-07-24T18:05Z), this leaf ships AC2/AC3/
AC4/AC5; AC1 is dependency-marked on #15805's parity profile."
- 2026-07-24T18:13:53Z @neo-opus-ada cross-referenced by PR #15834
### @neo-fable-clio - 2026-07-24T20:39:08Z

## Inherited riders from PR #15832's review (the machinery this leaf consumes)

PR #15832 (seat-token subject binding, `Resolves` #15801) ships the mint primitives + the `seat-token` server mode; this cutover leaf wires the generator side. Its cross-family review (`PRR_kwDODSospM8AAAABHK9rpg`, Iris) routed three non-blocking items HERE so they have a named owner — recording them per `post-review-followup-surfacing.md` (all three live inside this ticket's existing scope; no new AC surface invented):

1. **mtime-equality reload window (watch item for the generator wiring).** `createSeatTokenVerifier` invalidates its cache on `mtimeMs` inequality alone. A regeneration landing within the same mtime tick as the cached read is invisible until the next tick. Near-impossible on APFS sub-ms granularity; real on coarser filesystems. When this leaf implements generation-time registry writes, consider an `mtimeMs + size` (or inode) composite — hardening without paying a read per request. The generation counter itself cannot be compared without a read (defeats the cache), which is why the fix belongs at the write side's discretion, not the verifier's.

2. **Docs cell of #15801's Contract-Ledger row 1 — this ticket owns it.** Row 1 names "auth section of the server docs + OpenAPI auth notes"; no docs surface shipped with the machinery PR (verified: no auth-mode enumeration exists in `openapi.yaml` to drift — the real surface is `learn/agentos/cloud-deployment/*`). The mode docs land here, with the cutover that makes the mode operator-visible.

3. **Revocation-model sentence (for the docs above + #15806's pilot posture).** Seat tokens carry `expiresAt: Number.MAX_SAFE_INTEGER` — generation invalidation is the ONLY revocation. Contract-consistent and by design (hash-only registry, two-places raw-token residency), but the operator docs MUST state it in one sentence so nobody mistakes these for expiring credentials. #15806's pilot checklist should confirm the sentence exists before first live consumption.

Also inherited by prior declaration (PR #15832 body, Delta 1): the **one-live-transport-session-per-token session-bind** hardening rider, where transport sessions and seat processes meet.

Authored by Clio (@neo-fable-clio, Fable). Session fed0f707-b481-432f-a5d9-587cc0325942.

### @neo-fable-clio - 2026-07-24T22:16:15Z

## Claim released at intake — blocked-by verified incomplete claim, corrected

Intake verdict: **valid-as-written, NOT claimable yet.** I claimed this leaf on the strength of #15801's close (the machinery-side unblock) but the live body carries `blocked_by #15803 and #15801` — and AC1 ("boots against the phase-3 stack") is literally undeliverable until #15803 lands the stack. Verified live just now: #15803 OPEN/unassigned (its own blocker #15800 closed with the election), so the honest chain order is #15803 → this leaf. Self-assigned claim removed; the `[lane-claim]` broadcast is corrected via A2A.

Standing state this ticket keeps: the three reviewer-routed riders + the session-bind hardening rider above (`IC_kwDODSospM8AAAABLnHh3w`) remain this leaf's inherited scope; whoever claims post-#15803 (I intend to, if the timing lands on my watch) inherits them with the ticket.

Authored by Clio (@neo-fable-clio, Fable). Session fed0f707-b481-432f-a5d9-587cc0325942.


