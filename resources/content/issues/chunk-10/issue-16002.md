---
id: 16002
title: 'Data Sync is all-or-nothing: one facet''s failure discards every facet that already succeeded, so no run has made any progress since the corpus outgrew one query'
state: CLOSED
labels:
  - bug
  - ai
  - architecture
  - build
assignees: []
createdAt: '2026-07-26T16:11:24Z'
updatedAt: '2026-07-28T18:45:17Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16002'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[x] 16001 Discussion delta cutoff never engages — `updatedAt` is never persisted, so every run re-pages the entire history'
closedAt: '2026-07-28T18:45:17Z'
---
# Data Sync is all-or-nothing: one facet's failure discards every facet that already succeeded, so no run has made any progress since the corpus outgrew one query

## Context

Operator-designated **PRIO 0**, and stated as the requirement this sync has to satisfy:

> *"if one sync can no longer do ALL tickets, pulls, discussions, do as much as possible and sync that. next run catches up more. next run catches up more, until we got it all. if a sync BREAKS, we get nothing. and every next run fails."*

That is a precise description of current behaviour, not a hypothetical. `#16001` (discussion query cost) and `#15993` / PR `#15999` (label-stage credential) are **symptoms whose blast radius is set by this defect**. Fixing them individually leaves the next single failure just as total.

## The Problem

`SyncService.emitGeneratedContentAndDerive` is a strict sequential `await` chain with **no error isolation between facets**, and — per its own JSDoc step list — **metadata is saved at step 11**, after every facet has run.

| step | work | outcome when step 6 throws |
|---|---|---|
| 1 | fetch + cache releases | done, then **discarded** |
| 2 / 2b | reconcile closed issue + pull locations | done, then **discarded** |
| 3 | push local changes | done, then **discarded** |
| 4 | **pull remote issues/tickets** | done, then **discarded** |
| 5 | release notes | done, then **discarded** |
| **6** | **discussions** | **throws** |
| 7 – 7c | **pull requests**, duplicate repair, index realign, integrity | **never execute** |
| 11 | **save metadata to disk** | **never executes** |

Two independent losses compound:

1. **No facet isolation.** A discussions failure at step 6 means `PullRequestSyncer.syncPullRequests` (step 7) is **never called**. This is the mechanical reason `pulls/` and `discussions/` are both stale — one starves the other, and the two "unowned facets" blocking `#15972` are one defect, not two.
2. **No incremental persistence.** Metadata is written once, at the end. Steps 1–5 genuinely succeeded and their high-water marks are thrown away with the exception.

**And within a facet it is the same shape.** `DiscussionSyncer.syncDiscussions` accumulates every page into an in-memory `allDiscussions` array (`:401-441`) and performs **no writes inside the loop** — all file and metadata work happens after it. So a failure on page *N* discards pages 1…*N-1* as well.

### Why this produces permanent zero progress rather than degraded progress

The current step-6 failure is **deterministic**: `limit: 50` exceeds GitHub's per-query cost ceiling on every attempt (`#16001`, measured — 30 succeeds, 32 fails). A deterministic failure plus all-or-nothing persistence means:

- every run does the same work,
- fails at the same point,
- writes nothing,
- and the next run starts from the identical state.

**No run has advanced the corpus since the discussion corpus crossed that ceiling.** Not "slowly falling behind" — *zero* progress, indefinitely, while every individual facet's own code is working correctly.

## The Architectural Reality — the tension this must resolve, not ignore

Step 7c is a **deliberate** all-or-nothing, and its comment argues the case well:

> *"Committing a corpus we have already measured as broken is worse than failing the run: the run can be retried, but a generated commit is what every consumer then reads as truth."*

That reasoning is sound and must survive. **It is not in conflict with the operator's requirement, because the two are about different units:**

- 7c refuses to publish a facet **known to be internally broken**. Correct.
- The operator requires that a facet which completed **cleanly** not be rolled back by a *different* facet's failure.

The resolution is **facet-level atomicity**: each facet advances its own high-water mark if and only if that facet completed consistently, and no facet's failure reverts another's success. "Partial progress" means *fewer facets advanced this run*, never *a half-written facet published*.

## The Fix

1. **Isolate each facet.** Run facets so one throwing does not skip the others. Collect per-facet outcomes; the run's exit status reflects the aggregate, so a failure is still loud and still fails CI.
2. **Persist per facet.** Save metadata after each facet that completed cleanly, rather than once at the end. A facet that threw leaves its own high-water mark untouched.
3. **Persist within a facet, per page.** Write and advance the cursor per batch instead of accumulating the whole corpus in memory, so page *N* failing keeps pages 1…*N-1*.
4. **Preserve 7c.** Integrity verdicts still abort **their own facet's** publication. A facet measured broken does not advance.
5. **Report the shortfall explicitly.** A run that advanced 4 of 6 facets must say which two did not and why — a partial run that looks like a clean one is the silent-channel failure, and this ticket must not trade total loss for invisible loss.

## Acceptance Criteria

- [x] **AC1 — Facet isolation:** *(delivered — #16010 / PR #16011, `90403a8c13`)* an injected failure in the discussions facet leaves `PullRequestSyncer.syncPullRequests` **called** and its results persisted. Unit witness; RED against current `SyncService` ordering.
- [x] **AC2 — Per-facet persistence:** *(delivered — #16010 / PR #16011)* after that injected failure, on-disk metadata reflects the facets that succeeded, and the failed facet's high-water mark is **unchanged**. Assert both halves — advancement of the good, non-advancement of the bad.
- [ ] **AC3 — Within-facet resume:** a discussions sync failing on page 3 persists pages 1–2 and advances the cursor such that the next run starts at page 3. Assert no gap and no duplicate.
- [ ] **AC4 — Convergence, which is the operator's actual requirement:** across N successive runs against a corpus too large for one pass, coverage is **monotonically non-decreasing** and reaches complete. A test that runs the sync repeatedly against a fixture and asserts it converges — not merely that one run does not crash.
- [x] **AC5 — The run still fails loudly:** *(delivered — #16010 / PR #16011)* a partial run exits non-zero and names each facet that did not advance, with its reason. A partial success must never be reported as a success.
- [x] **AC6 — 7c preserved:** *(delivered — #16010 / PR #16011)* a facet failing its integrity verdict does **not** advance its high-water mark and does not publish, while other clean facets still do. Unit witness distinguishing "this facet is broken" from "another facet is broken".
- [~] **AC7 — Live evidence: RETIRED as unsatisfiable, 2026-07-26.** As written this AC required a local `syncGithubWorkflow` advancing a starved facet **"with `#16001` still unfixed"** — proving isolation independent of the delta-cutoff symptom. `#16001` merged (`b67e208115`, 2026-07-26T22:27:49Z), so that precondition is permanently unreachable and no future evidence can satisfy the clause. Retiring rather than quietly reinterpreting it: an AC nobody can satisfy either blocks closure forever or licenses hand-waving, and restating it post-hoc to fit whatever evidence exists is the same defect wearing the opposite hat. **What it was protecting is already covered**: AC1/AC2/AC6 are unit witnesses that are mutation-discriminating against the isolation logic itself, with no dependence on the `#16001` symptom (verified RED by reinstating each removed guard). The confounder AC7 existed to exclude was excluded by construction, not by timing.

## Out of Scope

- **`#16001`** — the discussion query cost ceiling. The trigger, not this defect. Fixing it first would hide this one by removing today's only failure.
- **`#15993` / PR `#15999`** — label-stage credential. Independent.
- **Ruleset `19087298`** — the CI-side `GH013` root cause of `#15972`; operator-held repo configuration.
- Changing what the corpus stores, or any facet's fetch semantics.
- Parallelising facets. Isolation is not concurrency, and mixing them would make the failure attribution harder.

## Avoided Traps

- **Fixing the symptom and declaring victory.** `#16001` would make today's run pass and leave the next single failure just as total. This ticket exists to make the blast radius small, and it must be verified **while a symptom is still live** (AC7).
- **Reading step 7c as an obstacle.** Its argument is correct; the fix keeps it and narrows its unit to the facet. Overturning it would trade a total-loss failure for a publish-broken-truth failure, which is worse.
- **Trading total loss for silent partial loss.** A partial run that exits 0 and looks clean is a regression in a different direction — AC5 is not optional.
- **"Just wrap it in try/catch."** Catching without per-facet persistence still discards steps 1–5, because the metadata write is at the end. Isolation and persistence are two changes, not one.
- **Assuming the current failure is transient.** It is deterministic at `limit: 50`; a retry loop spends the budget repeatedly and converges on nothing.

## Related

`#15972` (the alarm; ruleset root cause + serial-cause analysis) · `#16001` (discussion query cost — the current trigger) · `#15993` / PR `#15999` (label-stage credential) · `#15977` (corpus generator scheduled / publisher unowned — the ownership half of the same starvation) · `#15751` (blocked by this outage).

Live latest-open sweep at 2026-07-26T16:11Z across `incremental resume sync`, `facet isolation`, `partial progress`, `resumable`, `emitGeneratedContentAndDerive`, `all-or-nothing`: adjacent set is `#16001`, `#15977`, `#15693`, `#15490`; none covers facet isolation or incremental persistence in the sync chain. No duplicate.

Authored by Ada (@neo-opus-ada, Claude Opus 5, Claude Code).


## Timeline

- 2026-07-26T16:11:24Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-26T16:11:25Z @neo-opus-ada added the `bug` label
- 2026-07-26T16:11:26Z @neo-opus-ada added the `ai` label
- 2026-07-26T16:11:26Z @neo-opus-ada added the `architecture` label
- 2026-07-26T16:11:26Z @neo-opus-ada added the `build` label
- 2026-07-26T16:11:32Z @neo-opus-ada marked this issue as blocking #16001
- 2026-07-26T16:14:09Z @neo-opus-vega cross-referenced by #16003
- 2026-07-26T16:52:58Z @neo-opus-ada cross-referenced by PR #15999
- 2026-07-26T17:17:02Z @neo-opus-ada referenced in commit `bb0ed77` - "test(ai): mutation-discriminating witnesses for facet isolation (#16002)

Four witnesses for the isolation half, each verified RED against a `#runFacet`
that re-throws — which reproduces the pre-fix all-or-nothing chain exactly:

- a failing facet does not skip the facets AFTER it (the fact that starved one
  content facet from another's fault)
- a failing facet advances NOTHING while succeeded facets are persisted, driven
  by a facet that mutates the shared accumulator and THEN throws, which is the
  ordering that makes per-slice rollback load-bearing
- the aggregate verdict names every facet that did not advance, so partial
  progress cannot report as a clean run
- an integrity abort withholds the pull facet ONLY

The fourth needed strengthening. As first written it asserted rollback state that
holds under a whole-run abort too, so it witnessed rollback rather than isolation
and passed under the mutation. It now asserts the per-facet accounting in the
aggregate message — naming `pulls` and not `discussions` — and fails without it.

Method note: the first mutation run reported 1 failure of 4 and the remaining
three as passing. They had been SKIPPED — `test.describe.configure({mode:
'serial'})` halts the rest after a failure, so the evidence was inconclusive
rather than negative. Re-run per witness with `-g` to get real per-test
discrimination."
- 2026-07-26T17:18:24Z @neo-opus-ada cross-referenced by PR #16011
- 2026-07-26T17:18:35Z @neo-opus-ada cross-referenced by #16010
- 2026-07-26T18:08:52Z @neo-opus-ada cross-referenced by PR #16015
- 2026-07-26T20:00:49Z @neo-opus-ada referenced in commit `13e9d0a` - "fix(ai): sync facets persist independently instead of all-or-nothing (#16002)

`emitGeneratedContentAndDerive` was a bare sequential `await` chain with one
`MetadataManager.save` at the end. A facet throwing therefore discarded every
facet that had already succeeded AND skipped every facet after it: the
discussions failure at step 6 meant `syncPullRequests` was never called, and
steps 1-5 were thrown away unwritten.

With a deterministic trigger that is not "slowly falling behind" — it is a
corpus frozen indefinitely while each facet's own code works correctly, and it
is why `pulls/` and `discussions/` are both stale from one defect, not two.

Each facet now runs through `#runFacet`, which saves metadata the moment that
facet completes and rolls back only that facet's own slices when it does not.
Rollback is per-slice because facets mutate a shared accumulator in place:
without it, mutations that failed the integrity verdict would still be
persisted by the next facet's save.

`pullFromGitHub`'s fresh metadata object is merged into the accumulator rather
than replacing it, removing the hand-maintained carry-over that existed only
because the save happened once at the end.

The pull-integrity guarantee is preserved and narrowed, not removed: a pull
corpus measured broken still does not advance, and delivery is still never
reached, because the aggregate verdict throws before the auto-push. The three
existing tests that pinned the WHOLE-run abort now assert the invariant that
survives — no auto-push, and the failed facet advances nothing — rather than
the incidental save and derive counts.

The empty-object metadata contract moves from `|| {}` at the save site to one
normalization of the accumulator after load, which also makes
`MetadataManager.save` reading `metadata.issues` unguarded safe by construction.

Remaining for this ticket: within-facet page-level resume, and the dedicated
AC witnesses for isolation, convergence and the loud-partial verdict."
- 2026-07-26T20:00:49Z @neo-opus-ada referenced in commit `1daf46e` - "test(ai): mutation-discriminating witnesses for facet isolation (#16002)

Four witnesses for the isolation half, each verified RED against a `#runFacet`
that re-throws — which reproduces the pre-fix all-or-nothing chain exactly:

- a failing facet does not skip the facets AFTER it (the fact that starved one
  content facet from another's fault)
- a failing facet advances NOTHING while succeeded facets are persisted, driven
  by a facet that mutates the shared accumulator and THEN throws, which is the
  ordering that makes per-slice rollback load-bearing
- the aggregate verdict names every facet that did not advance, so partial
  progress cannot report as a clean run
- an integrity abort withholds the pull facet ONLY

The fourth needed strengthening. As first written it asserted rollback state that
holds under a whole-run abort too, so it witnessed rollback rather than isolation
and passed under the mutation. It now asserts the per-facet accounting in the
aggregate message — naming `pulls` and not `discussions` — and fails without it.

Method note: the first mutation run reported 1 failure of 4 and the remaining
three as passing. They had been SKIPPED — `test.describe.configure({mode:
'serial'})` halts the rest after a failure, so the evidence was inconclusive
rather than negative. Re-run per witness with `-g` to get real per-test
discrimination."
- 2026-07-26T20:53:51Z @neo-opus-ada referenced in commit `bb0d628` - "fix(ai): release history is a prerequisite, not an isolated facet (#16010)

@neo-gpt's review found that facet isolation removed an implicit guard rather
than only adding one.

`ReleaseNotesSyncer.sortedReleases` is the bucketing reference every
closed-artifact planner reads — its own JSDoc says so. An ABSENT reference does
not fail: `DiscussionSyncer:133` resolves it to "no release version applies" and
`activeItems.push()` places the artifact in the ACTIVE bucket. So a failed
release fetch does not skip one facet, it silently mis-buckets every closed
issue, pull and discussion — and per-facet persistence then writes that
placement to disk and to metadata.

The old sequential chain was ACCIDENTALLY safe: a release throw aborted
everything downstream. Isolating the facets removed that, which is the trap in
treating a caught failure as an independent one. Caught and independent are
different properties, and isolation is sound only over the dependency graph.

Release history is now a prerequisite. The four dependent facets run through
`dependentFacet`, which skips them when the bucketing reference is unavailable —
and a skipped facet is still REPORTED as not-advanced, so skipping cannot shrink
the denominator and let a partial run read as clean.

Satisfiable two ways, because a fetch failure is not an absent reference:
the facet advanced, OR a non-empty reference survived from the cached fast-path
that runs before the network call. An empty array after a SUCCESSFUL fetch stays
legitimate — a repo with no releases has nothing to bucket into.

Two witnesses, discriminating in OPPOSITE directions:
- removing the gate fails the prerequisite witness (under-gating)
- removing the cached fallback fails the cached witness (over-gating)
Each is silent on the other's mutation, because they guard different properties.

Also captured and restored `sortedReleases` in the spec lifecycle: the new
witnesses mutate a singleton, and an unrestored value leaks into every later test
in this serial file and into any spec sharing the worker.

JSDoc narrowed per the same review: the convergence claim now says BETWEEN-facet
only, with within-facet page resume explicitly left to #16002."
- 2026-07-26T21:14:34Z @tobiu referenced in commit `90403a8` - "fix(ai): sync facets persist independently instead of all-or-nothing (#16011)

* fix(ai): sync facets persist independently instead of all-or-nothing (#16002)

`emitGeneratedContentAndDerive` was a bare sequential `await` chain with one
`MetadataManager.save` at the end. A facet throwing therefore discarded every
facet that had already succeeded AND skipped every facet after it: the
discussions failure at step 6 meant `syncPullRequests` was never called, and
steps 1-5 were thrown away unwritten.

With a deterministic trigger that is not "slowly falling behind" — it is a
corpus frozen indefinitely while each facet's own code works correctly, and it
is why `pulls/` and `discussions/` are both stale from one defect, not two.

Each facet now runs through `#runFacet`, which saves metadata the moment that
facet completes and rolls back only that facet's own slices when it does not.
Rollback is per-slice because facets mutate a shared accumulator in place:
without it, mutations that failed the integrity verdict would still be
persisted by the next facet's save.

`pullFromGitHub`'s fresh metadata object is merged into the accumulator rather
than replacing it, removing the hand-maintained carry-over that existed only
because the save happened once at the end.

The pull-integrity guarantee is preserved and narrowed, not removed: a pull
corpus measured broken still does not advance, and delivery is still never
reached, because the aggregate verdict throws before the auto-push. The three
existing tests that pinned the WHOLE-run abort now assert the invariant that
survives — no auto-push, and the failed facet advances nothing — rather than
the incidental save and derive counts.

The empty-object metadata contract moves from `|| {}` at the save site to one
normalization of the accumulator after load, which also makes
`MetadataManager.save` reading `metadata.issues` unguarded safe by construction.

Remaining for this ticket: within-facet page-level resume, and the dedicated
AC witnesses for isolation, convergence and the loud-partial verdict.

* test(ai): mutation-discriminating witnesses for facet isolation (#16002)

Four witnesses for the isolation half, each verified RED against a `#runFacet`
that re-throws — which reproduces the pre-fix all-or-nothing chain exactly:

- a failing facet does not skip the facets AFTER it (the fact that starved one
  content facet from another's fault)
- a failing facet advances NOTHING while succeeded facets are persisted, driven
  by a facet that mutates the shared accumulator and THEN throws, which is the
  ordering that makes per-slice rollback load-bearing
- the aggregate verdict names every facet that did not advance, so partial
  progress cannot report as a clean run
- an integrity abort withholds the pull facet ONLY

The fourth needed strengthening. As first written it asserted rollback state that
holds under a whole-run abort too, so it witnessed rollback rather than isolation
and passed under the mutation. It now asserts the per-facet accounting in the
aggregate message — naming `pulls` and not `discussions` — and fails without it.

Method note: the first mutation run reported 1 failure of 4 and the remaining
three as passing. They had been SKIPPED — `test.describe.configure({mode:
'serial'})` halts the rest after a failure, so the evidence was inconclusive
rather than negative. Re-run per witness with `-g` to get real per-test
discrimination.

* fix(ai): release history is a prerequisite, not an isolated facet (#16010)

@neo-gpt's review found that facet isolation removed an implicit guard rather
than only adding one.

`ReleaseNotesSyncer.sortedReleases` is the bucketing reference every
closed-artifact planner reads — its own JSDoc says so. An ABSENT reference does
not fail: `DiscussionSyncer:133` resolves it to "no release version applies" and
`activeItems.push()` places the artifact in the ACTIVE bucket. So a failed
release fetch does not skip one facet, it silently mis-buckets every closed
issue, pull and discussion — and per-facet persistence then writes that
placement to disk and to metadata.

The old sequential chain was ACCIDENTALLY safe: a release throw aborted
everything downstream. Isolating the facets removed that, which is the trap in
treating a caught failure as an independent one. Caught and independent are
different properties, and isolation is sound only over the dependency graph.

Release history is now a prerequisite. The four dependent facets run through
`dependentFacet`, which skips them when the bucketing reference is unavailable —
and a skipped facet is still REPORTED as not-advanced, so skipping cannot shrink
the denominator and let a partial run read as clean.

Satisfiable two ways, because a fetch failure is not an absent reference:
the facet advanced, OR a non-empty reference survived from the cached fast-path
that runs before the network call. An empty array after a SUCCESSFUL fetch stays
legitimate — a repo with no releases has nothing to bucket into.

Two witnesses, discriminating in OPPOSITE directions:
- removing the gate fails the prerequisite witness (under-gating)
- removing the cached fallback fails the cached witness (over-gating)
Each is silent on the other's mutation, because they guard different properties.

Also captured and restored `sortedReleases` in the spec lifecycle: the new
witnesses mutate a singleton, and an unrestored value leaks into every later test
in this serial file and into any spec sharing the worker.

JSDoc narrowed per the same review: the convergence claim now says BETWEEN-facet
only, with within-facet page resume explicitly left to #16002."
- 2026-07-26T22:24:59Z @tobiu unassigned from @neo-opus-ada
- 2026-07-26T22:32:57Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-27T21:49:42Z @neo-opus-vega referenced in commit `93bb9bb` - "docs(github-workflow): correct the guard's premise — delivery fails, not detection (#16057)

I asserted in this file, in the PR body and on the ticket that the repair "has no scheduled invoker at
all". That is false, and the operator caught it. The orchestrator schedules `githubWorkflowSync`
(`taskDefinitions.mjs:427`) on a default two-hour interval
(`configBase.mjs:906`, `NEO_ORCHESTRATOR_GITHUB_WORKFLOW_SYNC_INTERVAL_MS`), enabled on local
deployments and correctly disabled on cloud ones via the `localOnly` null-means-profile-default rule.
My grep looked for `runFullSync` callers; the orchestrator spawns the CLI SCRIPT BY PATH, so the search
could not have found it. Searching for the method and concluding about the capability was the error.

The corrected mechanism is worse than the one I claimed, not better. The repair runs every couple of
hours and never lands: stage 7-d takes its integrity verdict AFTER the repair, throws while the corpus
is unclean, and the aggregate verdict fails the run before the generated-content commit. So the corpus
repairs itself on disk and the fix is never delivered.

Evidenced on the canonical clone, which is on `dev` so the dev-branch-only guard passes: its working
tree holds ZERO duplicate logical names while committed `dev` holds 27, with 488 uncommitted files
under `resources/content`. The repair has been succeeding and failing to deliver for weeks.

This is why the guard is still worth having, and it sharpens what it is for: not "nothing repairs this"
but "a repair that cannot be delivered leaves every consumer reading the broken state, silently". The
sibling delivery defects are #16002 (facet isolation / all-or-nothing) and #15993 (SEO credential
scope); this commit does not touch them."
- 2026-07-27T21:50:23Z @neo-opus-vega cross-referenced by PR #16067
- 2026-07-27T21:50:28Z @neo-opus-vega cross-referenced by #16057
- 2026-07-27T21:51:53Z @neo-opus-vega cross-referenced by #16069
- 2026-07-27T21:56:46Z @neo-opus-vega cross-referenced by #15972
- 2026-07-27T22:54:05Z @tobiu referenced in commit `c45d5db` - "Assert one artifact per logical name where the corpus is committed (#16067)

* feat(github-workflow): assert one artifact per logical name where the corpus is committed (#16057)

Two archived artifacts claiming one logical name are two documents asserting one identity, and
nothing on disk records which is current. `PullRequestSource` already refuses to embed in that
state -- correctly, fail-closed, with a precise message. That refusal was also the ONLY
enforcement, and it lives at the point of consumption: it runs on the Knowledge Base ingestion
schedule, days later, often on another host, long after the commit that broke the invariant. It
presents as "the deployment's Knowledge Base is empty", which is where it was in fact found.

The asymmetry that made it permanent: the fail-closed consumer is SCHEDULED, while the shipped
repair (`PullRequestSyncer.repairDuplicateArtifacts`, added in #15319) has no scheduled invoker at
all. `runFullSync` is reachable only from the `ai:sync-github-workflow` CLI and release-time
`publish.mjs`; no workflow references either. So a duplicate blocks ingestion continuously and is
repaired only when a human runs a command. The 27 collisions now on `dev` entered on 2026-06-21
and survived a repair merged 2026-07-17 for exactly that reason.

The invariant belongs where the corpus changes, so it is asserted at BOTH write points:

`commitRebaseAndPushGeneratedContent` is the load-bearing one, and it is the only place that can
work. It commits with `--no-verify` deliberately and correctly -- generated content carries
trailing whitespace the whitespace hook rejects -- which disables every git hook, so a
`lint-staged` guard is structurally unable to see the one automated commit that writes the corpus.
Wiring only the hook would have shipped a guard that cannot fire where duplicates enter.

`lint-staged` covers the hand-authored path on top, where hooks do run.

Both call one exported predicate, so the invariant has a single definition and the two enforcement
points cannot drift. The `ai/` -> `buildScripts/util` import direction follows the
`buildScripts/util/sanitizer.mjs` precedent in every `ai/mcp/server/*/mcp-server.mjs`.

Scoped to the STAGED set rather than the whole corpus. A collision a run did not touch is not that
run's to fix, and a full-corpus assertion would refuse every sync until an unrelated repair lands,
turning a guard against new damage into a block on all progress. `npm run ai:check-content-identity`
runs the full audit on demand; it reports the 27 known collisions today and is deliberately not
wired into any blocking surface until they are repaired.

Families are DERIVED from the archive tree, never listed. A hardcoded roster would cover the
families the author knew about and silently exempt the next one -- the failure mode
`check-derived-domain.mjs` exists to flag -- so a new archived-content family is covered with no
code change (asserted by a fixture that invents one). Collision scope is the FAMILY, not the
version bucket, matching how a consumer keys an artifact: a pull request belongs to exactly one
release, so the same name under two buckets is the same defect in a different shape.

Evidence: the guard was observed refusing a deliberately reintroduced duplicate end-to-end through
`npx lint-staged` (naming both paths), and returning to exactly 27 -- not 28 -- once removed. The
commit-seam assertion carries a positive control: a staged artifact with no collision still reaches
commit and push, so the refusal is not a guard that rejects everything. 65 + 23 focused green.

Does not resolve #16057: the 27 existing collisions and the full-sync receipt (AC2, AC5) need the
repair actuator to be RUN, which needs a corpus-mutating command this session was not permitted to
execute. AC1 was reported on the ticket; AC6's generator is named by ADR 0004 line 31.

* fix(content): re-derive 27 divergent PR artifacts from GitHub and realign the index (#16057)

Unblocks Knowledge Base ingestion. `PullRequestSource` refuses to embed while any pull request owns
more than one local artifact, and that refusal aborts source enumeration for the WHOLE corpus -- so a
deployment ingesting this repo reported zero documents, which is how this was found.

Repaired with the shipped actuator, `PullRequestSyncer.repairDuplicateArtifacts`, not by hand. Its
contract is that neither local copy is trusted: both files are real renderings of one PR and nothing
on disk records which is current, so the artifact is re-derived from GitHub and placed on the ordinal
that COMPLETE membership chooses. 27 repaired, 28 stale copies removed, 0 failed.

Placement: 27 files leave `archive/pulls/v13.0.0/chunk-1` -- the sealed-chunk-violating bucket ADR
0004 line 31 names, where `migrate-pr-archive-ac8.mjs --fallback-version v13.0.0` pre-staged them --
26 canonical copies are rewritten in chunk-2..12, and one lands in a new chunk-15, which is correct:
1445 unique members need chunk-1..15 under the ordinal-100 rule. Zero writes to the active tier.

`reconcilePullRequestIndex` then realigned `_index.json` (reindexed 2042, unchanged 2621, removed 0),
clearing the stale entries the moves created plus the 2015 that predated them.

Verified by two independent instruments, deliberately not one. `verifyCorpusIntegrity` reports ok:true
with divergentDuplicates, staleIndexEntries, inconsistentIndexEntries, duplicateIndexRows and
unindexedIds all zero; a separate file-level scan independently reports 0 duplicate logical names
across 14201 artifacts, down from 27.

Two facts this does NOT claim. `verifyCorpusIntegrity` measures duplicates and index rows and has no
opinion on which TIER an artifact occupies -- an earlier attempt of this repair passed the unit spec's
`{pulls: {}}` fixture as metadata, left `ReleaseNotesSyncer.sortedReleases` empty so every terminal PR
resolved to no version, wrote all 27 into the ACTIVE tier, and still scored ok:true. Placement is
therefore asserted from the path shape, above, and the corrected runner refuses to start on an empty
release list. And 175 artifacts remain on non-ADR-correct ordinals across three buckets; that is
ordinal drift, it does not create duplicate identities, and it does not block embedding.

* ci(content): hold the logical-identity invariant on the direct-push path (#16057)

The third layer, and the one that covers how this defect actually arrives. `lint-staged` covers
hand-authored commits; the in-process assertion covers the automated sync, which commits `--no-verify`.
Neither watches `dev` itself.

`push: branches: [dev]` is the load-bearing trigger. Corpus artifacts are pushed straight to `dev` by
the sync path rather than arriving by pull request, so a `pull_request`-only lint would guard a road
this defect never travels -- an earlier revision of this branch's PR body claimed CI could not reach
that path at all, which was wrong: the sibling lints here have always run on both events.

Full-tree `--all` audit rather than a changed-files subset, because a collision needs only ONE of its
two sides to appear in the commit under test; a subset check passes on the untouched side and reports
green over a corpus that refuses to embed. That is affordable as a BLOCKING gate only now that the
corpus is clean -- while it carried 27 known collisions this same job would have wedged every commit
in the repository. The script's JSDoc is updated to say so, since it previously documented itself as
deliberately unwired.

No `npm ci`: the check imports only `node:fs`, `node:path`, `node:process` and `node:url`.

* docs(github-workflow): correct the guard's premise — delivery fails, not detection (#16057)

I asserted in this file, in the PR body and on the ticket that the repair "has no scheduled invoker at
all". That is false, and the operator caught it. The orchestrator schedules `githubWorkflowSync`
(`taskDefinitions.mjs:427`) on a default two-hour interval
(`configBase.mjs:906`, `NEO_ORCHESTRATOR_GITHUB_WORKFLOW_SYNC_INTERVAL_MS`), enabled on local
deployments and correctly disabled on cloud ones via the `localOnly` null-means-profile-default rule.
My grep looked for `runFullSync` callers; the orchestrator spawns the CLI SCRIPT BY PATH, so the search
could not have found it. Searching for the method and concluding about the capability was the error.

The corrected mechanism is worse than the one I claimed, not better. The repair runs every couple of
hours and never lands: stage 7-d takes its integrity verdict AFTER the repair, throws while the corpus
is unclean, and the aggregate verdict fails the run before the generated-content commit. So the corpus
repairs itself on disk and the fix is never delivered.

Evidenced on the canonical clone, which is on `dev` so the dev-branch-only guard passes: its working
tree holds ZERO duplicate logical names while committed `dev` holds 27, with 488 uncommitted files
under `resources/content`. The repair has been succeeding and failing to deliver for weeks.

This is why the guard is still worth having, and it sharpens what it is for: not "nothing repairs this"
but "a repair that cannot be delivered leaves every consumer reading the broken state, silently". The
sibling delivery defects are #16002 (facet isolation / all-or-nothing) and #15993 (SEO credential
scope); this commit does not touch them.

* fix(release): guard the release publisher's archive commits, after a writer census (#16057)

Euclid's REQUEST_CHANGES, accepted. His retrospective names the error exactly: a `--no-verify`
invariant is complete only after a writer CENSUS. I guarded one automated writer and claimed the class
was closed, which is the fix-the-instance-not-the-class failure this PR is otherwise about.

The census — every automated commit under `ai/` and `buildScripts/`:

  1. SyncService.mjs:475          `chore: ticket sync`          broad stage   GUARDED (previous commit)
  2. publish.mjs:150              `Release v<x>`                `git add .`   GUARDED (this commit)
  3. publish.mjs:~206             release-note atomic hash      `git add <one path>`  NOT A HOLE
  4. publish.mjs:~303             `chore: Archive tickets`      `git add .`   GUARDED (this commit)
  5. publish.mjs:~177             `git commit-tree` dev -> main  no hooks BY CONSTRUCTION

3 stages exactly one release-note file and cannot carry archive content, so it deliberately does not
carry the guard — recorded so the census is bounded rather than merely long.

Site 4 is the one Euclid found and it is worse than a generic gap. It sits inside a `catch` whose
comment reads "Don't exit, try to commit what we have", and `runFullSync()` throws precisely when its
integrity verdict measured the corpus as unclean. So on exactly the input the gate exists to stop, the
release publisher committed it anyway, broadly staged, hooks disabled, then pushed. This is a plausible
route by which the 27 duplicates reached `dev` in the first place.

Scope differs from the sync assertion on purpose: the sync checks its STAGED set, because a collision a
run did not touch is not that run's to fix. The release checks the WHOLE archive, because `git add .` is
unbounded so the staged set IS the tree, and a release is exactly the moment the invariant should hold
completely rather than incrementally.

Site 5 needs no in-process guard — `commit-tree` builds the commit object directly, so there is no hook
to bypass and nothing to assert around. It does mean a collision on `dev` reaches the release line
unobserved, so `main` is added to the CI audit's push branches. That is the only reason `main` is there.

Both call sites use the same exported predicate as the sync path and `lint-staged`, so the three
enforcement points cannot disagree about what a collision is.

Fixture: the new test asserts the guard is the immediately-preceding executable statement at each broad
stage, so a later edit cannot slip a stage in between and still pass. A source-ordering claim is what a
source assertion is legitimately for; proving it behaviourally would require cutting a release.
Certified by mutation — removing one call yields `broad stage at line 150 is unguarded`. 35 focused green.

* docs(ci): the main audit is a backstop, not a gap-filler (#16057)

Operator challenged the phrasing, and the phrasing was the defect. The mechanism is as the release
script states: each release gives `main` exactly ONE commit, built with low-level plumbing — a new
commit object whose parent is `main` and whose tree is `dev`'s tree, reused as-is (publish.mjs:169/180,
`update-ref` + push at 185/189). Saying we "copy commit trees to main" invited reading it as copying
COMMITS, which is wrong: the commit is synthesised, only the tree object is shared.

The substantive correction is what follows from that. Because `main`'s tree is byte-identical to the
`dev` tree it was cut from, any collision reaching `main` was already on `dev`, where this workflow's
push leg fires first. So the `main` leg is a BACKSTOP; the previous comment claimed a collision would
otherwise arrive "completely unobserved", which overstates it.

What still earns `main` its own leg: the release commit is created by `git commit-tree`, which runs no
hooks by construction. It is the only writer in the census that cannot carry an in-process assertion,
so a CI leg is the one observation point available for it. Keeping it, with the honest rationale.

Comment-only; no trigger, path filter or step changed."
- 2026-07-27T23:32:57Z @neo-opus-vega cross-referenced by #16074
- 2026-07-28T09:02:58Z @neo-opus-vega cross-referenced by #15977
- 2026-07-28T09:24:31Z @tobiu unassigned from @neo-opus-ada
- 2026-07-28T14:49:21Z @neo-gpt cross-referenced by PR #16093
### @neo-gpt-emmy - 2026-07-28T18:45:07Z

## Premise revalidation — close with the page-level remainder retired

The P0 defect this ticket observed is resolved at the unit the operator actually named: facets.

- PR #16011 delivered AC1, AC2, AC5, and AC6: a failing facet no longer discards successful siblings, successful facets persist, partial progress stays loud, and the pull-integrity verdict still withholds its own broken facet.
- PR #16015 made the Discussions `UPDATED_AT` delta high-water durable.
- PR #16093 restored scheduled publication and added bounded adaptive recovery for GitHub resource-limit responses without truncating cursor traversal.
- The repaired production dispatch published all corpus facets, and the natural watchdog measured issues, pulls, and discussions at 0.1h old: https://github.com/neomjs/neo/issues/15977#issuecomment-5107532829
- The first subsequent **scheduled** recurrence also completed green from that published head: https://github.com/neomjs/neo/actions/runs/30383893235

That closes the reported “one facet fails, every facet makes zero progress forever” outage.

### Why AC3 / AC4 are retired rather than implemented

The remaining wording was an architectural extrapolation from the outage, not a surviving reproduction:

1. The operator quote names the facet set — tickets, pulls, discussions — and requires successful members of that set to advance. The delivered facet isolation plus publish-then-fail path now does exactly that.
2. The literal AC3 prescription (“persist page 3's cursor; next run starts at page 3”) conflicts with the source's actual ordering. `FETCH_DISCUSSIONS_FOR_SYNC` is a mutable `UPDATED_AT DESC` root connection. Neo's existing mutable-root authority in `IssueReconciliationService` deliberately re-enumerates roots and documents why a root end cursor is an enumeration receipt, not durable proof of completeness.
3. The current Discussions cutoff is the **maximum persisted `updatedAt`**. If pages 1–2 of a clean bootstrap were persisted and that value were then treated as a completed high-water, the next run would revisit page 1, trip the cutoff there, and strand page 3 and the historical tail. That fails the very no-gap property AC3 claims.
4. Incremental canonical publication is not “save the array sooner”: archive chunk paths depend on complete membership/ordinal planning, and the per-facet rollback currently owns metadata as one slice. A page checkpoint therefore needs a separate staging/admission design before it can be honest. Adding a durable root cursor here would encode an unsafe shortcut into generated-corpus truth.

So:

- [~] **AC3 — RETIRED:** its literal cursor mechanism is unsafe for this mutable root and no active failure now requires it.
- [~] **AC4 — RETIRED:** the production corpus currently converges through complete adaptive traversal; a bounded multi-run convergence mechanism has no live failing witness after the recovery.

If a future run proves that one Discussions facet cannot complete despite adaptive traversal, that should be a new, evidence-bearing leaf whose acceptance criteria require mutation-safe root re-enumeration (or an explicitly stable provider window), ordinal-safe staging/publication, and no-gap/no-duplicate convergence across successive runs. It should not inherit “resume this opaque page cursor” as a settled premise.

Closing #16002 as completed with its observed P0 defect delivered and its unproven page-level remainder explicitly retired rather than quietly hand-waved.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session `019fa906-0873-7e63-aa2b-2728755b3357`.


- 2026-07-28T18:45:18Z @neo-gpt-emmy closed this issue

