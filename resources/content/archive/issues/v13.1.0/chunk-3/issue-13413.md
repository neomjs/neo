---
id: 13413
title: First-widget evidence pane + deterministic grid render-together
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-06-16T07:34:55Z'
updatedAt: '2026-06-16T08:32:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13413'
author: neo-opus-ada
commentsCount: 0
parentIssue: 13349
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-16T08:32:25Z'
---
# First-widget evidence pane + deterministic grid render-together

## Context

Split from #13355 per the #13409 cross-family review (close-target remediation, `pr-review/references/close-target-remediation.md`). #13355 was re-scoped to consume the #13362/#13363 Neural-Link-created grid path; PR #13409 delivered the **deterministic** half — the safe evidence-pane UI + a local blueprint-driven grid render-together — but NOT the NL-create-path provenance integration. This leaf is the fully-delivered close-target for that deterministic slice; #13355 stays open for the NL-created-grid provenance residual.

Release classification: post-release Agent Harness product line → boardless.

## The Problem

H2 needs the visible evidence/provenance surface (request + response + accepted-blueprint metadata) rendered safely, alongside a live grid, as a deterministic + smoke-testable slice — independent of the harder NL-create-path provenance integration. Delivering this slice as its own leaf lets it close truthfully while the NL-provenance integration (#13355's residual) is designed separately, rather than overclaiming #13355's full re-scoped intent.

## The Architectural Reality

- `apps/agentos/childapps/widget/util/blueprintEvidence.mjs` — the safe allowlist projection (scalar metadata only, fail-closed).
- `apps/agentos/childapps/widget/view/EvidencePane.mjs` — request/response/metadata via safe vdom `text` nodes (no `html`/`innerHTML`).
- `apps/agentos/childapps/widget/view/Viewport.mjs` — mounts the EvidencePane + a `Neo.grid.Container` from ONE shared deterministic blueprint.
- Tests: `blueprintEvidence.spec.mjs` + `view/EvidencePane.spec.mjs` (safe-render source assertion) + `test/playwright/e2e/FirstWidgetEvidencePane.spec.mjs` (render-together).
- Distinct from #13362/#13363 (the NL-create-path proof, which targets `examples/grid/bigData`).

## The Fix

Delivered by PR #13409 (re-pointed to this leaf):
- The safe blueprint-evidence projection (allowlist, fail-closed).
- The EvidencePane (safe vdom text, fail-closed on invalid blueprint).
- The Viewport wiring: EvidencePane + a deterministic-blueprint grid (render-together), one shared blueprint = the provenance link.
- Unit coverage (valid/invalid/safe-render source) + a render-together e2e.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Evidence pane (request/response/metadata) | #13349, ADR 0020, #13018 | Safe vdom text only; deterministic request + response + accepted-blueprint metadata | invalid blueprint → bounded rejected-state text | JSDoc/@summary | unit + e2e (shipped green) |
| Blueprint-evidence projection | #13355 blueprint contract | scalar metadata only (schema/title/col-count/row-count); allowlist | unknown keys/malformed → fail closed | JSDoc/@summary | unit, 4 tests (shipped) |
| Deterministic grid render-together | ADR 0020 | one shared blueprint drives both the evidence pane + a live grid | n/a (deterministic) | JSDoc | render-together e2e (shipped) |

## Decision Record impact

Aligned with ADR 0020. Split from #13355 (does NOT supersede it — #13355 retains the NL-create-path provenance scope).

## Acceptance Criteria

- [ ] Evidence pane renders deterministic request + response + accepted-blueprint metadata via safe vdom text (no unsafe HTML).
- [ ] Invalid evidence fails closed to a bounded visible state.
- [ ] A live grid renders alongside, driven by the SAME deterministic blueprint (render-together).
- [ ] Unit coverage: valid/invalid evidence + safe-render source assertion.
- [ ] Render-together e2e: evidence pane + grid render together.

(all delivered by PR #13409)

## Out of Scope

- The NL-create-path grid provenance integration (tying the evidence pane to a grid created via the #13362/#13363 `create_component` seam) — stays on #13355.
- LLM / streaming / persistence / git-write / Electron.

## Related

- Parent epic: #13349
- Split from: #13355 (retains the NL-provenance residual)
- Delivered by: PR #13409
- Distinct foundation: #13362 / #13363 (NL-create-path proof)

Origin Session ID: 47b6dbc0-7673-4ad3-a9f5-bef3b606c56b


## Timeline

- 2026-06-16T07:34:57Z @neo-opus-ada added the `enhancement` label
- 2026-06-16T07:34:57Z @neo-opus-ada added the `ai` label
- 2026-06-16T07:34:57Z @neo-opus-ada added the `architecture` label
- 2026-06-16T07:36:48Z @neo-opus-ada cross-referenced by PR #13409
- 2026-06-16T07:36:50Z @neo-opus-ada cross-referenced by #13355
- 2026-06-16T07:36:57Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-06-16T07:37:50Z @neo-opus-ada added parent issue #13349
- 2026-06-16T08:22:58Z @neo-opus-ada referenced in commit `5591cbf` - "fix(agentos): Neo.isObject guard + real grid-row render proof (#13413)

Operator review on PR #13409:
- blueprintEvidence: replace the verbose null/typeof/Array guard with Neo.isObject (same intent, stricter — rejects class instances too).
- FirstWidgetEvidencePane e2e: now asserts the grid renders all 12 content cells (3 rows x 4 cols) + each row value, auto-waiting past the async render. The prior toContainText-only check passed against a stale reused dev-server and never validated a real rendered row — a false green. Verified 3x repeat-each green against a fresh server."
- 2026-06-16T08:32:25Z @tobiu closed this issue
- 2026-06-16T08:32:26Z @tobiu referenced in commit `c8255b1` - "feat(agentos): first-widget evidence pane + live grid render-together (#13355) (#13409)

* feat(agentos): safe first-widget blueprint-evidence projection (#13355)

First increment of the H2 first-widget transcript/evidence leaf: the safe blueprint-evidence projection. Projects an accepted grid blueprint to scalar metadata only (schema, title, columnCount, rowCount) — never the raw columns/rows arrays or any executable string — so the evidence pane can show provenance without rendering generated content as code. Allowlist-based: non-object input, unexpected keys, or missing/wrong-typed metadata fail closed to a bounded {accepted:false, reason} state. 4 unit specs green (valid projection + no-leak + 3 fail-closed paths). Next: the EvidencePane view component + Viewport wiring + local render smoke.

* feat(agentos): H2 first-widget evidence pane component (#13355)

Built on the StrategyCardPanel safe-render idiom (reactive _-configs + afterSet + vdom text nodes, never html/innerHTML); consumes the blueprint-evidence projection so only safe scalar metadata reaches the view, and fails closed to a bounded rejected-state line on invalid evidence. WIP increment. Live render verification is whitebox-e2e: Node-unit component construction hits the known main-thread-stub limit (getController / getConfigInstanceByNtype), so a Node spec is the wrong harness. The render-together-with-grid AC is blocked by the #13353/#13354 first-widget grid-pane foundation, which closed-unmerged (only the bare Viewport.mjs landed).

* feat(agentos): wire first-widget evidence pane + live grid render-together (#13355)

Mounts the EvidencePane + a live grid into the AgentOSWidget child-app Viewport, both driven from ONE deterministic blueprint (the provenance point: the evidence pane's metadata describes the exact blueprint that produced the grid below it). Adds the safe-render source assertion + the render-together e2e smoke.

* fix(agentos): Neo.isObject guard + real grid-row render proof (#13413)

Operator review on PR #13409:
- blueprintEvidence: replace the verbose null/typeof/Array guard with Neo.isObject (same intent, stricter — rejects class instances too).
- FirstWidgetEvidencePane e2e: now asserts the grid renders all 12 content cells (3 rows x 4 cols) + each row value, auto-waiting past the async render. The prior toContainText-only check passed against a stale reused dev-server and never validated a real rendered row — a false green. Verified 3x repeat-each green against a fresh server.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-16T09:31:55Z @neo-opus-ada cross-referenced by PR #13416
- 2026-06-16T12:23:17Z @neo-opus-ada cross-referenced by PR #13437
- 2026-06-16T13:05:45Z @neo-opus-ada cross-referenced by #13438
- 2026-06-16T15:29:47Z @neo-opus-ada cross-referenced by PR #13442

