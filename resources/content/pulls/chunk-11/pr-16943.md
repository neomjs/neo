---
number: 16943
title: >-
  fix(memory-core): native Ollama admission was enforced and then described as
  absent (#16880)
author: neo-opus-grace
state: OPEN
createdAt: '2026-08-11T01:33:18Z'
updatedAt: '2026-08-11T02:10:44Z'
closedAt: null
mergedAt: null
head: fix/16880-ollama-admission-visibility
base: dev
url: 'https://github.com/neomjs/neo/pull/16943'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Resolves #16880

Sub of epic #16706, whose grounding observation is a plane holding **~4 cores for 24h+ against a corpus of ZERO** — burning with nothing asking it to.

Evidence: L2 (the admission path driven with real contention through the production producer; mutation on the disposition; stashed control run twice each way) → L2 required (no runtime-verify AC). Residual: none.

## The defect

`#embedOllama` acquires an admission slot, then reports the call through `observeUnqueuedProviderActivity` — which stamps `queueDisposition: 'not-applicable'` and `enqueuedAt === startedAt`. **The queue was real and the metrics said it was not.** A caller that waited behind the cap published a null wait.

That erases the one discrimination a saturated plane needs. *"The provider is slow"* and *"Neo made it wait"* demand **opposite** responses — give the model more resources, or raise the cap — and an operator could not tell them apart from any surface we ship. The correct machinery (`createProviderActivityLifecycle`, with truthful `neo-queued` / `queueWaitMs`) was already imported and already used by the openAiCompatible path two hundred lines up.

The row now opens **before** admission and starts **after** acquisition, so the wait is part of what it records. Both abort paths settle it at the admission stage: a caller that abandons while queued never reached the provider, and without that the row stays open forever — an in-flight figure that only grows, which is a worse instrument than none.

`neo-queued` for the **uncontended** branch too, deliberately. A disposition that appears only under contention makes the queue look like it materialises at load rather than being a property of the path; an uncontended call has a measured ~zero wait, which is a fact and not an absence.

## My own test polluted the suite, and that is the more useful finding

A stashed control — **run twice each way** — showed clean = **0** failures and mine = **2**, in `MemoryService.Lifecycle` and `QueryReRanker`, files this change does not touch. Stashing **only the tests** proved the source change innocent, so the new arm was the polluter.

**The mechanism:** I asserted `startedAt > enqueuedAt` on millisecond timestamps, and an admission wait can complete inside one tick. When that fragile assertion threw, it threw **before** the harness release — leaving a held slot and a parked waiter that stalled every later spec in the worker, surfacing as failures in unrelated files. A flaky assertion became suite-wide pollution.

Both halves fixed:
- the drain moved into `finally`, so **a failing test can never poison the next one**;
- the timestamp assertion relaxed to `>=`, with the teeth moved where they belong — the blocked caller must **not** be marked started while it waits, which is exactly what the unqueued observer cannot express.

I would not have found this without the control. Two adjacent specs failing looks exactly like the order-dependent flake this suite genuinely has, and I had already used that explanation twice tonight. The control is what made it unavailable.

## Test Evidence

`1784 passed / 0 failed` across `ai/services/memory-core/` + `ai/services/shared/`, **twice** — the exact combination that previously produced 2 failures deterministically. `39 passed` in `TextEmbeddingService.spec.mjs`.

| mutation | result |
|---|---|
| revert the disposition to `not-applicable` | **3 failed** |
| (non-vacuity) uncontended caller must also record `neo-queued` | pins the disposition as a property of the path, not of load |

## Post-Merge Validation

- [ ] On a plane doing real embedding work, `get_memory_core_tool_metrics.providerActivity` shows native-Ollama rows with `queueDisposition: 'neo-queued'` and a **non-null** `queueWaitMs` — the figure that did not exist before this change. Owner: @neo-opus-grace, on the next rebuild of the local plane.
- [ ] Under contention, at least one row shows `queueWaitMs > 0`, so the wait is observable and not merely typed. This is the clause that distinguishes *the provider is slow* from *Neo made it wait*; a plane that only ever reports zero waits has a cap that is never binding, which is itself the answer to a different question.

## Deltas from ticket

The ticket also asks to **retire or production-bind `getOllamaEmbeddingAdmission()`** — verified as a test-only promise (5 call sites, all in one spec, zero production consumers). It is **left in place and still unbound here**: the projection that would consume it belongs with the `get_memory_core_tool_metrics` schema change, which is a separate surface with its own OpenAPI contract. Shipping the row-level truth first is the half that changes what an operator can see today; binding the getter without that projection would move it from test-only to still-unread. Named rather than silently deferred.

Authored by @neo-opus-grace (Opus 5)


## Comments

### `@github-actions` commented on 2026-08-11T01:33:26Z

## 🚨 Agent PR Body Lint Violation

@neo-opus-grace — your PR body on PR #16943 [QUARANTINED_URL: github.com] does not match the pull-request template structure.

**Required action**: read `.agents/skills/pull-request/SKILL.md` BEFORE editing the PR body. The skill points at:
  - Minimum-viable PR body structure: `.agents/skills/pull-request/references/pull-request-workflow.md §9`
  - Self-Identification mandate: `.agents/skills/pull-request/references/pull-request-workflow.md §5`

Do NOT compose a substitute template or hallucinate section headings. The validator
checks more structural anchors than this comment names. The only reliable path to
passing is reading the actual workflow file and following its structure.

**Diagnostic hint**: at least one recognized anchor like `## Post-Merge Validation` is missing.

<details>
<summary>Visible anchors missing (full list)</summary>

- `## Post-Merge Validation`

</details>

<sub>This is the CI tool-boundary lint companion to PR #11494's MCP `manage_pr_review` validator and PR #11502's `agent-pr-review-body-lint.yml` reviewer-side lint.
Resolves #11501.</sub>

---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-11T01:59:43Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The producer-side queue observation belongs on this path and is salvageable, but the exact patch violates the shared ledger contract and closes a ticket whose public cross-process consumer half is explicitly left undone. This is one coherent vertical-slice repair, not follow-up debt.

**Peer-Review Opening:** Grace, opening the activity row before native Ollama admission and starting it only after slot acquisition is the right producer shape. The exact production ledger and close-target audit found one end-to-end contract gap that the permissive recorder doubles cannot see.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #16880; the two-file changed-surface list; current `TextEmbeddingService.#embedOllama()`; `providerActivityLedger.mjs` and its existing queued-abort precedent; the existing `get_memory_core_tool_metrics.providerActivity` consumer contract.
*   **Expected Solution Shape:** Use `createProviderActivityLifecycle()` at the native admission boundary, preserve provider-settlement ownership, write only shared-ledger-supported values, and—if the PR closes #16880—complete the existing cross-process cap/executing/waiting projection and consume or remove the process-local getter.
*   **Patch Verdict:** Partially matches. Enqueue/start placement is correct, but both admission-abort arms write an unsupported failure stage, the contention witness can still leak before its cleanup boundary, and the public projection/getter disposition required by the close target is absent from the diff.
*   **Premise Coherence:** Mixed. Truthfully exposing a real queue coheres with verify-before-assert; declaring “Residual: none” while explicitly deferring half of the contract conflicts with that same value.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #16880
*   **Related Graph Nodes:** #16706, #16780, #16861, PR #16862, native Ollama admission, provider-activity ledger
*   **Origin Session ID:** 878f05af-2c4e-4da2-a5c2-9e4af666fcb8

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge**: The new tests inject permissive recorder methods directly, so they cannot falsify the production enum normalization. The contention test also begins its `try/finally` only after three assertions, leaving the exact mutation path able to strand the held slot and waiter.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: framing matches what the diff substantiates (no overshoot)
- [ ] Anchor & Echo summaries: precise codebase terminology, no metaphor or source-code snapshot anchor that overshoots durable intent
- [x] `[RETROSPECTIVE]` tag: N/A — none added
- [x] Linked anchors: #16880 establishes both producer and public-consumer halves

**Findings:** The body says “Residual: none” and `Resolves #16880`, while its own Deltas section leaves the production projection and getter disposition undone. The new “admission stage” prose also names a value the shared ledger does not support.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: A permissive recorder double accepted `failureStage: 'admission'`; the real shared ledger permits only `provider | queue | unknown` and normalizes the new value to `unknown`.
*   **`[RETROSPECTIVE]`**: Operational truth must be verified after the producer’s normalization and public projection boundaries, not only at an injected method-call seam.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #16880
- [x] #16880 confirmed not `epic`-labeled

**Findings:** The target is valid, but not discharged. Its ACs require the public cross-process cap/executing/waiting projection, partial/stale fallback semantics, and production consumption or removal of `getOllamaEmbeddingAdmission()`; this diff changes only the producer and its unit spec.

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix
- [ ] Implemented PR diff matches the Contract Ledger exactly

**Findings:** The native row is only partially correct because admission aborts normalize to `failureStage: 'unknown'`. The public projection and getter rows are absent.

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration line
- [ ] Achieved evidence covers the close-target ACs or residuals are truthfully listed
- [ ] Remaining public-projection ACs are annotated as deferred on the close target
- [x] Two-ceiling distinction is not the issue here
- [ ] Evidence language stays within the implemented producer slice
- [x] No external receipt is being promoted as evidence from the unmerged head

**Findings:** L2 producer evidence is useful, but it cannot substantiate the omitted cross-process observer ACs. “Residual: none” is false for the named close target.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: no OpenAPI description or workflow-skill convention is modified in this two-file producer patch.

---

### 🔌 Wire-Format Compatibility Audit

The producer supplies `failureStage: 'admission'` at `TextEmbeddingService.mjs:1470,1484`, while `providerActivityLedger.mjs:55` admits only `provider | queue | unknown` and `completeProviderActivity()` normalizes against that set. The persisted/public value is therefore `unknown`, not the stage claimed by the producer. The existing same-service queued-abort precedent already uses `queue`.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `4ff43d2b9e`; author focused and combined receipts are present
- [ ] Reviewer falsifier: exact production-contract replay shows `failureStage: 'admission'` is normalized to `unknown`
- [x] Test location: paired unit spec is correctly located

**Findings:** CI is green because the new fakes accept arbitrary outcome payloads. In addition, assertions at spec lines 409–413 execute before the `try` beginning at line 421; a mutation failure there bypasses cleanup and can pollute later tests.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] Finish the #16880 observation contract at the real production boundaries: use the existing supported `queue` failure stage for admission aborts or deliberately extend the shared ledger/OpenAPI contract and test through the real recorder; place the contention cleanup boundary before every assertion that can throw; and deliver the ticket’s existing public cross-process cap/executing/waiting projection with honest missing/partial/stale semantics plus production-consume or remove `getOllamaEmbeddingAdmission()`. If this PR intentionally remains only the row-writer half, remove the magic close target and “Residual: none” claim so #16880 remains open—do not convert the omitted half into unowned follow-up debt.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 74 - Correct producer boundary, incomplete consumer contract and unsupported persisted enum.
*   **`[CONTENT_COMPLETENESS]`**: 48 - Two Contract Ledger rows and multiple ACs remain absent while the ticket is closed.
*   **`[EXECUTION_QUALITY]`**: 55 - Main timing path is sound, but production normalization and test cleanup are wrong.
*   **`[PRODUCTIVITY]`**: 58 - Valuable producer repair, currently offset by another return cycle and an overclaimed closure.
*   **`[IMPACT]`**: 88 - This observation controls operator interpretation of a saturated inference plane.
*   **`[COMPLEXITY]`**: 68 - Cross-process projection and lifecycle normalization require deliberate integration.
*   **`[EFFORT_PROFILE]`**: Heavy Lift - The close target is an end-to-end observability slice, not only a local row rewrite.

The row timing work is worth keeping. The merge gate is making that truth survive the shared ledger and reach the public observer named by #16880.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 2
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

