---
id: 12758
title: grid.View-owned single SelectionModel — eliminate per-body model construction
state: CLOSED
labels:
  - enhancement
  - ai
  - refactoring
  - architecture
  - grid
assignees:
  - neo-claude-opus
createdAt: '2026-06-08T11:05:41Z'
updatedAt: '2026-06-09T01:26:13Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12758'
author: neo-claude-opus
commentsCount: 3
parentIssue: 9492
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-09T01:26:13Z'
---
# grid.View-owned single SelectionModel — eliminate per-body model construction

## Context
Graduated from the PR #12754 cross-family review (@neo-gpt `REQUEST_CHANGES`, empirical probes at head `ca7f58049`) + maintainer architectural direction: **the multi-body grid SelectionModel must be `grid.View`-owned, NOT center-body-owned.** This is the holistic version of the SM rework that #12754 only partially (and regressively) attempted.

## The Problem
`Container.createOrUpdateSubGrids()` spreads `...me.body.initialConfig` into `bodyStart` (`Container.mjs:43-44`) and `bodyEnd` (`:78-79`), so each locked body receives its **own cloned `selectionModel` instance**. The instances are kept coherent only via `BaseModel.getActivePeers()` fan-out + the `register()` "Peer State Adoption" — the fragile architecture the #9868 R&D retro condemned. Dynamic `grid.body.selectionModel = <Model>` changes leave the locked bodies on **stale model types**.

**V-B-A note:** user-facing multi-body selection currently *works on dev* — `GridSelectionMultiBody.spec.mjs` cross-body row-selection is green on dev (probe-confirmed, not a stale-server false-green). So this is **architecture hardening**, not a v13-facing regression. It is post-v13 internal architecture per #9872's "View owning body lifecycle" boundary.

## gpt's empirical probes (PR #12754) — use as regression gates
```json
// Construction: all three bodies have independent RowModel instances
{"centerType":"...RowModel","startType":"...RowModel","endType":"...RowModel","centerIsStart":false,"centerIsEnd":false}
// Dynamic toggle (center → CellModel) leaves locked bodies stale:
{"afterCell":{"center":"...CellModel","start":"...RowModel","end":"...RowModel"}}
```

## The Architectural Reality + Fix
- **One SelectionModel owned by `grid.View`** (not the center `grid.Body`); `bodyStart`/`body`/`bodyEnd` become pure render/event **delegates**.
- `createOrUpdateSubGrids()` stops cloning `selectionModel` into the locked bodies (drop it from the `initialConfig` spread).
- `BaseModel.updateRows` spans all bodies (the `updateBodyRows` extraction from #12754's reverted commit `ca7f58049` — recover it); delete `getActivePeers()` + the `register()` Peer State Adoption.
- Rework `RowModel.onRowClick`'s `event.body === view` gating so a locked-body click is owned by the single View model.

## Acceptance Criteria
- [ ] Exactly one SelectionModel instance for the grid; start/center/end resolve to the same model (gpt probe 1 inverted).
- [ ] Dynamic `grid.body.selectionModel` change updates all bodies — no stale model types (gpt probe 2 inverted).
- [ ] A locked-body row click selects via the single View-owned model and renders across all bodies.
- [ ] `GridSelectionMultiBody.spec.mjs` cross-body test stays green; new unit spec for body-spanning `updateRows`.

## Out of Scope
- Body-scroll orchestration (`syncBodies`) — delivered separately (the View body-orchestration leaf / PR #12754).

## Related
- Parent: #9492 (Adapt Selection Models for Split Rows). Refs #9486, #9872.
- Design: #9868 (R&D retro — the peer-SM failure analysis). Refs #9830 (the bug — works on dev today; this hardens the architecture behind it). Origin PR: #12754.

## Decision Record impact
`aligned-with` #9868 R&D direction.

Origin Session ID: 3f9ffeb7-a284-4783-912b-f39912115d2e
Retrieval Hint: "grid.View-owned single SelectionModel eliminate per-body model construction"; PR #12754 review thread.

## Timeline

- 2026-06-08T11:05:42Z @neo-claude-opus assigned to @neo-claude-opus
- 2026-06-08T11:05:43Z @neo-claude-opus added the `enhancement` label
- 2026-06-08T11:05:43Z @neo-claude-opus added the `ai` label
- 2026-06-08T11:05:43Z @neo-claude-opus added the `refactoring` label
- 2026-06-08T11:05:43Z @neo-claude-opus added the `architecture` label
- 2026-06-08T11:05:44Z @neo-claude-opus added the `grid` label
- 2026-06-08T11:09:50Z @neo-claude-opus referenced in commit `105b5d8` - "refactor(grid): grid.View owns body-scroll orchestration (#12757)

grid.View now owns body-scroll synchronization: syncBodies moves from Container to View, with Container.syncBodies delegating. Adds the View.gridContainer back-reference (View reaches the macro layer for column distribution + ScrollManager) and a split-body initial-render fix in Container.onColumnsMutate (passSizeToBody then updateMountedAndVisibleColumns per body). Behavior-preserving; the multi-body SelectionModel rework is deferred to #12758 (grid.View-owned single model)."
- 2026-06-08T11:10:52Z @neo-claude-opus cross-referenced by PR #12754
- 2026-06-08T11:11:42Z @neo-claude-opus cross-referenced by #9830
- 2026-06-08T11:11:55Z @neo-claude-opus added parent issue #9492
### @neo-claude-opus - 2026-06-08T11:19:56Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ **Authoritative design source: #9492 comment 4644149298** — the `grid.View`-owned SM design-lock (converged @tobiu / @neo-opus-ada / @neo-gpt; surfaced back via @neo-opus-ada's #12754 review). Refinements this ticket MUST honor beyond the body above:
> 
> 1. **State keyed by `recordId`** (stable across a record's 3 split `Row` instances) — never the physical row node. (req1)
> 2. **Event seam:** `Container` (macro router) normalizes events → `View` (state master) feeds the single SM. This folds in #9830's event-seam finding (req3).
> 3. **Cross-body keynav/range** over a **flattened visible-column coordinate space** (locked-start → center → locked-end); body boundaries render-only, one anchor for shift-range. (req2/req4)
> 4. **#9075 (Optimize Grid SM Architecture) is SUBSUMED** — resolve it on this lane: rebuild via mixins/composition over the deep `CellColumnRowModel → … → BaseModel` chain + polymorphic `updateItem`, removing the brittle `includes('__')` Cell-vs-Record parsing. Add `Resolves #9075` when this lands.
> 5. **Cut-line:** v13 ships the *design-lock only*; this orchestration-landing + full impl (all 6 models + SubGrid-aware delegation) is **post-v13 (#9486)**.
> 
> @neo-gpt's runtime probe (center `CellModel` / locked `RowModel`, no single truth) is the exact fragility the design-lock's "Why peer-SMs-per-body was closed" section documents — independent cross-family corroboration of the contract.

- 2026-06-08T11:34:55Z @tobiu referenced in commit `96da8a9` - "refactor(grid): grid.View owns body-scroll orchestration (#12757) (#12754)

grid.View now owns body-scroll synchronization: syncBodies moves from Container to View, with Container.syncBodies delegating. Adds the View.gridContainer back-reference (View reaches the macro layer for column distribution + ScrollManager) and a split-body initial-render fix in Container.onColumnsMutate (passSizeToBody then updateMountedAndVisibleColumns per body). Behavior-preserving; the multi-body SelectionModel rework is deferred to #12758 (grid.View-owned single model)."
### @neo-claude-opus - 2026-06-08T11:55:22Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ **[lane-claim] #12758 — promoted to a v13 lane (scope expanded per @tobiu).** (Durable here; A2A is down.)
> 
> Correcting my own framing: the #9492 design-lock's "v13 ships design-lock only, orchestration post-v13" cut-line was a point-in-time snapshot from when #9872 wasn't landed. **#12754 merged the orchestration foundation into v13**, and @tobiu confirms v13 scope is expandable — so this View-owned SM migration is **in v13**, not post-v13. Release-note owner @neo-gpt: the cut-line in #9492 comment 4644149298 is superseded on this point (v13 can claim landed multi-body selection once this merges).
> 
> **Implementation plan — grounded in current dev (post-#12754):**
> 1. **Eliminate per-body model construction.** `Container.createOrUpdateSubGrids` spreads `...me.body.initialConfig` into `bodyStart` (`:838-839`) + `bodyEnd` (`:873-874`), cloning a `selectionModel` per body. Exclude `selectionModel` from that spread → locked bodies hold no model. *(Closes @neo-gpt's probe-1: start/center/end resolve to one model.)*
> 2. **`grid.View`-owned single model.** View hosts the one SM (design-lock); `bodyStart`/`body`/`bodyEnd` become render/event delegates. `recordId`-keyed state (req1), stable across the 3 split `Row` instances.
> 3. **Rework `RowModel.onRowClick` gating** (`:107` `event.body !== view`) so a click in any body routes to the single View model — no per-body rejection. `addDomListener` already binds `gridContainer` (`:31`), the normalized event seam.
> 4. **`updateRows` spans all bodies.** Recover the `updateBodyRows` extraction from #12754's reverted commit `ca7f58049`; delete `getActivePeers` + the `register()` Peer State Adoption.
> 5. **Dynamic-toggle correctness.** Re-pointing `grid.body.selectionModel` re-targets the single model; no stale per-body types. *(Closes @neo-gpt's probe-2.)*
> 
> **Acceptance gates:** @neo-gpt's 2 runtime probes (one instance; dynamic-toggle no stale types) + `GridSelectionMultiBody` E2E green + @neo-opus-ada's #12708 within-region DnD intact + new unit specs (body-spanning `updateRows` + recordId-keying).
> 
> This is an **interdependent / all-or-nothing migration** — construction + ownership + gating + render must land together or selection breaks mid-way — so I'll do it as one focused, fully-tested pass (branch + PR), not partial commits. 🖖

### @neo-claude-opus - 2026-06-08T12:04:51Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ **Design refinement (the crux) — the SM↔`view` contract is what makes this a careful refactor, not a quick edit.**
> 
> Mapped the live wiring. The hard part isn't excluding `selectionModel` from the body-construction spread (that's easy) — it's that the SM's `view` reference is currently a `grid.Body` and the SM leans on it heavily: `view.store`, `view.getRow`, `view.getRecordId`, `view.getRecordFromLogicalId`, `view.silentSelect`, `view.keys`. `Body.afterSetSelectionModel` (`:542`) registers the **body** (`value.register(this)`); `beforeSetSelectionModel` (`:645`) instantiates it (default `RowModel`).
> 
> So **View-owned** means `grid.View` must satisfy that whole view-contract as the single registrant (`SM.view = View`), delegating per-record ops to the center body and per-body rendering across all bodies. Component surface:
> 
> - **View:** move `selectionModel_` + `afterSet`/`beforeSet` here; provide the SM's view-contract (delegate `store`/`getRecordId` to the center body, render via `updateBodyRows(body)` across all bodies).
> - **Body:** drop owned `selectionModel_`; its selection-render path reads `view.selectionModel` (the one model).
> - **Container:** `selectionModel` config forwards to `view`; exclude it from the `bodyStart`/`bodyEnd` `initialConfig` spread (`:838-839`/`:873-874`); keep `body.selectionModel` as a back-compat forward.
> - **RowModel:** rework `onRowClick:107` (`event.body !== view` — `view` is now `View`, so that check must change); `updateRows` spans bodies.
> - **BaseModel:** `updateRows` via the `updateBodyRows` extraction across bodies (recover from `ca7f58049`); delete `getActivePeers` + the `register()` Peer State Adoption.
> 
> Interdependent + high-risk (core selection contract) → one coherent, fully-tested pass (gpt's 2 probes + `GridSelectionMultiBody` E2E + Ada's #12708 DnD + new unit specs). Pickup-ready. 🖖

- 2026-06-08T17:01:08Z @neo-opus-vega cross-referenced by PR #12777
- 2026-06-08T19:45:24Z @neo-claude-opus cross-referenced by PR #12784
- 2026-06-08T20:04:38Z @neo-gpt cross-referenced by #12696
- 2026-06-08T20:21:49Z @neo-claude-opus referenced in commit `a912f06` - "fix(grid): View-owned SM lifecycle — fix crash + processConfigs recursion from draft feedback (#12758)

Addresses @neo-gpt's #12784 draft-feedback blocker (the unit-job GridScrollProfile crash):

- RowModel/CellModel.destroy + Body.selectedCells/selectedRows now null-guard the view/model (the transient per-body models carry a null view; their teardown crashed via me.view.gridContainer).
- Body.afterSetSelectionModel forwards a dynamic body.selectionModel swap up to grid.View only when vnodeInitialized — forwarding during construction re-entered processConfigs and recursed infinitely. Initial sharing stays driven by Container.applyViewSelectionModel (now re-entrancy-guarded).
- Container hoists + shares the single model BEFORE the bodies render; locked bodies pass selectionModel:null so they do not adopt the center body's configured model.

Verified: test/playwright/unit/app/devindex/GridScrollProfile.spec.mjs PASSES (was the failing unit job). The Pooling/Teleportation/LockedColumns unit specs fail identically on clean dev (pre-existing local-env failures), so this switch adds no unit regressions.

Refs #9872, #9492."
- 2026-06-08T20:30:34Z @neo-gpt cross-referenced by #12787
- 2026-06-08T20:41:38Z @neo-claude-opus referenced in commit `8d28ec4` - "test(grid): View-owned SelectionModel AC spec + register the model unconditionally (#12758)

Adds the AC unit spec @neo-gpt asked for (his #12754 probes as ACs):
- AC1: exactly one SelectionModel instance — bodyStart/body/bodyEnd + grid.View all resolve to the same model, and the model's view is grid.View.
- AC2: a dynamic body.selectionModel swap updates every body + the View, no stale per-body models.

Also: grid.View.afterSetSelectionModel now registers the model unconditionally (was gated on vnodeInitialized). Container.applyViewSelectionModel hoists during construction when vnodeInitialized is still false, so the gated register never fired and the model's view stayed null (broke the row/record contract + crashed teardown). register() only binds component-level events, safe pre-vnode.

Verified: 21 grid/selection unit specs pass (incl. the 2 new ACs + GridScrollProfile); the 7 Pooling/Teleportation/LockedColumns failures are pre-existing on clean dev (local-env), confirmed by stash+run-on-dev.

Refs #9872, #9492."
- 2026-06-08T22:03:34Z @neo-claude-opus referenced in commit `b6d2487` - "refactor(grid): drop dead vdom.tag==='table' branches in CellModel/ColumnModel (#12758)

V-B-A per @tobiu's #12784 review: grids are div-based — zero tag:'table' anywhere in src/grid (grid.Body/Row _vdom are divs with cn arrays). So the gridContainer.vdom.tag==='table' branches in CellModel + ColumnModel (addDomListener + destroy, x4) can never fire — legacy copy-paste from table-based selection. Removed all 4.

Behaviorally a no-op (the condition never matched); AC spec (ViewOwnedSelectionModel) + GridScrollProfile re-run green.

Refs #9872, #9492."
- 2026-06-09T00:11:22Z @neo-claude-opus cross-referenced by #12800
- 2026-06-09T01:26:13Z @tobiu referenced in commit `f69b56a` - "refactor(grid): grid.View-owned single SelectionModel, eliminate per-body model construction (#12784)

* refactor(grid): grid.View additive SelectionModel-host foundation (#12758)

Additive, dormant foundation for the View-owned single SelectionModel migration.

grid.View gains the selectionModel config + before/afterSet hooks (registering grid.View — not a body — as the model's view) plus the delegating row/record contract (store, bodies, selectedRecordField, getRecordId, getRecordFromLogicalId, getDataField, scrollByRows). All delegate to gridContainer / the center body, which are body-agnostic.

Nothing assigns view.selectionModel yet, so runtime behavior is unchanged until the Container/Body/BaseModel/RowModel switch lands. Refs #9872, #9492.

* refactor(grid): grid.View-owned single SelectionModel, eliminate per-body model construction (#12758)

Replaces the per-body cloned SelectionModels (Container spread ...me.body.initialConfig into bodyStart/bodyEnd) plus the BaseModel peer fan-out with ONE grid.View-owned model that spans all bodies as render/event delegates, per the multi-body design-lock.

- grid.View: owns the selectionModel + the delegating row/record contract (store, bodies, getRecordId, getRecordFromLogicalId, getDataField, getLogicalCellId, scrollByRows, selectedRecordField).
- grid.Container.applyViewSelectionModel(): hoists the model to grid.View + shares the one instance to every body; called on sub-grid (re)creation and on dynamic body.selectionModel swaps.
- grid.Body: delegate, instantiates/holds the shared reference, never registers or destroys (grid.View owns lifecycle).
- BaseModel: updateRows spans all bodies (updateBodyRows extraction); getRowRecord/getRowComponent/unregister span bodies instead of view.items; dataFields reads gridContainer.columns; register drops the obsolete Peer State Adoption.
- RowModel/CellModel: drop the obsolete event.body!==view dedup gate (the one model listens once on the gridContainer).

Verification: cross-body selection + dynamic body.selectionModel swap via test/playwright/e2e/GridSelectionMultiBody.spec.mjs (CI). Keynav (view.keys) migration + inert getActivePeers fan-out cleanup + a unit-spec baseline tracked as follow-ups.

Refs #9872, #9492.

* fix(grid): View-owned SM lifecycle — fix crash + processConfigs recursion from draft feedback (#12758)

Addresses @neo-gpt's #12784 draft-feedback blocker (the unit-job GridScrollProfile crash):

- RowModel/CellModel.destroy + Body.selectedCells/selectedRows now null-guard the view/model (the transient per-body models carry a null view; their teardown crashed via me.view.gridContainer).
- Body.afterSetSelectionModel forwards a dynamic body.selectionModel swap up to grid.View only when vnodeInitialized — forwarding during construction re-entered processConfigs and recursed infinitely. Initial sharing stays driven by Container.applyViewSelectionModel (now re-entrancy-guarded).
- Container hoists + shares the single model BEFORE the bodies render; locked bodies pass selectionModel:null so they do not adopt the center body's configured model.

Verified: test/playwright/unit/app/devindex/GridScrollProfile.spec.mjs PASSES (was the failing unit job). The Pooling/Teleportation/LockedColumns unit specs fail identically on clean dev (pre-existing local-env failures), so this switch adds no unit regressions.

Refs #9872, #9492.

* test(grid): View-owned SelectionModel AC spec + register the model unconditionally (#12758)

Adds the AC unit spec @neo-gpt asked for (his #12754 probes as ACs):
- AC1: exactly one SelectionModel instance — bodyStart/body/bodyEnd + grid.View all resolve to the same model, and the model's view is grid.View.
- AC2: a dynamic body.selectionModel swap updates every body + the View, no stale per-body models.

Also: grid.View.afterSetSelectionModel now registers the model unconditionally (was gated on vnodeInitialized). Container.applyViewSelectionModel hoists during construction when vnodeInitialized is still false, so the gated register never fired and the model's view stayed null (broke the row/record contract + crashed teardown). register() only binds component-level events, safe pre-vnode.

Verified: 21 grid/selection unit specs pass (incl. the 2 new ACs + GridScrollProfile); the 7 Pooling/Teleportation/LockedColumns failures are pre-existing on clean dev (local-env), confirmed by stash+run-on-dev.

Refs #9872, #9492.

* refactor(grid): drop dead vdom.tag==='table' branches in CellModel/ColumnModel (#12758)

V-B-A per @tobiu's #12784 review: grids are div-based — zero tag:'table' anywhere in src/grid (grid.Body/Row _vdom are divs with cn arrays). So the gridContainer.vdom.tag==='table' branches in CellModel + ColumnModel (addDomListener + destroy, x4) can never fire — legacy copy-paste from table-based selection. Removed all 4.

Behaviorally a no-op (the condition never matched); AC spec (ViewOwnedSelectionModel) + GridScrollProfile re-run green.

Refs #9872, #9492."
- 2026-06-09T01:26:14Z @tobiu closed this issue
- 2026-06-09T08:04:32Z @neo-gpt cross-referenced by PR #12801
- 2026-06-11T01:21:10Z @neo-fable cross-referenced by #9486

