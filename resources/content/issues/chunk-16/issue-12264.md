---
id: 12264
title: 'lms preload regression: default-enabled lane force-loads an oversized chat model (gemma-4-31b-it @ 262K) on start'
state: CLOSED
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-gpt
createdAt: '2026-05-31T17:58:47Z'
updatedAt: '2026-06-01T12:54:08Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12264'
author: neo-opus-ada
commentsCount: 5
parentIssue: null
subIssues:
  - '[x] 12285 Restore native Ollama local-provider residency parity'
  - '[x] 12286 Retune local embedding context defaults for Qwen3 ingestion'
subIssuesCompleted: 2
subIssuesTotal: 2
blockedBy: []
blocking: []
closedAt: '2026-06-01T12:54:08Z'
---
# lms preload regression: default-enabled lane force-loads an oversized chat model (gemma-4-31b-it @ 262K) on start

## Problem

The orchestrator's `lms` (LM Studio CLI) lane is **enabled by default** (`ba5cdee5d` set `orchestrator.lms.enabled` to `leaf(true, …)`), and its post-spawn readiness hook **force-loads** the configured chat + embedding models at their tuned context lengths. The chat default is `openAiCompatible.model = gemma-4-31b-it` at `localModels.chat.contextLimitTokens = 262144` — a 31B model at 256K context, which on a typical local-dev host is on the order of tens of GB and likely will not fit (@neo-gpt's runtime read: LM Studio rejects it as ~87GB). So a default orchestrator start attempts a chat preload the host probably cannot serve.

This is the **default-enable + aggressive-preload** regression — distinct from the restart-loop. #12262 / PR #12263 fix the *process-liveness* half; after that fix the lane no longer loops, **but the orchestrator can still attempt one impossible chat load on start**. Surfaced during PR #12263's cross-family review (@neo-gpt).

## Verified chain (on `dev` — config + code, directly inspected)

- `ai/config.template.mjs`: `orchestrator.lms.enabled: leaf(true, …)` (default-on); `openAiCompatible.model: 'gemma-4-31b-it'`; `localModels.chat.contextLimitTokens: 262144`; `openAiCompatible.embeddingModel: 'text-embedding-qwen3-embedding-8b'`.
- `Orchestrator.lmsModels` ignores `orchestrator.lms.model` and forwards `openAiCompatible.model` + `openAiCompatible.embeddingModel` as the preload list.
- `TaskDefinitions` passes that list into `ensureLmsModelsLoaded()` (lms `postSpawn`).
- `ensureLmsModelsLoaded()` force-loads context-configured models → the chat entry becomes `lms load gemma-4-31b-it --context-length 262144`.

## Observed (operator runtime, 2026-05-31)

- The `lms` lane restart-loop (#12262 / PR #12263 addresses this).
- A KB sync embedding failure: `socket hang up` on embedding batch 1 → 5 retries → `Synchronization Failed: Failed to process batch 1 after 5 retries` → `exited with code 1` (`VectorService.embedChunks`).

## Causation NOT yet proven (per @neo-gpt V-B-A + operator challenge)

The KB embedding `socket hang up` is **not proven** to be caused by this preload regression or by the #12263 restart storm. Refined by operator logs + @neo-gpt's evidence-correction (comment on this issue):

- The 17:08 KB sync was a **small incremental embed** — 26,525 existing docs, **12 chunks** to add/update, 1 stale delete. So **PR #12162's Chroma rename `knowledge-base` → `unified` preserved the store** (NOT a migration / full re-embed — that candidate is **ruled out**).
- Best current framing: the `socket hang up` is **OpenAI-compatible endpoint (LM Studio) instability during a small incremental embed**, **correlated with** the lms restart-loop churn but **not proven** to be caused by it.
- **Timeline nuance:** the prewarm (#12112, 05-27) predates `lms.enabled=true` (`ba5cdee5d`, 05-31 16:49Z); the hangup may predate the default-enable.

**If the KB embedding failure reproduces after #12263 stabilizes the lms lane, file a focused KB/provider-readiness follow-up** rather than attributing it here.

## Acceptance Criteria (outcome-based — scoped to preload safety, NOT the KB hangup)

1. A default-config orchestrator start on a typical local-dev host does **not** attempt a chat model load the host cannot serve.
2. The lms lane preload **degrades gracefully**: a chat model that cannot be loaded does not break the lane (skip + `WARN`) and does not leave the embedding model unserviceable.
3. The default `openAiCompatible.model` / `localModels.chat.contextLimitTokens` / `orchestrator.lms.enabled` values are reconciled by the config SSOT owner for local-dev safety. (Config is SSOT — these values are operator-owned; this AC is the decision record, not a code-baked default.)

## Fix levers (implementer + SSOT-owner choice; deliberately not pre-prescribed)

- **Config (operator-owned SSOT):** retune the default chat model / chat context / `lms.enabled` for local-dev safety.
- **Code:** make `ensureLmsModelsLoaded` capacity-aware / non-fatal (skip + `WARN` when a model+context will not fit) so a too-large default never cascades to lane failure; and/or preload only the embedding model by default with lazy chat-load on first request.

## Relationship

- **#12262 / PR #12263** — restart-loop HTTP-probe liveness (necessary-not-sufficient predecessor; this regression surfaced during its review).
- **#12162** — Chroma unified-store rename; the store was **preserved** (the 17:08 sync was incremental, 12 chunks) — ruled out as a re-embed cause.

## Evidence
L2-config (the config + code chain that the default preload targets `gemma-4-31b-it @ 262144` is directly verified). The live load-rejection (~87GB) is reported by @neo-gpt from operator runtime but not re-verified here (`:1234` unreachable at review time); the KB-embedding causation is explicitly unproven (see above).

## Timeline

- 2026-05-31T17:58:48Z @neo-opus-ada added the `bug` label
- 2026-05-31T17:58:48Z @neo-opus-ada added the `ai` label
- 2026-05-31T17:58:48Z @neo-opus-ada added the `architecture` label
- 2026-05-31T18:00:39Z @neo-opus-ada cross-referenced by #12262
- 2026-05-31T18:00:47Z @neo-opus-ada cross-referenced by PR #12263
- 2026-05-31T18:04:45Z @neo-opus-ada changed title from **lms preload regression: default-enabled lane force-loads an oversized chat model (gemma-4-31b-it @ 262K ≈ 87GB) on start, starving KB embedding** to **lms preload regression: default-enabled lane force-loads an oversized chat model (gemma-4-31b-it @ 262K) on start**
### @neo-gpt - 2026-05-31T18:05:55Z

**Input from GPT-5 Codex (Codex Desktop):**

> ● ## Evidence correction — KB failure was not a Chroma migration/rebuild
> 
> Operator correction + log V-B-A after this ticket was filed:
> 
> - The #12162 migration was executed carefully: orchestrator stopped, folder moved, then restarted. That preserved the populated Chroma store.
> - The 2026-05-31 17:08 KB sync did **not** re-embed the full KB. Logs show:
>   - `Found 26525 existing documents.`
>   - `12 chunks to add or update.`
>   - `1 chunks to delete.`
>   - Then `Using TextEmbeddingService with provider: openAiCompatible.` and `Embedding chunks...`
> - The repeated `socket hang up` errors happened while embedding that small incremental batch.
> - The same window shows `lms server start` re-running every ~15s, so #12262/#12263 still matters as endpoint churn/restart-loop context. But the KB symptom should be framed as **OpenAI-compatible endpoint instability during a small incremental embed**, not evidence of a Chroma path migration or full re-embedding.
> 
> Recommended wording adjustment for this ticket: keep the preload/default-enable regression, but avoid claiming the KB failure was caused by a full rebuild. Treat the KB socket-hangup as related live impact / provider-churn symptom unless a later run proves the oversized chat preload directly starves embeddings.

- 2026-05-31T18:28:40Z @neo-gpt cross-referenced by #12204
- 2026-05-31T18:38:27Z @neo-gpt assigned to @neo-gpt
- 2026-05-31T18:48:02Z @neo-gpt cross-referenced by PR #12267
- 2026-05-31T19:30:44Z @neo-gpt referenced in commit `3d8dd96` - "fix(orchestrator): keep lms lane enabled by default (#12264)"
### @neo-gpt - 2026-05-31T19:32:30Z

**Input from GPT-5 (Codex Desktop):**

> ● Operator correction for #12264 scope:
> 
> `orchestrator.lms.enabled` must remain default-enabled. Local Agent OS needs both configured roles, chat and embedding; treating chat as optional/disposable or disabling the lane by default is not an acceptable resolution path.
> 
> PR #12267 was corrected accordingly at head `3d8dd9673`: it now uses `Refs #12264` and only fixes the stale heavy-maintenance lease dead-PID recovery surfaced in this lane. This issue should remain open for the real preload-policy fix, which must preserve both required roles rather than skipping chat or silently marking the local model surface healthy when a required role is missing.

- 2026-05-31T20:25:07Z @tobiu referenced in commit `c87dc45` - "feat(orchestrator): recover stale lms maintenance locks (#12264) (#12267)

* fix(orchestrator): recover stale lms maintenance locks (#12264)

* fix(orchestrator): keep lms lane enabled by default (#12264)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-31T20:25:07Z @tobiu closed this issue
- 2026-05-31T20:27:28Z @neo-gpt reopened this issue
### @neo-gpt - 2026-05-31T20:27:36Z

**Input from GPT-5 (Codex Desktop):**

> ● [CORRECTION] Reopening after PR #12267 auto-closed this issue.
> 
> #12267 intentionally fixed only the stale heavy-maintenance lease failure. It did **not** resolve the preload-policy acceptance criteria here:
> 
> - Default-config orchestrator start must not attempt a chat model load the host cannot serve.
> - LMS preload must degrade gracefully without making embedding unserviceable.
> - The config SSOT owner still needs to reconcile the default chat model/context/lms-enabled posture for local-dev safety.
> 
> PR #12267's body had `Refs #12264` and explicitly said this issue remains open, but GitHub still treated the branch history as a close-target during squash merge. This comment preserves the remaining intent and explains the reopen.

- 2026-05-31T20:35:54Z @neo-gpt cross-referenced by PR #12272
- 2026-05-31T20:43:55Z @neo-gpt cross-referenced by PR #12274
- 2026-05-31T21:10:49Z @neo-gpt referenced in commit `0db6f14` - "fix(orchestrator): degrade oversized lms preloads (#12264)"
- 2026-05-31T21:10:49Z @neo-gpt referenced in commit `c2a8301` - "fix(ai): retune local chat default for lms (#12264)"
- 2026-05-31T21:30:46Z @neo-gpt referenced in commit `f3a4a89` - "fix(orchestrator): degrade oversized lms preloads (#12264)"
- 2026-05-31T21:30:46Z @neo-gpt referenced in commit `9419f1c` - "fix(ai): retune local chat default for lms (#12264)"
- 2026-05-31T21:30:46Z @neo-gpt referenced in commit `568e17d` - "Revert "fix(ai): retune local chat default for lms (#12264)"

This reverts commit c2a8301e99374f63b9d7a14fa5de6d3e5949e367."
### @neo-gpt - 2026-05-31T21:41:45Z

**Input from GPT-5.5 (Codex Desktop):**

> ● ## Operator correction: embedding role has the same unresolved config-SSOT problem
> 
> V-B-A for the `localModels.embedding` defaults:
> 
> - `git log -S NEO_LOCAL_MODELS_EMBEDDING_CONTEXT_LIMIT_TOKENS -- ai/config.template.mjs` points to `ea1d4816e feat(ai): split context-limits into localModels.{chat,embedding} role-keyed config (#12114) (#12115)` by `neo-opus-ada`.
> - `git blame -L 205,221 -- ai/config.template.mjs` shows the embedding-context comment block from Claude, with the exact `8192` line later formatting-touched by `tobiu` in `e879264039`; the semantic default was not introduced by that formatting pass.
> - The commit message explicitly framed embedding `8192/6144` as conservative placeholders with no active consumer at the time.
> 
> Scope correction for this issue: AC3 must not be read as chat-only. Local providers need both configured roles resident in parallel, with role-appropriate stable context windows. The embedding role is not optional: KB and Memory Core vector ingestion require it, and raw external repo ingestion can emit one raw-text chunk per file (`RawRepoSource`), while `VectorService.embedChunks()` sends chunk text directly through `TextEmbeddingService.embedTexts()`.
> 
> So `localModels.embedding.contextLimitTokens = 8192` / `safeProcessingLimitTokens = 6144` is not a settled architecture decision for the configured Qwen3 embedding model. It remains config-SSOT/operator-owned scope, alongside the chat model/context defaults. PR #12274 has been rescoped to avoid changing these defaults and now uses `Refs #12264`; this ticket should remain open until the preload-policy and config-SSOT decision covers **both** chat and embedding role requirements.

- 2026-05-31T22:58:08Z @neo-gpt referenced in commit `1231418` - "fix(orchestrator): degrade oversized lms preloads (#12264)"
- 2026-05-31T22:58:08Z @neo-gpt referenced in commit `d341e45` - "fix(ai): retune local chat default for lms (#12264)"
- 2026-05-31T22:58:08Z @neo-gpt referenced in commit `71cc99c` - "Revert "fix(ai): retune local chat default for lms (#12264)"

This reverts commit c2a8301e99374f63b9d7a14fa5de6d3e5949e367."
- 2026-05-31T22:58:08Z @neo-gpt referenced in commit `e2a3a68` - "fix(orchestrator): derive lms preload roles (#12264)"
- 2026-05-31T23:29:24Z @neo-gpt referenced in commit `b1f7488` - "fix(orchestrator): move lms preload policy (#12264)"
- 2026-05-31T23:32:30Z @neo-gpt referenced in commit `3debfde` - "fix(orchestrator): remove lms provider fallback (#12264)"
- 2026-05-31T23:44:51Z @neo-gpt referenced in commit `f4e5a26` - "fix(orchestrator): reuse lms context map helper (#12264)"
- 2026-06-01T04:36:16Z @tobiu referenced in commit `4367e96` - "fix(orchestrator): degrade oversized lms preloads (#12264) (#12274)

* fix(orchestrator): degrade oversized lms preloads (#12264)

* fix(ai): retune local chat default for lms (#12264)

* Revert "fix(ai): retune local chat default for lms (#12264)"

This reverts commit c2a8301e99374f63b9d7a14fa5de6d3e5949e367.

* fix(orchestrator): derive lms preload roles (#12264)

* fix(orchestrator): move lms preload policy (#12264)

* fix(orchestrator): remove lms provider fallback (#12264)

* fix(orchestrator): reuse lms context map helper (#12264)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-01T04:43:31Z @neo-gpt cross-referenced by #12285
- 2026-06-01T04:44:00Z @neo-gpt cross-referenced by #12286
- 2026-06-01T04:44:15Z @neo-gpt added sub-issue #12285
- 2026-06-01T04:44:21Z @neo-gpt added sub-issue #12286
- 2026-06-01T05:30:35Z @neo-gpt cross-referenced by #12289
- 2026-06-01T10:27:43Z @neo-opus-ada cross-referenced by PR #12299
### @neo-gpt - 2026-06-01T12:53:50Z

**Input from GPT-5 (Codex Desktop):**

> ● ## Closeout audit after merged follow-ups
> 
> Live V-B-A on fresh `dev` after the related PRs landed:
> 
> - #12274 is merged and covers the LMS preload-policy path: role-aware OpenAI-compatible preload derivation, required chat + embedding roles preserved, no hidden provider fallback, no pre-skip for large declared chat contexts, and partial `lms load` failure returns degraded readiness instead of silently healthy readiness.
> - #12286 / #12293 are closed/merged and retuned `localModels.embedding.*` from the old `8192 / 6144` placeholder to `32768 / 28672` for the configured Qwen3 embedding model.
> - #12285 / #12299 are closed/merged and restore native Ollama parity for local chat + embedding residency readiness.
> - Current `ai/config.template.mjs` keeps the operator-corrected model-role contract: local chat and embedding roles define their own context windows, and local provider residency requires both models in parallel.
> - Focused current-dev test evidence: `npm run test-unit -- test/playwright/unit/ai/scripts/runners/runSandman.spec.mjs test/playwright/unit/ai/config.template.spec.mjs` -> `46 passed`.
> 
> Classification: the original AC wording that default startup must avoid attempting the configured chat load is stale after the operator correction in this thread. The accepted architecture is not “skip chat” or “disable LMS”; it is required-role preload plus honest degraded readiness when an operator-configured local provider cannot keep the required roles resident. The remaining embedding-default and Ollama-parity residuals were split out and resolved by #12293 and #12299.
> 
> Closing this as completed by the merged chain above, with the corrected intent preserved here rather than buried in PR bodies.

- 2026-06-01T12:54:09Z @neo-gpt closed this issue
- 2026-06-03T07:48:42Z @neo-gpt cross-referenced by #10484

