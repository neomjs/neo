---
number: 16935
title: 'docs(agents): codify the maintainer test so it survives compaction (#16930)'
author: neo-opus-vega
state: OPEN
createdAt: '2026-08-10T22:29:04Z'
updatedAt: '2026-08-10T22:29:10Z'
closedAt: null
mergedAt: null
head: agent/16930-maintainer-test
base: dev
url: 'https://github.com/neomjs/neo/pull/16935'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16930

Codifies the operator's maintainer test into `AGENTS.md` so it survives compaction and reaches @neo-opus-ada and @neo-opus-grace.

Evidence: L1 (byte delta measured; `ai:lint-skill-manifest -- --base origin/dev` green) → L1 required (no runtime-verify AC). No residuals.

## Deltas

One file. `§self_evolving_systems` gains the two questions bound to the pre-commit and pre-PR moment, with an explicit prohibition on reporting them anywhere.

`AGENTS.md:22` already stated the value — duty is to the structural integrity of the organism. The **test** was missing, and an abstraction with no moment of application does not fire: 144 files in `ai/scripts` whose execution plane is unknowable (#16929) each passed a green AC, because no ticket mentioned the directory.

**Paid for in the same file, NET −57 bytes:**

| change | why it is not a loss |
|---|---|
| folded the `Coordination protocol` pointer into the `Mandate` above it | both fired on the same trigger (lead/peer work, lane handoff); the file pointers are retained |
| compressed `Consensus-mandate` | kept the quorum condition a reader needs *at* the gate; dropped the summary of the payload it points at |
| tightened `Rule Friction Capture` | same four fields, same routing, fewer words |
| dropped a ticket citation from `Pre-flight guard` | a citation in durable substrate rots; the instruction survives it |

Verified retained: `GRADUATION_APPROVED`, `lead-role-mode.md`, `peer-role-mode.md`, `Rule Friction Capture`, `escalation-ladder`.

## Two design choices worth a reviewer's attention

**The questions are deliberately un-reportable.** A question with an output slot gets satisfied by writing — `pr-review-guide.md` §7 is 10,694 bytes titled *Preventing Rubber-Stamp Approvals*, appears in 99/99 of my approvals, and only 41/99 cite an executed command. If these ever become a PR-body field they have already failed.

**Scope is claude-only, and that is deliberate.** `readlink .claude/CLAUDE.md` → `../AGENTS.md`, so this reaches every claude seat. `.codex/hooks/codex-context.mjs:220` reads `../CODEX.md`, so it does **not** reach gpt/kimi — the right bound, since those families already sit at 62–82% round-1 approval and are not the measured gap (Discussion #16923).

## Test Evidence

```
AGENTS.md  24403 -> 24346   NET -57      AC (<= 0): PASS
npm run ai:lint-skill-manifest -- --base origin/dev   →  OK
```

The second invocation is the one CI uses; a bare run prints `byte-delta gates skipped`.

**The test caught this change twice before review.** First draft measured **+646**, second **+106** — both failing this ticket's own numeric AC, in the edit whose ticket names "adding without paying" as the trap. Four paragraphs where two questions were asked for.

## Post-Merge Validation

None deferred. Both acceptance criteria are static and verified above.

## Review

Cross-family seat needed (author is opus). The useful question is not whether the questions are good — they are the operator's — but **whether the four compressions lost anything load-bearing.** That is the only place this PR could have removed something real, and the `Consensus-mandate` quorum line is the one I would check first.

Authored by @neo-opus-vega 🌿
