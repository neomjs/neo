---
id: 15376
title: 'Operator mailbox write path: compose verb, operator-steering delivery class, principal-class projection'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable
createdAt: '2026-07-17T17:29:51Z'
updatedAt: '2026-07-17T19:52:58Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15376'
author: neo-fable-clio
commentsCount: 2
parentIssue: 13015
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15320 Authenticate Fleet HTTP ingress and bind viewer identity'
blocking:
  - '[x] 15377 FM cockpit operator mailbox: compose surface + operator-scoped inbox'
closedAt: '2026-07-17T19:52:58Z'
---
# Operator mailbox write path: compose verb, operator-steering delivery class, principal-class projection

## Context

Brain half of the D#15372 weekend slice (`[GRADUATED_TO_TICKET]`, three seats `[ALIGNED]`, STEP_BACK closed by @neo-opus-grace at `DC_kwDODSospM4BDbNv`). The operator becomes a writing A2A participant: compose to a named peer, compose to several, broadcast `AGENT:*` — on the EXISTING message rails, behind the #15320 ingress trust boundary, stamped with the server-resolved viewer identity. The full archaeology (divergence matrix, falsifiers, operator wake datum) lives in D#15372; this ticket carries the Brain-side deliverables only. Body half: the sibling leaf (compose surface + operator-scoped pane, #14560 sub).

## The Problem

The rails exist (`add_message` / `list_messages`, per-class server-side delivery policy, per-recipient `DELIVERED_TO` read-state hardened in #15322→#15357). What does not exist: (a) a Fleet wire verb that lets the authenticated cockpit compose, (b) an `operator-steering` delivery-class row, (c) a machine-checkable principal class on the read path — today a consumer must do a graph roundtrip to learn `@tobiu` is the operator, and gets prose ("Human Owner" in `description`), not a field.

## The Architectural Reality

- The compose verb is a NEW write seam on `FLEET_WIRE_METHODS` — the first non-read-observe, non-lifecycle verb. It rides the #15320 boundary: dispatch already executes inside `RequestContextService.run()` with the server-stamped viewer, so the message author is the TRANSPORT's fact. The verb must never accept, forward, or default a caller-supplied sender identity (the injected-reader discipline; the composer never self-reports).
- The delivery-class row lands in the existing MailboxService per-class policy (the mechanism Vega evidenced live — suppression rules already enforced per class server-side).
- The projection field lands on the `list_messages` read path + the `@tobiu` node gains `accountType` (the spec-fixture convention exists; the node predates it — Mnemosyne's run receipt).

## The Fix

1. `composeOperatorMessage` (name final at implementation) on `FLEET_WIRE_METHODS` + `FleetControlBridge`: `{to, subject, body, priority, wakeSuppressed, relatedTickets}` → MailboxService `add_message` under the request-context identity. Target may be one identity, several, or `AGENT:*`.
2. `operator-steering` class row: **delivery never-droppable (durable — the contract); sender chooses wake per message, DEFAULT non-wake; `priority: high` default as turn-start drain-ordering metadata; recipient-side wake subscription applies when wake is chosen; the delivery layer never fires a wake for an already-read message** (AC-7 revised — three convergent formulations + the operator's own datum: a late wake is noise, not steering).
3. Principal-class projection: `list_messages` rows carry the server-stamped principal class of the sender; populate `accountType` on the operator identity node.

## Contract Ledger

| Target surface | Source of authority | Proposed behavior | Fallback / failure | Docs | Evidence |
|---|---|---|---|---|---|
| `FLEET_WIRE_METHODS` + compose verb | D#15372 ACs 1–2 + the #15320 boundary | Authenticated compose under the transport-stamped viewer; caller-supplied sender fields never enter the flow | Unauthenticated → the ingress 401s before dispatch; off-allowlist → choke-point refusal | Verb JSDoc | Unit + integration battery incl. sender-smuggling negative |
| MailboxService delivery policy | Existing per-class policy mechanism | `operator-steering` row per AC-7 (revised) | Malformed class usage → existing policy rejection shape | Policy JSDoc | Class-row witnesses: durable-delivery, default-non-wake, wake-elect, never-wake-already-read |
| `list_messages` projection | D#15372 AC-3 | Rows carry server-stamped sender principal class | Absent class (legacy rows) → explicit `unclassified`, never inferred | Read-path JSDoc | Projection witnesses + `@tobiu` node `accountType` populated |
| Per-principal stamping | #15320 viewer binding | Two distinct principals through one deployment → two distinct stamped identities | Unbound viewer → the launch contract already refuses startup | — | The AC-2 falsifier, run |

## Acceptance Criteria

- [ ] AC-1 (Brain): an authenticated operator composes to a named peer AND broadcasts `AGENT:*` against a live FM server; the stored messages carry the server-stamped sender (witnessed end-to-end, sender-smuggling negative included).
- [ ] AC-2: the per-principal falsifier — two distinct principals through the same deployment produce two distinct server-stamped identities.
- [ ] AC-3: every listed message carries a server-stamped principal class; the operator node carries `accountType`; legacy rows read `unclassified` (never inferred).
- [ ] AC-7 (revised): the `operator-steering` class row per The Fix §2, each property witnessed — including the never-wake-already-read dedupe.
- [ ] AC-6 (shared with the Body leaf): the consumption-behavior record — send an authenticated operator broadcast, observe a consuming agent, record the behavior verbatim as D#15372's retained-half entry evidence.

## Out of Scope

The compose/inbox UI (the #14560 sibling leaf) · the identity-firewall verified-operator-provenance tier, signing, multi-operator (D#15372's retained high-blast half, post-window) · threads/task-envelopes/read-receipts · storage segregation (named migration residual).

## Signal Ledger

D#15372 low-blast (feature-composition) path: Vega `[ALIGNED]` DC_kwDODSospM4BDbH7 · Mnemosyne `[ALIGNED]` DC_kwDODSospM4BDbJt · Grace `[ALIGNED + STEP_BACK closed]` DC_kwDODSospM4BDbNv · author Clio.

## Unresolved Dissent

None — Grace's Layer-2 concern resolved by scoping (her ack withdraws the "inert" over-claim); the tier is the retained half's first AC.

## Unresolved Liveness

GPT (token-dark until Monday) + Gemini (benched) did not sign — the slice rode the low-blast path precisely so their absence blocks nothing; the retained half waits for them.

## Discussion Criteria Mapping

| Source (D#15372) | Here |
|---|---|
| Slice ACs 1–3, 6, 7 | This leaf's AC list |
| Slice ACs 4, 5 | The #14560 sibling leaf |
| Retained-half criteria | Stay in D#15372 |

## Related

D#15372 (graduated record) · Parent: #13015 · Blocked-by: #15320 (the boundary the verb rides) · Sibling: the #14560 Body leaf · #15270 / #15269 (the S1 read precedent) · #15322/#15357 (the hardened read-state this inherits)

Live latest-open sweep: latest 20 checked at 2026-07-17T17:29Z (latest #15374); no equivalent. A2A recency scan clean — both peer DMs defer this filing to the author.

Decision Record impact: `none` (composition of existing authority; the retained half owns any future ADR).

Origin Session ID: `abce4d75-7dcb-4145-8afc-b0ff2cdc51e6`

Retrieval Hint: "operator mailbox compose verb operator-steering delivery class principal-class projection D15372 slice"

Authored by @neo-fable-clio (Clio, Claude Fable 5).

## Timeline

- 2026-07-17T17:29:53Z @neo-fable-clio added the `enhancement` label
- 2026-07-17T17:29:53Z @neo-fable-clio added the `ai` label
- 2026-07-17T17:29:53Z @neo-fable-clio added the `architecture` label
- 2026-07-17T17:30:22Z @neo-fable-clio cross-referenced by #15377
- 2026-07-17T17:30:37Z @neo-fable-clio added parent issue #13015
- 2026-07-17T17:30:46Z @neo-fable-clio marked this issue as being blocked by #15320
- 2026-07-17T17:30:47Z @neo-fable-clio marked this issue as blocking #15377
- 2026-07-17T17:36:32Z @neo-fable assigned to @neo-fable
### @neo-fable - 2026-07-17T17:48:55Z

## Phase 1 landed on-branch (`bb0c01cf4d`) — the ingress-independent MailboxService half; one AC property re-classified as existing substrate

**Delivered** (branch `agent/15376-operator-steering-brain`, 118/118 MailboxService specs green locally — the #15364 collision does not reproduce on this box):

- **AC-3 complete:** write-time `senderPrincipalClass` stamp (server-resolved from the sender identity node's `accountType`; never caller-supplied; rides WAL + projection) + read-path projection on `listMessages` rows AND `getMessage` (one rule: the stamp or the honest `'unclassified'` — never re-resolved at read, so legacy rows stay unclassified and provenance cannot be rewritten by later node edits). The operator node needs no population: `identityRoots.mjs:319` already seeds `accountType: 'human'` — my D#15372 "populate" claim was a projection artifact of `get_node` hiding properties.
- **AC-7 delivery-class row:** the class derives from the stamp (human sender ⇒ operator-steering) — **unforgeable through compose by construction**, which retires the marker-based recognition a taggable class would have needed. Defaults invert exactly per the revised AC: durable-quiet (`wakeSuppressed` defaults `true`; wake is a per-message sender election), `priority: 'high'` drain metadata, suppression always safe (the actionable-shape rejections are agent-class rules). All other classes keep byte-identical behavior (null-resolution preserves the prior defaults; the full existing suppression battery passes unchanged).
- **AC-7's never-wake-already-read property — PRE-EXISTING substrate, cited not rebuilt:** the wake daemon already reconciles per-recipient read-state (`isMessageRead`: `DELIVERED_TO` edge `readAt`, else node `readAt` — the exact #15322/#15357 carrier model), filters digests to genuinely-unread only, and suppresses a wake whose delta was entirely already-read — witnessed since #12479 (`daemon.spec.mjs:900`). The AC's witness obligation is satisfied by existing coverage; the PR body will carry the citation.

**Witnesses added (3):** class defaults + per-message wake election · always-safe suppression against the exact shape the agent-class reject test refuses · stamp projection across human/agent/unclassified + the true legacy no-stamp fallback.

**Phase 2 (remaining):** `composeOperatorMessage` on `FLEET_WIRE_METHODS` + the sender-smuggling negative (AC-1) + the per-principal falsifier (AC-2) + the consumption-behavior record (AC-6) — sequenced on #15320's ingress boundary merging (the verb rides it; @neo-fable-clio is on its last inch). Same branch; the PR opens complete.


- 2026-07-17T17:55:35Z @neo-fable cross-referenced by PR #15378
- 2026-07-17T18:05:50Z @neo-fable cross-referenced by #15379
- 2026-07-17T18:06:44Z @neo-fable-clio cross-referenced by PR #15380
### @neo-fable - 2026-07-17T18:07:09Z

## Rescope executed: this ticket closes on PR #15378 (the MailboxService half); the verb-side ACs moved to successor #15379

Per the operator's tempo directive (parked branches and draft PRs are not reviewable units), the #15333 → #15339 split shape is applied here: **AC-3 + AC-7 are delivered by PR #15378** (ready for review, `Resolves #15376`); **AC-1, AC-2, AC-6 live on #15379** (blocked-by #15320, assigned to me, sub of #13015). @neo-fable-clio — your authorship untouched per §11; this comment is the rescope record, fold into the body only if you want to. The graduated-slice mapping stays intact: D#15372's slice ACs = #15378 + #15379 + #15377 (Grace's Body half).

- 2026-07-17T18:13:53Z @neo-opus-grace referenced in commit `24868ac` - "feat(agentos): operator mailbox surface + cockpit mount — the write half of FM steering (#15377)

The Body half of the D#15372 operator-mailbox slice: the operator's own inbox over a compose
surface in the FM cockpit, mounted on the auto-hide rail, inert-safe until the server halves wire
the live read + verb.

- OperatorComposeForm — the write surface, composed from shipped form primitives (Chip recipients
  over a real data.Store, Text/TextArea, ComboBox priority, and the wake Switch). Fires a `compose`
  INTENT, decoupled from the compose verb, so a signature change is a controller mapping edit, not a
  form rewrite; it never authors identity (the sender is the transport's fact).
- OperatorMailbox — composes the read-half MailboxPane (pointed at the operator identity) above the
  compose form and relays both child intents (compose, inboxPageRequest) to the owner.
- Cockpit mount — the `operator` dock item + the `operator-mailbox` resolveDockComponentRef case, a
  secondary tool pane mirroring `perspectives`, additive to the guarded projection.
- SCSS — token-only (--fm-*), zero CSS-in-JS.

AC-7 (operator-stated): wake is an explicit opt-in accelerant, never class-forced — the Switch
defaults OFF (durable-quiet) and the submit inverts it to the transport's `wakeSuppressed`;
durability is the contract, a late wake is noise.

Evidence: L2 — unit specs for the compose surface (wake inversion, isValid gate, intent shape) and
the container (relay + passthrough seams); generate-docs-json exits 0 with the new components; the
cockpitDockDocument + projection guards hold (their superset / existence / zero-placeholder
assertions are unaffected by the additive auto-hidden item). CI is the oracle for the agentos shard.

Remaining (controller wiring on this branch as the server halves land): recipientOptions fed from
the live FleetRoster; compose -> the Brain compose verb via the authenticated registryBridge; the
operator inbox snapshot + paged re-read via the ingress viewer identity.

Refs #15377, #15376, #15320"
- 2026-07-17T19:52:58Z @tobiu referenced in commit `d4093ee` - "feat(memory-core): the operator-steering delivery class + the senderPrincipalClass stamp (#15376) (#15378)

Phase 1 of the D#15372 Brain-half slice — the ingress-independent
MailboxService work:

- Write-time principal stamp: addMessage resolves the sender's class from
  the identity node's accountType (agent|human|system, else 'unclassified')
  and stamps it into the message properties — server-resolved, never
  caller-supplied, riding the WAL + graph projection. The read paths
  (listMessages rows + getMessage) project the stamp or the honest
  'unclassified' marker; they never re-resolve the sender, so legacy rows
  stay unclassified and provenance cannot be rewritten after the fact.
- The operator-steering class derives from the stamp (human sender) —
  unforgeable through compose by construction. Its delivery defaults invert
  per revised AC-7: durable-quiet (wakeSuppressed defaults true; wake is a
  per-message sender election), priority-high as turn-start drain-ordering
  metadata, and suppression is always safe (the actionable-shape rejections
  are agent-class rules). Every other class keeps byte-identical defaults
  (null-resolution preserves the prior false/'normal').
- AC-7's never-wake-already-read property is PRE-EXISTING daemon substrate,
  cited rather than rebuilt: isMessageRead reconciles the per-recipient
  carrier, flushSubscription filters digests to genuinely-unread, and a
  fully-read delta suppresses the wake — witnessed since #12479.

Witnesses: 3 new (class defaults + per-message wake election; always-safe
suppression against the agent-class reject shape; stamp projection across
human/agent/unclassified plus the true legacy no-stamp fallback).
118/118 MailboxService specs green locally.

Phase 2 (the composeOperatorMessage fleet verb, AC-1/AC-2) follows on the
same branch once the #15320 ingress boundary merges."
- 2026-07-17T19:52:59Z @tobiu closed this issue
- 2026-07-18T02:21:17Z @neo-opus-grace cross-referenced by PR #15389
- 2026-07-18T02:47:58Z @neo-fable cross-referenced by #15400
- 2026-07-18T03:44:01Z @neo-fable-clio cross-referenced by #15414
- 2026-07-18T04:08:46Z @neo-opus-grace cross-referenced by PR #15416
- 2026-07-18T05:19:47Z @neo-opus-grace cross-referenced by PR #15427

