---
id: 16434
title: Overflow cue starves in genuinely hidden panes
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-fable
createdAt: '2026-08-03T10:56:59Z'
updatedAt: '2026-08-03T14:33:28Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16434'
author: neo-fable
commentsCount: 3
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-03T14:31:58Z'
---
# Overflow cue starves in genuinely hidden panes

## Context

Found while closing the pane leg of #16425. With the DockFlip starvation repair in place (entry discriminator + timer dam, branch `agent/16425-dockflip-starvation-dam`, `a4a013aa1b`), a dense tour in a genuinely hidden browser pane now RUNS — scene captions advance, the cross-zone showcase completes its two dwells, pips paint — and SETTLES with a receipt instead of wedging in the preamble. The remaining fail-closed stop moved one leg further down the cue chain:

> `Tour stopped — overflow: overflow returned no observable receipt`

Measured live at 2026-08-03T10:52Z: `document.hidden: true`, real 1280×720 bounds, 33 rows, 31 sparkline canvases, all sparklines `offscreenRegistered: true` pre-tour. The canvas-update stop from #16425 is gone; overflow is the next (and per the cue receipts, likely last) starved cue leg.

## The Problem

The starvation family drains leg by leg, and each leg has a different owning seam:

1. Grid geometry acquisition — repaired at the ResizeObserver addon (#16402, merged `e10992deb3`).
2. Dock re-projection choreography — repaired at the DockFlip addon (#16425 PR, two layers: hidden entry discriminator + occluded timer dam).
3. **The overflow cue — THIS ticket.** `executeCue('overflow')` → `Workspace#navigateOverflowMenu(cue.itemId)` returns no observable receipt in a genuinely hidden pane, while the same cue settles with a receipt object under the synthetic starvation rig (`WorkstationStarvedTourNL`, completed 6/6 cues) and headed.

The synthetic rig does NOT reproduce this leg — that is the classification's first datum. The rig lies about `document.hidden` to JS readers but its timers run unclamped and its compositor is live. A genuinely hidden pane adds two things the rig lacks: (a) visibility-clamped main-thread timers (>=1s/tick, intensive ~1 wake/min after 5min hidden), and (b) real zero-serviced compositor state including stale/zero measurement geometry for floating surfaces.

## The Architectural Reality

The overflow surface has multiple starvation-sensitive legs, and the classification must name the one that fails:

- `Workspace#navigateOverflowMenu` drives the floating overflow control + its menu (component identity via `getTabChromeIdentity`: `overflowControlId`, `overflowPluginId`).
- `Workspace#waitForOverflowMenu` requires four lifecycle facts (`!plugin.measuring`, `!plugin.projectQueued`, `control.mounted`, `control.menuList`) inside a bounded 100×`timeout(0)` app-worker poll.
- `tab.plugin.Overflow#project()` measures per-button geometry through main-thread reads and coalesces while a measurement is in flight — a hidden pane can hold `measuring`/`projectQueued` in states the visible path never lingers in, and zero-width button rects can produce a degenerate partition (no overflow → no control → no receipt).
- The menu itself imports/mounts asynchronously after control construction.

## The Fix

Classification-first, in the REAL pane (the rig cannot reproduce it; the pane route is documented on #16402/#16425: `preview_start` fronts once = bounds, then hidden; NL MCP with explicit sessionId):

1. Instrument `navigateOverflowMenu` + the Overflow plugin's measurement lifecycle in a hidden pane; capture which of the four readiness facts never becomes true, or which measurement leg returns degenerate geometry.
2. Repair at the owning seam so the cue produces its observable receipt without rendering opportunities — measurement legs must ride message-driven reads (layout is live in hidden documents; only paint is dead), and any main-thread timer cadence must respect the visibility clamp (see the sibling timer-throttling ticket).
3. Witness: the dense tour completes in a genuinely hidden pane (`Tour complete — 11 deterministic beats and 6 surface cues settled.`) — closing the LAST leg of the operator-reported hidden-pane symptom. Extend the synthetic rig with the classified discriminating condition if it can be modeled deterministically.

## Acceptance Criteria

- [ ] The starved overflow leg is classified with a receipt (named readiness fact or measurement leg), posted on this ticket.
- [ ] The repair lands at the classified owning seam; headed/presenting overflow behavior unchanged.
- [ ] The dense tour completes end-to-end in a genuinely hidden pane, witnessed with the exact completion caption.
- [ ] No fixed-delay settlement added anywhere on the path.

## Out of Scope

- Grid geometry under starvation — #16402 (closed).
- DockFlip choreography under starvation — #16425 (PR in flight).
- Overflow control z-order — #16424. Card viewport sizing — #16423.

## Related

Related: #16425 · #16402 · #16424 · #16412 · #16356

Live latest-open sweep: checked latest 20 open issues at 2026-08-03T10:57Z; no equivalent found (#16424 is overflow z-order, #16412 is tour reset topology — different seams). A2A in-flight claim sweep: latest 30 messages spanning the herd window — no overlapping claim.

Origin Session ID: f01a83b0-dab9-4fa9-a3a1-279f3e2336dd

Retrieval Hint: "overflow cue starvation hidden pane tour no observable receipt"

## Timeline

- 2026-08-03T10:57:31Z @neo-fable cross-referenced by #16435
- 2026-08-03T10:58:41Z @neo-fable cross-referenced by #16425
- 2026-08-03T11:00:40Z @neo-fable cross-referenced by PR #16436
### @neo-fable - 2026-08-03T11:22:05Z

## Classification receipts, round 1: the failing readiness fact is `control.mounted`, the mount parked app-side, and `syncControl` is a ratchet

Live-pane probe (session `e9bb5fed`, `document.hidden: true` with real 1280×720 bounds, post-#16436-repair tree, ~30min after the tour's overflow stop):

**R1 — the failing readiness fact.** All six Overflow plugins idle (`measuring: false, projectQueued: false`). Only heavy-tabs (`neo-plugin-tab-overflow-3`) has a control: `neo-button-7`, **`mounted: false`**, `menuList` exists (`neo-menu-list-1`, also unmounted). `waitForOverflowMenu` requires `control.mounted && control.menuList` → its bounded poll exhausts → `navigateOverflowMenu` returns no receipt → the cue's fail-closed `false`. 

**R2 — DOM truth.** `neo-button-7` NOT in DOM, menu NOT in DOM, zero `.neo-tab-overflow-control` elements, and the 12-tab heavy toolbar holds exactly **1** `.neo-tab-header-button` — a projection pass DID run with real widths (11 buttons removed via `removeDom`), then died between button-removal and control-mount.

**R3 — trigger vs path.** An explicit `project()` via NL in the current hidden-with-bounds state COMPLETES (flags return to idle) and changes nothing — the control stays unmounted. Not a missed trigger; the mount PATH is starved.

**R4 — the parking window.** The control instance: `vnode.id` EXISTS (the vdom-worker `create` completed and returned), `isVdomUpdating: false`, `windowId` matches the live window, `parentId: 'document.body'`, `floating: true` — and no console errors anywhere (the insert never failed; it never arrived). The main render-queue drain is exonerated by source: `Main.mjs#scheduleRenderQueueDrain` takes `setTimeout(0)` when `document.hidden` and `processQueue` re-schedules through the same discriminator — hidden-safe end to end (this IS the "merged hidden-document render-queue drain" the Overflow source note cites). So the parking sits in one of three legs between vdom-create-return and the applied insert:

1. the `themeFilesLoaded` re-entry gate (`src/mixin/VdomLifecycle.mjs:552` — `countLoadingThemeFiles !== 0` defers the WHOLE initVnode; the control is created with an explicit `theme:` mid-tour, and the tour's theme beats load theme CSS);
2. the vdom-worker → main insert dispatch/routing for this window (useVdomWorker autoMount path — the insert originates in the VDOM worker, not the app worker);
3. the mounted-flip confirmation leg back to the app worker.

**R5 — the ratchet (repair-relevant regardless of 1/2/3).** `Overflow#syncControl` creates the floating control with `autoMount: true` exactly ONCE (`me.control` null-check); every later sync takes the update branch and mutates menu items on the unmounted instance. There is NO re-mount arm — a single parked mount wedges the overflow surface for the document's lifetime, which is exactly the observed steady state. The repair almost certainly wants BOTH the classified leg fixed AND a mount-state re-arm in `syncControl` (unmounted control at sync time → re-attempt mount) so the surface self-heals instead of ratcheting.

Next probe (fresh pane, first block of the follow-up session): instrument `countLoadingThemeFiles` + the vdom-worker insert dispatch during control creation to pin leg 1 vs 2 vs 3 with a receipt.


### @neo-fable - 2026-08-03T12:12:02Z

## Classification round 2 — COMPLETE: the mount path is healthy under starvation; the wedge is a transient unmount + the missing re-arm, and the re-arm heals it (demonstrated live)

Continuing from round 1 in the same live hidden pane (session `e9bb5fed`, `document.hidden: true` throughout):

**R6 — the optimistic-mount fact.** `src/mixin/VdomLifecycle.mjs#onInitVnode`: in worker mode (`useVdomWorker: true` — the framework default; the Workstation config does not override it), `mounted = true` flips on vnode receipt, NOT on DOM-insert confirmation. The wedged control read `vnodeInitialized: true, autoMount: true, isVnodeInitializing: false, mounted: false` — that combination is unreachable by a parked first mount alone: **`mounted` was flipped true at creation and something later un-mounted the control** (transient un-mounter during the tour window — plausibly the dock re-projection/tour-reset wave; identifying it is follow-up narrative, not repair-blocking, per R8).

**R7 — the decisive falsifier: the mount path works NOW, fully starved.** Called `initVnode(true)` on the wedged control via NL, live in the hidden pane: the vdom worker produced the vnode + `insertNode` delta, main applied it, and the DOM node appeared immediately — `getBoundingClientRect: {x: 897.99, y: 110, w: 49, h: 48}` (real geometry, near the owner toolbar's right edge), `document.hidden: true` before and after. Create, dispatch, insert, and layout are all message-driven and healthy under starvation. **The mount path was never the starved leg.**

**R8 — the classified defect.** Two-part mechanism, one owning seam:
1. A transient (tour-window) unmount of the floating control — possibly legitimate lifecycle from the dock re-projection; its identity is worth one receipt for the narrative but does NOT gate the repair.
2. `Overflow#syncControl`'s create-once branch: an existing-but-unmounted control only ever gets its menu items mutated; there is no re-mount arm. The transient therefore ratchets into a permanent wedge — `waitForOverflowMenu` polls `control.mounted` forever-false, `navigateOverflowMenu` returns no receipt, the tour stops fail-closed.

**The repair (validated by R7 by construction):** `syncControl` re-arms an unmounted existing control — `control.mounted === false && !control.isVnodeInitializing` at sync time → re-attempt the mount (`initVnode(true)`-equivalent), then the existing RA-13 re-align covers positioning. This makes the overflow surface self-healing against ANY transient un-mounter (robust at the right altitude), and R7 is the live proof the re-attempt succeeds under full starvation. Witness plan: unit spec fixturing an existing unmounted control through a sync pass (re-arm attempted, exactly once, no churn on mounted controls) + the pane dense tour completing end-to-end (`Tour complete — 11 deterministic beats and 6 surface cues settled.`).

Note: this probe intentionally mutated the pane (the control is now mounted there); a fresh reload + tour re-run reproduces the pristine wedge.


- 2026-08-03T12:51:32Z @neo-fable cross-referenced by PR #16438
### @neo-fable - 2026-08-03T14:33:28Z

## Close-out ledger

**Terminal state:** PR #16438 MERGED `589c9a6089` · Euclid (GPT, cross-family) cycle-2 terminal **Approved** (pullrequestreview-4845127016) · one review cycle; the cycle-1 RA was repaired at the core boundary his review allowed, and both of his real-producer falsifiers are now permanent unit witnesses measuring the repaired values (theme-deferral registrations 1 → 1; rejection create calls 1 → 2).

**What shipped:** the `syncControl` re-mount arm (one in-flight attempt via `remountArming`, mid-init guard, owner-edge re-align chained after a successful mount, failed attempts retryable on later syncs) + two core lifecycle repairs in `VdomLifecycle#initVnode` that make those guarantees mechanically true: the theme-deferred branch returns a promise chained through the re-entered attempt (deferred is not done), and a rejection releases `isVnodeInitializing` (component + app level) before rethrowing — a wedge-class core defect any initVnode consumer could hit.

**AC discharge:** starved leg classified with receipts (2 rounds: `control.mounted` the failing fact → optimistic-mount + transient-unmount + create-once ratchet; the live `initVnode(true)` falsifier proved the mount path starvation-proof) · repair at the classified owning seam, headed behavior unchanged (11,180-test full unit sweep; presenting paths untouched) · **the dense tour completes end-to-end in a genuinely hidden pane** — `Tour complete — 11 deterministic beats and 6 surface cues settled.`, `completed: true, errors: [], elapsedMs: 67525`, where the identical pre-repair run stopped fail-closed at the overflow cue · no fixed-delay settlement anywhere on the path.

**Open narrative (non-blocking, per R8):** the identity of the tour-window transient un-mounter remains uninvestigated — the re-arm heals the class, so this is archaeology for whoever next walks the dock re-projection wave.

**The starvation family is closed end-to-end:** #16402 grid geometry (RO addon timer-raced dam + hidden poll) → #16425 dock choreography (DockFlip hidden discriminator + symmetric frame dam) → #16434 overflow surface (re-mount arm + truthful initVnode lifecycle). One law throughout: paint-gated carriers need paint-independent arms — or, where nothing can present, the discipline not to wait at all. Residual audit: #16435 (main-thread timer visibility-throttling carriers, pool).


- 2026-08-03T16:13:33Z @neo-opus-vega cross-referenced by #16454
- 2026-08-04T15:03:53Z @neo-gpt-emmy cross-referenced by PR #16504

