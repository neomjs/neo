---
id: 17008
title: 'ask admission is single-lane, so a dedicated endpoint or replica pool cannot serve two asks at once'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-08-11T23:29:38Z'
updatedAt: '2026-08-12T08:11:21Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17008'
author: neo-opus-vega
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-12T08:11:21Z'
---
# ask admission is single-lane, so a dedicated endpoint or replica pool cannot serve two asks at once

## Problem

Ask synthesis dispatches through a **single-lane** admission queue, so two asks arriving seconds apart are served one after the other **regardless of how much idle capacity the serving endpoint has.** Traced at `271bb132d8`:

```
SearchService.mjs:176        this.model = buildChatModel({…})
buildChatModel.mjs:160       return chatRequestQueue.enqueue(…)            // openAiCompatible = ask's default provider
buildChatModel.mjs:110       chatRequestQueue = sharedLocalChatRequestQueue // process-wide default
InteractiveBatchQueue.mjs:2  "A single-lane request scheduler that runs async tasks one at a time"
```

At the measured ~43 s per ask, the second returns near ~86 s.

**This is why provisioning capacity alone cannot fix it.** A dedicated ask endpoint, or a replica pool, yields **idle replicas behind a queued second ask** — and the symptom reads as *"the model is slow"* rather than *"the queue admitted one"*. The constraint is **admission**, and it is invisible to every latency measurement that does not separate queue wait from execution time.

Carved out of **#17000** after @neo-gpt's review of PR #17007: that PR delivers admission only, and closing #17000 with it would assign #17000's load-bearing remainder — the dedicated endpoint, exclusivity enumeration, external attribution, model decision and the L3 witness — back to a ticket declared resolved. This ticket is the part a PR can fully close; #17000 keeps the rest.

## Acceptance criteria

- [ ] `InteractiveBatchQueue` accepts an optional parallel **capacity**, defaulting to `1` — byte-identical to the previous single-lane scheduler, so every existing consumer is unchanged and evidenced as such.
- [ ] An unusable capacity (`0`, negative, fractional, non-numeric) is **refused at construction**, not at admission: a stalled lane is indistinguishable from a hung provider on the caller's side.
- [ ] Ask parallelism is operator-adjustable through a leaf in the **existing** `askSynthesis` node, read at the use site — no second config node, no threaded value (ADR 0019 B5), no `AiConfig` import in the provider layer (C1).
- [ ] **The leaf is reachable from a deployment** — carried through the canonical Compose profiles. A control with an env binding and no compose surface is not operator-adjustable.
- [ ] Ask receives its **own** queue instance rather than a raised capacity on the shared one, so parallelism cannot leak to other consumers in the process.
- [ ] **Spec: the queue is HANDED to the provider layer**, not merely constructible. Removing the injection must fail an arm — a helper-capacity assertion proves the helper, not the delivery.
- [ ] Spec: slot-filling admits two same-tick enqueues when capacity allows; a freed slot pulls the next waiting task; a throwing task releases its slot.

## Contract Ledger

| Target surface | Source of authority | Behaviour | Failure / fallback | Evidence |
|---|---|---|---|---|
| `InteractiveBatchQueue` | this ticket | admits up to `capacity`; interactive still preferred when a slot frees | capacity `1` reproduces the single-lane scheduler exactly | new arms + the original arms as the default-capacity control |
| construction guard | this ticket | integer `>= 1` required | anything else throws at construction | explicit arm with a valid-capacity control |
| `askSynthesis.maxParallel` | KB `AiConfig`, read at the use site | sizes the ask-owned queue | no builder-local fallback; a missing leaf lands on the primitive's own default | `buildAskRequestQueue` arms |
| Compose profiles | this ticket | the leaf is settable on a deployment | absent env → the declared default | grep with a sibling-leaf positive control |
| provider composition | this ticket | the ask-owned queue reaches `buildChatModel` | removing the injection fails an arm | composition arm, mutation-verified |

## Out of scope

- **The dedicated serving endpoint**, its exclusivity enumeration, external attribution, the model decision and the two-asks-2s-apart L3 witness — all remain on **#17000**, which needs host provisioning this ticket does not.
- The context budget (#16999) and model selection (#17001).

## Related

- **#17000** — parent lane; keeps the endpoint work this ticket deliberately excludes
- **#16998** — the epic
- PR #17007 — the implementation, reviewed by @neo-gpt

Live latest-open sweep at creation: #17006 (21:57Z), #17004 (21:56Z), #17001 / #17000 / #16999 (19:43Z) — no equivalent. Duplicate sweep on "admission queue capacity ask parallel" and `InteractiveBatchQueue in:title`: only #17000 / #16998, which this carves from. A2A: no lane-claim on this surface; #17000 is assigned to me.

Retrieval Hint: `ask admission single-lane queue capacity maxParallel compose reachable injection witness InteractiveBatchQueue`

Authored by @neo-opus-vega


## Timeline

- 2026-08-11T23:29:38Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-11T23:29:39Z @neo-opus-vega added the `bug` label
- 2026-08-11T23:29:40Z @neo-opus-vega added the `ai` label
- 2026-08-11T23:31:05Z @neo-opus-vega referenced in commit `1f98d21` - "fix(knowledge-base): make the capacity reachable and witness the composition (#17008)

A review of this PR found two defects and one dishonest close edge.

REACHABILITY. NEO_KB_ASK_MAX_PARALLEL was absent from every file under
ai/deploy/, verified with a positive control (the sibling NEO_KB_ASK_MODEL
appears in three). So the "operator-adjustable" control could not be set on any
deployment. This is the exact defect diagnosed on the miniSummary window leaves
four hours earlier — env-bound leaf with no compose surface — and then committed
here. Now carried through both canonical profiles.

COMPOSITION WITNESS. The spec asserted buildAskRequestQueue(...).capacity, which
proves the helper builds a queue and says nothing about asks using it: deleting
chatRequestQueue from the call site left the whole suite green. That is testing
the proxy and calling it the delivery. buildAskChatModelOptions now returns the
exact object handed to buildChatModel, so an arm asserts what the provider layer
receives. Mutation-verified: removing the injection fails exactly that arm.

CLOSE EDGE. This PR delivered admission only while Resolves #17000 assigned that
ticket's remainder — endpoint, exclusivity, attribution, model decision, L3
witness — back to a ticket it declared resolved. The narrower leaf carves out
the part this PR fully closes; the parent keeps the endpoint work, which needs
host provisioning that is not mine.

Evidence: L2 — 35/35 across SearchService and InteractiveBatchQueue specs.
Mutation-verified in both directions: removing the injection fails exactly the
new composition arm; the compose absence was confirmed with a sibling positive
control before and after."
- 2026-08-11T23:31:26Z @neo-opus-vega cross-referenced by PR #17007
- 2026-08-11T23:35:55Z @neo-opus-vega cross-referenced by #17009
- 2026-08-11T23:55:15Z @neo-opus-vega referenced in commit `3aefb1a` - "test(knowledge-base): witness the CONSTRUCTOR's composition, not the builder's (#17008)

Third round on one defect, and the shape is the finding: each previous fix
asserted the newly-extracted pure function while the unwitnessed edge moved UP to
its caller.

  round 1  asserted buildAskRequestQueue().capacity
           -> removing the injection from the call site stayed green
  round 2  asserted buildAskChatModelOptions()
           -> removing that CALL from construct stayed green (reviewer's mutation)

Both proved a builder. Neither proved this service composed anything. Extracting
a pure function makes the composition assertable and simultaneously creates a new
untested edge — caller to pure function — so the assertion has to come off a
value that actually travelled, not one the spec can rebuild.

`SearchService.askRequestQueue` is published from the SAME options object handed
to buildChatModel. It is a receipt, not a second construction: bypass the call in
construct and it is null, which no rebuilt-in-the-spec assertion can detect.

Evidence: L2 — 22/22 in SearchService.spec, 36/36 with the queue spec.
Mutation-verified against the reviewer's exact bypass (helper and its spec left
intact, construct rewired to an inline object): fails EXACTLY the new arm,
1 failed / 21 passed. Restored 22/22."
- 2026-08-12T07:28:35Z @neo-opus-vega referenced in commit `43b32df` - "fix(knowledge-base): make the capacity reachable and witness the composition (#17008)

A review of this PR found two defects and one dishonest close edge.

REACHABILITY. NEO_KB_ASK_MAX_PARALLEL was absent from every file under
ai/deploy/, verified with a positive control (the sibling NEO_KB_ASK_MODEL
appears in three). So the "operator-adjustable" control could not be set on any
deployment. This is the exact defect diagnosed on the miniSummary window leaves
four hours earlier — env-bound leaf with no compose surface — and then committed
here. Now carried through both canonical profiles.

COMPOSITION WITNESS. The spec asserted buildAskRequestQueue(...).capacity, which
proves the helper builds a queue and says nothing about asks using it: deleting
chatRequestQueue from the call site left the whole suite green. That is testing
the proxy and calling it the delivery. buildAskChatModelOptions now returns the
exact object handed to buildChatModel, so an arm asserts what the provider layer
receives. Mutation-verified: removing the injection fails exactly that arm.

CLOSE EDGE. This PR delivered admission only while Resolves #17000 assigned that
ticket's remainder — endpoint, exclusivity, attribution, model decision, L3
witness — back to a ticket it declared resolved. The narrower leaf carves out
the part this PR fully closes; the parent keeps the endpoint work, which needs
host provisioning that is not mine.

Evidence: L2 — 35/35 across SearchService and InteractiveBatchQueue specs.
Mutation-verified in both directions: removing the injection fails exactly the
new composition arm; the compose absence was confirmed with a sibling positive
control before and after."
- 2026-08-12T07:28:35Z @neo-opus-vega referenced in commit `4b83246` - "test(knowledge-base): witness the CONSTRUCTOR's composition, not the builder's (#17008)

Third round on one defect, and the shape is the finding: each previous fix
asserted the newly-extracted pure function while the unwitnessed edge moved UP to
its caller.

  round 1  asserted buildAskRequestQueue().capacity
           -> removing the injection from the call site stayed green
  round 2  asserted buildAskChatModelOptions()
           -> removing that CALL from construct stayed green (reviewer's mutation)

Both proved a builder. Neither proved this service composed anything. Extracting
a pure function makes the composition assertable and simultaneously creates a new
untested edge — caller to pure function — so the assertion has to come off a
value that actually travelled, not one the spec can rebuild.

`SearchService.askRequestQueue` is published from the SAME options object handed
to buildChatModel. It is a receipt, not a second construction: bypass the call in
construct and it is null, which no rebuilt-in-the-spec assertion can detect.

Evidence: L2 — 22/22 in SearchService.spec, 36/36 with the queue spec.
Mutation-verified against the reviewer's exact bypass (helper and its spec left
intact, construct rewired to an inline object): fails EXACTLY the new arm,
1 failed / 21 passed. Restored 22/22."
- 2026-08-12T07:28:35Z @neo-opus-vega referenced in commit `bba7d99` - "chore(lint): declare the ask parallelism knob in the compose census (#17008)

Surfaced only after rebasing onto dev: the compose default-parity census counts
AND names every NEO_/MCP_ key in the canonical profile, so adding
NEO_KB_ASK_MAX_PARALLEL to docker-compose.yml left it reported as `unexpected`
even once the count matched.

The count alone is not the gate, and that is the better design: a new compose
surface has to be NAMED in a bucket, so a reviewer sees which knob was added
rather than that some number moved. Declared under optionalOverrides, which is
its actual class — it carries a default and is not a required deployment input.

--update-parity does not touch this block. It updates declared leaf paths; the
census is hand-maintained, which is correct for a record whose purpose is that
adding a deployment knob be a deliberate reviewed act.

Second expectation site for one change, after the leaf-path snapshot earlier in
this branch. Both are now recorded."
- 2026-08-12T08:11:21Z @tobiu closed this issue
- 2026-08-12T08:11:22Z @tobiu referenced in commit `030aeec` - "feat(provider): bound ask admission so a dedicated endpoint can actually be used (#17000) (#17007)

* feat(provider): bound ask admission so a dedicated endpoint can actually be used (#17000)

Ask dispatches through a serializing admission queue, so two asks arriving
seconds apart are served one after the other regardless of how much idle capacity
the serving endpoint has. Provisioning a dedicated endpoint or replica pool
without addressing admission therefore yields idle replicas behind a queued
second ask, and the symptom reads as "the model is slow" rather than "the queue
admitted one". The constraint is admission, not capacity.

InteractiveBatchQueue gains an optional capacity, DEFAULT 1 — the original
single-lane behaviour exactly, so every existing consumer is unchanged. An
unusable capacity is refused at construction rather than at admission: a 0 or
negative bound makes the lane stall forever, and a scheduler that never runs a
task is indistinguishable from a hung provider on the caller's side.

Ask gets its OWN queue instance rather than a raised capacity on the shared one,
because capacity belongs to the consumer that has its own endpoint; raising it on
the process-wide queue would hand concurrency to every other local chat consumer
as a side effect. No contention changes hands: the two buildChatModel callers are
SearchService and memory-core's SessionService, which run in different processes,
so the process-wide queue never serialized them against each other and ask is the
only chat consumer in its process.

The capacity is read at the use site with NO builder-local fallback, and the
constructed queue is injected rather than passed as a number. The no-fallback
read follows the measurement that settled the budget leaves: the generated
config.mjs is a thin singleton extending the tracked base and declaring no data,
so a declared leaf reaches every overlay and cannot resolve absent. A `|| 1`
would be dead code that could only mask a real config break — and masking it as
"serialized" is the quiet failure, since a deployment that meant to run parallel
asks would silently keep serializing them.

Evidence: L2 — 7 new queue arms, mutation-verified both directions (making
slot-filling async fails 9; re-draining before releasing the slot fails 9 and
hangs, which is the documented stall). The original 7 arms still pass unchanged,
including "never runs two tasks concurrently", now the default-capacity control.
57/57 across queue, SearchService, SessionService and context-budget specs.

The dedicated ENDPOINT itself, its exclusivity enumeration and the two-asks-2s-apart
runtime witness are NOT in this commit: they need host provisioning that is not
mine to perform. #17000 stays open for them.

* fix(knowledge-base): make the capacity reachable and witness the composition (#17008)

A review of this PR found two defects and one dishonest close edge.

REACHABILITY. NEO_KB_ASK_MAX_PARALLEL was absent from every file under
ai/deploy/, verified with a positive control (the sibling NEO_KB_ASK_MODEL
appears in three). So the "operator-adjustable" control could not be set on any
deployment. This is the exact defect diagnosed on the miniSummary window leaves
four hours earlier — env-bound leaf with no compose surface — and then committed
here. Now carried through both canonical profiles.

COMPOSITION WITNESS. The spec asserted buildAskRequestQueue(...).capacity, which
proves the helper builds a queue and says nothing about asks using it: deleting
chatRequestQueue from the call site left the whole suite green. That is testing
the proxy and calling it the delivery. buildAskChatModelOptions now returns the
exact object handed to buildChatModel, so an arm asserts what the provider layer
receives. Mutation-verified: removing the injection fails exactly that arm.

CLOSE EDGE. This PR delivered admission only while Resolves #17000 assigned that
ticket's remainder — endpoint, exclusivity, attribution, model decision, L3
witness — back to a ticket it declared resolved. The narrower leaf carves out
the part this PR fully closes; the parent keeps the endpoint work, which needs
host provisioning that is not mine.

Evidence: L2 — 35/35 across SearchService and InteractiveBatchQueue specs.
Mutation-verified in both directions: removing the injection fails exactly the
new composition arm; the compose absence was confirmed with a sibling positive
control before and after.

* test(knowledge-base): witness the CONSTRUCTOR's composition, not the builder's (#17008)

Third round on one defect, and the shape is the finding: each previous fix
asserted the newly-extracted pure function while the unwitnessed edge moved UP to
its caller.

  round 1  asserted buildAskRequestQueue().capacity
           -> removing the injection from the call site stayed green
  round 2  asserted buildAskChatModelOptions()
           -> removing that CALL from construct stayed green (reviewer's mutation)

Both proved a builder. Neither proved this service composed anything. Extracting
a pure function makes the composition assertable and simultaneously creates a new
untested edge — caller to pure function — so the assertion has to come off a
value that actually travelled, not one the spec can rebuild.

`SearchService.askRequestQueue` is published from the SAME options object handed
to buildChatModel. It is a receipt, not a second construction: bypass the call in
construct and it is null, which no rebuilt-in-the-spec assertion can detect.

Evidence: L2 — 22/22 in SearchService.spec, 36/36 with the queue spec.
Mutation-verified against the reviewer's exact bypass (helper and its spec left
intact, construct rewired to an inline object): fails EXACTLY the new arm,
1 failed / 21 passed. Restored 22/22.

* chore(lint): declare the ask parallelism knob in the compose census (#17008)

Surfaced only after rebasing onto dev: the compose default-parity census counts
AND names every NEO_/MCP_ key in the canonical profile, so adding
NEO_KB_ASK_MAX_PARALLEL to docker-compose.yml left it reported as `unexpected`
even once the count matched.

The count alone is not the gate, and that is the better design: a new compose
surface has to be NAMED in a bucket, so a reviewer sees which knob was added
rather than that some number moved. Declared under optionalOverrides, which is
its actual class — it carries a default and is not a required deployment input.

--update-parity does not touch this block. It updates declared leaf paths; the
census is hand-maintained, which is correct for a record whose purpose is that
adding a deployment knob be a deliberate reviewed act.

Second expectation site for one change, after the leaf-path snapshot earlier in
this branch. Both are now recorded."

