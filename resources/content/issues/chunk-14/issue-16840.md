---
id: 16840
title: resolveMemorySaturationScope's JSDoc still documents the envelope-as-authority contract it replaced
state: CLOSED
labels:
  - bug
  - documentation
  - ai
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-09T22:57:18Z'
updatedAt: '2026-08-10T07:20:48Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16840'
author: neo-opus-vega
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-10T07:20:48Z'
---
# resolveMemorySaturationScope's JSDoc still documents the envelope-as-authority contract it replaced

## Context

`#16630` Slice B (PR `#16779`, merged `5fe2725f2922`) removed the envelope's scope authority from `resolveMemorySaturationScope` after @neo-gpt falsified it: an unreadable `docker inspect` refused with the same word — `not-node` — as a genuine non-Node service, so an unknown identity could manufacture an authoritative container-scoped `memory-saturation` and reach `diagnosed → throttle-shed`.

The implementation was repaired. **Its docblock was not**, and still teaches the defect.

## The Problem

`ai/daemons/orchestrator/services/ContainerHealthDiagnosisService.mjs`, JSDoc immediately above `resolveMemorySaturationScope`:

> **`container`** — no envelope, or an envelope explicitly reporting `not-node`.

The inline comment eleven lines below it, in the shipped body:

> **`nodeCommand === false` is the ONLY thing that licenses the container ratio**, and the envelope deliberately does not count — even when every envelope in the window says `not-node`.

The two are contradictory, and the docblock is the one a reader reaches first. Its `@param` list documents `samples` only; **`nodeCommand` — the destructured parameter that decides the entire outcome — is not mentioned at all.**

**Why this is more than cosmetic.** The docblock is the contract surface: it is what the doclet pipeline ingests, what the docs app renders, and what a future consumer copies when adding a second scope resolver. It currently instructs that reader to treat the envelope as scope authority — precisely the reasoning that produced the false `throttle-shed`. A stale docblock describing a *repaired* defect is worse than one describing nothing, because it reads as a design decision rather than an omission.

## The Architectural Reality

- The two authorities are asymmetric on purpose: `nodeCommand` is read live per collection, while envelopes ride on **retained** samples — so an all-`not-node` window held from earlier collections could silently outvote a live `nodeCommand: true`. That asymmetry is the reason the envelope was demoted, and it appears nowhere in the docblock.
- The producer now distinguishes `identity-unknown` from `not-node` (`DeploymentStateBridgeService`), but the consumer deliberately does **not** depend on that distinction being correct. That independence is the durable half of the fix and is undocumented.

## The Fix

Truth-fold the docblock to the shipped behaviour: state `nodeCommand === false` as the sole container licence, document the `nodeCommand` parameter, and record the retained-sample asymmetry as the reason rather than leaving it as an unexplained rule.

## Acceptance Criteria

- [x] The `container` bullet names `nodeCommand === false` as the sole licence and states explicitly that the envelope does not count, even when unanimous. **Met** — PR #16841.
- [x] `nodeCommand` is documented in the `@param` list. **Met** — typed `{Boolean|null}`, with `true` and `null` both routed away from the container ratio because "not Node" and "could not tell" must not collapse.
- [x] The docblock records **why** — retained-sample staleness, and consumer independence from the producer's `identity-unknown` distinction — so the rule is not re-litigated as an arbitrary constraint. **Met** — both reasons folded in as numbered points.
- [x] No behaviour change: `ContainerHealthDiagnosisService.spec` stays green unchanged, asserted as the control. **Met** — 87 passed, spec untouched. A docs-only change that moved a behavioural test would not be docs-only.
- [x] **Evidence level: L2.** A docs truth-fold closes on the unchanged spec plus the rendered doclet. **Met** — declared and unchanged.

## Out of Scope

- Any change to scope-resolution behaviour. The implementation is correct; only its description is stale.
- The producer's `identity-unknown` vocabulary (`#16630` / `#16763`).

## Avoided Traps

- **Deleting the stale bullet instead of replacing it.** The `container` scope is real and needs documenting; the defect is which authority licenses it.
- **Treating this as cosmetic.** The docblock is the ingested contract surface, and it currently teaches a falsified rule.

Live latest-open sweep: checked the latest 8 open issues (created-descending) at 2026-08-09T22:5xZ; no equivalent found. A2A in-flight claim sweep: no competing claim on this surface.

Origin Session ID: 4131135d-1b20-487f-9d23-d7213914246b

Retrieval Hint: `query_raw_memories("resolveMemorySaturationScope docblock envelope not-node nodeCommand sole licence retained samples")`

*Authored by Vega (@neo-opus-vega, Claude Opus 5, Claude Code).*



## Timeline

- 2026-08-09T22:57:18Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-09T22:57:19Z @neo-opus-vega added the `bug` label
- 2026-08-09T22:57:20Z @neo-opus-vega added the `documentation` label
- 2026-08-09T22:57:20Z @neo-opus-vega added the `ai` label
- 2026-08-09T22:57:20Z @neo-opus-vega added the `agent-os` label
- 2026-08-09T22:58:31Z @neo-opus-vega cross-referenced by PR #16841
- 2026-08-10T02:25:35Z @neo-opus-vega referenced in commit `3b1809e` - "docs(ai): read the docblock as ONE contract, not at the changed lines (#16840)

@neo-gpt's review found the fold incomplete, and the way it was incomplete is
the defect this ticket exists to remove -- reproduced by my own fix, eight
lines above it.

Two contradictions survived the first pass because I checked what I edited
rather than reading the block top to bottom as a new consumer would:

1. The opening paragraph still asserted that scope is read from the observation
   envelope rather than from a roster -- the exact premise Slice B retired --
   eight lines above the new bullet saying the envelope deliberately does not
   count. Both render as authoritative JSDoc, so leaving the retired premise
   adjacent to the repaired rule is worse than leaving nothing: a reader meets
   the wrong one first. Replaced with the axis split it should always have been
   -- identity from the live nodeCommand read, measurement from the envelope --
   and the retired premise recorded AS retired rather than silently deleted, so
   the next reader learns why it does not apply.

2. The heap bullet said "at least one usable V8 reading" while production
   requires usable.length === samples.length. Full coverage or nothing, and the
   reason is that a partial window is not a weaker heap signal but a DIFFERENT
   window than the count floor was calibrated against.

The generalizable half, and it is his: a truth-fold must be read as one
contract. Checking only the changed lines is how a docblock ends up teaching
both the repaired rule and the one it replaced.

No behavior change. ContainerHealthDiagnosisService.spec green unchanged,
87 passed, still the control."
- 2026-08-10T06:42:50Z @neo-opus-vega referenced in commit `4a3adc8` - "docs(ai): the two undocumented @returns fields are the two a caller cannot re-derive (#16840)

The @returns listed {scope, percents} while every branch returns four keys. The
omission was not evenly distributed: timestamps and unavailableReason are exactly
the two fields a consumer would otherwise reach past.

timestamps carries the SUBJECT's own observedAt stamps. samples[].observedAtMs is
sitting right there and looks equivalent — and substituting it is the
observer's-clock defect this same docblock spends a paragraph warning about. An
undocumented field whose obvious substitute is a named defect is worse than an
undocumented field.

unavailableReason reads as an internal diagnostic and is not one: it is published
verbatim in the heap-observation-unavailable fact, where it is the field naming
which repair is owed.

Also states that all four keys are present on every branch, so scope is what a
consumer reads to know which fields carry meaning, rather than probing for a key.

Evidence: check-jsdoc-types 1987 files / 0 unparseable; ContainerHealthDiagnosis
control spec 87 passed unchanged (a docblock has no behavioural witness — the
suite is a no-regression control, not conviction)."
- 2026-08-10T07:20:47Z @tobiu referenced in commit `a838189` - "docs(ai): truth-fold resolveMemorySaturationScope's docblock to shipped behavior (#16840) (#16841)

* docs(ai): truth-fold resolveMemorySaturationScope's docblock to shipped behavior (#16840)

The implementation was repaired in #16630 Slice B after @neo-gpt falsified the
envelope's scope authority. The docblock above it was not, and still taught the
defect: "container -- no envelope, or an envelope explicitly reporting
not-node", eleven lines above an inline comment stating the exact opposite.

nodeCommand -- the destructured parameter that decides the entire outcome --
was absent from the @param list.

That matters past tidiness. The docblock is the ingested contract surface: the
doclet pipeline reads it, the docs app renders it, and a future consumer adding
a second scope resolver copies it. It was instructing that reader to treat the
envelope as authority, which is precisely the reasoning that let an unreadable
inspect manufacture an authoritative container-scoped fact and reach
diagnosed -> throttle-shed. A stale docblock describing a REPAIRED defect is
worse than none: it reads as a design decision rather than an omission.

Folded in both reasons the envelope was demoted, because a rule without its
why gets re-litigated: the producer's not-node was a refusal gate rather than a
positive classification, and envelopes ride on RETAINED samples while
nodeCommand is read live -- so a stale window could silently outvote a live
identity.

No behavior change. ContainerHealthDiagnosisService.spec green unchanged,
87 passed, asserted as the control.

* docs(ai): read the docblock as ONE contract, not at the changed lines (#16840)

@neo-gpt's review found the fold incomplete, and the way it was incomplete is
the defect this ticket exists to remove -- reproduced by my own fix, eight
lines above it.

Two contradictions survived the first pass because I checked what I edited
rather than reading the block top to bottom as a new consumer would:

1. The opening paragraph still asserted that scope is read from the observation
   envelope rather than from a roster -- the exact premise Slice B retired --
   eight lines above the new bullet saying the envelope deliberately does not
   count. Both render as authoritative JSDoc, so leaving the retired premise
   adjacent to the repaired rule is worse than leaving nothing: a reader meets
   the wrong one first. Replaced with the axis split it should always have been
   -- identity from the live nodeCommand read, measurement from the envelope --
   and the retired premise recorded AS retired rather than silently deleted, so
   the next reader learns why it does not apply.

2. The heap bullet said "at least one usable V8 reading" while production
   requires usable.length === samples.length. Full coverage or nothing, and the
   reason is that a partial window is not a weaker heap signal but a DIFFERENT
   window than the count floor was calibrated against.

The generalizable half, and it is his: a truth-fold must be read as one
contract. Checking only the changed lines is how a docblock ends up teaching
both the repaired rule and the one it replaced.

No behavior change. ContainerHealthDiagnosisService.spec green unchanged,
87 passed, still the control.

* docs(ai): the two undocumented @returns fields are the two a caller cannot re-derive (#16840)

The @returns listed {scope, percents} while every branch returns four keys. The
omission was not evenly distributed: timestamps and unavailableReason are exactly
the two fields a consumer would otherwise reach past.

timestamps carries the SUBJECT's own observedAt stamps. samples[].observedAtMs is
sitting right there and looks equivalent — and substituting it is the
observer's-clock defect this same docblock spends a paragraph warning about. An
undocumented field whose obvious substitute is a named defect is worse than an
undocumented field.

unavailableReason reads as an internal diagnostic and is not one: it is published
verbatim in the heap-observation-unavailable fact, where it is the field naming
which repair is owed.

Also states that all four keys are present on every branch, so scope is what a
consumer reads to know which fields carry meaning, rather than probing for a key.

Evidence: check-jsdoc-types 1987 files / 0 unparseable; ContainerHealthDiagnosis
control spec 87 passed unchanged (a docblock has no behavioural witness — the
suite is a no-regression control, not conviction)."
- 2026-08-10T07:20:48Z @tobiu closed this issue

