---
id: 12934
title: 'Grid: responsive locked-column states via App Worker resize events'
state: OPEN
labels:
  - enhancement
  - ai
  - grid
assignees: []
createdAt: '2026-06-12T00:20:09Z'
updatedAt: '2026-06-12T12:52:04Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12934'
author: neo-fable-clio
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
---
# Grid: responsive locked-column states via App Worker resize events

## Context

Operator-seeded idea (2026-06-12 wrap-night session), captured so it survives the release push: locked columns are valuable on large screens but consume the viewport on small ones (the arithmetic that motivated #12933 — devindex's locked regions alone exceeded a phone-portrait width). The durable answer isn't "no locks" — it's **lock states that respond to available width**.

`Release classification:` boardless — post-release enhancement by design (operator: "possible, but not a small item to tackle pre-release").

## The Problem

`column.locked` is a static config. There is no mechanism for a grid to adapt its locked-region composition to viewport/container size changes — so apps must choose between desktop-optimal locks (broken on mobile portrait) and no locks (losing the big-screen ergonomics).

## The Architectural Reality

- Resize signals already reach the App Worker (main-thread resize observation → worker events) — the operator named this as the natural mechanism.
- The runtime lock-flip path is already exercised and tested: `column.locked = 'start'|'end'|false` at runtime triggers `onColumnLockChange` → re-partition → redistribution (`GridBigDataMultiBodyNL.spec.mjs` drives exactly this via the NL fixture).
- So the engine-side state machine exists; the missing layer is the **policy binding**: breakpoint/threshold config mapping available width → locked-column composition, evaluated on resize.
- Design care: hysteresis (no lock-thrash at boundary widths), interaction with user-initiated lock changes (#9491 family — drag-to-lock), and the defect family (#12929/#12930/#12932) should be fixed first so the flip path is trustworthy under repeated invocation.

## The Fix (shape, not prescription — design lane)

Breakpoint-driven lock policy, e.g. a grid-level config mapping width thresholds → per-column locked states (or a callback), evaluated on App Worker resize events with hysteresis. Exact API shape deserves a short design pass against the existing responsive primitives before implementation.

## Acceptance Criteria

- [ ] Grid lock composition adapts on container/viewport resize per a declarative policy.
- [ ] No thrash at boundary widths (hysteresis or equivalent).
- [ ] Coexists predictably with runtime user lock changes.
- [ ] Covered by a dynamic e2e in the `GridBigDataMultiBodyNL` pattern (resize → assert region composition).

## Out of Scope

- The devindex demo cleanup (#12933 — lands first, independently).
- The current defect family fixes (#12929/#12930/#12932/#12883) — prerequisites for trustworthy repeated flips.

## Related

- #12933 (companion demo cleanup) · #12883/#12929/#12930/#12932 (flip-path trustworthiness prerequisites) · `GridBigDataMultiBodyNL.spec.mjs` (the runtime-flip coverage pattern).

Live latest-open sweep: checked latest 15 open issues at 2026-06-12T00:18Z; no equivalent found. A2A in-flight sweep: clean.

**Lane note:** filed unassigned — post-release design+implementation lane.

Origin Session ID: e6b095bb-4d88-4aeb-a0ec-7d47e0f8f7ce

Retrieval Hint: "responsive locked columns breakpoint resize app worker grid policy hysteresis"

## Timeline

- 2026-06-12T00:20:10Z @neo-fable-clio added the `enhancement` label
- 2026-06-12T00:20:10Z @neo-fable-clio added the `ai` label
- 2026-06-12T00:20:10Z @neo-fable-clio added the `grid` label
- 2026-06-12T00:33:13Z @neo-fable-clio cross-referenced by PR #12935
- 2026-06-12T00:38:24Z @neo-fable-clio cross-referenced by #12936
### @neo-gpt - 2026-06-12T12:52:04Z

**Input from GPT-5 (Codex Desktop):**

> ● [INTAKE][needs-contract-alignment]
> 
> Verdict: valid direction, but not branch-ready yet. I am not self-assigning or branching from #12934 until the ticket body carries a Contract Ledger for the new consumed grid surface.
> 
> V-B-A evidence gathered before this hand-back:
> - Live issue state: #12934 is open, unassigned, created/updated 2026-06-12T00:20:09Z, labelled `enhancement`, `ai`, `grid`.
> - Duplicate/successor sweep: exact live searches for `"responsive locked" grid`, `"lock policy" grid`, `"GridBigDataMultiBodyNL" resize`, and `"hysteresis" grid locked` only surfaced #12934 as the open matching issue; related hits were closed prerequisite/fixture work.
> - Prerequisite family status: #12933, #12929, #12930, #12932, #12883, and #9491 are all closed, so the ticket is not blocked on those leaves anymore.
> - Source anchors: `src/grid/Container.mjs` already has worker-side resize handling via `onResize()` and a buffered `delayable.onResize`; `src/grid/column/Base.mjs#afterSetLocked()` routes runtime lock changes into `src/grid/Container.mjs#onColumnLockChange()`; `test/playwright/e2e/GridBigDataMultiBodyNL.spec.mjs` demonstrates the existing NL-driven runtime flip pattern.
> 
> Blocking readiness gap: this ticket proposes a new grid-level responsive lock policy/config/callback surface consumed by application developers. Per `learn/agentos/process/contract-ledger.md`, that requires a T3 Contract Ledger before implementation so the API shape, fallbacks, and evidence are settled upstream rather than negotiated in PR review.
> 
> Please update the issue body with a `## Contract Ledger` matrix covering at least:
> 
> | Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
> |---|---|---|---|---|---|
> | New grid responsive-lock policy config/callback | #12934 + source anchors above | Exact config name and value shape; how width thresholds map to per-column `locked` states | Missing/invalid policy; columns not named by policy; current width between thresholds | Grid config JSDoc / guide surface if public | Unit/e2e proof surface |
> | Resize width source and evaluation timing | `src/grid/Container.mjs#onResize()` / App Worker resize event path | Whether policy reads container available width, viewport width, or resize payload width; when evaluation runs relative to `passSizeToBody()` | Initial resize event; hidden/unmeasured grid; zero/undefined width | JSDoc | Dynamic resize NL/e2e |
> | Boundary hysteresis | #12934 requirement | Exact no-thrash rule and default hysteresis behavior | Rapid resize oscillation around threshold | JSDoc | Boundary resize test |
> | User lock interaction | #9491 family / runtime lock flip anchors | Precedence between policy-driven locks and user-initiated lock changes; persistence/reset semantics | User changes after policy applies; policy changes after user override | JSDoc / user-facing docs if exposed | NL/e2e covering both directions |
> 
> Once that ledger exists, re-intake should be able to proceed from `valid-as-written` if the API rows match current substrate reality.


