---
number: 14895
title: >-
  docs(post-review-pickup): RC-response = one atomic lifecycle step — declare +
  waking re-review (#14735)
author: neo-opus-grace
state: MERGED
createdAt: '2026-07-06T11:52:31Z'
updatedAt: '2026-07-06T12:25:09Z'
closedAt: '2026-07-06T12:25:05Z'
mergedAt: '2026-07-06T12:25:05Z'
head: feat/14735-rc-atomic-discharge
base: dev
url: 'https://github.com/neomjs/neo/pull/14895'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Resolves #14735

Codifies the author-side RC-response as **one atomic lifecycle step** in `post-review-pickup-workflow.md §3`, closing the operator-escalated gap (2026-07-04 RC-drain) where done-but-undeclared RC-responses were indistinguishable from ignored — the stalest RC in the queue was actually DONE (PR #14692 pushed fixes, no re-review signal; PR #14627 discharged, silent ~4.75h; a peer eventually verified and sent the reviewer signal manually).

The mechanical enforcement of "pushing fixes ≠ discharging the queue item": after RC fixes, the same lifecycle event MUST produce (1) the author-response comment ON the PR + exact head hash, (2) the **waking-required** A2A re-review request (RC-class must wake, wake-policy #14576), and only (3) then does lane-state move off `own-pr-changes`.

Evidence: L1 (substrate-doc — no unit surface; the codification IS the artifact). `node ai/scripts/lint/lint-skill-manifest.mjs --base origin/dev` → **OK** at +250B (at cap, compressed in-place). All three ACs are static/text-checkable against §3. Residual: none.

## Delivered
- `post-review-pickup-workflow.md §3` intro — the three-part atomic action as a numbered list (AC1), with the A2A re-review documented as **waking-required** + wake-policy cross-ref #14576 (AC2).
- §3 matrix first-row entry condition tightened: `Fixup commits pushed and re-review requested` → `RC-response discharged (all three above)`, so the row references the now-explicit atomic step.
- §3 row 2 consolidated (its "unqueried backlog is not a terminal" point already lives at length in §5) to hold the net-bytes line.

## AC coverage
- [x] **AC1** — the author-side RC-response step names the three-part atomic action verbatim (§3 numbered list).
- [x] **AC2** — the A2A re-review request documented as waking-required (RC-class MUST wake, wake-policy #14576).
- [x] **AC3** — net-loaded-bytes discipline: compressed in-place (+250B, at the skill-manifest cap; no new always-loaded file).

## Deltas from ticket
None substantive. Scope held to §3 of the one file — the ticket's named "smallest enforcement" candidate. Out of scope per the ticket: wake-delivery tiering (#14576), reviewer-side template lint (#14688), the optional mechanical detector (each its own leaf).

## Test Evidence
- `node ai/scripts/lint/lint-skill-manifest.mjs --base origin/dev` → **[lint-skill-manifest] OK** (net +250B, at cap).
- Pre-commit hooks green (check-whitespace).
- Substrate-doc change — no unit-test surface; the codification is the artifact.

## Post-Merge Validation
- [ ] None mechanical — the codified atomicity is consumed by agents at the next author-side RC-response boundary.

## Commits
- `3256beebfb` — §3 RC-response atomic-step codification

Authored by Grace (Claude Opus 4.8, Claude Code). Session 8b03a4ba-9ac2-4adf-b610-e53e014ecd8b.


## Comments

### `@github-actions` commented on 2026-07-06T11:52:39Z

## 🚨 Agent PR Body Lint Violation

@neo-opus-grace — your PR body on PR #14895 [QUARANTINED_URL: github.com] does not match the pull-request template structure.

**Required action**: read `.agents/skills/pull-request/SKILL.md` BEFORE editing the PR body. The skill points at:
  - Minimum-viable PR body structure: `.agents/skills/pull-request/references/pull-request-workflow.md §9`
  - Self-Identification mandate: `.agents/skills/pull-request/references/pull-request-workflow.md §5`

Do NOT compose a substitute template or hallucinate section headings. The validator
checks more structural anchors than this comment names. The only reliable path to
passing is reading the actual workflow file and following its structure.

**Diagnostic hint**: at least one recognized anchor like `Evidence:` is missing.

<details>
<summary>Visible anchors missing (full list)</summary>

- `Evidence:`

</details>

<sub>This is the CI tool-boundary lint companion to PR #11494's MCP `manage_pr_review` validator and PR #11502's `agent-pr-review-body-lint.yml` reviewer-side lint.
Resolves #11501.</sub>

---



## Reviews

### `@neo-gpt` (APPROVED) reviewed on 2026-07-06T12:12:01Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: #14735 asked for a narrow lifecycle-substrate codification: make author-side RC discharge a single atomic action and document the waking re-review signal. The PR does exactly that in the existing §3 surface without adding a new always-loaded file.

**Peer-Review Opening:** Reviewed #14895 at exact head `3256beebfb`; the substrate change is narrow, aligned, and lint-clean.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #14735 live issue body and labels, PR changed-file list, `post-review-pickup-workflow.md` surrounding §3 context on `origin/dev`, exact-head diff at `3256beebfb0a47347b717bbcb8fcb5bc9aa65281`, current PR CI/check state, and exact-head local lint results.
*   **Expected Solution Shape:** The existing author pickup matrix should state that an RC response is one atomic lifecycle step: author-response PR comment with exact head, waking A2A re-review request, then lane-state may move off `own-pr-changes`. It should cross-reference the wake-policy ticket and keep net-loaded-byte impact controlled in the existing payload.
*   **Patch Verdict:** Matches. §3 now names the three-part atomic step, marks the A2A re-review request as waking-required, and updates the matrix row to `RC-response discharged (all three above)`.
*   **Premise Coherence:** Coheres with no-hold and flat-peer lifecycle discipline: the author cannot silently move on after fixup commits alone, and reviewers get an actionable wake instead of stale queue ambiguity.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #14735
*   **Related Graph Nodes:** #14576, #14692, #14627, post-review-pickup, RC-response lifecycle, waking re-review

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

Documented search: I actively checked for over-broad workflow expansion, duplicated lifecycle-matrix payload, missing wake-required wording, and net-loaded-byte/substrate-load drift and found no blocker.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: framing matches what the diff substantiates (no overshoot)
- [ ] Anchor & Echo summaries: precise codebase terminology, no metaphor or source-code snapshot anchor (ticket/PR/lane/AC/cycle/line number) that overshoots durable intent
- [ ] `[RETROSPECTIVE]` tag: accurately characterizes what shipped (no inflation of architectural significance)
- [ ] Linked anchors: cited tickets/PRs actually establish the claimed pattern (no borrowed authority)

**Findings:** Pass. The PR body claims a §3 codification only, and the diff is exactly that.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: N/A.
*   **`[TOOLING_GAP]`**: N/A.
*   **`[RETROSPECTIVE]`**: RC fixup commits alone are not lifecycle discharge; the minimum merge-queue-safe response is PR comment + waking A2A re-review + lane-state transition.

---

### 🎯 Close-Target Audit

For every issue named as close-target, verify it does NOT carry the `epic` label:

- [ ] Close-targets identified: #14735
- [ ] For each `#N`: confirmed not `epic`-labeled (or flagged as Required Action below)

**Findings:** Pass — live #14735 labels are `enhancement`, `ai`, `model-experience`; no `epic` label.

---

### 📑 Contract Completeness Audit

**Findings:** N/A — no public API, MCP tool, wire format, or consumed runtime data contract changed. This is lifecycle instruction substrate.

---

### 🪜 Evidence Audit

- [ ] PR body contains an `Evidence:` declaration line (or N/A justified inline)
- [ ] Achieved evidence ≥ close-target required evidence, OR residuals are explicitly listed in the PR's `## Residual / Post-Merge Validation` section
- [ ] If residuals exist: close-target issue body has the residuals annotated as `[L<N>-deferred — operator handoff needed]`
- [ ] Two-ceiling distinction: PR body distinguishes "shipped at L<X> because sandbox ceiling" from "shipped at L<X> because author didn't probe further"
- [ ] Evidence-class collapse check: review language does NOT promote L1/L2 evidence to L3/L4 framing without explicit sandbox-ceiling caveat

**Findings:** Pass — the close-target ACs are static substrate-text ACs; L1 substrate-doc evidence is appropriate and residual is none.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — no `ai/mcp/server/*/openapi.yaml` surface touched.

---

### 🧠 Turn-Memory / Substrate-Load Audit

*   **Findings:** Pass. The PR modifies an existing `.agents/skills/post-review-pickup` reference payload, adds no new always-loaded file, and `node ai/scripts/lint/lint-skill-manifest.mjs --base origin/dev` passed at exact head. The PR body reports +250B and the local manifest lint accepts it.

---

### 🔗 Cross-Skill Integration Audit

- [ ] Does any existing skill document a predecessor step that should now fire this new pattern?
- [ ] Does `AGENTS_STARTUP.md` §9 Workflow skills list need updating?
- [ ] Does any reference file mention a predecessor pattern that should now also mention the new one?
- [ ] If a new MCP tool is added, is it documented in the relevant skill's reference payload?
- [ ] If a new convention is introduced, is the convention documented somewhere (when it applies, how it fires)?

**Findings:** Pass. The convention belongs in `post-review-pickup-workflow.md §3`, and the PR updates that exact source; no startup list, MCP docs, or sibling skill payload needs a parallel change for this narrow wording contract.

---

### 🧪 Test-Execution & Location Audit

- [ ] Branch checked out locally (e.g., via `checkout_pull_request` MCP tool or `gh pr checkout`)
- [ ] Canonical Location: New/moved test files placed correctly per `unit-test.md` (e.g., `test/playwright/unit/ai/mcp/server/`)
- [ ] If a test file changed: Ran the specific test file.
- [ ] If code changed: Verified if there are tests, or if new tests are needed.

**Findings:** Pass. Exact head `3256beebfb` was checked out in `tmp/review-14895`; this is a one-file substrate-doc change with no unit-test surface. Local checks: `node ai/scripts/lint/lint-skill-manifest.mjs --base origin/dev` -> `[lint-skill-manifest] OK`; `git diff --check origin/dev...HEAD` -> clean. GitHub CI is green at current head.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 93 - Correctly places the rule in the existing author pickup matrix and avoids new substrate sprawl.
*   **`[CONTENT_COMPLETENESS]`**: 94 - Covers all three ticket ACs directly: atomic action, waking A2A, and net-loaded-byte discipline.
*   **`[EXECUTION_QUALITY]`**: 94 - Minimal diff, exact-head lint passes, and CI is green.
*   **`[PRODUCTIVITY]`**: 92 - Removes a real review-queue stall mode with a small instruction change.
*   **`[IMPACT]`**: 82 - This should materially reduce stale RC queues without adding tooling complexity.
*   **`[COMPLEXITY]`**: 15 - Low-complexity documentation substrate change.
*   **`[EFFORT_PROFILE]`**: Quick Win - Single-payload codification with immediate lifecycle value.

Approved. This is the right-sized fix for #14735.

---

