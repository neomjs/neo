---
number: 17106
title: >-
  feat(ai): share one provider-timeout notification across both embedding lanes
  (#16997)
author: neo-opus-grace
state: OPEN
createdAt: '2026-08-14T10:07:34Z'
updatedAt: '2026-08-14T10:17:59Z'
closedAt: null
mergedAt: null
head: agent/16997-shared-provider-timeout-notification
base: dev
url: 'https://github.com/neomjs/neo/pull/17106'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Related: #16997
Related: #17072

Lands the shared mechanism #16997 specifies — Fix steps 1-5 — so a typed provider timeout reaches the caller's tenant-run circuit before either local embedding lane can dispatch again. **This does not close the ticket**: the two-repo composition witnesses are not in this PR, and the close-target stays open for them (see Scope below).

Evidence: L2 (pure predicate matrix + shared-helper containment, plus the existing provider and tenant-circuit suites green through the rewired paths) → L3 required for the ticket's AC-2/AC-3/AC-5/AC-10 composition witnesses, which are NOT claimed here. Residual: the two-repo cap-1 fixtures, Residual-Owner: #16997 (the close-target itself, deliberately left open).

## Scope — why this is `Related` and not `Resolves`

#16997 carries 11 ACs. This PR builds the mechanism all of them stand on and proves the unit-level ones; it does not author the two-repo compositions that prove the outcome end to end.

| AC | State here |
|---|---|
| AC-1 one common implementation | **Delivered** — both providers call the same predicate and the same notification helper; no provider-specific copy of timeout matching, containment, or ordering was added |
| AC-6 final failure only | **Delivered by placement** — the helper runs in the drain's task-level catch, after `#postOpenAiCompatible` has exhausted its contention/unload retries, so intermediate attempts cannot reach it |
| AC-7 negative matrix | **Delivered** — pinned as a spec, including abort, circuit-open, generic, and busy/model-load codes |
| AC-8 post-open hook containment | **Delivered** — helper contains the hook error and lets the source provider error stand; the throw-before-open case is documented as outside the guarantee rather than silently handled |
| AC-9 honest settlement | **Delivered** — nothing here claims server-side settlement or aborts dispatched work; Ollama admission still releases from provider settlement |
| AC-11 no false provider neutrality | **Delivered** — the boundary is named in the helper's JSDoc; Gemini is untouched |
| AC-2 / AC-3 / AC-4 / AC-5 / AC-10 | **Not claimed** — each needs a two-repo cap-1 composition, and @neo-opus-vega's pickup note is the reason they are not improvised here (below) |

Splitting at the mechanism boundary was deliberate: the composition fixtures need to be authored against selector semantics that moved three times this week, and a fixture written against the wrong semantics passes for the wrong reason.

## Deltas from ticket

**Step 2 is narrower than "replace the repeated four-code classifiers", on purpose.** The census found four sites in three shapes, and two are not pure four-code classifiers:

- `VectorService`'s cause-walk also matches `ABORT_ERR` and `KB_VECTOR_EMBED_PROVIDER_CIRCUIT_OPEN`. Those are **composed** around the shared predicate, never folded into it — an abort and an open circuit are caller-owned facts with different outcomes, and absorbing them would make a cancelled request read as a provider timeout at every consumer at once, contradicting AC-7.
- `TextEmbeddingService.isOpenAiCompatibleContentionTimeoutError` matches **three** codes plus an HTTP contention regex — `PROVIDER_TIMEOUT` is absent, because the OpenAI-compatible embedding transport never stamps it. Substituting the four-code predicate there would widen contention retry rather than deduplicate it, so it is left alone with a comment saying why. The ticket names only `VectorService` and the drain cycle for this step, so this matches the prescription as written.

**One behaviour change beyond the ticket's letter, called out rather than buried.** The native Ollama site previously notified only on `PROVIDER_TIMEOUT`; through the shared helper it now notifies on the full typed set. A native request dying at the socket layer (`ETIMEDOUT` / `ESOCKETTIMEDOUT`) was invisible to the circuit there — the same defect in the native path that this ticket fixes in the queued one. AC-1 requires one shared predicate, so this follows from the ticket; it is still a widening and reviewers should treat it as one.

**A threading gap the ticket implies but does not state.** Step 5 says the `onProviderTimeout` control is "already-normalized" — true at the public entry points, but `#embedOpenAiCompatibleBatch` did not accept it at all, so the queue task could not reach the hook even after wiring. Both hops now carry it.

## Test Evidence

- `npm run test-unit -- test/playwright/unit/ai/provider/createTimeoutError.spec.mjs test/playwright/unit/ai/daemons/embed/drainCycle.spec.mjs test/playwright/unit/ai/services/memory-core/TextEmbeddingService.retry.spec.mjs test/playwright/unit/ai/services/memory-core/TextEmbeddingService.spec.mjs test/playwright/unit/ai/services/knowledge-base/VectorService.batchFailureIsolation.spec.mjs` — **121 passed**.
- `npm run test-unit -- test/playwright/unit/ai/daemons/orchestrator/services/TenantRepoSyncService.spec.mjs test/playwright/unit/ai/services/knowledge-base/embedFailureClassification.spec.mjs` — **163 passed**, including the existing tenant-circuit witness that AC-5 requires stay green through the shared helper.
- One assertion in the new spec was **rewritten after it passed while proving nothing**: it asserted `Object.isFrozen(PROVIDER_TIMEOUT_CODES)` and would have shipped as a guarantee, but `Object.freeze` does not stop `Set.prototype.add` (verified: size goes 1 → 2 on a frozen set). A frozen exported set is a shared mutable classifier any one consumer could widen for all of them. The durable fix was to stop exporting it; the spec now asserts the module exports no mutable collection at all.
- Pre-commit gates green on both commits: whitespace, shorthand, AiConfig test-mutation, atomic-write shape, derived-domain, JSDoc types, ticket archaeology, block alignment, parse, OpenAPI service parity.

## Post-Merge Validation

- [ ] None required for what this PR claims — every delivered AC is pre-merge verifiable. The unclaimed ACs are follow-up work on #16997, not deferred validation of this diff.

## Evolution

The ticket already carried its own intake chain, and citing it was cheaper than repeating it: @neo-gpt posted a `needs-narrowing` verdict backed by an empirical falsifier (cap 1, A holding the provider, B queued, a hook throwing before opening its circuit — B dispatched), and @neo-gpt-emmy narrowed AC-8 to the implementable containment cell in response. That narrowed cell is what the helper's contract documents.

The pickup revalidation @neo-opus-vega asked for is also why the composition fixtures are not in this PR: `TextEmbeddingService`'s queue region moved three times this week, and the post-timeout next-selection can now be an interactive task before the queued repository's batch. A fixture asserting "B makes zero provider calls" written against the older selector could pass for the wrong reason, which is worse than not having it yet.

Authored by Grace (Claude Opus 5, Claude Code). Session 471d17f2-777c-4676-a137-fa37a9ac834d.
