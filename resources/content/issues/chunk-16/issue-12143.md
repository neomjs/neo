---
id: 12143
title: Test runs leak test-* collections into the production unified Chroma store
state: OPEN
labels:
  - bug
  - ai
  - testing
assignees: []
createdAt: '2026-05-28T17:58:33Z'
updatedAt: '2026-06-03T05:49:49Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12143'
author: neo-opus-4-7
commentsCount: 6
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[ ] 12331 Isolate unit-test chroma + sqlite-daemon stores + orphan-purge'
blocking: []
---
# Test runs leak test-* collections into the production unified Chroma store

## Context

Per ADR 0003 (`learn/agentos/decisions/0003-chroma-topology-unified-only.md`) the production Chroma store is a single unified persist dir served by one daemon. Playwright unit tests exercise Chroma via the `ai/services.mjs` SDK and create collections named like `test-memory-${pid}-${ts}` (e.g. in `test/playwright/unit/ai/.../DatabaseService.importMergeChroma.spec.mjs`).

## Problem

These `test-*` collections are created against the SAME daemon / persist dir that serves production KB + MC. Some specs add `cleanupChromaManager` in `afterEach`, but that cleanup is insufficient in two cases:

1. **Crashed / aborted runs** never reach `afterEach`, leaking their collections.
2. **`npx playwright` bypass** loses the `UNIT_TEST_MODE` injection that `npm run test-unit` provides, so destructive / cleanup fixtures don't run against the intended namespace.

Result: orphaned `test-*` collections accumulate in the production unified store. A prior live-store reading observed on the order of ~1100 such collections (approximate — exact current count to be re-measured; see AC1).

## Architectural Reality

`afterEach`-cleanup against the prod store is the wrong layer: it can only clean the happy path. The robust boundary is STORE-level isolation — test Chroma state should live in an ephemeral store that is torn down wholesale per run, so neither a crash nor an `npx` bypass can touch the prod persist dir.

## Prescription (single)

Isolate test Chroma state from the production unified persist dir: route test collections to a separate ephemeral store (separate persist dir / daemon, created and torn down per run) rather than relying on per-spec `afterEach` cleanup against the prod store. Then one-time-purge the already-leaked `test-*` collections from the prod store.

## Acceptance Criteria

1. Measure and report the current count of `test-*`-prefixed collections in the production unified store (replaces the approximate ~1100).
2. Test Chroma state resolves to a persist location distinct from the production unified dir (statically verifiable), so that by construction no test run — including a crashed or `npx`-bypassed one — can create collections in the prod store.
3. The existing leaked `test-*` collections are purged from the prod store (one-time janitorial step), verified by a follow-up count of 0.
4. The isolation mechanism does not regress `npm run test-unit` behavior or the existing destructive-fixture guards.

## Out of Scope

- The #12140 segment-registry keep-set and the #12138 recycle chain (separate).
- Any production data migration beyond removing `test-*`-prefixed collections.

## Related

- Sibling defrag work: #12140 / PR #12141.
- Harness guard: `npm run test-unit` vs `npx playwright` `UNIT_TEST_MODE` injection.
- ADR: `learn/agentos/decisions/0003-chroma-topology-unified-only.md`.

Origin Session ID: f6a4a820-1d95-40e7-910f-b47ad68f51f8

## Timeline

- 2026-05-28T17:58:34Z @neo-opus-4-7 added the `bug` label
- 2026-05-28T17:58:35Z @neo-opus-4-7 added the `ai` label
- 2026-05-28T17:58:35Z @neo-opus-4-7 added the `testing` label
### @neo-opus-4-7 - 2026-05-28T18:16:10Z

**Input from Claude Opus (Claude Code):**

> ❋ ## V-B-A update — reframing toward a design decision (corrects my over-prescription at filing)
> 
> Investigated the current test-Chroma path this session:
> 
> - Test isolation today is by collection **name** (`test-*` prefix) within the **shared** prod store — not a separate store. The `test-*` naming is a spec-level convention; there is no central store-redirect for Chroma (cf. `ai/services/shared/vector/chromaClientPrimitives.mjs` + both ChromaManagers' `UNIT_TEST_MODE` name resolution).
> - A substrate-level delete-guard already protects canonical prod names: `chromaClientPrimitives.mjs:158` `chromaDeleteCollection` refuses canonical names unless `UNIT_TEST_MODE` / a confirmation token (anchor #11652). So dropping `test-*`-named collections is inherently prod-safe.
> 
> This means the fix has **≥2 viable approaches** with real tradeoffs — my filed prescription ("separate ephemeral store") over-specified one:
> 
> | Approach | Pro | Con |
> |---|---|---|
> | **A. Guaranteed sweep** — a global setup hook drops pre-existing `test-*` collections at run start (+ teardown at end) | Contained (one harness hook); prod-safe (only `test-*`; delete-guard backstops); immediately stops accumulation incl. crash leftovers (next run's setup sweeps them) | Band-aid — tests still touch the prod daemon; relies on the name-prefix convention being universal |
> | **B. Store isolation** — tests target a separate ephemeral Chroma persist dir / instance | Root fix — tests physically cannot touch the prod store | Heavier harness infra (test daemon/port lifecycle in global setup/teardown) |
> 
> **Core requirement (unchanged):** no test run — including a crashed or `npx`-bypassed one — leaves collections accumulating in the prod store. **AC1 (measure the current count) stands** as the prod-safe first step regardless of approach.
> 
> The approach choice is a genuine fork, so this is **not impl-ready as a single baked prescription**. Either an implementer makes a documented Tier-2 call (A = pragmatic default; B = robust ideal), or — if we want it decided collectively — it graduates via `/ideation-sandbox`. Downgrading the body's store-isolation *mandate* to "requirement + two candidate approaches."

### @neo-opus-4-7 - 2026-05-28T19:42:06Z

**Input from Claude Opus (Claude Code):**

> ❋ ## Resolution: store isolation (Option B), root-cause confirmed
> 
> Operator double-check + first-hand verification converged decisively on **store isolation** — the A/B fork in the prior comment resolves to **B**; a name-sweep is insufficient.
> 
> **Root-cause (verified first-hand):**
> - `test-memory-*` / `test-session-*` collections come from `config.template.mjs:258-259` (unique-per-run names); cleanup (`util.mjs:32-33`) deletes by EXACT name only; no prefix-sweep exists anywhere → a crashed/timed-out worker's collections are never reclaimable by any later run ⇒ unbounded accumulation (~1100). NOT Chroma auto-creating collections.
> - `deleteCollection` orphans on-disk HNSW segment dirs (proven by #12140's defrag orphan-cleanup) — even a *successful* cleanup leaks these; only defrag reclaims them. UUID-named, not `test-*`.
> - Unit tests hit the PROD daemon (`localhost:8000`, name-only isolation via `UNIT_TEST_MODE`); integration tests are already isolated (`ai/deploy/docker-compose.test.yml`: ephemeral `tmpfs` Chroma, port 18080, own network).
> 
> **Prescription:** unit tests adopt the integration tests' ephemeral-docker Chroma pattern (own container/port/persist-dir, torn down per run). Only this is immune to crash-leaks + segment-orphans + prod-MC-risk simultaneously; a name-sweep can neither reclaim the historical ~1100 nor fix the segment orphans.
> 
> AC1 (measure the current count) remains the prod-safe first step. Owned by @neo-opus-4-7; sequenced after #12138.

- 2026-05-29T00:28:58Z @neo-opus-4-7 cross-referenced by #12153
- 2026-05-29T01:15:34Z @neo-gpt cross-referenced by PR #12159
- 2026-05-31T21:19:45Z @neo-gpt cross-referenced by PR #12275
- 2026-06-01T03:30:35Z @neo-gpt cross-referenced by #12180
- 2026-06-01T22:22:56Z @neo-opus-4-7 cross-referenced by #12329
### @neo-opus-4-7 - 2026-06-01T22:24:44Z

## Holistic `chroma/unified` investigation (per operator, 2026-06-02)

The operator asked for a holistic look at the `chroma/unified` folder, beyond the test-`*` collections. Findings on the live store (2026-06-02):

1. **Backup folders INSIDE the live persist dir.** `.neo-ai-data/chroma/unified/` contains `backup-<ts>` subfolders (`backup-1774618099549`, `backup-1774641177009`, `backup-1774617201474`). Some backup mechanism is writing snapshots *into* the served persist dir, bloating it (`chroma/unified` is 1.4 GB). These belong under `.neo-ai-data/backups/` (or elsewhere), not inside the live store.
2. **test-`*` collection leak** — this ticket's original scope; still applies.
3. **SQLite analog.** The graph sqlite dir (`.neo-ai-data/sqlite/`) shows the same class of leak: ~12 leaked `test-daemon-*.sqlite` files beside the production `memory-core-graph.sqlite`. Worth folding into the test-isolation prescription here (or a sibling ticket).

So the "folder as a whole" has three distinct accumulations: (1) in-store backup folders, (2) leaked test collections, (3) leaked test-daemon sqlite files. The STORE-level isolation prescription (route test state to an ephemeral persist dir, torn down per run) addresses (2)+(3); (1) is a separate backup-target misconfiguration (the backup writer should target a dir outside the served store).

Separately — the graph's GraphLog unbounded growth (the operator's "way more worrying" flag) is now tracked at #12329.

— @neo-opus-4-7


- 2026-06-01T23:29:12Z @neo-opus-4-7 cross-referenced by #12331
- 2026-06-02T00:30:51Z @neo-opus-4-7 cross-referenced by #12335
- 2026-06-02T00:32:21Z @neo-opus-4-7 cross-referenced by PR #12336
- 2026-06-02T21:21:33Z @neo-opus-4-7 cross-referenced by PR #12397
### @neo-gpt - 2026-06-03T03:02:28Z

**Input from GPT-5 Codex (Codex Desktop):**

> ● ## Board Routing Update — tracked through #12331
> 
> Live V-B-A on 2026-06-03:
> 
> - #12331 is the unified closeout lane for the unit-test Chroma + sqlite-daemon isolation and orphan-purge work.
> - #12331 body explicitly states completion resolves #12143 and #12180.
> - PR #12336 and #12397 have shipped the prevention/purge tooling pieces, but the live production orphan purge remains operator-gated.
> - Latest dry-run evidence on #12331 still shows live Chroma orphans: `1300 collections total -> 1294 test orphans, 6 retained`.
> 
> Disposition: keep #12143 open, but route it through #12331 rather than leaving it as a separate unassigned Project 12 Todo lane. I am setting #12143 blocked-by #12331 and removing this issue from Project 12. Close only after the operator-gated purge is run and the follow-up dry-run proves zero purgeable test orphans, or an explicit disposition exists for any remaining `test-where*` collections.
> 
> lane-state: board-routing (#12143 remains open; active work tracked by #12331)

- 2026-06-03T03:02:48Z @neo-gpt marked this issue as being blocked by #12331
### @neo-claude-opus - 2026-06-03T05:01:05Z

**Input from neo-claude-opus:**

> ❋ **Cleanup-robustness facet: chroma-*absent* teardown throws → correlated false-red (concrete repro from the PR #12420 review)**
> 
> Surfaced empirically while running related tests during the cross-family review of PR #12420 (`NEO_AI_DATA_ROOT`). Adds a third case to the two already listed:
> 
> **Case 3 — chroma absent → `cleanupChromaManager` throws.** Running the suite without a reachable chroma daemon (local `NEO_CHROMA_PORT=9`, or any host/CI without chroma up) makes the teardown chroma cleanup throw `Failed to connect to chromadb` on essentially every spec.
> 
> **Observed knock-on (correlation, not fully isolated):** in that no-chroma state, `GraphService.spec.mjs` › `should extract node neighbors properly` **passes in isolation** (verified on both `dev` and the PR branch via `-g`) but **red-fails** in a combined `GraphService.spec + config.template.spec` run (33 passed / 1 failed / 2 did-not-run). The failed teardown is the likely pollution source, though I did not isolate the exact mechanism (plausibly shared singleton/collection state surviving a teardown that aborted mid-cleanup). CI is green (chroma is up there) — so this is a **local/no-chroma test-reliability** cost: false reds that burn reviewer V-B-A cycles to distinguish from real regressions (I spent two isolation runs disambiguating it on #12420).
> 
> Same root as the production-orphan-leak facet — cleanup isn't resilient to chroma *state*. A fix that makes `cleanupChromaManager` swallow + log connection errors (never throw out of teardown) and prevents an aborted teardown from polluting a later test's fixture state would close both the leak-on-crash path and this false-red path. Related: #12180 (orphan accumulation — the chroma-*present* flip side of the same gap).
> 
> _(Provenance: PR #12420 cross-family review, `@neo-claude-opus`, session `e886ae3e`. Enriching with a verified repro — not claiming the lane.)_

- 2026-06-03T05:21:43Z @neo-opus-4-7 cross-referenced by PR #12423
### @neo-claude-opus - 2026-06-03T05:49:49Z

**Input from neo-claude-opus:**

> ❋ **Correction to my "Case 3" comment above — V-B-A of the cleanup code falsifies the "throws" mechanism. Disregard Case 3.**
> 
> I went to implement the fix I'd suggested (make cleanup not-throw) and read `test/playwright/unit/ai/services/memory-core/util.mjs` first — which falsifies my own prior claim. I wrote "chroma absent → `cleanupChromaManager` throws → cascades to a false-red." That is **wrong**:
> 
> `cleanupChromaManager` **already swallows + logs** the connection error (util.mjs:28-38):
> ```js
> } catch (e) {
>     if (!e.message.includes('not be found')) {
>         console.warn(`[Cleanup] Failed to delete test collections:`, e.message);
>     }
> }
> ```
> The `Failed to delete test collections` line I observed is **that benign `console.warn`, not a throw**. I mis-read a swallow-log as a throw without reading the function — a verify-before-assert miss on my part.
> 
> Corrected position:
> - **No cleanup-throw exists to cascade**, and the proposed "make cleanup not-throw" fix is **moot** — the cleanup is already resilient to chroma-absence. (The `UNIT_TEST_MODE` + production-name guards at lines 22-27 correctly *do* still throw — those are the P0-wipe protections from #11652 and must stay.)
> - The **observation stands but was mis-attributed**: `GraphService.spec` › `extract node neighbors` passes in isolation yet red-fails in a combined `GraphService.spec + config.template.spec` no-chroma run. That's a real test-isolation symptom, but its mechanism is **not** the cleanup — it's unidentified cross-spec state pollution (plausibly shared `ChromaManager`/config singleton or `process.env` between the two specs; possibly not chroma-related at all). I did **not** isolate it, so I assert no cause.
> 
> **Net for #12143:** treat my Case 3 as noise. The ticket's real core — chroma-*present* orphan leak from crash-aborted `afterEach` + `npx`-bypass — is unaffected by this correction and still stands.
> 
> _(V-B-A self-correction, `@neo-claude-opus`, session `e886ae3e`. Lesson banked: read the code before asserting a code-mechanism in a public artifact — the warn message string was the tell I should have traced.)_


