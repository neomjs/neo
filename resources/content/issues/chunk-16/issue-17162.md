---
id: 17162
title: An intake credential fault freezes corpus publication before a corpus exists
state: CLOSED
labels:
  - bug
  - ai
  - architecture
  - build
assignees:
  - neo-opus-ada
createdAt: '2026-08-15T09:32:36Z'
updatedAt: '2026-08-15T12:54:16Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17162'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-15T12:54:16Z'
---
# An intake credential fault freezes corpus publication before a corpus exists

## Context

`#17148` established the principle for this pipeline: **an optional DevIndex intake failure must not decide whether the corpus publishes.** It fixed that at the *stage* level — the four intake stages now defer instead of aborting emission.

The same defect exists one layer up, at the *step* level, and it is currently the larger exposure. Found while verifying the `#17150` credential work, not from a live outage — filing before it bites rather than after.

## The Problem

`.github/workflows/data-sync-pipeline.yml` runs, in order:

| line | step |
|---|---|
| 69 | Mint Publisher installation token |
| 83 | **Mint Intake installation token** |
| 125 | Checkout repository |
| 164 | Setup Node.js |
| 176 | Run bounded Data Sync emission and publish |
| 190 | Push Data to neomjs/pages |

The intake mint at :83 has **no `continue-on-error` and no `if:`**, so any failure there fails the job — *before checkout, before Node, before a single corpus byte is generated*. The corpus then freezes for a reason that has nothing to do with the corpus: the DevIndex intake identity is an **enrichment** credential, and the corpus is emitted under the `reader` identity, which the intake mint has no bearing on.

`actions/create-github-app-token` **fails the step** when the credential cannot serve the request. Measured both ways on live probes, and **both are real triggers**:

| injected fault | mint result | run |
|---|---|---|
| permission not granted to the installation | **HTTP 422** — *"The permissions requested are not granted to this installation"* | [31877595849](https://github.com/neomjs/neo/actions/runs/31877595849) |
| named repository has no installation | **404 Not Found** | [31879164402](https://github.com/neomjs/neo/actions/runs/31879164402) |

> **Reading caution, learned the hard way on this ticket.** Once `continue-on-error` is in place, a *failed* mint reports `conclusion: success` (only `outcome` keeps the failure), so it renders **green** in `gh run view --json jobs`. I read that field, concluded the mint could not fail, and briefly re-scoped this whole ticket on it — the guard had masked the failures that justify it. **Check the mint by reading its log.** Caught by @neo-gpt.

A third trigger, a rotated or expired private key, is untestable here. So the realistic triggers are ordinary administration, not exotic faults:

- a permission is revoked on `neomjs-devindex-intake` while the workflow still requests it;
- `DATA_SYNC_INTAKE_PRIVATE_KEY` is rotated or expires;
- the installation is removed from one of the DevIndex repos.

**This is newly likely.** `#17150` adds `permission-contents: write` to that mint because GitHub now infers collaborator status from contents-write, and @tobiu granted the App several permissions during that investigation with an explicit intent to clean up the extras. The workflow line and the App grant are now a coupled pair that must move together — and if they drift apart in the wrong direction, the failure is not "DevIndex opt-in is stale", it is "the whole corpus stops publishing", which is the 19-hour freeze `#17131` recorded.

## The Architectural Reality

- `.github/workflows/data-sync-pipeline.yml:83-109` — the intake mint, unguarded, ahead of everything.
- `.github/workflows/data-sync-pipeline.yml:125` — checkout; nothing before this contributes to the corpus.
- `buildScripts/dataSyncPipeline.mjs` — `emissionCommands`; the corpus stage runs under `tokenScope: 'reader'` (the implicit Actions token), and `content indexes and SEO` likewise. **Neither consumes the intake token.**
- `buildScripts/dataSyncPipeline.mjs` — `emitGeneratedData` already models the correct semantics for exactly this: a preflight denial is deferred, every stage still runs, the corpus publishes, and the deferred error is rethrown so the run still exits non-zero (`#17148`).

The runtime already knows how to degrade. The *workflow* does not, and it fails earlier.

## The Fix

Make an intake mint failure degrade rather than abort, mirroring the semantics `#17148` already shipped one layer down:

1. `continue-on-error: true` on the intake mint step (the Publisher mint stays fatal — it is the publish credential, and without it there is nothing to fix).
2. Pass the intake token through as it does today; when the mint failed, `steps.intake-token.outputs.token` is empty, and `scopedStageEnv('intake')` already produces a credential-less child, so the intake stages fail on their own terms and defer via `publishGeneratedProgressOnFailure`. No new error path is needed — the existing deferral absorbs it.
3. Ensure the run still exits non-zero and names the mint as the cause, so this cannot become a silent channel where the corpus publishes forever while DevIndex intake is quietly dead.

**Explicitly rejected — do NOT simply mark the step non-fatal and move on.** Without (3), a revoked intake permission would produce indefinitely green runs with a permanently dead intake, which is strictly worse than today's loud failure. The point is to move the failure *later*, not to remove it.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Intake mint step | `.github/workflows/data-sync-pipeline.yml:83` | tolerate failure; corpus still emits and publishes | Publisher mint stays fatal | inline comment | dispatch with a deliberately invalid intake permission ⇒ corpus commit still produced |
| Run exit status on mint failure | `buildScripts/dataSyncPipeline.mjs` deferral path | non-zero, naming the mint | unchanged when the mint succeeds | inline comment | same dispatch ⇒ run red, cause named |

## Decision Record impact

`none` — extends the stage-isolation property `#17148` established to the step layer; introduces no new architectural authority.

## Acceptance Criteria

- [ ] An intake mint failure no longer prevents checkout, emission, or publication: a corpus commit is still produced.
- [ ] That run still exits non-zero and names the intake mint as the cause — no silent green.
- [ ] The Publisher mint remains fatal.
- [ ] Verified by `workflow_dispatch` with a deliberately unsatisfiable intake permission, which fails the mint for real rather than simulating it.
- [ ] The coupling note added by `#17150` (workflow line and App grant move together) is updated to reflect that drift now degrades rather than freezes.

## Out of Scope

- The `#17150` permission decision itself.
- The DevIndex opt-in phase isolation (`#17150`), which is the same class inside the service rather than in the workflow.
- Any change to the Publisher identity or the two-App split (`#15744`).

## Avoided Traps

- **Assuming the runtime deferral already covers this.** It does not: `dataSyncPipeline.mjs` never executes, because the job dies three steps before it is invoked. The fix has to be in the workflow.
- **Making the step non-fatal without preserving the failure.** That trades a loud freeze for a silent dead intake — the carve-out-opens-a-silent-channel shape this pipeline has been bitten by before.
- **Treating this as hypothetical.** The trigger is a scheduled permission cleanup that is already planned.

## Related

- `#17148` / PR #17149 — the same blast-radius principle at the stage layer; this is its step-layer sibling.
- `#17150` / PR #17160 — adds the `contents` coupling that makes this newly reachable.
- `#17131` — the standing alarm that records what a corpus freeze costs (~19h).
- `#15744` — the two-identity split whose Publisher/intake asymmetry is the reason only one mint should be fatal.

Retrieval Hint: "intake mint failure aborts data sync before checkout; corpus freeze from an enrichment credential"





## Timeline

- 2026-08-15T09:32:37Z @neo-opus-ada added the `bug` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `ai` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `architecture` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `build` label
- 2026-08-15T09:38:14Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-15T09:40:34Z @neo-opus-ada referenced in commit `41f8335` - "probe: move the unsatisfiable permission to the INTAKE mint (#17162)"
- 2026-08-15T09:43:18Z @neo-opus-ada cross-referenced by PR #17165
- 2026-08-15T10:18:40Z @neo-opus-ada referenced in commit `4117b45` - "docs(build): correct the falsified mint-failure trigger (#17162)

An earlier revision of this comment claimed `create-github-app-token` fails
outright when asked for a permission the installation lacks, and used that to
argue a revoked App permission is a realistic trigger for the mint failure this
guard exists to absorb.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- not held by `neomjs-devindex-intake` -- and the mint SUCCEEDED. The
action does not validate requested permissions against the installation.

The ticket's premise survives on the triggers that do fail the mint for real: a
rotated or expired private key, or an installation removed from a named
repository. Those still kill the job before a corpus byte exists, which is the
defect. What changes is that a revoked PERMISSION degrades the capability -- and
#17148's deferral already absorbs that -- rather than freezing publication.

Also worth stating in-file because the inverse belief is actively misleading: if
you think the mint validates, a successful mint looks like proof the token
carries what was requested. It is not, and only a capability probe answers that."
### @neo-opus-ada - 2026-08-15T10:23:25Z

## I filed this on a premise I could not reproduce — and the live probe found the real failure mode instead

**Correcting my own ticket before it merges.** Two fault injections, neither produced the failure this ticket is named after.

| probe | injected fault | mint step | run |
|---|---|---|---|
| 1 | `permission-deployments: write` — a permission the App does not hold | **success** | [31877595849](https://github.com/neomjs/neo/actions/runs/31877595849) |
| 2 | `repositories: devindex-opt-in-does-not-exist` — no installation | **success** | [31879164402](https://github.com/neomjs/neo/actions/runs/31879164402) |

`create-github-app-token` validates neither the requested permissions nor the named repositories. **I could not make the intake mint fail.**

### What probe 2 did produce — and it is the more useful result

```
3. Mint Intake installation token -> success
...
[DataSync] attempt=1/2 base=f87cff0c5e
[DataSync] [DataSync preflight] No intake token was provided. The pipeline cannot reach the
DevIndex repositories without an installation token, and proceeding would spend a full
collection run to rediscover that.
```

**The step goes green and emits nothing.** `outputs.token` is empty, `DATA_SYNC_INTAKE_TOKEN` is empty, and `assertDataSyncAccess` rejects it by its falsy-token branch.

That is a *silently successful step with an empty output* — categorically different from the failed step this ticket describes, and it means `continue-on-error` **does not fire on it at all**. The protection that actually covers this path is `#17148`'s deferral, which already shipped.

### Consequence for this ticket and PR #17160's sibling

**The premise as filed is not demonstrated.** I wrote that "any intake credential fault killed the job before a single corpus byte existed" and listed three realistic triggers. Two are now falsified — a revoked permission and a removed installation both leave the mint green. Only a **rotated or expired private key** remains plausible, and it is untested because I cannot rotate that secret.

**What survives, honestly scoped:**

1. The **unit test** in PR #17165 (`an ABSENT intake token degrades exactly like a denied one`) pins the path probe 2 just exercised in production. That is now a live-verified contract rather than a speculative one, and it is the part I would keep if only one thing landed.
2. `continue-on-error: true` is **defence-in-depth for the one untested trigger**, not the load-bearing fix I claimed. It is one line and it is correct; it is not urgent.

**What I am NOT doing:** quietly leaving the ticket's framing in place because the diff is still fine. A future reader would inherit "an intake credential fault freezes corpus publication" as established, and it is not — the deferral already covers the reachable faults. The title overstates what was demonstrated, and the body's "newly reachable, not theoretical" paragraph is wrong in the direction that flatters my own filing.

### The generalisable half

**A green step is not a produced output.** I checked step conclusions to decide whether the mint worked, when the question was whether it emitted a token — the same envelope-versus-capability error as `#17148`'s preflight reading `repository{id}` instead of the stargazer connection, and the same one I made an hour ago reading mint-success as proof of held permissions. **Third instance today of trusting a wrapper's status instead of probing the thing itself.** That is a pattern, not a slip, and I have said so to @neo-gpt rather than waiting to be told.

Re-scoping the body and PR #17165 accordingly.

---

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


---

> **↩ THIS COMMENT IS RETRACTED — 2026-08-15, and everything it concludes is false.** It rests on reading `conclusion` from the jobs API as proof the mint succeeded. It did not: the probe branches carried `#17162`'s `continue-on-error` guard, which sets `conclusion: success` on a **failed** step while `outcome` keeps the failure. Raw logs: `31877595849` → `##[error]The permissions requested are not granted to this installation` (HTTP 422); `31879164402` → `##[error]Not Found` (404). **Both probes failed the mint**, so `create-github-app-token` *does* validate permissions and repository installations, and the original claims this comment withdrew were correct. Falsified by @neo-gpt. Kept for provenance; the corrected account lives in the issue body. Do not cite anything below this line.

- 2026-08-15T10:23:43Z @neo-opus-ada changed title from **An intake credential fault freezes corpus publication before a corpus exists** to **The intake mint emits an empty token on a GREEN step, and nothing pins that path**
- 2026-08-15T11:08:32Z @neo-opus-ada referenced in commit `216c2ee` - "docs(build): my correction was the false one -- both mint triggers are real (#17162)

Reverting my own correction. @neo-gpt falsified it and the raw logs confirm him,
going further than he did: BOTH probes failed the mint, not one.

  run 31877595849  ##[error]The permissions requested are not granted to this installation.
  run 31879164402  ##[error]Not Found - .../get-a-repository-installation-for-the-authenticated-app

I read `conclusion` from `gh run view --json jobs` and saw `success` on both, and
concluded the mint cannot fail. But `continue-on-error: true` sets
`conclusion: success` on a FAILED step while `outcome` retains the failure --
so the field I read is the one THIS PR's own guard rewrites.

The guard masked the failures that justify it, and I read that as evidence the
guard was unnecessary. A mechanism that converts failure into success makes its
own necessity invisible to any status-based check.

So the original premise stands and this PR is better evidenced than when it was
opened: run 31879164402 is valid L4 proof of the continuation -- mint failed,
job proceeded through checkout and Node to the preflight, which rejected the
empty token and left the corpus path intact.

Restores the trigger list, records both measured failure modes with their runs,
and adds the instruction that would have prevented the error: read the LOG, not
the step status, because once this guard exists a failed mint renders green."
- 2026-08-15T11:08:56Z @neo-opus-ada referenced in commit `38ff6a0` - "docs(build): restore the true coupling claim -- my correction was the false one (#17150)

Reverting f5ce78fe4a's correction. It asserted `create-github-app-token` does not
validate requested permissions, citing run 31877595849 as a success. The raw log
of that run says otherwise:

  ##[error]The permissions requested are not granted to this installation.

The step FAILED. I read `conclusion` from the jobs API, which `continue-on-error`
rewrites to `success` on a failed step -- and #17162's guard was present on that
probe branch. So the guard rewrote the field I used to judge whether the guard
was needed.

Consequence for the operator warning, restored to the stronger form: revoking
`contents` on the App while this line remains does not merely cost the stargazer
read, it fails the mint outright."
- 2026-08-15T11:09:58Z @neo-opus-ada cross-referenced by #17150
- 2026-08-15T11:10:00Z @neo-opus-ada changed title from **The intake mint emits an empty token on a GREEN step, and nothing pins that path** to **An intake credential fault freezes corpus publication before a corpus exists**
- 2026-08-15T11:10:02Z @neo-opus-ada cross-referenced by PR #17160
- 2026-08-15T12:53:16Z @tobiu referenced in commit `c03f3a5` - "fix(devindex): restore the stargazer read and stop a dead phase killing the working one (#17150) (#17160)

* fix(devindex): a denied stargazer read no longer takes the issue path down with it (#17150)

Opt-in has two INDEPENDENT intake mechanisms reading the same repository over
different connections: `stargazers` and `issues`. GitHub restricted access at
connection granularity -- stargazer reads limited to repository admins and
collaborators (announced 2026-06-30) -- so exactly one of the two is currently
unavailable.

The stargazer loop ran first and threw straight out of `run()`, so
`processIssues()` was never reached and opt-in intake went to ZERO rather than
halving. The repository-not-found branch had the same defect for the same reason:
it `return`ed out of `run()`, and `processIssues` already handles NOT_FOUND for
itself by returning null, so pre-empting it bought nothing.

The stargazer phase moves into `collectStargazerOptIns`, which returns its own
result instead of mutating `run()` state -- that is what makes the isolation
expressible, since the caller can now lose the phase to a failure without
holding half-updated state. `run()` wraps it, continues to the issue path, and
rethrows LAST, after the issue work is persisted.

Deferred, never swallowed. A catch that quietly returned would leave a dead
intake mechanism reporting success indefinitely, which is the failure mode that
let this go unnoticed until the corpus froze.

Coverage: every test verified RED against the pre-fix source INDIVIDUALLY --
`describe.serial` skips after the first failure, so one suite run proved only the
first test and would have hidden three vacuous ones.

Also strips pre-existing trailing whitespace on 12 lines and applies
block-alignment; the commit hooks check whole staged files, and moving the
stargazer loop brought both into scope.

Does NOT decide whether stargazer access is restored; that is the open half of
#17150 and an operator call on a credential trade-off.

* feat(build): grant the intake token the stargazer read via contents:write (#17150)

GitHub limited the stargazers endpoint to repository admins and collaborators
(announced 2026-06-30) and infers that status from contents-write. `metadata:
read` used to carry the read and no longer does, so the DevIndex star-based
opt-in has been dead since the restriction reached this installation on 08-14.

Measured rather than argued -- four `preflight_only` probes, one variable each,
each mutating nothing:

  issues:write + metadata:read   -> DENIED    (run 31870344604)
  + administration:read          -> DENIED    (run 31874111382)
  + contents:WRITE               -> reachable (run 31875177082)
  + contents:READ                -> DENIED    (run 31875769230)

Read does not suffice; there is no lesser lever. `devindex-opt-out.issues` stayed
reachable across all four, which rules out a sick token and isolates the finding
to the connection.

The cost is stated in the workflow rather than buried: the old note claimed this
token "must never be able to write code anywhere", which is no longer true. What
it can now write is two 4 KB repositories holding a README, a `.gitignore` and
issue templates -- no code, no data. The DevIndex corpus lives in `neomjs/neo`,
which this App has no installation on, so the #15744 property that matters --
the intake identity cannot publish here -- is intact.

Operationally this line and the App grant are ONE unit: `create-github-app-token`
fails the step outright when asked for a permission the installation lacks, so a
future permission cleanup that drops `contents` must land in the same commit that
removes this line, or the pipeline dies before emitting a corpus.

* docs(build): correct a falsified claim about create-github-app-token (#17150)

An earlier revision of this comment asserted that `create-github-app-token`
"fails the step outright when asked for a permission the installation lacks",
and used that to argue revoking `contents` would take the pipeline down before it
could emit a corpus.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- a permission `neomjs-devindex-intake` does not hold -- and the mint
step SUCCEEDED. The action does not validate requested permissions against the
installation.

Two consequences, both worse to leave standing than to correct:

- The operational warning was overstated. Revoking `contents` degrades the
  stargazer read (which #17148's deferral now absorbs), it does not kill the mint
  and freeze the corpus. The coupling between this line and the App grant is
  real; the failure mode is not.

- A successful mint proves NOTHING about what the token carries. #17150's probe
  matrix leaned on mint-success as conclusive evidence that `administration:
  read` was genuinely held. That inference is void. The matrix's CONCLUSIONS
  survive -- they rest on the capability probe (stargazers reachable / denied),
  which is a direct observation -- but the corroborating argument does not, and
  the ticket is corrected to say so.

* docs(build): restore the true coupling claim -- my correction was the false one (#17150)

Reverting f5ce78fe4a's correction. It asserted `create-github-app-token` does not
validate requested permissions, citing run 31877595849 as a success. The raw log
of that run says otherwise:

  ##[error]The permissions requested are not granted to this installation.

The step FAILED. I read `conclusion` from the jobs API, which `continue-on-error`
rewrites to `success` on a failed step -- and #17162's guard was present on that
probe branch. So the guard rewrote the field I used to judge whether the guard
was needed.

Consequence for the operator warning, restored to the stronger form: revoking
`contents` on the App while this line remains does not merely cost the stargazer
read, it fails the mint outright."
- 2026-08-15T12:54:16Z @tobiu closed this issue
- 2026-08-15T12:54:16Z @tobiu referenced in commit `6772ea0` - "fix(build): an intake credential fault no longer freezes corpus publication (#17162) (#17165)

* fix(build): an intake credential fault no longer freezes corpus publication (#17162)

The intake mint ran before checkout, Node and emission with nothing tolerating
its failure, so any intake credential fault killed the job before a single corpus
byte existed. That is #17148's blast radius one layer up: the corpus is emitted
under the READER identity and never consumes the intake token, yet an ENRICHMENT
credential decided whether the whole repository published.

`create-github-app-token` fails outright when asked for a permission the
installation lacks, so the realistic triggers are ordinary administration -- a
revoked permission, a rotated key, an installation removed from one repo -- and
the `contents` grant #17150 requires makes exactly that drift possible.

`continue-on-error: true` needs no new error path behind it. A failed mint leaves
`outputs.token` empty, the empty `DATA_SYNC_INTAKE_TOKEN` reaches
`assertDataSyncAccess` as "No intake token was provided", and `emitGeneratedData`
already defers a preflight failure: every stage runs, the corpus publishes, the
deferred error is rethrown and the run exits non-zero. The failure moves later
rather than disappearing -- a silently green run over a dead intake would be
strictly worse than the freeze it replaces.

The Publisher mint stays fatal. It is the publish credential; without it there is
nothing to salvage.

Adds the unit test pinning the runtime half, because the workflow change is
otherwise trusting an unasserted path: if the absent-token case ever threw
instead of deferring, a credential fault would resume killing publication while
the workflow guard still looked correct.

* docs(build): correct the falsified mint-failure trigger (#17162)

An earlier revision of this comment claimed `create-github-app-token` fails
outright when asked for a permission the installation lacks, and used that to
argue a revoked App permission is a realistic trigger for the mint failure this
guard exists to absorb.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- not held by `neomjs-devindex-intake` -- and the mint SUCCEEDED. The
action does not validate requested permissions against the installation.

The ticket's premise survives on the triggers that do fail the mint for real: a
rotated or expired private key, or an installation removed from a named
repository. Those still kill the job before a corpus byte exists, which is the
defect. What changes is that a revoked PERMISSION degrades the capability -- and
#17148's deferral already absorbs that -- rather than freezing publication.

Also worth stating in-file because the inverse belief is actively misleading: if
you think the mint validates, a successful mint looks like proof the token
carries what was requested. It is not, and only a capability probe answers that.

* docs(build): my correction was the false one -- both mint triggers are real (#17162)

Reverting my own correction. @neo-gpt falsified it and the raw logs confirm him,
going further than he did: BOTH probes failed the mint, not one.

  run 31877595849  ##[error]The permissions requested are not granted to this installation.
  run 31879164402  ##[error]Not Found - .../get-a-repository-installation-for-the-authenticated-app

I read `conclusion` from `gh run view --json jobs` and saw `success` on both, and
concluded the mint cannot fail. But `continue-on-error: true` sets
`conclusion: success` on a FAILED step while `outcome` retains the failure --
so the field I read is the one THIS PR's own guard rewrites.

The guard masked the failures that justify it, and I read that as evidence the
guard was unnecessary. A mechanism that converts failure into success makes its
own necessity invisible to any status-based check.

So the original premise stands and this PR is better evidenced than when it was
opened: run 31879164402 is valid L4 proof of the continuation -- mint failed,
job proceeded through checkout and Node to the preflight, which rejected the
empty token and left the corpus path intact.

Restores the trigger list, records both measured failure modes with their runs,
and adds the instruction that would have prevented the error: read the LOG, not
the step status, because once this guard exists a failed mint renders green."

