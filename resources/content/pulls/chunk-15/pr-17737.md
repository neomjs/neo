---
number: 17737
title: 'fix(ai): enforce Playwright suite path containment (#16508)'
author: neo-gpt
state: OPEN
createdAt: '2026-08-24T21:42:37Z'
updatedAt: '2026-08-24T21:42:37Z'
closedAt: null
mergedAt: null
head: codex/16508-playwright-path-containment
base: dev
url: 'https://github.com/neomjs/neo/pull/17737'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16508

`runPlaywrightTest()` now compares the canonical requested path against the canonical `test/playwright` root with `path.relative()`, so substring-shaped siblings cannot impersonate the suite. The outer `ensureSandboxed()` jail remains unchanged, and the existing MCP unit spec now covers the bypass, the in-suite positive control, traversal, and symlink resolution.

Evidence: L2 (behavioral unit coverage exercises canonical filesystem paths and the real runner boundary) → L2 required (all five close-target ACs are unit-observable). No residuals.

## AC Evidence

| Acceptance criterion | Evidence |
|---|---|
| AC-1 | CI-covered: `FileSystemService.runPlaywrightTest()` uses `path.relative()` over canonical candidate and suite-root paths. |
| AC-2 | CI-covered: `#16508 a substring-shaped path outside the Playwright suite is refused`; pre-fix red resolved to `Test Failed: No tests found` instead of rejecting. |
| AC-3 | CI-covered: `#16508 a legitimate spec inside the Playwright suite still crosses the guard`. |
| AC-4 | CI-covered: dedicated traversal and in-suite-symlink-to-outside-suite refusal arms. |
| AC-5 | CI-covered: `ensureSandboxed()` is unchanged and all pre-existing canonical jail regression arms remain in the same focused spec. |

## Deltas from ticket

None substantive. Executor process isolation remains the separate concern tracked by `#16979`; this PR changes only the narrower suite-directory guarantee.

## Test Evidence

- Pre-fix red proof: the focused file failed exactly 1 of 18 tests because `/atest/playwright/` passed the substring guard and reached the runner.
- All coverage runs in CI.

## Post-Merge Validation

None; no post-merge-only observable remains.

Authored by Euclid (GPT-5.6 Sol, Codex). Session e3e2d32f-430b-4861-af1f-b6a214fa0513.
