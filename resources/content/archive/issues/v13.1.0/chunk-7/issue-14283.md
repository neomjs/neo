---
id: 14283
title: Extract the embedding-dimension helpers out of GoldenPathSynthesizer
state: CLOSED
labels:
  - ai
  - refactoring
assignees:
  - neo-opus-grace
createdAt: '2026-06-28T00:07:29Z'
updatedAt: '2026-06-28T10:35:59Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14283'
author: neo-opus-grace
commentsCount: 0
parentIssue: 14281
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-06-28T10:35:59Z'
---
# Extract the embedding-dimension helpers out of GoldenPathSynthesizer

Refs #14281

The first (lowest-blast) extraction of the GoldenPathSynthesizer SRP-decomposition — the 3 clearly-misplaced embedding-dimension helpers.

## Context

`ai/services/graph/GoldenPathSynthesizer.mjs` (the 1553-line static grab-bag, #14281) declares 3 top-level `export function`s that are embedding-dimension utilities, not Golden-Path synthesis: `getEmbeddingVectorLength`, `getEmbeddingModelName`, `buildEmbeddingDimensionMismatchMessage`. V-B-A'd: they have NO external by-name consumers (consumers import the default class); they are used only internally at GPS L1018-1028 (the frontier-embedding dimension-guard); and they have no direct test coverage.

## The Fix

- Move the 3 functions to a focused module `ai/services/graph/embeddingDimension.mjs` (pure, no I/O).
- GPS imports them back (for the L1018-1028 internal dimension-guard) + re-exports them (API stability — they were public exports; behavior-preserving).
- Add focused unit tests (currently untested).

## Acceptance Criteria

- [ ] The 3 helpers live in `ai/services/graph/embeddingDimension.mjs`; GPS imports + re-exports them (public API stable).
- [ ] GPS's internal dimension-guard (the L1018-1028 frontier check) still works — behavior-preserving.
- [ ] Focused unit tests for the 3 helpers: `getEmbeddingVectorLength` (valid/invalid payloads), `getEmbeddingModelName` (each provider + default), `buildEmbeddingDimensionMismatchMessage` (message shape) — green.
- [ ] `node --check` clean + the related specs green.

## Out of Scope

- The other 8 GoldenPathSynthesizer responsibilities (the rest of #14281's leaves).
- Any behavior change (this is a pure move + re-export).

Origin Session ID: 090a68e6-1a28-4b20-a5fd-842ebac3e729. Authored by Grace (@neo-opus-grace, Claude Opus 4.8, Claude Code).


## Timeline

- 2026-06-28T00:07:30Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-06-28T00:07:31Z @neo-opus-grace added the `ai` label
- 2026-06-28T00:07:31Z @neo-opus-grace added the `refactoring` label
- 2026-06-28T00:08:22Z @neo-opus-grace added parent issue #14281
- 2026-06-28T00:17:50Z @neo-opus-grace cross-referenced by PR #14285
- 2026-06-28T00:19:30Z @neo-opus-grace cross-referenced by #14281
- 2026-06-28T01:06:21Z @neo-opus-grace referenced in commit `965e77c` - "fix(ai): widen the QueryService exact-anchor test limit so the new embedding helper does not evict a boundary anchor (#14283)

Same brittleness as #14288: the #14283 extraction added ai/services/graph/embeddingDimension.mjs. QueryService.queryDocuments.spec.mjs:352 path-matches the whole ai/services/graph dir (lexical rescue), score-sorts, caps at limit; the new file pushed memory-core GraphService.mjs past the limit:15 cutoff. Raised the test limit to 25 (production default + headroom); the semantic top-k is stubbed, so the limit only sizes the final rescued set, not the 'miss' premise. The extraction itself is unchanged.

Verified: 13/13 QueryService.queryDocuments green."
- 2026-06-28T10:22:05Z @neo-opus-grace referenced in commit `644d354` - "refactor(ai): extract the embedding-dimension helpers out of GoldenPathSynthesizer (#14283)

First (lowest-blast) leaf of the GoldenPathSynthesizer SRP-decomposition (#14281): the 3 embedding-dimension helpers (getEmbeddingVectorLength / getEmbeddingModelName / buildEmbeddingDimensionMismatchMessage) were misplaced embedding utils in a Golden-Path file. Moved to a focused module ai/services/graph/embeddingDimension.mjs; GoldenPathSynthesizer imports them back (the internal frontier dimension-guard) and re-exports them (public API stable). Behavior-preserving — the operator-facing message stays byte-identical.

Added focused unit tests (the helpers were previously untested). Evidence: 39/39 green (the 3 new helper tests + the 36 existing GoldenPathSynthesizer tests, confirming the re-export didn't break the class); node --check clean on all three files."
- 2026-06-28T10:35:59Z @tobiu referenced in commit `c2eed12` - "refactor(ai): extract the embedding-dimension helpers out of GoldenPathSynthesizer (#14283) (#14285)

First (lowest-blast) leaf of the GoldenPathSynthesizer SRP-decomposition (#14281): the 3 embedding-dimension helpers (getEmbeddingVectorLength / getEmbeddingModelName / buildEmbeddingDimensionMismatchMessage) were misplaced embedding utils in a Golden-Path file. Moved to a focused module ai/services/graph/embeddingDimension.mjs; GoldenPathSynthesizer imports them back (the internal frontier dimension-guard) and re-exports them (public API stable). Behavior-preserving — the operator-facing message stays byte-identical.

Added focused unit tests (the helpers were previously untested). Evidence: 39/39 green (the 3 new helper tests + the 36 existing GoldenPathSynthesizer tests, confirming the re-export didn't break the class); node --check clean on all three files."
- 2026-06-28T10:35:59Z @tobiu closed this issue
- 2026-06-28T12:02:20Z @neo-opus-grace cross-referenced by PR #14289
- 2026-06-28T12:31:50Z @tobiu referenced in commit `f66fb7e` - "refactor(ai): SRP-decompose GoldenPathSynthesizer — extract the remaining 4 clusters into focused modules (#14281) (#14289)

Finish the GoldenPathSynthesizer SRP decomposition: extract the remaining inline-logic
clusters into focused sibling modules, behavior-preserving via stable static delegating
shims (the public API is unchanged).

New modules (ai/services/graph/):
- agentFamilyResolution.mjs     — identity/family resolution from the roster + PR self-id + cross-family review
- computedGoldenPathRouting.mjs — the Computed Golden Path routing-decision surface + routed/empty/failure render sections
- activePrCycleSection.mjs      — Active PR Cycle State handoff rendering
- frontierConsolidation.mjs     — frontier GUIDE-edge pruning + recency-ordered summary reads + Consolidation Gaps

The issue-focus clusters were already extracted to issueFocusSections.mjs; the embedding
helpers + capture-timestamp formatter landed via #14283 / #14287.

GoldenPathSynthesizer.mjs: 1504 -> 1106 lines — now the synthesizeGoldenPath orchestration
core plus the stable-API shim layer. Pure moves + import rewiring; the routing-vs-visibility
section boundary is preserved (no behavior change).

Verified locally: GoldenPathSynthesizer.spec 36/36; QueryService.queryDocuments + MemoryService.Frontier 14/14 (graph-dir now 17 files, still within the limit:25 exact-anchor headroom)."

