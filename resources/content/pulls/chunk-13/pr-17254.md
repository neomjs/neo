---
number: 17254
title: >-
  feat(agentos): store-bound viewer wake rendering + honest-degraded telltale
  (#17130)
author: neo-fable-clio
state: OPEN
createdAt: '2026-08-16T20:21:49Z'
updatedAt: '2026-08-16T20:40:08Z'
closedAt: null
mergedAt: null
head: feature/17130-cockpit-wake-rendering
base: dev
url: 'https://github.com/neomjs/neo/pull/17254'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17130

Leg 2 of the browser-direct cockpit wake consumer: the cockpit now RENDERS its own push lane. `WakeSignal` + the bounded newest-first `ViewerWakeFeed` land per-viewer wakes as provider-owned store records; `viewerWake.stream` / `viewerWake.catchUp` carry the consumer's own observations verbatim (liveness vocabulary untouched, `fresh ≠ empty ≠ failed` by construction, `null` as honest absence); a quiet chrome telltale renders live/degraded with the consumer's reason — and `ensureViewerWakeStream` keeps the stream bound to the CURRENT bridge (custody heal: a replaced bridge object retires the consumer built from the old closure). The leg-1 consumer gains exactly two observational surfaces (`onWake`, `lastCatchUp`), and the bridge capability whitelist admits `onWake` — destination, credentials, and transport stay pinned.

Evidence: L2 (unit 52/52 + whitebox-e2e on the mounted cockpit against the PRODUCTION fan-out: armed handshake → live telltale → pushed digest in the page → transport loss renders the consumer's reason → self-reconnect restores live) → L2 required (remote journey, two-credential arming wire-level, app-gate store path, watermark client-held). Residual: the cold-drain `poll-digest` clause of AC 1 — the browser page holds no plane credential by design and the composed server exposes no viewer poll surface yet, Residual-Owner: #17252

## Deltas from ticket

- **Envelope field names verbatim, not the contract sketch's:** the leg-2 contract sketched `{subscriptionId, envelopeDigest, kind, sentAt, receivedAt}`; the landed model reads the signed envelope's OWN grammar (`eventId`, `eventType` as `kind`, `logId`, `emittedAt`) plus the client `receivedAt` stamp — "nothing re-derived" argued for verbatim names once the real grammar was in view.
- **Degraded speaks through the telltale, not a SpineBanner extension:** the contract's "degraded speaks through the existing SpineBanner" collided with the banner module's own law ("a verdict may only speak for the surface that produced it"; per-agent truth is explicitly a different surface — per-VIEWER truth is too). A dead push lane is comfort loss, not spine degradation (push is latency, poll is truth), so the telltale owns the full tri-state with the consumer's reason, reusing the banner's token-indirection skin idiom, and no new alarm surface exists. The e2e pins the independence: spine degraded while MY push lane is live.
- **The pollDigest seam is honestly injection-only in the browser:** build-time discovery — `poll-digest` is an authenticated MC-plane call, the mints live in bridge closures, and the composed server exposes no viewer poll surface; so the cockpit's `wakePollDigest` seam passes through when a composition supplies one and renders honest absence otherwise. The server-brokered surface (which lights the seam up with zero client change) is #17252.
- **Consumer + capability extension:** leg-1's consumer gains `onWake` (observer, absorbed-fault, validated) and `lastCatchUp` (timestamped tri-state); `openWakeStream`'s observational whitelist adds exactly `onWake`. `formatAge` is exported from `agentFreshness` (one relative-age formatter, two chrome consumers — no duplicate).

## Test Evidence

- `npm run test-unit -- test/playwright/unit/apps/agentos/fleet/fleetWakeStreamConsumer.spec.mjs test/playwright/unit/apps/agentos/fleet/installFleetBridge.spec.mjs test/playwright/unit/apps/agentos/store/viewerWakeFeed.spec.mjs test/playwright/unit/apps/agentos/view/fleet/viewerWakeTelltale.spec.mjs test/playwright/unit/apps/agentos/view/fleet/fleetCockpitViewerWake.spec.mjs` → **52 passed (5.2s)** — consumer onWake/lastCatchUp, capability whitelist projection, feed bound/newest-first/re-receipt, telltale derivation (verbatim reasons, catch-up tri-state, five-signal title bound), cockpit wiring (not-wired honesty, single open, custody-heal rebuild, capability-vanish teardown, seam pass-through).
- `NEO_E2E_PORT=8121 npx playwright test agentos/FleetCockpitViewerWakeNL -c test/playwright/playwright.config.e2e.mjs --workers=1` → **1 passed (7.8s)**, zero page errors — the full journey against `createFleetWakeFanout` (real arming path, real handshake, real digest push), including the two-header wire assertion (`class1` admitted + distinct `class3` observed server-side) and consumer self-reconnect after transport death. e2e is non-CI by design; this is the exact-head local receipt.
- `npm run check-theme-surfaces` → green (incl. shell-seam check 5: the telltale skin is chrome, not a drawer-pane root).
- `npx sass resources/scss/src/apps/agentos/fleet/ViewerWakeTelltale.scss` → compiles clean; mounted via `additionalThemeFiles` beside the SpineBanner registration.
- `npm run agent-preflight -- --change-class capability ...` → all gates passed (ticket-archaeology clean after comment hygiene, block-alignment auto-applied).
- Surfaces: `apps/agentos` cockpit: unit + NL e2e above | `ai/services/fleet` (read-only consumption): fan-out exercised by the e2e fixture, no source change.

## Post-Merge Validation

- [ ] devCockpit against a live composed fleet server with real per-viewer arming: telltale reaches `wake: live` and renders real digests — the plane-backed topology no fixture can fully mirror; proven alongside the cold-drain journey.
- [ ] The brokered viewer poll surface lands → the cold-drain clause of AC 1 closes end-to-end with zero client change (the `wakePollDigest` seam + capability default).

Residual-Owner: #17252

Authored by Clio (Claude Fable 5, Claude Code). Session 71baabc5-3ebe-46ff-99ce-a301e78cb7c5.
