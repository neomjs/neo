---
id: 17150
title: Star-based DevIndex opt-in is dead by GitHub policy and blocks the issue path
state: OPEN
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-08-15T07:53:31Z'
updatedAt: '2026-08-15T11:38:07Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17150'
author: neo-opus-ada
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Star-based DevIndex opt-in is dead by GitHub policy and blocks the issue path

## Context

GitHub restricted the stargazers endpoint to repository **admins and collaborators** ([changelog, 2026-06-30](https://github.blog/changelog/2026-06-30-upcoming-access-restrictions-to-public-api-endpoints-and-ui-views/), titled "Upcoming", no published enforcement date). The rollout reached our `intake` App between `12:05:23Z` and `13:19:19Z` on 2026-08-14 — the last-green → first-red boundary of `#17131`.

Verified live, and it tracks collaborator status rather than repository visibility:

| probe | `neomjs/neo` (collaborator) | `devindex-opt-in` (`pull` only) | `devindex-opt-out` (`pull` only) |
|---|---|---|---|
| REST `/stargazers` | 200, 3 rows | **404** | **404** |
| GraphQL `stargazers` | returns edges | `edges: []` | — |
| REST repo root | 200 | 200 (`stargazers_count: 16`) | 200 |

Under the `intake` App token the GraphQL connection answers `Resource not accessible by integration`. This is permanent and by design — not an outage that clears.

`#17148` (PR #17149) fixes the *blast radius*: an intake denial no longer freezes corpus publication. It deliberately does **not** touch DevIndex behaviour, which is this ticket.

## The Problem

`OptIn.run()` has two independent intake mechanisms (`apps/devindex/services/OptIn.mjs`):

1. **Stargazers** — `// 1. Check Stargazers`, the paged `stargazers` GraphQL loop at `:49-113`. **Dead by policy.**
2. **Issues** — `// 2. Check Issues` → `processIssues()` at `:114`, the issue-template opt-in. **Still works.**

The star loop runs first and throws, so `processIssues()` is never reached. A permanently-dead half is taking the working half down with it: DevIndex opt-in is currently 100% unavailable when it could be ~50% available. The last green run shows both paths executing (`[OptIn] Checking for new opt-in requests...` then `[OptIn] Checking for new opt-in issue requests...`), so this is a regression in reachability, not a missing feature.

Note the asymmetry that hid this: `OptIn.mjs:78` already skips gracefully on `NOT_FOUND` / `Could not resolve`. A non-collaborator REST call returns 404 (which that branch would absorb), but the GraphQL connection returns `Resource not accessible by integration` — which is not in the branch, so it throws.

## The Architectural Reality

- `apps/devindex/services/OptIn.mjs:49-113` — the stargazer loop; `:76` the throwing call; `:77-83` the existing NOT_FOUND skip.
- `apps/devindex/services/OptIn.mjs:114` — `processIssues()`, unreachable behind it.
- `apps/devindex/services/GitHub.mjs:658` — where the GraphQL error is raised.
- `buildScripts/dataSyncPreflight.mjs:33-36` — `REQUIRED_REPOSITORIES` still declares `connection: 'stargazers'` for `devindex-opt-in` (added by `#17148`). If the star path is retired, that entry's connection must change with it or the preflight will deny forever on a read nothing performs.

## The Fix

**DECIDED 2026-08-15 by @tobiu: option B — restore access. Implemented in PR #17160 via `contents: write`, which is the permission GitHub infers collaborator status from.**

Four `preflight_only` probes, one variable each, none mutating anything:

| requested intake permissions | `devindex-opt-in.stargazers` | run |
|---|---|---|
| `issues:write`, `metadata:read` (as shipped) | DENIED | 31870344604 |
| + `administration:read` | DENIED | [31874111382](https://github.com/neomjs/neo/actions/runs/31874111382) |
| + `contents:` **write** | **reachable** ✅ | [31875177082](https://github.com/neomjs/neo/actions/runs/31875177082) |
| + `contents:` **read** | DENIED | [31875769230](https://github.com/neomjs/neo/actions/runs/31875769230) |

Read does not suffice and there is no lesser lever. `devindex-opt-out.issues` stayed reachable in all four, which rules out a sick token and isolates the finding to the connection.

**All four mint steps ran clean** — zero errors in their logs — and all four predate `#17162`'s `continue-on-error` guard, so their green is genuine rather than guard-rewritten and the grants were live at probe time. Verified by direct log inspection, not inferred from the step status, because that inference failed elsewhere on this ticket the same day.

**One wrong turn, recorded because the mechanism generalises.** After `administration: read` failed I concluded no App permission could satisfy an account-status restriction. That was an over-generalisation from a single negative, and @tobiu challenged it; `contents: write` then worked. GitHub's own docs list App installation tokens as supported for this endpoint, which should have stopped the claim before it was made.

**These results are only trustworthy because `#17148` merged first.** The old preflight probed `repository{id}`, which resolves from metadata and would have printed `reachable` in all four rows. The instrument had to be fixed before the experiment could mean anything.

**Option B is the work and it is delivered in PR #17160.** Option A below is retained as the fallback that turned out not to be needed.

**B (CHOSEN). Restore access.** Grant the `intake` App collaborator/admin-equivalent access on `neomjs/devindex-opt-in` and `neomjs/devindex-opt-out`.

> **The grant alone will NOT reach the pipeline — there are TWO places, and both must agree.**
> `.github/workflows/data-sync-pipeline.yml:96-97` mints the intake token with an EXPLICIT permission list:
> ```yaml
> permission-issues: write
> permission-metadata: read
> ```
> `actions/create-github-app-token@v3` mints exactly what is requested — the workflow's own comment says so: *"without this, v3 mints a token carrying every permission the installation holds"*. That cap is deliberate (it keeps least-privilege reviewable in the diff), but it means **widening the App installation while leaving these lines unchanged produces a token that still lacks the new permission**, and the failure is bit-identical to no grant at all. Whatever permission unlocks the stargazer read must be added here in the same change.

**A (fallback). Retire the star path.** Delete the stargazer loop, keep issue-based opt-in as the sole mechanism, set `REQUIRED_REPOSITORIES`' connection for `devindex-opt-in` to `issues`, and update any doc still saying "star the repo to opt in".

**Verification.** Note that the *current* preflight cannot confirm either outcome: it probes `repository{id}`, which resolves from metadata and passes whether or not stargazers is readable. `#17148` / PR #17149 is what makes the grant verifiable — it selects the connection the entry names and requires it to resolve. With that merged, `DATA_SYNC_PREFLIGHT_ONLY=true` verifies the credential topology with **no side effects** (it skips the collection stages, so OptOut comments on and closes nothing), which is the cheap iteration loop for getting the grant right.

Either way the star loop must stop being fatal to `processIssues()`.

## Acceptance Criteria

- [ ] A denied stargazer read no longer prevents `processIssues()` from running; issue-based opt-in works while the star path is unavailable.
- [ ] The unavailability is surfaced (logged//counted), not silently swallowed — a carve-out that quiets the guard would let the whole intake rot undetected.
- [ ] `REQUIRED_REPOSITORIES`' declared `connection` for `devindex-opt-in` matches whatever read the code actually performs after the decision.
- [ ] If the star path is retired, no user-facing doc still instructs "star to opt in".
- [ ] Unit coverage: stargazer denial ⇒ issue path still runs and its opt-ins are recorded.

## Out of Scope

- The pipeline blast-radius fix — `#17148` / PR #17149.
- The GitHub-side restriction itself; it is not appealable by us.

## Avoided Traps

- **Adding the denial string to the `NOT_FOUND` skip branch and calling it done.** One line, turns everything green, and converts a permanent capability loss into a silent no-op — the opt-in feature would rot with nobody noticing. The loss must be visible.
- **Assuming the whole opt-in feature is dead.** Only the star half is; `processIssues()` is intact and was working in the last green run.

## Related

- `#17148` / PR #17149 — the pipeline-side blast-radius fix that keeps the corpus publishing.
- `#17131` — the standing alarm, which stays red until this or a credential change resolves the intake.
- `#15744` — the two-identity least-privilege split that option B would reverse.

Retrieval Hint: "GitHub stargazers restricted to collaborators; DevIndex star opt-in dead, issue path still works"









## Timeline

- 2026-08-15T07:53:32Z @neo-opus-ada added the `bug` label
- 2026-08-15T07:53:32Z @neo-opus-ada added the `ai` label
- 2026-08-15T07:53:32Z @neo-opus-ada added the `architecture` label
- 2026-08-15T08:09:11Z @neo-gpt cross-referenced by PR #17149
- 2026-08-15T08:16:49Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-15T08:22:47Z @neo-opus-ada referenced in commit `54ab733` - "Revert "feat(build): request administration:read so the intake token can read stargazers (#17150)"

This reverts commit 5c8a1f0. Keeping the experiment in history because the
result is the finding: `administration: read` does NOT restore the stargazer
read, so shipping it would widen the intake identity for nothing.

Empirical, run 31874111382 (preflight_only dispatch, no side effects):

  permission-administration: read        <- accepted by the mint step
  [DataSync preflight] neomjs/devindex-opt-in attempt 1/3 failed
    (Resource not accessible by integration); retrying
  [DataSync preflight] neomjs/devindex-opt-in DENIED (OptIn stargazer read)
  [DataSync preflight] neomjs/devindex-opt-out reachable (OptOut issue read + close)

The mint SUCCEEDING is what makes this conclusive: `create-github-app-token`
fails outright when asked for a permission the installation does not hold, so a
successful mint proves the App genuinely held `administration: read` at probe
time. Held, requested, minted -- and stargazers still denied, while the `issues`
connection on the sibling repository stayed reachable in the same run.

So the restriction is on ACCOUNT STATUS, exactly as GitHub's wording says, and no
App permission satisfies it. #17150 option B (restore access) is falsified;
option A (retire the star path) is the remaining shape. @tobiu should revoke the
`administration` and `organization administration` grants on neomjs-devindex-intake
-- they are now known to buy nothing and only widen the identity."
- 2026-08-15T08:32:32Z @neo-fable-clio cross-referenced by #17153
- 2026-08-15T08:43:16Z @neo-opus-ada referenced in commit `1593f89` - "test(build): probe whether contents:write is the stargazer collaborator signal (#17150)

EXPERIMENT, not a merge candidate. `administration: read` was tested and did not
restore the stargazer read (run 31874111382). Reporting points at `contents:
write` as the permission GitHub reads as collaborator status, but no source
states it citably, so this asks GitHub directly via a preflight_only dispatch.

@tobiu granted code write on neomjs-devindex-intake for this test and asked to
keep the wider grants a day rather than revoke between probes."
- 2026-08-15T08:57:37Z @neo-opus-ada referenced in commit `cb64be3` - "test(build): does contents:READ suffice for the stargazer read, or is write required? (#17150)

`contents: write` restored the stargazer read (run 31875177082). Write is a
much larger grant than this pipeline wants, so before proposing it, test whether
READ suffices -- the App holds write, and create-github-app-token mints only what
is requested, so requesting read yields a read-only token on a write-capable
installation. If read works, the trade @tobiu faces shrinks from code-write to
code-read on the two intake repos."
### @neo-opus-ada - 2026-08-15T09:04:39Z

## Option B is VIABLE after all — `contents: write` is the collaborator signal

My earlier "no App permission satisfies it" was an over-generalisation from a single negative test, and @tobiu was right to push on it. GitHub's docs list **App installation access tokens** as supported for this endpoint, which should have stopped that claim. Four controlled probes, same App, same installation, one variable each — all `preflight_only`, all side-effect-free:

| # | requested intake permissions | `devindex-opt-in.stargazers` | run |
|---|---|---|---|
| 1 | `issues:write`, `metadata:read` (shipped) | **DENIED** | 31870344604 |
| 2 | + `administration:read` | **DENIED** | 31874111382 |
| 3 | + `contents:**write**` | **reachable** ✅ | 31875177082 |
| 4 | + `contents:**read**` | **DENIED** | 31875769230 |

`devindex-opt-out.issues` stayed reachable in every run, so the token was healthy throughout — the stargazer connection is the only thing moving.

**Probes 3 and 4 together are the finding**: write is required, read does not suffice. That matches the reporting claim that contents-write is how GitHub infers collaborator status, and now we have it first-party rather than second-hand.

**These results are only trustworthy because #17148 merged first.** The old preflight probed `repository{id}`, which resolves from metadata and would have printed `reachable` in all four rows. The instrument had to be fixed before the experiment could mean anything.

## What `contents: write` actually exposes

Measured, not assumed:

| repo | contents | size |
|---|---|---|
| `neomjs/devindex-opt-in` | `.github/`, `.gitignore` (184 B), `README.md` (1631 B) | 3 KB |
| `neomjs/devindex-opt-out` | `.github/`, `.gitignore` (184 B), `README.md` (1030 B) | 1 KB |

No code, no data. The DevIndex corpus lives in `neomjs/neo` under `apps/devindex/resources/data/`, which the intake App **cannot reach at all** — it has no installation there. So the grant's blast radius is a README, a `.gitignore` and issue templates across two 4 KB repos, recoverable from git history.

The property `#15744` exists to protect — the intake identity cannot publish to the main repository — is untouched by this. What does change is the workflow's claim that this token *"must never be able to write code anywhere"*: that sentence becomes false and must be rewritten rather than silently contradicted.

## Recommendation

**Take option B.** Grant `contents: write` (already granted), add `permission-administration: read`-free `permission-contents: write` to the mint step, and rewrite the intake-token comment to state precisely what the grant is for and what it costs. It preserves one-click star opt-in — materially lower friction than "file an issue from a template" for a community-facing index — at a cost of write access to 4 KB of non-code.

Scope of this ticket accordingly shifts from *retire the star path* to *restore access + document the trade*. The AC about `REQUIRED_REPOSITORIES`' connection now resolves as "stays `stargazers`", and the AC about `processIssues()` being unreachable behind a throwing star loop **still stands** — that is a real robustness defect regardless of whether access is restored, and it is what made a single denial take down both intake mechanisms.

Awaiting @tobiu's call on the trade before implementing; the permission line is not shipped and the experiment branch is not a merge candidate.

- 2026-08-15T09:22:53Z @neo-opus-ada referenced in commit `74a9c74` - "fix(devindex): a denied stargazer read no longer takes the issue path down with it (#17150)

Opt-in has two INDEPENDENT intake mechanisms reading the same repository over
different connections: `stargazers` and `issues`. GitHub restricted access at
connection granularity -- stargazer reads limited to repository admins and
collaborators (announced 2026-06-30) -- so exactly one of the two is currently
unavailable.

The stargazer loop ran first and threw straight out of `run()`, so
`processIssues()` was never reached and opt-in intake went to ZERO rather than
halving. The repository-not-found branch had the same defect for the same reason:
it `return`ed out of `run()`, and `processIssues` already handles NOT_FOUND for
itself by returning null, so pre-empting it bought nothing.

The stargazer phase moves into `collectStargazerOptIns`, which returns its own
result instead of mutating `run()` state -- that is what makes the isolation
expressible, since the caller can now lose the phase to a failure without
holding half-updated state. `run()` wraps it, continues to the issue path, and
rethrows LAST, after the issue work is persisted.

Deferred, never swallowed. A catch that quietly returned would leave a dead
intake mechanism reporting success indefinitely, which is the failure mode that
let this go unnoticed until the corpus froze.

Coverage: every test verified RED against the pre-fix source INDIVIDUALLY --
`describe.serial` skips after the first failure, so one suite run proved only the
first test and would have hidden three vacuous ones.

Also strips pre-existing trailing whitespace on 12 lines and applies
block-alignment; the commit hooks check whole staged files, and moving the
stargazer loop brought both into scope.

Does NOT decide whether stargazer access is restored; that is the open half of
#17150 and an operator call on a credential trade-off."
- 2026-08-15T09:26:37Z @neo-opus-ada referenced in commit `e42899a` - "feat(build): grant the intake token the stargazer read via contents:write (#17150)

GitHub limited the stargazers endpoint to repository admins and collaborators
(announced 2026-06-30) and infers that status from contents-write. `metadata:
read` used to carry the read and no longer does, so the DevIndex star-based
opt-in has been dead since the restriction reached this installation on 08-14.

Measured rather than argued -- four `preflight_only` probes, one variable each,
each mutating nothing:

  issues:write + metadata:read   -> DENIED    (run 31870344604)
  + administration:read          -> DENIED    (run 31874111382)
  + contents:WRITE               -> reachable (run 31875177082)
  + contents:READ                -> DENIED    (run 31875769230)

Read does not suffice; there is no lesser lever. `devindex-opt-out.issues` stayed
reachable across all four, which rules out a sick token and isolates the finding
to the connection.

The cost is stated in the workflow rather than buried: the old note claimed this
token "must never be able to write code anywhere", which is no longer true. What
it can now write is two 4 KB repositories holding a README, a `.gitignore` and
issue templates -- no code, no data. The DevIndex corpus lives in `neomjs/neo`,
which this App has no installation on, so the #15744 property that matters --
the intake identity cannot publish here -- is intact.

Operationally this line and the App grant are ONE unit: `create-github-app-token`
fails the step outright when asked for a permission the installation lacks, so a
future permission cleanup that drops `contents` must land in the same commit that
removes this line, or the pipeline dies before emitting a corpus."
- 2026-08-15T09:26:39Z @neo-opus-ada cross-referenced by PR #17160
- 2026-08-15T10:17:37Z @neo-opus-ada referenced in commit `f5ce78f` - "docs(build): correct a falsified claim about create-github-app-token (#17150)

An earlier revision of this comment asserted that `create-github-app-token`
"fails the step outright when asked for a permission the installation lacks",
and used that to argue revoking `contents` would take the pipeline down before it
could emit a corpus.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- a permission `neomjs-devindex-intake` does not hold -- and the mint
step SUCCEEDED. The action does not validate requested permissions against the
installation.

Two consequences, both worse to leave standing than to correct:

- The operational warning was overstated. Revoking `contents` degrades the
  stargazer read (which #17148's deferral now absorbs), it does not kill the mint
  and freeze the corpus. The coupling between this line and the App grant is
  real; the failure mode is not.

- A successful mint proves NOTHING about what the token carries. #17150's probe
  matrix leaned on mint-success as conclusive evidence that `administration:
  read` was genuinely held. That inference is void. The matrix's CONCLUSIONS
  survive -- they rest on the capability probe (stargazers reachable / denied),
  which is a direct observation -- but the corroborating argument does not, and
  the ticket is corrected to say so."
### @neo-opus-ada - 2026-08-15T10:19:04Z

## Correcting my own evidence argument — the probe CONCLUSIONS stand, the corroborating claim does not

I wrote, in the comment establishing the four-probe matrix:

> **The mint SUCCEEDING is what makes this conclusive.** `create-github-app-token` fails outright when asked for a permission the installation does not hold, so a successful mint proves the App genuinely held `administration: read` at probe time.

**That is false, and I found it by accident.** Run [31877595849](https://github.com/neomjs/neo/actions/runs/31877595849) requested `permission-deployments: write` — a permission `neomjs-devindex-intake` does **not** hold, per @tobiu's own listing — and the mint step **succeeded**. The action does not validate requested permissions against the installation.

Leaving it visible per retain-and-mark rather than editing it away, because a reader who saw the original needs to know the argument moved.

### What survives, and why

The matrix's **conclusions are unaffected**, because they rest on a direct capability observation rather than on the mint:

| requested | `devindex-opt-in.stargazers` | basis |
|---|---|---|
| `issues:write`, `metadata:read` | DENIED | preflight probe result |
| + `administration:read` | DENIED | preflight probe result |
| + `contents:` **write** | **reachable** | preflight probe result |
| + `contents:` **read** | DENIED | preflight probe result |

Every row is *"we asked GitHub for the stargazers connection and recorded what it answered"*. That is an observation, not an inference from the mint. And the decisive positive — `contents: write` ⇒ reachable — is **strengthened** rather than weakened by this correction: a capability appearing can only be explained by the token carrying it.

### What is lost

The alternative explanation I claimed to have ruled out for the two DENIED rows: *"the grant was not actually live at probe time."* I cannot exclude that from mint-success any more. It remains improbable — @tobiu confirmed each grant on the App page before I dispatched — but that is now operator report rather than machine proof, and the difference matters when someone re-reads this in three months.

### The generalisable half

**A mint success proves nothing about what a token carries.** Only a capability probe does. This is the same shape as the defect this ticket's sibling fixed one layer down — `#17148`'s preflight reported `reachable` off `repository{id}` while the stargazer read was denied, because it checked the envelope rather than the capability. I then reproduced the identical error *reasoning about my own instrument*: I trusted a step's exit status as evidence about a capability, one hour after fixing exactly that in code.

Corrected at source in both open PRs: [#17160](https://github.com/neomjs/neo/pull/17160) (`f5ce78fe4a`) and [#17165](https://github.com/neomjs/neo/pull/17165) (`4117b45963`).

### Operational consequence for @tobiu — my earlier warning was overstated

I told you *"`administration` and `organization administration` are safe to revoke; `contents` is not — dropping it takes the pipeline down before it emits a corpus."*

The **coupling is real** — revoking `contents` kills the stargazer read again, so it must move together with the workflow line. The **failure mode was wrong**: it degrades the capability, which `#17148`'s deferral now absorbs, rather than failing the mint and freezing the corpus. Less severe than I said, and the corpus keeps publishing either way.

---

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


---

> **↩ THIS COMMENT IS RETRACTED — 2026-08-15, and everything it concludes is false.** It rests on reading `conclusion` from the jobs API as proof the mint succeeded. It did not: the probe branches carried `#17162`'s `continue-on-error` guard, which sets `conclusion: success` on a **failed** step while `outcome` keeps the failure. Raw logs: `31877595849` → `##[error]The permissions requested are not granted to this installation` (HTTP 422); `31879164402` → `##[error]Not Found` (404). **Both probes failed the mint**, so `create-github-app-token` *does* validate permissions and repository installations, and the original claims this comment withdrew were correct. Falsified by @neo-gpt. Kept for provenance; the corrected account lives in the issue body. Do not cite anything below this line.

- 2026-08-15T11:08:56Z @neo-opus-ada referenced in commit `38ff6a0` - "docs(build): restore the true coupling claim -- my correction was the false one (#17150)

Reverting f5ce78fe4a's correction. It asserted `create-github-app-token` does not
validate requested permissions, citing run 31877595849 as a success. The raw log
of that run says otherwise:

  ##[error]The permissions requested are not granted to this installation.

The step FAILED. I read `conclusion` from the jobs API, which `continue-on-error`
rewrites to `success` on a failed step -- and #17162's guard was present on that
probe branch. So the guard rewrote the field I used to judge whether the guard
was needed.

Consequence for the operator warning, restored to the stronger form: revoking
`contents` on the App while this line remains does not merely cost the stargazer
read, it fails the mint outright."

