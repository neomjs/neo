---
id: 17024
title: Elect the smallest provider-lane envelope that meets the joint SLO
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
  - performance
  - agent-os
assignees:
  - neo-gpt
createdAt: '2026-08-12T12:17:34Z'
updatedAt: '2026-08-12T23:21:40Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17024'
author: neo-gpt-emmy
commentsCount: 6
parentIssue: 17018
subIssues:
  - '[x] 17034 Publish a verifiable provider-lane election runner'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 17021 Every model role resolves through its declared provider lane'
blocking:
  - '[x] 17022 Prove provider-lane composition on the canonical plane'
  - '[x] 17023 One durable election authority commits a coordinated vector-plane generation'
closedAt: '2026-08-12T23:21:40Z'
milestone: v13.2
---
# Elect the smallest provider-lane envelope that meets the joint SLO

Sub of epic #17018 (Row F, OQ4/OQ6/OQ7, and AC-D validation/election stage). This ticket is intentionally unassigned for peer self-selection and is blocked by #17021's canonical two-lane profile.

## Context

D#17015 r6 converged on an Ollama chat lane and OpenAI-compatible embedding lane under one fixed total CPU/memory envelope. The embedding slot count is not prescribed: candidates `{1,2,4}` must be elected empirically, and the smallest candidate meeting the joint service-level objective wins.

Live duplicate sweep (2026-08-12T12:16Z): open searches for `joint load resource election` and `embedding slot election` found only parent #17018 and its composition leaves. No implementation ticket owns this outcome.

## The Problem

A two-engine topology can preserve the original contention class if each service independently requests the whole machine. Conversely, choosing four embedding slots because it was the day-one wish can oversubscribe CPU or divide the model context below the required per-slot ceiling.

The relevant knobs are not comparable in isolation:

- llama.cpp-style `--ctx-size` is total across `-np` slots; slot truth must be observed at runtime.
- Ollama chat remains parallelism 1 and must retain bounded progress during embedding load.
- Neo's KB, MC, and orchestrator processes create the production concurrency, so a single synthetic caller is not representative.
- queue latency, provider wall time, CPU, memory, and refusal behavior answer different questions.

## The Architectural Reality

The election consumes #17021's stable service identities and explicit envelope inputs. It does not create another runtime policy layer. Candidate values are proposed, validated, and either elected or refused; they are never silently clamped or adapted after deployment.

Evidence is generated only on Neo's canonical/disposable plane. No external endpoint, corpus, token, or deployment mutation belongs in this ticket.

## The Fix

Build one reproducible election protocol:

1. hold the total CPU and memory envelope fixed,
2. evaluate embedding slot candidates `1`, `2`, and `4` with chat parallelism `1`,
3. allocate lane resources explicitly for every candidate,
4. verify observed per-slot context and refuse a candidate whose slot truth is below the embedding contract,
5. drive production-shaped concurrent embedding callers representing KB, MC, and orchestrator while overlapping a bounded chat workload,
6. record queue wait, provider duration, throughput, CPU, RSS, errors/refusals, and bounded-progress/starvation outcomes per lane,
7. repeat/interleave candidates sufficiently to expose warm-order and model-residency effects,
8. elect the smallest passing candidate and emit its values as immutable declarative deployment inputs.

The runner may reuse existing benchmark primitives, but any new module placement must pass structural pre-flight before implementation.

## Contract Ledger

| Producer | Consumer | Contract | Failure semantics |
|---|---|---|---|
| #17021 canonical profile | election runner | stable service identities, resource-envelope coordinates, immutable model/image digests | profile mismatch aborts the run |
| candidate matrix | both provider lanes | same total CPU/memory budget for every candidate | budget drift invalidates the candidate |
| runtime receipts | election rule | observed per-slot context plus joint-load metrics | knob-only evidence is invalid |
| election rule | deployment profile | smallest candidate satisfying every joint SLO becomes the immutable input | no passing candidate means no election, never fallback/clamp |

## Acceptance Criteria

- [ ] Candidates `{1,2,4}` run under one identical declared total CPU/memory envelope with chat parallelism fixed at `1`.
- [ ] Every candidate records its explicit per-lane resource allocation and immutable image/model digests.
- [ ] Runtime evidence verifies per-slot embedding context; one over-limit request per lane proves refusal rather than silent truncation.
- [ ] Workload composition includes concurrent KB-, MC-, and orchestrator-shaped embedding demand plus overlapping chat demand.
- [ ] Receipt separates Neo queue wait, provider duration, throughput, CPU, RSS, and errors/refusals; it never labels summed request wall time as CPU utilization.
- [ ] Both lanes must make bounded progress with no starvation under the declared joint SLO.
- [ ] Candidate order is interleaved/counterbalanced and repeated enough to expose warm-order/residency effects; sample count and uncertainty are reported.
- [ ] The smallest fully passing candidate is elected. If none pass, the output is `NO_ELECTION`.
- [ ] Elected values are emitted as immutable declarative inputs consumed by the canonical profile; there is no runtime adaptation or silent clamp.
- [ ] All execution is restricted to Neo's canonical/disposable plane; no external-plane probe or data access exists.

## Decision Record Impact

Implements Row F and the immutable-election rule in amended ADR-0014. No new ADR or AiConfig leaf is implied.

## Out of Scope

- Role routing and Compose/readiness implementation (#17021).
- Composition, containment, and restart-isolation proof (#17022).
- Vector-generation identity/promotion and corpus migration (separate AC-C/AC-E leaves).
- External deployment or acceptance.

## Avoided Traps

- Benchmarking one caller against a multi-process production topology.
- Comparing candidates under different total resources.
- Reading configured total context as per-slot context.
- Choosing four because it is the largest candidate.
- Calling request wall time CPU time.
- Hiding a failed matrix behind an automatic fallback.

## Related

Parent #17018 · blocked by #17021 · D#17015 r6 · siblings #17019, #17022

Origin Session ID: 35e0863b-4d45-4b94-90ff-5e66278bea7d

Retrieval hints: `D#17015 Row F slots 1 2 4 per-slot context joint SLO immutable election`

## Timeline

- 2026-08-12T12:17:35Z @neo-gpt-emmy added the `enhancement` label
- 2026-08-12T12:17:35Z @neo-gpt-emmy added the `ai` label
- 2026-08-12T12:17:35Z @neo-gpt-emmy added the `testing` label
- 2026-08-12T12:17:35Z @neo-gpt-emmy added the `performance` label
- 2026-08-12T12:17:36Z @neo-gpt-emmy added the `agent-os` label
- 2026-08-12T12:18:41Z @neo-gpt-emmy added parent issue #17018
- 2026-08-12T12:18:42Z @neo-gpt-emmy marked this issue as being blocked by #17021
- 2026-08-12T12:18:54Z @neo-gpt-emmy added this to the **v13.2** milestone
- 2026-08-12T12:20:31Z @neo-gpt-emmy cross-referenced by #17026
- 2026-08-12T12:20:51Z @neo-gpt-emmy marked this issue as blocking #17023
- 2026-08-12T12:22:09Z @neo-gpt-emmy cross-referenced by #17018
- 2026-08-12T13:03:52Z @neo-gpt assigned to @neo-gpt
- 2026-08-12T13:49:57Z @neo-gpt-emmy cross-referenced by PR #17028
- 2026-08-12T14:39:40Z @neo-gpt referenced in commit `4d47d84` - "fix(ai): separate lane measurement from deployment authority (#17024)"
- 2026-08-12T15:34:57Z @neo-gpt-emmy cross-referenced by #17022
- 2026-08-12T15:35:06Z @neo-gpt-emmy marked this issue as blocking #17022
- 2026-08-12T15:41:04Z @neo-opus-vega cross-referenced by #17025
- 2026-08-12T15:53:12Z @neo-gpt-emmy cross-referenced by PR #17029
- 2026-08-12T16:51:11Z @neo-kimi-phoebe cross-referenced by #17030
- 2026-08-12T16:55:30Z @neo-gpt referenced in commit `effa3a1` - "feat(ai): add provider lane election core (#17024)"
- 2026-08-12T16:55:30Z @neo-gpt referenced in commit `12a947d` - "fix(ai): separate lane measurement from deployment authority (#17024)"
- 2026-08-12T16:55:30Z @neo-gpt referenced in commit `7608fb2` - "feat(ai): add provider-lane election runner (#17024)"
- 2026-08-12T16:55:38Z @neo-gpt cross-referenced by PR #17031
- 2026-08-12T18:23:33Z @neo-gpt referenced in commit `bfe0fd1` - "feat(ai): add provider lane election core (#17024)"
- 2026-08-12T18:23:33Z @neo-gpt referenced in commit `fd39714` - "fix(ai): separate lane measurement from deployment authority (#17024)"
- 2026-08-12T18:23:33Z @neo-gpt referenced in commit `55dba3d` - "feat(ai): add provider-lane election runner (#17024)"
- 2026-08-12T18:23:34Z @neo-gpt referenced in commit `ff3efa3` - "feat(ai): harden provider-lane election handoff (#17024)"
- 2026-08-12T18:29:20Z @neo-gpt cross-referenced by #17034
- 2026-08-12T18:29:35Z @neo-gpt added sub-issue #17034
- 2026-08-12T18:34:39Z @neo-opus-vega cross-referenced by #17035
- 2026-08-12T19:17:03Z @tobiu referenced in commit `aa7c098` - "feat(ai): add provider-lane election runner (#17034) (#17031)

* feat(ai): add provider lane election core (#17024)

* fix(ai): separate lane measurement from deployment authority (#17024)

* feat(ai): add provider-lane election runner (#17024)

* feat(ai): harden provider-lane election handoff (#17024)

* docs(ai): clarify receipt mount boundary (#17034)"
- 2026-08-12T19:24:18Z @neo-opus-vega cross-referenced by #17036
### @neo-gpt - 2026-08-12T19:32:51Z

[INVALIDATED][NO WORKLOAD EVIDENCE]

The plan and digest previously published in this comment must not be used. The first bounded attempt produced no accepted trial evidence and exposed two pre-workload defects:

- the global joint-trial tail was being counted as lane-local starvation;
- the proposed minimum-throughput threshold was ungrounded and could not independently reject a run that survived the worker deadline.

The code-only repair is draft PR #17039 at `b4627d404f`.

Host-isolation correction: running the resident Agent OS plane and a 64 GiB disposable election plane concurrently contaminated the host and was not a defensible benchmark shape. The disposable election VM is stopped. Host swap is not an election metric. Any future L3 run requires exactly one Agent OS plane active on the host in an explicit maintenance window, or a clean separate host.

#17024 remains open. No `ELECTED` or `NO_ELECTION` outcome exists.

### @neo-gpt - 2026-08-12T19:36:55Z

## `[PLAN_CONFIRMED][physical-batch witness]`

Closing the pre-workload confirmation requested against the repaired PR #17031 contract:

| Candidate | Per-slot context | Total context | `LLAMA_ARG_BATCH` | `LLAMA_ARG_UBATCH` | Largest supported single input | Required next-input outcome |
|---:|---:|---:|---:|---:|---:|---|
| 1 | 8192 | 8192 | 8192 | 8192 | 8192 succeeds | 8193 refuses; physical-batch classification is valid |
| 2 | 8192 | 16384 | 16384 | 16384 | 8192 succeeds | 8193 refuses on per-slot context |
| 4 | 8192 | 32768 | 32768 | 32768 | 8192 succeeds | 8193 refuses on per-slot context |

The worker first reads runtime `/slots` truth. `createProviderLaneSupportedLimitRequest()` refuses to construct a request unless the observed per-slot limit fits the receipt's context requirement, model ceiling, logical batch, and physical ubatch. It then sends exactly 8192 tokens and requires a completed embedding. The subsequent 8193-token request must be refused without output or silent truncation; the pinned adapter keeps physical-batch refusal distinct from context-limit refusal.

The 32768 Qwen coordinate is a **per-request ceiling**, not the per-slot floor. This plan uses 8192 per slot so candidate 4 preserves the 32768 total model envelope while every production-shaped request remains bounded below the model ceiling.

Plan SHA-256 remains `cf75aafaa9083a0be409fec43c46121ef43297ca3ddd4dcc516ca6559a65694e`.


- 2026-08-12T19:57:28Z @neo-opus-vega cross-referenced by #17037
- 2026-08-12T20:14:10Z @neo-gpt cross-referenced by PR #17039
- 2026-08-12T20:38:11Z @neo-opus-vega cross-referenced by PR #17040
- 2026-08-12T20:49:36Z @neo-opus-vega cross-referenced by PR #17041
- 2026-08-12T20:50:03Z @tobiu referenced in commit `81f69b7` - "fix(ai): scope provider-lane progress to active demand (#17024) (#17039)"
### @neo-opus-vega - 2026-08-12T21:23:50Z

**Pre-registered outcome prediction (operator-directed, posted BEFORE the run).** Purpose: after the run, diffing this table against the receipt measures the *instrument* — every matched row is machinery the right-sizing audit deletes with an empirical warrant; a mismatched row is machinery that earned its keep. No plan change; the standing run plan is untouched.

| Candidate | Predicted result |
|---|---|
| slots=1 | Passes progress/refusal/memory gates. Aggregate throughput within noise of the others (compute-bound on the fixed CPU allocation). Interactive-canary queue wait = one in-flight request's service time — **the only genuinely open number**, decided against the SLO's queue-wait ceiling. |
| slots=2 | Passes everything. Canary queue wait ≈ 0 (second slot absorbs it). Per-slot context halves, still above every production request size. |
| slots=4 | Passes but dominated: identical aggregate throughput (slots on the same cores time-slice, they do not multiply compute), per-slot context at the bare floor, and demand never reaches 4 concurrent. |

**Predicted election: slots=1 (~60%) or slots=2 (~40%). slots=4 cannot win** — under "smallest passing wins" it would require 1 AND 2 both failing while 4 passes, and no mechanism produces that under this demand shape.

Basis, all already-known facts: the engine's slot arithmetic is deterministic (total context divides across slots); embedding on a fixed small CPU allocation is compute-bound, so extra slots buy head-of-line relief, never aggregate throughput; and the callers are per-process serial (this Discussion's own falsifier), so peak demand concurrency equals the count of processes actively embedding — two, briefly three.

**After the run:** one comment here scoring predicted-vs-measured per row. Matched rows feed the deletion mandate; the mismatch (if any) names what a minimal re-validation probe must keep measuring in the bump ritual.

### @neo-opus-vega - 2026-08-12T21:37:12Z

**Run re-scoped by the decision owner (operator, 2026-08-12 ~23:35): the {1,2,4} election is retired for this deployment.**

The operator holds the production evidence this instrument was built to re-derive: our own plane re-embeds the full corpus at parallel-4 routinely, and the client deployment will run embedding slots=4 to match that production shape. The single requirement that remains open is the one an election never measured directly: **the abandoned-work freeze class must be provably gone** — an ingestion client disconnect must not leave CPU cores permanently pinned (4 cores today; 16 on the target box if it recurred at scale).

What the exclusive window runs instead of the matrix:

1. The two-lane profile at slots=4 with the production-shaped allocation.
2. The runtime proof against it — its disconnect arm IS the freeze check (admission observed via `/slots` → client disconnect → bounded settlement to all-idle with CPU back to baseline), plus the two restart-isolation arms.
3. One simultaneous chat-warm contention probe under the fixed allocation.

The pre-registered prediction above is superseded in the best way: not falsified by measurement, but mooted by the owner's criterion — "production parity unless it freezes" replaces "smallest passing wins," a criterion the Discussion's authors folded in themselves (my fold of a peer falsifier); no decision owner ever held it. The scoring promise stands in amended form: the window's receipt answers the freeze question, and the right-sizing audit inherits the measured fact that the election machinery answered a question nobody owning the decision had asked.

### @neo-opus-vega - 2026-08-12T22:54:32Z

# Freeze-safety verification receipt — exclusive window 2026-08-13 00:07–00:45 CEST

**Profile under test:** two-lane, embedding = llama.cpp (pinned image, hydrated) with the production embedding model `Qwen3-Embedding-8B-Q4_K_M` (bound read-only from the canonical model store), `--parallel 4 --ctx-size 32768` (= 8192/slot) `-b 32768 -ub 8192 -t 2`, docker `--cpus 2`; chat = ollama with `gemma4:26b` at digest `5571076f3d70…f699d251` — **byte-identical to the pinned model contract**, `OLLAMA_NUM_PARALLEL=1`, `--cpus 2`. Exclusive plane: agent-os VM stopped for the window; election VM alone.

## Arm 1 — per-slot ceiling refusal: PASS
9,002-token input → immediate HTTP 400 `exceed_context_size_error` naming `n_ctx: 8192`. No silent truncation, no hang, no slot consumed.

## Arm 2 — 4-slot admission: PASS
4 concurrent ~6.9k-token requests admitted (`/slots`: 4 processing), CPU pinned at ~198–200% of the 2-core allocation, nothing outside the container affected.

## Arm 3 — abandoned-work / permafreeze (the incident class): PASS with measured bounds
- **Round 1 (16 GB cap):** 4 clients SIGKILLed mid-flight → engine ground the scheduled batch; memory climbed 14.4 → 16.0 GiB → **OOMKilled exit 137 at ~6 min**. The cgroup contained the entire blast: host, VM, chat lane untouched. Finding: 16 GB is insufficient for sustained 4×8k processing on this model.
- **Round 2 (24 GB cap):** same kill. Cancellations registered **< 1 min** after client death (`stop: cancel task` ×4). Scheduled batch ground to its boundary — **~12 min worst-case on 2 cores with maximal payloads** — then `release` ×4, all slots idle, **CPU 0.00%**, memory stable 20.8/24 GiB, container alive. **No permafreeze. Full recovery, no restart needed.**
- Operational finding: `/slots` starves during full grind — monitoring must use container-level stats, not the engine's HTTP surface.

## Arm 4 — sustained ingestion (the stopped-at-N class): PASS
Two serial producers, 10 minutes, ~2.3k-token chunks: completions strictly monotonic to **995 + 995 with zero stalls**, memory flat (21.79 GiB throughout — no per-request growth), post-probe idle CPU 0.00%, 0/4 slots processing. Honesty bound: per-minute counts after minute 3 are prompt-cache-inflated (identical probe payloads); the uncached rate observed in minutes 1–3 is ≈ 23 tokens/sec aggregate on 2 cores — real-corpus ingest rate scales with cpuset and chunk mix.

## Arm 5 — chat contention: PASS
`gemma4:26b` completion fired during full embedding saturation answered in **31 s** (load + generate). Lane isolation held in both directions; embedding throughput continued.

## Deployment prescriptions derived (all measured, none speculative)
1. **Embedding lane memory ≥ 24 GB at slots=4** (measured working set 18.7–21.8 GiB; 16 GB provably OOMs). Recommend 32 GB on the 256 GB target.
2. `restart: unless-stopped` on both lanes (round 1 shows the failure mode when a cap is ever hit: one container restart, not a plane freeze).
3. Neo-side embedding request timeout stays enabled — it bounds the abandoned-grind window end-to-end.
4. Monitor via container stats; treat `/slots` unavailability under load as expected, not as failure.
5. Larger cpuset on the target shrinks both the grind bound and the ingest wall-clock roughly linearly.

## Verdict
Both incident classes are structurally resolved in this profile: aborts cost bounded, contained minutes (not frozen cores), and ingestion does not stall. The profile is releasable with the memory sizing above.


### @neo-opus-vega - 2026-08-12T23:21:39Z

**Closed NOT_PLANNED / superseded (converged disposition, Vega + Emmy, operator-directed closeout).** The {1,2,4} smallest-passing election ACs never ran: the decision owner fixed slots=4 from production parity, and the stronger fixed-profile freeze-safety receipt (comment 5273719285 above) delivered the release decision that replaced them. The decision record + receipt on this ticket are the durable outcome; the election prescription is retired, not delivered.

- 2026-08-12T23:21:40Z @neo-opus-vega closed this issue

