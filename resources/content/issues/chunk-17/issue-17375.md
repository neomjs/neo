---
id: 17375
title: 'DevIndex collection leaves neo, and stops committing what it generates'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - build
assignees:
  - neo-opus-grace
createdAt: '2026-08-19T08:38:53Z'
updatedAt: '2026-08-19T20:13:27Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17375'
author: neo-opus-grace
commentsCount: 0
parentIssue: 17238
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 17374 The Data Factory reads its previous index from git, never from the web'
blocking:
  - '[ ] 17377 Register neomjs/devindex as a pull-mode tenant on the local Agent OS'
  - '[ ] 17376 Reclaim the generated-artifact history in neo and pages'
closedAt: '2026-08-19T20:13:27Z'
---
# DevIndex collection leaves neo, and stops committing what it generates

## Context

Sub 2 of #17238, and the ticket where the growth actually stops. `neomjs/devindex` exists, the application and its guides and unit suite already live there, and neo's copy is still the one the pipeline drives.

`#17238` measured `users.jsonl` at 90.5% of neo's blob bytes, and all devindex paths at 95.6%. Re-measured 2026-08-19: neo **5.21 GiB**, `neomjs/pages` **0.96 GiB**, 1,790 lifetime commits to that single file.

**These two changes are one ticket on purpose.** #17238's own body names *"relocating DevIndex without changing the storage model"* as the most attractive wrong answer, with `neomjs/pages` as the live proof of that failure mode — a fresh repository committing this file hourly reaches multiple GiB inside six months. Building the artifact-publish in neo and then moving it a week later is that mistake with an extra step. The new repository has no pipeline to retrofit, which makes greenfield strictly cheaper than sequencing them apart.

## The Problem

Two separable problems that must be solved together.

**1. The collection stages do not belong here.** `buildScripts/dataSyncPipeline.mjs` runs seven emission stages. Four are DevIndex:

| stage | tokenScope |
|---|---|
| install dependencies | `none` |
| GitHub Workflow corpus | `reader` |
| **DevIndex Opt-In** | **`intake`** |
| **DevIndex Opt-Out** | **`intake`** |
| **DevIndex Spider** | **`intake`** |
| **DevIndex Updater** | **`intake`** |
| content indexes and SEO | `reader` |

Every DevIndex stage uses `intake`; no neo stage does. The Intake token is minted for `devindex-opt-in` and `devindex-opt-out` (`data-sync-pipeline.yml`). **The credential axis is a clean cut, not an untangle.**

**2. Whatever runs them must not commit what they generate.** Otherwise the disease relocates with the app.

## The Architectural Reality

**What moves.** Four stages, and exactly one of six copy operations in `Push Data to neomjs/pages` — `cp apps/devindex/resources/data/users.jsonl …`. The other five (portal data, `workstation.png`, `sitemap.xml` + `llms.txt`, `learn/**`, `resources/content/**` across six buckets) are neo-corpus and stay. This is a pipeline **split**, not a move.

**What retires rather than moves.** Every DevIndex stage carries `publishGeneratedProgressOnFailure: true` plus the `deferredError` rethrow. Its own comment records why: a denial on the first DevIndex stage threw out of the loop, so `content indexes and SEO` never ran and the corpus was discarded unpublished — freezing `resources/content/**` on `dev` for nineteen hours. **That machinery exists only because DevIndex enrichment shares a process with neo's corpus publication.** Split the process and a DevIndex failure structurally cannot reach neo's corpus. The flag has nothing left to protect and should not be ported.

**What must not enter git, and what must.** Measured on disk:

```
users.jsonl · tracker.json · visited.json      26.5 MiB   DERIVED  — the Spider rebuilds it
allowlist · blocklist · optin-sync
optout-sync · threshold · failed                  543 B   CURATED  — irreplaceable
```

A 51,000× ratio, and only the small half is information. Losing the derived half costs Spider time; losing `blocklist.json` loses somebody's opt-out decision. **The curated 543 bytes stay in git — for this project the opt-out audit trail is an ethical asset, not merely data.** The derived 26.5 MiB never enters git again, in any repository.

**Why publishing without a commit is possible at all.** `#17374` establishes the read side: the pipeline obtains its previous state from the published artifact over HTTPS, verified live (`200`, `etag`, `accept-ranges`, open CORS). Once the producer no longer reads from git, nothing requires it to write to git either.

**Hosting is a late binding.** The shape is identical whether the destination is GitHub Pages' artifact deploy, an upload to whatever serves `neomjs.com` (`x-powered-by: Express` — it is not bare Pages), or object storage. Every candidate supports publish-without-commit; only the branch-based Pages source does not, and that source is precisely the mechanism that built the 985 MB. **Hosting decides the workflow's final step, not the design.**

## The Fix

- a devindex-owned workflow runs the four `intake`-scoped stages, on the same hourly cadence
- it publishes the derived artifacts **without a commit**, to the destination the operator selects
- `.gitignore` in `neomjs/devindex` excludes the derived three; the curated six stay tracked
- neo's `dataSyncPipeline.mjs` drops the four stages, the Intake token mint, and the `users.jsonl` copy
- `publishGeneratedProgressOnFailure` / `deferredError` are removed from the moved stages rather than ported
- neo deletes `learn/guides/devindex/**` and `apps/devindex/**`
- the checkout drops to `fetch-depth: 1` once nothing here pushes generated data (see below)

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| DevIndex emission stages | `buildScripts/dataSyncPipeline.mjs` | the four `intake` stages are removed from neo and defined in the devindex workflow | none — a stage that runs in neither repository is a gap, not a degradation | the Data Factory guide's Orchestrator page | a fixture asserting neo's stage list contains no `intake`-scoped entry |
| Intake installation token | `data-sync-pipeline.yml` token mint | minted by the devindex workflow; neo mints reader + publisher only | none | the workflow's own comments | neo's mint step no longer requests `devindex-opt-in` / `devindex-opt-out` |
| derived artifacts in git | `.gitignore` in `neomjs/devindex` | `users.jsonl`, `tracker.json`, `visited.json` are never committed | none by design — a committed copy is the defect | the repository README | a guard failing if any derived path is tracked |
| curated inputs in git | same | the six small files stay tracked and reviewable | none | as above | the same guard asserting they ARE tracked, so the rule cannot be satisfied by ignoring everything |

## Decision Record impact

`none`. Structure-map gate executed; **N/A** for Agent OS placement — surfaces are `apps/devindex/**`, `buildScripts/dataSyncPipeline.mjs`, `.github/workflows/data-sync-pipeline.yml` and the new devindex-side workflow.

## Acceptance Criteria

- [ ] **AC-1:** the four `intake`-scoped stages run from a devindex-owned workflow on the hourly cadence, and neo's stage list contains no `intake`-scoped entry. A spec asserts the absence rather than a reviewer reading the list.
- [x] **AC-0 (added 2026-08-19; DONE — and its second half was never the blocker I claimed):** no consumer in `neomjs/devindex` depends on the checkout copy being *committed*. Producer: `neomjs/devindex@72a7043`. Delivery: `neomjs/devindex@a7dcd27` — `npm install` fetches the published index (~10 MB gzipped, ~1.4 s) to the path the app already reads. **`store/Contributors.mjs` was never changed and never needed to be** — see the corrected note below.
- [x] **AC-2 (DONE):** `neomjs/devindex@a7dcd27` — `users.jsonl`, `tracker.json` and `visited.json` are gitignored and `git rm --cached`ed; `buildScripts/checkDataTracking.mjs` fails if any is tracked. Verified failing on a re-added derived file, then green.
- [x] **AC-3 (DONE):** the same guard asserts the curated seven ARE tracked, and was verified failing on a removed `blocklist.json` — the shortcut it exists to block. It immediately earned itself by catching that `index-provenance.json` did not yet exist; that was seeded through `Storage.recordIndexProvenance` so the format cannot drift from what the producer writes.
- [ ] **AC-4:** `publishGeneratedProgressOnFailure` and the `deferredError` arm are **absent** from the moved stages, and the reason is recorded where a future reader will meet it. Porting them is the defect: after the split there is no shared corpus publication for a DevIndex failure to abort.
- [ ] **AC-5:** neo's `Push Data to neomjs/pages` no longer copies `users.jsonl`, and the other five copy operations are untouched. A spec pins the five, so a later edit cannot quietly widen this removal.
- [ ] **AC-6:** `learn/guides/devindex/**` and `apps/devindex/**` are removed from neo, and no neo surface references them — swept with a stated file set and a control proving the sweep can reach a planted reference.
- [ ] **AC-6b:** `data-sync-pipeline.yml` checks out at `fetch-depth: 1`. Raised in review of #17378: `actions/checkout` defaults to shallow and this workflow explicitly sets `fetch-depth: 0` — the only option on that step with no justifying comment. The git work the pipeline does (`rev-parse`, `reset --hard origin/dev`, `diff --cached`, SHA **equality** rather than ancestry) needs no history; the depth is presumed to exist for the publish push, since remotes reject shallow updates. **This ticket removes that push**, so the presumption is testable here and nowhere else. If shallowing still fails, record why — an uncommented option that turns out to be load-bearing deserves the comment it never had. Expected payoff: the 239 s `Checkout repository` step of a 22.6 min run.
- [ ] **AC-7 (post-merge):** one full hourly cycle completes end to end from the devindex workflow, and the served artifact's `last-modified` advances. Flagged post-merge: no pre-merge evidence can observe a scheduled run.

## Correction, 2026-08-19 — this ticket counted one consumer and there are two

**The Architectural Reality above says "The browser has always read this file over HTTPS from the deployed site; only the producer read it from git."** That is true of `neomjs/neo`, where the browser fetches the deployed copy, and it is **false of `neomjs/devindex` as it stands.** Measured:

```
apps/devindex/store/Contributors.mjs:50
    url: Neo.config.basePath + 'apps/devindex/resources/data/users.jsonl'
```

`basePath`-relative resolves against whatever is serving the app — which, in local development, is the checkout. So in this repository the checkout has **two** readers, not one, and AC-2's guard would have gone green while the grid rendered empty for the operator on `localhost:8082` and for anyone cloning fresh.

I inherited the single-consumer framing from #17374, where it was correct, and did not re-check it against the repository this ticket actually targets.

### Second correction, same day — my remedy was wrong even though the observation was right

I wrote that the browser consumer was "the remaining blocker" and gated AC-2 on it. **It was not a blocker, and the operator's reframing is what exposed why:** *local* and *committed* are separable axes. The bloat never came from the file being on disk — it came from committing it hourly. So the store's `basePath`-relative URL was correct all along; the only thing missing was something to put the file at that path once git stopped.

`npm run devindex:pull-data` (also run from `postinstall`) does that in ~1.4 s, and **`store/Contributors.mjs` is unchanged**. Fetching remotely at runtime — which is where my "blocker" framing was heading — would have moved the cost from one download per developer to one per visitor per page load, which is worse on every axis including the one that motivated the ticket.

The general shape worth keeping: **an observation can be correct and its implied remedy still wrong.** I measured a real dependency and then reached for the expensive way to break it.

### What the same measurement pass also found

The file is committed hourly into **two** repositories, not one. In `neomjs/pages` it lands at `node_modules/neo.mjs/apps/devindex/resources/data/users.jsonl` — which is exactly the URL the app fetches, proxied by the Cloud Run middleware. A blob census there (against a 46-day-stale clone, so a **floor**) attributes **75.8% of all blob bytes** to that one path. GitHub reports `neomjs/pages` at **986 MB** and `neomjs/neo` at **5,368 MB**; `neomjs/devindex` is **11 MB** and now stays there.

This widens AC-5: neo's copy step is not the only writer that has to stop.

**This does not change the design**, and it is worth saying why rather than leaving it implied: the browser fix is the same late binding as the producer's — point the store at the published artifact instead of a checkout-relative path. It is blocked on the same hosting decision and lands with it, not before it.

## Out of Scope

- **Reclaiming existing history.** Axis 3. This stops accrual; it reclaims nothing, and neo stays at 5.21 GiB until that lands.
- **`neomjs/pages` growth from the neo corpus.** Removing DevIndex stops DevIndex's contribution; `resources/content/**` and portal data keep the curve going. Same mechanism would fix it and that is the larger prize, but it is neo's corpus, not this app's.
- **The DevIndex product roadmap** — collection heuristics, threshold tuning, opt-in flows.

## Avoided Traps

- **Relocating without the storage model.** The named most-attractive-wrong-answer of #17238. `neomjs/pages` at 0.96 GiB is the proof, and it is five months behind neo on the identical curve.
- **Building the artifact-publish in neo first, then moving it.** Correct in sequence, wasteful in fact: the new repository has no pipeline to retrofit.
- **Porting the deferral machinery because it looks like hardening.** It is a workaround for a coupling this ticket deletes. Carrying it forward preserves a scar with no wound.
- **`.gitignore`-ing the whole data directory.** It satisfies AC-2 and silently discards the opt-out audit trail — the one artifact in there that cannot be regenerated. AC-3 exists to make that failure loud.
- **Waiting on the hosting decision to start.** The design is destination-independent; only the final publish step differs.

## Related

Parent: #17238 · depends on #17374 (the read side) · Axis 3 depends on this.

Retrieval Hint: `query_raw_memories("devindex pipeline split intake token four stages deferredError retire")` · `"publish without commit derived versus curated"`

Live latest-open sweep: checked the latest 20 open issues at 2026-08-19T08:36:42Z plus a 30-message A2A claim scan across all read-states; no equivalent ticket and no in-flight claim on this scope.

Origin Session ID: a105d215-c261-4b34-82a9-546596f665ef




## Timeline

- 2026-08-19T08:38:54Z @neo-opus-grace added the `enhancement` label
- 2026-08-19T08:38:54Z @neo-opus-grace added the `ai` label
- 2026-08-19T08:38:54Z @neo-opus-grace added the `architecture` label
- 2026-08-19T08:38:55Z @neo-opus-grace added the `build` label
- 2026-08-19T08:39:33Z @neo-opus-grace cross-referenced by #17376
- 2026-08-19T08:39:41Z @neo-opus-grace added parent issue #17238
- 2026-08-19T08:39:48Z @neo-opus-grace marked this issue as being blocked by #17374
- 2026-08-19T08:39:50Z @neo-opus-grace marked this issue as blocking #17376
- 2026-08-19T08:48:35Z @neo-opus-grace cross-referenced by #17377
- 2026-08-19T08:48:41Z @neo-opus-grace marked this issue as blocking #17377
- 2026-08-19T09:02:57Z @neo-opus-grace cross-referenced by PR #17378
- 2026-08-19T11:04:03Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-19T11:06:41Z @neo-opus-grace referenced in commit `72a7043` - "feat(services): the producer reads its previous index from the published artifact (neomjs/neo#17375)

Ports the read side of neomjs/neo#17374 into this repository, where the pipeline
is going to live. Until now the Data Factory obtained its previous state from
whatever the checkout happened to hold, which is the only reason a 24 MB derived
file has to be committed at all.

`getUsers()` now prefers the deployed artifact over HTTPS and falls back to the
checkout copy audibly. Three properties make preferring it safe: the fetched
bytes are compared against a SHA-256 this pipeline recorded when it last wrote
the index, ABSENCE of that record is treated as the documented bootstrap case
rather than as mismatch, and every retreat to the checkout names its reason. The
JSONL parse is inside the guard specifically because the bootstrap run has no
digest to reject a mangled body with.

Provenance is recorded inside `writeJson` rather than at the three call sites
that write the index, because provenance only some of them update is worse than
none: a stale digest reads as a foreign artifact and sends every later run down
the fallback path for a reason nobody could find.

The URL still points at the artifact neo publishes, because that is where the
index is served from today. It is a late binding by design — when the hosting
destination is chosen, that one literal changes and nothing else does.

Verified end to end against the live artifact: 50,000 records via the published
path in 2.4s, no fallback, emitting exactly the one documented bootstrap warning.

This is the prerequisite for the untracking, not the untracking itself. Both
consumers still read from the checkout — `store/Contributors.mjs` loads a
basePath-relative URL — so removing the derived files from git today would empty
the grid on a fresh clone. Recorded on neomjs/neo#17375.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T12:03:32Z @neo-opus-grace referenced in commit `355e447` - "feat(ci): the four DevIndex collection stages move here from neo (neomjs/neo#17375)

The collection half of the pipeline split. optin, optout, spider and updater now
have a home in the repository that owns them, each carrying the Intake App token
that neo minted for exactly these four and no others — the credential axis was
always a clean cut rather than an untangle.

Deliberately NOT ported: publishGeneratedProgressOnFailure and its deferredError
rethrow. That machinery exists in neo because DevIndex enrichment shared a
process with neo's corpus publication, so one denied stage discarded the whole
corpus unpublished and froze resources/content for nineteen hours. Splitting the
process removes the coupling it protected against; there is no shared
publication here to abort. Carrying it over would import a workaround into a
structure that cannot have the problem, and would read as though it still could.

No schedule, and that is the design rather than an omission. The publish step
does not exist yet, and these stages are not inert: optout comments on and
closes real issues, the spider and updater spend API quota. Running hourly
without a destination would mutate other repositories to produce output the
runner then throws away. So dispatch-only, collection opt-in per run, and the
default dispatch is a credential probe with no side effects — the check you can
safely repeat while correcting an installation, which is when you need it.

The publish step is present and fails loudly rather than being absent, so a run
cannot be mistaken for a working pipeline. It is the one piece blocked on the
operator's hosting decision.

fetch-depth: 1 because nothing here pushes, resets to a remote head, or compares
SHAs for ancestry. neo sets 0 for its publish push, which this does not have.

Verified: YAML parses, no schedule trigger, all four npm scripts resolve.
Requires DATA_SYNC_INTAKE_APP_ID and DATA_SYNC_INTAKE_PRIVATE_KEY as repository
secrets — operator-provisioned; the probe run exists to check them.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T12:38:21Z @neo-opus-grace referenced in commit `a7dcd27` - "feat(data): the derived index is fetched, not versioned (neomjs/neo#17375)

26.5 MiB of derived data leaves git. users.jsonl, tracker.json and visited.json
are rewritten in full every run, and committing them is the most expensive
possible way to deliver a file nobody reads the history of: the same file in
neomjs/neo carries 1,802 commits behind a 4.1 GB .git. This repository is at
commit one with a 14 MB .git, which is the only reason the choice is still cheap.

Local was never the problem — committed was. The app keeps reading
apps/devindex/resources/data/users.jsonl through the same basePath-relative,
same-origin URL, so it is exactly as fast as before and costs no per-visitor
traffic. Only the delivery changed: postinstall fetches the published index
(~10 MB gzipped, ~1.4s), and `npm run devindex:pull-data` refreshes it. Pointing
the store at a remote host would have moved the cost from one download per
developer to one per visitor per page load.

The pull is never fatal and skips under CI: the pipeline's producer fetches and
digest-verifies the index itself, so downloading it into a runner that discards
it would be waste on every scheduled run. Offline installs print the remedy and
continue.

checkDataTracking asserts BOTH directions, and the second is what makes the
first honest: derived files must be untracked, and the curated seven must stay
tracked. Checking only the first is satisfied by ignoring the whole directory,
which passes loudly while discarding blocklist.json — somebody's opt-out
decision, 459 bytes, unregenerable. Verified failing in both directions and on
the shortcut.

index-provenance.json is seeded from the published bytes via Storage's own
recordIndexProvenance, so the format cannot drift from what the producer writes.
getUsers() now completes with ZERO warnings — previously it took the documented
bootstrap exemption and accepted the artifact unverified; it now fetches,
digests and matches. 50,000 records, 1.6s.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T12:58:35Z @neo-opus-grace referenced in commit `2d12880` - "docs(ci): record the third mint failure mode, measured on our own probe run (neomjs/neo#17375)

A non-integer App ID fails while BUILDING the JWT, so it implicates neither the
private key nor the installation — but it surfaces as the same red mint step as
the two failures neo already documents. Run 32252194883 hit it because the App
settings page shows Client ID adjacent to App ID and the Client ID begins 'Iv'.

Recorded beside neo's two so the next person reads the message-to-cause mapping
instead of rotating a key that was never wrong.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T13:05:26Z @neo-opus-grace referenced in commit `9a2a376` - "feat(ci): discovery state survives between runs via an artifact (neomjs/neo#17375)

tracker.json and visited.json are the Spider's memory — which logins are known
and which have been walked. They are gitignored alongside users.jsonl and have
no browser consumer, so they are neither committed nor published; they only need
to survive from one run to the next, which is what an artifact is for. 3.6 MB,
7-day retention, a handoff rather than an archive.

Both halves are non-fatal in the direction that matters. A missing artifact is
the normal first-run bootstrap, not an error: the Spider rediscovers, which is
the same reason these files are safe to keep out of git, applied one layer up.

Deliberately NOT an `if: always()` upload. Persisting state from a FAILED run is
the same shape as publishGeneratedProgressOnFailure, which this workflow refuses
on purpose — it trades a simple invariant, that state comes from a run which
completed, for protection against a failure mode that is already cheap. A
crashed run discards its state and the next one re-derives.

`actions: read` is added for exactly one reason: gh run download reaching the
previous run's artifact. The job still cannot write anything anywhere.

This half needs no hosting decision. Where users.jsonl gets published is still
open; where the discovery state lives never was.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T13:10:48Z @neo-opus-grace referenced in commit `14dfe31` - "fix(services): an unreadable index must not become an empty one (neomjs/neo#17375)

A data-loss path I introduced two commits ago, found by tracing what a stale
fetch would actually do.

getUsers() fell back to `readJson(paths.users, [])`. updateUsers() merges new
records INTO that and writes the result as the ENTIRE index. So a transient
fetch failure did not degrade — it TRUNCATED: a run that enriched 200 users
would publish an index of 200, record provenance for the truncated file, and
ship it. 49,800 contributors gone, every log line green.

That fallback was safe while the checkout carried a committed copy, because it
always landed on ~50,000 real records. Removing users.jsonl from git removed the
net without changing the fallback, and under CI the file is absent by
construction — gitignored, and the pull deliberately skips. The guard outlived
the assumption it was written under.

Now: fetched index wins; a local copy still wins after that, so the developer
path is untouched and the check is for ABSENCE rather than for CI; and only the
case with neither throws. An absent prior index is indistinguishable from a lost
one and the two have opposite correct responses, so it fails closed.

DEVINDEX_ALLOW_EMPTY_INDEX=1 is the declared escape for a true first-ever run —
the one case that legitimately has nothing, and which no measurement can tell
apart from an outage.

Verified in both directions: with the fix it throws; with the old fallback
restored it returns 0 records and would have truncated. Developer path re-checked
at 50,000 records over the published path.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:10:48Z @neo-opus-grace referenced in commit `9d34544` - "refactor(services): the three derived files are one working set, fetched atomically (neomjs/neo#17375)

They are not an index plus some state. Every run reads all three, mutates all
three and writes all three — Cleanup alone rewrites the lot before every
command. Treating them as one deliverable and two lesser files was an error of
sizing: by on-disk bytes tracker and visited look cheap, but in neo's history
they carry 3.76 GB and 1.38 GB against users.jsonl's 40.06 GB, and tracker.json
has MORE commits than the index does.

So they travel together. hydrateWorkingSet() fetches all three once per process,
before anything reads them, and adopts them all-or-nothing. A partial adoption
is the dangerous outcome: tracker.json decides who gets enriched, so an index
from one generation paired with a tracker from another makes the Updater skip
users that are stale and re-enrich users that are not, silently. Verified — with
users.jsonl serving and tracker.json 404ing, the whole set is refused and the
local copies stand.

Materialised to disk rather than memoised, so every existing reader and writer
keeps working unchanged and no caller can see the fetched copy while another
sees the checkout copy. Memoising parsed objects would have left updateVisited,
which reads its path directly, on stale data.

index-provenance.json becomes working-set-provenance.json: one digest per file,
written together, checked together. A per-file record could be partially
current, which describes a state no reader may act on.

It ships with `digests: null` on purpose. While neo is still the publisher this
repository cannot hold digests for bytes it did not write — neo's next hourly run
would invalidate them and every hydration would reject. Null takes the documented
absence branch and adopts the published set unverified, which IS the hand-off
this migration needs. The first publishing run writes real digests.

Corrected a claim I had shipped in config: all three ARE served today (200 from
the neomjs.com base, because neomjs/pages carries the whole node_modules/neo.mjs
tree and the middleware proxies it). I had written that only users.jsonl was
there.

Guides updated, which is where this outlives the code:
- Storage.md gains "The Working Set: derived, delivered, never versioned" — why
  they are not in git, why they are verified as a set, and what getUsers() does
  when there is nothing to merge into.
- Orchestrator.md's pipeline section described neo's workflow and a
  buildScripts/dataSyncPipeline.mjs that has never existed here. Rewritten to
  describe the workflow that now actually ships beside it, with the two
  deliberate absences named.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:24:14Z @neo-opus-grace referenced in commit `58d4792` - "feat(ci): the working set is published as objects, and the manifest ships with it (neomjs/neo#17375)

The publish step exists now. buildScripts/publishWorkingSet.mjs uploads the three
payload objects and then the manifest, in that order, because a manifest must
never advertise a set that is still arriving — uploads are not transactional and
that ordering is what substitutes for it.

Deliberately not a port of neo's dataSyncPipeline.mjs. All eighteen of its
functions serve git publication, multi-credential scoping, or the deferred-failure
flag; this pipeline does none of the three. Its bounded state machine exists
because neo commits to dev, a branch humans also push to — an object upload has no
rebase, no allowlist and no "did the branch move" question. Porting it would
inherit an answer to a question this architecture never asks.

provenance -> manifest, and the rename is the honest part. It was tracked in git,
which made it an anchor the artifacts could not influence. That is stronger, and
it required committing it every run — a write checkout, a push credential and a
race answer, i.e. the whole state machine above, for 355 bytes. So it ships with
the set and is derived rather than trusted. Still caught: torn publication, failed
propagation, mixed generations. No longer caught: a wholesale consistent
overwrite. Stated in the leaf, the guide and the script rather than implied,
because a consistently stale set is safe to work from and a mixed one is not.

The Actions artifact round-trip from 9a2a376 is REMOVED rather than kept beside
this. It was a second mechanism for files that share one lifecycle with the index,
and it had a 7-day retention cliff the publication does not. One mechanism, four
files. `actions: read` goes with it — it existed only for gh run download, so the
job is back to contents:read and nothing else.

Verified: torn publication still refused (users.jsonl serving, tracker.json 404 ->
whole set rejected, local copies stand); live hydration adopts all three; guard
green at 4 derived untracked / 6 curated tracked; publish refuses with no
destination rather than silently discarding.

Guides updated — Storage.md carries the manifest-vs-provenance trade, and
Orchestrator.md's "not yet wired" section now names what actually remains.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:32:08Z @neo-opus-grace referenced in commit `76f9986` - "fix(ci): publish authenticates keyless, and not as the deploy identity (neomjs/neo#17375)

I had written `credentials_json` with a service-account key. That would have put
the one long-lived Google credential into a project that deliberately has none:
`neomjs/middleware-v2`'s deploy-gcp.yml already uses Workload Identity Federation,
and setup-gcp.sh provisions the pool, the provider and a per-repository binding.
It would also have sat beside a GitHub App token this same workflow mints fresh
every run — a permanent secret in an otherwise short-lived setup.

Now WIF: `id-token: write`, google-github-actions/auth@v3 with
workload_identity_provider + service_account, no stored credential.

And a SEPARATE service account from the middleware's deploy identity. That one
holds run.admin, artifactregistry.writer and iam.serviceAccountUser — it can
redeploy the service. This job writes four objects to one bucket prefix, so its
identity should hold object-write on that prefix and nothing else. Reusing the
deploy SA would hand an hourly data job the ability to roll Cloud Run revisions.

WIF bindings are scoped per-repository via attribute.repository, so the
middleware's binding does not cover neomjs/devindex; it needs its own.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:36:32Z @neo-opus-grace referenced in commit `a85e1e5` - "chore(gcp): one-time, idempotent setup for publish auth (neomjs/neo#17375)

The WIF setup was scripted in middleware-v2 and is easy to forget, so this is the
devindex-side equivalent rather than a runbook nobody re-reads.

Reuses the Workload Identity pool and provider that already exist for the
project — those are the fiddly part and are per-project, so nothing here
re-creates them. What is genuinely new is two things: a devindex-publish-sa with
roles/storage.objectAdmin on one bucket, and a per-repository impersonation
binding, because attribute.repository bindings are scoped to one repo and the
middleware's does not cover this one.

A SEPARATE identity from github-actions-sa on purpose. That one holds run.admin
and artifactregistry.writer — it can redeploy the service. An hourly data job
that writes four objects should not be able to.

objectAdmin rather than objectCreator: the publish overwrites the same four
objects every run, and objectCreator cannot replace.

Objects land under the bucket's dist/ prefix because the Cloud Run mount uses
only-dir=dist — a bucket-root object surfaces at /app/dist/dist/... and 404s,
which is recorded in the middleware's own architecture notes as the rev-00039
lesson.

Neither stored value is a credential: WIF_PROVIDER is a resource path,
PUBLISH_SA_EMAIL an address. The runner mints a short-lived token per run.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:42:51Z @neo-opus-grace referenced in commit `de44c8c` - "fix(ci): the probe now tests the publish credential too (neomjs/neo#17375)

Two defects, both found by checking what actually landed rather than assuming.

DEVINDEX_PUBLISH_BUCKET was specified as a variable and stored as a secret, so
`vars.DEVINDEX_PUBLISH_BUCKET` resolved EMPTY and the publish would have failed
with "no destination" while the value was sitting right there. Now reads either,
so neither choice fails silently. A variable remains the better home — as a
secret the path is masked as *** in logs, hiding the destination in exactly the
output you read when a publish goes wrong — but that is a preference, not a
requirement.

The Google auth step was gated on run_collection, so the credential probe tested
the GitHub App and skipped the Google identity entirely — the half that was just
configured. WIF authentication exchanges an OIDC token and has no side effects,
so there is no reason to gate it. Ungated now.

Added a read-only publish-access probe, because minting a token is not the same
as being able to reach the destination: a storage.objectAdmin binding on the
WRONG bucket authenticates perfectly and fails at the first write. It lists the
prefix and says what it is pointed at.

So `run_collection=false` is now a complete credential check for both identities
with zero side effects, which is what a probe was supposed to be.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:47:31Z @neo-opus-grace referenced in commit `feed6e8` - "fix(gcp): devindex needs its own WIF provider, not a binding on the shared one (neomjs/neo#17375)

The probe caught this and my earlier instructions were wrong.

github-provider carries --attribute-condition="assertion.repository ==
'neomjs/middleware-v2'". A devindex token is rejected AT THE PROVIDER, before any
service-account binding is consulted — which is the unauthorized_client the run
returned. So the three commands I gave would not have worked: the binding was
necessary and insufficient, and the WIF_PROVIDER value pointed at a provider that
can only ever reject this repository.

Widening that condition would work and is the wrong move: it is what
middleware-v2's production deploys authenticate through, and a bad edit breaks
them. A pool holds many providers, so this adds one scoped to neomjs/devindex and
touches nothing that already works.

WIF_PROVIDER must therefore point at .../providers/devindex-provider, not
.../providers/github-provider.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:56:20Z @neo-opus-grace referenced in commit `67cc1f8` - "fix(ci): the publish probe blamed the wrong failure (neomjs/neo#17375)

It printed "check the objectAdmin binding" for any failure. On run 32266632122
the actual cause was a 403 on iam.serviceAccounts.getAccessToken — impersonation
refused by IAM propagation, minutes after the binding was created — and the
bucket grant was correct the whole time. I nearly chased the wrong resource on my
own error message.

Now it reads the error and separates the two, because they resolve differently:
impersonation 403 usually fixes itself with time, a bucket 403 never does.

Both credential paths verified green on run 32266891168.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T14:58:25Z @neo-opus-grace referenced in commit `c0c7c29` - "fix(gcp): deployment identifiers must not live in a public repo (neomjs/neo#17375)

neomjs/devindex is PUBLIC. neomjs/middleware-v2, where the GCP project id,
project number and bucket name live, is PRIVATE. Hardcoding them here moved them
private -> public, which is the one direction that is never acceptable, and I did
it without checking either repository's visibility first.

Not a vulnerability: nothing here is executable by a stranger. Every mutating
call is authorisation-gated server-side, the script carries no credential, and
Workload Identity's safety rests on an attribute condition only GitHub can
satisfy for this repository — not on any of these values being unknown.

But together they map the whole publish path: project, number, bucket, pool,
provider, and which identity holds which role. That is reconnaissance handed over
for free, and withholding it costs nothing.

Now read from DEVINDEX_GCP_PROJECT_ID / _PROJECT_NUMBER / _BUCKET with `:?`
guards, so the script fails with an instruction rather than a wrong default. Also
removed the real bucket from publishWorkingSet.mjs's example error text.

HEAD is clean. The commits that introduced them are still in history — see the
follow-up note; a rewrite is cheap on an 11 MB repository if we want one.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:49Z @neo-opus-grace referenced in commit `d71b698` - "feat(ci): the working set is published as objects, and the manifest ships with it (neomjs/neo#17375)

The publish step exists now. buildScripts/publishWorkingSet.mjs uploads the three
payload objects and then the manifest, in that order, because a manifest must
never advertise a set that is still arriving — uploads are not transactional and
that ordering is what substitutes for it.

Deliberately not a port of neo's dataSyncPipeline.mjs. All eighteen of its
functions serve git publication, multi-credential scoping, or the deferred-failure
flag; this pipeline does none of the three. Its bounded state machine exists
because neo commits to dev, a branch humans also push to — an object upload has no
rebase, no allowlist and no "did the branch move" question. Porting it would
inherit an answer to a question this architecture never asks.

provenance -> manifest, and the rename is the honest part. It was tracked in git,
which made it an anchor the artifacts could not influence. That is stronger, and
it required committing it every run — a write checkout, a push credential and a
race answer, i.e. the whole state machine above, for 355 bytes. So it ships with
the set and is derived rather than trusted. Still caught: torn publication, failed
propagation, mixed generations. No longer caught: a wholesale consistent
overwrite. Stated in the leaf, the guide and the script rather than implied,
because a consistently stale set is safe to work from and a mixed one is not.

The Actions artifact round-trip from 9a2a376 is REMOVED rather than kept beside
this. It was a second mechanism for files that share one lifecycle with the index,
and it had a 7-day retention cliff the publication does not. One mechanism, four
files. `actions: read` goes with it — it existed only for gh run download, so the
job is back to contents:read and nothing else.

Verified: torn publication still refused (users.jsonl serving, tracker.json 404 ->
whole set rejected, local copies stand); live hydration adopts all three; guard
green at 4 derived untracked / 6 curated tracked; publish refuses with no
destination rather than silently discarding.

Guides updated — Storage.md carries the manifest-vs-provenance trade, and
Orchestrator.md's "not yet wired" section now names what actually remains.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:49Z @neo-opus-grace referenced in commit `c2a69da` - "fix(ci): publish authenticates keyless, and not as the deploy identity (neomjs/neo#17375)

I had written `credentials_json` with a service-account key. That would have put
the one long-lived Google credential into a project that deliberately has none:
`neomjs/middleware-v2`'s deploy-gcp.yml already uses Workload Identity Federation,
and setup-gcp.sh provisions the pool, the provider and a per-repository binding.
It would also have sat beside a GitHub App token this same workflow mints fresh
every run — a permanent secret in an otherwise short-lived setup.

Now WIF: `id-token: write`, google-github-actions/auth@v3 with
workload_identity_provider + service_account, no stored credential.

And a SEPARATE service account from the middleware's deploy identity. That one
holds run.admin, artifactregistry.writer and iam.serviceAccountUser — it can
redeploy the service. This job writes four objects to one bucket prefix, so its
identity should hold object-write on that prefix and nothing else. Reusing the
deploy SA would hand an hourly data job the ability to roll Cloud Run revisions.

WIF bindings are scoped per-repository via attribute.repository, so the
middleware's binding does not cover neomjs/devindex; it needs its own.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:49Z @neo-opus-grace referenced in commit `969392f` - "chore(gcp): one-time, idempotent setup for publish auth (neomjs/neo#17375)

The WIF setup was scripted in middleware-v2 and is easy to forget, so this is the
devindex-side equivalent rather than a runbook nobody re-reads.

Reuses the Workload Identity pool and provider that already exist for the
project — those are the fiddly part and are per-project, so nothing here
re-creates them. What is genuinely new is two things: a devindex-publish-sa with
roles/storage.objectAdmin on one bucket, and a per-repository impersonation
binding, because attribute.repository bindings are scoped to one repo and the
middleware's does not cover this one.

A SEPARATE identity from github-actions-sa on purpose. That one holds run.admin
and artifactregistry.writer — it can redeploy the service. An hourly data job
that writes four objects should not be able to.

objectAdmin rather than objectCreator: the publish overwrites the same four
objects every run, and objectCreator cannot replace.

Objects land under the bucket's dist/ prefix because the Cloud Run mount uses
only-dir=dist — a bucket-root object surfaces at /app/dist/dist/... and 404s,
which is recorded in the middleware's own architecture notes as the rev-00039
lesson.

Neither stored value is a credential: WIF_PROVIDER is a resource path,
PUBLISH_SA_EMAIL an address. The runner mints a short-lived token per run.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:49Z @neo-opus-grace referenced in commit `6d3fc79` - "fix(ci): the probe now tests the publish credential too (neomjs/neo#17375)

Two defects, both found by checking what actually landed rather than assuming.

DEVINDEX_PUBLISH_BUCKET was specified as a variable and stored as a secret, so
`vars.DEVINDEX_PUBLISH_BUCKET` resolved EMPTY and the publish would have failed
with "no destination" while the value was sitting right there. Now reads either,
so neither choice fails silently. A variable remains the better home — as a
secret the path is masked as *** in logs, hiding the destination in exactly the
output you read when a publish goes wrong — but that is a preference, not a
requirement.

The Google auth step was gated on run_collection, so the credential probe tested
the GitHub App and skipped the Google identity entirely — the half that was just
configured. WIF authentication exchanges an OIDC token and has no side effects,
so there is no reason to gate it. Ungated now.

Added a read-only publish-access probe, because minting a token is not the same
as being able to reach the destination: a storage.objectAdmin binding on the
WRONG bucket authenticates perfectly and fails at the first write. It lists the
prefix and says what it is pointed at.

So `run_collection=false` is now a complete credential check for both identities
with zero side effects, which is what a probe was supposed to be.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:49Z @neo-opus-grace referenced in commit `ae4b04a` - "fix(gcp): devindex needs its own WIF provider, not a binding on the shared one (neomjs/neo#17375)

The probe caught this and my earlier instructions were wrong.

github-provider carries --attribute-condition="assertion.repository ==
'neomjs/middleware-v2'". A devindex token is rejected AT THE PROVIDER, before any
service-account binding is consulted — which is the unauthorized_client the run
returned. So the three commands I gave would not have worked: the binding was
necessary and insufficient, and the WIF_PROVIDER value pointed at a provider that
can only ever reject this repository.

Widening that condition would work and is the wrong move: it is what
middleware-v2's production deploys authenticate through, and a bad edit breaks
them. A pool holds many providers, so this adds one scoped to neomjs/devindex and
touches nothing that already works.

WIF_PROVIDER must therefore point at .../providers/devindex-provider, not
.../providers/github-provider.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:50Z @neo-opus-grace referenced in commit `95f7153` - "fix(ci): the publish probe blamed the wrong failure (neomjs/neo#17375)

It printed "check the objectAdmin binding" for any failure. On run 32266632122
the actual cause was a 403 on iam.serviceAccounts.getAccessToken — impersonation
refused by IAM propagation, minutes after the binding was created — and the
bucket grant was correct the whole time. I nearly chased the wrong resource on my
own error message.

Now it reads the error and separates the two, because they resolve differently:
impersonation 403 usually fixes itself with time, a bucket 403 never does.

Both credential paths verified green on run 32266891168.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:50Z @neo-opus-grace referenced in commit `8396112` - "fix(gcp): deployment identifiers must not live in a public repo (neomjs/neo#17375)

neomjs/devindex is PUBLIC. neomjs/middleware-v2, where the GCP project id,
project number and bucket name live, is PRIVATE. Hardcoding them here moved them
private -> public, which is the one direction that is never acceptable, and I did
it without checking either repository's visibility first.

Not a vulnerability: nothing here is executable by a stranger. Every mutating
call is authorisation-gated server-side, the script carries no credential, and
Workload Identity's safety rests on an attribute condition only GitHub can
satisfy for this repository — not on any of these values being unknown.

But together they map the whole publish path: project, number, bucket, pool,
provider, and which identity holds which role. That is reconnaissance handed over
for free, and withholding it costs nothing.

Now read from DEVINDEX_GCP_PROJECT_ID / _PROJECT_NUMBER / _BUCKET with `:?`
guards, so the script fails with an instruction rather than a wrong default. Also
removed the real bucket from publishWorkingSet.mjs's example error text.

HEAD is clean. The commits that introduced them are still in history — see the
follow-up note; a rewrite is cheap on an 11 MB repository if we want one.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:03:50Z @neo-opus-grace referenced in commit `15037b3` - "fix(gcp): the explanation was worse than the values (neomjs/neo#17375)

Scrubbing the identifiers left prose that named the private repository, identified
it as where the credentials live, described the deploy identity and its roles, and
laid out the threat model. That is more useful to an attacker than the values
were: it points at the target and explains what protects it.

The public script now says what to export and nothing about why. Rationale lives
with the deployment configuration and in the ticket.

Pool name parameterised too, so no topology is named here at all."
- 2026-08-19T15:06:48Z @neo-opus-grace referenced in commit `0f462a5` - "fix(gcp): identifiers live in repository variables, not exports (neomjs/neo#17375)

Scrubbing them from the public tree was right; making the script demand four
exported env vars was not. That traded a disclosure problem for manual toil and
put the setup back in the state it started in — something nobody remembers how to
run six months later.

Repository variables are the middle ground the situation actually wanted:
invisible to the world, visible to anyone with repository access, versioned
nowhere, and readable by BOTH this script and the workflow without anyone
exporting anything.

Variables rather than secrets, and the reason is decisive rather than stylistic:
secrets are write-only. The script resolves values with `gh variable get`, which a
secret cannot serve, so secrets would reintroduce the exports. Masking also hurts
here — a bucket path shown as *** hides the destination in exactly the log you
read when a publish fails.

The line: secret if something authenticates with it, variable if it merely
identifies something. The App private key is a credential. A project number is
not.

Env overrides still work, so the script runs against another project unedited.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:26:10Z @neo-opus-grace referenced in commit `d6c196d` - "feat(publish): a collapse brake and a dated archive (neomjs/neo#17375)

Leaving git cost ~1,800 restore points and I had not said so. The bucket has
object versioning OFF and soft delete at 7 days, so an overwrite is recoverable
for a week and then not at all. Git's window was unbounded.

Worse, the pipeline reads its own output. A corrupt set is published, the next
run fetches it, the manifest MATCHES because it is exactly what we wrote, and the
damage is adopted as truth and compounds. The manifest proves integrity of
transmission, never correctness of content — a corrupt-but-consistent set passes
every check this pipeline has. Under git an implausible diff at least landed in a
commit someone could read.

Two additions:

- The publish compares the index it is about to write against the one it fetched
  and refuses a drop below 80%. A catastrophe brake, not a quality gate: pruning
  legitimately evicts a tail, and a guard that fires on ordinary churn gets
  disabled. DEVINDEX_ALLOW_INDEX_COLLAPSE=1 is the declared override.

- A dated copy under archive/, once per run. Daily granularity rather than
  hourly, because soft delete already covers today and 24x the snapshots buys
  restore points nobody can distinguish.

Not enabling bucket versioning: that bucket is the shared content plane and the
setting is global, so it would version the whole ~9G dist to protect four
objects.

First full collection run published successfully at 15:23Z — 26.45 MiB, manifest
last. It saved 0 records because neo's hourly pipeline had already refreshed every
candidate today, which is correct behaviour and means that run proved transport
rather than enrichment.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:41:23Z @neo-opus-grace referenced in commit `219441e` - "feat(ci): DevIndex collection leaves the neo pipeline (#17375)

The four collection stages now run in `neomjs/devindex`, which publishes its own
working set as objects. Removing them here is what makes that real rather than
duplicated.

Removed:
- the four stages (optin, optout, spider, updater)
- the Intake App mint and DATA_SYNC_INTAKE_TOKEN plumbing
- buildScripts/dataSyncPreflight.mjs and its spec — it probed devindex-opt-in and
  devindex-opt-out and nothing else, so it was entirely a DevIndex concern
- the `intake` token scope, now unreachable; a declared scope nothing can use is
  a trap for the next stage author
- `preflight_only`, which existed solely to answer the DevIndex intake credential
  question
- the users.jsonl copy AND its git add in the pages push — the step that put the
  same 23 MB file into a SECOND repository hourly, and 76% of neomjs/pages blob
  bytes

spec-retired: DataSyncPreflight.spec.mjs covered assertDataSyncAccess, which
probed devindex-opt-in stargazers and devindex-opt-out issues and nothing else.
The module is deleted here because the identity it verified no longer exists in
this repository — not renamed, not folded. The equivalent check now lives in
`neomjs/devindex`'s own workflow as its "Probe intake access" step, which reaches
both repositories with the Intake token before any mutating stage runs. No
behavior lost coverage; the behavior moved repositories.

Deliberately kept:
- `publishGeneratedProgressOnFailure` and the deferral machinery. It still has a
  real user in `GitHub Workflow corpus`, so it is not DevIndex-only dead code.
- the apps/devindex/resources/data allowlist entries. The app itself stays for
  now; with no producer they are inert, and narrowing the commit guard early buys
  nothing.

Tests updated rather than deleted where the property survived. The deferral block
shrank from eight tests to two and was repointed at `GitHub Workflow corpus`: six
proved properties of an intake identity this repository no longer holds, while
the mechanism they guard is still live. Credential-scoping and tokenScope tests
dropped the intake scope and keep asserting the same isolation for the rest.

3,285 passing across the buildScripts and devindex suites.

Ordering note: neo stops refreshing the index at merge, and the app reads the
pages copy until a middleware route serving the devindex bucket lands. Data
freezes at its current state in the interim rather than breaking.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-19T15:42:05Z @neo-opus-grace cross-referenced by PR #17391
- 2026-08-19T17:12:59Z @neo-opus-grace referenced in commit `c6b73f0` - "fix(storage): create the data directory, and stop an empty index reading as one (neomjs/neo#17375)

The first scheduled run died in `ensureFiles` with ENOENT on `users.jsonl.tmp` (run 32276538403), before a single collection stage executed. Untracking the working set removed the directory with it: git cannot express an empty one, and `pullDevIndexData` — the only other thing that creates it — deliberately skips on CI. The full run that had verified this path two hours earlier used the previous tree, where seven still-tracked files were holding the directory open, so it was green against the defect.

Both writers in Storage shared a temp-and-rename idiom; they now share one `writeAtomic` that guarantees the directory. The guarantee sits at the write rather than in `initAsync`, because the failure was a writer running where the directory did not exist, and an init hook is something a later caller can bypass.

Repairing that alone would have traded a loud crash for a silent one. `ensureFiles` creates `users.jsonl` as a zero-byte file, `readJson` answers an empty JSONL with `[]`, and `[]` is truthy — so on a fresh checkout whose hydration then failed, `getUsers` returned the empty file and the fail-closed guard never fired, in exactly the case it was written for. A run enriching 200 users would have published an index of 200. Empty now counts as absent; DEVINDEX_ALLOW_EMPTY_INDEX remains the door for a first-ever run.

Five specs, each mutation-checked: removing the mkdir fails three with the production ENOENT signature, and restoring `if (users)` fails the empty-index test and only that one."
- 2026-08-19T17:20:21Z @neo-opus-grace referenced in commit `87fd449` - "docs(devindex): the guides describe the pipeline that ships, not the one that left (neomjs/neo#17375)

Twelve sites still described an hourly cadence, and two of them were doing active harm rather than merely being stale. OptIn.md and OptOut.md told users they could un-star "after an hour or two" — advice calibrated to the old schedule. `collectStargazerOptIns` pages the CURRENT stargazer connection, so at a two-hour cadence a user who follows that timing withdraws the request before any run has seen it. On the opt-out page that is a privacy request failing silently while the page says it succeeded. Both now explain the mechanism, so the next cadence change cannot quietly invalidate the advice.

FAQ.md additionally claimed 800 users per cycle; the ceiling is 200 and GraphQL budget admission usually allows fewer. Stated as the shape rather than a number that will drift.

Storage.md still documented the guard's second assertion — that the curated files stay tracked — which 6c600d8 removed, along with the claim that blocklist.json is protected in git. That was the defect, not the documentation of it: the split was by file size when the question is who writes a file, and OptOut appends to blocklist. The page now says what the total guard does and why.

DataHygiene.md asserted arithmetic the cadence change falsified: the 30-day Penalty Box TTL spanned ~3 ceiling-rate passes hourly, and spans ~1.1-1.4 now. The number is corrected and the tradeoff surfaced; the TTL itself is unchanged, because retention is a privacy policy and not this page's call. Canonical sorting's rationale was git-diff hygiene, which no longer exists now that nothing is committed — it reads as removable while being load-bearing for the manifest digest, so it now says so.

Verified rather than assumed: Manager.mjs runs Cleanup.run() as pre-run hygiene (:64, :111), so the sort does execute in the pipeline despite not being one of the four workflow stages."
- 2026-08-19T17:27:28Z @neo-opus-grace referenced in commit `7a12ff9` - "fix(storage): the unverified-adoption warning called a broken loop a transitional state (neomjs/neo#17375)

`config.publishedWorkingSet.baseUrl` resolves to https://neomjs.com/node_modules/neo.mjs/apps/devindex/resources/data/ — neo's `pages` copy — while `publishWorkingSet.mjs` writes to gs://neomjs-middleware-dist/dist/devindex. The read side and the write side name different artifacts, so the round trip this design is built on does not close: each run adopts neo's copy, publishes its own to a prefix nothing serves, and the next run starts from neo's copy again.

Measured rather than reasoned: the manifest 404s at the read URL, neomjs.com/dist/devindex/ 404s (it proxies to pages), and the bucket answers 403 because it is private and mounted into Cloud Run. The published optin-sync cursor reads 2026-03-29 and optout-sync reads null — they are not stuck, they are being reset every run by a hydration that overwrites them with neo's copy.

The warning said this was 'expected while this repository is not yet the publisher', which is now false in the direction that reassures: this repository IS the publisher. It now names the actual condition, and the fetchManifest docblock says what a null return currently means rather than what it meant during the handover window.

Not fixed here, because it is not a code choice: closing the loop needs the read side pointed at the published set, which means either the drafted middleware route (a middleware-v2 deploy) or a public prefix. Merge order matters — neo's dev still refreshes the pages copy, so neomjs/neo#17391 freezes the index the moment it lands unless the loop closes first."
- 2026-08-19T17:32:40Z @neo-opus-grace referenced in commit `f19b504` - "fix(ci): the preflight consumer must not outlive its producer (#17375)

@neo-opus-ada's RA-1. The preflight-only mode existed to probe the DevIndex intake credential without side effects; this PR removed the workflow input, the env assignment and two of the three reads, and left the third at dataSyncPipeline.mjs:716. `git grep DATA_SYNC_PREFLIGHT_ONLY` at 219441ebe5 returned that line and nothing else in code — no setter, no spec.

It failed closed, so there was no runtime risk. The cost was the comment: it argued carefully that the early return must be TERMINAL rather than merely quiet, which reads to the next maintainer as live machinery whose input was deleted by mistake. Deleted with its rationale, since the hazard it described — an emission loop falling through to the publish path — is unreachable without a preflight mode. Nothing consumed the `preflightOnly` return field.

Ada found one instance; I swept the class rather than patching it. Every `process.env` read in the pipeline scripts, differenced against dev: DATA_SYNC_PREFLIGHT_ONLY is the only surface this PR orphaned. GITHUB_REPOSITORY is runtime-provided and the five WATCHDOG_* options are orphaned on dev too, so they are pre-existing and out of scope.

The direction is the lesson: my census asked who still references a removed thing, and this was a surviving consumer whose producer was removed — the same orphaning, invisible to a grep aimed the other way."
- 2026-08-19T17:44:57Z @neo-opus-grace referenced in commit `c2ce2af` - "ci(devindex): the test suite and the tracking guard get an observer (neomjs/neo#17375)

This repository shipped an outage today that its own suite would have caught, because nothing ran the suite. data-sync-pipeline.yml was the only workflow here and it runs the pipeline, not the tests; 49 specs executed only on whichever maintainer typed the command. check-data-tracking was worse — defined in package.json and wired to nothing, no husky hook and no workflow, while being the single thing keeping the derived working set out of git.

--project=unit excludes the two profiling specs on purpose. They readFileSync the real users.jsonl, which is gitignored and whose postinstall fetch skips under CI, so they cannot pass here without a 10 MB download per run — and they assert wall-clock budgets that a shared contended runner invalidates, which the config's own comment already records as a false regression. They are machine-local profiling tools.

--fail-on-flaky-tests is the half that makes the observer honest. The config sets retries: 2 under CI, and a Playwright retry runs in a FRESH worker, so a cross-file leak always heals and reports as flaky-but-passed. Not hypothetical: the spec added in c6b73f0 leaked through the shared working-set manifest and passed serially while failing under parallel workers. Without the flag CI would have called that green.

Verified by running CI's exact two commands locally: the guard passes, and the project selection is non-vacuous at 47 tests rather than the 0 a mistyped project name would silently report."
- 2026-08-19T17:58:12Z @neo-opus-grace cross-referenced by #17394
- 2026-08-19T20:13:27Z @tobiu referenced in commit `f2c592a` - "feat(ci): DevIndex collection leaves the neo pipeline (#17375) (#17391)

* feat(ci): DevIndex collection leaves the neo pipeline (#17375)

The four collection stages now run in `neomjs/devindex`, which publishes its own
working set as objects. Removing them here is what makes that real rather than
duplicated.

Removed:
- the four stages (optin, optout, spider, updater)
- the Intake App mint and DATA_SYNC_INTAKE_TOKEN plumbing
- buildScripts/dataSyncPreflight.mjs and its spec — it probed devindex-opt-in and
  devindex-opt-out and nothing else, so it was entirely a DevIndex concern
- the `intake` token scope, now unreachable; a declared scope nothing can use is
  a trap for the next stage author
- `preflight_only`, which existed solely to answer the DevIndex intake credential
  question
- the users.jsonl copy AND its git add in the pages push — the step that put the
  same 23 MB file into a SECOND repository hourly, and 76% of neomjs/pages blob
  bytes

spec-retired: DataSyncPreflight.spec.mjs covered assertDataSyncAccess, which
probed devindex-opt-in stargazers and devindex-opt-out issues and nothing else.
The module is deleted here because the identity it verified no longer exists in
this repository — not renamed, not folded. The equivalent check now lives in
`neomjs/devindex`'s own workflow as its "Probe intake access" step, which reaches
both repositories with the Intake token before any mutating stage runs. No
behavior lost coverage; the behavior moved repositories.

Deliberately kept:
- `publishGeneratedProgressOnFailure` and the deferral machinery. It still has a
  real user in `GitHub Workflow corpus`, so it is not DevIndex-only dead code.
- the apps/devindex/resources/data allowlist entries. The app itself stays for
  now; with no producer they are inert, and narrowing the commit guard early buys
  nothing.

Tests updated rather than deleted where the property survived. The deferral block
shrank from eight tests to two and was repointed at `GitHub Workflow corpus`: six
proved properties of an intake identity this repository no longer holds, while
the mechanism they guard is still live. Credential-scoping and tokenScope tests
dropped the intake scope and keep asserting the same isolation for the rest.

3,285 passing across the buildScripts and devindex suites.

Ordering note: neo stops refreshing the index at merge, and the app reads the
pages copy until a middleware route serving the devindex bucket lands. Data
freezes at its current state in the interim rather than breaking.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* fix(ci): the preflight consumer must not outlive its producer (#17375)

@neo-opus-ada's RA-1. The preflight-only mode existed to probe the DevIndex intake credential without side effects; this PR removed the workflow input, the env assignment and two of the three reads, and left the third at dataSyncPipeline.mjs:716. `git grep DATA_SYNC_PREFLIGHT_ONLY` at 219441ebe5 returned that line and nothing else in code — no setter, no spec.

It failed closed, so there was no runtime risk. The cost was the comment: it argued carefully that the early return must be TERMINAL rather than merely quiet, which reads to the next maintainer as live machinery whose input was deleted by mistake. Deleted with its rationale, since the hazard it described — an emission loop falling through to the publish path — is unreachable without a preflight mode. Nothing consumed the `preflightOnly` return field.

Ada found one instance; I swept the class rather than patching it. Every `process.env` read in the pipeline scripts, differenced against dev: DATA_SYNC_PREFLIGHT_ONLY is the only surface this PR orphaned. GITHUB_REPOSITORY is runtime-provided and the five WATCHDOG_* options are orphaned on dev too, so they are pre-existing and out of scope.

The direction is the lesson: my census asked who still references a removed thing, and this was a surviving consumer whose producer was removed — the same orphaning, invisible to a grep aimed the other way."
- 2026-08-19T20:13:27Z @tobiu closed this issue

