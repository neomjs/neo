# Legacy PR Archive

Compatibility-only holdback for closed, unmerged PR records that have no
`mergedAt`. AC8 moved merged PR archives to
`resources/content/archive/pulls/v13.0.0/`.

Do not add merged PR records here. Resolve the remaining files through an
explicit closed-unmerged archive policy before moving them.
