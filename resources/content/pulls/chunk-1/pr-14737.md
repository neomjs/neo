---
number: 14737
title: >-
  fix(agentos): kindRegistry resolvers guard the closed set against
  prototype-shaped keys (#14728)
author: neo-opus-vega
state: OPEN
createdAt: '2026-07-04T10:04:24Z'
updatedAt: '2026-07-04T10:04:43Z'
closedAt: null
mergedAt: null
head: agent/14728-kindregistry-hasown
base: dev
url: 'https://github.com/neomjs/neo/pull/14737'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #14728

Refs #14560 (parent epic — never a close-target) · Refs #14701 (kindRegistry, merged — the file this hardens) · Refs #14722 / #14726 (sibling resolvers hardened on the same pattern).

Closes the **last** instance of the systemic `MAP[k] || fallback` prototype-leak across the fleet resolver layer: `kindToken` and `kindLabel` returned an inherited `Object.prototype` member for a prototype-shaped key.

Evidence: L2 (unit-tested; 3/3 green at head `1fa0d955f`).

## What it fixes

`kindToken('toString')` / `kindLabel('constructor')` etc. previously resolved to an inherited `Object.prototype` function (truthy → `||` short-circuits), leaking a function into the `--fm-chip` binding and the chip label. Both resolvers now use `Object.hasOwn(MAP, kind)` so a prototype-shaped key degrades to the neutral token (`kindToken`) / its literal string (`kindLabel`) — the same guard `familyToken`, `stateToken`, and `stateLabel` carry.

## Systemic close

The pattern spanned four resolvers; this is the final one:
- `familyToken` — fixed in the FamilyRail branch (#14722)
- `stateToken` + `stateLabel` — fixed in the HealthSwatch branch (#14726, fix-at-source)
- `kindToken` + `kindLabel` — **this PR**

## Test Evidence

`npm run test-unit -- test/playwright/unit/apps/agentos/view/fleet/kindRegistry.spec.mjs` → **3 passed** at head `1fa0d955f`. Prototype-key regression (`toString` / `constructor` / `__proto__`) added to both the `kindToken` (→ `--fm-kind-neutral`) and `kindLabel` (→ literal string) tests, alongside the existing derived-DTO coverage.

## Post-Merge Validation

- [ ] None beyond CI — pure resolver hardening, no render-surface change. The merged EventChip consumer (#14594) already binds `kindToken`/`kindLabel`; known kinds resolve identically, so it is unaffected.

## Deltas from ticket

Narrowed from the filed scope: #14728 originally named all four resolvers (`kindToken`/`kindLabel`/`stateLabel`/`stateToken`). The state-axis pair (`stateToken` + `stateLabel`) was folded into #14726 (HealthSwatch) per @neo-gpt's RC there — fix-at-source in the PR that ships/reuses them — so this PR covers the remaining kindRegistry pair. Net effect is identical (all four guarded); the split just lands each fix in the PR that owns its file.

Authored by Vega (@neo-opus-vega · Claude Opus 4.8 · Claude Code) — origin session 3bc21462.
