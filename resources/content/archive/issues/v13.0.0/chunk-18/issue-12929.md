---
id: 12929
title: Scrolled-state column drop desyncs header toolbar from body (scrollLeft clamps at drag-end)
state: CLOSED
labels:
  - bug
  - ai
  - regression
  - grid
assignees:
  - neo-fable
createdAt: '2026-06-11T23:12:18Z'
updatedAt: '2026-06-12T01:55:00Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12929'
author: neo-fable
commentsCount: 2
parentIssue: 12883
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-12T01:55:00Z'
---
# Scrolled-state column drop desyncs header toolbar from body (scrollLeft clamps at drag-end)

## Context

Operator L4 re-test on dev tip (post `#12908` merge) found devindex "fully broken": cell content rendering under wrong headers (his screenshot: identical dates under four distinct headers; numbers under text headers) and header movements breaking, especially across heterogeneous widths. Headless reproduction tonight isolated the mechanism with a forensic probe.

`Release classification:` ON the board — this IS the v13 gate failure (parent #12883).

## The Problem (reproduced + convicted)

Probe sequence: clean drags audit green; **overdrag + drop in the scrolled state** then breaks the world:

- Post-drop forensics: headers render at HOME positions (`Total@387`, `Commits %@487`, …) while the first-row cells beneath are year-block cells (`y2024`, `y2021`, `y2019` — 80px cells under 90-200px headers). Header row vs body offset by the FULL drop-time scroll (~1878px).
- The desync **self-heals on the next manual scroll** (the `GridHorizontalScrollSync` addon copies scrollbar→header on the scroll event) — which is why post-scroll audits read clean while the post-drop window looks destroyed.
- **Compounding failure**: a second drag started inside the desynced window has the zone's scroll term (fed from the worker pipeline: ~1878) contradicting the toolbar element's actual scroll (0) — the drag-start `itemRects` snapshot is taken in toolbar space, so every delta is off by the scroll amount: switch storms, wrong landings, and `switchItems`' columnPositions x/width writes land wrong — the persistent "columns duplicate / widths broken" corruption the operator accumulated interactively.

## The Mechanism

At drag end, the items' `wrapperStyle` restore removes `position: absolute`. During the reflow there is a frame where the center toolbar has no scrollable overflow → **the browser clamps the toolbar element's native `scrollLeft` to 0**. Nothing re-applies it: the scrollbar element and the body's `--grid-scroll-left` var still hold the drop-time value; the addon only copies on scrollbar `scroll` events, and a same-value `scrollTo` on the scrollbar fires no event. The toolbar is left as the odd surface out.

## The Fix

In `draggable.grid.header.toolbar.SortZone#processDragEnd`, after the restore/update chain settles (both branches — within-region landing AND lock-change re-home): re-apply the worker-side scroll truth to the toolbar element via `Neo.main.DomAccess.scrollTo({direction: 'left', id: owner.id, value: owner.scrollLeft})` when `owner.scrollLeft > 0`. The body var and scrollbar are already correct; the toolbar rejoins them. Idempotent, scoped to the one surface that clamps.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `grid header SortZone#processDragEnd` (drag-end scroll re-sync) | This conviction (probe forensics) + the `#12908` pipeline model (scrollbar = SSOT; toolbar = display copy) | After drop in a scrolled state, the header toolbar's native scrollLeft is re-applied from `owner.scrollLeft` (the worker mirror fed by `grid.ScrollManager`) | `owner.scrollLeft === 0` → no-op (unscrolled drops unaffected); lock-change branch re-applies after `onColumnLockChange` settles | JSDoc on the re-sync block | L3 headless: post-drop forensics show header↔cell alignment WITHOUT any subsequent scroll (new regression leg in `GridColumnOverdragScroll.spec.mjs`); L4: operator re-pass on #12883 |
| Second-drag-after-scrolled-drop math | Zone scroll-term semantics (absolute, seeded from `owner.scrollLeft`) | With the toolbar re-synced, the next drag's itemRects snapshot space and term agree | Covered by the same re-sync; no separate code path | n/a | Probe: post-fix drag-after-scrolled-drop audits clean |

## Acceptance Criteria

- [ ] Post-drop (scrolled state), headers and cells align immediately — no scroll required (probe forensics + the new spec leg).
- [ ] A second drag in that state lands at the pointer-indicated slot with bounded switches.
- [ ] Existing overdrag spec legs stay green.
- [ ] Operator L4 re-pass (parent #12883's human gate).

## Out of Scope

- The deeper architectural question the operator raised (multi-body vs the main-branch single-body model) — if the re-sync + re-pass still leaves width-class failures, that conversation escalates to a Discussion.

## Related

- Parent: #12883 · Pipeline model: `#12908` (`#12906`/`#12907`) · Mid-drag mapping: `#12894`

Live latest-open sweep: checked at 2026-06-11T23:1xZ; no equivalent. A2A in-flight sweep: clean.

Origin Session ID: e605ce21-3668-445c-bc00-45896aa9a092

Retrieval Hint: "scrolled drop header toolbar scrollLeft clamp desync body var drag-end reflow"

## Timeline

- 2026-06-11T23:12:19Z @neo-fable assigned to @neo-fable
- 2026-06-11T23:12:20Z @neo-fable added the `bug` label
- 2026-06-11T23:12:20Z @neo-fable added the `ai` label
- 2026-06-11T23:12:20Z @neo-fable added the `regression` label
- 2026-06-11T23:12:20Z @neo-fable added the `grid` label
- 2026-06-11T23:12:34Z @neo-fable added parent issue #12883
### @neo-fable - 2026-06-11T23:27:13Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ## Investigation checkpoint — two legs fixed, the core duplicator isolated but not yet closed
> 
> Branch `agent/12929-scrolled-drop-resync`, commit `aafa9cc8b` (WIP, no PR yet). Headless forensic probe (uncommitted exploration spec) drives: heterogeneous-width drags → scroll cycles → overdrag + scrolled-state drop → drag-in-the-desync-window → cross-region scrolled drop, with per-step audits (worker order / DOM order / header↔cell alignment+widths / content-dupe scan / `verify_component_consistency` on toolbar AND view / scroll-surface snapshot).
> 
> **Fixed and probe-verified:**
> 1. **Scrolled-drop header desync** (the ticket's headline): drag-end restore clamps the toolbar's native scrollLeft to 0 while scrollbar + body var keep the drop-time value → headers and cells offset by the full scroll until the next manual scroll; a drag started in that window has its term contradict the toolbar (switch storms, wrong landings). Fix: drag-end re-applies `owner.scrollLeft` to the toolbar element. Post-fix: drop-in-scrolled-state aligns immediately; drag-in-window lands clean (probe legs green).
> 2. **Double rebuild on lock change**: `onColumnLockChange` ran the sub-grid layout sync twice (the non-silent `columns.add()` fires the registered `mutate` listener AND an explicit `onColumnsMutate()` call followed). Removed the explicit call; the event contract is documented in-code.
> 
> **THE OPEN CORE DEFECT — region-body DOM duplication:**
> `verify_component_consistency` on `grid.View` catches it red-handed: `items`/`vdom` = `[body-2, body-1, body-3]` while the **DOM holds up to 17 children — `neo-grid-body-3` (locked-end) duplicated ×8-14**, copies tiled at ~120px intervals across the viewport (= the operator's "columns duplicate" screenshot: the same region rendering repeatedly beside itself). Time-of-birth: during the overdrag scroll walk / its drop — and importantly it **pre-existed fix 2** (the double rebuild was partially *masking* it: the second `view.items` pass re-diffed some copies away; single-fire now lets the raw duplication stand, which is louder but honest).
> 
> Working hypothesis: a stale-vnode multi-apply — per scroll beat, `View.syncBodies` (updateDepth 3 + update) races the bodies' own `createViewData` updates; the view's childNodes diff against an outdated vnode re-inserts the trailing `body-3` as an append, once per collision. Same defect family as the cross-toolbar header duplication fixed earlier (`#12892`) — now in the view→body layer.
> 
> **Next steps (in order):** (1) delta-level evidence — enable main-thread delta logging during ONE overdrag beat, capture the insertNode storm's source update; (2) the fix at the collision point (likely vnode-baseline discipline in the view/body update interplay, NOT symptom-side dedup); (3) regression leg in `GridColumnOverdragScroll.spec.mjs` asserting view-consistency through the walk; (4) the cross-region scrolled-drop leg re-verified after the duplicator dies (its residual misalignment is plausibly downstream of the duplicated bodies' layout).
> 
> The exploration spec + full probe outputs are reproducible on the branch; NL's `verify_component_consistency` (added to the fixture today) is the conviction tool.

- 2026-06-11T23:33:44Z @neo-fable-clio cross-referenced by #12930
- 2026-06-11T23:50:07Z @neo-fable-clio cross-referenced by #12931
- 2026-06-12T00:09:29Z @neo-fable cross-referenced by #12932
- 2026-06-12T00:19:39Z @neo-fable-clio cross-referenced by #12933
- 2026-06-12T00:20:10Z @neo-fable-clio cross-referenced by #12934
### @neo-fable - 2026-06-12T00:27:26Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ## Delta-level evidence: the duplication is stale-baseline diffs during walk-beat update races
> 
> Main-thread `logDeltaUpdates` capture during ONE overdrag walk (probe spec, body-filtered harvest; DOM `grid-body` count 3 → 5 in a single walk):
> 
> ```
> update 37  (1):   set        neo-grid-body-3                 ← full-node re-render of the end body
> update 43  (120): insertNode → neo-grid-body-1__row-N idx 22 ← ID-LESS cell inserts, per row
> update 51  (100): insertNode → …__row-N idx 23
> update 63-75:     insertNode → …__row-0 idx 24/25/26 (140-160 deltas per beat)
> update 89  (1):   insertNode → neo-grid-view-1 idx 2         ← ID-LESS body insert: a body-3 COPY born
> update 107 (1):   set        neo-grid-body-3
> ```
> 
> Two readings, one mechanism:
> 
> 1. **Id-less `insertNode` into the view at the body-3 slot (update 89)** — the differ created a NEW node from vdom instead of matching the existing one: its vnode baseline didn't contain what the DOM already had. That's the region-body duplication (layer C) being born — one copy per stale-baseline view update during the walk.
> 2. **Id-less `insertNode` of cells into center rows (updates 43-75)** — new cells created instead of pool recycling during the buffered re-mounts: @neo-fable-clio's two-generation cell doubling (#12930) caught at the moment of birth. Same defect, one layer down.
> 
> **The race**: per walk beat, `View.syncBodies` runs `updateDepth = 3; update()` (throttled 16ms) while each body's own `createViewData` update emits 100-160 deltas — concurrent updates whose vnode baselines go stale against each other. The defect family is the update-collision class, in the grid's view→body→row stack, exercised at walk-beat frequency.
> 
> Cross-posted context: this is the source-level confirmation of #12930's hypothesis (the differ "can't match ids across schemes" because the id-less inserts never carry identity to match) and the reason the operator's no-locks control still reproduces — the racing updates exist regardless of locked regions.
> 
> Next: the fix belongs at the collision point (vnode-baseline discipline / update serialization for the view-body stack during scroll beats), not symptom-side dedup. Deep vdom-core territory — flagging for Grace's morning review alongside my continuation; the probe spec reproduces it deterministically in ~10s.

- 2026-06-12T00:33:30Z @neo-fable-clio cross-referenced by #12883
- 2026-06-12T00:38:24Z @neo-fable-clio cross-referenced by #12936
- 2026-06-12T01:00:47Z @neo-fable cross-referenced by PR #12938
- 2026-06-12T01:04:08Z @neo-gpt cross-referenced by PR #12935
- 2026-06-12T01:07:24Z @neo-fable cross-referenced by #12939
- 2026-06-12T01:55:00Z @tobiu referenced in commit `c3d3720` - "fix: scrolled-drop re-sync, single-fire lock rebuild, stale-echo gate (#12929, #12932) (#12938)

* fix(grid): drag-end scroll re-sync + single-fire column lock rebuild (#12929)

* fix(grid): gate stale out-of-order scroll echoes behind the commanded target (#12932)

* fix(grid): re-id pool-born cells when recycled into the permanent pass (#12930)

* fix(grid): reject all non-command scroll writes mid-drag + gate unit net (#12932)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-12T01:55:00Z @tobiu closed this issue

