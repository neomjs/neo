---
id: 17112
title: 'Batch ingestion persists embeddings only per full slice, so one slow chunk discards completed siblings and the lane recomputes finished work forever'
state: CLOSED
labels:
  - bug
  - ai
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-14T13:06:31Z'
updatedAt: '2026-08-14T14:52:15Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17112'
author: neo-opus-vega
commentsCount: 1
parentIssue: 17072
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-14T14:52:15Z'
---
# Batch ingestion persists embeddings only per full slice, so one slow chunk discards completed siblings and the lane recomputes finished work forever

## Problem

`NEO_KB_EMBEDDING_BATCH_SIZE` (default 5) is the **durable unit** of KB ingestion: `VectorService`/`IngestionService` persist embeddings only after the full slice returns. One slow or failed chunk therefore discards up to four *completed* provider results, and the retry re-submits identical work.

On a constrained CPU-only plane this composes into a zero-progress loop: a slot-legal 9,144-token chunk was measured completing cleanly (~6 min at ~26 tok/s) and its result — plus its completed slice siblings — never persisted, because a caller-side giveup fired first. The engine then recomputes the identical chunks on every sweep: full CPU burn, zero corpus growth. Work the provider finished must never be paid for twice.

Related but distinct: #16972 (timeout retries at identical batch size — convergence), #16853 (abort strand class). This ticket owns **work conservation**.

## Acceptance Criteria

1. No completed embedding is ever re-purchased across any non-crash exit of the embed call (success, yield, failure, timeout): the completed prefix is durably persisted before the error propagates — with the prefix WRITE riding the shared retry budget (a transient storage failure retries the write, never re-enters the provider, and never escapes retry accounting) — and a re-sweep re-submits only chunks without persisted vectors. *(Truth-folded 2026-08-14 from literal per-chunk-before-next-dispatch persistence — the original is preserved in the correction comment; process-crash residue is explicitly out of scope.)*
2. A re-sweep after a mid-slice failure re-submits only chunks without persisted vectors — proven by a production-path spec (real `VectorService` + ingestion collaborators, temp state), not mock seams.
3. The batch retry ladder and dedupe/upsert semantics are regression-covered at the new persistence boundary.
4. `NEO_KB_EMBEDDING_BATCH_SIZE=1` remains a valid deployment-side containment (slice == chunk) and is documented as such at the leaf.

## Evidence class

Live engine task log + tenant-sync state on a constrained CPU-only plane, 2026-08-14 (completed n_tokens=9144 task with zero corpus delta); source-bound to the slice persistence path.

Part of epic #17072.


## Timeline

- 2026-08-14T13:06:32Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-14T13:06:33Z @neo-opus-vega added the `bug` label
- 2026-08-14T13:06:33Z @neo-opus-vega added the `ai` label
- 2026-08-14T13:06:33Z @neo-opus-vega added the `agent-os` label
- 2026-08-14T13:07:32Z @neo-opus-vega added parent issue #17072
- 2026-08-14T13:31:00Z @neo-opus-vega cross-referenced by PR #17120
### @neo-opus-vega - 2026-08-14T13:52:38Z

**AC-1 truth-fold (correction record).** Original AC-1 as filed: *"A successfully embedded chunk is durably persisted (checkpoint-append) before the next chunk dispatches; slice failure semantics preserve prior chunk successes."* Amended to exit-boundary work conservation with the shared-budget write-retry, per the reviewer finding on PR #17120: literal before-next-dispatch persistence would couple VectorService to provider-internal chunking for no additional conservation (every non-crash exit already persists the prefix), while the reviewer's second arm — the prefix write escaping retry accounting — was real and is now fixed and falsifier-pinned (upserts=3, provider entries [50, 40]). Body state supersedes; this comment preserves the original.

— Vega (Claude Fable 5, Claude Code) 🌿

- 2026-08-14T13:53:06Z @neo-opus-vega referenced in commit `7ba0c14` - "fix(ai): carried-prefix writes ride the shared retry budget (#17112)"
- 2026-08-14T13:56:48Z @neo-opus-vega referenced in commit `2ca2eef` - "chore(ai): classify the carried-prefix write-retry in the retry-bound registry (#17112)"
- 2026-08-14T14:17:19Z @neo-opus-vega referenced in commit `244a912` - "fix(ai): yield-arm carried prefixes ride the same shared write-retry contract (#17112)"
- 2026-08-14T14:19:22Z @neo-opus-vega referenced in commit `9e43fa3` - "chore(ai): re-anchor the retry-bound registry keys after the carry-helper extraction (#17112)"
- 2026-08-14T14:27:12Z @neo-opus-vega referenced in commit `2f5d675` - "test(ai): AC-2 re-sweep witness rides the deployed embed() selector, not a test-authored filter (#17112)"
- 2026-08-14T14:52:15Z @tobiu closed this issue
- 2026-08-14T14:52:15Z @tobiu referenced in commit `3b41239` - "fix(ai): carry and persist completed embedding prefixes across batch failures (#17112) (#17120)

* fix(ai): carry and persist completed embedding prefixes across batch failures (#17112)

* fix(ai): carried-prefix writes ride the shared retry budget (#17112)

* chore(ai): classify the carried-prefix write-retry in the retry-bound registry (#17112)

* fix(ai): yield-arm carried prefixes ride the same shared write-retry contract (#17112)

* chore(ai): re-anchor the retry-bound registry keys after the carry-helper extraction (#17112)

* test(ai): AC-2 re-sweep witness rides the deployed embed() selector, not a test-authored filter (#17112)"
- 2026-08-14T17:25:47Z @neo-opus-vega cross-referenced by PR #17133

