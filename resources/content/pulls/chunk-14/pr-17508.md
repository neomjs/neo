---
number: 17508
title: 'docs: the concision bound loads with the license it bounds (#16528)'
author: neo-opus-vega
state: OPEN
createdAt: '2026-08-21T21:20:07Z'
updatedAt: '2026-08-21T21:23:33Z'
closedAt: null
mergedAt: null
head: vega/16528-concision-bound
base: dev
url: 'https://github.com/neomjs/neo/pull/17508'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16528

🌿 *The license and its bound now load together — "read as permission to expand" is unsayable.*

`§pr_diff_equals_pr_body` was turn-loaded while every terseness guidance was read-once, so the license won every turn, in every family (mechanism + specimens: [ticket comment](https://github.com/neomjs/neo/issues/16528#issuecomment-5375227797)). The bound now lives in the same line; the three payloads swap their expansion-licensing words for pointers to it.

Evidence: L1 (static substrate edits; load path verified — `.claude/CLAUDE.md` is a symlink to `AGENTS.md`, so the rewrite reaches every Claude-seat turn) → L1 required (discipline codification, no runtime ACs). No residuals.

## Slot rationale (§1.1)

- `AGENTS.md §pr_diff_equals_pr_body` — disposition `rewrite` (ADR-0007): density up, placement unchanged, L1 firewall shape kept (named prior + override, per the Atlas warning anchored on PR #11534). File lands at **24,521 of the 24,576 hard cap** — 55B headroom, and the first draft sat at exactly 24,576 before its own rule trimmed it.
- `AGENTS_ATLAS.md §pr_diff_equals_pr_body_anchor` — `rewrite`: consistency with the new Map text; carries the mechanism (rot pairs, operator flags, the −59% specimen); net −2 lines.
- Three payloads — `rewrite`, replacing not adding (AC-1): the pull-request `--body` example (−27B), the pr-review `[CONTENT_COMPLETENESS]` rubric (duplication now caps the score like absence — reviewers stop rewarding volume), the ticket-create Fat-Ticket intro (world atlases forbidden alongside skeletons).

## Deltas from ticket

AC-1's codification point moved up to the turn-loaded tier per operator direction (2026-08-21): a read-once rule structurally loses to a turn-loaded license. The payload lines point at the section instead of restating it — practicing the rule they ship.

## Test Evidence

- `lint-skill-manifest --base origin/dev` OK — after rejecting my first draft at +274 > 250 net, which is AC-2 ("if the rule text is long, it failed its own bar") enforcing itself mechanically.
- `lint-guides` OK · `lint-tree-json` OK (194 nodes) · `wc -c AGENTS.md` = 24,521 ≤ 24,576.
- Calibration (AC-4): PR #17506's compressed body (8,635 → 3,570 bytes, zero information loss), @neo-opus-ada's #17239 handoff message, @neo-gpt-emmy's D#17489 fold comment — dense artifacts that carried full review weight.

## Post-Merge Validation

None owed — discipline codification with no runtime effects; efficacy observation is the MX loop's ambient job, and the duplication-guard successor (mechanical check) is scoped on the ticket as a follow-up, not this PR.

Authored by Vega (Claude Fable 5, Claude Code). Session 93ad792e-6f69-4c46-9207-d31deb113846.
