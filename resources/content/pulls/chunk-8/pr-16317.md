---
number: 16317
title: 'feat(agentos): keep detached mailbox live (#15650)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-02T00:12:09Z'
updatedAt: '2026-08-02T00:13:20Z'
closedAt: null
mergedAt: null
head: codex/15650-n-window-mailbox-beat
base: dev
url: 'https://github.com/neomjs/neo/pull/16317'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Refs #15650

Keeps the gesture-torn operator mailbox live by resolving its owner-held vessel handle for identity and inbox refreshes, then adds a deterministic headed Neural Link runner for the full three-window beat: detail vessel, raw InteractionService mailbox tear-out born before mouse-up, concurrent refresh of all three surfaces, and same-instance reintegration.

Evidence: L3 (headed Chromium on macOS proved the raw gesture, three live updating windows, and both same-instance returns) → L3 required (recorded footage plus decoded first/mid/final frame verification). Residual: AC1 and AC4 [#15650].

## Deltas from ticket

- The headed witness exposed a narrow live-state defect: `getReference('operator-mailbox')` cannot reach a pane after it moves into a vessel. `getOperatorMailboxPane()` now follows the existing detached-detail owner-handle pattern.
- The gesture decision is raw `simulateEvent` over the production InteractionService path; no cockpit executor or new pop-out machinery was added.
- This remains a draft until the production capture and decoded-frame handoff land.

## Test Evidence

- Fleet cockpit owner boundary: `node node_modules/@playwright/test/cli.js test apps/agentos/view/fleet/fleetCockpit.spec.mjs -c test/playwright/playwright.config.unit.mjs --workers=1` — 80 passed.
- Fleet cockpit N-window journey: `NEO_E2E_PORT=8137 node node_modules/@playwright/test/cli.js test agentos/FleetCockpitNWindowNL -c test/playwright/playwright.config.e2e.mjs --workers=1 --headed` — 1 passed.
- Agent gates: `npm run agent-preflight -- --change-class capability ...` — passed.

## Post-Merge Validation

- [ ] Run the headed witness with `NEO_FILM_HOLD_MS` pacing and capture the production N-window beat.
- [ ] Decode and verify the first, mid-gesture, and final frames before handoff.
- [ ] Attach the accepted film evidence to #15650 and promote this draft with an honest close target.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session 019fb600-58b9-7fa2-86a7-5a15e1ccf659.
