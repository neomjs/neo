---
id: 16699
title: 'Cockpit fleet-connection UX: reconnect affordance + shell-aware offline guidance + auto-recheck'
state: OPEN
labels:
  - enhancement
  - ai
assignees:
  - neo-fable-clio
createdAt: '2026-08-08T13:41:42Z'
updatedAt: '2026-08-08T15:30:50Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16699'
author: neo-fable-clio
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Cockpit fleet-connection UX: reconnect affordance + shell-aware offline guidance + auto-recheck

**Provenance: operator UX request during the first live harness iteration loop (2026-08-08):** *"UX: how about a connect or reconnect button inside the harness?"* — asked after the second consecutive run landed on the offline banner for a *different* root cause than the first. Both causes were correct fail-closed behavior; the product gave the operator no way to see which case they were in or to recover without restarting processes.

## Context

The day's live-run sequence (receipts on #16693/#16694/PR #16696) produced three distinct offline-banner causes in three runs: (1) bearer non-coordination (fixed by #16694's self-supplied transport), (2) the UI-mode capability gate resolving null (same fix), (3) a **stale manually-started transport squatting `:8083`** — the shell's foreign-listener branch correctly refused adoption, but the banner still said *"start it: npm run ai:fleet-server"*, which in the shell topology is now actively wrong advice (the shell self-supplies; a manual start CAUSES the foreign-listener case).

## The Problem

Three UX gaps, one surface:

1. **No recovery affordance.** The offline state is computed at load; when the transport becomes reachable later (the #16694 spawn completing after first paint, a freed port, a restarted server), nothing re-queries. The only operator recovery is restarting the app.
2. **The banner's guidance is topology-blind.** "start it: npm run ai:fleet-server" is right for the plain-browser flow and wrong inside the shell — there it should distinguish: *transport starting* (spawn in flight) vs *another fleet server holds the port* (foreign listener, with the probe's named refusal) vs *transport stopped*.
3. **No auto-recheck on late readiness.** In the shell, main KNOWS the moment `bootUiFleetTransport()` resolves `up: true` — the renderer never hears about it.

## The Architectural Reality

- The offline state + banner live in the `apps/agentos` fleet views (the fail-closed routing matrices from the W2 arc; the banner is #15284's operable-cold surface).
- The shell↔renderer channel exists: `preload.cjs` exposes IPC (`fleet-request`); a `fleet-ready` push (main → renderer) is the same trusted-sender pattern in the other direction, consumed via the preload allowlist. Browser flow has no such signal — the button covers it there.
- `bootUiFleetTransport()` (PR #16696) returns `{mode: 'reuse'|'spawn'|'foreign-listener', up}` — exactly the states the banner should render.

## The Fix

One leaf, three parts:

1. **Reconnect/Connect button on the offline banner** (Body, `apps/agentos`): re-runs the fleet load path (roster + activity + sources) through the existing bridge — works identically in browser and shell; no page reload.
2. **Topology-aware banner text**: the shell hands the renderer its transport state (`spawn`/`reuse`/`foreign-listener` + the named refusal) via a preload-exposed fact; the banner renders the honest case — including "another fleet server holds :8083 — quit it, then Reconnect" for the foreign case. Browser flow keeps the current text.
3. **Auto-recheck in the shell**: main pushes `fleet-ready` when the UI transport boot resolves up; the renderer runs the same re-load the button triggers. The button remains as the manual fallback and the browser-flow affordance.

## Acceptance Criteria

- [ ] Offline banner carries a Reconnect control; clicking it with a now-reachable transport transitions the cockpit to live WITHOUT app restart (browser AND shell).
- [ ] Shell foreign-listener case renders the named refusal + correct guidance (never "npm run ai:fleet-server").
- [ ] Shell spawn case: cockpit transitions to live automatically when the transport boot resolves (no click needed).
- [ ] Fail-closed preserved: the button triggers reads through the existing authenticated bridge only — no new transport, no credential surface.
- [ ] Unit coverage for the banner-state routing; smoke stays green.

## Out of Scope

- #16693 (brain-leg attach re-triage) and #16694 (self-supply — merged prerequisite).
- Stale-transport auto-reaping in product mode (the smoke-mode sweep's product sibling — worth its own ticket if the foreign-listener case recurs; adopting/killing foreign processes is deliberately NOT this leaf).
- Any polling loop in the browser flow (the button is the browser affordance; polling is a separate decision).

## Avoided Traps

- **Auto-adopting a foreign listener** — the same-bearer-same-viewer proof is the security floor; the UX names the case instead of weakening the gate.
- **Weakening the capability gate to "retry until reachable"** — the gate stays; the RENDERER re-queries, the gate keeps deciding.
- **Shell-only button** — the browser flow has the identical stale-offline problem after a late server start; one control serves both.

## Related

- #14560 (cockpit epic — this is a Lane B/E leaf) · #16694 / PR #16696 (the transport self-supply this builds on) · #16693 (sibling) · #15284 (the operable-cold banner this extends).

Creation-gate record: live latest-8 sweep 2026-08-08T13:40Z — no equivalent; #16694 explicitly excludes UX affordances. Open for claim (Body-side UI + a small preload fact; good first cockpit leaf for a fresh seat — I hold author context on the transport half if pairing helps).

Origin Session ID: 3f80bf5c-66d1-4bc8-bc5a-4dbe482863c9
Retrieval Hint: "cockpit reconnect button offline banner topology-aware fleet-ready auto-recheck"

## Timeline

- 2026-08-08T13:41:43Z @neo-fable-clio added the `enhancement` label
- 2026-08-08T13:41:43Z @neo-fable-clio added the `ai` label
- 2026-08-08T14:03:09Z @neo-gpt-emmy cross-referenced by PR #16696
- 2026-08-08T14:38:43Z @neo-fable-clio cross-referenced by #16708
- 2026-08-08T15:24:40Z @neo-fable-clio cross-referenced by PR #16711
### @neo-fable-clio - 2026-08-08T15:25:11Z

**Claimed + upgraded: UX-affordance → functional defect, with the decisive receipt.**

The PR #16711 review's smoke run produced the datum three operator runs couldn't isolate: **`brainUp: true` — the isolated organism's fleet transport was READY and awaited BEFORE window observation — and the verdict still records `rosterState: 'sample'` / `rosterLabel: 'static roster · offline'`.** Not a race, not bearers, not the transport: the cockpit renderer never issues/binds its live loads in the shell topology, even against a ready owned transport with a correct boot receipt feeding the capability gate.

Consequence for this ticket's shape: part 3 (auto-recheck) is not sufficient — there is nothing to re-check if the initial load path never fires. The implementation starts with root-causing the shell-mode load path (does `loadRoster` issue a wire call at all? the smoke's `fleetMethods` observable will answer it in one run), then lands the three parts as scoped. The operator's three-run day (bearer mismatch → stale squatter → this) ends here.

Also folding in the PR #16711 review's non-blocking observability note: the plane-attach FAILURE path never logs its selected plan — the banner/log truth work in part 2 should carry it.

— Clio (@neo-fable-clio, Claude Fable 5, Claude Code) 📜 · session 32bcb092-f1c7-4856-9141-3e602ea88ed8

### @neo-fable-clio - 2026-08-08T15:30:50Z

## Session handover — claimed lane, implementation-ready (sunset 2026-08-08, session 959222e2 family)

**Ownership:** @neo-fable-clio (claimed today, upgraded to functional defect). Successor session picks this up FIRST.

**The decisive receipt (do not re-derive):** the PR #16711 review's `smoke:brain` run at `54b5cfefb0` — `brainUp: true` (isolated transport READY + awaited *before* window observation) and the verdict still records `rosterState: 'sample'` / `rosterLabel: 'static roster · offline'`. Three operator live-runs peeled bearer-mismatch → stale squatter → this. **The cockpit renderer never goes live in the shell topology even against a ready owned transport.** Not a race; the capability gate awaits the boot promise correctly (verified in source during the PR #16696 cycle).

**Pickup protocol (first probe, one run):** re-run `NEO_HARNESS_SMOKE=1 NEO_HARNESS_BRAIN=1` (i.e. `npm --prefix harness run smoke:brain`) and capture the FULL verdict (do not pipe through `tail` — my truncation lost it): the `fleetMethods` array (populated by `fleetCapability`'s `onAdmitted` in diagnostic mode) answers the fork in one datum — **empty** = the renderer never issues a single fleet request in shell mode (hunt the load trigger: `apps/agentos/view/ViewportController.mjs` `loadRoster`/`loadActivity` orchestration — when does it fire relative to app:// boot, and does the shell branch's `installFleetBridge` land before it?); **populated** = requests are admitted and the pane discards results (hunt the routing matrix binding in the fleet views). Announce headed runs to the operator first — his desktop hosted three surprise windows today.

**Then land the three scoped parts** (body above) + the two folded notes: the plane-attach failure path should log its selected plan; the banner must never say "start it: npm run ai:fleet-server" in the shell.

**Adjacent state the successor needs:** #16694 (umbrella) stays OPEN and its renderer live-roster receipt is functionally gated on THIS fix — the operator cannot produce it while the renderer never binds. PR #16711 (config-declared plane classifier) is MERGED — `start:brain` is plane-safe; the operator's `NEO_FLEET_PLANE_BASE`/`BEARER` producer section exists in the local-agent-os runbook.

— Clio (@neo-fable-clio, Claude Fable 5, Claude Code) 📜


