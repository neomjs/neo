---
id: 17209
title: Four FM side panels render skinless and the pinned-drawer shell has no layout contract — the cockpit's first-run face is a text dump
state: CLOSED
labels:
  - bug
  - design
  - ai
assignees:
  - neo-fable-clio
createdAt: '2026-08-15T20:23:10Z'
updatedAt: '2026-08-16T19:17:28Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17209'
author: neo-fable-clio
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-16T19:17:28Z'
---
# Four FM side panels render skinless and the pinned-drawer shell has no layout contract — the cockpit's first-run face is a text dump

## Context

Operator escalation with screenshots, 2026-08-15 evening: opening the AgentOS cockpit's side panels (Memories, Catch up, Wake routes, Operator) renders unstyled text dumps — raw headings, hundreds of pixels of dead vertical gap, overlapping/clipped row text ("whole fleet Ada Grace VegaMnemosyne Cli…" squeezed into one broken line), form controls floating in black. His words: "should it look like THAT?!? Now my eyes hurt." He is right, and this is the FIRST-RUN face: the panels are most likely to be opened while exploring, exactly when nothing else is running.

## The census (view components vs skin files, measured)

`apps/agentos/view/fleet/` vs `resources/scss/src/apps/agentos/fleet/`:

- **No SCSS at all:** `MailboxPane.mjs`, `MemoriesPane.mjs`, `WakeRoutePane.mjs`, `OperatorMailbox.mjs` — four pane components ship structural markup only.
- **SCSS exists but renders defective:** `CatchUpPane.scss` (the one skinned pane still shows agent-scope rows overlapping/clipping); `AddAgentForm.scss` (form chips render raw, giant gaps around the field groups).
- **The common denominator across ALL panels (including skinned ones): the pinned-drawer SHELL** — the container hosting Agent detail / Perspectives / Add agent / Catch up / Memories / Wake routes / Operator — has no layout contract of its own: no header treatment, no section rhythm, no gap discipline. Even a skinned child renders inside an unstyled well, which is why every panel shows the same dead-space signature.

## Acceptance Criteria

- [ ] The pinned-drawer shell gets its own skin contract: header row (title + pin affordance), content well with a defined vertical rhythm, and scroll behavior — so every hosted pane inherits a sane frame.
- [ ] The four skinless panes (Mailbox, Memories, Wake routes, Operator mailbox) receive SCSS skins consistent with the existing fleet look (AgentCard/FleetGrid/ActivityStream are the reference idiom — SCSS token/skin layers only, zero CSS-in-JS).
- [ ] CatchUpPane's agent-scope row no longer overlaps/clips (the row is a wrapping chip set, not a single squeezed line).
- [ ] AddAgentForm's field groups and harness chips sit in the drawer's rhythm (no unexplained multi-hundred-px gaps).
- [ ] Empty/awaiting states inside panels ("Pick an agent…", "Catch-up source unavailable.", "Mailbox feed not wired") render as designed quiet-state rows, not bare paragraphs.
- [ ] Verified in the offline topology (static server, no fleet server) — the degraded view IS the first-run view and is the acceptance surface.

## Out of Scope

The per-card "Roster not nominal · malformed source fact" alarm on the static roster (sibling ticket — that is a data/validation defect plus a degraded-state design question, different surface). C3 render-state semantics (#16744) — this ticket is skin and layout, not state vocabulary.

## Related

#13015 (FM umbrella) · #16744 (C3 render states) · the C2 leg-2 pane work consumes the same drawer surface.

Live latest-open sweep: latest 8 checked 2026-08-15T20:1xZ (nearest: #17207/#17204/#17203 — process/test surfaces, no overlap); no equivalent. A2A claim sweep: current-hour window clean.

Origin: operator escalation (screenshots on session dd4568bd) · Retrieval Hint: "FM side panel skins pinned drawer shell text dump catch up memories wake routes operator"

## Timeline

- 2026-08-15T20:23:10Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-08-15T20:23:11Z @neo-fable-clio added the `bug` label
- 2026-08-15T20:23:11Z @neo-fable-clio added the `design` label
- 2026-08-15T20:23:11Z @neo-fable-clio added the `ai` label
- 2026-08-15T20:23:47Z @neo-fable-clio cross-referenced by #17210
- 2026-08-15T20:31:19Z @neo-fable-clio cross-referenced by #17211
- 2026-08-15T20:31:35Z @neo-fable-clio cross-referenced by #17212
### @neo-fable-clio - 2026-08-15T21:27:05Z

## Pinned-drawer shell contract (posted before any skin lands, per the seam promise to @neo-opus-vega)

**Mechanism, verified first (the operator's path insight, confirmed against source + build artifact):** the theme loader strips the `view` segment from the className before resolving the skin path — `src/worker/App.mjs:497` (`className[0] === 'view' && className.shift()`), so `AgentOS.view.fleet.MailboxPane` resolves to map key `apps.agentos.fleet.MailboxPane` → file `resources/scss/src/apps/agentos/fleet/MailboxPane.scss`. Loading is gated on `resources/theme-map.json` (`cssMap.fileInfo`, App.mjs:513): a file absent from the map is never fetched. The four skinless panes have **no SCSS file at all** → no map entry → the loader cannot load what does not exist. Root cause is absence, not misplacement — the 17 existing `fleet/` skins are correctly placed and correctly registered (map inspected).

**Ownership:** the shell is dock chrome (`src/dashboard` — `neo-dashboard-dock-edge-rail`, `neo-dashboard-dock-rail-tab`, `neo-dashboard-dock-split-*`, tab-zone containers), styled **app-locally under the `.fm-fleet-cockpit` scope** — zero engine SCSS edits (precedent already in `FleetCockpit.scss`, which skins `.neo-dashboard-dock-rail-tab` and `.neo-tab-body-container` under that scope).

**The contract — what every hosted pane may assume, and what it must not do:**

1. **Frame:** the shell owns the content well — outer padding and the `--fm-panel` surface. Pane roots render `background: transparent` (the `AgentDetail.scss` precedent: inner `.neo-container` transparency so no stock-dark boxes bleed through).
2. **Header row:** shell-owned — title + pin affordance live in dock chrome. Panes never render a top-level title duplicating the dock tab.
3. **Vertical rhythm:** shell sets the section gap; separators are `--fm-line-soft`. Panes define **no outer margins** — gap discipline belongs to the container, which is exactly what makes the current "hundreds of px of dead gap" signature a shell defect, not four separate pane defects.
4. **Scroll:** the pane body scrolls (`overflow-y: auto`); the header row does not. No fixed heights inside panes.
5. **Quiet states:** `.fm-pane-placeholder` and per-pane empty states follow the `.fm-detail-empty` idiom — centered, small, `--fm-ink-dim`, never a bare paragraph. The offline topology renders these by design (AC-6: degraded IS first-run).
6. **Tokens only:** colors exclusively from the `--fm-*` vocabulary (`apps/agentos/TOKENS.md`); `--fm-ink-faint` never on text (`check-agentos-theme` check 4 enforces mechanically).
7. **Splitter visibility:** `.neo-dashboard-dock-split-*` gains a visible affordance (`--fm-line` at rest, `--fm-signal` on hover/drag) — the operator-reported invisible splitter between cards and timeline.

**Files this lane lands:** `MailboxPane.scss` · `MemoriesPane.scss` · `WakeRoutePane.scss` · `OperatorMailbox.scss` (new, in `resources/scss/src/apps/agentos/fleet/`) + `CatchUpPane.scss` (agent-scope row becomes a wrapping chip set) + `AddAgentForm.scss` (field groups into drawer rhythm) + `FleetCockpit.scss` (shell zones 1–4, 7). Each addition rides with a regenerated `theme-map.json` (`npm run build-themes`) — the map is part of the diff, not a follow-up.

**Seam note (Vega / the perspectives leaf):** a future pane inheriting this frame gets items 1–5 for free; the contract binds the drawer, not the pane. Anything a pane needs beyond it is that pane's own skin file at the resolved path above.

**Out of this lane, sibling-ticketed separately:** the map inspection surfaced dead skins elsewhere — `childapps/dockdemo/view/*.scss` is unreachable (map key keeps `view/`, but `appThemeFolder: 'agentos'` makes the loader resolve `apps.agentos.<File>`), and five map-root orphans (`InterventionPanel`, `StrategyPanel`, `StrategyCardPanel`, `DockPreview`, `FleetSettingsPanel`) plus `fleet/Chips.scss` have no corresponding view class. Different defect class (hygiene/dead-weight), different ticket.

- 2026-08-15T21:44:44Z @neo-opus-ada cross-referenced by PR #17213
- 2026-08-15T22:00:01Z @neo-fable-clio cross-referenced by PR #17219
### @neo-fable-clio - 2026-08-15T22:00:19Z

Contract correction (one clause): the shell-contract comment above says "the map is part of the diff, not a follow-up" — in THIS repo `resources/theme-map.json` is gitignored build output (`build-themes` regenerates it; CI produces its own), so there is nothing to commit. The workspace-overlay model the guide describes is where a committed map lives. Everything else in the contract stands as posted; implementation landed as PR #17219.

- 2026-08-15T23:12:34Z @neo-fable-clio cross-referenced by #17222
- 2026-08-15T23:12:52Z @neo-fable-clio cross-referenced by #16322
- 2026-08-15T23:53:25Z @neo-fable-clio referenced in commit `420dbdb` - "refactor(agentos): the reveal slot becomes the one drawer-frame owner (#17209)"
- 2026-08-16T00:00:21Z @neo-kimi-iris cross-referenced by PR #17228
- 2026-08-16T00:42:18Z @neo-fable-clio cross-referenced by #17130
- 2026-08-16T01:19:44Z @neo-fable-clio referenced in commit `1faebb7` - "feat(agentos): FM drawer-shell skin contract + the four missing pane skins (#17209)"
- 2026-08-16T01:19:44Z @neo-fable-clio referenced in commit `6a01a1f` - "refactor(agentos): the reveal slot becomes the one drawer-frame owner (#17209)"
- 2026-08-16T02:29:11Z @neo-fable-clio referenced in commit `7452b11` - "refactor(agentos): the shell-seam census derives from the live dock inventory and fails closed (#17209)"
- 2026-08-16T02:54:59Z @neo-fable-clio referenced in commit `ed9b2a7` - "refactor(agentos): the shell-seam guard closes its last three fail-open shapes (#17209)"
- 2026-08-16T03:27:46Z @neo-fable-clio referenced in commit `cfeb385` - "refactor(agentos): the reveal override must fully neutralize the root itself, and the census reads every JS spelling (#17209)"
- 2026-08-16T19:16:51Z @neo-opus-grace cross-referenced by #17241
- 2026-08-16T19:17:28Z @tobiu referenced in commit `ad3706b` - "feat(agentos): FM drawer-shell skin contract + the four missing pane skins (#17209) (#17219)

* feat(agentos): FM drawer-shell skin contract + the four missing pane skins (#17209)

* refactor(agentos): the reveal slot becomes the one drawer-frame owner (#17209)

* refactor(agentos): the shell-seam census derives from the live dock inventory and fails closed (#17209)

* refactor(agentos): the shell-seam guard closes its last three fail-open shapes (#17209)

* refactor(agentos): the reveal override must fully neutralize the root itself, and the census reads every JS spelling (#17209)"
- 2026-08-16T19:17:28Z @tobiu closed this issue
- 2026-08-16T19:22:15Z @neo-opus-grace cross-referenced by #17242

