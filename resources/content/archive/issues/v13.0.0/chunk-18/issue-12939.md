---
id: 12939
title: Grid view→body update races spawn id-less insertNode duplicates (stale vnode baselines)
state: CLOSED
labels:
  - bug
  - ai
  - regression
  - grid
assignees:
  - neo-fable
createdAt: '2026-06-12T01:07:23Z'
updatedAt: '2026-06-12T05:39:45Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12939'
author: neo-fable
commentsCount: 4
parentIssue: 12883
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-12T05:39:45Z'
---
# Grid view→body update races spawn id-less insertNode duplicates (stale vnode baselines)

## Context

The delta-level source of the v13 gate family's duplication symptoms, captured 2026-06-12 ~00:25 via main-thread `logDeltaUpdates` during a single overdrag walk (full harvest on #12929; reproducible deterministically in ~10s by the probe spec on the `agent/12929-12932-grid-fixes` trail). Splits the FIX lane out of #12929 (whose within-region legs PR #12938 delivers) so the trail survives that close.

`Release classification:` ON the board — v13 gate family (parent #12883).

## The Problem (delta evidence)

During one overdrag walk (DOM `grid-body` count 3 → 5):

```
update 37  (1):   set        neo-grid-body-3                 ← full-node re-render, end body
update 43  (120): insertNode → neo-grid-body-1__row-N idx 22 ← ID-LESS cell inserts, per row
update 51-75:     insertNode → …__row-N idx 23/24/25/26 (100-160 deltas per beat)
update 89  (1):   insertNode → neo-grid-view-1 idx 2         ← ID-LESS body insert: a body-3 COPY born
update 107 (1):   set        neo-grid-body-3
```

An **id-less insertNode** means the differ created a new node from vdom instead of matching an existing one — its vnode baseline did not contain what the DOM already held. Two layers, one mechanism:

- view-level: region-body copies born into `neo-grid-view-1` (up to ×14 observed across a session — the operator's "columns duplicate" screenshot)
- row-level: new cells created instead of pool recycling — the two-generation cell-id doubling (#12930) at the moment of birth

Locked regions are irrelevant: the operator's no-locks control reproduces (recorded on #12930) — it is the multi-body-era view→body→row update stack, exercised at walk-beat frequency.

## The Architectural Reality

Per walk beat: `grid.ScrollManager` ingests the scroll → `View.syncBodies` runs silent per-body sets + `createViewData(true)` then `view.updateDepth = 3; view.update()` (throttled 16ms) — **concurrently**, each body's own update paths fire (buffered re-mounts emitting 100-160 deltas per beat; mid-drag, `switchItems` additionally runs `body.updateDepth = -1; body.update()`). Updates whose vnode baselines go stale against each other produce created-not-matched children. The framework's update-collision machinery (VdomLifecycle merging/queueing) is either bypassed by the silent-set + direct-`createViewData` paths or defeated by the depth-spanning parent update racing child updates.

## The Fix (territory — exact patch from the investigation)

Vnode-baseline discipline / update serialization at the collision point: the view's depth-spanning update and the bodies' own updates must not diff against each other's stale baselines. Candidate shapes (to be validated against the probe): route the per-beat body updates THROUGH the view's single update (true silent aggregation), or gate `syncBodies`' view update on no body-update-in-flight (and vice versa), or fix the baseline capture in the depth-merge path. Symptom-side dedup is explicitly rejected.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Grid view→body per-beat update flow (`View.syncBodies` + body `createViewData` interplay) | This delta harvest + the framework's VdomLifecycle collision contract | One coherent baseline per frame: no id-less insertNode for nodes that exist in the DOM; pool cells recycle, never re-create | Behavior under non-scroll updates (store loads, resize) unchanged — probe forensics cover baseline + drags + scrolls | JSDoc at the touched flow points | L3: the 10s probe — `verify_component_consistency(view)` clean through walk+drop; delta capture shows zero id-less inserts for existing nodes; #12930's repro A/B gestures clean. L4: operator gate session |
| `grid.Row` pool-cell identity (`__cell-N` ↔ field-named) | #12930 (the cell-layer manifestation) | Unchanged by this ticket's fix UNLESS the bisect (#12930, @neo-fable-clio) shows an independent identity seam — the two lanes converge on one patch if not | Cross-validated with her bisect baselines | n/a here | Her #12930 verification matrix |

## Acceptance Criteria

- [ ] One full probe run (drags, scroll cycles, overdrag walk + scrolled drop, drag-in-window): `verify_component_consistency` on the view AND a body sample = zero mismatches at every step.
- [ ] Delta capture during a walk shows no id-less insertNode for already-mounted nodes.
- [ ] #12930's repros A (same-drag reversal) and B (overdrag cells don't animate) re-tested against the fix — closed there if cured, else the residual is characterized.
- [ ] Operator gate session (batched).

## Out of Scope

- The already-fixed legs (PR #12938: drop re-sync, single-fire rebuild, echo gate).
- NL motion-forensics granularity (#12931).

## Related

- Parent: #12883 · Evidence trail: #12929 (delta harvest comment) · Cell layer: #12930 (@neo-fable-clio's bisect — convergence expected) · Fixture: #12936

Live latest-open sweep: checked at 2026-06-12T01:0xZ; #12930 covers the cell-layer manifestation and its bisect — this ticket owns the update-race FIX; no other equivalent. A2A in-flight sweep: the lane split (her bisect / my fix) is on the record both directions.

Origin Session ID: e605ce21-3668-445c-bc00-45896aa9a092

Retrieval Hint: "id-less insertNode stale vnode baseline view syncBodies update race grid duplication fix"

## Timeline

- 2026-06-12T01:07:24Z @neo-fable assigned to @neo-fable
- 2026-06-12T01:07:24Z @neo-fable added the `bug` label
- 2026-06-12T01:07:25Z @neo-fable added the `ai` label
- 2026-06-12T01:07:25Z @neo-fable added the `regression` label
- 2026-06-12T01:07:25Z @neo-fable added the `grid` label
- 2026-06-12T01:07:41Z @neo-fable added parent issue #12883
### @neo-fable - 2026-06-12T01:09:55Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ## Investigation: the collision machinery exists and is comprehensive — the defect is upstream of it
> 
> `VdomLifecycle` read: `updateVdom` runs a full serialization protocol — `mergeIntoParentUpdate` (children catch a departing parent's bus), `isParentUpdating` + `isChildUpdating` (mutual yield), `VDomUpdate.registerInFlightUpdate`, teleportation with disjoint payloads + collision filtering. The bodies' `parentId` IS the view, so the parent/child serialization applies to them. A naive "two updates raced" reading doesn't survive this — the machinery would have merged or yielded.
> 
> **Sharpened hypothesis — the id-scheme flip in SILENT rebuilds:** the grid's per-beat path mutates row vdom **silently** (`createViewData(true)`) — no update cycle, no serialization involvement. If a silent rebuild regenerates a row's `cn` with cells under the OTHER id scheme (pool-ordinal `__cell-N` vs field-named — exactly #12930's two generations, fed by `Row.mjs:225`'s fallback vs `:424`'s in-place rewrite), then the next depth-spanning update diffs fresh vdom (new ids) against the vnode baseline (old ids): old ids → removes, new ids → **id-less inserts**. With beats interleaving, removes can target baselines that already changed — the DOM accumulates both generations while every individual update believes it acted correctly. The view-level body copies are the same flip one level up (body nodes re-entering the view's vdom with changed identity — e.g. the `set neo-grid-body-3` full-node payloads in the harvest).
> 
> **This converges on @neo-fable-clio's #12930 bisect exactly as the ledger predicted**: the deciding question is now precise — *which `createViewData` / `updateMountedAndVisibleColumns` path flips a mounted row's cell-id scheme mid-walk, and what state (pool window position, `cellPoolSize`, region membership) selects the scheme?* Her answer IS the fix point; the patch then makes the scheme STABLE for mounted nodes (an id, once mounted, never changes scheme silently) — restoring the differ's ability to match, which dissolves both layers without touching the update machinery.
> 
> Status: analysis checkpointed; the sharpened bisect question is with Clio; Grace's morning eyes remain welcome on the machinery reading (especially whether `syncVnodeTree` write-back covers silent child-vdom mutations under depth-spanning parent updates — the one residual question this read leaves open).

### @neo-fable - 2026-06-12T01:12:50Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ## Premise correction before the fix: the locked-body "two generations" are BY DESIGN — the anomaly set narrows
> 
> `Row.mjs` createVdom read (the seam itself):
> 
> - **Pass 1** renders pooled cells (`hideMode === 'removeDom' && !locked`, ids `__cell-N`), then **deliberately fills every unused pool slot with a `display:none` placeholder and pushes the full pool** (`Row.mjs:472-482` — "Fill gaps with placeholders to maintain O(1) stability").
> - **Pass 2** renders permanent cells (locked / non-removeDom), field-named ids.
> 
> Consequence: a fully-locked body's rows **always** carry poolSize placeholders + N permanent cells — the locked-start 3→6 and locked-end 1→2 counts from the #12930 evidence are the **designed structure** (display:none, zero-width — matching what I measured during the #12911 verification and originally called benign; that read was right, my later retraction was the error; the center body's 24+10=34 needs a `cellPoolSize` sanity check against the mounted window before it counts as anomalous).
> 
> **The anomaly set narrows to what no design explains:**
> 1. **DOM-side region-body duplication** — view vdom holds 3 bodies, the DOM up to 17 (`body-3` ×14). vdom==DOM consistency does NOT hold at the view level; this is the hard defect.
> 2. **Visible duplicate cell content** (the operator's repeated date columns) — content, not placeholder, duplication.
> 3. The id-less cell inserts at idx 22-26 during the walk need re-reading against pool-window growth (mountedColumns expanding may make first-renders at new indexes LEGITIMATE inserts; the question is whether `vdom.cn` is fully rebuilt or accumulates across silent rebuilds — `vdom.cn.push(...)` at :482 is only safe if cn is reset upstream every pass).
> 
> Next falsifiers (fresh-focus work, mine or @neo-fable-clio's per the convergence): (a) is `vdom.cn` reset before the passes in every createVdom path (accumulation = the doubling); (b) what diffs the VIEW's children such that body-3 inserts id-less while view vdom is stable (the one remaining update-machinery question — `syncVnodeTree` write-back under silent child mutations); (c) `cellPoolSize` vs mounted-window arithmetic during walks.
> 
> Correction loop noted for the record: my benign-spares read → retracted on Clio's evidence → partially reinstated by the source — the design intent lives at `Row.mjs:472` and neither of us had read it when interpreting the counts. The chronicle stays honest by keeping all three states.

- 2026-06-12T01:13:55Z @neo-fable-clio cross-referenced by #12930
- 2026-06-12T01:18:53Z @neo-fable cross-referenced by PR #12938
- 2026-06-12T02:52:09Z @neo-fable cross-referenced by #12946
- 2026-06-12T02:54:42Z @neo-fable-clio cross-referenced by #12947
- 2026-06-12T03:40:44Z @neo-fable-clio cross-referenced by #12954
- 2026-06-12T03:55:02Z @neo-fable-clio cross-referenced by #12955
- 2026-06-12T04:01:11Z @neo-fable cross-referenced by PR #12956
- 2026-06-12T04:15:20Z @neo-fable cross-referenced by #12957
### @neo-fable - 2026-06-12T05:02:21Z

De-boarded from the v13 release (operator wrap decision, 2026-06-12 ~05:00): re-classified `Release classification: 13.1` (locked-columns-complete milestone) + known-issue note in the v13 release notes. Rationale: the class's worst expression (the silent permanent wedge) is fixed + watchdogged on dev (PR #12956 — failures in this family can no longer be silent); the remaining write-back mechanism expresses only on locked grids under drag+update collisions, and devindex ships lock-less in 13.0. Lane stays mine and hot.

### @neo-fable - 2026-06-12T05:04:18Z

Correction — RE-BOARDED (operator: "wrap up means RESOLVE"). The de-board comment above is retracted; this ticket resolves ON the v13 anchor, tonight. Lane executing now: (1) re-run the deterministic duplication probes against current dev tip (`dceab0b17` — which carries the five #12938 fixes + the #12948 net + the #12950 re-plumb + the #12956 reply-rejection fix, any of which may have already killed the residual) on the lockedColumns fixture; (2) if alive: drop-moment delta capture convicts the surviving writer → fix → net; (3) if dead: A/B evidence + a committed stateful regression net (zero duplicate ids, zero id-less insertNode births during locked drag walks) closes it honestly.

- 2026-06-12T05:16:08Z @neo-fable cross-referenced by PR #12962
- 2026-06-12T05:39:45Z @tobiu referenced in commit `82778b0` - "test(grid): locked-DnD duplication net — four stateful oracles per gesture (#12939) (#12962)

The stale-baseline duplication class (duplicate cell ids after cross-region
re-homes, ghost region-body copies per overdrag walk, id-less insertNode births)
is dead on current dev: clean across all seven checkpoints on BOTH the
lockedColumns fixture AND re-locked devindex (the original measurement surface,
where it scored x8-14 per walk pre-fix). Sensitivity A/B-falsified: knocking out
the Pass-2 re-id in grid.Row makes this net count 73 duplicate ids and a fourth
ghost body from the first walk. The net pins the class with per-gesture asserts:
zero duplicate DOM ids, zero id-less inserts at the apply boundary, exactly three
bodies, DOM==vdom child counts on every region body.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-12T05:39:45Z @tobiu closed this issue

