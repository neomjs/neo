---
id: 12401
title: Rename Agent-OS "bridge daemon" → "wake-daemon" to disambiguate from Neural Link's Bridge
state: CLOSED
labels:
  - ai
  - architecture
  - model-experience
assignees:
  - neo-claude-opus
createdAt: '2026-06-03T00:10:48Z'
updatedAt: '2026-06-06T21:50:27Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12401'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[x] 11822 Implement Claude sibling instance wake routing'
blocking: []
closedAt: '2026-06-06T21:50:27Z'
---
# Rename Agent-OS "bridge daemon" → "wake-daemon" to disambiguate from Neural Link's Bridge

## Context

Two unrelated subsystems are both called "Bridge" / "bridge daemon", which recurrently confuses cross-family agents (operator-reported 2026-06-02, esp. `@neo-gpt`):

1. **Neural Link "Bridge"** (`ai/mcp/server/neural-link/Bridge.mjs`, `run-bridge.mjs`) — the WebSocket bridge between the Neural Link MCP server and the live browser Neo app. `Server.mjs` even calls it "Bridge daemon". This name is **semantically correct**: it genuinely bridges agent ↔ running application.
2. **Agent-OS "bridge daemon"** (`ai/daemons/bridge/daemon.mjs`, launched via `ai/scripts/bridge-daemon.mjs`) — the wake-delivery daemon that polls GraphLog and delivers A2A wake digests to local desktop harnesses via osascript/tmux.

## The Problem

Same term, two subsystems → agents mis-attribute behavior, mis-route debugging, and conflate the WebSocket app-bridge with the wake-delivery daemon. The Agent-OS daemon is the **misnomer**: its function is wake delivery, not "bridging" in the NL sense — and its data dir is **already** named `.neo-ai-data/wake-daemon/`, so the naming is internally inconsistent today.

## The Fix

Rename the **Agent-OS** daemon (keep NL's "Bridge", which is correct):
- `ai/daemons/bridge/` → `ai/daemons/wake/`
- `ai/scripts/bridge-daemon.mjs` → `ai/scripts/wake-daemon.mjs`
- `bridge-daemon.pid` / `bridge.log` → `wake-daemon.pid` / `wake-daemon.log`
- Internal identifiers, log prefixes (`[Bridge Daemon]` → `[Wake Daemon]`), comments, and the ~112 code + ~60 doc/skill references.

This makes **name ↔ function ↔ data-dir** consistent (the data dir is already `wake-daemon/`) and frees "Bridge" for NL.

## CRITICAL nuance — two layers; the wire-format one needs a decision

V-B-A shows the rename is NOT purely internal:
1. **Internal naming** (dir, script, PID, log, log-prefixes, comments, docs) — safe to rename freely.
2. **Public wire-format value**: the `manage_wake_subscription` `harnessTarget` enum (`ai/mcp/server/memory-core/openapi.yaml`) includes the literal value `bridge-daemon`, which is **stored in existing wake subscriptions**. Renaming this enum value is a BREAKING change to persisted subscriptions.
3. **Lane/config identifier**: the orchestrator's hardcoded `continuousTasks` array carries a `bridgeDaemon` lane id (per the cloud-deployment lane taxonomy), another non-internal surface.

The ticket must DECIDE between:
- **(A) Decouple** internal name from wire value: rename all internal naming to `wake`/`wake-daemon` but KEEP `harnessTarget: 'bridge-daemon'` (+ the `bridgeDaemon` lane id) as stable wire/config constants (lowest risk; a residual "bridge" token documented as a frozen wire constant).
- **(B) Rename the wire value + lane id too** with a back-compat alias + a migration of existing subscriptions' `harnessTarget`.

Recommend **(A)** unless migration proves cheap — the wire value is invisible to humans, and the confusion lives in the code/doc surface, not the enum string.

## Sequencing

MUST land AFTER #11822 (PR #12399) merges. #12399 actively edits `ai/daemons/bridge/daemon.mjs`; renaming the dir mid-review would churn that PR. Clean post-merge follow-up.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `ai/daemons/bridge/` dir + `bridge-daemon.mjs` script | this ticket | renamed to `wake/` + `wake-daemon.mjs` | atomic rename | runbooks / AGENTS docs | `grep -rn "daemons/bridge"` clean |
| PID/log files | this ticket | `wake-daemon.pid` / `wake-daemon.log` | data dir already `wake-daemon/` | deploy runbook | daemon boots + writes new paths |
| `harnessTarget` enum value | wake-subscription wire contract | DECISION A (keep `bridge-daemon`) or B (alias+migrate) | existing subs keep working | openapi.yaml description | existing subscriptions still deliver post-rename |
| `bridgeDaemon` lane id | orchestrator continuousTasks / lane taxonomy | same A/B decision as the enum | lane keeps scheduling | lane-taxonomy doc | orchestrator still schedules the lane |
| Docs/skills (~60 refs) | this ticket | updated to wake-daemon | n/a | learn/ + .agents/ | grep clean |

## Acceptance Criteria

- [ ] AC1: NL's `Bridge.mjs`/`run-bridge.mjs` are UNTOUCHED (only the Agent-OS daemon is renamed).
- [ ] AC2: `ai/daemons/bridge/` → `ai/daemons/wake/`; `ai/scripts/bridge-daemon.mjs` → `ai/scripts/wake-daemon.mjs`; all imports updated; `grep -rn "daemons/bridge"` is clean.
- [ ] AC3: PID/log renamed to `wake-daemon.*`; the daemon boots, acquires the lock, and delivers a wake end-to-end on the new paths.
- [ ] AC4: The `harnessTarget` wire-value decision (A or B) is made + documented; existing wake subscriptions continue to deliver (if B: migration + back-compat alias proven by test).
- [ ] AC5: The `bridgeDaemon` lane id is handled consistently with AC4's A/B decision; the orchestrator still schedules the lane.
- [ ] AC6: All ~60 doc/skill refs updated; no stale "bridge daemon" for the Agent-OS daemon in learn/ or .agents/.

## Out of Scope
- Renaming NL's Bridge (it is correctly named).
- Any behavioral change to wake delivery (pure rename + wire-value decision).

## Related
- Blocked-by (sequencing): #11822 / PR #12399.
- Related (also touches the bridge-daemon): #12008.
- Origin: operator naming-collision report 2026-06-02; recurring cross-family confusion (esp. `@neo-gpt`).

## Timeline

- 2026-06-03T00:10:48Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-06-03T00:10:49Z @neo-opus-ada added the `ai` label
- 2026-06-03T00:10:49Z @neo-opus-ada added the `architecture` label
- 2026-06-03T00:10:49Z @neo-opus-ada added the `model-experience` label
- 2026-06-03T00:11:39Z @neo-opus-ada marked this issue as being blocked by #11822
- 2026-06-03T00:15:59Z @neo-gpt cross-referenced by PR #12399
- 2026-06-03T02:55:20Z @neo-gpt cross-referenced by #11812
- 2026-06-03T03:49:17Z @neo-opus-ada cross-referenced by #12417
- 2026-06-03T06:07:18Z @neo-gpt cross-referenced by PR #12424
### @neo-opus-ada - 2026-06-03T06:46:58Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ ## Handover (sunset) — updated land-after list for the rename sweep
> 
> Owner: @neo-opus-4-7 (this is my assigned lane; sunsetting a long session — solo-refresh).
> 
> **Sequencing — #12401's rename (daemons/bridge → daemons/wake) must land AFTER every PR that actively edits `daemon.mjs`/`daemon.spec.mjs`, else it churns them:**
> - #12399 — instance-addressable wake routing (MERGED).
> - #12424 — #10422 osascript frontmost guard (APPROVED, awaiting operator merge; edits `daemon.mjs`).
> - #12426 — #12425 deterministic ps stub (APPROVED, awaiting operator merge; edits `daemon.spec.mjs`).
> - Also soft-gated on **#12421** (my open PR adds 5 "bridge daemon" *comment* refs in `BootEnvelopeResolver.mjs`) — the rename sweep should pick those up too.
> 
> So: do this rename only after #12424 + #12426 + #12421 merge, then `grep -rn "daemons/bridge\|bridge[- ]daemon\|Bridge Daemon"` and sweep all refs in one pass. **Decision A** still stands (keep `harnessTarget:'bridge-daemon'` wire value + `bridgeDaemon` lane id frozen — no data migration; only internal naming/dir/docs rename), so the above PRs' references to those wire constants stay valid through the rename.
> 
> Not started (still sequencing-gated). Pickup-ready for whoever holds the lane post-merge.

- 2026-06-06T01:45:29Z @neo-gpt cross-referenced by #12613
- 2026-06-06T18:09:10Z @tobiu unassigned from @neo-opus-ada
- 2026-06-06T18:09:11Z @tobiu assigned to @neo-claude-opus
- 2026-06-06T19:02:04Z @neo-claude-opus referenced in commit `df5332d` - "docs(ai): rename bridge daemon → wake-daemon across docs/skills + regen manifest (#12401)

Completes #12401 AC6. Renamed Agent-OS 'bridge daemon' prose → 'wake daemon' across
15 docs/skills (ADR 0002/0014, wake-substrate guides, structural-pre-flight + ticket-create
+ session-sunset + architecture-pre-flight skills, ArchitectureOverview Structural Inventory).
Stale paths ai/scripts/bridge-daemon.mjs → ai/daemons/wake/daemon.mjs. Historical
misplacement anchors preserved with '(originally ai/scripts/bridge-daemon.mjs)' parenthetical.
Regenerated skills.manifest.json to match the structural-pre-flight SKILL.md frontmatter.

KEPT (Decision A): harnessTarget 'bridge-daemon' wire value + bridgeDaemon lane id (frozen).
Neural Link's 'WebSocket Bridge' refs untouched. resources/content/ archives untouched."
- 2026-06-06T19:03:18Z @neo-claude-opus cross-referenced by PR #12651
- 2026-06-06T19:21:33Z @neo-claude-opus referenced in commit `04f8342` - "docs(ai): rename bridge daemon → wake-daemon across docs/skills + regen manifest (#12401)

Completes #12401 AC6. Renamed Agent-OS 'bridge daemon' prose → 'wake daemon' across
15 docs/skills (ADR 0002/0014, wake-substrate guides, structural-pre-flight + ticket-create
+ session-sunset + architecture-pre-flight skills, ArchitectureOverview Structural Inventory).
Stale paths ai/scripts/bridge-daemon.mjs → ai/daemons/wake/daemon.mjs. Historical misplacement
anchors preserved with '(originally ai/scripts/bridge-daemon.mjs)' parenthetical. Regenerated
skills.manifest.json to match the structural-pre-flight SKILL.md frontmatter.

KEPT (Decision A): harnessTarget 'bridge-daemon' wire value + bridgeDaemon lane id (frozen).
Neural Link's 'WebSocket Bridge' refs untouched. resources/content/ archives untouched.

[skip docs]: structural-pre-flight's downstreamDocsTargets (learn/agentos/ProgressiveDisclosureSkills.md,
learn/guides/fundamentals/CodebaseOverview.md) need no update — the only structural-pre-flight change
is a historical-anchor rename in the frontmatter description (the misplaced file's then-name
bridge-daemon.mjs → current ai/daemons/wake/daemon.mjs), which does not change the skill's
behavior or its catalog entry; neither downstream doc references that anchor."
- 2026-06-06T21:33:29Z @neo-claude-opus referenced in commit `e75e07b` - "refactor(ai): rename residual internal bridge->wakeDaemon watermark labels (#12401)

gpt #12651 review RA1: rename non-frozen internal labels only —
readBridgeWatermark->readWakeDaemonWatermark, bridgeWatermark->wakeDaemonWatermark
(var/result-key/log), and the instanceResolver test title. KEPT FROZEN: the
'bridge-daemon' wire constant (name field + 50+ harnessTarget assertions =
persisted Shape-C wake-subscription routing), NEO_BRIDGE_LAST_SYNC_ID_PATH env,
bridgeLastSyncIdPath leaf, --bridge-state-file CLI. compactGraphLog spec 8/8 green."
- 2026-06-06T21:50:27Z @tobiu referenced in commit `d27db48` - "refactor(ai): rename Agent-OS bridge daemon → wake-daemon (#12401) (#12651)

* refactor(ai): rename Agent-OS bridge daemon → wake-daemon — code + tests (#12401)

Disambiguate the Agent-OS wake-delivery daemon from Neural Link's WebSocket Bridge
(recurring cross-family confusion). Decision A (ticket recommendation): rename internal
naming only — dir ai/daemons/bridge → ai/daemons/wake, log prefixes [Bridge Daemon] →
[Wake Daemon], PID/log → wake-daemon.{pid,log}. The harnessTarget 'bridge-daemon' wire
value + the bridgeDaemon lane id are KEPT as frozen wire/config constants (no migration of
persisted WAKE_SUBSCRIPTION rows). Neural Link's Bridge.mjs/run-bridge.mjs untouched.
Boyscout: cleaned stale ADR/ticket refs in touched-file comments (archaeology hook).

* docs(ai): rename bridge daemon → wake-daemon across docs/skills + regen manifest (#12401)

Completes #12401 AC6. Renamed Agent-OS 'bridge daemon' prose → 'wake daemon' across
15 docs/skills (ADR 0002/0014, wake-substrate guides, structural-pre-flight + ticket-create
+ session-sunset + architecture-pre-flight skills, ArchitectureOverview Structural Inventory).
Stale paths ai/scripts/bridge-daemon.mjs → ai/daemons/wake/daemon.mjs. Historical misplacement
anchors preserved with '(originally ai/scripts/bridge-daemon.mjs)' parenthetical. Regenerated
skills.manifest.json to match the structural-pre-flight SKILL.md frontmatter.

KEPT (Decision A): harnessTarget 'bridge-daemon' wire value + bridgeDaemon lane id (frozen).
Neural Link's 'WebSocket Bridge' refs untouched. resources/content/ archives untouched.

[skip docs]: structural-pre-flight's downstreamDocsTargets (learn/agentos/ProgressiveDisclosureSkills.md,
learn/guides/fundamentals/CodebaseOverview.md) need no update — the only structural-pre-flight change
is a historical-anchor rename in the frontmatter description (the misplaced file's then-name
bridge-daemon.mjs → current ai/daemons/wake/daemon.mjs), which does not change the skill's
behavior or its catalog entry; neither downstream doc references that anchor.

* refactor(ai): rename residual internal bridge->wakeDaemon watermark labels (#12401)

gpt #12651 review RA1: rename non-frozen internal labels only —
readBridgeWatermark->readWakeDaemonWatermark, bridgeWatermark->wakeDaemonWatermark
(var/result-key/log), and the instanceResolver test title. KEPT FROZEN: the
'bridge-daemon' wire constant (name field + 50+ harnessTarget assertions =
persisted Shape-C wake-subscription routing), NEO_BRIDGE_LAST_SYNC_ID_PATH env,
bridgeLastSyncIdPath leaf, --bridge-state-file CLI. compactGraphLog spec 8/8 green.

---------

Co-authored-by: Tobias Uhlig <tobiasuhlig78@gmail.com>"
- 2026-06-06T21:50:27Z @tobiu closed this issue

