---
id: 15727
title: Make packaged-main import guards source-relative
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
  - build
assignees:
  - neo-gpt
createdAt: '2026-07-22T16:19:09Z'
updatedAt: '2026-07-22T18:17:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15727'
author: neo-gpt
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-22T18:17:25Z'
---
# Make packaged-main import guards source-relative

## Context

Exact-head review of PR `#15725` closed the syntax-scanning defect in `#15723` and left the current packaged-main closure green. A Depth Floor probe then tested the future nested-module case instead of assuming the top-level guard generalized.

At `c72d3ddc983d3d87610ed9e10d9f58cdbad9aab2`:

- the recursive local closure rooted at `harness/main.mjs` is currently five files: `main.mjs`, `appLifecycle.mjs`, `brain.mjs`, `contentPolicy.mjs`, and `fleetCapability.mjs`;
- none of those files currently escapes the packaged harness root; and
- a synthetic `harness/tools/x.mjs` containing `import '../../ai/Agent.mjs'` resolves outside `app.asar`, while the current literal-only guard does not reject it.

This is therefore a verified durability gap, not a claim that the approved PR ships a current regression.

## The Problem

The guard in `test/playwright/unit/harness/pack.spec.mjs:22-59` receives source text but no source-file path. It normalizes the literal itself and only rejects the exact `../ai` / `../src` prefix forms. That is correct for top-level files, but relative depth changes the spelling without changing the escape:

```text
harness/tools/x.mjs + ../../ai/Agent.mjs
→ <repo>/ai/Agent.mjs
→ outside the packaged harness module closure
```

The same test reads only `brain.mjs`, `fleetCapability.mjs`, and `main.mjs` into the guard. The current closure also contains `appLifecycle.mjs` and `contentPolicy.mjs`. A later nested local module can therefore create an installed-only `ERR_MODULE_NOT_FOUND` failure while the packaging test remains green.

## The Architectural Reality

ADR 0034 §2.6 requires one source graph with an explicit packaged boundary; it does not authorize static main-process ESM edges to escape `app.asar`.

`harness/pack.mjs:199-209` already provides syntax-accurate literal import extraction and a local-`.mjs` projection. `harness/electron-builder.yml` remains the authority for files shipped inside `app.asar`. The guard should connect those two existing authorities with source-relative resolution.

The runtime `loadFleetRuntimeContracts(organismRoot)` seam is different by design: it loads the staged organism through an explicit runtime root. A static-closure guard must not turn that intentional expression-based loader into a false positive.

## The Fix

Harden the existing packaging-test seam so it is source-aware and closure-complete:

1. Seed traversal at `harness/main.mjs`.
2. Reuse `extractLiteralImportSpecifiers()`; do not add a regex or a second parser.
3. Resolve each relative literal against the importing module's directory.
4. Reject any resolved static/literal-dynamic module target outside the harness root.
5. Recursively visit local `.mjs` dependencies with a visited set and fail loudly on a missing local module.
6. Assert that every discovered packaged-main module is present in the `electron-builder.yml` `files` closure.
7. Preserve expression-based runtime loaders and bare-package imports as separate contracts.

Keep this as packaging enforcement. Do not introduce a production runtime API solely to make the test convenient.

## Contract Ledger Matrix

Not applicable: this hardens an internal build-test invariant and introduces no human-, agent-, or external-system-facing API or configuration surface.

## Decision Record impact

`aligned-with ADR 0034 §2.6` — the ticket makes the existing explicit packaged-source boundary depth-invariant; it does not amend the decision.

## Acceptance Criteria

- [ ] A synthetic nested source at `harness/tools/x.mjs` importing `../../ai/Agent.mjs` fails the packaged-main guard.
- [ ] Equivalent normalized escapes, including `../middle/../../src/Neo.mjs`, fail relative to the importing source path.
- [ ] The real five-file packaged-main closure is traversed and passes.
- [ ] Adding a new local `.mjs` edge under `harness/` automatically brings that module into the checked closure.
- [ ] A missing local `.mjs` target fails loudly instead of silently shrinking the closure.
- [ ] Cyclic local imports terminate through a visited-set witness.
- [ ] Static imports, side-effect imports, re-exports, and literal dynamic imports share the same syntax-accurate source authority.
- [ ] Strings, templates, comments, and expression-based dynamic imports remain inert.
- [ ] The explicit `loadFleetRuntimeContracts(organismRoot)` runtime seam remains allowed.
- [ ] The focused harness packaging suite is green, with a red-before/green-after witness for the nested escape.

## Out of Scope

- Reopening or blocking PR `#15725`; its current closure is green.
- Bundling all of `ai/` or `src/` into `app.asar`.
- Replacing `electron-builder.yml` as the packaged-main manifest authority.
- Reworking renderer content-policy enforcement.
- Resolving arbitrary expression-based dynamic imports.

## Avoided Traps

- **Prefix accumulation:** adding `../../ai`, `../../../ai`, and deeper regex branches would encode directory depth instead of the boundary.
- **Whole-repository import ban:** the staged organism has intentional root-aware runtime loaders; only the packaged static main-module closure is in scope.
- **Second scanner:** the syntax authority repaired by `#15723` remains the one extractor.

## Duplicate Sweep

Immediately before creation at 2026-07-22T16:19:08.684Z, the latest 20 open issues, the last 30 all-state A2A messages, three targeted all-state GitHub issue searches, and raw-memory plus summary retrieval were rechecked. `#15723` is the scanner-authority predecessor and explicitly leaves the local dependency model unchanged; no ticket covers source-relative nested closure enforcement.

## Related

Related: #15723, #15725

Origin Session ID: bb641b19-2dcb-4fd5-bd85-97a17cf162c3

Retrieval Hint: "packaged-main nested harness module source-relative import closure guard"


## Timeline

### @neo-gpt - 2026-07-22T17:15:47Z

## Ticket Intake — `needs-relinking`

**[ARCH_ALIGNMENT]** The source-relative closure goal and proposed test boundary are correct, but the ticket is not claim-ready on current `dev`: its required syntax authority exists only in approved, unmerged PR #15725. This is a merge-order/topology gate, not an architectural objection.

### Live premise and dependency falsifiers

- Current `origin/dev@68facc2d126f51b318f05f485144d814bd8da937` still uses the comment-strip plus `IMPORT_SPECIFIER_RE` scanner in `harness/pack.mjs`; it does **not** contain the exported Acorn-based `extractLiteralImportSpecifiers()` required by this ticket.
- PR #15725 is open at exact head `c72d3ddc983d3d87610ed9e10d9f58cdbad9aab2`, approved, with all required checks green, but `mergedAt` is still null. Its close target #15723 is likewise still open.
- At that exact predecessor head, the ticket's durability gap is real: `assertNoParentRootImports(source)` normalizes a literal without receiving the importing file path, and the closure test reads only `brain.mjs`, `fleetCapability.mjs`, and `main.mjs`. A nested `harness/tools/x.mjs` importing `../../ai/Agent.mjs` therefore resolves outside the harness root without matching the top-level literal-prefix guard.
- `harness/electron-builder.yml` explicitly lists the five packaged-main modules; source-relative traversal from `main.mjs` is the correct way to connect the syntax authority to that manifest authority without touching the expression-based `loadFleetRuntimeContracts(organismRoot)` seam.

### Contract, age, and successor audit

- **Ticket age:** created/updated `2026-07-22T16:19:09Z`.
- **Bot stale-band:** `pre-stale` under the live 90-day stale / +14-day close workflow; neither `stale` nor `no auto close` is present.
- **Short-horizon successor / close-link sweep:** no PR references or closes #15727. #15725 is the required predecessor, not a duplicate or successor; no other current artifact owns nested source-relative packaged-main closure enforcement.
- **Contract Ledger:** N/A is correct — this hardens an internal build-test invariant and adds no consumed public/config/wire surface.
- **ADR successor-risk:** `adr-aligned` — artifact #15727/2026-07-22; ADR 0034/Accepted 2026-07-10; evidence §2.6's single explicit packaged source graph plus current `electron-builder.yml`; route continue **after predecessor authority lands**.
- **ROI:** positive and bounded. Recursive source-relative traversal with a visited set prevents installed-only `ERR_MODULE_NOT_FOUND` regressions without adding runtime code or a second scanner.

### Required topology repair

Please make the merge-order dependency explicit in the ticket body:

1. `Depends on: #15725 merged to dev`;
2. no assignment, branch, or implementation until current `origin/dev` exposes the syntax extractor;
3. after merge, rebase/re-run the nested-escape red witness against the landed extractor before claiming.

**Intake verdict:** `needs-relinking`. The prescription is otherwise coherent; the sole blocker is that its source authority has not landed.

### @neo-gpt - 2026-07-22T17:27:53Z

## Ticket Intake — `valid-as-written` (dependency discharged)

The sole gate in the prior `needs-relinking` intake is now closed.

### Fresh live evidence

- PR #15725 merged to `dev` at `2026-07-22T17:16:24Z` with merge commit `14b64e433d7ba63a5e69e05fcc3f66f8e44bb3ac`; its exact head `c72d3ddc983d3d87610ed9e10d9f58cdbad9aab2` was approved and fully green.
- Current `origin/dev` contains the exported Acorn-backed `extractLiteralImportSpecifiers()` authority in `harness/pack.mjs`.
- The nested-depth falsifier remains real: resolving `../../ai/Agent.mjs` from `harness/tools/x.mjs` escapes the harness root, while a literal-prefix-only check is depth-dependent.
- Issue #15727 is still open, unassigned, and has no delivery PR or competing claim.

### Re-intake verdict

- **Architecture:** source-relative recursive traversal from `harness/main.mjs`, backed by the landed syntax extractor and `electron-builder.yml`, is the correct packaging-test boundary. It must continue to ignore bare imports and expression-based runtime loaders.
- **Contract Ledger:** N/A remains correct; this is an internal build-test invariant.
- **ADR successor-risk:** aligned with ADR 0034 §2.6; no decision amendment.
- **ROI:** positive and bounded — one test seam plus adversarial fixtures, no runtime API and no second parser.

**Intake verdict:** `valid-as-written`. The predecessor authority is landed; the ticket is claim-ready.

- 2026-07-22T17:43:29Z @neo-gpt cross-referenced by PR #15731

