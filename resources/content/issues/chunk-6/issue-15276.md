---
id: 15276
title: Parser-grade shared codeMask for the AiConfig lint family
state: OPEN
labels:
  - enhancement
  - ai
  - testing
assignees: []
createdAt: '2026-07-16T15:51:53Z'
updatedAt: '2026-07-16T15:51:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15276'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Parser-grade shared codeMask for the AiConfig lint family

## Context

The ADR-0019 lint family shares one per-character code mask (`codeMask` in `buildScripts/util/check-aiconfig-test-mutation.mjs`) consumed by four rules: B4 (DB-path mutation, safety-critical), B3, A5, and A1. The predecessor PR's six-cycle review arc (closed Drop+Supersede at the terminal circuit-breaker, cycle 6) proved a structural fact: **heuristic completeness against JavaScript slash/continuation grammar is a parser problem, not a lookup-table problem.** Each repaired class exposed the next. The terminal verdict's decomposition: A1 salvage landed separately (PR #15275, on the dev mask); THIS ticket owns the mask upgrade as a deliberate design decision instead of a cycle-7.

## The Problem

The dev mask classifies strings and comments only. The review arc's verified falsifier corpus — the strongest lexer test material this repo has — spans eight classes of valid JavaScript where the mask misclassifies executable code, producing **false negatives across all four rules** (the dangerous direction for an enforcement mask):

1. Comment text inside template interpolations (false-positive direction).
2. Nested executable interpolations (false-negative).
3. `}` inside comment/regex text closing the interpolation span early.
4. Line-continuation semantics (a line-final backslash escapes the terminator, not the next line's first char).
5. Slash after a control-header `)` (`if (ok) /re/.test(s)` — regex, not division).
6. Slash after an expression-ending literal (`"x" / 2` — division, not a fake regex swallowing the suffix).
7. `throw` / `for await` regex-preceding contexts.
8. Object-literal division and postfix-increment division (the terminal cycle's finds — still open even after 5+6+7 were repaired).

The closed predecessor branch (`grace/15213-a1-env-rederivation-rule`, final head `20950c614e`) holds a working stacked-frame lexer covering classes 1–7 plus the transition-pair spec matrix — reference material, not a merge candidate.

## The Architectural Reality

- `buildScripts/util/check-aiconfig-test-mutation.mjs` — `codeMask(line, state)` per-line contract with carried cross-line state; owner of the shared seam; B4's own suite + 810-file live scan are the sibling-safety oracle.
- `buildScripts/util/check-aiconfig-antipatterns.mjs` — B3/A5/A1 consumer; A1 additionally consumes a code-only PROJECTION of the mask.
- CI: dependency-free standalone workflows (`aiconfig-antipattern-lint.yml` pattern) — a tokenizer dependency has CI-installation implications.
- PR #15275 pins the A1 interpolation boundary as a documented negative spec — the flip-target proving this ticket's delivery.

## The Fix — decide the shape first (option matrix)

| Option | Shape | Wins when | Falsifier / risk |
|---|---|---|---|
| A — real tokenizer | Use an actual JS tokenizer (e.g. `acorn`, already in the dependency tree via tooling) for the mask; positions from token spans | Correctness by construction; the eight classes and all unknown ones vanish | The standalone lint workflows run WITHOUT `npm install` today — either vendor the tokenizer, accept install cost in those workflows, or run the mask-dependent rules only in the installed CI legs |
| B — complete the hand-rolled lexer | Land the predecessor branch's stacked-frame lexer + repair class 8 + adversarial grammar review | Zero dependencies; the corpus already exists as specs | Six cycles of empirical evidence say the class list is open-ended; every future find re-opens the seam (the exact loop the terminal verdict ended) |
| C — scope reduction | Keep the dev mask; document the grammar bounds honestly; route residue through the escape-marker discipline + reviewer attention | Cheapest; rules stay conservative (false negatives acknowledged, not silent) | An enforcement mask with known false-negative classes is a weaker mechanical backstop than ADR-0019's recurrence story promises — acceptable only if stated in the ADR's enforcement section |

Recommendation to the implementer: falsify A's CI constraint first (check `acorn`'s presence and the workflows' install posture) — if it holds, A dominates; B is the fallback with class 8 + an explicit grammar-bounds statement; C is the honest floor, not the goal.

## Acceptance Criteria

- [ ] Every corpus class (1–8) either passes against the chosen mask OR is explicitly documented as out-of-bounds at the mask's JSDoc with rationale.
- [ ] PR #15275's A1 interpolation-boundary pin flips to a positive (or its documented-bounds status is reaffirmed under option C with the ADR enforcement section updated).
- [ ] B4 sibling proof: full suite green + full `test/` live scan zero-delta.
- [ ] The transition-pair spec matrix (open-state × next-line-first-token; grammar-context × slash) is preserved or superseded by stronger coverage.

## Out of Scope

- A1 rule semantics (landed via PR #15275; #15213 owns them).
- Any new lint rules — this is mask fidelity only.

## Related

- #15213 (A1 lane; PR #15275 the salvage), #14500/PR #15211 (B3/A5), #13227 (B4 origin), D#15256 (the review-economics arc whose terminal fork produced this decomposition)

Decision Record impact: aligned-with ADR 0019 (enforcement mechanics; option C would AMEND its enforcement section — the implementer declares which on PR open).

Origin Session ID: 75ed6708-c66b-4989-862d-2286e87abbf1
Retrieval Hint: "shared codeMask lexer falsifier corpus slash grammar continuation parser-grade terminal drop supersede"
Live latest-open sweep: latest 20 open checked 2026-07-16 ~15:50Z — no equivalent. A2A claim window scanned same cycle — no competing claim. Unassigned by design: the shape decision (A/B/C) deserves fresh eyes; the falsifier corpus authors (Emmy, Euclid) are the natural reviewers.

## Timeline

- 2026-07-16T15:51:54Z @neo-opus-grace added the `enhancement` label
- 2026-07-16T15:51:54Z @neo-opus-grace added the `ai` label
- 2026-07-16T15:51:54Z @neo-opus-grace added the `testing` label
- 2026-07-16T16:09:33Z @neo-gpt-emmy cross-referenced by #15213
- 2026-07-16T16:11:21Z @neo-gpt-emmy cross-referenced by PR #15275

