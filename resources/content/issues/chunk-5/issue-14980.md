---
id: 14980
title: Early gestures are deaf to worker-side drag zones + no gesture-time key capture exists
state: OPEN
labels:
  - bug
  - ai
assignees:
  - neo-fable-clio
createdAt: '2026-07-10T18:25:46Z'
updatedAt: '2026-07-11T01:10:36Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14980'
author: neo-fable-clio
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
milestone: v13.2
---
# Early gestures are deaf to worker-side drag zones + no gesture-time key capture exists

## Context

Found while building the binding §06 gesture witness for PR `#14974` (2026-07-10). Two related engine-tier gaps in the drag layer's lifecycle, both bisected against the live dockdemo surface with a scripted-pointer reproducer.

## The Problem

**Gap 1 — the early-gesture deafness race.** A real pointer drag started within ~1s of app mount renders a drag proxy and the `neo-is-dragging` state (main-thread `DragDrop` addon — instant), but **no worker-side sort zone ever hears the gesture**: the zones' delegated `drag:start` / `drag:move` DOM-listener registration round-trip (worker → main) has not landed when the gesture begins. Consequences on the dock surfaces: no `dockCrossZoneDragMove` / `dockCrossZoneDrop` fire, so the drop-indicator menu never lights and the release commits nothing — the drag LOOKS alive and is functionally dead.

Bisected cleanly (fixture probe, identical flow): pointer-down at +0ms after readiness selectors → deaf (subclass `onDragMove` never runs — verified via an instance-field trail read over the Neural Link, since SharedWorker consoles don't bubble to the page); pointer-down at +1000ms → everything fires. The `.neo-draggable` cls is NOT a sufficient readiness witness — it lands before the listener registration completes.

**Gap 2 — no gesture-time key capture.** During a header drag, the base sort zone placeholder-swaps the dragged button out of the DOM; browser focus falls to `<body>`, so element-scoped `keydown` listeners (the only kind a workspace can register) can never receive a real mid-drag Escape. The §06 Escape-cancel seam (PR `#14974`) is therefore drivable and unit/e2e-proven at the seam tier, but a REAL keystroke has no path to it. The same family as the known no-abort-API residual (a cancelled drag's proxy visual rides until release).

## The Architectural Reality

- Main-thread `DragDrop` addon owns proxy/visuals and works from frame one; worker zones subscribe via `addDomListeners` (async round-trip) — the two halves have no shared readiness contract.
- `DockTabSortZone` fires the dock seams from `onDragStart/Move/End` — all downstream of the deaf registration.
- No `drag:cancel` primitive exists anywhere in the stack (grep-verified), and no window-scoped key listener surface is available to worker components during a gesture.

## The Fix

Investigation-first, two candidate shapes:

1. **Readiness**: either the main-thread addon defers gesture-start until the worker's drag listeners for the pressed element's path are registered (a registration-complete handshake), or a queryable/awaitable readiness signal is exposed so surfaces (and specs) can gate honestly. Today's only workaround is a blind settle (documented in `test/playwright/e2e/dashboard/DemoADragMenuNL.spec.mjs`).
2. **Gesture-time input capture**: while a drag session is active, the main-thread drag owner captures `keydown` (at minimum Escape) and routes it into the drag event stream (`drag:cancel` or a keyed field on `drag:move`/`drag:end`) — giving every drop consumer one honest cancel primitive and retiring the per-workspace Escape workarounds.

## Acceptance Criteria

- [ ] A pointer drag started immediately after first paint reaches worker-side zones (or is deferred until it can) — the +0ms reproducer passes without a settle.
- [ ] The blind settle in `DemoADragMenuNL.spec.mjs` is removed in favor of the readiness contract.
- [ ] A real mid-drag Escape reaches a documented cancel primitive; the Demo-A Escape e2e upgrades from seam-driven to real-keystroke.
- [ ] Regression specs at the owning tier for both gaps.

## Out of Scope

- The dock surfaces' consumer logic (PR `#14974`) — correct once events arrive.
- The `#14970` overlay cascade defect (separate, PR `#14975` in flight).

## Decision Record impact

`none` — engine defect investigation; no ADR authority touched.

Related: `#14974` (the witness that found both) · `#13158` · `#14923` (drag-lifecycle destroy family) · `#14970` (sibling finding from the same gate lineage).

Live latest-open sweep: latest 12 checked at 2026-07-10T18:12Z; no equivalent (nearest `#14970` = CSS cascade, disjoint). A2A herd window checked: no competing claim.

Origin Session ID: 183ac080-2f24-4837-8f63-69bfc536af0d
Retrieval Hint: "early gesture drag listener registration race worker deaf escape key capture drag cancel"

## Timeline

- 2026-07-10T18:25:47Z @neo-fable-clio added the `bug` label
- 2026-07-10T18:25:47Z @neo-fable-clio added the `ai` label
- 2026-07-10T18:27:49Z @neo-fable-clio referenced in commit `2dddcd5` - "fix(dashboard): close the four review contracts — shared-ancestor tokens, release-truth drops, complete-menu validation, the binding gesture witness (#14959)

Review cycle 1 (PR #14974), all four Required Actions:

- [P1] motion policy on the SHARED overlay ancestor: the dock-host carries
  .neo-dashboard (custom properties inherit downward only — the projected
  child's scope never reached the overlay SIBLINGS); the indicator family's
  call-site 120ms fallbacks are GONE (token-only, DockFlip authority
  discipline: unscoped hosts land instantly, reduced-motion collapses with
  nothing to override it)
- [P1] release truth, never cached hover truth: onDockCrossZoneDrop
  re-hit-tests the indicator at the RELEASE coordinates and only accepts a
  candidate built for the gesture's own item; stale-selection and
  foreign-item releases fall to pointer inference or no-op — pinned by
  three unit cases incl. the review's (999,999) falsifier
- [P1] the binding §06 gesture witness (DemoADragMenuNL.spec.mjs, 3/3):
  a REAL pointer drag lights the menu (5 over the root-coincident zone,
  9 over a non-root zone — the chip-omission design asserted), re-targets
  the per-option preview, commits the selected descriptor (worker-truth
  topology), FLIP transforms observable; Escape mid-real-drag releases
  with ZERO commit (seam-driven keystroke — real key delivery is blocked
  by the engine gaps below); reduced-motion collapses the live overlay to
  0s through the token via per-page emulateMedia
- [P2] dockCandidates.v1 genuinely fail-closed: exactly 5 unique cross
  positions + 4 unique chips, itemId/target identity, and position→kind
  §06-grammar coherence (crossKindMatchesPosition); 8 negative probes incl.
  the one-entry center→edge-left review falsifier; Contract Ledger
  backfilled on the ticket

Also hardened: a gesture's first move can outrace measurability — a
degenerate geometry result no longer latches for the whole drag session
(self-healing re-measure on the next frame).

Engine-tier residuals found while building the witness, filed as #14980
with reproducers: gestures within ~1s of mount are DEAF to worker-side
zones (proxy renders, no zone hears — the spec documents its settle), and
no gesture-time key capture exists (placeholder-swap drops focus to body,
so element-scoped keydown can never receive a real mid-drag Escape).

Unit 420/420 (dashboard + agentos), e2e journey 3/3."
- 2026-07-10T18:28:19Z @neo-fable-clio cross-referenced by PR #14974
- 2026-07-10T19:23:54Z @neo-gpt cross-referenced by #14983
- 2026-07-10T19:33:45Z @tobiu referenced in commit `9fa53b7` - "feat(dashboard): the drop-indicator menu — candidate-set producer + DockDropIndicators + the Demo-A §06 experience (#14959) (#14974)

* feat(dashboard): drop-indicator menu — candidate-set producer + DockDropIndicators (#14959)

The §06 primary drag affordance, core tier:

- dockPreviewContract: neo.harness.dockCandidates.v1 schema + isValidCandidateSet
  (fail-closed, every candidate wraps an individually valid dockPreview.v1)
- DockPreviewProducer.produceCandidates(): the full §06 menu for the hovered zone —
  5-position cross (center tab-merge + directional splits via the SAME orientation
  grammar as pointer inference) + container edge chips targeting the caller-supplied
  document root; one shared buildPreview assembly path so an indicator candidate and
  the pointer-inferred preview for the same placement are field-for-field identical
- DockDropIndicators (new): commit-free, pointer-transparent menu layer — nine
  persistent children (object permanence: the cross GLIDES between zones under the
  motion contract), workspace-threaded per-frame hit-test (the drag proxy makes DOM
  hover unreliable mid-drag by construction), fail-closed hide on malformed input
- DockLayoutAdapter: threads onDockCrossZoneDragMove through the projection context
  (the DockTabSortZone move stream finally has a consumer seam)
- Container.scss: --dock-transition-duration-fast joins the token contract WITH the
  reduced-motion collapse (closes the #14955 alias gap), indicator/chip visual family
  on token-fed surfaces, motion-contract timing only

Unit tier: 13 new specs (candidate grammar + parity + fail-closed; component
permanence, hit-test stacking, active-selection lifecycle, producer handshake) —
dashboard dir 237/237 green.

* feat(dockdemo): the §06 drag experience on Demo-A — indicator menu + preview + Escape + the cold-open invite (#14959)

Demo-A becomes the first full consumer of the drop-indicator pipeline:

- DockDropIndicators + the AgentOS DockPreview renderer compose as PERSISTENT
  overlay siblings of the projection child; refreshDockWorkspace turns surgical
  (removeAt(0)/insert(0)) so both survive every coarse re-projection — object
  permanence at the affordance level
- the projection threads onDockCrossZoneDragMove + onDockCrossZoneDrop: the
  move stream measures drag geometry once per gesture (memoized promise —
  the ~60hz stream never stacks measurements), swaps the candidate set on
  zone change only (the cross GLIDES), selects indicators geometrically, and
  routes the §06 tier order — indicator candidate first, pointer inference
  as the fallback tier; drops commit through previewToOperation unchanged
- chips target the edge-zone CENTER node (splitNode wraps any existing node
  — verified against the reducer); addTab detaches first, so tab-merge
  commits are move-semantic by construction
- Escape = the §06 mid-drag cancel (absorbs tree line C5): affordances clear,
  the release commits nothing; the proxy visual rides the base drag until
  release (no drag-layer abort API — documented residual)
- the cold-open invite lands in the tour caption: hand-drag first, 'watch an
  agent do it' second — the camera-beat staging

Live receipts (dev server, Neural-Link-driven seams): menu lights with the
full §06 grammar (screenshot on the PR), per-option target-region preview
(tab-into full zone / edge-left 24px band), two indicator drops committed
their exact descriptors (splitNode horizontal + split-after vertical in the
topology), the FLIP envelope played on the commit (instrumented play() →
true @ 409ms), Escape-suppressed drop committed nothing. Unit: dashboard
239/239 + agentos 177/177 green.

* fix(dashboard): close the four review contracts — shared-ancestor tokens, release-truth drops, complete-menu validation, the binding gesture witness (#14959)

Review cycle 1 (PR #14974), all four Required Actions:

- [P1] motion policy on the SHARED overlay ancestor: the dock-host carries
  .neo-dashboard (custom properties inherit downward only — the projected
  child's scope never reached the overlay SIBLINGS); the indicator family's
  call-site 120ms fallbacks are GONE (token-only, DockFlip authority
  discipline: unscoped hosts land instantly, reduced-motion collapses with
  nothing to override it)
- [P1] release truth, never cached hover truth: onDockCrossZoneDrop
  re-hit-tests the indicator at the RELEASE coordinates and only accepts a
  candidate built for the gesture's own item; stale-selection and
  foreign-item releases fall to pointer inference or no-op — pinned by
  three unit cases incl. the review's (999,999) falsifier
- [P1] the binding §06 gesture witness (DemoADragMenuNL.spec.mjs, 3/3):
  a REAL pointer drag lights the menu (5 over the root-coincident zone,
  9 over a non-root zone — the chip-omission design asserted), re-targets
  the per-option preview, commits the selected descriptor (worker-truth
  topology), FLIP transforms observable; Escape mid-real-drag releases
  with ZERO commit (seam-driven keystroke — real key delivery is blocked
  by the engine gaps below); reduced-motion collapses the live overlay to
  0s through the token via per-page emulateMedia
- [P2] dockCandidates.v1 genuinely fail-closed: exactly 5 unique cross
  positions + 4 unique chips, itemId/target identity, and position→kind
  §06-grammar coherence (crossKindMatchesPosition); 8 negative probes incl.
  the one-entry center→edge-left review falsifier; Contract Ledger
  backfilled on the ticket

Also hardened: a gesture's first move can outrace measurability — a
degenerate geometry result no longer latches for the whole drag session
(self-healing re-measure on the next frame).

Engine-tier residuals found while building the witness, filed as #14980
with reproducers: gestures within ~1s of mount are DEAF to worker-side
zones (proxy renders, no zone hears — the spec documents its settle), and
no gesture-time key capture exists (placeholder-swap drops focus to body,
so element-scoped keydown can never receive a real mid-drag Escape).

Unit 420/420 (dashboard + agentos), e2e journey 3/3."
- 2026-07-10T23:23:31Z @neo-fable-clio added this to the **v13.2** milestone
- 2026-07-10T23:24:14Z @neo-fable-clio cross-referenced by #15000
- 2026-07-11T01:06:14Z @neo-fable-clio assigned to @neo-fable-clio
### @neo-fable-clio - 2026-07-11T01:10:36Z

## Lane opened: the seam map (dev `58653d78f`) — and a correction to my own premise

Static trace of the full gesture pipeline, file:line anchored:

**Main thread:** `Mouse` sensor (dynamic-imported post-addon-construct) gates `mousedown` on `testPathInclusion(event, dragTargetClasses)` (pure DOM cls check, no round-trip — `sensor/Mouse.mjs:95`), dispatches `drag:start` ON THE ELEMENT with the real mousedown path (`:193-201`). The `DragDrop` addon forwards `drag:start` to the app worker **unconditionally** (`DragDrop.mjs:380`). Moves forward only while `!dragProxyElement || alwaysFireDragMove` (`:345`) — the proxy element is captured main-side via a worker-driven remote call (`:583`).

**Worker:** the zone's `dragStart` (`DragZone.mjs:~380-437`) awaits the `setConfigs` round-trip (carrying `alwaysFireDragMove` — `getMainThreadConfigs():482`), adds `neo-is-dragging` (`:414`), then awaits `createDragProxy` (`:426`). `container/SortZone` defaults `alwaysFireDragMove: true` (`SortZone.mjs:52`).

**Premise correction (V-B-A on my own filing):** the ticket says *"no worker-side sort zone ever hears the gesture"* — but `neo-is-dragging` is stamped by the WORKER zone, and the original bisect observed that cls appearing. So in the deaf runs the base zone DID process `drag:start`; the silence is specifically the **move leg** ("subclass `onDragMove` never runs" — the precise 14974-era trail). The deafness is downstream of start-routing.

**Named race candidates, one experiment discriminates:**
1. **Addon move-gate flip**: config-order says `setConfigs` (with `alwaysFireDragMove`) lands before the proxy mounts, keeping `:345` open — but if the proxy-capture leg (`:583`) ever lands FIRST (two independent async legs), moves die at the gate exactly until re-gesture.
2. **Subclass arming race**: `container/SortZone.onDragStart` measures sort geometry (owner rects) before its `onDragMove` does anything — an awaited measurement inside the subclass could no-op every move while resolving (worker-local race, addon innocent).
3. **Sensor/eventpath staleness**: moves carry the mousedown-time path (`Mouse.mjs:137`) — sound for routing, but any placeholder-swap re-render in the first frames could invalidate delegate targets worker-side.

**The experiment (next session, fresh context):** the +0ms reproducer with five instance-field trails read over the NL — sensor trigger, addon forward (+gate state at each move), worker `domEvent` receipt, base-zone `dragStart` entry/exit timestamps (incl. both await legs), subclass `onDragMove` entry. One run paints the exact dead link; the fix follows the same falsifier-paired pattern as `#15009`/`#14985` (both convicted-then-fixed tonight with witness specs).

Fourth empirical bite logged meanwhile: tonight's cockpit forensics (`#14985` trail) had run 1's drag commit nothing with this exact signature; retry succeeded.


