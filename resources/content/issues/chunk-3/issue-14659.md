---
id: 14659
title: 'GP pickup bridge: frontier-empty declared-intent fallback + parent-inherited structural weight'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
  - neo-fable-clio
createdAt: '2026-07-04T03:45:10Z'
updatedAt: '2026-07-04T08:55:59Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14659'
author: neo-fable-clio
commentsCount: 0
parentIssue: 14472
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-04T08:55:59Z'
---
# GP pickup bridge: frontier-empty declared-intent fallback + parent-inherited structural weight

## Context

Operator-escalated with live evidence (2026-07-04 02:50Z capture): the Computed Golden Path recommends ONE item — a discussion, semantic-only, structural 0.00 — with "active frontier currently empty," on the same night ~80 fat tickets were filed. Autonomous pickup is effectively dead at the exact moment the runway was built, and it degrades hardest precisely when fable capacity leaves (the operator's stated risk). Verbatim operator bar: full analysis and SOLUTIONS, not theater.

## The Problem

Five verified causes compose tonight's blindness (analysis on the operator thread + D#14561): (1) REM starvation emptied the last-2-sessions frontier → the semantic anchor has nothing to rank against; (2) structural cold-start zeroes every NEW item (#14422's traced mechanism) — all ~80 tonight-filed tickets carry ≈0.00 structural weight, so the formula cannot recommend the plan; (3) the contradiction guard collapses to zero routes post-release (#14588, claimed separately); (4) the disposition label filter eats design-phase work (the 42.2% exclusion class — split out as its own follow-up); (5) sync latency for minutes-old items. THIS leaf fixes (1) and (2) as one bounded bridge.

## The Architectural Reality

- `GoldenPathSynthesizer` ranks `2×semantic + 1×structural` against the frontier; both axes fail tonight for opposite reasons (empty anchor; zero weight).
- The proper fix is graduated and building: the #14565 direction chain (`EVOLUTION_GOAL` anchors + attribution). This leaf is its **explicit bridge**, designed to dissolve: the fallback consults declared-intent SUBSTRATE that already exists tonight — native epic parent_child edges (wired across the trees), the lane table (D#14561), and blocked_by state — none of it new machinery.
- Fail-open discipline (the #13751 hard AC, inherited): the fallback is additive and can never zero or gate the base route; it activates ONLY when the frontier is empty or the routed-node count is zero.

## The Fix

One PR in the synthesizer's formula region, two additive branches:

1. **Frontier-empty fallback:** when the frontier anchor set is empty (or routed nodes = 0 after the guard), rank actionable items by declared intent: (a) epic-tree membership (item has a parent edge into an open epic) weighted by the epic's open-PR/recent-motion activity, (b) unblocked-status (no open blocked_by), (c) filing recency as the tiebreak. Rendered with an explicit `fallback: declared-intent (frontier empty)` provenance line so the handoff never masquerades as the semantic ranking.
2. **Parent-inherited structural weight (the #14503 minimal shape, folded with Vega's bless-or-veto pending):** a new item with structural ≈0 inherits `α × parent-epic structural weight` (α ≈ 0.5, constant documented, no knob-tuning theater) — making tree-filed leaves visible to the NORMAL formula, not just the fallback.

Both branches carry their falsifying queries per the #14430 discipline (e.g. "GP capture with frontier empty must list ≥N unblocked tree leaves; a capture where the fallback fires while the frontier is non-empty = defect").

## Acceptance Criteria

- [ ] Frontier-empty capture lists unblocked tree leaves ranked by declared intent, with the provenance line rendered.
- [ ] Fallback NEVER activates when the frontier is non-empty (spec-asserted both directions).
- [ ] Parent-inheritance lifts a fixture new-leaf's structural weight from 0 to α×parent (spec w/ the real edge substrate).
- [ ] Fail-open preserved: no branch can zero the base route (the #13751 AC restated as a spec).
- [ ] Unit specs green via the custom config; a live capture before/after on the operator's 02:50 scenario documented in the PR body.
- [ ] Dissolution note in-code: both branches cite the #14565 chain as their replacement and the removal trigger (EVOLUTION_GOAL anchors live).
- [ ] Cross-family review.

## Out of Scope

- #14588's contradiction-guard fix (Mnemosyne's claim; coordination A2A sent).
- The label-filter split (follow-up leaf, cause 4).
- REM slot aging (cause 1's root — needs an orchestrator owner; flagged).
- The full #14503 disposition (stays open under Vega unless folded per his call).

## Related

Parent: #14472 (GP v2 epic — linked native) · bridge-for #14565/#14567/#14568 · folds-minimal #14503 (Vega, pending) · sibling #14588 · evidence: the operator's 02:50Z capture + #14453's rationale (the 42.2% + cold-start + frontier-proximity analysis, live-verified 2026-07-02).

Live latest-open sweep: latest filings checked at 03:44Z (top ~#14654); no equivalent leaf. A2A in-flight: S1/S3 coordination DMs sent (65ffb4f3, 92302a68); no competing claim on this scope.

Origin Session ID: fa2a6fd5-7488-4af6-a0d2-3855c86003e4
Retrieval Hint: "GP pickup bridge frontier empty fallback parent inherited structural weight"

## Timeline

- 2026-07-04T03:45:11Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-07-04T03:45:12Z @neo-fable-clio added the `enhancement` label
- 2026-07-04T03:45:12Z @neo-fable-clio added the `ai` label
- 2026-07-04T03:45:12Z @neo-fable-clio added the `architecture` label
- 2026-07-04T03:45:19Z @neo-fable-clio added parent issue #14472
- 2026-07-04T04:07:09Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-04T04:13:53Z @neo-opus-ada cross-referenced by PR #14686
- 2026-07-04T04:22:06Z @neo-opus-ada referenced in commit `bbdce73` - "feat(graph): wire parent-inherited structural weight into the synthesizer (#14659)

Injection point 1 of 2: at the struct_score computation in the 2xsemantic+1xstructural formula,
a cold-start (~0) leaf now inherits alpha*parent-epic structural weight via the pickup-bridge
helper. Parent reached through the inbound PARENT_OF edge (IssueIngestor's epic->sub edge);
parent weight from the verified getIssueStructuralWeight query (no hand-rolled edge-shape guess).

Fail-open: non-cold-start scores pass through untouched, so the base route is preserved — the
41 existing GoldenPathSynthesizer specs stay green (regression-verified). The helper's own logic
is unit-covered (6 tests). Injection point 2 (frontier-empty declared-intent fallback) + the live
before/after capture on the 02:50Z scenario land next.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T04:32:25Z @neo-opus-ada referenced in commit `6d36553` - "feat(graph): declared-intent fallback render for the GP pickup-bridge (#14659)

The render half of injection 2: renderDeclaredIntentFallback turns a rankByDeclaredIntent result
into a markdown section LED by the provenance line (so a frontier-empty capture never masquerades
as the semantic ranking), bounded to a limit, empty-string when there is nothing to surface (the
caller then renders the normal empty section). Pure + testable — 8 helper tests green.

Remaining on injection 2: the GraphService gather (open actionable unblocked ISSUE nodes + their
PARENT_OF epic membership / activity / filedAt) + wiring into the synthesizer's frontier-empty
branch + the AC's live before/after capture on the 02:50Z scenario.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T04:51:02Z @neo-opus-ada referenced in commit `f5c391f` - "feat(graph): frontier-empty declared-intent fallback wired into the synthesizer (#14659)

Injection point 2 of 2: when the semantic route is empty, buildDeclaredIntentFallback surfaces
unblocked open-epic TREE leaves ranked by declared intent + renders the provenance-led section.

- Source: SQLite (issue- id prefix + the state extraction the main formula proves) → in-memory
  node + getAdjacentNodes topology load → PARENT_OF (epic) / BLOCKS (blocker) edges. The in-memory
  node store is lazy-loaded, so a full .items scan misses unloaded nodes — hence the SQLite read.
- Scope: fires ONLY for open-epic tree leaves (the ~80-fat-tickets-under-epics scenario). A
  standalone open issue is not a tree leaf, so an all-standalone empty pass still renders the
  normal empty section — no behavioral regression (all 41 prior specs green).
- Fail-open: only reachable on the already-empty route branch; never zeroes/gates the base route.

42 synthesizer specs green incl. a new falsifier (open epic + actionable leaf + blocked leaf +
blocker → fallback lists the leaf, drops the blocked + the epic). Remaining on #14659: the AC's
live before/after capture on the 02:50Z scenario (needs the live orchestrator graph).

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T07:14:56Z @neo-opus-ada referenced in commit `46639be` - "fix(graph): make the declared-intent fallback cold-cache correct — read from SQLite, not the lazy node cache (#14659)

Addresses the cross-family RC (Euclid): buildDeclaredIntentFallback gated each row on the lazy
in-memory GraphService.db.nodes.get(id) BEFORE the topology load — so it dropped exactly the
cold-cache rows (fresh in SQLite, not yet hydrated) that the fallback exists to rescue. The prior
fixture passed only because upsertNode pre-warmed the cache.

Rewrote the gather to be fully SQLite-sourced (cold-cache correct by construction): open issues,
their inbound PARENT_OF / BLOCKS edges, and neighbor (parent-epic / blocker) OPEN states are all
read straight from the SQLite source of truth — never from a possibly-unhydrated nodes.get /
getByIndex.

Added a TRUE cold-cache falsifier: insert the epic + tree leaf directly into SQLite so the
in-memory node store NEVER sees them; the fallback still surfaces the leaf. (The old get-before-
load code drops it.) 43 synthesizer specs green.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T07:29:50Z @neo-opus-ada cross-referenced by #14708
- 2026-07-04T07:46:20Z @neo-opus-ada referenced in commit `cbf4090` - "perf(graph): bound the frontier-empty fallback candidate scan to recent-N (#14659)

The declared-intent fallback scanned ALL open issues with per-item SQLite edge/state reads — a
cost risk exactly when it fires (a large undigested backlog / hundreds of open issues). Cap to the
250 most-recent open issues (recency is already the ranking tiebreak, and the fat-ticket scenario
is recent), bounding the per-item work. 43 synthesizer + 8 helper specs green.

(Investigated the #14686 red 'unit' CI: the 4 failures are unrelated flakes — KB queryDocuments,
memory-core cross-process SQLite identity, ArchiveByIdentity, WriteSideInvariant/MailboxService —
none exercise this code; WriteSideInvariant passes locally 3/3; 5762 passed + 3 flaky. This push
re-triggers a fresh run.)

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T08:13:15Z @neo-opus-ada referenced in commit `6f3337a` - "fix(kb): exact-anchor rescue collects the full path-matched dir — restores #12703 CI (#14659)

Same deterministic #12703 regression as #14707: the path-dir rescue collected only 12 files of a
matched dir, so ai/services/graph (23 .mjs) silently dropped GoldenPathSynthesizer.mjs before the
outer result cap ranked it — adding a graph file (goldenPathPickupBridge.mjs here) shifted the
collected slice. Fix: collect the full dir (inner 12 -> 40; outer cap still bounds results) + size
the #12703 test cap to the full rescued set. QueryService.queryDocuments 13/13 green locally.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T08:21:22Z @neo-opus-ada cross-referenced by PR #14717
- 2026-07-04T08:55:59Z @tobiu referenced in commit `c94b9df` - "feat(graph): GP pickup-bridge fallback + parent-inherited structural weight (#14659) (#14686)

* feat(graph): GP pickup-bridge fallback + parent-inherited structural weight (#14659)

Bounded, fail-open bridge for the autonomous-pickup blindness when the REM-starved frontier is
empty AND newly-filed tickets carry ~0 structural weight (cold-start). Pure helper (the
synthesizer delegates to it, matching frontierConsolidation/computedGoldenPathRouting):

- shouldActivateFallback: fires ONLY when frontier empty OR routed count 0 (#13751 fail-open —
  can never fire alongside a healthy route; asserted both directions).
- inheritParentStructuralWeight: a cold-start (~0) leaf inherits alpha=0.5 x parent-epic weight,
  making tree-filed leaves visible to the normal 2xsemantic+1xstructural formula; an already-
  scored item is untouched; only ever lifts, never lowers.
- rankByDeclaredIntent: frontier-empty fallback ranks unblocked open-epic-tree leaves by epic
  membership x activity, recency tiebreak, tagged with an explicit declared-intent provenance
  line so a fallback capture never masquerades as the semantic ranking.

Designed to dissolve once the EVOLUTION_GOAL direction chain makes declared intent first-class.
6 unit tests green. Synthesizer wiring + the live before/after capture on the 02:50Z scenario
land next in this PR.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* feat(graph): wire parent-inherited structural weight into the synthesizer (#14659)

Injection point 1 of 2: at the struct_score computation in the 2xsemantic+1xstructural formula,
a cold-start (~0) leaf now inherits alpha*parent-epic structural weight via the pickup-bridge
helper. Parent reached through the inbound PARENT_OF edge (IssueIngestor's epic->sub edge);
parent weight from the verified getIssueStructuralWeight query (no hand-rolled edge-shape guess).

Fail-open: non-cold-start scores pass through untouched, so the base route is preserved — the
41 existing GoldenPathSynthesizer specs stay green (regression-verified). The helper's own logic
is unit-covered (6 tests). Injection point 2 (frontier-empty declared-intent fallback) + the live
before/after capture on the 02:50Z scenario land next.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* feat(graph): declared-intent fallback render for the GP pickup-bridge (#14659)

The render half of injection 2: renderDeclaredIntentFallback turns a rankByDeclaredIntent result
into a markdown section LED by the provenance line (so a frontier-empty capture never masquerades
as the semantic ranking), bounded to a limit, empty-string when there is nothing to surface (the
caller then renders the normal empty section). Pure + testable — 8 helper tests green.

Remaining on injection 2: the GraphService gather (open actionable unblocked ISSUE nodes + their
PARENT_OF epic membership / activity / filedAt) + wiring into the synthesizer's frontier-empty
branch + the AC's live before/after capture on the 02:50Z scenario.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* feat(graph): frontier-empty declared-intent fallback wired into the synthesizer (#14659)

Injection point 2 of 2: when the semantic route is empty, buildDeclaredIntentFallback surfaces
unblocked open-epic TREE leaves ranked by declared intent + renders the provenance-led section.

- Source: SQLite (issue- id prefix + the state extraction the main formula proves) → in-memory
  node + getAdjacentNodes topology load → PARENT_OF (epic) / BLOCKS (blocker) edges. The in-memory
  node store is lazy-loaded, so a full .items scan misses unloaded nodes — hence the SQLite read.
- Scope: fires ONLY for open-epic tree leaves (the ~80-fat-tickets-under-epics scenario). A
  standalone open issue is not a tree leaf, so an all-standalone empty pass still renders the
  normal empty section — no behavioral regression (all 41 prior specs green).
- Fail-open: only reachable on the already-empty route branch; never zeroes/gates the base route.

42 synthesizer specs green incl. a new falsifier (open epic + actionable leaf + blocked leaf +
blocker → fallback lists the leaf, drops the blocked + the epic). Remaining on #14659: the AC's
live before/after capture on the 02:50Z scenario (needs the live orchestrator graph).

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* fix(graph): make the declared-intent fallback cold-cache correct — read from SQLite, not the lazy node cache (#14659)

Addresses the cross-family RC (Euclid): buildDeclaredIntentFallback gated each row on the lazy
in-memory GraphService.db.nodes.get(id) BEFORE the topology load — so it dropped exactly the
cold-cache rows (fresh in SQLite, not yet hydrated) that the fallback exists to rescue. The prior
fixture passed only because upsertNode pre-warmed the cache.

Rewrote the gather to be fully SQLite-sourced (cold-cache correct by construction): open issues,
their inbound PARENT_OF / BLOCKS edges, and neighbor (parent-epic / blocker) OPEN states are all
read straight from the SQLite source of truth — never from a possibly-unhydrated nodes.get /
getByIndex.

Added a TRUE cold-cache falsifier: insert the epic + tree leaf directly into SQLite so the
in-memory node store NEVER sees them; the fallback still surfaces the leaf. (The old get-before-
load code drops it.) 43 synthesizer specs green.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* perf(graph): bound the frontier-empty fallback candidate scan to recent-N (#14659)

The declared-intent fallback scanned ALL open issues with per-item SQLite edge/state reads — a
cost risk exactly when it fires (a large undigested backlog / hundreds of open issues). Cap to the
250 most-recent open issues (recency is already the ranking tiebreak, and the fat-ticket scenario
is recent), bounding the per-item work. 43 synthesizer + 8 helper specs green.

(Investigated the #14686 red 'unit' CI: the 4 failures are unrelated flakes — KB queryDocuments,
memory-core cross-process SQLite identity, ArchiveByIdentity, WriteSideInvariant/MailboxService —
none exercise this code; WriteSideInvariant passes locally 3/3; 5762 passed + 3 flaky. This push
re-triggers a fresh run.)

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

* fix(kb): exact-anchor rescue collects the full path-matched dir — restores #12703 CI (#14659)

Same deterministic #12703 regression as #14707: the path-dir rescue collected only 12 files of a
matched dir, so ai/services/graph (23 .mjs) silently dropped GoldenPathSynthesizer.mjs before the
outer result cap ranked it — adding a graph file (goldenPathPickupBridge.mjs here) shifted the
collected slice. Fix: collect the full dir (inner 12 -> 40; outer cap still bounds results) + size
the #12703 test cap to the full rescued set. QueryService.queryDocuments 13/13 green locally.

Co-Authored-By: Ada (Claude Opus 4.8) <ada@neomjs.dev>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>
Co-authored-by: Ada (Claude Opus 4.8) <ada@neomjs.dev>"
- 2026-07-04T08:55:59Z @tobiu closed this issue

