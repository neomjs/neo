---
id: 15524
title: 'FM door: the zero-setup demo authority — bundled sample default, rate-measured public-fleet opt-in'
state: CLOSED
labels:
  - enhancement
  - ai
assignees:
  - neo-gpt-emmy
createdAt: '2026-07-18T19:30:18Z'
updatedAt: '2026-07-22T12:44:32Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15524'
author: neo-fable-clio
commentsCount: 6
parentIssue: 15519
subIssues:
  - '[x] 15549 Measure zero-credential public-fleet first-paint budget'
  - '[x] 15563 Preserve bundled Fleet sample on cold empty roster'
subIssuesCompleted: 2
subIssuesTotal: 2
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-22T12:44:32Z'
---
# FM door: the zero-setup demo authority — bundled sample default, rate-measured public-fleet opt-in

## Context
Sub of epic #15519 (graduated D#15498, OQ6 + OQ7). The third abandonment cliff: a flawless cold install of the real FM first-paints EMPTY PANELS (the views bind Brain-fed stores). The demo authority decision is graduated: bundled deterministic sample = the shipped default; public-fleet read-only backend = opt-in behind the measured rate falsifier.

## The Work
Ship the bundled sample (the trinity take + a canned roster, honestly captioned — tour provenance, never posed as live Memory Core arrival) as the packaged shell's first-paint default, satisfying the first-run bar: download → double-click → the fleet visibly working inside a minute. Run the rate-budget measurement for the opt-in tier (unauthenticated GitHub API 60 req/h vs the synced readers' first-paint request pattern): PASS → the public-fleet read-only backend ships as the zero-credential opt-in; FAIL → token-gated only, and the measurement is the recorded reason.

## Acceptance Criteria
- [ ] Cold first-run shows the sample fleet working inside a minute, honestly labeled.
- [ ] The rate measurement is recorded with its verdict; the opt-in tier matches it.
- [ ] The org-adjacency dependence of the public-fleet tier is stated in the topology ADR's C-falsifier record.

## Related
Epic #15519 · D#15498 (OQ6/OQ7; Vega's third cliff + rate falsifier; Phoebe's first-run bar) · ADR-0034 (the shell this rides in).


## Timeline

- 2026-07-18T19:30:19Z @neo-fable-clio added the `enhancement` label
- 2026-07-18T19:30:19Z @neo-fable-clio added the `ai` label
- 2026-07-18T19:30:43Z @neo-fable-clio cross-referenced by #15519
- 2026-07-18T19:35:42Z @neo-fable-clio added parent issue #15519
### @neo-gpt-emmy - 2026-07-18T22:59:34Z

## Ticket intake — VALID AFTER NARROWING

**Classification:** `revise-before-execute` · **ROI:** high · **successor risk:** `adr-aligned`

### Freshness and authority

- The source authority is the graduated D#15498 OQ6/OQ7 disposition: bundled deterministic sample is the shipped default; the public-fleet read-only tier is opt-in behind its unauthenticated 60 req/h falsifier.
- ADR-0037 PR #15546 now carries the required org-adjacent, credential-free Option-C provenance mechanism and the current #15527 PASS-with-provisioning receipt.
- The open-issue sweep found no competing zero-setup Fleet demo lane. Draft PR #15545 owns demo-vs-product host separation; its composition must land before this lane touches the cockpit.

### Premise falsification — do not rebuild the sample

Most of “ship the bundled sample” is already current source truth:

- `apps/agentos/store/FleetRoster.mjs:15-18,41-43` declares the native JSON seed and its replacement by live registry truth.
- `apps/agentos/view/fleet/FleetCockpit.mjs:212-224` provider-autoLoads that Store; `:276-281` and `:407-419` retain honest `sample` state for roster and activity.
- `apps/agentos/view/fleet/FleetGrid.mjs:304` renders “sample roster”; `ActivityStream.mjs:301-309` renders “sample · live feed pending.”
- Existing browser witnesses already mount cards, health, and activity inside 60 seconds.

A broad implementation that adds another sample owner would therefore be negative ROI and create split truth.

One real product hole remains: `loadRoster()` currently treats an authoritative empty bridge answer as live truth, clears the sample, and marks the cockpit live-empty. Once the packaged private bridge is wired, a fresh empty registry can erase the graduated shipped-default sample immediately. #15524 must make the first-run source-mode transition explicit; it cannot accidentally depend on whether an empty/auth-failed bridge wins the boot race.

### Contract Ledger — narrowed residual

| Producer | Boundary | Consumer | Truth / witness |
|---|---|---|---|
| existing bundled roster + activity fixtures | packaged `app://neo` content graph | cold Fleet cockpit | final artifact first-paints a visibly working, explicitly sample-labeled fleet inside one minute |
| explicit first-run source-mode policy | empty / unavailable / populated private bridge answer | packaged cockpit | empty first-run cannot silently erase the shipped sample; a genuinely selected/live source transition remains honest |
| existing synced GitHub readers' first-paint request pattern | unauthenticated GitHub API budget | rate-measurement receipt | measured calls remain within 60 req/h, or the receipt records FAIL |
| rate verdict | existing Fleet read-observe trust boundary | explicit public-fleet opt-in | PASS permits zero-credential opt-in; FAIL keeps it token-gated; neither path changes the bundled default |
| trinity screenplay | dedicated demo host from #15545 | site/demo proof | tour provenance never re-enters the real Fleet Manager |
| ADR-0037 Option-C record | topology authority | #15524 closeout | #15546 exact-head wording lands before closeout |

### Dependencies and collision boundary

The packaged semantic witness must run after merge-ready PRs #15543 and #15544 land: #15543 owns the retained product lifecycle and packaged-source graph receipt; #15544 repairs the E6 packaging boundary. Testing a feature-branch SHA as if it were the released artifact would be false evidence.

PR #15545 must also land before the cockpit/source-mode implementation: it is actively separating tours from the real product and already touches `FleetCockpit.mjs` plus the walkthrough host/spec. #15524 must consume that separation, never reintroduce Play Tour or autoplay into the product.

#15537's private Electron Fleet capability is not a reason to wait, but its empty/available answer is an explicit case in the source-mode ledger; the implementation must not assume “bridge absent forever.”

### Route

Proceed with the narrowed residual only:

1. measure and record the existing synced-reader first-paint budget independently;
2. after #15543/#15544/#15545 merge, implement the explicit first-run sample→selected/live transition;
3. implement the opt-in public-fleet projection according to the measured verdict without exposing credentials to the Body;
4. run the final packaged cold-first-run witness.

Do **not** add a second sample store, second fixture vocabulary, demo-only control, or second Fleet control plane. The sample-authority transition and rate-gated opt-in are significant halves of one tiered first-run contract, so keep them in this ticket unless implementation evidence proves a genuinely independent owner.

- 2026-07-18T23:04:34Z @neo-gpt-emmy assigned to @neo-gpt-emmy
- 2026-07-18T23:12:13Z @neo-gpt-emmy cross-referenced by #15549
- 2026-07-18T23:12:21Z @neo-gpt-emmy added sub-issue #15549
- 2026-07-18T23:13:41Z @neo-gpt-emmy cross-referenced by PR #15550
- 2026-07-18T23:59:10Z @neo-gpt-emmy cross-referenced by PR #15546
- 2026-07-19T00:31:20Z @neo-gpt-emmy cross-referenced by #15526
### @neo-gpt-emmy - 2026-07-19T01:06:25Z

## Measurement fold — FAIL is now the selected product branch

**Exact evidence:** PR #15550 @ `216620f74730ac7407278ce758623dff0f140873` is CLEAN and cross-family APPROVED; merge remains operator-owned.

The graduated rate falsifier failed on **capability before volume**:

- anonymous REST exposes a 60-request/hour core bucket;
- anonymous GraphQL reports limit `0` and the read probe returns 403;
- the collaborator trust census returns 401;
- the existing reader requires GraphQL for roots/reviews/Discussions and the collaborator census for its trust contract.

Therefore #15524's settled source modes are:

| Mode | Default calls | Product status |
|---|---:|---|
| bundled deterministic sample | 0 | shipped default; visibly working and honestly sample-labeled |
| public fleet without token | 0 | **unavailable**; no dead toggle and no partial REST-only impersonation |
| public fleet with token | explicit opt-in | allowed through the existing read-observe boundary; never the default |

A REST-only subset would be a different query plan with different family/trust semantics. This ticket does not authorize silently renaming it “public fleet,” and it does not authorize a second acquisition stack.

### Remaining implementation residual

1. After #15543/#15544/#15545 merge, make the bundled-sample → selected/live transition explicit so an empty or failed private bridge cannot erase first-run truth.
2. Add only the token-present public-fleet opt-in selected above.
3. Run the packaged cold-first-run witness and preserve the product/demo separation: no Play Tour/autoplay control returns to the real cockpit.
4. Consume ADR-0037's org-adjacency falsifier after #15546 merges.

D#15556 governs a different high-blast question—the authority for a remote tenant Fleet snapshot. It does **not** gate this offline sample default or the existing-reader token-gated public-activity opt-in.

**AC status:** the measurement/verdict half is delivered pending #15550 merge; the matching opt-in and packaged first-paint witness remain open.

### @neo-gpt-emmy - 2026-07-19T06:30:16Z

## Re-entry receipt — dependencies merged; narrowed implementation starts

**Classification:** `valid-after-narrowing` · **ROI:** high · **collision:** none

All five named delivery dependencies are now on `dev`: #15543, #15544, #15545, #15546, and #15550. The live open-PR sweep found no competing #15524 branch.

The current exact source still has the residual identified at intake: `FleetCockpit.loadRoster()` treats any `rows: []` response as authoritative on its first successful bridge read, clears the bundled roster, and marks the grid live. That is correct **after a live source has been established**, but wrong for cold first-run because a fresh empty private registry can erase the graduated zero-call sample before the operator sees a working fleet.

### Execution split under this ticket

1. **Cold-empty admission (first PR):** preserve the honestly labelled sample when the first well-formed roster answer is empty; a populated answer still promotes to live. Once live has been established, a later empty snapshot remains authoritative and clears departed residents. This keeps both sides truthful.
2. **Token-present public opt-in (second PR):** consume the existing canonical read-observe boundary and the merged #15549 FAIL receipt. No anonymous/degraded REST subset, no second acquisition stack, and no credential byte in the Body. #15537 owns shell credential ingress; this lane owns the explicit product source choice and projection.
3. **Packaged witness:** cold first paint remains visibly working and sample-labelled; no Play Tour/autoplay control returns to the real Fleet Manager.

This is an implementation sequence, not scope fan-out: both beats remain under #15524 and converge on the same source-mode contract.

ADR successor-risk: `adr-aligned` — artifact #15524 (2026-07-18); ADR 0037 accepted through merged PR #15546 on 2026-07-19; evidence ADR 0037 §2.5 and #15549/#15550; route `continue`.

- 2026-07-19T06:42:15Z @neo-gpt-emmy cross-referenced by #15563
- 2026-07-19T06:42:32Z @neo-gpt-emmy added sub-issue #15563
- 2026-07-19T06:43:44Z @neo-gpt-emmy cross-referenced by PR #15564
- 2026-07-19T08:31:57Z @tobiu referenced in commit `1549b8a` - "fix(agentos): preserve Fleet sample on cold empty roster (#15524) (#15564)"
### @neo-gpt-emmy - 2026-07-19T09:22:41Z

## Collision correction — converge on #15566 before the remaining two beats

The prior re-entry receipt's `collision: none` is superseded by fresh source and open-PR evidence.

PR #15564 has now merged the cold-empty admission half. The remaining **packaged first-paint witness** and **token-present public-source opt-in** both meet the capability boundary introduced by open PR #15566, which owns `harness/fleetCapability.mjs`, `harness/main.mjs`, and `harness/preload.cjs`. Implementing either beat before that head lands would stack directly on the credential and packaged-shell authority we need to consume.

### Narrowing discovered by the live consumer sweep

“Public fleet” must mean **public activity**, not a public process roster:

- `fleetRoster()` is local process/registry truth. GitHub collaborator activity cannot attest a live resident process.
- `communityActivityShadowReader.mjs` is probe/shadow acquisition with `notAuthority: true`; it emits raw occurrence families, not `FleetCockpitEvent` DTOs.
- `FleetCockpit` reads `fleetActivity()` every 15 seconds. Provider acquisition and credential resolution must **not** ride that poll.
- The valid shape is one shell-owned, token-present admission that fills a bounded Brain-side projection; ordinary `fleetActivity()` reads that projection without provider calls or credential bytes in the Body.
- The anonymous / REST-only degraded path remains unavailable per the merged FAIL receipt. D#15556's remote tenant snapshot authority remains separate.

### Post-#15566 execution order

1. Add the one-shot public-activity admission to the merged shell capability boundary.
2. Keep the existing community shadow reader as acquisition SSOT and add an explicit raw-occurrence → `FleetCockpitEvent` adapter/cache boundary.
3. Preserve current local `fleetRoster()` semantics; do not fabricate public process residents.
4. Add the packaged first-paint diagnostic as a distinct semantic report: visible cockpit, at least one card, exact honest sample labels, zero product-tour controls, and `firstPaintMs <= 60000`.
5. Run the final default-double-click packaged witness. A browser-only or `NEO_HARNESS_BRAIN=0` proxy does not close the packaged AC.

This is dependency ordering and authority clarification, not scope expansion. No preparatory PR is warranted: it would be partial, unprovable in the packaged product, and likely freeze the wrong source contract.

### @neo-gpt-emmy - 2026-07-22T11:43:27Z

## Re-entry premise correction — packaged witness stays; public acquisition moves to ADR-0036

**Classification:** `needs-narrowing + needs-relinking` · **ROI:** high for the packaged witness · **wrong-owner risk:** confirmed for the July 19 one-shot acquisition prescription

Fresh live authority changes the remaining implementation shape. The prior [collision-correction comment](https://github.com/neomjs/neo/issues/15524#issuecomment-5015187129) remains correct about **not** fabricating a public process roster and **not** polling a provider from `fleetActivity()`. It is superseded only where it recommends a shell-owned one-shot GitHub admission plus a new adapter/cache boundary.

### Falsifying evidence

ADR-0036 landed through merged PR #15473 and separates these owners:

- GitHub Workflow/provider connectors own credentials, API traversal, reconciliation, pagination, and rate handling.
- Memory Core owns provider-neutral durable admission, receipts, history, and coverage; it never polls a provider or resolves credentials.
- The local Orchestrator owns coordination/retry/health and advances acquisition state only after neutral admission succeeds.
- Bird View reads the durable ledger, not a provider-only live query.
- A future Fleet community pane is read-only unless it invokes the canonical claim transaction.

That chain is now executable substrate rather than a hypothetical seam:

- #15153 has active PR #15698 for exhaustive PR/review reconciliation.
- #15155 owns local Orchestrator coordination.
- #15157 owns the provider-neutral temporal Bird View that future Fleet readers consume.
- #15159 owns the bounded count-only projection.

Therefore a `harness/fleetCapability.mjs` GitHub fetch followed by a shell/Brain cache would create a second acquisition and completeness path beside #15145. Reusing the transitional shadow reader does not cure that: ADR-0036 option N explicitly permits the existing sync seam only as a removable first-adapter bridge, never as portable authority or a second ledger.

### Corrected execution split

1. **Proceed now under #15524:** add the packaged first-paint semantic receipt already specified—cockpit visible, at least one card, exact honest sample labels, zero product-tour controls, and `firstPaintMs <= 60000`—then run it against the real Brain-on packaged shell.
2. **Do not add here:** direct GitHub calls, provider credentials, a shell-owned occurrence cache, a second raw-occurrence adapter, or public process residents.
3. **Relink the public opt-in residual:** Fleet should eventually consume the provider-neutral ledger/Bird-View contract after its producer leaves land. The token/rate FAIL receipt remains useful acquisition evidence, but #15524 is a consumer integration lane, not the acquisition owner.
4. **Keep the bundled sample as default:** PR #15564 already delivered cold-empty admission; the packaged receipt is the remaining independently closeable proof.

### ADR successor-risk audit

- Ticket outcome: `adr-aligned after narrowing`
- July 19 one-shot implementation prescription: `authority-conflicting; superseded`
- Route: implement only the packaged witness now; consume #15157/#15159 later rather than duplicate #15145

This preserves the graduated OQ6/OQ7 product outcome while moving the mechanism to the authority that now exists.

- 2026-07-22T12:17:39Z @neo-gpt-emmy cross-referenced by #15706
- 2026-07-22T12:23:39Z @neo-gpt-emmy cross-referenced by PR #15708
### @neo-gpt-emmy - 2026-07-22T12:24:26Z

## Exact packaged first-paint receipt — delivered in PR #15708

Live evidence on exact head `e09cb62c47251e195a7f4acefe74c8df47682593`:

| Contract | Receipt |
|---|---|
| Packaged default-on Brain | `packagedMode: true`, `brainMode: true`, `brainUp: true` |
| Bound | `firstPaintMs: 458` |
| Working sample | 10 visible cockpit cards |
| Honest labels | `static roster · offline`; `sample · live feed pending` |
| Product/demo boundary | zero `.fm-fusion-tour` / `.fm-tour-caption` controls |
| Verdict | `passed: true`, `productWitnessPassed: true` |
| Lifecycle hygiene | no matrix violations; both Brain groups stopped cleanly; groups empty; ports released |
| Security/renderer hygiene | no asset failures, renderer errors, or secret leaks |

The exact-head screenshot visually confirms the same first paint.

The broader lifecycle smoke remains honestly **red on a separate pre-existing boundary**: the packaged App Worker never emits `fleetRoster` before or after popup close, although shell-window `listAgents` calls, Brain startup, popup behavior, security controls, and teardown pass. The producer/consumer mismatch is now durable substrate in #15706; PR #15708 does not weaken those assertions or misreport the full smoke as green.

This closes the independently closeable packaged-witness residual recorded in the July 22 authority correction. Provider acquisition remains with ADR-0036's provider-neutral producer chain; no second acquisition path was added.

- 2026-07-22T13:05:51Z @neo-gpt-emmy cross-referenced by #15714
- 2026-07-22T16:27:40Z @neo-kimi-iris cross-referenced by PR #15717
- 2026-07-24T07:27:15Z @neo-fable-clio cross-referenced by #15490

