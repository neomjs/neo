---
id: 12191
title: 'gh-workflow syncer: needsUpdate reads wrong field (always re-renders) + O(N²) #planBuckets in reconcile loop'
state: CLOSED
labels:
  - bug
  - ai
  - performance
assignees:
  - neo-opus-ada
createdAt: '2026-05-30T01:15:28Z'
updatedAt: '2026-05-30T06:33:37Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12191'
author: neo-opus-ada
commentsCount: 0
parentIssue: 12186
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-30T06:33:37Z'
---
# gh-workflow syncer: needsUpdate reads wrong field (always re-renders) + O(N²) #planBuckets in reconcile loop

## Context

Sub of #12186 (P2). Two `IssueSyncer` efficiency/correctness bugs, grounded against the code. (The wgr9t499x investigation mislocated the O(N²) at `:990`; corrected here to `:1007`.)

## Findings (grounded)

**1. `needsUpdate` reads a non-existent field → every issue re-rendered every sync.**
`IssueSyncer.mjs:624` compares `oldIssue.updated !== issue.updatedAt`, but the persisted metadata field is `updatedAt` (`MetadataManager.mjs:83`; `IssueSyncer.mjs:156/664/830/1013` all write `updatedAt`, never `updated`). So `oldIssue.updated` is always `undefined` → `needsUpdate` is always `true` → every delta issue is re-formatted, re-hashed and re-written every run; the content-hash short-circuit never engages.
→ Fix: `:624` → `oldIssue.updatedAt !== issue.updatedAt`.

**2. `#planBuckets` recomputed per-iteration in the reconcile loop (loop-invariant → O(N²)).**
`IssueSyncer.mjs:1007` calls `this.#planBuckets(metadata)` inside the per-issue reconcile loop, but its only input (`metadata`) is loop-invariant, so the identical plan is rebuilt for every archived issue. The main pull path is already correct (`:572` precomputes once; the loop does O(1) `planBuckets.get()`) — the reconcile path should mirror it.
→ Fix: hoist the `:1007` `#planBuckets(metadata)` above its loop.
(`:808` in `refetchIssuesByNumber` also calls per-iteration, but with a changing `[issue]` arg and typically small K — minor; verify at implementation.)

## Acceptance Criteria

- [ ] `:624` compares `oldIssue.updatedAt`; unchanged issues skip re-render/re-hash (content-hash short-circuit verified to engage).
- [ ] `#planBuckets(metadata)` computed once per reconcile run (`:1007` hoisted), not per issue.
- [ ] Main pull path (`:572`) unchanged.
- [ ] A regression test covers the `updatedAt` short-circuit (unchanged issue → not re-pulled).

## Related

- #12186 (epic).

Retrieval Hint: grep `planBuckets` + `needsUpdate` + `oldIssue.updated` in `IssueSyncer.mjs`.

## Timeline

- 2026-05-30T01:15:29Z @neo-opus-ada added the `bug` label
- 2026-05-30T01:15:29Z @neo-opus-ada added the `ai` label
- 2026-05-30T01:15:29Z @neo-opus-ada added the `performance` label
- 2026-05-30T01:15:38Z @neo-opus-ada added parent issue #12186
- 2026-05-30T06:16:18Z @neo-opus-ada cross-referenced by #12194
- 2026-05-30T06:16:24Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-05-30T06:25:38Z @neo-opus-ada cross-referenced by PR #12195
- 2026-05-30T06:33:37Z @tobiu closed this issue
- 2026-05-30T06:33:37Z @tobiu referenced in commit `d6ffb8e` - "fix(github-workflow): needsUpdate compares persisted updatedAt field (#12191) (#12195)

IssueSyncer needsUpdate compared oldIssue.updated, but the persisted metadata field is updatedAt (MetadataManager + every writer). oldIssue.updated was always undefined, so needsUpdate was always true — every issue re-formatted, re-hashed and re-written every sync, defeating the content-hash short-circuit. One-character field fix.

The epic second P2 item (hoisting the per-iteration planBuckets call out of the reconcile loop) was investigated and dropped as unsafe: planBuckets derives oldVersion from metadata.issues paths, which the reconcile loop mutates per-iteration, so the call is not loop-invariant and hoisting would change chunk-packing behavior. The loop is also over active-closed issues only (typically few), so the cost is small.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-30T06:36:18Z @neo-opus-ada cross-referenced by #12186

