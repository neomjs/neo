---
id: 14745
title: Convert FM cockpit primitive layer functional → class-based (idiomatic Neo components)
state: OPEN
labels:
  - epic
assignees:
  - neo-opus-vega
createdAt: '2026-07-04T12:01:48Z'
updatedAt: '2026-07-12T09:01:10Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14745'
author: neo-opus-vega
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Convert FM cockpit primitive layer functional → class-based (idiomatic Neo components)

## Context

@tobiu vetoed building AgentCard (#14598) as a functional component: **"functional components are vastly inferior to class-based counterparts. VBA."** VBA confirms it: apps/ is **320 : 9 class-vs-functional**; functional (`defineComponent`) is a still-maturing feature (#6992) that thinly wraps the model, while the mature class idiom (`extends Component.Base`, reactive `x_` configs, `afterSetX()` → vdom + `this.update()`, `items:[{module}]` composition, state.Provider binding — see `src/component/Chip.mjs`, `apps/agentos/view/FleetSettingsPanel.mjs`) is the production Neo component.

**My miss:** I built the whole FM cockpit primitive layer functional — StateDot (#14700, merged), FamilyRail (#14722, merged), EventChip (#14701, merged), HealthSwatch (#14726, merging). Non-idiomatic across the board, not just AgentCard.

## The Fix

Convert the FM primitive layer to class-based components, preserving each primitive's PUBLIC surface (the exported pure resolvers `stateToken`/`familyToken`/`kindToken`/`kindLabel`/`stateLabel` stay; the `state`/`family`/`kind`/`count`/`label`/`live` configs stay reactive) so consumers + the CSS token layer are unaffected — only the component base + internal render switch functional→class.

Sequence (foundational first): **StateDot** (others reuse `stateToken`) → FamilyRail → EventChip → HealthSwatch. Then AgentCard (#14598) is built class-based composing the converted primitives.

## Acceptance Criteria

- [ ] Each FM primitive `extends Neo.component.Base` (or the apt base), reactive `x_` configs with `afterSetX()` render, `Neo.setupClass`.
- [ ] Public surface preserved: exported resolvers + the CSS `--fm-*` binding contract unchanged; unit specs pass with the class components (same assertions).
- [ ] Zero functional (`defineComponent`) left in `apps/agentos/view/fleet/`.

## Related

Epic #14560 (FM cockpit) · #14598 (AgentCard, built class after) · operator veto 2026-07-04 · idiom refs `src/component/Chip.mjs` + `apps/agentos/view/FleetSettingsPanel.mjs`. Release classification: not release-blocking (idiom correction of merged work).

Origin Session ID: 3bc21462-8122-4b02-81e4-324ed781e1ac


## Timeline

- 2026-07-04T12:02:14Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-07-04T12:06:13Z @neo-opus-vega cross-referenced by PR #14746
- 2026-07-04T12:18:06Z @neo-opus-vega cross-referenced by PR #14747
- 2026-07-04T12:23:33Z @neo-opus-vega cross-referenced by #14748
- 2026-07-04T12:23:34Z @neo-opus-vega added the `epic` label
- 2026-07-04T12:23:34Z @neo-opus-vega cross-referenced by #14749
- 2026-07-04T12:38:30Z @tobiu referenced in commit `5c09efc` - "refactor(agentos): StateDot → class-based component (functional→idiomatic) (#14745) (#14746)

Operator vetoed functional FM components ("vastly inferior to class-based, VBA"); VBA confirmed apps/ is 320:9 class-vs-functional. Converts StateDot from defineComponent to `extends Neo.component.Base` — reactive state_/live_ configs with afterSet render (--fm-dot via style, fm-live via cls), the src/component/Chip.mjs idiom. Public surface preserved: exported stateToken (hardened) + the --fm-* binding contract unchanged.

Pattern-setter for the FM-layer conversion (#14745): FamilyRail/EventChip/HealthSwatch follow, then AgentCard #14598 builds class-based. statePrimitives.spec.mjs passes 3/3 UNCHANGED — the class version is behavior-identical (Neo.create + initVnode + vdom.style/cls all hold)."
- 2026-07-04T12:43:29Z @tobiu referenced in commit `31698dd` - "refactor(agentos): FamilyRail/EventChip/HealthSwatch → class-based (#14745) (#14747)

Completes the FM primitive layer functional→class conversion (StateDot was PR #14746). Each now extends Neo.component.Base with reactive configs + afterSet render (the src/component/Chip.mjs idiom): FamilyRail (family_ → --fm-rail + unclassified cls), EventChip (kind_/label_ → --fm-chip + text), HealthSwatch (state_/count_/label_ → dot/count/label cn nodes, count removeDom-toggled). Exported resolvers (familyToken/isKnownFamily/stateLabel) + the --fm-* binding contract preserved.

Specs green: familyRail 4/4 + eventChip 3/3 + healthSwatch 6/6 = 13/13. One healthSwatch assertion updated to the class idiom (the count node is removeDom-toggled, not omitted from cn). AgentCard #14598 now builds class-based composing these."
- 2026-07-06T09:54:25Z @neo-opus-vega cross-referenced by #14560
### @neo-opus-vega - 2026-07-12T09:01:10Z

## Verified complete — recommending closure (all AC met)

A fresh V-B-A of the FM cockpit primitive layer confirms this epic's conversion work is done. Flagging for closure (I'm leaving the actual close to the operator — an autonomous epic-close is a gated external action):

- **AC-3 (zero functional left):** `grep -rn defineComponent apps/agentos/view/fleet/` → **0 matches**. No `defineComponent` remains in the FM cockpit layer.
- **AC-1 (class idiom):** the four named primitives are all class-based — StateDot / FamilyRail / EventChip / HealthSwatch each `@extends Neo.component.Base` with reactive `afterSetX()` render methods + `Neo.setupClass`. AgentCard (#14598) was built class-based composing them, and the full component set is class-based (12/12 components: ActivityStream, AgentCard + AgentCardController, EventChip, FamilyRail, FleetCockpit + FleetCockpitController, FleetGrid, HealthBar, HealthSwatch, SourceHealthMarker, StateDot; the remaining `fleet/` files are non-component pure helpers — registries/factories/adapters).
- **AC-2 (public surface preserved):** the exported pure resolvers (`stateToken`/`familyToken`/`kindToken`/`kindLabel`/…) and the `--fm-*` CSS binding contract are intact; the primitive unit specs (stateDot/familyRail/eventChip/healthSwatch) are present + green on dev.

No open sub-issue references this epic. The FM primitive layer is 100% class-based and the operator idiom-veto (2026-07-04) is satisfied. Ready to close as completed whenever you're ready, @tobiu.

Authored by Vega (Claude Opus 4.8, Claude Code). Session d99146da-0478-4f23-bc16-dff04f5d650c.


