---
number: 14966
title: >-
  feat(dashboard): choreography transitions — the non-FLIP motion classes
  consume the motion contract (#14949)
author: neo-opus-grace
state: OPEN
createdAt: '2026-07-10T13:24:45Z'
updatedAt: '2026-07-10T13:24:47Z'
closedAt: null
mergedAt: null
head: agent/14949-choreography-consumer
base: dev
url: 'https://github.com/neomjs/neo/pull/14966'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #14949

The choreography consumer: the non-FLIP motion classes riding the merged #14947 contract — every duration/easing reads `--dock-transition-*` ONLY, every routed motion opens/closes the `DockMotionSignal` window, `prefers-reduced-motion` collapses everything through the token, and zero motion state touches documents/previews/persistence (JSON-first guardrail).

Evidence: L2 (224/224 dashboard unit incl. 3 new choreography specs; 11/11 dashboard e2e incl. the reveal journey exercising the overlay signal path live and the drag journeys exercising splitter suppression) → L3-visual deferred by sandbox ceiling (the pixel judgment — does the glide FEEL right — is a browser/human gate per the design-render rule; routed in Post-Merge Validation, not self-claimed). Residual: the visual pass, named below.

## AC map

- **Splitter size ease** → split children permanently carry `neo-dashboard-dock-motion` (adapter projection seam — a committed flex change glides by itself); live pointer drags suppress via the splitter's capability-optional `setChildMotionSuppressed()` (duck-typed parent contract honored); the workspace opens the signal window on committed `resizeSplit` and closes it on a locally-mounted `transitionend` (flex-grow filtered).
- **Auto-hide rail slide** → the reveal overlay animates FROM its owning edge (the per-edge cls the rail already stamps picks the keyframe); `display:none` hidden-state restarts the animation natively per reveal; signal enter on hidden→visible flips (never double-enters across visible-state shifts), filtered `animationend` leave, dismiss = instant cut with no signal.
- **Tab insert morphing** → entering tab headers settle in via a self-completing CSS animation scoped to dock surfaces (fires on insertion — no toggle, no timer). Commit-correlation signal for structural ops is the FLIP layer's seam per the signed #14779 disposition (#14929 owns it) — recorded on the ticket.
- **Arrival settle** → the motion substrate ships (`neo-dashboard-dock-arrival` + keyframes, transform/opacity/outline only); the TOGGLE re-homes to #14769 whose arrival wiring is the trigger's only product path (ticket-reconciled — an event-toggle against a nonexistent hook would be dead code).
- **Token-only durations** → review-checkable: zero duration/easing literals at any call site; the two DEAD pre-contract duration vars in the landed overlay CSS (`--neo-dock-reveal-ms`/`--neo-dock-dismiss-ms`, zero consumers) are deleted — the contract's single-source claim now holds repo-wide.
- **Reduced-motion** → token collapse covers transitions AND animations by construction (0ms animations complete instantly); no per-site checks exist.
- **Zero document state** → review-checkable; nothing here reads or writes dock documents.
- **Cross-family review** → requested.

## Deltas

- `resources/scss/src/dashboard/Container.scss` — the four choreography classes + keyframes (token-pure); dead pre-contract duration vars deleted
- `src/dashboard/DockLayoutAdapter.mjs` — split children carry the motion class (projection seam)
- `src/dashboard/DockSplitter.mjs` — `setChildMotionSuppressed()` + drag-lifecycle wrap (capability-optional per the duck-typed parent contract)
- `src/dashboard/DockRevealOverlay.mjs` — signal enter on hidden→visible, locally-mounted filtered `animationend` leave
- `examples/dashboard/dock/MainContainer.mjs` — the reference workspace's signal window for committed resizes (locally-mounted `transitionend`, flex-grow filtered)
- Specs: adapter motion-class projection, splitter suppression toggle (incl. the no-throw capability-optional pin), overlay signal lifecycle (enter/filtered-leave/no-double-enter/dismiss-silent)

## Test Evidence

```
npm run test-unit -- test/playwright/unit/dashboard/ --workers=1
224 passed
NEO_E2E_PORT=8093 npx playwright test dashboard -c test/playwright/playwright.config.e2e.mjs --workers=1
11 passed
```

## Post-Merge Validation

- [ ] Visual pass on `examples/dashboard/dock` (browser gate — operator or browser-capable peer): NL `resizeSplit` glides, reveal slides from its edge, entering tabs settle, live splitter drag tracks 1:1 with zero lag.
- [ ] #14769 toggles `neo-dashboard-dock-arrival` at its arrival seam (one line, named in its scope).
- [ ] The #14591-successor authors `observe_motion` assertions against the now-emitting `neo-dashboard-dock-animating` signal.
- [ ] #14929's FLIP consumer amendments route the same signal (the owner's recorded ledger).

Process note: authored during the operator-granted temporary Fable 5 window.

Authored by Grace (Claude Fable 5, Claude Code). Session ef6b9a4a-54ec-4afb-8438-f89a3ee46ad2
