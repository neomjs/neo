---
id: 17419
title: Route Dock tab close actions through model policy
state: OPEN
labels:
  - enhancement
  - ai
  - testing
  - core
  - accessibility
assignees: []
createdAt: '2026-08-20T13:54:31Z'
updatedAt: '2026-08-20T13:54:31Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17419'
author: neo-gpt-emmy
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[ ] 17418 Add flat actions to TabContainer header toolbars'
blocking: []
---
# Route Dock tab close actions through model policy

## Context

Discussion #17415 graduates DockLayout integration as the second bounded leaf after the generic flat TabContainer-actions contract.

The Dock item schema already carries `closable`, but only through the serialization allow-list. No runtime path reads it. `DockZoneModel.closeItem` currently permits every item, and projected tab chrome must never remove a pane before the committed Dock document changes.

This ticket is blocked by **#17418**.

Live latest-open sweep and exact-title searches were clean at creation time; no equivalent open ticket or PR exists.

## The Problem

An optional persistent close action in a projected TabContainer needs Dock semantic authority:

- the active Dock item must be resolved at dispatch time, after activation or reorder;
- explicit `closable: false` must fail closed in the operation layer, not only disappear visually;
- an absent `closable` field must preserve current behavior and remain closeable;
- the model must commit exactly once before projection changes chrome;
- closing the focused active item must land focus on a deterministic successor rather than `document.body`.

A direct `TabContainer.removeAt()` would let runtime chrome outrun model truth and would bypass serialized policy.

## The Architectural Reality

- `DockZoneModel.closeItem` is the semantic mutation authority.
- `DockLayoutAdapter` / projection config owns the runtime action handler and visibility derived from current item policy.
- `DockProjectionReconciler` owns chrome convergence after the document commit.
- `closable` adds no schema field or migration; this ticket gives an existing serialized field runtime meaning.
- The generic action capability emits intent and preserves explicit handler precedence. Dock supplies the effect without serializing functions into the document.

## The Fix

1. Add an optional DockLayout/adapter configuration that contributes one persistent close action to projected TabContainer headers.
2. Resolve the active `itemId` at action dispatch time and route exactly one existing `applyDockZoneOperation({operation: 'closeItem', itemId})` intent.
3. Enforce `closable` inside `DockZoneModel.closeItem`: absent means allowed; explicit false returns a named failure with no document change.
4. Project the close action as unavailable/hidden for explicit false and no-active-item states, while treating the model check as authoritative.
5. Let reconciliation remove pane/header chrome only from the committed document.
6. After closing a focused active item, focus the successor tab header; if none remains, focus the TabContainer root.
7. Prove reorder, Overflow, orientation, retained-topology, cross-stack, and tear-out paths do not capture stale indices or double-commit.

## Contract Ledger

| Target surface | Source of authority | Proposed behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Dock close action config | Dock adapter/runtime config | optional persistent flat action; disabled by default | omitted config preserves current UI | adapter JSDoc | projection specs |
| Active target | committed Dock document + live projection | resolve current `itemId` at dispatch | no active item = no-op/unavailable | handler JSDoc | reorder-before-click arm |
| `closable` policy | `DockZoneModel.closeItem` | absent allows; explicit false returns named refusal | current documents remain closeable | model JSDoc | direct operation specs |
| Commit ordering | Dock host operation pipeline | one semantic commit, then reconciliation | operation failure leaves chrome untouched | reconciliation docs | trace/count witness |
| Focus successor | TabContainer/Dock projection | successor tab header, else TabContainer root | never `document.body` | focus policy JSDoc | pointer/keyboard close matrix |
| Serialized document | existing item allow-list | no new field and no function/action config persisted | N/A | N/A | round-trip equality |

## Decision Record impact

None. The ticket assigns runtime semantics to the existing `closable` field without changing the serialized Dock schema.

## Decision Record

Not needed, inherited from Discussion #17415.

## Signal Ledger

- GPT author family: low-blast graduation fold in D#17415.
- Claude non-author family: substantive peer cycle at `DC_kwDODSospM4BFBWY`.

## Unresolved Dissent

None.

## Unresolved Liveness

None for this low-blast gate.

## Discussion Criteria Mapping

- OQ5 → generic intent, Dock-owned explicit effect.
- OQ6 → absent=true and explicit-false model refusal.
- OQ7 → successor header/root focus.
- OQ8 → every orientation.
- OQ10 → action-space/local-sort geometry remains distinct from outer Dock boundaries.
- Invariants 8–10 → semantic commit authority and dispatch-time identity.

## Acceptance Criteria

- [ ] Feature disabled: projected Dock tabs render no close action and current behavior is unchanged.
- [ ] Feature enabled: one persistent close action is visible whenever an active, closeable item exists, independent of contextual focus state.
- [ ] `DockZoneModel.closeItem` accepts missing `closable` and rejects explicit false with a stable named error and byte-identical document.
- [ ] Projection visibility mirrors policy, but a forged/direct operation against explicit false still fails at the model layer.
- [ ] The handler resolves the current active `itemId` at dispatch time after activation/reorder and commits exactly one `closeItem` operation.
- [ ] Failed operations leave pane, tab header, active index, and focus unchanged; successful operations update chrome only through reconciliation.
- [ ] Closing the first, middle, last, only, and reordered active item selects the correct surviving item and never closes a stale target.
- [ ] Focus moves to the successor tab header, or to the empty TabContainer root when no tab remains; it never falls to `document.body`.
- [ ] Top/right/bottom/left, Overflow-active, retained-topology, cross-stack, and tear-out/re-entry cases preserve action placement and exactly-once semantics.
- [ ] No handler/function/action config enters the serialized Dock document.
- [ ] Focused unit/model/reconciliation coverage plus a Whitebox E2E close journey pass at exact head.

## Out of Scope

- Generic toolbar/TabContainer action materialization (owned by #17418).
- Custom application action semantics.
- Inline close icons per tab.
- Changing the Dock document schema or adding a migration.
- Automatic persistence of action configs/functions.
- Broad focus or drag-system refactoring.

## Avoided Traps

- **Direct `TabContainer.removeAt()`:** removes chrome before model truth and bypasses policy.
- **Visual-only `closable`:** hides one button but leaves every operation caller able to close.
- **Captured active index:** closes the wrong item after reorder or activation.
- **Chrome-first focus repair:** can focus an element reconciliation immediately destroys.
- **Absent=false default:** would silently make every existing Dock item non-closeable.

## Related

- Discussion #17415
- Blocked by #17418
- Grace peer cycle: https://github.com/neomjs/neo/discussions/17415#discussioncomment-18093464

Origin Session ID: `0f8b5b8e-3f01-45c8-889e-1c2fd90b0584`

Retrieval Hint: "DockLayout TabContainer close action closable semantic model focus successor"

— **Emmy (GPT-5.6 Sol Ultra, Codex)** · `@neo-gpt-emmy` 🪡

## Timeline

- 2026-08-20T13:54:33Z @neo-gpt-emmy added the `enhancement` label
- 2026-08-20T13:54:33Z @neo-gpt-emmy added the `ai` label
- 2026-08-20T13:54:33Z @neo-gpt-emmy added the `testing` label
- 2026-08-20T13:54:33Z @neo-gpt-emmy added the `core` label
- 2026-08-20T13:54:33Z @neo-gpt-emmy added the `accessibility` label
- 2026-08-20T13:54:39Z @neo-gpt-emmy marked this issue as being blocked by #17418

