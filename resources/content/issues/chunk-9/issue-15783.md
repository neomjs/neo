---
id: 15783
title: Measure unit-suite CI wall-clock with workers 4
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
assignees:
  - neo-opus-vega
createdAt: '2026-07-24T09:12:14Z'
updatedAt: '2026-07-24T09:27:17Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15783'
author: neo-opus-vega
commentsCount: 4
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-24T09:27:17Z'
---
# Measure unit-suite CI wall-clock with workers 4

## Context

Operator-named friction (2026-07-24 morning, continuing the 2026-07-23 discussion): the CI unit job takes ~10 minutes and is the longest pole on every non-docs PR. Evidence gathered in-session today: two recent successful runs measured 9:56 and 10:36 wall with setup (checkout + `npm ci` + parse5 bundle) only ~25s — the suite run IS the wall. The unit tree holds 820 spec files (560 under `unit/ai`). The decisive fact: `test/playwright/playwright.config.unit.mjs:25` sets `workers: process.env.CI ? 1 : undefined` — the entire suite runs single-worker in CI on a 4-vCPU public runner.

`git log -L` archaeology: that line is unchanged since the config's creation (2025-10-12, `#7471` granular-config split) — born-conservative, no recorded incident behind it. The isolation-by-construction substrate postdates it: per-process Chroma DB isolation (the `#12335` orphan-incident fix), `UNIT_TEST_MODE` isolation by construction (ADR 0019 B4), and Playwright's per-file worker processes all landed after that default was chosen.

## The Problem

Every code PR pays ~10 minutes of serial test execution whose parallelism ceiling was set once, two model-generations of substrate ago, and never revisited. Before any structural lever (sharding, suite splitting, changed-file selection — each with real cost or risk), the zero-cost lever is unmeasured: raise `workers` on the same runner. This ticket is the measurement, not the decision.

## The Architectural Reality

- `test/playwright/playwright.config.unit.mjs` — `fullyParallel: true`, `retries: process.env.CI ? 2 : 0`, `workers: process.env.CI ? 1 : undefined`; projects `unit` (Body, pure Node) and `unit-brain` (`/ai/` specs, Chroma via `chroma-setup` dependency).
- Playwright workers are OS processes; cross-file singleton state does not collide. The plausible hazards under `workers: 4` are: the SHARED Chroma instance behind `unit-brain` (mitigated by per-process DB naming from the `#12335` fix), any spec binding a fixed port, and wall-clock-sensitive assertions on a loaded runner.
- `retries: 2` is already active in CI, so a latent cross-file collision surfaces as retry telemetry in the JSON report rather than a silent red.
- GitHub-hosted `ubuntu-latest` for public repos: 4 vCPU — headroom for 4 workers exists.

## The Fix (measurement protocol)

1. Probe PR changing exactly one line: `workers: process.env.CI ? 4 : undefined`.
2. Let the standard `test.yml` matrix run the FULL unit suite at the probe head (the config file is a non-docs path, so the classifier runs unit unconditionally); re-run the job for a second sample.
3. Record wall-clock + retry/flake counts for ≥2 probe runs against the ≥2 baseline runs cited above (9:56, 10:36).
4. Post the verdict on this ticket:
   - **Wall-clock materially down AND no new flake class** → hand the probe PR to the team + operator as an ordinary merge decision.
   - **Any repeatable cross-file collision** → close the probe PR unmerged and file the specific isolation defect it exposed (also a win — it names real, currently-masked coupling debt).

## Acceptance Criteria

- [ ] Probe PR diff is exactly the one `workers` line in `playwright.config.unit.mjs`.
- [ ] ≥2 full unit CI runs at the probe head with wall-clock + retry counts recorded on this ticket next to the baseline numbers.
- [ ] A verdict comment routes the outcome: merge-decision handoff (operator + team) OR probe closed with named isolation-defect ticket(s).
- [ ] No silent merge: the probe PR merges only through the ordinary review + human merge gate after the verdict.

## Out of Scope

- Matrix sharding (doubles runner minutes), classifier suite-splitting (the `unit`/`unit-brain` project boundary already exists for a future 4th matrix row), and changed-file→spec selection (the risky lever — a prior targeted-selection miss on a pinning spec is documented evidence that selection needs transitive import-graph derivation; deliberately deferred).
- Any change to the chroma setup/teardown projects or `retries`.

## Avoided Traps

- Do NOT treat a green probe as team-wide authority — the merge decision stays human; this ticket only converts opinion into data.
- Do NOT tune `workers` and sharding simultaneously — one variable per measurement.

## Related

- `#7471` (config birth, the unrevisited line) · `#12335` (per-process Chroma isolation that makes parallelism plausible) · ADR 0019 B4 (`UNIT_TEST_MODE` isolation by construction) · `#15368` (classifier suite-gating precedent for a future `unit-brain` split, out of scope here).

Decision Record impact: none.

Release classification: not release-blocking (CI throughput; boardless).

Live latest-open sweep: checked latest 20 open issues at 2026-07-24T09:13Z; no equivalent found. A2A claim sweep (all-status, last 12, ~50-min window): no overlapping `[lane-claim]`/`[lane-intent]` (recent claims #15780, #15782 are disjoint).

Origin Session ID: 2cca0fff-6354-4036-bfdd-fc3320938015

Retrieval Hint: `query_raw_memories("unit CI single worker wall-clock workers 4 probe measurement")`

## Timeline

- 2026-07-24T09:14:04Z @neo-opus-vega cross-referenced by PR #15784
- 2026-07-24T09:16:21Z @neo-opus-ada cross-referenced by #15785
### @neo-opus-vega - 2026-07-24T09:21:07Z

## Measurement — sample 1 (head `baa7852938`, run 30081818399, attempt 1)

**Suite wall: 3.6m (9099 passed, 119 skipped) vs single-worker baselines 9:56 / 10:36 → ~2.7× faster.** Job wall 4m21s including setup. Outcome taxonomy at `workers: 4`:

| Class | Spec | Diagnosis |
|---|---|---|
| **1 failed (hard)** | `unit/app/devindex/StoreFilterProfile.spec.mjs:21` ("Profile first-time filter on real …") | Performance-profiling assertion — wall-clock-sensitive by design; under 4-way CPU contention its budget blows. Not a correctness collision. Options for the verdict: parallel-aware budget, or annotate the profiling specs serial/`test.slow`-class. |
| **2 flaky (passed on retry #1)** | `unit/selection/GalleryInternalId.spec.mjs:88` | **Genuine isolation defect exposed:** the retry-1 trace shows `SortZone.spec.mjs:281`'s global `Neo.applyDeltas` override executing inside GalleryInternalId's run — a within-worker CROSS-FILE global bleed (Playwright reuses a worker process across spec files; SortZone patches `Neo.applyDeltas` and does not restore it). Single-worker ordering masked the pairing. This is real, currently-masked coupling debt regardless of the workers decision. |
| | `unit/ai/mcp/server/McpServerListToolsSmoke.spec.mjs:273` | `this.aiConfig.transport` undefined at `BaseServer.mjs:537` — matches the known ESM-import-race flake class already documented for `ai/` unit tests (pre-existing, re-run heals; parallelism raises its incidence). |

The `[GitHub] REST Error / 503 / teapot` noise in the log is expected negative-test fixture output (resilience suites), not failures.

**Interim read:** the speedup is real and large; the one hard fail is a profiling-budget artifact, not corruption; and the measurement has already paid for itself by naming the `SortZone` global-bleed defect. Sample 2 (rerun of the unit job at the same head) triggered — watching for failure-class stability before the verdict comment.

- 2026-07-24T09:24:50Z @neo-opus-vega cross-referenced by #15789
- 2026-07-24T09:25:19Z @neo-opus-vega cross-referenced by #15790
### @neo-opus-vega - 2026-07-24T09:25:40Z

## Measurement — sample 2 (run 30081818399, attempt 2) + VERDICT

Sample 2 (failed-job rerun, same head): **`StoreFilterProfile.spec.mjs:54` failed again on every retry — 748.1ms against its absolute `< 400ms` budget** (comment in the spec: shallow-clone target <400ms vs deep-clone's ~800-1000ms — under 4-way contention the shallow path lands inside the old deep band, so the assertion can no longer discriminate the regression it guards). The suite aborted early after the failure (2137 passed in 46.7s, 7064 did not run, plus one error outside any test), so **sample 1 (3.6m full suite) stands as the timing evidence**; sample 2's value is failure-class stability: the profiling budget is deterministically parallel-incompatible (2/2 attempts, all retries).

### Verdict (per this ticket's protocol — branch 2)

- **Probe PR #15784 closes unmerged.** The ~2.7× wall-clock win (3.6m vs 9:56/10:36) is real and available, but not with an absolute profiling budget in the suite.
- **Named defects filed:**
  - #15789 — SortZone leaks a global `Neo.applyDeltas` override across spec files (correctness defect independent of the workers decision; masked today only by stable single-worker ordering).
  - #15790 — make profiling budgets parallel-compatible (ratio-control or serial `unit-profiling` project); carries the AC to re-land `workers: 4` with ≥2 green full-suite samples once #15789 + the census clear.
- The `McpServerListToolsSmoke` retry-flake matches the known pre-existing `ai/` ESM-import-race class (incidence raised by parallelism; heals on retry; monitored, not newly filed).

**Net for the team decision:** ~6.3 minutes per non-docs PR is recoverable for the cost of two small, independently valuable test-suite fixes. AC 1–3 of this ticket are satisfied (probe diff, ≥2 recorded runs with taxonomy, verdict routing); AC 4 (no silent merge) is satisfied by closing the probe.

### @neo-opus-vega - 2026-07-24T09:26:40Z

## Operator follow-up folded in (2026-07-24): the scope-limiting alternative

@tobiu confirms the missing archaeology: parallel unit runs have a real incident history (the `workers: 1` line's unrecorded *why* — today's probe independently re-derived it as #15789 + #15790 + the ESM-race class). Alternative lever he names: **limit which tests each change triggers** — core changes ⇒ all; `ai/` changes ⇒ no Body tests; possibly finer.

Design note for whoever picks this lane up — the SAFE version is asymmetric and mechanically derivable from the dependency DAG, no per-file selection needed:

- The Brain imports the Body (`ai/**` imports `src/core/Base.mjs` etc.); the Body never imports the Brain. Therefore: `src/**` change ⇒ run Body **and** Brain unit projects (Brain depends on src); `ai/**`-only change ⇒ Brain project only (`unit-brain`) — the Body provably cannot be affected.
- The mechanical slots already exist: `playwright.config.unit.mjs` already splits `unit` (Body, `testIgnore: brainTestMatch`) vs `unit-brain` projects, and `test.yml`'s classifier already does path-gated suite selection (`#15368` precedent). The change is a classifier output pair + `--project` selection or a 4th matrix row.
- Measure before building: the win depends on how the ~10min splits between the 260 Body specs (pure Node, fast) and 560 Brain specs (Chroma-backed) — the per-project timing is extractable from existing CI JSON reports for free.
- The unsafe version is per-file/changed-spec selection — deliberately out of scope here (transitive-import-derivation prerequisite; prior pinning-spec miss documented in this ticket's Out of Scope).

**Priority routing (also per operator):** FM-first stands; none of this blocks it. #15789 and #15790 are small, independently valuable, and claimable by any peer between reviews; the scope-limiting lane above is unclaimed and waits for a bench slot — it upgrades this ticket's verdict from "recover 6.3min via parallelism prerequisites" to a menu of two independent levers.

### @neo-opus-vega - 2026-07-24T09:27:16Z

Measurement complete — all four ACs satisfied: one-line probe diff (PR #15784), two recorded CI samples with failure taxonomy, verdict routed per branch 2 (probe closed unmerged; enablers #15789 + #15790 filed; workers:4 re-land AC lives on #15790), no silent merge. The scope-limiting alternative + its safe asymmetric design and the operator's priority routing (FM-first; these are bench-slot lanes) are recorded above for whoever picks up the next lever.

- 2026-07-24T10:58:28Z @neo-opus-ada cross-referenced by PR #15797
- 2026-07-24T20:04:19Z @neo-opus-ada cross-referenced by PR #15846
- 2026-07-24T20:12:26Z @neo-opus-ada cross-referenced by #15847
- 2026-07-24T20:50:20Z @neo-opus-ada cross-referenced by PR #15853
- 2026-07-24T22:54:07Z @neo-opus-ada cross-referenced by #15861
- 2026-07-24T23:21:15Z @neo-opus-ada cross-referenced by PR #15881
- 2026-07-25T11:55:17Z @neo-kimi-phoebe cross-referenced by PR #15889
- 2026-07-26T12:36:07Z @neo-opus-vega cross-referenced by PR #15983

