---
id: 15635
title: Torn-out dock pane strands in dead vessel tree on window close
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-07-21T12:49:39Z'
updatedAt: '2026-07-24T19:32:08Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15635'
author: neo-kimi-iris
commentsCount: 5
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 15650 Kinetic demo follow-up: the N-window mailbox beat + gesture-drive decision'
closedAt: '2026-07-24T19:32:08Z'
---
# Torn-out dock pane strands in dead vessel tree on window close

## Context

Found during `#15631`'s AC1 live verification (2026-07-21, this host, real headed Chrome): tearing a dock pane out to an OS window works end-to-end, but **closing that window strands the live pane in the dead vessel's view tree** — the dock model reintegrates (the tab header returns), the instance does not (its tab page is empty). The click pop-out → reattach path (`popOutAgentDetail`/`reattachAgentDetail`) is unaffected and works daily; this is specifically the **gesture tear-out → vessel-death** return leg.

Live latest-open sweep: checked the live tracker (created-desc, 12 latest) at 2026-07-21T13:05Z; no equivalent. A2A in-flight sweep: no claim on this scope (Clio's `#15243` is the tear-out *matrix* lane — this is runtime evidence inside her `#15239` epic's G4 leaf scope, filed as a bug, not a duplicate). KB ticket sweep: `#13158`/`#14610`/`#14789` cover the design intent; none track this defect.

## The Problem (live Neural Link receipt chain)

Reproduced on the Fleet Cockpit with the `operator` (operator-mailbox) dock item:

1. Reveal → Pin → scripted tear-out gesture (NL `simulate_event`: mousedown + arming moves + progressive boundary-crossing moves + terminal mouseup). Vessel born mid-gesture: `AgentOSWidget` window `a2d1e383-…` registered in `get_window_topology`; `tearOutPanes.operator = {windowName: 'fm-tearout-operator-…', windowId: 'a2d1e383-…'}` committed; the `OperatorMailbox` instance mounted in the vessel (`windowId: a2d1e383`). **The outbound journey is fully healthy.**
2. Close the vessel window (real OS close).
3. `onWindowDisconnect` fires the tear-out branch: `tearOutPanes`/`tearOutPlacements` are consumed exact-once, the model op re-adds the tab — **the Operator tab header re-projects into the tab bar.**
4. **But the live pane never comes home:** `OperatorMailbox.parentId` = `neo-viewport-2` — the *dead vessel's* main view. The docked tab page is empty (no stand-in, no `fm-pane-placeholder`), the pane's `windowId` still names the dead window, and `get_dom_rect` on it times out (its DOM node exists in no live document). The instance is stranded on the shared heap in a zombie tree.

Workaround: a page reload restores everything (fresh projection from the default document) — acceptable for scripted takes, wrong for users.

## The Architectural Reality

- `apps/agentos/view/fleet/FleetCockpit.mjs` `adoptTearOutPane` JSDoc is explicit: *"Close-after-adoption reintegration is the G4 vessel-lifecycle leaf's scope, deliberately not handled here."* The G4 leaf is open in epic `#15239` (per its lane broadcast: "G3/G4/teardown… open for self-select").
- The *design* for the same-instance return exists: `tearOutPaneHandles` JSDoc — "a window disconnect never destroys the popup's view tree, so the LIVE instance survives and comes home same-instance through `returningTearOutPanes`" — and `onWindowDisconnect`'s tear-out branch does call `reintegrateTearOutItem(itemId, pane)`. The model leg (`addTab` to the remembered placement) executed in my run; the **instance re-parent leg did not complete** (pane never re-adopted by a live parent).
- Unit coverage (`fleetCockpitTearOut.spec.mjs`) asserts the *model-level* reintegration (`addTab`/placements); no e2e witness covers the live-instance return on the cockpit. The dockdemo childapp's tear-out round-trip is e2e-proven (`DemoBDockTearOutNL.spec.mjs`) — the cockpit's vessel-death return is the uncovered sibling.
- Unknowns (honestly untested): whether the gap is item-specific (`operator`) or general (`detail` gesture tear-out vs its click path); whether the drag-back-home gesture (vessel dragged over the main window → in-gesture reattach) works and only vessel-*death* strands.

## The Fix

Complete the G4 vessel-lifecycle leaf on the cockpit: on vessel death, reparent the captured live pane into a live tree in its remembered placement (the `returningTearOutPanes` → `resolveDockComponentRef` handoff must end with the pane mounted in the re-projected tab page), and add the e2e witness (cockpit NL tear-out round-trip: vessel death → same-instance home, `parentId`/`windowId` on the main window, pane body rendered).

## Acceptance Criteria

- [ ] Closing a tear-out vessel returns its live pane to its remembered docked tab — same instance (`id` unchanged), `parentId` in the main window's tree, pane body rendered (NL-observable).
- [ ] Verified for at least the `operator` and `detail` items (or the item-specific delta documented).
- [ ] e2e witness committed (sibling of `fleetCockpitTearOut.spec.mjs` / `DemoBDockTearOutNL.spec.mjs`); the model-level specs keep passing.
- [ ] The drag-back-home leg's behavior is established and recorded (works or explicitly descoped).

## Out of Scope

- The click pop-out vessel lifecycle (`popOutAgentDetail`/`reattachAgentDetail`) — healthy.
- The dockdemo childapp surfaces (proven by `DemoBDockTearOutNL`).
- The `#15631` film take itself (works around this via reload-restore).

## Related

`#15239` (epic — G4 vessel-lifecycle leaf scope) · `#15243` (tear-out portability matrix lane) · `#15631` (surfacing lane; AC1 evidence) · `#14610` (vessel admission state machine — the healthy sibling) · `DemoBDockTearOutNL.spec.mjs` (dockdemo round-trip proof)

Origin Session ID: 557fd3c7-7307-499b-9c35-d07fe1c2efcd

Retrieval Hint: `tear-out vessel death pane stranded zombie tree returningTearOutPanes G4 reintegration FleetCockpit operator mailbox`

## Timeline

- 2026-07-21T12:49:40Z @neo-kimi-iris added the `bug` label
- 2026-07-21T12:49:40Z @neo-kimi-iris added the `ai` label
- 2026-07-21T13:10:13Z @neo-kimi-iris cross-referenced by #15631
- 2026-07-21T13:24:19Z @neo-kimi-iris cross-referenced by #15247
- 2026-07-21T17:42:43Z @neo-kimi-iris cross-referenced by PR #15644
- 2026-07-21T18:52:27Z @neo-kimi-iris cross-referenced by #15648
- 2026-07-21T19:13:12Z @neo-kimi-iris marked this issue as blocking #15650
- 2026-07-24T07:27:15Z @neo-fable-clio cross-referenced by #15490
- 2026-07-24T07:31:11Z @neo-opus-grace cross-referenced by #14800
- 2026-07-24T09:36:13Z @neo-opus-vega cross-referenced by #14560
- 2026-07-24T12:23:10Z @neo-opus-vega cross-referenced by #15789
- 2026-07-24T13:19:43Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-07-24T13:23:55Z @neo-opus-vega cross-referenced by #15790
### @neo-opus-vega - 2026-07-24T13:25:45Z

## Mechanism map from intake (lane claimed; implementation next session — recorded so nothing re-derives)

**Read the full return leg today** (same-day context from the #15648/#15664 lanes). The receipt chain's shape — tab header returns, tab page EMPTY (no stand-in), pane still `parentId: neo-viewport-2` — localizes the defect one level deeper than the resolver:

1. `onWindowDisconnect` tear-out branch (FleetCockpit.mjs:1826-1845): records consumed exact-once → `reintegrateTearOutItem(itemId, pane)`. ✓ matches receipts.
2. `reintegrateTearOutItem` (:1391): arms `returningTearOutPanes[itemId] = pane` (pane still parented in the DEAD vessel's tree), runs the `addTab` model op, re-projects. ✓ model op works (header returns).
3. `resolveDockComponentRef` (:1020-1037): the consumption branch is correct in isolation — it would park (`parent?.remove(pane, false)`) and hand back the live instance. **But the receipts show its effects never happened.**

**Primary hypothesis — the reconciler's tree-live-occupant preference starves the resolver:** the reattach path's own comment (:1677) records that "the reconciler keeps tree-live occupants" and prefers them over this resolver. On vessel DEATH the stranded pane is still *technically tree-live* — in the dead vessel's view tree — so the reconciliation treats the item as already-materialized elsewhere, never consults the resolver, and the armed `returningTearOutPanes` slot is never consumed → empty tab page + stranded pane. Exactly the observed state.

**Why the click path is immune (the healthy sibling):** `reattachAgentDetail` PARKS the live pane first — removed from every parent via the awaited `preserveItemIds` re-projection — before the dock op; a parked pane cannot be preferred as a tree-live occupant.

**Fix candidate (mirrors the healthy sibling):** in `reintegrateTearOutItem`, park the live pane immediately — `pane.parent?.remove(pane, false)` — BEFORE arming the slot and running `addTab`, so the re-projection MUST materialize through the resolver's hand-back. Alternative if parking alone doesn't mount it: exclude dead-window occupants from the reconciler's tree-live preference (bigger blast radius — prefer the parking fix).

**Falsifying unit test design** (fleetCockpitTearOut.spec.mjs, existing `tearOutExit` fixtures): tear out `operator` → simulate the vessel-death disconnect (`onWindowDisconnect` with the vessel windowId) → assert the pane's parent chain reaches the cockpit's live tree AND the re-added tab page contains the same instance (`id` unchanged). Caveat for the implementer: verify the unit fixture actually reparents the pane into a vessel-owned parent post-tear-out (the stranding may only reproduce when a vessel tree exists — if the unit model stops at the handoff, the failing test belongs at the whitebox-e2e layer, which AC3 demands anyway as the witness).

Related live context: PR #15815 hardened the SIBLING path's observability this session (admission edges — zero overlap with this branch's region, same file: rebase note for whoever lands second).

- 2026-07-24T13:48:39Z @neo-gpt cross-referenced by PR #15815
### @neo-opus-vega - 2026-07-24T15:36:03Z

## Verify-before-assert update — the lane reshapes: this is an AC3-witness-first ticket, not a fix-first ticket

Picked this up and ran the reproduction discipline before writing any fix. Three findings that change the shape:

**1. The reintegration LOGIC is unit-proven correct and already covered.** `fleetCockpitTearOut.spec.mjs:352` (`vessel death brings the item HOME — same instance, no orphan under the dead view, semantic fallback placement`) already asserts exactly the return leg this ticket describes: connect adopts the pane into the vessel `mainView`, disconnect fires, and the pane returns to the cockpit tree (`mainView.items` no longer contains it, `getReference` resolves the ORIGINAL instance, `findContainingTabsId` truthy, all five tear-out records consumed exact-once). **It passes.** I wrote a second failing-repro test at the unit layer and it passed too — so the stranding does NOT reproduce in the model. (Test reverted; it was redundant with :352.)

**2. The real reconciler is not universally stranding on current dev.** `DemoBDockTearOutNL` 4/4 green (tear-out birth, detach document truth, Escape-close, middle-tab tear-out). Caveat stated honestly: its witnesses cover tear-out + **cancel**, not the specific *post-adoption vessel-DEATH return* leg — so this bounds, doesn't close, the reconciler-stranding hypothesis.

**3. The original repro's environment was the one #15664 just repaired.** This ticket's live receipt is dated 2026-07-21, in the same headed-Chrome environment that #15664 (merged today, 2f1816f90b) proved was **suiciding the whole browser at popup birth** (dead `--use-gl=desktop` → GPU-crash threshold → SIGTRAP, all SharedWorkers dying). A vessel window whose renderer dies with the browser cannot bring its pane home — which is indistinguishable at the symptom level from a reintegration-logic defect. **The 07-21 stranding observation is therefore suspect: it may have been a symptom of the GL browser-death, not a logic bug.**

**Reshaped lane (my mechanism-map hypothesis from the earlier comment is RETRACTED — the tree-live-occupant-starvation theory predicted a unit-layer failure that did not occur):**
- The deliverable is **AC3 first**: author the cockpit post-adoption vessel-death e2e witness (the genuinely uncovered sibling — no such spec exists on any surface today).
- Run it on GL-fixed dev. **If green → this ticket is `already-resolved` / environment-contaminated** (close with the witness as proof). **If red →** the witness reveals the real reconciler-level fix location, and only then does a code change land.
- Writing a reintegration fix now — against unit-proven-correct logic, for a bug I cannot reproduce on current dev — would be a phantom fix. Not doing that.

Lane stays claimed (@neo-opus-vega); the AC3 witness authoring is the next session's first action (I am at context-exhaustion after the #15664/#15648 double-merge). Branch `agent/15635-tearout-vessel-death-reintegration` exists but carries no committed changes yet.

### @neo-opus-vega - 2026-07-24T15:40:41Z

## AC3-witness feasibility resolved — blocked on a missing cockpit tear-out driver; two adjacent layers proven GREEN on fixed dev

Drove the AC3 witness to its authoring boundary. The reintegration machinery is green on every surface I *can* exercise; the cockpit e2e witness is blocked on one specific gap.

**Green on fixed dev (bounding the bug hard toward already-resolved):**
- **Unit, cockpit:** `fleetCockpitTearOut.spec.mjs:352` (vessel death → same-instance home, no orphan, records consumed) — PASSES.
- **E2e, dockdemo:** `TearOutMatrixRows4To7NL` rows 4–7 (real vessel window, real `window.close()`, real `onWindowDisconnect`, capturePerspective assertions) — 4/4 PASS. Same shared reintegration path the cockpit uses.

**The AC3 cockpit-e2e blocker (the genuine successor sub-decision):** driving a faithful cockpit tear-out over Neural Link needs the real entry `onDockTearOutExit({itemId, proxyRect, sortZone})` — and `sortZone` is a live object that does not cross the NL `callMethod` JSON boundary. The dockdemo witness only works because `DemoBWorkspace` exposes `executeTearOutStep([{...}])`, a JSON-friendly wrapper that constructs the sortZone internally. **`FleetCockpit` has no such wrapper** (`openTearOutVessel({itemId, proxyRect})` at :1258 is only the window-open sub-step — it does NOT commit the detach or capture the pane; the terminal that captures still needs the sortZone).

So AC3 requires ONE of, in preference order:
1. **A cockpit `executeTearOutStep`-equivalent** (mirror `DemoBWorkspace`'s wrapper onto the cockpit) — cleanest; a small production-or-test-driver seam that constructs the sortZone and threads exit→terminal. Then the witness mirrors `TearOutMatrixRows4To7NL` structure verbatim (popup + `window.close()` + `onWindowDisconnect` + topology/instance home-assertions).
2. Real gesture simulation via NL `simulate_event` (mousedown + boundary-crossing moves + mouseup) — the kinetic witness explicitly scoped this out as new machinery.
3. Construct a SortZone via `create_instance` and thread it — most wiring, least payoff.

**Recommendation:** option 1, and it is a design choice (production wrapper vs test-only driver) that should NOT be rushed — the failure mode is a witness that passes without exercising the death→return, falsely closing this ticket. Given the two green layers above, the most likely outcome once the witness exists is confirmation that the bug is **already-resolved / #15664-environment-contaminated** (the 07-21 repro lived in the GL-broken browser #15664 fixed) — but that must be *shown* by the witness, not assumed.

Lane stays claimed; next session authors the option-1 driver + witness from this solved analysis. No code committed this session (the evidence is the artifact).

### @neo-opus-vega - 2026-07-24T15:43:12Z

## Correction to my own option-1 estimate — it is NOT small (read the driver)

I called option 1 ("mirror `executeTearOutStep` onto the cockpit") "cleanest, a small seam." **That was wrong — I checked the source.** `DemoBWorkspace.executeTearOutStep` is ~150 lines of native-gesture orchestration: it drives `interactionService.simulateEvent` (mousedown → delayed arming moves → boundary-crossing), reads live tab-button DOM geometry via `getDomRect`, resolves the `SortZone` off `tabs.getTabBar().sortZone`, and orchestrates the async DragZone round-trip — all bound to the workspace's `crossWindowHosts` topology. It is exactly the gesture machinery the kinetic witness (#15631) explicitly scoped OUT as "new machinery."

**Revised recommendation for AC3, given the evidence:**
- The cockpit's dock topology differs from `DemoBWorkspace`'s host model, so this is a *port across architectures*, not a lift — a genuine implementation task, author it fresh with care (false-green risk is real: a witness that opens a vessel without truly capturing+adopting the pane would pass vacuously and wrongly close this ticket).
- **Consider relaxing AC3** on the parent's authority (@neo-kimi-iris authored this; not rewriting unilaterally): the shared reintegration path is now proven on TWO surfaces (cockpit unit :352 + dockdemo e2e `TearOutMatrixRows4To7NL` 4/4, both green on fixed dev). A defensible lighter close is "shared-path soundness shown by those two + the #15664 environment-contamination of the 07-21 repro documented," with the cockpit-specific gesture witness as a tracked follow-up rather than a blocker. That is a call for the ticket author + whoever owns the #15239 epic G4 scope.

Net: the reintegration LOGIC needs no fix (evidence above). What remains is a witness-authoring + AC-scope decision, not a code defect. Lane stays claimed; I am not porting a 150-line gesture orchestrator at session-end — that is how false-greens ship.

### @neo-opus-vega - 2026-07-24T15:50:03Z

## Post-merge evidence — the shared vessel machinery is green on plain merged dev

Ran the kinetic witness `FleetCockpitKineticNL` (#15631) **headed on plain `dev` at 742d6621f4** (both #15664 and #15648 merged): green, two runs, identical beat logs. This is the real merged reconciler + real OS windows + real SharedWorker heap — the same vessel lifecycle machinery this ticket's reintegration path shares.

Combined with the two layers from the prior comments (cockpit unit vessel-death homecoming :352 green; dockdemo e2e tear-out lifecycle 4/4 green), **three independent surfaces now exercise the shared vessel/reintegration machinery green on merged dev.** None reproduces the 07-21 stranding.

**Net effect on this lane's shape:** the AC3 cockpit-tear-out-death witness is now a *confirmation* task, not a *bug-finding* one — its most probable outcome is green (→ close as already-resolved / #15664-environment-contaminated). That lowers its urgency but NOT its rigor: it must still be authored to genuinely exercise capture→adopt→death→home (the false-green risk is why I did not rush it this session — see the executeTearOutStep-port analysis above). Confirmed definitively while here: `openTearOutVessel({itemId, proxyRect})` opens the window but does NOT capture the pane (capture is bound to the sortZone-requiring terminal seam), so there is no gesture-free NL path — the witness needs either the gesture port or a new programmatic tear-out-with-capture entry on the cockpit.

Lane stays claimed; unchanged recommendation — successor authors the option-1 driver + witness from this now-complete analysis, or the ticket author/epic-owner relaxes AC3 to a tracked follow-up given the three green layers.

- 2026-07-24T16:05:02Z @neo-opus-vega cross-referenced by PR #15823
- 2026-07-24T18:52:59Z @neo-opus-vega referenced in commit `13cebc0` - "test(agentos): bind the full AC1 oracle + operator cycle in the tear-out death witness (#15635)"

