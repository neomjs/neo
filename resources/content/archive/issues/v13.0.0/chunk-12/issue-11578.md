---
id: 11578
title: Extend unit-test setup.mjs to mock Neo.Main and addon.LocalStorage
state: CLOSED
labels:
  - enhancement
  - developer-experience
  - ai
  - testing
  - core
assignees:
  - neo-gpt
createdAt: '2026-05-18T09:51:24Z'
updatedAt: '2026-08-24T19:34:17Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11578'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-05-18T11:54:54Z'
---
# Extend unit-test setup.mjs to mock Neo.Main and addon.LocalStorage

## Context

Surfaced from downstream dogfooding: a Neo.mjs downstream application (a private tenant project, dogfooding the in-progress v13 dev branch) tried to write its first Playwright unit test that instantiates the app's root `Neo.container.Viewport` subclass via `Neo.create()`. The class loads and its static config verifies correctly, but `Neo.create()` blows up at construction time inside Neo's framework code — not in the downstream app's class — because `test/playwright/setup.mjs` does not mock two surfaces that any production-shaped controller will hit.

This is the second-and-third recurrence of a pattern we already know: framework test mocks that live ad-hoc in per-spec `beforeEach` (see Related), and never make it into the canonical `setup.mjs` until friction surfaces in another spec. Lifting these into `setup.mjs` now is cheap; the alternative is every downstream consumer rediscovering the same gap.

## The Problem

Empirically reproduced via `npm run test-unit` against a minimal sanity spec:

```
TypeError: Cannot read properties of undefined (reading 'setRoute')
    at ViewportController.initAsync (node_modules/neo.mjs/src/controller/Base.mjs:134:57)
    at node_modules/neo.mjs/src/core/Base.mjs:306:13
```

The failing line in `src/controller/Base.mjs` is:

```js
!Neo.config.hash && defaultHash && Neo.Main.setRoute({value: defaultHash, windowId})
```

After stubbing `Neo.Main`, the next failure cascades into `onConstructed` accessing `Neo.main.addon.LocalStorage.readLocalStorageItem({...})`. Neo's stock `setup.mjs` mocks `Neo.main.addon` with `DragDrop`, `Navigator`, and `ResizeObserver` — but no `LocalStorage`.

**Why Neo's internal suite doesn't trip these:** the framework's own unit tests deliberately instantiate stateless or controller-less components (`Neo.core.Base`, `Progress`, plain `Container` populated with buttons). The gap is invisible from inside the framework but immediate for any downstream app whose root viewport has a real controller — i.e., effectively every non-trivial Neo application.

## The Architectural Reality

- `test/playwright/setup.mjs` exports a `setup(options)` helper that mocks `Neo.config`, `Neo.applyDeltas`, `Neo.main`, `Neo.currentWorker`, `Neo.worker` so unit tests can run in a single Node thread without a real worker bridge.
- `Neo.Main` (capital M) is the main-thread proxy used by `Neo.controller.Base.initAsync` for routing. It is structurally distinct from `Neo.main` (lowercase) which holds addon facades. The two share a naming root but live on separate substrate surfaces — both are runtime-only, neither is currently mocked in unitTestMode.
- `Neo.main.addon.LocalStorage` is the addon facade for the persistent-storage main-thread service. Any controller that persists theme/route/filter state will reference it during `onConstructed` or async setup hooks.
- The existing `setup(options)` shape already accepts an options bag (`neoConfig`, `appConfig`), so extending it with feature-flagged mocks is consistent with the established API.

## The Fix

Extend `test/playwright/setup.mjs` to provide stock mocks for both surfaces, gated on opt-out flags so existing tests that intentionally test routing/storage behavior aren't perturbed:

```js
export function setup(options = {}) {
    const {
        neoConfig    = {},
        appConfig    = {},
        mockMain     = true,   // new
        mockLocalStorage = true // new
    } = options;

    // ... existing setup ...

    if (mockMain) {
        Neo.Main ??= {
            setRoute: () => {}
        };
    }

    if (mockLocalStorage) {
        Neo.main.addon.LocalStorage ??= {
            readLocalStorageItem  : async () => ({value: null}),
            updateLocalStorageItem: async () => {},
            removeLocalStorageItem: async () => {}
        };
    }
}
```

Both mocks use `??=` so a caller that pre-installs richer mocks (e.g., a test that exercises route dispatch end-to-end) wins.

Optional follow-up: factor each mock into a named export (e.g., `mockNeoMain()`, `mockLocalStorage()`) so downstream apps can compose them à la carte without re-invoking the full `setup()` cycle. Not required for this ticket.

## Acceptance Criteria

- [ ] `test/playwright/setup.mjs` provides default stub for `Neo.Main.setRoute`, opt-out via `mockMain: false`.
- [ ] `test/playwright/setup.mjs` provides default stub for `Neo.main.addon.LocalStorage` (read / update / remove), opt-out via `mockLocalStorage: false`.
- [ ] Both mocks use `??=` so caller pre-installed mocks take precedence.
- [ ] A regression spec covers the smoke path: instantiating a `Neo.container.Viewport` subclass with a `Neo.controller.Component` controller that declares a `defaultHash` succeeds via `Neo.create()` and tears down cleanly via `destroy()`.
- [ ] `learn/guides/testing/UnitTesting.md` updated to reference the new flags and the "downstream viewport instantiation" use case.

## Out of Scope

- A full extracted "extended setup helper" for downstream consumers (e.g., a client project-side wrapper). Downstream apps can still build their own helpers on top; this ticket centralizes only the universally-needed mocks.
- Mocking `Neo.Main` beyond `setRoute` (e.g., the full worker-bridge protocol). The opt-out flag lets downstream specs install richer mocks when they need to.
- Refactoring `Neo.controller.Base.initAsync` to be defensive about a missing `Neo.Main`. Routing IS a runtime concern; the test environment should provide the stub, not the runtime code.

## Avoided Traps

- **"Mock everything by default with deeply structured stubs."** Rejected — increases setup-time cost and obscures genuine missing-dep failures. Minimal stubs (no-op functions, `{value: null}` returns) are sufficient for the failure modes observed; deeper behavior is downstream's responsibility.
- **"Make `Neo.Main.setRoute` no-op when `unitTestMode` is true inside `controller/Base.mjs`."** Rejected per Out of Scope §3. Runtime code should not branch on test mode; that bleeds the test-mode boundary into runtime substrate.
- **"Wait until v13 stabilizes to revisit test infra."** Rejected — the friction blocks downstream v13 adopters writing their first viewport spec right now. Cheap fix, immediate ROI.

## Related

- **#9443** (closed) — "Stabilize Playwright Unit Tests by Centralizing Global Mocks". Originally proposed centralizing mocks for `Neo.main`, `Neo.currentWorker`, `Neo.worker`, `Neo.applyDeltas` in `setup.mjs`. Pivoted mid-flight to a different root cause (`trap()` for delayed async imports on destroyed components) and closed without landing the centralized-mock surface this ticket targets. `Neo.Main.setRoute` and `Neo.main.addon.LocalStorage` were never on its list. This ticket explicitly fills that gap.
- **#8101** (closed, v11.17.0) — Earlier per-spec mock of `Neo.main.addon.DragDrop.setDragProxyElement` inside a single test's `beforeEach`. Second datapoint that per-spec ad-hoc mocks repeat across the suite; lifting universally-needed surfaces into `setup.mjs` is the substrate-correct primitive.

## Handoff Retrieval Hint

`Retrieval Hint: "setup.mjs mock Neo.Main setRoute LocalStorage downstream viewport unit test"`

## Provenance

Origin: downstream dogfooding session in a private tenant project (privately hosted, dogfooding the Neo.mjs v13 dev branch). No Neo Memory Core session ID — the surfacing context lives outside the canonical swarm.


---
*Redaction note (2026-08-24, @neo-opus-ada): a private tenant's name, repository path and hosting platform were replaced with generic equivalents. No technical claim, measurement or attribution was altered.*


## Timeline

- 2026-05-18T09:51:26Z @neo-opus-ada added the `enhancement` label
- 2026-05-18T09:51:26Z @neo-opus-ada added the `developer-experience` label
- 2026-05-18T09:51:26Z @neo-opus-ada added the `ai` label
- 2026-05-18T09:51:26Z @neo-opus-ada added the `testing` label
- 2026-05-18T09:51:26Z @neo-opus-ada added the `core` label
- 2026-05-18T10:22:58Z @neo-gpt assigned to @neo-gpt
- 2026-05-18T10:30:20Z @neo-gpt cross-referenced by PR #11579
- 2026-05-18T10:47:41Z @neo-gpt referenced in commit `8fde258` - "feat(test): mock main route and local storage in setup (#11578)"
- 2026-05-18T10:47:41Z @neo-gpt referenced in commit `fac9152` - "fix(test): use Neo namespace helper in setup mocks (#11578)"
- 2026-05-18T11:54:54Z @tobiu closed this issue
- 2026-05-18T11:54:55Z @tobiu referenced in commit `bcd8611` - "feat(test): mock main route and local storage in setup (#11578) (#11579)

* feat(test): mock main route and local storage in setup (#11578)

* fix(test): use Neo namespace helper in setup mocks (#11578)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-19T03:36:51Z @neo-opus-ada referenced in commit `8393cb6` - "fix(test): seed @neo-test fixture identities for new validateMailboxTarget guard (#11417)

PR #11615's `validateMailboxTarget` guard correctly rejects unrecognized `to:`
values that previously stored as silent `to: null` orphans. Two pre-existing
test fixtures used the placeholder pattern that exercised the orphan bug
behavior (passing because of silent-null storage); the new validator surfaces
the latent fixture gap.

Fixes:

- `test/playwright/unit/ai/services/memory-core/WriteSideInvariant.spec.mjs:117`:
  the positive-case test sends to `@neo-test-receiver` but only seeded
  `@neo-test-writer` and `AGENT:*`. Added seed for `@neo-test-receiver` so the
  happy path exercises both endpoints registered.
- `test/playwright/unit/ai/scripts/trioWakeCooldown.spec.mjs`: subprocess invokes
  `MailboxService.addMessage({to: '@neo-test', ...})` per the signal payload.
  Added `beforeAll` hook that imports LifecycleService + GraphService, calls
  `initAsync`, and seeds `@neo-test` as an AgentIdentity node visible to the
  subprocess via the shared SQLite graph file.

Verification:
- `npm run test-unit -- WriteSideInvariant.spec.mjs` → 3/3 PASS (619ms)
- trioWakeCooldown.spec.mjs blocked locally by pre-existing `Neo.ns is not a
  function` setup-time issue (introduced by PR #11578 `mock main route and
  local storage in setup`) when run in isolated worker without prior Neo import;
  CI uses `workers: 1` so prior spec's Neo load persists into the same worker —
  CI verification will confirm the fix."
- 2026-05-19T03:37:13Z @neo-opus-ada cross-referenced by PR #11615
- 2026-05-19T03:49:23Z @neo-opus-ada referenced in commit `d491a50` - "fix(test,mcp): replace `@neo-test` with canonical identity + compact openapi to: description (#11417)

Cycle 2 addresses GPT review-hold (MESSAGE:ed55919e) on PR #11615 head 8393cb629.

Two surfaces:

### 1. trioWakeCooldown.spec.mjs — canonical-identity fix

Previous Cycle 1 attempt at `8393cb629` added a `beforeAll` that called
`GraphService.upsertNode({id: '@neo-test', ...})` from the fixture process.
That approach was wrong: the subprocess runs with `UNIT_TEST_MODE='true'` →
graph storage is `:memory:` per `config.template.mjs:214` (per-process), so
fixture-process seeding cannot reach the subprocess.

Corrected approach: use a canonical seeded identity (`@neo-opus-4-7`) that
`seedAgentIdentities.mjs` provisions at the subprocess's
`LifecycleService.initAsync` boot. The subprocess's fresh `:memory:` DB has
the canonical identities available; the validator passes.

Removed the unused `beforeAll` hook + replaced `@neo-test` with
`TEST_COORDINATOR = '@neo-opus-4-7'` in all 3 test signal payloads.

### 2. openapi.yaml `to:` description — MCP-tool-description budget compliance

Per `.agents/skills/pr-review/audits/mcp-tool-description-budget.md` (referenced
in pr-review-guide §5.3): tool descriptions are loaded into every consuming
agent's context window when enumerated; bloat competes with reasoning budget.
Previous description was multi-line block-literal (~280 bytes) with an internal
`#11417` ticket reference and "prior to this guard" archaeology — violating
the audit criteria.

Compacted to single-line (289 chars; well under 1024 cap), removed the ticket
reference + the pre-fix archaeology. Describes call-site usage only: canonical
form + AGENT:* sentinel + alias resolution policy.

Verification:
- `npm run test-unit -- MailboxService.spec.mjs WriteSideInvariant.spec.mjs` → 62/62 PASS (1.4s)
- `node ai/scripts/lint-agents.mjs --base origin/dev` → OK
- `node ai/scripts/check-substrate-size.mjs` → PASS
- `node buildScripts/util/check-whitespace.mjs` → PASS

Not in scope: `wakeSafetyGate.spec.mjs` Neo.ns setup failure noted in GPT's
review-hold is a separate test-infra issue (PR #11578 setup mock requires
prior Neo import; trioWakeCooldown failure was likely the cascade-trigger).
If CI re-run on this Cycle 2 head still surfaces wakeSafetyGate Neo.ns
failure independently, separate follow-up ticket warranted."
- 2026-05-19T04:02:49Z @neo-gpt cross-referenced by #11617
- 2026-05-19T04:24:43Z @neo-opus-ada cross-referenced by PR #11618
- 2026-05-19T04:28:00Z @neo-gpt cross-referenced by #11619
- 2026-05-19T04:40:42Z @neo-opus-ada cross-referenced by PR #11620
- 2026-05-19T06:36:10Z @tobiu referenced in commit `3748019` - "fix(memory-core/mailbox): reject or resolve invalid `to:` in add_message instead of silent null-storage (#11417) (#11615)

* fix(memory-core/mailbox): reject or resolve invalid `to:` in add_message instead of silent null-storage (#11417)

Pre-fix, `add_message` accepted any `to:` string and silently stored a null
SENT_TO edge target when the value didn't match a registered AgentIdentity node.
The MESSAGE node retained the malformed value as its `to:` property; the missing
SENT_TO edge surfaced as `to: null` in `get_message` reads. Orphan messages
became invisible to the intended recipient, breaking cross-skill A2A
coordination (`/lead-role`, `/peer-role`, `/pull-request`, `/pr-review`).

Empirical anchor: ~17 A2A messages sent with alias-format
`to: "AGENT:<family>/<model>"` (confab from the openapi schema's lone
`'AGENT:*'` example) silently orphaned in the 2026-05-15 session.

Changes (3 independent improvements; shipped together for substrate coherence):

1. `ai/services/memory-core/MailboxService.mjs` — new `validateMailboxTarget`
   helper called after `normalizeMailboxTarget` and BEFORE the SENT_TO
   `linkNodes` call. Resolves known `AGENT:<family>/<model>` aliases against
   `AgentIdentity.modelFamily` when exactly one match exists; rejects ambiguous
   matches and other unresolvable formats with clear errors. Fires BEFORE the
   BLOCKED_BY / CAN_REPLY_TO permission checks so failures surface as
   substrate-level errors rather than permission-shaped ones. Cache-warm retry
   mirrors GraphService.linkNodes #10347 discipline.

2. `ai/mcp/server/memory-core/openapi.yaml` — team-agnostic `to:` description
   naming canonical `@<identity>` form, the `AGENT:<family>/<model>` resolution
   policy, and the pre-#11417 silent-null failure mode. Placeholders use
   `@<peer-agent>` (no neo-X identities hardcoded) per ticket Avoided Traps —
   framework substrate must remain forkable.

3. Skill substrate worked-examples in `lead-role-mode.md` §2.2,
   `peer-role-mode.md` §6.5, and `review-response-protocol.md` §14 — concrete
   `add_message` code blocks with team-agnostic placeholders demonstrating the
   canonical `to: '@<peer-agent>'` form + the rejected alias-confab pattern.

Plus 3 new regression tests under
`test.describe('#11417 add_message reject/resolve invalid to:')`:
- reject path: 'AGENT:nonsense/blob' → throws "Unrecognized 'to' format"
- resolve path: AGENT:<family>/<model> with single match → SENT_TO edge points
  at resolved canonical identity; MESSAGE.to reflects resolved form (not alias,
  not null)
- ambiguous path: multiple AgentIdentities sharing modelFamily → throws
  "Ambiguous 'to' alias"

Incidental: stripped pre-existing trailing-whitespace lines in
MailboxService.mjs, MailboxService.spec.mjs, and openapi.yaml to clear husky
check-whitespace pre-commit gate.

Verification:
- `npm run test-unit -- MailboxService.spec.mjs --grep #11417` → 3/3 PASS (802ms)
- `npm run test-unit -- MailboxService.spec.mjs` → 59/59 PASS (1.2s, no regression)
- `node ai/scripts/lint-agents.mjs --base origin/dev` → OK
- `node ai/scripts/check-substrate-size.mjs` → PASS
- `node buildScripts/util/check-whitespace.mjs` → PASS

Out of Scope (per ticket):
- Migration of historical orphan records (forensic preservation; separate ticket)
- Full alias registry beyond AGENT:<family>/<model>
- Cross-harness wake-delivery infrastructure

* fix(test): seed @neo-test fixture identities for new validateMailboxTarget guard (#11417)

PR #11615's `validateMailboxTarget` guard correctly rejects unrecognized `to:`
values that previously stored as silent `to: null` orphans. Two pre-existing
test fixtures used the placeholder pattern that exercised the orphan bug
behavior (passing because of silent-null storage); the new validator surfaces
the latent fixture gap.

Fixes:

- `test/playwright/unit/ai/services/memory-core/WriteSideInvariant.spec.mjs:117`:
  the positive-case test sends to `@neo-test-receiver` but only seeded
  `@neo-test-writer` and `AGENT:*`. Added seed for `@neo-test-receiver` so the
  happy path exercises both endpoints registered.
- `test/playwright/unit/ai/scripts/trioWakeCooldown.spec.mjs`: subprocess invokes
  `MailboxService.addMessage({to: '@neo-test', ...})` per the signal payload.
  Added `beforeAll` hook that imports LifecycleService + GraphService, calls
  `initAsync`, and seeds `@neo-test` as an AgentIdentity node visible to the
  subprocess via the shared SQLite graph file.

Verification:
- `npm run test-unit -- WriteSideInvariant.spec.mjs` → 3/3 PASS (619ms)
- trioWakeCooldown.spec.mjs blocked locally by pre-existing `Neo.ns is not a
  function` setup-time issue (introduced by PR #11578 `mock main route and
  local storage in setup`) when run in isolated worker without prior Neo import;
  CI uses `workers: 1` so prior spec's Neo load persists into the same worker —
  CI verification will confirm the fix.

* fix(test,mcp): replace `@neo-test` with canonical identity + compact openapi to: description (#11417)

Cycle 2 addresses GPT review-hold (MESSAGE:ed55919e) on PR #11615 head 8393cb629.

Two surfaces:

### 1. trioWakeCooldown.spec.mjs — canonical-identity fix

Previous Cycle 1 attempt at `8393cb629` added a `beforeAll` that called
`GraphService.upsertNode({id: '@neo-test', ...})` from the fixture process.
That approach was wrong: the subprocess runs with `UNIT_TEST_MODE='true'` →
graph storage is `:memory:` per `config.template.mjs:214` (per-process), so
fixture-process seeding cannot reach the subprocess.

Corrected approach: use a canonical seeded identity (`@neo-opus-4-7`) that
`seedAgentIdentities.mjs` provisions at the subprocess's
`LifecycleService.initAsync` boot. The subprocess's fresh `:memory:` DB has
the canonical identities available; the validator passes.

Removed the unused `beforeAll` hook + replaced `@neo-test` with
`TEST_COORDINATOR = '@neo-opus-4-7'` in all 3 test signal payloads.

### 2. openapi.yaml `to:` description — MCP-tool-description budget compliance

Per `.agents/skills/pr-review/audits/mcp-tool-description-budget.md` (referenced
in pr-review-guide §5.3): tool descriptions are loaded into every consuming
agent's context window when enumerated; bloat competes with reasoning budget.
Previous description was multi-line block-literal (~280 bytes) with an internal
`#11417` ticket reference and "prior to this guard" archaeology — violating
the audit criteria.

Compacted to single-line (289 chars; well under 1024 cap), removed the ticket
reference + the pre-fix archaeology. Describes call-site usage only: canonical
form + AGENT:* sentinel + alias resolution policy.

Verification:
- `npm run test-unit -- MailboxService.spec.mjs WriteSideInvariant.spec.mjs` → 62/62 PASS (1.4s)
- `node ai/scripts/lint-agents.mjs --base origin/dev` → OK
- `node ai/scripts/check-substrate-size.mjs` → PASS
- `node buildScripts/util/check-whitespace.mjs` → PASS

Not in scope: `wakeSafetyGate.spec.mjs` Neo.ns setup failure noted in GPT's
review-hold is a separate test-infra issue (PR #11578 setup mock requires
prior Neo import; trioWakeCooldown failure was likely the cascade-trigger).
If CI re-run on this Cycle 2 head still surfaces wakeSafetyGate Neo.ns
failure independently, separate follow-up ticket warranted.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-19T07:08:03Z @neo-opus-ada cross-referenced by PR #11622

