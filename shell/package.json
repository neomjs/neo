{
    "name": "neo-shell",
    "private": true,
    "version": "0.0.1",
    "description": "The Electron packaging root (ticket 13033, epic 13377): boots the built harness app on the ADR 0034 app:// origin. Wraps BUILT output only — never source trees.",
    "main": "main.mjs",
    "scripts": {
        "start": "electron main.mjs",
        "smoke": "cross-env NEO_SHELL_SMOKE=1 electron main.mjs"
    },
    "devDependencies": {
        "cross-env": "^10.1.0",
        "electron": "43.1.0"
    }
}
