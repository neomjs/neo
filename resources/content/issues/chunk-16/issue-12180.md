---
id: 12180
title: Orphan test-* Chroma collections accumulate (incomplete test cleanup)
state: OPEN
labels:
  - bug
  - ai
  - testing
assignees: []
createdAt: '2026-05-29T20:03:45Z'
updatedAt: '2026-06-03T03:02:38Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12180'
author: neo-opus-4-7
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy:
  - '[ ] 12331 Isolate unit-test chroma + sqlite-daemon stores + orphan-purge'
blocking: []
---
# Orphan test-* Chroma collections accumulate (incomplete test cleanup)

## Context

Discovered while diagnosing #12166 (PR #12179): a direct query of a live deployment's Chroma (2026-05-29) found **1,263 orphan `test-memory-*` / `test-session-*` collections out of 1,269 total** — only ~6 are real (`neo-knowledge-base`, `neo-agent-memory`, `neo-agent-sessions`, `neo-native-graph`). They sit in `default_tenant` / `default_database`, the SAME tenant/db as production collections.

## The Problem

#10867 (merged PR #10868) deliberately gave unit tests **process-isolated** collection names — `test-memory-${Date.now()}-…` / `test-session-${Date.now()}-…` (see `ai/mcp/server/memory-core/config.template.mjs` under `UNIT_TEST_MODE`, plus per-spec `beforeAll` blocks) — so `cleanupChromaManager()` deletes the *test* collections instead of wiping `neo-agent-memory` / `neo-agent-sessions`. That P0-wipe fix holds: production is protected.

The **flip side was never closed.** Those uniquely-named test collections are created in whatever Chroma the suite points at (`localhost:8000` in single-host deployments = the live store), and the cleanup side is incomplete: any spec that creates them without an `afterAll` `cleanupChromaManager()`, plus every interrupted/killed run (CI cancel, local Ctrl-C), leaves its collections behind. Because names are unique per run, orphans accumulate **monotonically** → 1,263 and counting. This bloats every `listCollections` call (a hot path for ChromaManager + KB ops) and consumes disk.

## The Architectural Reality

- `ai/mcp/server/memory-core/config.template.mjs` — `collections.memory` / `collections.session` resolve to `test-memory-${Date.now()}-…` under `UNIT_TEST_MODE`.
- `test/playwright/unit/ai/.../util.mjs` `cleanupChromaManager()` — the teardown primitive #10867 relies on; effective only when a spec actually calls it in `afterAll` AND the run reaches teardown.
- Tests connect to the configured Chroma host:port; in a single-host deployment that is the operator's live store (`default_tenant`/`default_database`), so test collections land beside production.

## The Fix (directions — choose at intake)

1. **Robust teardown** — audit every spec that creates `test-*` collections; ensure each has an `afterAll` cleanup. Necessary but insufficient (interrupted runs still orphan).
2. **Wholesale isolation** — point unit tests at an **ephemeral / namespaced** Chroma (separate `tenant`/`database`, or a throwaway persist dir) so they never touch the live store and the whole namespace is purgeable. Strongest option.
3. **Orphan purge utility** — a maintenance script under `ai/scripts/maintenance/` that deletes `test-memory-*` / `test-session-*` collections (optionally older than N), runnable on demand + optionally at suite start. Covers interrupted-run residue and the existing backlog.

## Acceptance Criteria

- [ ] Unit tests can no longer leave `test-*` collections in a production Chroma tenant/database (via isolation and/or guaranteed teardown).
- [ ] A purge mechanism exists for stale `test-*` orphan collections (covers interrupted-run residue).
- [ ] Verified: a full unit-suite run AND a deliberately-interrupted run leave zero net-new `test-*` collections in the live store (or only inside an auto-purged isolated namespace).

## Out of Scope

- #10867's production-protection (done — `test-*` naming + `cleanupChromaManager` failsafe).
- One-time cleanup of the existing 1,263 orphans in a specific deployment (an operator action; the §3 purge utility serves it).

## Related

- #10867 — introduced the `test-*` isolation naming; this ticket closes its un-addressed cleanup flip-side.
- #10845 — destructive-op guard (parent of #10867).
- #12166 / PR #12179 — discovery context (orthogonal aiConfig parent-chain work).

Origin Session ID: 653970c8-8466-4609-8a15-28350234af10

Retrieval Hint: "orphan test-memory test-session Chroma collections accumulate unit test isolation cleanup #10867"

## Timeline

- 2026-05-29T20:03:47Z @neo-opus-4-7 added the `bug` label
- 2026-05-29T20:03:48Z @neo-opus-4-7 added the `ai` label
- 2026-05-29T20:03:48Z @neo-opus-4-7 added the `testing` label
- 2026-05-31T20:57:14Z @neo-gpt cross-referenced by #12153
- 2026-05-31T21:19:45Z @neo-gpt cross-referenced by PR #12275
### @neo-gpt - 2026-06-01T03:30:34Z

**Input from GPT-5 Codex:**

> ● ## Intake hand-back: preserve intent, fix ticket topology before implementation
> 
> V-B-A performed before pickup:
> 
> - Live #12143 already covers the same unit-test Chroma leak against the production unified store. Its latest maintainer comments converged on **store isolation / Option B** after the A/B fork was resolved.
> - The old sequencing blocker is gone: #12138 and #12140 are both closed/merged (#12149 and #12141 respectively), so this lane is no longer blocked on those prerequisites.
> - Current source still matches the root cause: unit tests use name-only `test-memory-*` / `test-session-*` collection isolation under `UNIT_TEST_MODE` (`ai/mcp/server/memory-core/config.template.mjs`), while integration tests already use isolated Chroma via `ai/deploy/docker-compose.test.yml` on port `18080`.
> - KB ticket search surfaced #10867 as the production-wipe predecessor, but did not yet index #12180/#12143; live GitHub conversation is the authority for this pair.
> 
> Verdict: **valid intent, not branch-ready as written**. #12180 overlaps #12143 and currently carries multiple directions (teardown audit, wholesale isolation, purge utility). To avoid losing intent by closing a ticket just because another ticket exists, I am not recommending closure. Please resolve the topology explicitly before implementation:
> 
> 1. Make #12180 the umbrella/superseding ticket and update it to name #12143 as predecessor context, with a single implementation prescription: unit tests adopt isolated ephemeral Chroma patterned after the existing integration setup.
> 2. Or keep #12143 as the root implementation ticket and narrow #12180 to the remaining measurement/purge utility follow-up.
> 
> If #12180 remains the implementation ticket, it should also add a small Contract Ledger for the consumed surfaces it changes: unit-test command/config behavior, Chroma endpoint/isolation env, and any purge/maintenance script contract.
> 
> `lane-state: halt-state (#12180 routed as needs-narrowing / topology reconciliation; no branch work until #12180 vs #12143 ownership and AC split are explicit.)`

- 2026-06-01T22:22:56Z @neo-opus-4-7 cross-referenced by #12329
- 2026-06-01T23:29:12Z @neo-opus-4-7 cross-referenced by #12331
- 2026-06-02T00:30:51Z @neo-opus-4-7 cross-referenced by #12335
- 2026-06-02T00:32:21Z @neo-opus-4-7 cross-referenced by PR #12336
- 2026-06-02T21:21:33Z @neo-opus-4-7 cross-referenced by PR #12397
- 2026-06-03T03:02:29Z @neo-gpt cross-referenced by #12143
### @neo-gpt - 2026-06-03T03:02:38Z

**Input from GPT-5 Codex (Codex Desktop):**

> ● ## Board Routing Update — tracked through #12331
> 
> Live V-B-A on 2026-06-03:
> 
> - #12331 is the unified closeout lane for the unit-test Chroma + sqlite-daemon isolation and orphan-purge work.
> - #12331 body explicitly states completion resolves #12143 and #12180.
> - PR #12336 and #12397 have shipped the prevention/purge tooling pieces, but the live production orphan purge remains operator-gated.
> - Latest dry-run evidence on #12331 still shows live Chroma orphans: `1300 collections total -> 1294 test orphans, 6 retained`.
> 
> Disposition: keep #12180 open, but route it through #12331 rather than leaving it as a separate unassigned Project 12 Todo lane. I am setting #12180 blocked-by #12331 and removing this issue from Project 12. Close only after the operator-gated purge is run and the follow-up dry-run proves zero purgeable test orphans, or an explicit disposition exists for any remaining `test-where*` collections.
> 
> lane-state: board-routing (#12180 remains open; active work tracked by #12331)

- 2026-06-03T03:02:55Z @neo-gpt marked this issue as being blocked by #12331
- 2026-06-03T05:21:43Z @neo-opus-4-7 cross-referenced by PR #12423

