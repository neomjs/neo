---
id: 13694
title: Add who_is_online to getHealthExemptTools (survives embed-drain)
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - model-experience
assignees:
  - neo-opus-ada
createdAt: '2026-06-21T02:03:57Z'
updatedAt: '2026-06-21T03:08:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13694'
author: neo-opus-ada
commentsCount: 0
parentIssue: 13498
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-06-21T03:08:25Z'
---
# Add who_is_online to getHealthExemptTools (survives embed-drain)

## Context

#13498 leaf-4 — the co-authored split with @neo-opus-grace (confirmed 2026-06-21: I take this edit; she owns the read-degradation policy invariant + the exempt-set pinning test; both co-land as the embedding-resilience set under #13624). Surfaced this session: the local embedder's canary timed out under heavy-sync load (root-fixed by #13692), and `who_is_online` **fail-loud'd** — but it's a graph-backed read that should survive an embed-drain.

## The Problem

`who_is_online` is omitted from the memory-core server's `getHealthExemptTools()` allow-list, so the embed-canary gate rejects it during an embedder outage — denying a read the outage never actually touched.

## The Architectural Reality

- The exempt-list lives at `ai/mcp/server/memory-core/Server.mjs:114-133`; the documented invariant (`:104-111`) is **no-embedder → exempt; must-embed → fail-loud**.
- `who_is_online` → `WakeSubscriptionService.whoIsOnline` (`toolService.mjs:56` → `WakeSubscriptionService.mjs:549`) → `_listAgentIdentityNodes` (`:591-616`): a SQLite `prepare().all()` over `AgentIdentity` `Nodes` + `_projectAgentLiveness`. **No `embedText`/embedder anywhere on the path**; the docstring (`:559`) states liveness is *"graph-backed (survives an embed-drain)"*.

## The Fix

- Add `'who_is_online'` to the `getHealthExemptTools()` array (`Server.mjs:115-132`).
- Extend the JSDoc exempt-rationale (`:104-111`) to name `who_is_online` as a no-embedder graph-backed recency read.
- Add a unit test asserting `who_is_online` ∈ `getHealthExemptTools()` (the per-tool exempt assertion; the broader exempt-set pinning test is @neo-opus-grace's policy half).

## Acceptance Criteria

- [ ] `who_is_online` is present in `getHealthExemptTools()`.
- [ ] The JSDoc names it as a no-embedder graph read.
- [ ] A unit test pins `who_is_online` as exempt.

## Out of Scope

- The read-degradation policy invariant + the broader exempt-set test (@neo-opus-grace's half, co-landing).
- The embed-path root-cause fix (#13692, @neo-opus-grace).

## Related

Parent #13498 (who_is_online liveness). Siblings: #13692 (embed-path batch fix), #13624 (backlog-drain epic). Surfaced by @neo-opus-ada (who_is_online owner — #13517/#13527).

Release classification: post-release (embedding-resilience hardening — non-blocking).

Origin Session ID: 93531a2a-e304-4dc5-a4c4-ab7280c7b3ae

## Timeline

- 2026-06-21T02:03:58Z @neo-opus-ada added the `enhancement` label
- 2026-06-21T02:03:58Z @neo-opus-ada added the `ai` label
- 2026-06-21T02:03:58Z @neo-opus-ada added the `architecture` label
- 2026-06-21T02:03:59Z @neo-opus-ada added the `model-experience` label
- 2026-06-21T02:04:53Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-06-21T02:05:40Z @neo-opus-ada added parent issue #13498
- 2026-06-21T02:14:27Z @neo-opus-ada referenced in commit `f64a91a` - "fix(ai): exempt who_is_online from the embed-canary health gate (#13694)

who_is_online -> WakeSubscriptionService.whoIsOnline is a graph-backed AgentIdentity-roster recency read (no embedder), so the memory-core health gate must serve it while the embedder is down. It failed loud during an embed-drain this session; adds it to getHealthExemptTools + the JSDoc exempt-rationale + a per-tool exempt unit test. #13498 leaf-4 (co-authored w/ @neo-opus-grace, who owns the policy invariant + exempt-set test)."
- 2026-06-21T02:16:01Z @neo-opus-ada cross-referenced by PR #13696
- 2026-06-21T03:08:26Z @tobiu referenced in commit `9ca1ed8` - "fix(ai): exempt who_is_online from the embed-canary health gate (#13694) (#13696)

who_is_online -> WakeSubscriptionService.whoIsOnline is a graph-backed AgentIdentity-roster recency read (no embedder), so the memory-core health gate must serve it while the embedder is down. It failed loud during an embed-drain this session; adds it to getHealthExemptTools + the JSDoc exempt-rationale + a per-tool exempt unit test. #13498 leaf-4 (co-authored w/ @neo-opus-grace, who owns the policy invariant + exempt-set test).

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-21T03:08:26Z @tobiu closed this issue
- 2026-06-21T03:13:58Z @neo-opus-ada referenced in commit `83df2c4` - "test(ai): pin the full graceful-degradation exempt set in the who_is_online test (#13694)

Folds @neo-opus-grace's full-exempt-set loop (her co-authored half of #13498 leaf-4): every non-embedding read stays exempt, so a regression dropping query_recent_turns / record_turn_presence / get_session_memories / who_is_online fails loud — the policy invariant completely pinned. Replaces the single who_is_online assertion."

