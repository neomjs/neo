---
number: 16627
title: 'feat: The returning vessel departs behind a theme-ground overlay (#16626)'
author: neo-fable
state: OPEN
createdAt: '2026-08-07T10:57:00Z'
updatedAt: '2026-08-07T10:57:00Z'
closedAt: null
mergedAt: null
head: agent/16626-vessel-departing
base: dev
url: 'https://github.com/neomjs/neo/pull/16627'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16626

The stack-return's source vessel now presents a deliberate terminal state instead of a raw content yank. At `documents-adopted` — the measured moment the vessel's content leaves (probe timeline on #16499 r4) — the commit handler fires one presentation-only `applyDeltas` at the source window's body, scoped strictly to the `vesselToMain` direction. The class engages a fixed full-viewport `::after` overlay in `Viewport.scss` that fades IN to `var(--workstation-ground)` over 250ms and holds until the OS close. The overlay shape (not a body-opacity fade) avoids the html-underlay flash and additionally covers any residual gesture artifacts in the closing window — including the orphaned drag proxy that PR #16624 retires at its root, giving defense in depth from the presentation layer. No phase, ordering, or await changes ride the dispatch; the sheet placement follows `Viewport.scss`'s documented two-gate doctrine (the `?popout=` vessel never loads `Workspace.css`).

Evidence: L3 (headed browser runtime, real popup windows) → L3 required (the close-target ACs are cross-window render-truth). Residual: the whole-take film receipt rides the parent #16499's next staged capture, per its film QA gate.

## Deltas from ticket

None substantive — the shipped shape is the ticket's Fix section; the witness poll cadence (16ms × 600) is bounded by the popup's own close rather than a wall-clock guess.

## Test Evidence

- Scene 4 (`WorkstationFiveBeatNL`, film-paced headed): during-step witness RED-PROVEN with the dispatch stashed (the departing-state assertion failed; the class never appears pre-fix) → GREEN with the fix (9.1s), all existing phase/receipt/shell-identity/DOM-continuity assertions intact.
- Frame receipt from the green run's vessel video tail: a uniform theme-ground surface — no chip, no content fragments (contrast: the #16499 r1 tail frame showed a black body plus the floating drag chip).
- `test/playwright/unit/apps/workstation/Workspace.spec.mjs`: 36/36 at the fixed head.
- Themes rebuilt (`build-themes -n -t all -e dev`) before the headed verification — the e2e server serves built css.

## Post-Merge Validation

- [ ] Next staged take on #16499: the returning vessel's final second reads as a deliberate exit at film pace, both themes.

## Commits (if multi-commit)

- single commit: `feat(workstation): the returning vessel departs behind a theme-ground overlay (#16626)`

Related: #16499 (parent S-3b + probe timeline), #16623 / PR #16624 (root retirement of the proxy this overlay also covers), #16621 / PR #16622 (merged — the fast path that shortened the window this overlay decorates).

Authored by Mnemosyne (Claude Fable 5, Claude Code). Session 84d669f4-2271-4d6a-8878-45e8754be6b3.
