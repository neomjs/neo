---
id: 13936
title: Prove deployment immune-system closeout via live smoke
state: OPEN
labels:
  - enhancement
  - ai
  - testing
  - architecture
  - model-experience
assignees:
  - neo-gpt
createdAt: '2026-06-23T17:10:44Z'
updatedAt: '2026-06-25T09:57:22Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13936'
author: neo-gpt
commentsCount: 20
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Prove deployment immune-system closeout via live smoke

## Context

The deployment immune-system epics are not ready to close on code merge evidence alone. Live closeout comments on #13860, #13874, and #13889 all converge on the same residual: the cloud deployment still needs an L3/operator smoke proving that the deployed stack exposes current diagnosis/recovery state through the approved public surfaces.

Release classification: deployment-readiness closeout, boardless in this turn; promote to the active release/project board if the operator wants this smoke tracked as a release gate.

Live latest-open sweep: checked the latest 20 open issues on 2026-06-23; no equivalent dedicated deployment smoke/proof ticket found. A2A in-flight claim sweep: `list_messages({status: 'all', limit: 30})` returned 0 messages. Duplicate keyword sweeps for open and closed issues returned no equivalent.

## The Problem

#13860, #13874, and #13889 can each look structurally complete at L2 while still failing the operational question that triggered the emergency: can the deployed system prove and recover from a wedged model / unhealthy Memory Core state without relying on graph-only proof or private shell access?

Leaving that proof only in scattered PR post-merge checklists is fragile. It is easy to merge the code and never run the smoke that makes the parent epics legitimately closable.

## The Architectural Reality

Public surfaces remain KB and MC only. Runtime authority remains inside the orchestrator deployment-runtime holder; KB/MC must not gain Docker socket, shell, exec, restart, daemon-route, or public write authority.

The current dependency chain is:

- #13926 / PR #13935 provides the graph-independent deployment-state bridge read by KB/MC.
- #13914 owns the public cloud-deployment observability MCP tool on top of the approved surfaces.
- #13860 needs public proof for diagnostics/current deployment state.
- #13874 needs controlled recovery proof for the privileged actuator/recovery-run path.
- #13889 needs deployed message-WAL drain parity proof for acknowledged A2A durability.

## The Fix

Run and record one live deployment smoke after the required public-surface PRs land. The smoke should seed or observe a safe degraded-state fixture and verify the public MCP read path, recovery-run ledger, and message-WAL drain behavior without adding any new public privileged route.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| KB/MC deployment-state public read path | #13926, #13914 | Read current bounded deployment-state snapshot through public tools only | Missing/stale/unconfigured state is explicit | PR bodies + tool schemas | L3 live smoke transcript |
| Recovery actuator proof | #13874, ADR-0026 | Controlled allowlisted recovery action records recovery-run ledger + reobserve result | Backoff/escalation is recorded, not silent | Epic closeout comment + ADR-0026 | L3 live smoke transcript |
| Diagnostics proof | #13860, ADR-0025 | Wedged/unhealthy service state produces diagnosis/current-state evidence visible remotely | Graph unavailable does not erase KB proof path | Epic closeout comment + ADR-0025 | L3 live smoke transcript |
| Message-WAL drain parity | #13889 | Deployed drain host starts in intended mode, acquires sole drain lock, projects a seeded pending message WAL row, and survives restart | Double-drain refuses or leaves row pending with explicit diagnostics | Epic closeout comment | L3 live smoke transcript |

## Decision Record Impact

Aligned with ADR-0025 and ADR-0026. This ticket does not amend either ADR; it captures the remaining live-evidence gate required before closing the parent epics.

## Acceptance Criteria

- [ ] #13926 and #13914 are merged or otherwise deployed enough that KB/MC public tool calls can read the intended deployment-state surface.
- [ ] Live smoke proves a wedged-model / unhealthy-memory or equivalent safe degraded-state fixture through KB/MC public reads, with no private shell/socket path in the proof.
- [ ] Live smoke proves recovery-run ledger + reobserve evidence for one safe allowlisted recovery path, or records an explicit fail-closed/backoff/escalation outcome.
- [ ] Live smoke proves deployed message-WAL drain parity: intended host mode, sole-drainer lock, graph marker write, no duplicate drain, and restart-safe completion or explicit pending-state diagnostics.
- [ ] Closeout comments are posted back to #13860, #13874, and #13889 with the smoke evidence and any residuals.

## Out of Scope

- Adding a new public daemon, orchestrator route, shell, exec, restart, or Docker-socket MCP tool.
- Changing the actuator design or diagnosis thresholds.
- Implementing #13914 or #13926.
- Publishing private deployment configuration values in the public repo.

## Avoided Traps

- Do not treat green CI as deployment proof.
- Do not close epics from L2 evidence when their own comments require L3/operator smoke.
- Do not prove the failure class through graph-only reads; the known outage class includes graph-unavailable / Memory Core-unhealthy states.
- Do not expose private deployment specifics or client names in public artifacts.

## Related

Related: #13860, #13874, #13889, #13914, #13926, ADR-0025, ADR-0026.

Retrieval Hint: `deployment immune-system live smoke closeout #13860 #13874 #13889 #13926 #13914`


## Timeline

- 2026-06-23T17:10:45Z @neo-gpt assigned to @neo-gpt
- 2026-06-23T17:10:45Z @neo-gpt added the `enhancement` label
- 2026-06-23T17:10:46Z @neo-gpt added the `ai` label
- 2026-06-23T17:10:46Z @neo-gpt added the `testing` label
- 2026-06-23T17:10:46Z @neo-gpt added the `architecture` label
- 2026-06-23T17:10:46Z @neo-gpt added the `model-experience` label
- 2026-06-23T17:11:03Z @neo-gpt added parent issue #13860
- 2026-06-23T17:11:22Z @neo-gpt cross-referenced by #13889
- 2026-06-23T17:11:32Z @neo-gpt cross-referenced by #13874
### @neo-gpt - 2026-06-23T18:06:39Z

## [intake-classification] prerequisite-gated, do not smoke via private shell

V-B-A checks run 2026-06-23:
- #13926 is still open and closes through PR #13935; PR #13935 is green but unmerged.
- #13914 is still open, assigned to @neo-opus-grace, and has no closing PR. Current checkout also has no `inspect_deployment` MCP operation.
- KB and MC MCP servers are reachable, but both report stale runtime config identity, so this harness cannot be used as proof of deployed-current tool schema.

Classification: valid closeout gate, but not executable yet. The first AC is unsatisfied until #13935 is merged/deployed and #13914 ships/deploys the public KB/MC read surface. Running the smoke through private shell/docker access would violate this ticket and the security invariant: public proof must go through KB/MC only, with no new public privileged route.

Next actionable state: keep #13936 open as the L3 closeout gate; run it immediately after #13935 + #13914 are both deployed enough to expose the intended public read path.

- 2026-06-23T18:18:02Z @neo-gpt cross-referenced by #13860
- 2026-06-23T18:34:42Z @neo-gpt cross-referenced by #13914
- 2026-06-23T19:30:25Z @neo-gpt cross-referenced by PR #13938
### @neo-gpt - 2026-06-23T19:37:34Z

## [readiness-update] prerequisite chain narrowed

Live V-B-A check on 2026-06-23:

- #13935 is now merged (`mergedAt: 2026-06-23T19:17:11Z`), so the graph-independent deployment-state bridge prerequisite is no longer the open blocker.
- #13914 is still open, now covered by PR #13938 (`head: 77f87bf1a06fdfb5afb846e56db22504353c7f79`).
- #13938 is open and mergeable, but its GitHub `unit` and `integration-unified` checks are still in progress. I have not routed review yet because the current head is not green.

Current gate for this smoke: wait for #13938 to pass CI, receive review, merge, deploy/config-overlay/restart KB+MC so the public MCP schemas include `inspect_deployment`, then run this ticket through KB/MC only. Private shell/docker proof still does **not** satisfy this ticket.


- 2026-06-24T08:52:07Z @neo-gpt cross-referenced by #13942
- 2026-06-24T09:18:51Z @neo-gpt cross-referenced by PR #13943
- 2026-06-24T10:10:40Z @neo-gpt cross-referenced by #13944
- 2026-06-24T12:28:21Z @neo-gpt cross-referenced by #13950
- 2026-06-24T13:28:37Z @neo-gpt cross-referenced by #13952
- 2026-06-24T13:32:20Z @neo-gpt cross-referenced by PR #13953
- 2026-06-24T16:18:01Z @tobiu removed parent issue #13860
### @tobiu - 2026-06-24T16:18:57Z

detached this ticket from the parent epic. implementation and CI green need to stay separate from follow-up deployments.

### @neo-gpt - 2026-06-24T16:52:25Z

## [readiness-update] public smoke still not executable

Live V-B-A check on 2026-06-24:

- #13914 is closed and PR #13938 is merged, so the public deployment-inspection tool implementation prerequisite is no longer open.
- KB and MC MCP servers are reachable and expose the deployment-state snapshot read surface.
- Both public reads currently return `snapshot-missing`, so there is no deployment-state snapshot to smoke yet.
- Both KB and MC health checks report stale runtime OpenAPI identity in this harness, so this run cannot be used as deployed-current schema proof.

Classification: #13936 remains valid but deployment-gated. The next acceptable proof is still a KB/MC-only public-surface smoke after the deployed services have been rebuilt/restarted and the runtime holder is producing a fresh deployment-state snapshot. No private shell, Docker socket, or graph-only proof was used here.

### @neo-gpt - 2026-06-24T19:04:12Z

## [readiness-update] public tool reachable, snapshot still missing

Live KB/MC-only check on 2026-06-24:

- `KB get_deployment_state_snapshot(staleAfterMs: 300000)` is callable and returns `status: unavailable`, `reason: snapshot-missing`.
- `MC get_deployment_state_snapshot(staleAfterMs: 300000)` is callable and returns `status: unavailable`, `reason: snapshot-missing`.
- KB `healthcheck` is healthy for Chroma/embedding, but reports stale runtime OpenAPI identity, so this harness still cannot be used as deployed-current schema proof.
- MC `get_rem_pipeline_state` is callable and reports `undigested: 78`, `digested: 1326`, `sessionNodes: 808`, `topologyConflicts: 5`, `recentCycles: []`.

Classification: the #13936 public-surface smoke has progressed one step: the public read tools are present, but the required deployment-state snapshot is still absent. No private shell, Docker socket, graph-only proof, or privileged public route was used. The acceptable next proof remains a KB/MC-only smoke after the runtime holder writes a fresh deployment-state snapshot.

### @neo-gpt - 2026-06-24T21:43:36Z

## [readiness-update] KB/MC public-surface smoke still blocked by missing snapshot

Live KB/MC-only check on 2026-06-24:

- Public KB healthcheck is healthy and reports current runtime source/config identity.
- Public MC healthcheck reports current runtime source/config identity, but is degraded because the embedding write canary timed out.
- Public MC REM pipeline state is not available while MC is degraded.
- KB deployment-state snapshot read returns `status: unavailable`, `reason: snapshot-missing`.
- MC deployment-state snapshot read returns `status: unavailable`, `reason: snapshot-missing`.

Classification: #13936 remains valid but not closable. The public surfaces are current enough to test, but the required deployment-state snapshot is still absent and MC is degraded on the write canary. This is still not a successful L3 closeout smoke, and no private shell, Docker socket, privileged route, or graph-only proof was used.


### @neo-gpt - 2026-06-24T22:44:55Z

## [readiness-update] public surfaces current; deployment snapshot still absent

Live KB/MC-only check on 2026-06-24T22:44:44Z:

- Public KB healthcheck is healthy and reports current runtime source/config identity.
- Public MC healthcheck is healthy and reports current runtime source/config identity.
- Public MC REM pipeline state is readable: `undigested: 77`, `digested: 1324`, `sessionNodes: 824`, `topologyConflicts: 5`, `recentCycles: []`.
- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB and MC `inspect_deployment(staleAfterMs: 600000)` both return the same `snapshot-missing` state.

Classification: #13936 remains valid but not closable. The public surfaces are now current and healthy enough for the smoke, but the required deployment-state snapshot is still absent. No private shell, Docker socket, privileged public route, or graph-only proof was used.

### @neo-gpt - 2026-06-24T23:16:38Z

## [readiness-update] public surfaces current; deployment snapshot still absent

Live KB/MC-only check on 2026-06-24T23:16:19Z:

- Public KB healthcheck is healthy and reports current runtime source/config identity.
- Public MC healthcheck is healthy and reports current runtime source/config identity.
- Public MC REM pipeline state is readable: `undigested: 53`, `digested: 1353`, `sessionNodes: 845`, `topologyConflicts: 5`, `recentCycles: []`.
- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB and MC `inspect_deployment(staleAfterMs: 600000)` both return the same `snapshot-missing` state.

Classification: #13936 remains valid but not closable. The public surfaces are current and healthy enough for the smoke, but the required deployment-state snapshot is still absent. No private shell, Docker socket, privileged public route, or graph-only proof was used.

### @neo-gpt - 2026-06-24T23:32:16Z

## [readiness-update] public snapshot still absent

Live KB/MC-only check on 2026-06-24T23:34Z:

- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB and MC `inspect_deployment(staleAfterMs: 600000)` return the same `snapshot-missing` state.
- MC `get_rem_pipeline_state` is readable: `undigested: 38`, `digested: 1368`, `sessionNodes: 863`, `topologyConflicts: 5`, `recentCycles: []`.

Classification: #13936 remains valid but not closable. Public tools are reachable, but the required deployment-state snapshot is still absent. No private shell, Docker socket, privileged public route, or graph-only proof was used.

- 2026-06-24T23:49:07Z @neo-gpt cross-referenced by #13978
### @neo-gpt - 2026-06-25T02:28:29Z

## [readiness-update] public snapshot still absent

Live KB/MC-only check on 2026-06-25T02:28Z:

- KB healthcheck is healthy for Chroma/embedding, but reports stale runtime config identity (`configDigest`).
- MC `get_rem_pipeline_state` is readable: `undigested: 38`, `digested: 1368`, `sessionNodes: 863`, `topologyConflicts: 5`, `recentCycles: []`.
- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB and MC `inspect_deployment(staleAfterMs: 600000)` both return the same `snapshot-missing` state.

Classification: #13936 remains valid but not closable. Public tools are reachable, but the required deployment-state snapshot is still absent, and KB runtime config identity is stale in this harness. No private shell, Docker socket, privileged public route, or graph-only proof was used.

### @neo-gpt - 2026-06-25T02:57:58Z

Smoke attempt update (2026-06-25T02:58Z): KB and MC public MCP surfaces are reachable, but the deployment-state bridge is not yet observable.

Evidence:
- KB `get_deployment_state_snapshot(staleAfterMs:600000)` -> `snapshot-missing`
- KB `inspect_deployment(staleAfterMs:600000)` -> `snapshot-missing`
- MC `get_deployment_state_snapshot(staleAfterMs:600000)` -> `snapshot-missing`
- MC `inspect_deployment(staleAfterMs:600000)` -> `snapshot-missing`
- MC `get_rem_pipeline_state` -> `undigested=38`, `digested=1368`, `sessionNodes=863`, `topologyConflicts=5`, `recentCycles=[]`

Status: #13936 remains blocked on a fresh deployment-state snapshot becoming visible through KB/MC. No private or non-public surface was used.

### @neo-gpt - 2026-06-25T03:26:31Z

## [readiness-update] snapshot writer disabled in running orchestrator

Live V-B-A check on 2026-06-25T03:24Z:

- PR #13986/#13987/#13988 are still open, green, and review-routed; #13986 remains the first merge/deploy gate for the REM parser overrun.
- KB and MC public deployment tools remain callable, but still return `snapshot-missing`.
- The running orchestrator environment does **not** include `NEO_DEPLOYMENT_STATE_BRIDGE_ENABLED`.
- The running checkout config has `AiConfig.orchestrator.deploymentStateBridge.enabled` defaulting to `false`.
- `DeploymentStateBridgeService.writeSnapshotIfDue()` returns `disabled` before attempting a write when that leaf is false.

Classification: #13936 is not closable yet. The absent deployment-state snapshot is explained by the writer being disabled in the running orchestrator, not by the public KB/MC read tools. This needs either an explicit enablement in the running deployment or a Neo-side default/opt-out follow-up, depending on the intended deployment contract. No private shell, Docker socket, privileged public route, or graph-only proof was used.

- 2026-06-25T03:27:59Z @neo-gpt cross-referenced by #13989
- 2026-06-25T03:31:15Z @neo-gpt cross-referenced by PR #13990
### @neo-gpt - 2026-06-25T03:41:42Z

## [readiness-update] current merge gates for live smoke

Live lifecycle check on 2026-06-25T03:38Z:

- PR #13986 is CLEAN, all checks green, and review-requested from `tobiu`. It remains the first merge/deploy gate because it bounds REM parser provider calls and exposes active REM diagnostics.
- PR #13990 is CLEAN, all checks green, and review-requested from `tobiu`. It is the deployment-state bridge default-on follow-up for the snapshot writer.
- #13936 remains not closable until those changes are deployed and the public KB/MC tools can observe a fresh deployment-state snapshot.

Post-merge smoke sequence:
1. Pull `dev` and migrate overlays.
2. Restart/eject so the active pre-fix REM stream cannot continue under the old runtime.
3. Verify MC `get_rem_pipeline_state` exposes bounded `activeCall` when REM is in flight, and does not allow the marathon session to run past the configured graph output budget.
4. Verify KB/MC `inspect_deployment` resolves a fresh snapshot.
5. Confirm KB/MC remain the only public surfaces.

No private shell, Docker socket, privileged public route, or graph-only proof was used for this update.

### @neo-gpt - 2026-06-25T04:10:36Z

## [readiness-update] restart required for post-#13986/#13990 proof

Live KB/MC-only check on 2026-06-25T04:10Z:

- MC health is healthy, but runtime freshness is stale on `configDigest`.
- KB health is healthy, but runtime freshness is stale on `configDigest`.
- MC `get_rem_pipeline_state` is callable, but still exposes the old snapshot shape: `undigested=38`, `digested=1368`, `sessionNodes=863`, `topologyConflicts=5`, `recentCycles=[]`; the current tool handbook still describes the 5-axis state and does not expose `activeCall`.
- KB and MC `inspect_deployment(staleAfterMs:600000)` both still return `snapshot-missing`.

Classification: #13936 remains not closable. After #13986 and #13990 merge, restart/reconnect is mandatory before claiming bounded REM `activeCall` observability or default-on deployment-state snapshot behavior. No private shell, Docker socket, privileged public route, or graph-only proof was used; KB/MC remain the only public surfaces.

### @neo-gpt - 2026-06-25T04:36:00Z

Verified closeout gate as of 2026-06-25T04:36Z.

Current merge order for the deployable stack:
1. #13986 — bound REM parser model calls and expose live `activeCall` diagnostics.
2. #13990 — enable deployment-state bridge by default.
3. #13987 — fail loud when Golden Path handoff write proof is missing.

#13988 must wait until after #13987 and then be rebased/updated; it conflicts with #13987 in `GoldenPathSynthesizer.mjs` and should preserve #13987’s `wroteHandoff` / `renderActivePrCycleState` path.

Post-merge local validation sequence:
1. Pull `dev` in the runtime checkout.
2. Run `node ./ai/scripts/setup/initServerConfigs.mjs --migrate-config`.
3. Restart orchestrator and the harness; eject the stale local model stream during restart.
4. Verify MC `get_rem_pipeline_state` now includes `activeCall`.
5. Verify KB/MC `inspect_deployment` no longer returns `snapshot-missing`.
6. Confirm KB/MC remain the only public surfaces; orchestrator stays internal.

Current runtime proof is still pre-merge/stale: MC REM state has no `activeCall`, and `inspect_deployment` returns `snapshot-missing`.

### @neo-gpt - 2026-06-25T06:00:10Z

## [readiness-update] public smoke still blocked after fresh KB/MC check

Live KB/MC-only check on 2026-06-25T05:59Z:

- Public KB healthcheck is healthy, but runtime freshness is stale on `configDigest`.
- KB `get_deployment_state_snapshot(staleAfterMs:600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB `inspect_deployment(staleAfterMs:600000)` returns the same `snapshot-missing` state.
- MC `get_deployment_state_snapshot(staleAfterMs:600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `inspect_deployment(staleAfterMs:600000)` returns the same `snapshot-missing` state.
- MC `get_rem_pipeline_state` is readable but still exposes the old 5-axis shape: `undigested=38`, `digested=1368`, `sessionNodes=863`, `topologyConflicts=5`, `recentCycles=[]`; no `activeCall` field is visible yet.
- #13986 is still open after the cleanup push; most checks are green, but `unit` and `integration-unified` were still running at the lifecycle check. Review remains requested from `tobiu`.

Classification: #13936 remains valid but not closable. The public tools are reachable, but the deployment-state snapshot is still absent and the running public surfaces do not yet expose the post-#13986 REM `activeCall` diagnostics. The next acceptable proof remains post-merge/restart: pull `dev`, migrate overlays, restart orchestrator/harness, then rerun this KB/MC-only smoke. No private shell, Docker socket, privileged public route, or graph-only proof was used.

### @neo-gpt - 2026-06-25T06:02:41Z

## [readiness-update] #13986 is green; smoke still waits for review/merge/restart

Live lifecycle check on 2026-06-25T06:02Z:

- PR #13986 is `CLEAN`; all checks are green, including `unit` and `integration-unified`.
- #13986 still has review requested from `tobiu`; it is not merged.
- Because the running KB/MC public surfaces still show pre-#13986 behavior (`get_rem_pipeline_state` has no `activeCall`) and deployment-state reads still returned `snapshot-missing` in the 05:59Z smoke, #13936 is still not closable.

Current next valid smoke sequence remains:
1. Merge/deploy #13986 so REM parser calls are bounded and `activeCall` diagnostics are exposed.
2. Merge/deploy #13990 so the deployment-state bridge writer is default-on.
3. Merge/deploy #13987 before the Golden Path handoff proof lane.
4. Pull `dev`, run config overlay migration, restart orchestrator/harness, then rerun KB/MC-only smoke.

No private shell, Docker socket, privileged public route, or graph-only proof was used.

### @neo-gpt - 2026-06-25T07:18:01Z

## [readiness-update] public surfaces current; deployment snapshot still absent

Live KB/MC-only smoke on 2026-06-25T07:17Z:

- Public KB `healthcheck` is healthy and reports current runtime source/config identity.
- Public MC `healthcheck` is healthy and reports current runtime source/config identity.
- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB `inspect_deployment(staleAfterMs: 600000)` returns the same `snapshot-missing` state.
- MC `inspect_deployment(staleAfterMs: 600000)` returns the same `snapshot-missing` state.
- MC `get_rem_pipeline_state` is readable: `undigested=24`, `digested=1361`, `sessionNodes=872`, `topologyConflicts=0`, `recentCycles=[]`.

Classification: #13936 remains valid but not closable. The public KB/MC surfaces are current and healthy enough to test, but the deployment-state bridge snapshot is still absent.

Correction to earlier smoke sequencing: after the #13986 adversarial fixup, `activeCall` is no longer a public `get_rem_pipeline_state`/MCP contract. The REM parser diagnostics for #13984 are internal run-state/log evidence; the #13936 public smoke should keep focusing on KB/MC deployment-state reads, recovery evidence, and message-WAL drain parity without adding a public privileged route.

Current lifecycle gate: PR #13986 is open at `def553f745`; `integration-unified` is green, `unit` is still running, and review remains requested from `tobiu`. No private shell, Docker socket, privileged public route, or graph-only proof was used.


### @neo-gpt - 2026-06-25T07:20:08Z

## [lifecycle-update] #13986 fully green, still review-gated

Live PR gate check on 2026-06-25T07:19Z:

- PR #13986 is open at `def553f745`.
- All GitHub checks are now green, including `unit` and `integration-unified`.
- Review is still requested from `tobiu`; the PR is not merged.
- PR #13990 is also open, all checks green, and review-requested from `tobiu`.
- PR #13987 is open, all checks green, and review-requested from `tobiu`.

This updates the stale subpoint in the 07:17Z smoke comment where #13986 `unit` was still running. The #13936 closeout state is otherwise unchanged: KB/MC public surfaces are healthy/current, but deployment-state reads still return `snapshot-missing`; the smoke remains not closable until the green/review-gated PR stack is merged/deployed/restarted and the KB/MC-only proof is rerun.


- 2026-06-25T08:06:56Z @neo-gpt cross-referenced by #13994
### @neo-gpt - 2026-06-25T09:57:22Z

## [readiness-update] public smoke still blocked by missing deployment snapshot

Live KB/MC-only smoke on 2026-06-25T09:56:59Z:

- Public KB `healthcheck` is healthy, but runtime freshness is stale on `configDigest`.
- Public MC `healthcheck` is healthy, but runtime freshness is stale on `configDigest`.
- KB `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- MC `get_deployment_state_snapshot(staleAfterMs: 600000)` returns `status: unavailable`, `reason: snapshot-missing`.
- KB `inspect_deployment(staleAfterMs: 600000)` returns the same `snapshot-missing` state.
- MC `inspect_deployment(staleAfterMs: 600000)` returns the same `snapshot-missing` state.
- MC `get_rem_pipeline_state` is readable: `undigested=4`, `digested=1399`, `sessionNodes=908`, `topologyConflicts=5`, `recentCycles=[]`.

Classification: #13936 remains valid but not closable. The public KB/MC tools are reachable, but the required deployment-state snapshot is still absent, and both public MCP runtimes report stale config identity relative to the current checkout. No private shell, Docker socket, privileged public route, or graph-only proof was used.

- 2026-06-25T10:06:35Z @neo-gpt cross-referenced by #13997

