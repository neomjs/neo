---
id: 14913
title: 'DockCrossZoneDragNL red on dev: drop resolves to split, SortZone moveTo TypeError'
state: CLOSED
labels:
  - bug
  - ai
  - testing
  - regression
assignees:
  - neo-opus-grace
createdAt: '2026-07-10T01:43:26Z'
updatedAt: '2026-07-10T12:21:05Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14913'
author: neo-opus-grace
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-10T12:21:05Z'
---
# DockCrossZoneDragNL red on dev: drop resolves to split, SortZone moveTo TypeError

## Context
Discovered while running the full dock e2e set for PR #14906 (#14654/#14660). `DockCrossZoneDragNL` fails on PURE dev — verified by stashing every branch change and running at the branch base (`ad9e5310b`, current dev head lineage): same failure. This is a pre-existing dev regression, not fallout from the auto-hide work.

## The Problem
The journey drags the `Strategy` tab header onto the `Terminal` tab header and asserts the committed model relocates `strategy` into `terminal-tabs` (tab-into). On current dev:

1. The committed result is a SPLIT instead: `strategy: main-tabs -> tabs-strategy-0` (a fresh tabs node from a `splitNode` op) — the producer resolves an edge/split placement for a pointer released on the target zone's tab-header strip.
2. The drag additionally throws in the browser:
```
TypeError: Cannot read properties of undefined (reading 'moveTo')
    at DockTabSortZone.moveTo (src/draggable/tab/header/toolbar/SortZone.mjs:34)
    at DockTabSortZone.processDragEnd (src/draggable/container/SortZone.mjs:483)
```

## Timeline hypothesis (unverified — for the investigator)
The spec landed green with the cross-zone slice (#14857 / PR #14864). The producer-routed dock drag rework (#14886 / PR #14898) later reshaped this exact path (`previewToOperation` producer pipeline + `onDockCrossZoneDrop`). Suspect the tab-header-strip pointer band now classifies as an edge/split placement instead of `tab-into`, and the source toolbar's within-container `moveTo` path lost a guard for cross-container drops. Both claims need falsification against the #14898 diff.

## Reproducer
```
npx playwright test DockCrossZoneDragNL -c test/playwright/playwright.config.e2e.mjs --workers=1
```
Note: ensure the spawned web server serves YOUR checkout — a dev server from another clone squatting on 8080 silently serves the wrong tree (`NEO_E2E_PORT` override lands with PR #14906).

## Acceptance Criteria
- [ ] Root cause identified against the #14898 delta (producer band classification and/or SortZone moveTo guard).
- [ ] `DockCrossZoneDragNL` green on dev.
- [ ] No `moveTo` TypeError on cross-container header drops.
- [ ] Cross-family review.

## Related
#14857 · #14864 · #14886 · #14898 · surfaced during PR #14906

Live latest-open sweep: checked latest 20 open issues + recent A2A claims at 2026-07-10T01:42Z; no equivalent found.
Process note: filed during the operator-granted temporary Fable 5 window.
Origin Session ID: ef6b9a4a-54ec-4afb-8438-f89a3ee46ad2
Retrieval Hint: "cross zone drag e2e red producer tab-into split moveTo undefined"

## Timeline

- 2026-07-10T01:43:27Z @neo-opus-grace added the `bug` label
- 2026-07-10T01:43:28Z @neo-opus-grace added the `ai` label
- 2026-07-10T01:43:28Z @neo-opus-grace added the `testing` label
- 2026-07-10T01:43:28Z @neo-opus-grace added the `regression` label
- 2026-07-10T01:48:25Z @neo-opus-grace cross-referenced by PR #14906
- 2026-07-10T04:33:27Z @neo-opus-grace assigned to @neo-opus-grace
### @neo-opus-grace - 2026-07-10T04:34:26Z

## Triage log 1 — both mechanisms named at line level (claimed; lane active)

**Finding 1 — the `moveTo` TypeError is a destroy-during-gesture race (cousin of #14911's class):** `tab/header/toolbar/SortZone.moveTo()` is `this.owner.up().moveTo(fromIndex, toIndex)` (`:34`). Since the producer rework (#14898) made cross-zone drops COMMIT a semantic operation, the resulting workspace re-projection destroys the SOURCE toolbar while the base `processDragEnd` latch is still completing — `owner.up()` on the orphaned/destroyed owner yields `undefined` → TypeError. Fix direction: the drag-end path guards for a destroyed/detached owner (`this.owner?.up()?.moveTo?.(...)` at minimum; better — the base SortZone's drag-end latch checks `owner.isDestroyed` before the within-container reorder branch, since a committed cross-zone drop makes the local reorder moot by definition).

**Finding 2 — the split-vs-tab drift is a producer classification gap for header-strip drops:** `DockPreviewProducer` five-zone hit-test (`:95-102`): interior → `tab-into`, outer `edgeBandRatio × min(w,h)` bands → edge/split. A drop on the TAB HEADER STRIP — the most intentional tab-into gesture there is, and exactly what `DockCrossZoneDragNL` performs — lands near the zone's top edge, INSIDE the band → classified `edge-top`/split → the observed `tabs-strategy-0` split node. It passed at #14864-time because the pre-producer path was the hardcoded which-rect → `moveItem` shortcut (the example's own comment documents the replacement). Fix direction: the producer's hit-test carves out the header strip — a drop within the tabs node's header band classifies `tab-into` regardless of the outer edge band (requires threading the header-strip extent or an upper-band exemption for `tabs`-type zones; geometry stays config-tier per the class's own non-reactive-threshold design).

**Sequencing:** fix 2 restores the spec's semantic expectation; fix 1 kills the console error and hardens the gesture path either way. Both are independently spec-pinnable (producer unit for the header-strip carve-out; the existing `DockCrossZoneDragNL` flips green as the e2e proof). Implementation next block on this branch (`agent/14913-crosszone-triage`).

*Fable 5 window (final entries).* — Grace

- 2026-07-10T04:44:33Z @neo-opus-vega cross-referenced by #14928
- 2026-07-10T04:50:36Z @neo-opus-grace cross-referenced by PR #14933
- 2026-07-10T11:57:59Z @neo-opus-grace referenced in commit `5cb8888` - "fix(dashboard): carve-out owns the strip row at every projected scale — cap at half-height, never deactivate (#14913)"
- 2026-07-10T12:21:05Z @tobiu referenced in commit `daf5d9e` - "fix(dashboard): header-strip carve-out + destroyed-owner drag guards restore cross-zone tab drops (#14913) (#14933)

* fix(dashboard): header-strip carve-out + destroyed-owner drag guards restore cross-zone tab drops (#14913)

* fix(dashboard): carve-out owns the strip row at every projected scale — cap at half-height, never deactivate (#14913)"
- 2026-07-10T12:21:05Z @tobiu closed this issue

