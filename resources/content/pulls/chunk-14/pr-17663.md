---
number: 17663
title: >-
  feat(agentos): defer wakes that would type into a pending interactive dialog
  (#17629)
author: neo-preview
state: OPEN
createdAt: '2026-08-23T23:49:22Z'
updatedAt: '2026-08-23T23:49:22Z'
closedAt: null
mergedAt: null
head: fix/17629-defer-wake-on-interactive-dialog
base: dev
url: 'https://github.com/neomjs/neo/pull/17663'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17629

## The problem

The wake adapter verified WHICH process receives keystrokes but not WHAT STATE the harness is in (#17231 bound the spatial half). When the target seat holds a pending interactive prompt, injected wake text becomes that prompt's answer — operator intent destroyed, corrupted answer data, double delivery cost.

## The fix — read before writing

Transport-split risk model: keystroke-class adapters are vulnerable by mechanism; the gate composes ticket Options 1+2 at the smallest seam.

**Adapter (`localWakeAdapters.mjs`)** — a new dialog-gate probe runs once before delivery on the osascript path: one read-only System Events query asking whether the target's focused UI element is a text-entry field. A readable non-text role means a prompt owns the input path → the adapter returns `{outcome: 'deferred', outcomeReason: 'interactive-dialog-pending'}` and never types. Any probe failure that does not name a dialog **fails open** into normal delivery — unreadable state must never silently withhold coordination (the mailbox stays authoritative).

**Receiver (`receiver.mjs`)** — accepts the new `deferred` outcome with the same park-and-reschedule contract as the context gate: transition back to `pending` with `deferCount`/`deferReason`, retried on subsequent drains. Dialogs are transient, so deferral is BOUNDED (`DIALOG_DEFER_BOUND = 20`): exhaustion transitions to `failed` with `dialog-defer-bound-exhausted:<reason>` plus an error log — observable starvation, never silent parking.

## AC Evidence

| AC | Proof |
|---|---|
| AC-1 | probe fires before any typing argv — dialog-pending state returns `deferred` and never reaches paste/submit; deferred records re-drain to composer-state delivery (deferred-not-dropped) |
| AC-2 | `localWakeAdaptersDialogGate.spec.mjs` run RED-first (6 failed / 2 passed pre-fix, failures matching documented pre-fix behavior), 8/8 green post-fix — the red receipt is the pre/post proof |
| AC-3 | receiver bounds deferrals at `DIALOG_DEFER_BOUND = 20`; exhaustion transitions to `failed` with `dialog-defer-bound-exhausted:<reason>` plus an error-level log line |
| AC-4 | envelope round-trip test asserts byte-identical payload on the post-defer delivery |
| AC-5 | legacy osascript suite untouched and green across the full 317-test wake sweep; the post-submit draft-restore invariant strengthened — typing-once asserted separately from the read-only probe |

## Deltas from ticket

- Probe is a separate spawn rather than inline AppleScript phases: keeps failure semantics clean (dialog-named errors classify as defer; everything else fails open) and leaves the delivery argv untouched.
- API-class transports (opencode-server/kimi-server) left unprobed this pass — their server-side queues make the vulnerability profile unmeasured, not proven-safe. Follow-up measurement rides the epic.

## Test Evidence

Red-first receipt: 6 failed / 2 passed before implementation (each failure matching a documented pre-fix behavior), 8/8 after. Full wake suite after rebase onto current dev: **317 passed**, including the strengthened post-submit draft-restore invariant.

Evidence: L2 (offline effects-injected arms at the adapter seam; no live seats disturbed) → L2 required (ACs govern injector behavior and deterministic park/bound semantics). Residual: AC-2 real-harness arm for the Claude Code seat shape (live dialog fixture), Residual-Owner: #13652.

## Post-Merge Validation

None required by the close-target ACs — behavior is fully exercised at L2 by the red-first fixture pair, and the probe is read-only (no plane mutation). The real-harness Claude-shape dialog fixture rides #13652 as declared residual; failure there would create a new ticket, not reopen this one.

Authored by Eos (ox-alpha, OpenCode). Session b644277f-7fcf-4079-a363-a7f9099a4566.
