---
id: 17640
title: 'Scaffold neomjs/neo-agent-brain: dev branch, labels, provenance shell'
state: CLOSED
labels:
  - enhancement
  - ai
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-23T18:18:52Z'
updatedAt: '2026-08-24T14:42:54Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17640'
author: neo-opus-vega
commentsCount: 0
parentIssue: 17500
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-24T14:42:54Z'
---
# Scaffold neomjs/neo-agent-brain: dev branch, labels, provenance shell

## Context

The operator created `neomjs/neo-agent-brain` (2026-08-23T17:57Z, `Neo.mjs AI Devs` team = maintainers) — Epic #17500's leaf 5, unblocked since the #17502 naming close + ADR 0040 merge. The repository is **empty**: unborn default branch, no contents, the 9 stock GitHub labels. Operator direction (in-session, same day): start the in-repo prep now — labels, package placeholder, shell files — while ticket migration waits for the cut; reserve heavier budget for the cut window.

## The Problem

Every downstream leaf needs a repository that is not a bare shell: leaf 8's canary transfer needs the **label taxonomy** provisioned first (its explicit first half); the move leaf needs a **`dev` working branch + branch discipline** to target; ADR 0040 §2.9 requires the **provenance statement** (fresh history + pointer, no `neomjs/neo` SHA rewrite) recorded where a visitor lands. An empty repo also cannot receive a pull request, so the very first commit is direct by necessity — that exception must be one minimal, recorded init commit, never a habit.

## The Fix

1. **Branch topology:** one minimal init commit (README shell only) creates `dev` directly — the single unavoidable direct push, recorded here; set `dev` as the default branch; `main` stays uncreated until the release machinery makes it (mirroring the Engine's main-is-release-only shape, per the new repo's own cadence per ADR 0040 §3).
2. **Labels:** sync the `neomjs/neo` taxonomy (name + color + description; create-or-update, stock labels updated in place) so leaf 8's canary lands on identical semantics.
3. **Shell files via branch + PR onto `dev`:** README (what this repo is, pre-move status, provenance pointer per ADR 0040 §2.9, link to Epic #17500 + the ADR in `neomjs/neo`), `LICENSE` (MIT, mirroring the Engine), `.gitignore` (minimal seed), `package.json` placeholder (`name`, `private: true`, `description`, a `$comment` naming ADR 0040 as the manifest authority and the move leaf as the writer — root scripts stay EMPTY: the root manifest becomes the Host-Edge package only at the move).
4. **Commit convention:** cross-repo ticket refs in full form (`neomjs/neo#NNNNN`) until the tracker migrates.

## Decision Record impact

`aligned-with ADR 0040` (§2.1 root topology deferred to the move; §2.9 provenance; naming). No ADR touched.

## Acceptance Criteria

- [ ] `dev` exists, is the default branch, and holds exactly one direct init commit (README shell); every subsequent change lands via branch + PR.
- [ ] The label set matches `neomjs/neo`'s (name/color/description), verified by a diff of both label lists in the PR/receipt.
- [ ] README states: pre-move shell · Brain-of-the-organism identity · provenance rule (fresh history, no Engine SHA rewrite, pointer to `neomjs/neo`) · Epic #17500 + ADR 0040 links.
- [ ] `package.json` placeholder is `private: true` with empty scripts and the `$comment` authority pointer; no dependency and no workspaces key.
- [ ] `main` does not exist.
- [ ] No production/AgentOS source file moves (the move leaf owns relocation; blocking proofs unaffected).

## Out of Scope

Ticket/tracker migration (leaf 8 canary + bulk) · any `ai/**` relocation · npm publish/name reservation (operator-owned) · CI workflows (the workflow-disposition leaf owns them) · seat provisioning (leaf 11).

## Related

Parent: Epic #17500 (leaf 5). Authority: ADR 0040. Naming: `#17502` (closed). Blocking proofs untouched: `#17533`.

Origin Session ID: 0fdaef3c-fcaf-4983-87a3-88d6eb611357

Retrieval Hint: `query_raw_memories("neo-agent-brain scaffold dev branch labels provenance shell")`

Live latest-open sweep: checked latest 8 open issues at 2026-08-23T18:13Z (newest #17635); no equivalent. A2A in-flight sweep: continuous wake stream this session; no scaffold claim.

## Timeline

- 2026-08-23T18:18:53Z @neo-opus-vega added the `enhancement` label
- 2026-08-23T18:18:53Z @neo-opus-vega added the `ai` label
- 2026-08-23T18:18:53Z @neo-opus-vega added the `agent-os` label
- 2026-08-23T18:19:00Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-23T18:19:03Z @neo-opus-vega added parent issue #17500
- 2026-08-23T18:21:50Z @tobiu cross-referenced by PR #1
- 2026-08-23T18:33:26Z @neo-gpt-emmy cross-referenced by PR #17637
- 2026-08-24T10:12:44Z @tobiu referenced in commit `194e939` - "chore: the ignore seed becomes a real one, and the copyright starts when the repo did (neomjs/neo#17640)

Three operator findings on the scaffold PR.

The LICENSE carried the Engine's 2015 start date. This repository was created
2026-08-23 and holds no code yet; claiming eleven years of copyright on an empty
tree is wrong in the direction that matters.

The ignore seed was three lines. The Brain is the repository whose working tree
sits next to the live plane, so the absent rules were `.env`, `.neo-ai-secrets`,
`.neo-ai-data`, and the resolved config overlays that carry hosts, ports, and
keys. That is not an incomplete seed, it is one `git add -A` away from
publishing an operator's credentials.

Derived from the Engine's file rather than invented: the Agent OS, harness, and
toolchain blocks are taken, and the Engine-tree blocks — app whitelists, docs
output, theme map, webpack json — are dropped, because ignore rules for paths
that do not exist are rules nobody can evaluate.

One rule is deliberately NOT copied verbatim. The Engine writes a bare
`.neo-ai-data` followed by a negation for its concepts directory, and git does
not descend into an ignored directory, so that negation never fires: its two
tracked concept files survive only because they predate the rule, and a new one
would be dropped in silence. Verified with check-ignore in both repositories.
Written here as `.neo-ai-data/*`, which makes the negation reachable — also
verified. The Engine carries the same defect and needs its own fix.

Ignore behaviour is asserted rather than assumed: every rule above was probed
with `git check-ignore -v`, including the two negations and a control path that
must stay tracked."
- 2026-08-24T10:14:01Z @neo-opus-vega cross-referenced by PR #2
- 2026-08-24T10:16:44Z @neo-opus-vega cross-referenced by #17697
- 2026-08-24T11:10:14Z @neo-gpt-emmy cross-referenced by #17699
- 2026-08-24T11:38:03Z @neo-gpt-emmy cross-referenced by #17701
- 2026-08-24T14:42:54Z @tobiu referenced in commit `6107629` - "Merge pull request #2 from neomjs/vega/17640-scaffold-shell

chore: the scaffold shell — license, a real ignore file, and the manifest placeholder (neomjs/neo#17640)"
- 2026-08-24T14:42:54Z @tobiu closed this issue
- 2026-08-24T14:53:04Z @neo-preview cross-referenced by PR #17698
- 2026-08-24T15:06:17Z @neo-opus-ada cross-referenced by PR #17700

