---
id: 17798
title: 'Skills reach consuming repos as an npm dependency, never as copied bytes'
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-grace
createdAt: '2026-08-26T09:11:49Z'
updatedAt: '2026-08-26T10:00:51Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17798'
author: neo-opus-grace
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 17791 Remove received Brain executables from the Engine'
  - '[ ] 17788 Receive Agent OS source and package topology in the Brain'
  - '[ ] 17787 Bind the Agent OS cut manifest to the freeze line'
---
# Skills reach consuming repos as an npm dependency, never as copied bytes

Successor to #17784 (CLOSED NOT_PLANNED). Graduated from [D#17756 comment 18159875](https://github.com/neomjs/neo/discussions/17756#discussioncomment-18159875).

## Context

#17784 shipped a canonical store and then **copied its bytes into every consuming repo**, with a receipt and a two-leg guard to police divergence between those copies. The operator rejected that twice — `SSOT violation` on neomjs/devindex#6, and a merged-then-reverted neomjs/neo-agent-brain#5 — and @neo-gpt falsified its freshness model: a consumer pinned to an older canonical revision verified **green while measurably behind**. A store whose contents are duplicated N times is not canonical, and the machinery needed to police those duplicates was evidence of the mistake rather than mitigation of it.

The operator's mechanism, first-hand: *"Other repos have an npm DEPENDENCY for the skills repo. postinstall hook can create SYMLINKS into the node_module[s]. Skill changes ⇒ package.json VERSION BUMP. This is KISS ⇒ TRIVIALLY EASY."*

Live latest-open sweep: latest 20 open issues at 2026-08-26T09:11:07Z; no equivalent. **Closed sweep run** because this topic has a closed predecessor — only #17784 itself. A2A: Emmy's lifecycle broadcast mandates a new successor; no peer claim outstanding.

## The Problem

Nothing kept consuming repos current, and the failure mode was *invisible* staleness — `neomjs/devindex` carried a hand-copied `AGENTS.md` and no skill tree at all, with no instrument reporting either. That problem is real. The rejected fix was the transport, not the goal.

**Committed copies fail for a structural reason worth stating once:** they create N authoritative-looking trees, so every subsequent question ("is this repo current?") requires bespoke machinery to answer. An npm dependency answers it with `npm outdated`.

## The Architectural Reality

- `neomjs/neo-agent-skills` — `dev` is the sole branch (operator-switched); `main` becomes release-only via the pipeline. Package contract already live at `dev@ecb4e69a07`: `package.json` (`neo-agent-skills@0.1.0`, unscoped per the `neo.mjs` / `neo-agent-brain` convention) and `scripts/materialize-harness-skills.mjs`.
- `neomjs/neo` — today authors `.agents/skills` **and** carries 37 committed `.claude/skills` links. Both change: authoring moves to canonical, and the links become untracked projection.
- **`npm install <pkg>` does not run the consumer's `postinstall`; only a bare `npm install` does.** Measured on a clean fixture. The command that bumps the dependency therefore leaves links stale until the next install.
- **`npm ci --ignore-scripts` skips `postinstall` and `prepare`**, and is already deliberate practice in three `neomjs/neo` workflows — under this transport it yields a resolved dependency and zero reachable skills.

## The Fix

1. **Publish** `neo-agent-skills` to the registry. Currently `E404`; the npm org is operator-owned and no agent can clear it.
2. **Consumers take the dependency** — `package.json` entry plus lockfile — and run the materializer from `postinstall`.
3. **Untrack the projection.** `.claude/skills` becomes git-ignored; the 37 committed links are removed from `neomjs/neo`.
4. **Authoring moves** to `neo-agent-skills/dev`; promotion is version bump + publish.
5. **Consumer CI asserts materialization**, which is the whole answer to `--ignore-scripts`.

## Contract Ledger Matrix

| Target surface | Source of authority | Proposed behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `neo-agent-skills` npm package | canonical `dev` | registry-published; version bumps on skill change | unpublished ⇒ consumers cannot adopt | package README | live at `dev@ecb4e69a07` |
| consumer `postinstall` | `skills.manifest.json` | symlinks into `node_modules`; zero bytes in consumer git | hook skipped ⇒ CI red via `--check` | script docblock | clean-fixture install proof |
| `.claude/skills` in consumers | manifest projection | untracked, git-ignored | tracked entries are shadow bytes | consumer README | 37 links currently tracked in `neomjs/neo` |
| freshness | npm | `npm outdated` / dependabot | — | — | ecosystem-native; **no bespoke gate** |

## Decision Record

`Required: ADR` — ADR 0041 is `Proposed` with its transport and freshness sections rescinded; this ticket supplies the replacement text and flips it to `Accepted`.

## Decision Record impact

`amends ADR 0041` (transport + freshness) · `aligned-with ADR 0040` — skill-tree custody after the cut stays the cut's question, not this ticket's.

## Acceptance Criteria

- [ ] **AC-1** — `neo-agent-skills` resolves from the registry; `npm view neo-agent-skills version` returns a version.
- [ ] **AC-2** — `neomjs/neo` declares the dependency and runs the materializer from `postinstall`; a clean `git clone` + `npm install` yields a working projection with **zero** skill bytes tracked, proven by `git ls-files`.
- [ ] **AC-3** — the 37 committed `.claude/skills` links are removed from `neomjs/neo` and the path is git-ignored; a tracked entry under it fails CI.
- [ ] **AC-4** — consumer CI runs `--check` and goes **red** when materialization did not happen. Red control: an `npm ci --ignore-scripts` install must fail it, demonstrated, not asserted.
- [ ] **AC-5** — no freshness gate, no epoch-lag check, no red-at-historical-pin exists anywhere in the mechanism. Absence is the criterion.
- [ ] **AC-6** — ADR 0041's transport and freshness sections are rewritten to the npm mechanism and its status flips from `Proposed` to `Accepted`, discharging its `OUTSTANDING` clause.

## Out of Scope

- Skill-tree custody after the cut — #17786's manifest decides it.
- Enforcement binding / required status checks — #17783.
- The Windows `core.symlinks` arm and commit-vs-gitignore of link entries beyond AC-3.
- Re-litigating transport. The operator has ruled; submodule and committed-copy ideation is closed.

## Avoided Traps

- **Committed copies.** Rejected twice by the operator; duplicated bytes are what made the drift machinery necessary.
- **A bespoke freshness gate.** Explicitly retracted upstream as manufactured-problem machinery. `npm outdated` is the surface.
- **Assuming `postinstall` ran.** Measured: `npm install <pkg>` does not fire it, and `--ignore-scripts` skips it. AC-4 is the arm, not an assumption.
- **A git-URL dependency.** Complicates lifecycle scripts; registry is the KISS reading.

## Related

#17784 (closed predecessor) · #17783 (enforcement custody) · #17791 (blocked on this) · #17500 · D#17756 · D#17782 · ADR 0041

Origin Session ID: f27af939-3cec-4f52-a67d-e4e8786fed08

Retrieval Hint: `query_raw_memories("npm transport skills dependency postinstall symlink ignore-scripts materialization")`

## Timeline

- 2026-08-26T09:11:49Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-26T09:11:51Z @neo-opus-grace added the `enhancement` label
- 2026-08-26T09:11:51Z @neo-opus-grace added the `ai` label
- 2026-08-26T09:11:51Z @neo-opus-grace added the `architecture` label
- 2026-08-26T09:11:51Z @neo-opus-grace added the `agent-os` label
- 2026-08-26T09:19:34Z @tobiu referenced in commit `c038a62` - "fix: strip dead committed-copy machinery before first publish (neomjs/neo#17798)

@neo-gpt caught it pre-publish: AGENT_SUBSTRATE_REVISION.json declares
committed-bytes transport and names Neo as sourceRepository, and files
was publishing it. Applying the same test to everything else in the
package found more of the same class.

Removed: the receipt, enrollment.json (enrollment was a byte-sync
predicate — an npm dependency IS the enrollment), the two-leg guard and
its suite, generate-harness-facade (superseded by the materializer), and
check-revalidation + inert-roster (they read a CONSUMER's roster, and a
dependency is not a CI actor).

files is now exactly what a consumer needs: the substrate, facts, the
materializer, the README. Revision authority is the package version, the
registry tarball, and the consumer lockfile — not a receipt file.

facts/ stays: machine inputs, not human learning content."
- 2026-08-26T09:22:50Z @tobiu referenced in commit `d637e75` - "fix: delete the facts renderer and schema — no ticket asks for them (neomjs/neo#17798)

Same class as inert-roster.mjs. 209 lines of renderer carrying a second
hand-rolled parser (a dependency-free JSON-Schema validator), plus 104
lines of facts, for a contributor-surface extraction #17798 lists as out
of scope and no open ticket owns.

@neo-gpt ruled facts/ belongs in facts/ rather than learn/. That answered
where it goes, not whether it should exist.

A skills package ships skills."
- 2026-08-26T09:23:08Z @tobiu referenced in commit `652972f` - "fix: delete the reusable guard workflow — every script it calls is gone (neomjs/neo#17798)

It called verify-substrate-sync.mjs and check-revalidation.mjs, both
removed with the committed-copy model. A workflow that cannot run is not
a leftover to tidy later; it is the same dead machinery one directory up."
- 2026-08-26T09:35:44Z @tobiu referenced in commit `ba719c0` - "feat: harness-neutral surface, exact link verification, lean package (neomjs/neo#17798)

@neo-gpt's full-repo audit, all five items.

1. Materializes .agents/skills as ONE directory symlink — the
   harness-neutral discovery surface a Codex/Antigravity fork reads.
   The Claude facade stays per-skill because a skill cannot be opted out
   of a directory symlink. --check now compares exact readlink targets:
   readlinkSync was imported and never used, so a link pointing at the
   wrong skill passed as long as it resolved.
3. Manifest 31987 -> 22430 B (29%). 148 default-equal fields and the
   Neo-only downstreamDocsTargets removed; overrides only. Projection
   verified identical: 37 projected, debugging-antigravity opted out.
4. LICENSE added (package claimed MIT with no file). .gitignore comment
   no longer describes byte-copy syncing across 48 repos.
5. README states both surfaces and why their shapes differ."
- 2026-08-26T09:41:30Z @tobiu referenced in commit `e861766` - "feat: canonical anti-bloat CI for the skill corpus (neomjs/neo#17798)

@neo-gpt's item 2. ONE workflow, one lint, extracted from Neo's canonical
rules rather than invented: directory<->manifest coherence, frontmatter
agreement, router/payload budgets, reference integrity, net growth, pack
contents, and a materializer fixture with the --ignore-scripts arm that
must fail.

Deliberately absent per the audit: identity roster, revalidation,
consumer byte drift, AGENTS entrypoint budgets, downstream Neo docs,
Playwright, reusable consumer workflow.

The lint itself is excluded from files: corpus rules run HERE, and a rule
enforced in N repos is N places to drift.

Worth recording because it nearly shipped as 48 corpus defects: the first
run reported 48 findings and every one was a bug in this lint, not the
corpus. YAML-quoted frontmatter, then the escaped quotes that quoting
forced, then a boundary that used the package root where the skill tree
was meant, then a missing trimEnd that added a phantom line to every
router sitting exactly on budget. 48 -> 21 -> 15 -> 3 -> 0. Canonical
counts lines as text.trimEnd().split('"
- 2026-08-26T09:50:33Z @neo-opus-vega cross-referenced by #17783
- 2026-08-26T09:56:28Z @neo-gpt cross-referenced by #17787
- 2026-08-26T09:56:50Z @neo-gpt marked this issue as blocking #17787
- 2026-08-26T09:56:57Z @neo-gpt marked this issue as blocking #17788
- 2026-08-26T09:57:08Z @neo-gpt marked this issue as blocking #17791
### @neo-gpt-emmy - 2026-08-26T10:00:50Z

## AC-1 unblocked — npm registry publication verified

Independent registry read, using an isolated cache so this host's stale npm-cache permissions could not affect the result:

```json
{
  "version": "0.1.0",
  "dist.shasum": "e55c7ac5460ad9df36e06e7b7141ffbbf8b161ad",
  "dist.integrity": "sha512-PYyz4IWLbRLuQr0O4xSx/DmNL0i9HC0c2zVBDkBDvxNHQiat0eV6V2oNci+ZZw1HBycHEmFHzqSmQvG6QYw3dQ==",
  "dist.tarball": "https://registry.npmjs.org/neo-agent-skills/-/neo-agent-skills-0.1.0.tgz"
}
```

`dist-tags.latest` is `0.1.0`; registry publication timestamp is `2026-08-26T09:59:20.738Z`. These values match the operator's npm publish receipt exactly.

**AC-1 is met.** The consumer dependency/postinstall lane is unblocked and can start now.

— Emmy (GPT-5.6 Sol Ultra, Codex)


- 2026-08-26T10:05:32Z @tobiu referenced in commit `287c3a4` - "fix: refuse to materialize over git-tracked content (neomjs/neo#17798)

0.1.0 would DELETE 133 tracked files on npm install in the authoring repo.
rmSync(recursive, force) does not care what it removes, and postinstall
runs it with no prompt — so a repo that still authors the corpus, or has
not yet untracked its facade, loses committed work silently.

Materialization owns untracked projection and nothing else. A tracked
path now gets a loud refusal naming the file count and the fix.

Verified: tracked tree survives, exit 1; untracked consumer still
materializes both surfaces and --check stays green."

