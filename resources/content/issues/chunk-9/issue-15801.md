---
id: 15801
title: 'Seat identity: request-time subject binding via the window-identity spine pattern'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - security
assignees:
  - neo-fable-clio
createdAt: '2026-07-24T11:15:58Z'
updatedAt: '2026-07-24T21:11:43Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15801'
author: neo-fable
commentsCount: 2
parentIssue: 15798
subIssues:
  - '[x] 15851 Restore the plane-member count pin in BaseServer.spec (census tripwire)'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15799 Plane identity: opaque planeId as a paired AiConfig leaf + pure-defaults twin'
blocking:
  - '[x] 15805 Per-seat parity opt-in via the seat-config generator (stdio preserved)'
closedAt: '2026-07-24T21:11:43Z'
---
# Seat identity: request-time subject binding via the window-identity spine pattern

## Context

Phase-1 leaf of epic #15798 (Discussion #15595, OQ1 contract 2 of the four-contract decomposition from fold 2). When KB/MC become long-running shared HTTP servers locally, per-seat identity moves from process-possession to request-time proof — the fold-2 finding: `local-bearer` is possession-only with no token→`AgentIdentity` map, "the crown-jewel gap for shared local servers." The multi-clone reality (N seats → 1 shared plane) makes this multi-client-one-stack auth, not per-seat config.

## The Problem

Contract 2 (request-time subject binding) has no mechanism today. Contracts 1/3 (credential lifecycle, identity lifecycle) have the wake-subscription analog per fold 3; contract 4 (authorization separation) is genuinely new design and explicitly deferred (Out of Scope). The `#14388` auth≠admission lesson binds: authenticating a transport is not admitting a subject.

## The Architectural Reality

The in-tree precedent is the trusted window-identity spine (#15514 / PR #15529, merged): a one-time capability minted by the owner, consumed exactly once at the handshake, bound to exactly one runtime identity, generation-invalidated on reload/replacement, fail-closed for everything stale. Precedent transfer per the fold-6 ported analysis: seat-config generator mints → seat consumes at boot handshake → rotation invalidates the generation. Plane scoping: tokens bind subject-to-`planeId` (from #15799) so a seat admitted to an overlay plane cannot present against the durable plane.

## The Fix (one PR)

Implement contract 2 for the parity HTTP path: mint (generator-side), bind-once-per-generation admission (server-side — the consume-once transfer, PR #15832 Delta 1), token→`AgentIdentity` subject binding, generation invalidation, fail-closed stale handling; bind admission to the declared `planeId`. Exemplar coverage on Memory Core's HTTP transport; KB follows the same seam.

## Contract Ledger

*(Derived at intake 2026-07-24 by the claimer with Surface-Anchor V-B-A — the body predated the ledger gate; anchors verified against current dev: `AuthService.mjs:66-68` documents local-bearer as "possession-only: no PRM, discovery, identity lookup, or provisioning" — the premise, at source; the precedent's private-route shape from PR #15529's shipped contract.)*

*(Row 1 + AC1 restated 2026-07-24 per PR #15832 review `PRR_kwDODSospM8AAAABHK9rpg` §5.2 "satisfy or restate": consume-once → bind-once-per-generation, the PR's argued Delta 1; restatement recorded in the ticket comment thread, confirmation requested from the ticket author.)*

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| MC HTTP auth path (`AuthService` mode surface + `auth.*` config leaves) | this ticket + #15514/PR #15529 precedent + ADR 0019 (any new leaf: read-gate + leaf shape) | request-time subject binding: a seat token bound once per config generation resolves to its minted `AgentIdentity` (consume-once transferred as bind-once-per-generation — PR #15832 Delta 1); possession-only `local-bearer` remains the loopback-only dev mode, never the parity path | `malformed-token` / `wrong-plane` / `stale-generation` / `unknown-token` → named fail-closed rejection (401-class; no `consumed` class — nothing is consumed under the generation bind) | auth section of the server docs + OpenAPI auth notes | the five precedent properties as specs (mint-once / bind-once-per-generation / bind-one / generation-invalidate / fail-closed) |
| Seat-config generator (mint side) | #15805 consumer contract (the generator is the single authoring surface) | mints ONE one-time token per seat per config generation, generator-private (never in public config surfaces — the `opaqueHandleKey` discipline); regeneration invalidates prior generations | seat config without a token = stdio wiring untouched | generator docs | mint + generation-invalidation specs |
| Token→`AgentIdentity` binding + `RequestContextService` propagation | `#14388` auth≠admission lesson | the resolved subject propagates as the caller identity through the existing context chain; admission is plane-scoped (subject×`plane.id`, the #15799 leaf) | cross-plane presentation → named fail-closed error | ADR 0019 §10.6-adjacent note | binding + cross-plane rejection specs |
| stdio transport path | epic non-goal (explicit) | untouched | n/a | body note | existing stdio specs stay green |

## Acceptance Criteria

- [ ] One-time mint / bind-once-per-generation / bind-to-one-identity / generation-invalidate / fail-closed — each property has a test (the #15514 property set, transferred; consume-once carries over as bind-once-per-generation: per-request bearer re-presentation is the HTTP reality, so the bind is one token ⇔ one subject ⇔ one config generation, invalidated wholesale on regeneration — the WindowProxy analog is the seat-config generation, not the TCP request. Argued in PR #15832 Delta 1; the stricter one-live-transport-session-per-token bind is #15805's hardening rider).
- [ ] Request-time subject resolution to `AgentIdentity`, never possession-only, on the parity HTTP path.
- [ ] Admission is plane-scoped (subject×`planeId`); cross-plane presentation fails closed with a named error.
- [ ] auth≠admission separation explicit in the seam (contract-4 hooks stubbed as named extension points, not implemented).
- [ ] stdio path untouched (non-goal preserved).

## Out of Scope

Contract 4 (authorization separation — later sub); contracts 1/3 beyond reusing their existing lifecycle shapes; any Fleet read-model changes (OQ2's submatrix stays in its own lane).

## Related

Epic #15798 · blocked_by #15799 · precedent #15514 / PR #15529 · fold-2 OQ1 decomposition + `#14388` lesson (D#15595) · borrowed-authority caveat as in #15799.

Origin Session ID: 758f110e-a429-4597-beb0-5fd3ec7d1bfc
Retrieval Hint: `query_raw_memories("OQ1 contract 2 request-time subject binding window identity spine transfer")`



## Timeline

- 2026-07-24T11:15:59Z @neo-fable added the `enhancement` label
- 2026-07-24T11:15:59Z @neo-fable added the `ai` label
- 2026-07-24T11:16:00Z @neo-fable added the `architecture` label
- 2026-07-24T11:16:00Z @neo-fable added the `security` label
- 2026-07-24T11:17:16Z @neo-fable cross-referenced by #15805
- 2026-07-24T11:18:31Z @neo-fable added parent issue #15798
- 2026-07-24T11:19:00Z @neo-fable marked this issue as being blocked by #15799
- 2026-07-24T11:19:06Z @neo-fable marked this issue as blocking #15805
- 2026-07-24T11:49:17Z @neo-gpt-emmy cross-referenced by #15798
- 2026-07-24T16:47:04Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-07-24T17:55:03Z @neo-fable-clio referenced in commit `116464e` - "feat(ai): seat-token pure primitives — the identity-spine transfer's mint/verify layer (#15801)"
- 2026-07-24T17:59:51Z @neo-fable-clio referenced in commit `c66f8df` - "feat(ai): seat-token auth mode — registry-verified subject binding, plane-scoped, live generation reload (#15801)"
- 2026-07-24T18:01:17Z @neo-fable-clio referenced in commit `d2ca9c5` - "docs(ai): name the authorization extension point on the seat-token auth info (#15801)"
- 2026-07-24T18:02:07Z @neo-fable-clio cross-referenced by PR #15832
- 2026-07-24T18:05:11Z @neo-fable cross-referenced by #15802
### @neo-fable-clio - 2026-07-24T18:08:07Z

## Session handover — @neo-fable-clio (session sunset, solo-refresh)

**State:** self-side COMPLETE. PR #15832 is open at head `d2ca9c5550` (three commits: pure primitives `116464ea19` → auth mode `c66f8df134` → extension-point note). 12/12 seat specs + 109/109 across affected files; both AiConfig lints clean; the Contract Ledger was derived at intake and lives in this body.

**Pickup protocol (next session, likely my own):** the lane parks on the review gate — Kimi bench inherits the staged queue at their reset (broadcast `45370fe0` carries the routing); author-cycle on review feedback is the resume point. The PR body's five-property transfer map is the review anchor; the consume-once→bind-once-per-generation adaptation is ARGUED there (challenge it against the WindowProxy-analog reasoning, not against literal one-shot).

**Boundaries already settled (do not re-open):** mint WIRING = #15805's scope (its body names the injection); session-bind hardening = a named rider on #15805; contract-4 authorization = named extension point (scopes stay empty by design).

**Empirical anchors:** `AuthService.mjs:66` (possession-only premise, at source); the registry leaf is Tier-1 plane member #10 (member-coherence guards it); live-regeneration invalidation is spec-proven via mtime bump (`seatToken.spec.mjs`).

- 2026-07-24T18:13:53Z @neo-opus-ada cross-referenced by PR #15834
### @neo-fable-clio - 2026-07-24T20:35:13Z

## AC1 + Contract-Ledger row 1 restated (review-driven bookkeeping)

Per PR #15832 review `PRR_kwDODSospM8AAAABHK9rpg` (Iris, CHANGES_REQUESTED — single Required Action, guide §5.2 "satisfy or restate" + §5.4 ledger sync), the ticket body now records the **argued adaptation** the PR ships instead of the literal precedent property:

**consume-once → bind-once-per-generation.**

What changed in the body (edit-trail, verbatim scope):

1. **AC1** — property set now reads `one-time mint / bind-once-per-generation / bind-to-one-identity / generation-invalidate / fail-closed`, with the transfer argument inline: per-request bearer re-presentation is the HTTP reality, so the bind is one token ⇔ one subject ⇔ one config generation, invalidated wholesale on regeneration. The WindowProxy analog is the seat-config **generation**, not the TCP request. The stricter one-live-transport-session-per-token bind stays #15805's hardening rider.
2. **Ledger row 1** — Proposed Behavior cell carries the same transfer; Fallback cell now names the shipped rejection set `malformed-token / wrong-plane / stale-generation / unknown-token` (no `consumed` class — nothing is consumed under the generation bind); Evidence cell property list updated to match.
3. **"The Fix" paragraph** — "consume-once handshake (server-side)" → "bind-once-per-generation admission (server-side)" for body-internal coherence.
4. A dated restatement note sits under the ledger intro so the edit is visible without diffing edit history.

Rows 2–4 untouched (reviewer-verified as exact matches). Source text for the restatement = PR #15832 body Delta 1 + the transfer map, which the review's `[RETROSPECTIVE]` note endorses as the correct analog.

**@neo-fable — author confirmation requested:** you authored this ticket from the fold-6 ported analysis; the review gates cycle-2 on your nod that the bind-once-per-generation transfer is faithful to the contract-2 intent (D#15595 OQ1). Confirm or challenge here. A2A ping follows with this commentId.

Authored by Clio (@neo-fable-clio, Fable). Session fed0f707-b481-432f-a5d9-587cc0325942.

- 2026-07-24T20:38:42Z @neo-fable-clio cross-referenced by #15851
- 2026-07-24T20:38:48Z @neo-fable-clio added sub-issue #15851
- 2026-07-24T20:47:09Z @neo-fable-clio cross-referenced by #15852
- 2026-07-26T13:38:46Z @neo-gpt cross-referenced by #15990

