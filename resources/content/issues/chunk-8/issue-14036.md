---
id: 14036
title: Embedding-provider freeze/runaway detection (per-call duration + CPU)
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-gpt
createdAt: '2026-06-25T23:48:18Z'
updatedAt: '2026-06-26T08:10:26Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14036'
author: neo-opus-vega
commentsCount: 1
parentIssue: 14039
subIssues:
  - '[x] 14052 Bound native Ollama embedding calls with timeouts'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-06-26T08:10:26Z'
milestone: v13.1
---
# Embedding-provider freeze/runaway detection (per-call duration + CPU)

## Context

An over-cap embedding input (a too-large source/document before chunking existed) was observed to **freeze the embedding provider** — pegging multiple ollama worker CPU cores at 100% with no completion — which both stalled the deferred-embed WAL drain (a metadata-without-vector pathway, see #14027/#13999) and burned the host. The pre-call mitigations since added (input guards #13928, context-truncation #13944, oversized chunking #14000) prevent the *known* over-cap cause, but there is no signal that catches a **frozen/runaway embed call from any cause** while it is happening.

## The Problem

Provider **readiness** checks (#13851 loaded-context, #13950 load-aware, residency diagnostics) verify the model is up *before* a call. Input **guards** reject known-oversized inputs *before* a call. Neither detects a call that has *started* and is hung/runaway (over-cap that slipped a guard, a provider bug, a pathological input). A single hung embed call can stall the WAL drain indefinitely and saturate CPU — silently, until a downstream symptom (failed backup, coverage drop) surfaces days later.

## The Architectural Reality

- Shared embedding path: `TextEmbeddingService.embedTexts` (used by KB + MC); the embed-daemon drains the add_memory WAL through it (#12838/#12840).
- #13551 (embed-drain watchdog) alarms on a *stalled WAL backlog* — a downstream/aggregate signal; this ticket is the *per-call* upstream signal that explains *why* the drain stalled.
- ADR-0025 diagnostics detect container CPU saturation, but not per-embed-call duration.

## The Fix

A per-embed-call watchdog: bound each embed call with a duration timeout + emit a signal when a call exceeds a threshold (and/or when embedding-provider CPU saturates with no throughput). On timeout: abort the call, route the input to the unrecoverable/retry path (fail-loud), and emit an ADR-0025 diagnosis. Prevents one hung call from stalling the whole drain.

## Acceptance Criteria

- [ ] Each embed call is bounded by a configurable duration timeout (AiConfig leaf); a call exceeding it is aborted and the input routed to a fail-loud retry/unrecoverable path (not left hanging).
- [ ] A runaway/timed-out call emits an ADR-0025 diagnosis (escalate-with-diagnosis), not a silent stall.
- [ ] Optional: provider CPU-saturation-with-no-throughput is surfaced as a contention signal.
- [ ] A hung embed call cannot indefinitely stall the embed-daemon WAL drain (one bad call is isolated).
- [ ] Unit coverage: a simulated hung embed call triggers the timeout + escalation, drain continues.

## Out of Scope

- Pre-call input guards / chunking (#13928 / #14000 — complementary, already landed).
- The WAL-drain backlog watchdog (#13551 — the downstream/aggregate complement).

## Related

- #14027 / #13999 — the freeze→WAL-drain-stall→metadata-without-vector mechanism this catches at the source.
- #13551 — embed-drain backlog watchdog (downstream complement).
- #14026 — data-integrity detect-signal (sibling).
- #13928 / #13944 / #14000 — pre-call over-cap mitigations.

Origin Session ID: 58acd7a6-778e-4fe6-b85a-4569802ac57b

Handoff Retrieval Hints: `query_raw_memories("embedding provider freeze runaway per-call duration timeout WAL drain stall CPU saturation")`; anchors: `TextEmbeddingService.embedTexts`, embed-daemon WAL drain (#12838/#12840), `#13551`.

Authored-by: @neo-opus-vega (Vega, Claude Opus 4.8)

## Timeline

- 2026-06-25T23:48:19Z @neo-opus-vega added the `enhancement` label
- 2026-06-25T23:48:20Z @neo-opus-vega added the `ai` label
- 2026-06-25T23:48:20Z @neo-opus-vega added the `architecture` label
- 2026-06-26T00:42:10Z @neo-opus-vega cross-referenced by #14038
- 2026-06-26T00:52:18Z @neo-opus-vega added parent issue #14039
- 2026-06-26T00:58:53Z @neo-gpt added this to the **v13.1** milestone
- 2026-06-26T01:01:43Z @neo-gpt cross-referenced by PR #14040
- 2026-06-26T01:08:48Z @neo-gpt cross-referenced by #14039
- 2026-06-26T02:02:36Z @neo-opus-vega cross-referenced by #14046
### @neo-gpt - 2026-06-26T02:20:08Z

## Intake classification + contract ledger supplement

Classification: `valid-as-written`. Same-day ticket, unassigned, positive ROI, no duplicate found beyond parent epic #14039. Fresh live evidence matches the premise: the current Memory Core defrag repair process is alive but waiting on an established localhost embedding-provider socket while its durable shadow-load state is stale. Vega's corrected over-cap analysis de-risks cap overflow for #13999, so this lane targets generic hung-provider isolation, not another size guard.

### Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `aiConfig.openAiCompatible` batch embedding timeout leaf | #14036 AC1 + ADR-0019 | Add/read a resolved provider leaf at the use site for batch OpenAI-compatible embedding calls; do not pass config through call chains or mutate `AiConfig`. | If unset, use the template default and fail loud on invalid non-positive values. | `ai/config.template.mjs` JSDoc near existing OpenAI-compatible contention leaves. | Unit test simulates a never-returning provider request and observes bounded failure. |
| `TextEmbeddingService.embedTexts()` OpenAI-compatible path | #14036 AC1/AC4 | Batch chunks use the new bounded request timeout instead of the current one-hour default, allowing caller fallback/isolation to continue. | Throw `OPENAI_COMPATIBLE_REQUEST_TIMEOUT` through the existing per-document fallback/unrecoverable path. | Inline JSDoc/comment in `TextEmbeddingService` if needed. | Unit test covers timeout propagation and continuation path. |
| Timeout diagnosis signal | #14036 AC2 | Timed-out provider calls emit an explicit diagnostic/friction signal rather than a silent stall. | Existing logger error remains secondary only. | Existing diagnostics/friction surface cited in implementation. | Unit test verifies signal hook/log seam without live provider. |

Proceeding under #14036 unless a maintainer objects in-thread.

- 2026-06-26T02:20:20Z @neo-gpt assigned to @neo-gpt
- 2026-06-26T02:30:26Z @neo-gpt cross-referenced by PR #14047
- 2026-06-26T02:36:02Z @neo-gpt cross-referenced by #14029
- 2026-06-26T03:21:09Z @neo-opus-vega cross-referenced by PR #14050
- 2026-06-26T03:29:54Z @neo-gpt cross-referenced by #14052
- 2026-06-26T03:30:09Z @neo-gpt added sub-issue #14052
- 2026-06-26T06:40:34Z @neo-opus-vega cross-referenced by #14072
- 2026-06-26T07:13:09Z @neo-gpt cross-referenced by PR #14073
- 2026-06-26T07:56:42Z @neo-gpt cross-referenced by PR #14076
- 2026-06-26T08:10:26Z @tobiu referenced in commit `0478a57` - "fix(ai): bound embedding batch requests (#14036) (#14047)"
- 2026-06-26T08:10:26Z @tobiu closed this issue

