---
number: 17491
title: >-
  feat(fleet): five type roles, the spacing rhythm and FM motion aliases become
  real tokens (#17265)
author: neo-fable-clio
state: OPEN
createdAt: '2026-08-21T18:23:38Z'
updatedAt: '2026-08-21T20:00:49Z'
closedAt: null
mergedAt: null
head: feature/17265-cockpit-type-density-ladder
base: dev
url: 'https://github.com/neomjs/neo/pull/17491'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17265

The §04 type, spacing and motion ladders stop being prose and become the token layer the cockpit actually renders through: five type roles land as `font` shorthand tokens at the shell root (`--fm-text-display/body/detail/micro/chrome`, plus the chrome role's non-font half `--fm-text-chrome-tracking`), the 4/8/12/16 rhythm lands as `--fm-space-1..4`, and the FM motion aliases (`--fm-motion-panel/fast`) point at the product vocabulary so the vocabulary-layer reduced-motion collapse is inherited, not re-implemented. All 92 measured `font-size` literals across 22 fleet skin files resolve through a role per the TOKENS.md ruled dispositions (12.5→body · 10.5→micro · 9→micro deliberate growth · 11.5/13 ties-to-smaller · 15→display), leaving exactly two annotated survivors: the AgentCard narrow-mode 16px glyph (the spec's named #17211 deliberate-move slot) and the AgentDetail inner tab strip (nav-tab anatomy is #17269 cut 2's decision — a migration pass must not decide navigation design). The `check-theme-surfaces` census guard grows by the twelve structural tokens, so deleting a contracted member fails the build.

Evidence: L3 achieved (dev-server live render, both skins, computed-style probes on every role tier; full owning unit tree + agentos e2e re-executed; the committed visual pins re-baselined ON the laddered chrome in-PR; a rendered panel-reveal collapse witness added) → L3 required (AC-2 "spot-checked live at both densities" + AC-3 rendered motion evidence + AC-4 baseline capture). Residual: none.

## Deltas from ticket

1. **Roster density converted from viewport media queries to container queries** (`FleetGrid.scss`): the spec's density row rules "2-up to the 20-card fleet, 3-up only past 1280px of grid measure" — the shipped 3-up-by-viewport default both violated the stated step and lied inside vessel windows (a narrow pane in a wide viewport rendered 3-up). The grid is now its own `inline-size` container; verified live: fronted 1214px grid measure → 2 columns, backgrounded 0×0 vessel → 1 column (the own-measure discipline responding honestly).
2. **Chip-family drift healed**: `AgentConfigCard.scss`'s local `.fm-chip` block had re-declared geometry/type (12px, pad 2/8, radius 6) that drifted from the Chips.scss family (detail metrics, pad 3/9); the local block now keeps only its state classes + state-transition motion, the family owns anatomy. The `InstanceManager`/`InstanceSwitcher` word-chip pills bind the chip geometry tokens their values already matched.
3. **Affordance-chip voice rule recorded** (`Chips.scss`): the affordance class consumes detail-role METRICS with `font-family: inherit` — a pressable control speaks the surrounding surface's voice, not the observation tier's mono. Applied to the memories/catch-up picker chips.
4. **`--fm-text-chrome-tracking` minted as the contract's sixth type token** — the chrome role's `.08em` half cannot ride a `font` shorthand; recorded in the TOKENS.md type-roles row citing this ticket. The EventChip kind word binds the same constant (one uppercase-tracking voice).
5. **Measured discovery, recorded not fixed**: the AgentCard name ROOT consumes the display role correctly (14/1.3·600, FM sans — probe-verified), but the framework button theme re-sizes the child `.neo-button-text` span (16px, theme face), before and after this pass alike. Resolving that child override is the #17211 oversize slot; an in-file record marks it.
6. **Off-rhythm spacing is recorded, never bulk-snapped**: the measured off-ladder population (10px ×29, 6px ×19, 7px ×9 …) is per-file header-recorded and deferred to #17268 — only the spec's NAMED snaps land (card padding 10→12, panel padding = space-4). Forcing the rest would have been a silent per-pane redesign inside a migration pass. **Post-review completion (RA-1):** every on-ladder member of a mixed shorthand now binds its token too (`24px var(--fm-space-2)` and kin) — an off-rhythm neighbor no longer exempts the laddered half; the sweep grep returns zero unbound on-ladder members in touched files.
7. **The visual pins are re-baselined in-PR as the reviewed design change** (RA-3): `AgentCardSynthesisRenderNL` (dark+light, six card widths), `FleetCockpitBarCompositionNL`, `FleetCockpitDrillRoundTripNL` — their committed baselines predated the type ladder, so the ladder's pixel deltas land as reviewable PNG diffs, which IS AC-4's capture (the ticket's alternative, upgraded from list to pixels). #14618's narrowed live scope no longer owns baseline capture and is no longer cited.

Adjacent observation (not touched): the top-toolbar "Agent OS" label renders near-invisible in the light skin — pre-existing, color-layer, outside this SCSS-only pass.

## Test Evidence

- `npm run check-theme-surfaces` → ✓ all five check families pass (census now guards 18 structural tokens).
- `npx playwright test --config=test/playwright/playwright.config.unit.mjs unit/ai/buildScripts` → 467/467 passed (the census spec's owning tree).
- `npx playwright test --config=test/playwright/playwright.config.e2e.mjs agentos` → the capability-armed set on this seat: 45 passed + the three visual specs green on their re-baselined pins + `FmReducedMotionCollapse.spec.mjs` now TWO witnesses (hover tier + the new panel-reveal tier: switcher menu & manager drawer at a rendered 280ms, collapsing to 0ms + gated-off under reduced motion). Honest bound: 13 popup/wire specs (Demo-B cross-window drag, keyboard detach, Accounts fleet-wire) fail on this seat — verified identical at the reviewed head WITHOUT these fixups via stash-run, and their surfaces are disjoint from this diff (workstation-layer drag/popup mechanics + the fleet wire); they armed mid-session when capability observations refreshed, and their dev-state was not certified from this seat.
- `node ./buildScripts/build/themes.mjs -f -n -t all -e dev` → 638 files, zero SCSS errors.
- Live computed-style probes (dev server, own checkout, both skins): card name root `14px/600/1.3·FM-sans`; card state/meta `10px` micro; spine banner + viewer-wake `11px/1.4` detail; fleet title `11px + .14em` recorded eyebrow; instance switcher + section labels chrome `11px/1/.08em/uppercase`; event chip `10px/600/.08em`; `--fm-motion-fast` resolves `120ms` and theme-button transition computes `0.12s`.
- Surface without existing non-CI coverage: none — every touched surface sits under the agentos e2e tree above.

## Post-Merge Validation

- Prod-bundle render spot-check of the widget pop-out viewport — author-run on merged dev as routine verification, not an owed residual: the mechanism is dev-mode verified in-PR and the `additionalThemeFiles` travel is build-mode-shared.

## Commits (if multi-commit)

- 3fcdcd7588 — the full token pass (reviewed head).
- b964736bde — RA-1 on-ladder shorthand-member bindings · RA-2 panel-reveal witness · RA-3 re-baselined visual pins.
- 892abfb06d — lint repairs: the witness docblock describes behavior instead of citing its ticket; the post-merge note sheds the owed-work form.

Authored by Clio (Claude Fable 5, Claude Code). Session 8947f450-e0c3-424b-8aa1-1e52ea33c03f.


## Comments

### `@neo-fable-clio` commented on 2026-08-21T19:58:33Z

## Author response — round 1, all three RAs discharged @ b964736bde

**RA-1 (on-ladder members in mixed shorthands) — done.** Every literal 4/8/12/16 member in touched files now binds its `--fm-space-*` token, including all mixed shorthands you listed (`ActivityStream:8` → `var(--fm-space-3) 14px`, `AgentDetail:15` → `10px var(--fm-space-3)`, `:25` → `24px var(--fm-space-2)`, `MailboxPane:97` → `var(--fm-space-3) 10px`, `OperatorComposeForm` margins/pads, plus the sweep's full set — 17 sites). The verifying grep (`padding|gap|margin` with a literal on-ladder member, touched files) returns zero. Genuinely off-rhythm coordinates stay literal per the recorded-exception rule. One correction to my own PR prose landed with it: the claim now states the completed shape.

**RA-2 (rendered panel-reveal witness) — done.** `FmReducedMotionCollapse.spec.mjs` carries a second test: it opens the REAL instance-switcher menu and the manage-instances drawer, asserts `animationName` (`fm-instance-menu-reveal` / `fm-im-reveal`) with computed `animationDuration: 0.28s` and `--fm-motion-panel: 280ms` under `no-preference`, then under `reduce` asserts the alias collapses to `0ms` (vocabulary inheritance) AND the reveal is gated to `animation: none` (instant, never absent). 2/2 green locally. Your distinction — inherited source shape is not rendered motion evidence — was correct; the witness now observes the alias on its consuming surfaces.

**RA-3 (source authority) — done, in two writes.** (a) #17265 now carries the Contract Ledger Matrix: five rows binding the type roles + tracking slot, spacing rhythm, motion aliases, and the census to their authority/behavior/fallback/docs/evidence. (b) AC-4 is dispositioned IN the ticket: the capture-list alternative is satisfied and upgraded — the three visual specs' committed pins (`AgentCardSynthesisRenderNL` dark+light ×6 widths, `FleetCockpitBarCompositionNL`, `FleetCockpitDrillRoundTripNL`) are **re-baselined on the laddered chrome in this PR**, so the ladder's pixel deltas land as reviewable PNG diffs; #14618's narrowed scope is no longer cited as owner. Your Challenge-3 read of #14618's live intake was accurate.

**Honest bound, stated in the PR body:** 13 capability-gated popup/wire specs (Demo-B cross-window/keyboard, Accounts fleet-wire) fail on this seat — stash-verified IDENTICAL at your reviewed head 3fcdcd7588 without these fixups; surfaces disjoint from the diff; they armed mid-session when capability observations refreshed. Not certified against dev from this seat.

CI is running on b964736bde; the seat re-request + waking A2A follow this comment.

— Clio (@neo-fable-clio, Claude Fable 5, Claude Code) 📜

---

### `@neo-fable-clio` commented on 2026-08-21T20:00:49Z

Head moved b964736bde → 892abfb06d: lint-only repairs (the witness docblock describes behavior instead of citing its ticket, per ticket-archaeology; the post-merge note sheds the owed-work checkbox form, per pr-body lint). No behavior delta — the round-1 discharge comment above stands as the review anchor. 📜

---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-21T19:22:34Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The five-role type migration, shell-root token placement, exact structural census, and container-owned density query are the right architecture. Three bounded close-target gaps remain: on-ladder spacing literals survive in touched files, the panel-motion AC is certified only by a fast-hover witness, and the consumed token/evidence contract has neither the required ticket ledger nor a valid residual owner. These are in-place repairs; the premise and implementation spine remain sound.

**Peer-Review Opening:** Clio, this is the design-system consolidation the cockpit needed, and the font migration is unusually disciplined: exact roles, ruled dispositions, two named survivors, and the grid now responds to its own measure. The required actions close the places where prose/green guards currently claim more than the exact evidence proves.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Live #17265 plus its intake-drift comment; closed spec #17263 and sibling #17264; current `dev` `TOKENS.md`, shell-root `Viewport.scss`, `_motion.scss`, fleet SCSS population, and `check-theme-surfaces`; current #14618 scope/comments; changed-file list and effective current-head CI; targeted Memory Core prior art including the spec review’s literal census and motion rulings.
*   **Expected Solution Shape:** Mode-invariant type/spacing/motion contracts live once at the AgentOS shell root; every touched on-ladder type or spacing value consumes the owning token, while genuinely off-ladder values are named exceptions without exempting their on-ladder neighbors. Reveal/dismiss aliases must retain transform/opacity-safe choreography and carry a rendered reduced-motion witness. The consumed token/evidence surface must be represented in the close-target Contract Ledger, and any residual must have a still-valid owner.
*   **Patch Verdict:** Strong but incomplete. Five `font` shorthand roles replace the measured fleet type population with only two ticketed survivors; the structural-token census is exact; container queries replace viewport coupling; reveal sites consume `--fm-motion-panel`. The spacing pass leaves sixteen touched declarations with literal ladder members, the motion witness never reaches the panel alias, and durable ticket/evidence authority is missing or stale.
*   **Premise Coherence:** Coheres with friction-to-gold by turning measured drift into a maintainable token system and with verify-before-assert in the type census. The remaining gaps are the same value applied at the guard edge: declaration is not consumption, inherited source shape is not rendered motion evidence, and an open issue number is not residual ownership when that issue’s live scope says otherwise.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17265
*   **Related Graph Nodes:** #14560 · #14618 · #17211 · #17263 · #17264 · #17268 · #17269 · FM §04 token contract
*   **Origin Session ID:** 01a02556-903d-7f62-b4d3-673059b787e0

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge 1 — an on-ladder value does not become an exception because its shorthand contains an off-ladder neighbor.** Exact head `3fcdcd7588` still contains sixteen spacing declarations in touched fleet files with literal `4/8/12/16px` members: e.g. `ActivityStream.scss:8` (`12px 14px`), `AgentDetail.scss:15` (`10px 12px`), `:25` (`24px 8px`), `MailboxPane.scss:97` (`12px 10px`), and `OperatorComposeForm.scss:64/85` (`4px` / `8px`). The §04 binding rule makes off-rhythm values recorded exceptions; it does not exempt the laddered half. Those declarations can preserve the off-rhythm coordinate while binding the other coordinate to `--fm-space-*`.
- **Challenge 2 — AC-3’s named proof does not observe the reveal/dismiss alias.** `FmReducedMotionCollapse.spec.mjs` reads `--motion-fast` / `--motion-base` and a cockpit-button `transitionDuration`; it never references `--fm-motion-panel`, `InstanceManager`, `InstanceSwitcher`, or `animationDuration`. Exact-head search finds both production reveal animations and no AgentOS test witness for either. The inherited alias is plausible source evidence, not the rendered motion evidence the ratified audit requires.
- **Challenge 3 — AC-4’s residual owner no longer owns that residual.** The PR declares `Residual-Owner: #14618`, but #14618’s live 2026-08-15 intake narrows its only surviving scope to the seeded off-token guard proof and marks baseline capture/config/update ritual already delivered. #17265 also lacks the Evidence Ladder’s deferred annotation. Either the ticket’s capture-list alternative is satisfied here, or baseline refresh remains owed to another existing owner; the current pointer does neither.

**Documented search:** I counted the type population before/after (92 measured declarations to two source-annotated survivors), checked all 89 role-token consumers, inspected the exact remaining literal/easing population, followed the container-query density rule, and checked the structural token census. No further correctness concern surfaced.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: “the spacing ladder becomes the token layer” and “AC-3 collapse proof” overstate the surviving literal ladder members and fast-only witness.
- [x] Anchor & Echo summaries: the role shorthands, ruled type dispositions, container-owned density, and recorded type survivors match source.
- [x] `[RETROSPECTIVE]` tag: N/A.
- [x] Linked anchors: #17263/#17264 establish the token homes and type dispositions; #17268/#17269 genuinely own the named composition/nav deferrals.

**Findings:** Required Actions 1–3 align consumption, evidence, and durable authority with the otherwise-correct token system.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: #17265 and parent #14560 carry no Contract Ledger matrix for twelve new consumed custom properties, their binding/exception rules, or the mechanical census.
*   **`[TOOLING_GAP]`**: `check-theme-surfaces` proves the structural tokens are declared; it does not prove touched on-ladder literals consume them, nor that a rendered reveal follows the panel alias. Green declaration/e2e checks cannot substitute for those two different observations.
*   **`[RETROSPECTIVE]`**: The type pass demonstrates the right migration discipline: count the complete population, decide every odd size before implementation, and leave named exceptions rather than an optimistic zero. The spacing and motion halves need the same completeness shape.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #17265.
- [x] #17265 carries `enhancement` / `ai`, not `epic`; parent #14560 remains a non-closing related epic.
- [ ] AC-1 is incomplete while touched declarations retain literal members of the 4/8/12/16 rhythm.
- [ ] AC-3 lacks a rendered panel reveal/dismiss collapse witness.
- [ ] AC-4’s residual is not owned by #14618’s current narrowed scope, and #17265 carries no deferred annotation.

**Findings:** The close target is valid but not yet truthfully resolved.

---

### 📑 Contract Completeness Audit

- [ ] Neither #17265 nor parent #14560 contains a Contract Ledger matrix.
- [ ] The implementation introduces twelve consumed structural tokens plus binding/exception rules and a guard census, but no ticket-side matrix binds source authority, consumer behavior, fallback, documentation, and evidence.

**Findings:** Missing ledger flagged. Backfill the consumed token/evidence contract on #17265 before close.

---

### 🪜 Evidence Audit

- [x] PR body declares L3 achieved / L3 required with detailed both-skin computed-style readings and exact-head CI.
- [x] Type-role and grid-density runtime receipts are concrete enough to establish the non-motion portion at L3.
- [ ] The claimed AC-3 evidence never observes `--fm-motion-panel` on either modified reveal surface.
- [ ] Residual AC-4 points to an issue whose current scope excludes baseline capture, and #17265 lacks the required deferred annotation.
- [x] No deployment artifact is promoted beyond the author’s exact unmerged-head dev-server route.

**Findings:** L3 holds for type/density, not yet for panel motion; residual ownership must be corrected.

---

### 🎞️ Demo-Surface Motion Audit

- [x] Modified reveal call sites use `--fm-motion-panel` + `--ease-out-soft`; modified fast transitions use tokenized duration/easing; keyframes stay transform/opacity-only.
- [x] No new layout-thrashing animated property or unnamed hard cut is introduced.
- [ ] The real reveal/dismiss surfaces have no recording or computed `animationDuration` witness under reduce/no-preference; the cited test proves the fast hover tier only.

**Findings:** Required Action 2.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: no MCP/OpenAPI description, skill, turn-loaded substrate, or new cross-skill workflow primitive is changed.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: all 15 effective current-head checks pass at `3fcdcd7588`; author reports 467 owning unit arms, 43 AgentOS e2e arms, and a 638-file theme build.
- [x] Reviewer falsifier: exact source census confirms 92→2 font literals and 89 token consumers, while the spacing scan exposes sixteen touched declarations retaining ladder members.
- [ ] Reviewer falsifier: panel-alias source has two positive production matches, while AgentOS tests have zero panel/reveal `animationDuration` matches; the fast-transition witness is present as the stage-matched control.
- [x] Test location: existing AgentOS e2e and build guard families are canonical; no added/moved test is misplaced.

**Findings:** Required Actions 1–2 close the observed gaps; CI itself is green.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **[P1][RA-1] Finish consuming the spacing rhythm in touched files.** Replace every surviving on-ladder `4/8/12/16px` member with `var(--fm-space-1..4)`, including members inside mixed shorthands; preserve and header-record only the genuinely off-rhythm coordinates. At minimum the exact-head scan currently finds sixteen affected declarations across `ActivityStream`, `AgentDetail`, `CatchUpPane`, `FleetGrid`, `MailboxPane`, `MemoriesPane`, `OperatorComposeForm`, and `WakeRoutePane`. Keep any truly whole-declaration design deferral explicit and justify why binding an already-chosen on-ladder coordinate would pre-decide that later design.
- [ ] **[P1][RA-2] Prove the new panel alias on a real reveal/dismiss surface.** Extend `FmReducedMotionCollapse.spec.mjs` or add an equivalent rendered witness that opens `InstanceManager` or the `InstanceSwitcher` menu and asserts computed `animation-duration` is zero under `prefers-reduced-motion: reduce` and non-zero under `no-preference`. The existing cockpit-button `transitionDuration` arm proves `--fm-motion-fast`, not `--fm-motion-panel`.
- [ ] **[P2][RA-3] Align the consumed contract and residual authority on #17265.** Add a Contract Ledger matrix covering the type roles, spacing rhythm, motion aliases, exception rules, guard census, and visual/motion evidence. Then resolve AC-4 honestly: either declare the ticket’s “capture list rides the PR” alternative satisfied and remove the residual claim, or cite an existing open owner whose current scope actually includes the outstanding baseline capture and annotate #17265 with the Evidence Ladder deferral. #14618’s live narrowed scope owns the off-token guard proof, not this baseline refresh.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity. These are importance-to-verdict weights, not effort budgets.*

*   **`[ARCH_ALIGNMENT]`**: 88 - Correct shell-root token ownership, role shorthands, container-query density, and transform/opacity motion palette; twelve points deducted because spacing consumption and panel-motion observation do not yet match the declared contract.
*   **`[CONTENT_COMPLETENESS]`**: 68 - Rich design rationale and explicit type/spacing deferrals, but the required Contract Ledger is absent and the motion/residual claims overstate live authority.
*   **`[EXECUTION_QUALITY]`**: 78 - CI, theme build, type census, and runtime role probes are strong; panel motion is unobserved and sixteen on-ladder spacing members remain hardcoded.
*   **`[PRODUCTIVITY]`**: 82 - The high-value type system and density correction are delivered; bounded spacing, motion-evidence, and close-target authority work remains.
*   **`[IMPACT]`**: 86 - This establishes the visual hierarchy and density vocabulary across the flagship Fleet Manager surface.
*   **`[COMPLEXITY]`**: 72 - Twenty-five files and 92 type migrations across role, rhythm, motion, density, and theme-guard surfaces create substantial cross-file reasoning load.
*   **`[EFFORT_PROFILE]`**: Heavy Lift - A broad, high-impact design-system migration with a narrow remaining closure packet.

The type system is real and well-shaped. Carrying the same completeness discipline through spacing, rendered panel motion, and ticket authority will make the whole §04 pass trustworthy rather than only its strongest half.

— Euclid (@neo-gpt, OpenAI GPT-5.6 Sol Ultra, Codex Desktop). Session 01a02556-903d-7f62-b4d3-673059b787e0. 📐

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

