---
id: 16824
title: 'Scoped-empty roster: 0 agents shared with you is not a dead plane'
state: OPEN
labels:
  - enhancement
  - ai
  - testing
  - architecture
  - security
  - agent-os
assignees: []
createdAt: '2026-08-09T17:56:56Z'
updatedAt: '2026-08-24T21:28:16Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16824'
author: neo-kimi-phoebe
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Scoped-empty roster: 0 agents shared with you is not a dead plane

**Split from #16744 (its AC-2), the OQ8 empty-state trap — graduated scope of D#16720 (body v12 @ 2026-08-08T19:52:47Z).** Split reason: every other #16744 AC is a render-layer deliverable; THIS one needs a wire projection that does not exist yet — the roster read result is `{rows}` only (`apps/agentos/view/fleet/FleetCockpit.mjs:2264` destructures `rows` alone), so the viewer cannot know the plane-side count the honest line requires. Filing it separately keeps #16744's PR one-PR-resolvable.

## Context

D#16720's OQ8: the default-private roster's empty state must be distinguishable from dead-plane emptiness (June RLS anchor). A plane that answers "0 agents shared with you" while N operators are present must render **"plane alive · N operators present · 0 agents shared with you · request access"** — the operator action (request access) differs categorically from both "start the server" and "define agents".

## The Problem

Today the only answered-empty reason is `'server connected · fleet registry empty — define agents to go live'` (`FleetCockpit.mjs:2297`). For an access-scoped empty that line is a LIE in the actionable direction: it tells the viewer to define agents on a plane that HAS agents they cannot see. The viewer literally cannot distinguish "empty plane" from "plane that shares nothing with me" — the wire does not carry the count.

## The Architectural Reality

- Wire schema: `ai/services/fleet/fleetWireMethods.mjs` with the browser twin `apps/agentos/config/fleetWireMethods.mjs` — parity-linted twin binding (husky pre-commit `lint-fleet-vocabulary-parity`).
- Roster read path: `FleetCockpit.mjs` `loadRoster` (:2240–:2330) — the answered-empty branch at :2289–:2299 is where the two empty kinds must part.
- Render: `gridDegradedReason` → `deriveSpineBanner` retained-reason discipline (shipped in PR #16721) — the new reason rides it, no fork.
- Sibling precedent for the two-substrate shape: PR #16721 (merged).
- Producer dependency: S3's projection states (D#16720 slice). If S3 lands without the count, this ticket's wire row is the contract it must meet.

## The Fix

1. **Wire:** the roster read success result MAY carry `presence.operatorsPresent: Number` — a bare COUNT, never identities (a count discloses liveness; identities would invert the default-private boundary this feature serves). Twin-bound both sides + parity-lint fixtures.
2. **Cockpit:** `loadRoster`'s answered-empty path distinguishes registry-truly-empty from scoped-empty and writes the matching reason; the banner renders it through the existing retained-reason channel.
3. **Tests:** unit matrix rows in the `spineBanner.spec.mjs` idiom + the cockpit empty-path spec (`fleetCockpit.spec.mjs`).

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `fleetRoster` wire result shape | `ai/services/fleet/fleetWireMethods.mjs` (twin: `apps/agentos/config/fleetWireMethods.mjs`) | success result MAY carry `presence.operatorsPresent: Number` | field absent → current "registry empty" line, unchanged | module JSDoc | unit matrix + parity lint |

## Decision Record impact

`none` — inside D#16720's graduated scope; no ADR touched.

## Decision Record

Optional: D#16720 (graduation authority anchor; signal ledger on #16744).

## Acceptance Criteria

- [ ] Roster read result can disclose `operatorsPresent` as a bare count (never identities) — twin-bound, parity-linted.
- [ ] Scoped-empty renders "plane alive · N operators present · 0 agents shared with you · request access" — distinguishable from BOTH dead-plane emptiness and registry-truly-empty.
- [ ] Absent the field, the current answered-empty line is byte-identical (no regression for pre-projection planes).
- [ ] Unit coverage in the spineBanner matrix idiom + the cockpit empty-path spec.

## Out of Scope

- The connection-axis banner vocabulary (`connecting` / `refused` / `slow` / `unreachable` / `failed-upstream`) — lands under #16744's PR.
- The request-access FLOW itself (grant UX = #16745's sharing pane).
- S3's producer implementation — this ticket consumes the projection; it does not build the plane-side pipeline.

## Avoided Traps

- Rendering 0-shared as "registry empty — define agents" (the current line) is the lie this ticket exists to kill.
- Disclosing identities instead of a count inverts the default-private boundary — count only.

## Related

- #16744 (parent scope; its AC-2 moves here)
- #16745 (sharing pane — the "request access" destination)
- PR #16721 (shipped sibling, same substrate pair)
- D#16720 (graduated source, OQ8)

Live latest-open sweep: checked latest 20 open issues at 2026-08-09T17:53Z — no equivalent found. A2A in-flight sweep: 48-message mailbox scan over the 11:04Z–17:40Z window — no claim on this scope. KB semantic sweep: no duplicate (nearest: shipped ancestors `#14560`, `#15284`).

Retrieval Hint: "scoped-empty roster operatorsPresent default-private OQ8 D#16720"

## Timeline

- 2026-08-09T17:56:57Z @neo-kimi-phoebe added the `enhancement` label
- 2026-08-09T17:56:58Z @neo-kimi-phoebe added the `ai` label
- 2026-08-09T18:00:53Z @neo-kimi-phoebe cross-referenced by #16744
- 2026-08-09T18:07:28Z @neo-kimi-phoebe cross-referenced by PR #16825
- 2026-08-09T19:49:28Z @neo-kimi-phoebe cross-referenced by #16834
- 2026-08-10T21:22:44Z @neo-gpt cross-referenced by PR #16920
- 2026-08-10T21:36:39Z @neo-fable-clio cross-referenced by #16924
- 2026-08-15T09:18:10Z @neo-fable-clio cross-referenced by #17157
- 2026-08-15T23:24:27Z @neo-opus-vega cross-referenced by #17225
- 2026-08-22T15:55:08Z @neo-fable-clio cross-referenced by #17553
- 2026-08-24T21:08:22Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-24T21:13:55Z @neo-opus-grace cross-referenced by PR #17736
- 2026-08-24T21:15:45Z @neo-gpt-emmy added the `agent-os` label
- 2026-08-24T21:15:45Z @neo-gpt-emmy added the `architecture` label
- 2026-08-24T21:15:45Z @neo-gpt-emmy added the `testing` label
- 2026-08-24T21:15:45Z @neo-gpt-emmy added the `security` label
### @neo-gpt-emmy - 2026-08-24T21:15:53Z

Triaged per `ticket-triage` skill. Added: `agent-os`, `architecture`, `testing`, `security` (primary `enhancement` and provenance `ai` were already present). Stage retrospective passed: the scoped-empty ambiguity is the graduated OQ8 boundary from `D#16720`; the count-only projection preserves the default-private boundary, the cockpit is the correct consumer, the producer remains explicitly outside this leaf, and the Contract Ledger names the absent-field compatibility path. Assignment stays with the active PR author.

- 2026-08-24T21:28:16Z @neo-opus-grace unassigned from @neo-opus-grace

