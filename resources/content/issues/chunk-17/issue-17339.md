---
id: 17339
title: 'An approval''s anchor is invisible, and the review validator has no context to see it'
state: CLOSED
labels:
  - bug
  - ai
  - model-experience
  - agent-os
assignees:
  - neo-opus-grace
createdAt: '2026-08-18T09:36:53Z'
updatedAt: '2026-08-18T18:06:43Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17339'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-18T18:06:43Z'
---
# An approval's anchor is invisible, and the review validator has no context to see it

## Context

Two independent observations from two seats on the same surface, 2026-08-17/18, paired here because neither is worth much alone.

**Face A — false-reject with no available shape (@neo-opus-grace, PR #17324).** My `APPROVED` sat at `8a24e213c1`; a rebase moved the head to `23588b661a`. GitHub reported `reviewDecision: APPROVED` the whole time, so the board showed an approval for a commit nobody had read. Trying to post a re-anchored approval, `validate_pr_review_body` refused twice — once for matching no template, once with *"This body dispositions nothing — the table needs one row per Round-1 required action"*, because Round 1 had closed with zero required actions. Both refusals are correct in isolation and together leave no shape.

**Face B — false-accept (@neo-opus-ada, PR #17323, 2026-08-17).** The same tool returned `valid: true` on a body the **submit** gate then rejected. The dry-run has no PR context, so it cannot run the prior-round comparison it is trusted for.

Observation vs inference: the review states, SHAs, and both tool verdicts are observed. The mechanism below is code-read.

## The Problem

`validate_pr_review_body` takes exactly one input — `body` — and is advertised as *"Dry-run validate a PR review body against the canonical pr-review template before posting."* Reviewers use it as the pre-submit gate. But template selection is not a property of the body alone: whether a body should be Round 1, Round 2, or a follow-up depends on **how many prior rounds exist, what their required actions were, and which head they anchored to**. None of that is reachable from a string.

So the tool answers a strictly easier question than the one it is trusted for, and the gap shows up in both directions — it passes bodies the submit gate refuses, and it refuses bodies with no valid alternative to offer.

The re-anchor case is the sharpest instance because it exposes the *upstream* gap: **an approval's anchor is invisible where merge decisions get made.** GitHub surfaces `reviewDecision: APPROVED` and does not surface which commit earned it. `#17273` was the first sighting; #17324 is the second. Both were caught by a human or peer noticing, not by an instrument.

**The fix is not to make re-anchoring cheap.** A cheap re-anchor is rubber-stamping with a nicer name, and `#17273` is the argument for keeping a real re-review expensive. The gap is display truth: the badge reports a verdict without reporting what it was a verdict *about*.

## The Architectural Reality

- `ai/mcp/server/**` — `validate_pr_review_body`'s handler: body-only input, no `pr_number`, therefore no prior-round or head context.
- `manage_pr_review` — has PR context and validates on submit. This is why the two disagree: they are not the same check, but the dry-run's name and description imply they are.
- `validateMergeReady` (per `pull-request-workflow.md §6.1`) — already encodes that a formal `APPROVED` is necessary-but-not-sufficient and that non-empty `reviewRequests` blocks. It does **not** compare the approving review's commit against the current head.
- `.agents/skills/pr-review/assets/pr-review-round-2-template.md` — Round 2 is defined as a disposition over Round-1 required actions. A Round 1 that closed with none has no Round-2 shape by construction; that is a correct definition meeting an unhandled case.

## The Fix

1. **Surface the anchor where merge readiness is decided.** `validateMergeReady` reports the approving review's commit alongside the head, and says plainly when they differ — e.g. `APPROVED at 8a24e213c1; head is 23588b661a (anchor stale)`. Reporting, not blocking: a content-free rebase is common and must not manufacture a red.
2. **Make the validator honest about its own reach.** Either give it optional PR context (`pr_number`) so it can run the comparison it is trusted for, or narrow its advertised contract to shape-only so no reviewer treats a `valid: true` as a submit prediction. Both are acceptable; silently continuing to imply the stronger check is not.
3. **Name the no-shape case.** When a prior round closed with zero required actions, the validator's refusal should say what IS available rather than only what is missing.

## Acceptance Criteria

*(Narrowed 2026-08-18 by the author. This ticket was mis-sized at filing: it bundled two surfaces — the approval **anchor** on the merge-readiness observation, and the **review-body validator** — and `ticket-create-workflow.md` §4 requires a standalone to be one-PR-resolvable. The validator half moved to #17354 with its evidence intact. This is a scope replacement, not a reduction: no AC was dropped.)*

- [ ] Merge-readiness output names the approving review's commit and the current head, and flags a mismatch in words a reader cannot miss.
- [ ] A stale anchor whose reviewed paths are content-identical is reported, never auto-failed — the control that stops this becoming a red-on-every-rebase.

**Moved ACs — dispositions (2026-08-18):**

The four validator ACs now live on **#17354** (`validate_pr_review_body holds only the body, so its dry-run can accept what the submit gate rejects`), which also carries the two live specimens collected since this ticket was filed — a disposition cell refused for trailing prose, and a verbatim quote refused for stripped markdown — plus the refusal-message asymmetry that makes both expensive.

- **PR context / shape-only declaration (was AC-3): moved to #17354.**
- **Dry-run/submit-gate divergence (was AC-4): moved to #17354**, carrying PR #17323 as the named specimen.
- **Refusals name the available path (was AC-5): moved to #17354**, sharpened there to the prescriptive shape `agent-preflight` already uses.
- **Both-faces regression coverage (was AC-6): moved to #17354**, with the two live specimens as fixtures.

Why a move rather than a second PR against this ticket: the two halves touch different services and share only a root cause (*context that exists is not available to the surface that needs it*). A shared root is a good reason to cross-link, not to bundle delivery.

## Out of Scope

Changing GitHub's own review badge (not ours to fix; we surface the anchor in our tooling instead) · making re-anchoring a cheap ceremony · the `Residual-Owner` shape-vs-state gap, which is the sibling instance in #17314 and stays there.

## Avoided Traps

- **Cheap re-anchor.** Considered and rejected on @neo-opus-ada's argument: an approval that can be refreshed without re-reading is rubber-stamping renamed, and `#17273` exists because that is expensive to undo.
- **"The delta was empty" as the reason no re-review was needed.** This was my first rationale on #17324 and it was wrong evidence for a right answer. My probe enumerated paths (`src ai test buildScripts learn .agents apps/agentos`) and omitted `docs`, so it could not have returned anything else; the delta's one real file was `docs/output/class-hierarchy.json`. The durable reason is that the file is a generated artifact under a byte-exact regeneration gate — `.github/workflows/class-hierarchy-freshness.yml:82,86` runs `npm run generate-docs-json` and fails on any diff — so there is nothing a reviewer can assert that CI has not already proven. **An enumerated path list answers "did anything change where I thought to look", not "did anything change".** Diff everything, then subtract the trees you can name as pipeline-owned.

## Related

`#17273` (first anchor-staleness sighting) · #17314 (sibling: shape-vs-state in the `Residual-Owner` gate) · #17324, PR #17323 (the two specimens) · `#17141` (Round-2 terminality, adjacent)

Origin Session ID: ad99f59b-9d2c-4f82-b6ce-8c8357ef1879

## Handoff Retrieval Hints

- `query_raw_memories`: "approval anchor stale rebase reviewDecision head commit"
- `query_raw_memories`: "validate_pr_review_body false accept submit gate no PR context"
- Specimens: PR #17324 (`8a24e213c1` → `23588b661a`), PR #17323 body at its Round-2 attempt

Gate records — live latest-open sweep: latest 20 open issues checked 2026-08-18T09:35:33Z, no equivalent found. A2A in-flight sweep: latest messages, all read-states; the only overlapping traffic is @neo-opus-ada's own hand-off of this lane to me. Agent OS Structure Map gate: deferred to implementation — the change lands in an existing MCP handler and an existing merge-readiness helper, no new file.



## Timeline

- 2026-08-18T09:36:54Z @neo-opus-grace added the `bug` label
- 2026-08-18T09:36:55Z @neo-opus-grace added the `ai` label
- 2026-08-18T09:36:55Z @neo-opus-grace added the `model-experience` label
- 2026-08-18T09:36:55Z @neo-opus-grace added the `agent-os` label
- 2026-08-18T09:37:29Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-18T10:15:39Z @neo-opus-grace cross-referenced by PR #17340
- 2026-08-18T10:55:32Z @neo-opus-grace referenced in commit `1cc2d8e` - "docs(agentos): the containment comment names both viewports it actually reaches (#17313)

Comment-only. No selector, rule or behaviour changes; the compiled CSS is
identical.

Maintainer-polish clause from Ada's approval (pullrequestreview-4960166193), and
it is a correction to my own prose rather than a note added to it. The comment
said the rule was scoped to "this app's viewport", singular. It is not:
`AgentOSWidget.view.Viewport` carries the same `agent-os-viewport` cls AND
declares `additionalThemeFiles: ['AgentOS.view.Viewport']`, so the popup host
opts into this exact sheet BY NAME. Verified at source rather than taken from the
review. Every popped-out panel gets this rule.

She tried to make that harmful and could not, and neither could I: an OS window
bounds the pane for free -- the same asymmetry MailboxPane.scss already
documents -- and that childapp hosts no dock and no cockpit. So this was never a
defect, only a sentence that had stopped being true. Fixing it matters because
the next reader budgets their blast radius from that sentence.

It also answers, better than I did, why app-scope beat a framework default. I had
argued it loosely, worrying that the next shell would rediscover the bug. It will
not: `additionalThemeFiles` IS the sharing mechanism, already in use by the one
other consumer. A future shell declares one line -- greppable and opt-in per
consumer, which is a stronger property than a default nobody can see reaching
them.

Also records the width-axis caution she raised: four of these additions are
`min-width: 0`, nothing in the repo exercises this app below 900px (the only
`setViewportSize` in the agentos e2e directory is 900x1000), and per-level `min-*`
work across this same spine has collapsed both dock zones before -- #15657, still
open. Her direction there was the inverse of this one, so it is a caution rather
than a precedent, and it is carried with a `ticket-ref-ok` marker because naming
the open regression IS the value of the note.

THE APPROVAL ANCHOR MOVES WITH THIS COMMIT. Ada approved 362b75694a; this pushes
past it, so the badge will read APPROVED for a commit nobody reviewed. That is
precisely #17339's premise, and the honest response is the one that ticket argues
for: report it rather than let the badge speak. The delta is one comment block in
one file -- stated here, and sent to her directly, so the staleness is visible
instead of inferred.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T11:00:30Z @neo-opus-ada cross-referenced by PR #17341
- 2026-08-18T11:27:21Z @tobiu referenced in commit `e5b8364` - "fix(agentos): the shell spine can shrink, so a tall pane scrolls instead of lifting it (#17313) (#17341)

* fix(agentos): the detail tab body contains its pane on both axes (#17313)

Activating the agent-detail Mailbox tab pushed the fleet activity stream out
of the viewport: measured live, the stream's top edge moved from y=687.3 to
y=1695.8 in a 1084px viewport while it reported mounted:true, hidden:false.
The state layer was telling the truth; the layout was hiding it.

The containment intent was already largely present -- .fm-mailbox-pane and
.fm-detail-tabs both carry min-height:0, and .fm-mailbox-rows already scrolls.
The seat that was missing is the tab BODY container, a different box from
.fm-detail-tabs: a flex item's automatic minimum size is its content size on
the main axis, so the body grew to the pane's full 2454px and carried the
detail rail with it.

Both axes are pinned, because the pane overran horizontally too -- 1081.64px
inside a 256px slot, which the ticket did not name. A height-only fix restores
the stream and leaves the sideways overrun standing. The pop-out host never
showed either symptom, because an OS window viewport bounds the pane for free:
same defect, two geometries, only one of them forgiving.

The tab header carried 309.4px of buttons in a 256px bar, rendering
"Configuration" as "Configurat". Padding is tightened so all three fit at the
shipped width; the scroll is the guarantee rather than the mechanism, since
the rail is resizable and a padding tune alone would only move the clipping
width rather than retire it.

Config-level throughout -- nothing here implicates the engine's layout, which
matches the DockLayout-vs-config verdict on the ticket.

Verification is NOT yet complete: the live plane cannot reach the loaded-rows
state (the cockpit viewer holds no CAN_READ_INBOX_OF grant, so the pane renders
its denied line for every agent), which makes a post-fix measurement there
vacuous. The AC-4 regression guard is the instrument that can drive real rows,
and it follows in this branch.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* test(agentos): the AC-4 guard proves the detail-rail fix is only half done (#17313)

The regression guard the previous commit promised. It drives a FULL 50-row
mailbox page through the possession seam and reads client rects, because during
the incident the state layer was telling the truth while the pixels were not.

It is RED, and that is the finding. Measured at 1280x720 on this branch:

  pane   width  256  inside detail width 280    <- horizontal axis: FIXED
  detail height 3173, top -151
  rows   scrollHeight 2844 == clientHeight 2844 <- never scrolls, grew to content
  stream top    1811.1                          <- still below a 720px fold

The pane fitting 256px inside a 280px slot is what proves the SCSS is compiled
and served, so this is not a stale-build artifact: the min-width half of the
previous commit demonstrably works. The min-height on .neo-tab-body-container
does not, because .fm-agent-detail itself is 3173px tall -- the rail is not
bounded by the shell, so zeroing a descendant's automatic minimum size has
nothing to shrink against. The containment seat is higher than the tab body,
in the dock projection that places the pane, and finding it is a real
investigation rather than another one-line tune.

Committed red on purpose. No PR is opened: the branch does not resolve its
ticket, and a guard that encodes the true state is worth more than a green one
that agrees with a fix nobody verified. Non-vacuity is asserted in the spec
itself -- the sibling test's three rows overflow nothing, which is why the same
assertions would have passed on the unfixed tree and proved only that the page
loads.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* fix(agentos): the shell spine can shrink, so a tall pane scrolls instead of lifting it (#17313)

The guard the previous commit landed RED is now green, and the seat was three
levels above where I had pinned it.

MEASURED, not guessed. With a full 50-row mailbox page open at 1280x720, the
ancestor chain from the detail rail to the viewport read:

  neo-viewport                  720   visible   <- bounded
  neo-tab-container.neo-left   3286   visible   <- FIRST box past the viewport
  neo-tab-body-container       3286   hidden
  fm-fleet-cockpit             3284   visible
  neo-dashboard-dock-tabs      3224   visible
  fm-agent-detail              3173   hidden

A flex item's automatic minimum size is its CONTENT size, and `min-height: auto`
resolves to zero only when overflow is not `visible`. Every box marked visible
above therefore refuses to shrink below its content, independently of the others.
That is why pinning the tab body could never have worked: it and the rail already
carried containment, and three ancestors above them were doing the pushing. The
fleet activity stream sat at y=1811 in a 720px viewport -- mounted, unhidden, and
entirely below the fold.

The repair is `min-height: 0` on that spine, scoped to
`.agent-os-viewport.neo-viewport` rather than to the framework classes it names.
The same boxes serve layouts elsewhere that legitimately size to content, and a
global change would bill every one of them for this app's shell. The scroll seats
already existed one level down in the panes; this only stops the spine growing
past the screen so they engage.

Mutation-verified: removing the rule reproduces the ORIGINAL measurement exactly
-- stream top 1811.1, detail 3173, rows scrollHeight 2844 == clientHeight 2844.
The guard is non-vacuous and the rule is load-bearing.

Also fixed, and it was my bug rather than the app's: the guard's AC-3 tab-count
locator used `.neo-tab-button`, which matches zero here -- the header strip is
`.neo-tab-header-button`, as the sibling test in the same file already knew. It
had been masked behind the geometry failure, and surfaced the moment the
containment landed.

agentos unit suite 698 passed. The agentos e2e directory is running for blast
radius before this asks for a reviewer; the spine is shell-wide and a regression
there would matter more than this fix does.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* docs(agentos): the containment comment names both viewports it actually reaches (#17313)

Comment-only. No selector, rule or behaviour changes; the compiled CSS is
identical.

Maintainer-polish clause from Ada's approval (pullrequestreview-4960166193), and
it is a correction to my own prose rather than a note added to it. The comment
said the rule was scoped to "this app's viewport", singular. It is not:
`AgentOSWidget.view.Viewport` carries the same `agent-os-viewport` cls AND
declares `additionalThemeFiles: ['AgentOS.view.Viewport']`, so the popup host
opts into this exact sheet BY NAME. Verified at source rather than taken from the
review. Every popped-out panel gets this rule.

She tried to make that harmful and could not, and neither could I: an OS window
bounds the pane for free -- the same asymmetry MailboxPane.scss already
documents -- and that childapp hosts no dock and no cockpit. So this was never a
defect, only a sentence that had stopped being true. Fixing it matters because
the next reader budgets their blast radius from that sentence.

It also answers, better than I did, why app-scope beat a framework default. I had
argued it loosely, worrying that the next shell would rediscover the bug. It will
not: `additionalThemeFiles` IS the sharing mechanism, already in use by the one
other consumer. A future shell declares one line -- greppable and opt-in per
consumer, which is a stronger property than a default nobody can see reaching
them.

Also records the width-axis caution she raised: four of these additions are
`min-width: 0`, nothing in the repo exercises this app below 900px (the only
`setViewportSize` in the agentos e2e directory is 900x1000), and per-level `min-*`
work across this same spine has collapsed both dock zones before -- #15657, still
open. Her direction there was the inverse of this one, so it is a caution rather
than a precedent, and it is carried with a `ticket-ref-ok` marker because naming
the open regression IS the value of the note.

THE APPROVAL ANCHOR MOVES WITH THIS COMMIT. Ada approved 362b75694a; this pushes
past it, so the badge will read APPROVED for a commit nobody reviewed. That is
precisely #17339's premise, and the honest response is the one that ticket argues
for: report it rather than let the badge speak. The delta is one comment block in
one file -- stated here, and sent to her directly, so the staleness is visible
instead of inferred.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T12:24:22Z @neo-opus-ada cross-referenced by #17350
- 2026-08-18T13:17:36Z @neo-opus-grace referenced in commit `282f240` - "feat(ai): the merge-readiness observation reports which commit earned the approval (#17339)

Second slice: the call-site wiring the first one deliberately left out. The channel
existed on the predicate and nothing supplied it, so the anchor reached no surface --
a parameter with no producer reports nothing and fails nothing.

GET_MERGE_READINESS did not fetch review nodes at all. It now fetches them with
'last' rather than 'first', which is the whole point: only the most recent approval
can say which commit earned the badge, and the oldest 100 would truncate away exactly
the reviews the question is about. hasPreviousPage is reported but does not gate -- an
approval found inside the most-recent window IS the latest one however many older
reviews exist, and an empty window yields silence rather than a claim.

Only APPROVED reviews are carried into the normalized snapshot. That collection also
enters the double-read drift comparison, so keeping COMMENTED/PENDING reviews would
fail observations with SOURCE_CHANGED_DURING_READ every time a peer commented
mid-read, for a change that moves no readiness. A CHANGES_REQUESTED landing mid-read
still trips the comparison through reviewDecision, where that state actually lives.

The derivation sorts rather than trusting connection order: the caller reads the last
element as 'latest', and an ordering assumption that held today would fail silently as
a WRONG anchor rather than a missing one.

Red-proofed by unwiring the call site: the stale-anchor case fails while the rest of
the suite stays green. 891 passed across the github-workflow and lifecycle trees."
- 2026-08-18T13:22:34Z @neo-opus-grace cross-referenced by #17354
- 2026-08-18T13:23:40Z @neo-opus-grace referenced in commit `4d1517f` - "feat(ai): merge-readiness can report which commit earned the approval (#17339)

First slice of the anchor-visibility lane: the predicate gains the channel. The
call-site wiring is the next block and is deliberately not squeezed in here --
the merge-readiness GraphQL query does not fetch review nodes at all, so
supplying `approvedAtOid` needs a query extension plus a latest-approval
derivation, which is its own change with its own tests.

WHY THIS IS A CHANNEL AND NOT A GATE. `reviewDecision: APPROVED` says a verdict
exists; it never says which commit earned it. A rebase or a fixup moves the head
and leaves the badge alone, so the board reads APPROVED for a commit nobody has
read. Today the only things that notice are a human remembering or a peer
checking by hand -- both happened on this repo in one day, on #17273 and #17324.

Advisory rather than blocker, and that is the load-bearing decision. Most stale
anchors are content-free: a rebase over data-sync commits moves every sha and
changes nothing anyone reviewed. Blocking those would red every rebased PR in the
repo and teach reviewers to ignore the signal, which costs more than the gap it
closes. Whether a delta MATTERS is a judgement over the diff, and this function
holds no diff -- so it reports the pair and lets the reader judge.

The un-fetched case is silence, which INVERTS this module's rule for every other
field, deliberately. The others are part of the merge-ready predicate, so an
unasked question cannot certify and must fail closed. The anchor certifies
nothing. A caller that never supplies it is not making a weaker claim, and
blocking it would break every existing call site while adding no safety.

The advisory text carries the discipline the lane was born from: diff EVERYTHING
and subtract the pipeline-owned trees, because an enumerated path list answers
only "did anything change where I thought to look". That is the error that
produced a right answer on wrong evidence earlier today.

Mutation-verified: disabling the comparison fails exactly the two specs that
assert an advisory is produced, and leaves the two fences green -- fresh-anchor
and unfetched-anchor both assert the ABSENCE of an advisory, so they pass in both
directions by design and are not counted as red-proof. A fourth spec pins that
advisories and blockers never leak into each other, so "advisory" cannot quietly
become "downgraded blocker".

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T13:23:40Z @neo-opus-grace referenced in commit `b26ee27` - "feat(ai): the merge-readiness observation reports which commit earned the approval (#17339)

Second slice: the call-site wiring the first one deliberately left out. The channel
existed on the predicate and nothing supplied it, so the anchor reached no surface --
a parameter with no producer reports nothing and fails nothing.

GET_MERGE_READINESS did not fetch review nodes at all. It now fetches them with
'last' rather than 'first', which is the whole point: only the most recent approval
can say which commit earned the badge, and the oldest 100 would truncate away exactly
the reviews the question is about. hasPreviousPage is reported but does not gate -- an
approval found inside the most-recent window IS the latest one however many older
reviews exist, and an empty window yields silence rather than a claim.

Only APPROVED reviews are carried into the normalized snapshot. That collection also
enters the double-read drift comparison, so keeping COMMENTED/PENDING reviews would
fail observations with SOURCE_CHANGED_DURING_READ every time a peer commented
mid-read, for a change that moves no readiness. A CHANGES_REQUESTED landing mid-read
still trips the comparison through reviewDecision, where that state actually lives.

The derivation sorts rather than trusting connection order: the caller reads the last
element as 'latest', and an ordering assumption that held today would fail silently as
a WRONG anchor rather than a missing one.

Red-proofed by unwiring the call site: the stale-anchor case fails while the rest of
the suite stays green. 891 passed across the github-workflow and lifecycle trees."
- 2026-08-18T13:23:42Z @neo-opus-grace cross-referenced by PR #17355
- 2026-08-18T15:44:21Z @neo-opus-grace referenced in commit `31862c7` - "feat(ai): merge-readiness can report which commit earned the approval (#17339)

First slice of the anchor-visibility lane: the predicate gains the channel. The
call-site wiring is the next block and is deliberately not squeezed in here --
the merge-readiness GraphQL query does not fetch review nodes at all, so
supplying `approvedAtOid` needs a query extension plus a latest-approval
derivation, which is its own change with its own tests.

WHY THIS IS A CHANNEL AND NOT A GATE. `reviewDecision: APPROVED` says a verdict
exists; it never says which commit earned it. A rebase or a fixup moves the head
and leaves the badge alone, so the board reads APPROVED for a commit nobody has
read. Today the only things that notice are a human remembering or a peer
checking by hand -- both happened on this repo in one day, on #17273 and #17324.

Advisory rather than blocker, and that is the load-bearing decision. Most stale
anchors are content-free: a rebase over data-sync commits moves every sha and
changes nothing anyone reviewed. Blocking those would red every rebased PR in the
repo and teach reviewers to ignore the signal, which costs more than the gap it
closes. Whether a delta MATTERS is a judgement over the diff, and this function
holds no diff -- so it reports the pair and lets the reader judge.

The un-fetched case is silence, which INVERTS this module's rule for every other
field, deliberately. The others are part of the merge-ready predicate, so an
unasked question cannot certify and must fail closed. The anchor certifies
nothing. A caller that never supplies it is not making a weaker claim, and
blocking it would break every existing call site while adding no safety.

The advisory text carries the discipline the lane was born from: diff EVERYTHING
and subtract the pipeline-owned trees, because an enumerated path list answers
only "did anything change where I thought to look". That is the error that
produced a right answer on wrong evidence earlier today.

Mutation-verified: disabling the comparison fails exactly the two specs that
assert an advisory is produced, and leaves the two fences green -- fresh-anchor
and unfetched-anchor both assert the ABSENCE of an advisory, so they pass in both
directions by design and are not counted as red-proof. A fourth spec pins that
advisories and blockers never leak into each other, so "advisory" cannot quietly
become "downgraded blocker".

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T15:44:21Z @neo-opus-grace referenced in commit `99cb5ea` - "feat(ai): the merge-readiness observation reports which commit earned the approval (#17339)

Second slice: the call-site wiring the first one deliberately left out. The channel
existed on the predicate and nothing supplied it, so the anchor reached no surface --
a parameter with no producer reports nothing and fails nothing.

GET_MERGE_READINESS did not fetch review nodes at all. It now fetches them with
'last' rather than 'first', which is the whole point: only the most recent approval
can say which commit earned the badge, and the oldest 100 would truncate away exactly
the reviews the question is about. hasPreviousPage is reported but does not gate -- an
approval found inside the most-recent window IS the latest one however many older
reviews exist, and an empty window yields silence rather than a claim.

Only APPROVED reviews are carried into the normalized snapshot. That collection also
enters the double-read drift comparison, so keeping COMMENTED/PENDING reviews would
fail observations with SOURCE_CHANGED_DURING_READ every time a peer commented
mid-read, for a change that moves no readiness. A CHANGES_REQUESTED landing mid-read
still trips the comparison through reviewDecision, where that state actually lives.

The derivation sorts rather than trusting connection order: the caller reads the last
element as 'latest', and an ordering assumption that held today would fail silently as
a WRONG anchor rather than a missing one.

Red-proofed by unwiring the call site: the stale-anchor case fails while the rest of
the suite stays green. 891 passed across the github-workflow and lifecycle trees."
- 2026-08-18T15:44:22Z @neo-opus-grace referenced in commit `4413091` - "fix(ai): the approval-anchor advisory reaches the surface that travels to the merge gate (#17339)

The advisory existed and reached no reader. Nested inside predicate it fired only on
an otherwise-green observation -- verdict still read merge-ready-observed, marker still
read merge-eligible, blockers was empty -- so it competed with nothing and was seen by
nobody. A diagnosis reaching no surface never ran.

The asymmetry is the argument. blockers is lifted to top level AND coded despite
already being discoverable three other ways: it flips verdict, rewrites statement, and
suppresses marker. An advisory has no redundancy at all, and it fires precisely when
nothing else draws the eye. So it is now lifted in the same coded shape, and the
merge-ready statement names it -- that sentence travels beside [merge-eligible] to the
human gate, and one that says strict merge-ready and stops is true and misleading in
the same breath.

The clean case asserts an EMPTY advisories array rather than an absent key: a surface
that only appears when non-empty is one every consumer must guard for, while blockers
is unconditionally present.

Also corrected: the query docblock called hasPreviousPage reported when nothing reads
or surfaces it. By this design it cannot change a decision -- an approval inside the
most-recent window IS the latest, and an empty window already yields silence -- so the
honest fix is the claim rather than a manufactured consumer.

Found by @neo-opus-ada, who proved it with a reach grep plus a strictMergeReady control
rather than inferring it. Red-proofed by removing the lift."
- 2026-08-18T17:50:54Z @neo-opus-grace referenced in commit `9a47a8e` - "feat(ai): merge-readiness can report which commit earned the approval (#17339)

First slice of the anchor-visibility lane: the predicate gains the channel. The
call-site wiring is the next block and is deliberately not squeezed in here --
the merge-readiness GraphQL query does not fetch review nodes at all, so
supplying `approvedAtOid` needs a query extension plus a latest-approval
derivation, which is its own change with its own tests.

WHY THIS IS A CHANNEL AND NOT A GATE. `reviewDecision: APPROVED` says a verdict
exists; it never says which commit earned it. A rebase or a fixup moves the head
and leaves the badge alone, so the board reads APPROVED for a commit nobody has
read. Today the only things that notice are a human remembering or a peer
checking by hand -- both happened on this repo in one day, on #17273 and #17324.

Advisory rather than blocker, and that is the load-bearing decision. Most stale
anchors are content-free: a rebase over data-sync commits moves every sha and
changes nothing anyone reviewed. Blocking those would red every rebased PR in the
repo and teach reviewers to ignore the signal, which costs more than the gap it
closes. Whether a delta MATTERS is a judgement over the diff, and this function
holds no diff -- so it reports the pair and lets the reader judge.

The un-fetched case is silence, which INVERTS this module's rule for every other
field, deliberately. The others are part of the merge-ready predicate, so an
unasked question cannot certify and must fail closed. The anchor certifies
nothing. A caller that never supplies it is not making a weaker claim, and
blocking it would break every existing call site while adding no safety.

The advisory text carries the discipline the lane was born from: diff EVERYTHING
and subtract the pipeline-owned trees, because an enumerated path list answers
only "did anything change where I thought to look". That is the error that
produced a right answer on wrong evidence earlier today.

Mutation-verified: disabling the comparison fails exactly the two specs that
assert an advisory is produced, and leaves the two fences green -- fresh-anchor
and unfetched-anchor both assert the ABSENCE of an advisory, so they pass in both
directions by design and are not counted as red-proof. A fourth spec pins that
advisories and blockers never leak into each other, so "advisory" cannot quietly
become "downgraded blocker".

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T17:50:55Z @neo-opus-grace referenced in commit `22e6cff` - "feat(ai): the merge-readiness observation reports which commit earned the approval (#17339)

Second slice: the call-site wiring the first one deliberately left out. The channel
existed on the predicate and nothing supplied it, so the anchor reached no surface --
a parameter with no producer reports nothing and fails nothing.

GET_MERGE_READINESS did not fetch review nodes at all. It now fetches them with
'last' rather than 'first', which is the whole point: only the most recent approval
can say which commit earned the badge, and the oldest 100 would truncate away exactly
the reviews the question is about. hasPreviousPage is reported but does not gate -- an
approval found inside the most-recent window IS the latest one however many older
reviews exist, and an empty window yields silence rather than a claim.

Only APPROVED reviews are carried into the normalized snapshot. That collection also
enters the double-read drift comparison, so keeping COMMENTED/PENDING reviews would
fail observations with SOURCE_CHANGED_DURING_READ every time a peer commented
mid-read, for a change that moves no readiness. A CHANGES_REQUESTED landing mid-read
still trips the comparison through reviewDecision, where that state actually lives.

The derivation sorts rather than trusting connection order: the caller reads the last
element as 'latest', and an ordering assumption that held today would fail silently as
a WRONG anchor rather than a missing one.

Red-proofed by unwiring the call site: the stale-anchor case fails while the rest of
the suite stays green. 891 passed across the github-workflow and lifecycle trees."
- 2026-08-18T17:50:55Z @neo-opus-grace referenced in commit `190f9cc` - "fix(ai): the approval-anchor advisory reaches the surface that travels to the merge gate (#17339)

The advisory existed and reached no reader. Nested inside predicate it fired only on
an otherwise-green observation -- verdict still read merge-ready-observed, marker still
read merge-eligible, blockers was empty -- so it competed with nothing and was seen by
nobody. A diagnosis reaching no surface never ran.

The asymmetry is the argument. blockers is lifted to top level AND coded despite
already being discoverable three other ways: it flips verdict, rewrites statement, and
suppresses marker. An advisory has no redundancy at all, and it fires precisely when
nothing else draws the eye. So it is now lifted in the same coded shape, and the
merge-ready statement names it -- that sentence travels beside [merge-eligible] to the
human gate, and one that says strict merge-ready and stops is true and misleading in
the same breath.

The clean case asserts an EMPTY advisories array rather than an absent key: a surface
that only appears when non-empty is one every consumer must guard for, while blockers
is unconditionally present.

Also corrected: the query docblock called hasPreviousPage reported when nothing reads
or surfaces it. By this design it cannot change a decision -- an approval inside the
most-recent window IS the latest, and an empty window already yields silence -- so the
honest fix is the claim rather than a manufactured consumer.

Found by @neo-opus-ada, who proved it with a reach grep plus a strictMergeReady control
rather than inferring it. Red-proofed by removing the lift."
- 2026-08-18T17:50:55Z @neo-opus-grace referenced in commit `a70b0c8` - "fix(ai): the merge-gate sentence agrees in number instead of hedging with a slash (#17339)

@neo-opus-ada's flagged wording, taken. At one advisory the sentence rendered
"1 advisory/advisories require a reader judgement" -- slash-plural against a plural
verb, in the one string in the observation aimed at a human.

Cosmetic anywhere else; not here. That sentence IS RA-1's deliverable and it travels
beside [merge-eligible] to the merge gate, so it is the one place wording carries
weight -- generated-looking text is text a reader discounts, and an advisory that gets
discounted has failed at the only job it has.

The spec now asserts the singular AGREEING rather than merely present, plus the
negative that the slash form is gone: toContain('advisory') alone would pass against
exactly the string being removed."
- 2026-08-18T18:06:43Z @tobiu referenced in commit `cd38d28` - "feat(ai): the merge-readiness observation reports which commit earned the approval (#17339) (#17355)

* feat(ai): merge-readiness can report which commit earned the approval (#17339)

First slice of the anchor-visibility lane: the predicate gains the channel. The
call-site wiring is the next block and is deliberately not squeezed in here --
the merge-readiness GraphQL query does not fetch review nodes at all, so
supplying `approvedAtOid` needs a query extension plus a latest-approval
derivation, which is its own change with its own tests.

WHY THIS IS A CHANNEL AND NOT A GATE. `reviewDecision: APPROVED` says a verdict
exists; it never says which commit earned it. A rebase or a fixup moves the head
and leaves the badge alone, so the board reads APPROVED for a commit nobody has
read. Today the only things that notice are a human remembering or a peer
checking by hand -- both happened on this repo in one day, on #17273 and #17324.

Advisory rather than blocker, and that is the load-bearing decision. Most stale
anchors are content-free: a rebase over data-sync commits moves every sha and
changes nothing anyone reviewed. Blocking those would red every rebased PR in the
repo and teach reviewers to ignore the signal, which costs more than the gap it
closes. Whether a delta MATTERS is a judgement over the diff, and this function
holds no diff -- so it reports the pair and lets the reader judge.

The un-fetched case is silence, which INVERTS this module's rule for every other
field, deliberately. The others are part of the merge-ready predicate, so an
unasked question cannot certify and must fail closed. The anchor certifies
nothing. A caller that never supplies it is not making a weaker claim, and
blocking it would break every existing call site while adding no safety.

The advisory text carries the discipline the lane was born from: diff EVERYTHING
and subtract the pipeline-owned trees, because an enumerated path list answers
only "did anything change where I thought to look". That is the error that
produced a right answer on wrong evidence earlier today.

Mutation-verified: disabling the comparison fails exactly the two specs that
assert an advisory is produced, and leaves the two fences green -- fresh-anchor
and unfetched-anchor both assert the ABSENCE of an advisory, so they pass in both
directions by design and are not counted as red-proof. A fourth spec pins that
advisories and blockers never leak into each other, so "advisory" cannot quietly
become "downgraded blocker".

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* feat(ai): the merge-readiness observation reports which commit earned the approval (#17339)

Second slice: the call-site wiring the first one deliberately left out. The channel
existed on the predicate and nothing supplied it, so the anchor reached no surface --
a parameter with no producer reports nothing and fails nothing.

GET_MERGE_READINESS did not fetch review nodes at all. It now fetches them with
'last' rather than 'first', which is the whole point: only the most recent approval
can say which commit earned the badge, and the oldest 100 would truncate away exactly
the reviews the question is about. hasPreviousPage is reported but does not gate -- an
approval found inside the most-recent window IS the latest one however many older
reviews exist, and an empty window yields silence rather than a claim.

Only APPROVED reviews are carried into the normalized snapshot. That collection also
enters the double-read drift comparison, so keeping COMMENTED/PENDING reviews would
fail observations with SOURCE_CHANGED_DURING_READ every time a peer commented
mid-read, for a change that moves no readiness. A CHANGES_REQUESTED landing mid-read
still trips the comparison through reviewDecision, where that state actually lives.

The derivation sorts rather than trusting connection order: the caller reads the last
element as 'latest', and an ordering assumption that held today would fail silently as
a WRONG anchor rather than a missing one.

Red-proofed by unwiring the call site: the stale-anchor case fails while the rest of
the suite stays green. 891 passed across the github-workflow and lifecycle trees.

* fix(ai): the approval-anchor advisory reaches the surface that travels to the merge gate (#17339)

The advisory existed and reached no reader. Nested inside predicate it fired only on
an otherwise-green observation -- verdict still read merge-ready-observed, marker still
read merge-eligible, blockers was empty -- so it competed with nothing and was seen by
nobody. A diagnosis reaching no surface never ran.

The asymmetry is the argument. blockers is lifted to top level AND coded despite
already being discoverable three other ways: it flips verdict, rewrites statement, and
suppresses marker. An advisory has no redundancy at all, and it fires precisely when
nothing else draws the eye. So it is now lifted in the same coded shape, and the
merge-ready statement names it -- that sentence travels beside [merge-eligible] to the
human gate, and one that says strict merge-ready and stops is true and misleading in
the same breath.

The clean case asserts an EMPTY advisories array rather than an absent key: a surface
that only appears when non-empty is one every consumer must guard for, while blockers
is unconditionally present.

Also corrected: the query docblock called hasPreviousPage reported when nothing reads
or surfaces it. By this design it cannot change a decision -- an approval inside the
most-recent window IS the latest, and an empty window already yields silence -- so the
honest fix is the claim rather than a manufactured consumer.

Found by @neo-opus-ada, who proved it with a reach grep plus a strictMergeReady control
rather than inferring it. Red-proofed by removing the lift.

* fix(ai): the merge-gate sentence agrees in number instead of hedging with a slash (#17339)

@neo-opus-ada's flagged wording, taken. At one advisory the sentence rendered
"1 advisory/advisories require a reader judgement" -- slash-plural against a plural
verb, in the one string in the observation aimed at a human.

Cosmetic anywhere else; not here. That sentence IS RA-1's deliverable and it travels
beside [merge-eligible] to the merge gate, so it is the one place wording carries
weight -- generated-looking text is text a reader discounts, and an advisory that gets
discounted has failed at the only job it has.

The spec now asserts the singular AGREEING rather than merely present, plus the
negative that the slash form is gone: toContain('advisory') alone would pass against
exactly the string being removed."
- 2026-08-18T18:06:43Z @tobiu closed this issue

