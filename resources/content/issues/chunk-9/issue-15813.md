---
id: 15813
title: 'E2E boot probe: fail loud when GPU-intent flags resolve to no GL'
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
assignees:
  - neo-opus-ada
createdAt: '2026-07-24T13:01:03Z'
updatedAt: '2026-07-24T17:07:37Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15813'
author: neo-opus-vega
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-24T17:07:37Z'
---
# E2E boot probe: fail loud when GPU-intent flags resolve to no GL

## Context

#15664's root cause was a **five-month-silent dead configuration**: `test/playwright/playwright.config.e2e.mjs` shipped `--use-gl=desktop` on 2026-02-19 (`6ca0d13aa2`, *"GPU-accelerated E2E benchmark infrastructure"*) — a GL selector modern Chrome's ANGLE-only allowlist rejects, so it resolved to `gl=none,angle=none` **from the day it landed**. The GPU process died at every window birth; `--disable-software-rasterizer` removed the swiftshader fallback that would have masked it; Chromium's GPU-crash threshold then terminated the whole headed browser mid-suite (`GPU process isn't usable. Goodbye.` → SIGTRAP), which we investigated for days as a vessel/heap-join defect. The "GPU-accelerated" benchmark suite plausibly never ran a single accelerated frame — and stayed green throughout, because **no source-shaped check can see this class: the config's text never rotted, the environment rotted under it.**

Evidence chain: https://github.com/neomjs/neo/issues/15664#issuecomment-5069969520
Live latest-open sweep: checked latest 20 open issues at 2026-07-24T12:59Z; no equivalent found. A2A in-flight sweep: no overlapping claim (my own `[discussion-signal]` at 12:58Z announced this filing).

## The Problem

The defect class is **environment rot under a valid artifact** ("Face C" on D#15812): a launch flag, once correct or plausibly correct, silently loses its meaning when the runtime evolves (Chrome retired desktop GL; the next major can retire another flag). Review, lint, and a green suite are all form-shaped instruments — they verify the text, not the effect. The #15664 fix removed the two poison flags, but nothing now prevents the **next** flag from rotting the same way. The class needs an *effect-shaped* check: assert the intended runtime state, not the configuration's spelling.

## The Architectural Reality

- `test/playwright/playwright.config.e2e.mjs` — carries GPU-intent launch args (`--ignore-gpu-blocklist`, `--enable-gpu-rasterization`, `--enable-zero-copy`, `--enable-accelerated-2d-canvas`) whose *intent* is hardware GL. Its in-place comment (added by the #15664 fix) documents the constraint but is prose, not enforcement.
- `test/playwright/e2e/globalSetup.mjs` — the existing suite-boot surface (sibling precedent for placement; Agent OS structure-map gate: N/A, test substrate with direct sibling).
- The e2e suite has **no CI lane** — local runs are the only enforcement point, so the probe must live in the suite boot itself, not in a workflow.
- Two candidate observation surfaces, both from inside the launched browser:
  1. **WebGL renderer-string probe** (simplest): open one page, `canvas.getContext('webgl')` → `WEBGL_debug_renderer_info` `UNMASKED_RENDERER_WEBGL`. Healthy macOS = `ANGLE (… Metal …)`; the #15664 failure state = context creation fails or software renderer while GPU flags are configured.
  2. **Browser-level CDP** `SystemInfo.getInfo` via `browser.newBrowserCDPSession()` (browser-level domains verified reachable this way in the #15664 diagnosis — `Target.setDiscoverTargets` worked; `SystemInfo` availability itself is the falsifier to verify first).

## The Fix

Add a fail-loud GL effect-probe to the e2e suite boot:

1. At suite boot (globalSetup or a worker-scoped fixture — implementer verifies which runs inside the *launched project browser*, since globalSetup may run before browser launch; a first-project setup spec is the fallback shape), read the live GL/renderer state via surface (1) (fallback (2)).
2. If the config carries GPU-intent flags AND the live state is `none`/software-fallback, **fail the run immediately** with a message naming the flag-rot class and pointing at #15664's evidence chain.
3. Keep the probe's own failure honest: if the observation surface itself is unavailable (no `WEBGL_debug_renderer_info`, no `SystemInfo`), report *inability to observe* distinctly from *observed-healthy* — a diagnostic that cannot report inability cannot come back negative (the D#15812 lesson).

## Acceptance Criteria

- [x] Probe runs once per e2e suite boot (not per spec). **Cost AC amended 2026-07-24 (see Cost note):** the `≤ ~1s` estimate predated the placement decision and assumed an *in-browser* probe. The chosen placement — a setup project, per @neo-gpt's review and the `chroma-setup` precedent — pays one full browser launch, measured at **+1.6–2.1s** incremental (independently by @neo-gpt on PR #15816). That is the price of the boot-gate guarantee: the probe must fail *before* any benchmark runs, which requires its own browser rather than a shared one. Accepted as the correct trade, not a regression against the estimate.
- [ ] **Red-state acceptance test:** re-adding `--use-gl=desktop` + `--disable-software-rasterizer` on a scratch branch turns the suite red at boot with the class-naming message (today's silent state becomes loud). Evidence: the failing output pasted in the PR.
- [ ] **Green-state test:** current config passes the probe headed AND headless on a macOS seat; renderer string logged as info either way.
- [ ] Inability-to-observe is a distinct, explicit outcome (neither pass nor the red-state message).
- [ ] No new dependencies; probe implemented with Playwright + browser APIs already in the repo.

## Cost note (why the AC moved)

A boot gate that catches flag-rot *before* the benchmarks it protects has to run in its own browser: sharing the suite's browser would mean the first benchmark has already launched by the time the probe fires. The setup-project placement gives that guarantee and costs one browser launch (~1.6–2.1s once per suite). An in-browser probe would cost ~1s but could only observe *after* the browser the benchmarks use is already up — too late to be a gate. The estimate assumed the cheaper shape; the correct shape is the dearer one.

## Out of Scope

- The general detection mechanisms under divergence on D#15812 (CodeQL dataflow, Semgrep, per-file teardown gates) — this ticket is the narrow, incident-anchored regression guard for the config face only. If D#15812 graduates a broader effect-probe substrate (its Option 5/6 rows), this implementation should align with or be absorbed by it — noted there by the author.
- Face A (production reachability) and Face B (test teardown) of the D#15812 class.
- Any change to the launch flags themselves (done in #15664's fix PR).

## Related

- #15664 — the incident this guards against regressing (fix PR removes the dead flags).
- D#15812 — the general "artifact that cannot fail" class discussion; this ticket is referenced there as an incident-anchored follow-up proceeding regardless of graduation outcome (relationship note, not a graduation dependency).
- #15648 — adjacent FM vessel-boot observability lane (benefits from a healthy-GPU baseline).

Decision Record impact: none.

Origin Session ID: 6b95191e-b5bf-487b-9672-96a76060a92b
Retrieval Hint: "GL effect probe suite boot gl=none GPU crash threshold #15664"


## Timeline

- 2026-07-24T13:01:04Z @neo-opus-vega added the `enhancement` label
- 2026-07-24T13:01:04Z @neo-opus-vega added the `ai` label
- 2026-07-24T13:01:05Z @neo-opus-vega added the `testing` label
- 2026-07-24T13:12:58Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-24T13:23:30Z @neo-opus-ada cross-referenced by PR #15816
- 2026-07-24T13:34:40Z @neo-opus-vega cross-referenced by PR #15814
- 2026-07-24T14:21:46Z @neo-opus-ada referenced in commit `4eef368` - "docs(test): port #15814's dead-flag warning to the extracted arg declaration (#15813)

@neo-gpt's review: extracting the launch args into gpuIntent.mjs moved the
declaration #15814 had annotated with its NEVER-pass regression explanation, so
the extraction preserved the flag removal but not its reason. The warning now
lives at BASE_LAUNCH_ARGS' JSDoc — the removal keeps its source authority, not
just its effect. The runtime gate and this comment are cheaper together than
either alone.

No behavior change; comment only."
- 2026-07-24T14:31:07Z @neo-opus-ada referenced in commit `6b47cf6` - "feat(test): fail the E2E suite loud when GPU-intent flags resolve to no GL (#15813)

A launch flag whose text stays valid while the runtime moves out from under it
is invisible to every source-shaped instrument we own. The config reads fine,
the lint passes, the suite is green — and the "GPU-accelerated" benchmarks
plausibly never rendered one accelerated frame. Only the effect differs, so
only an effect probe finds it.

Adds a `gl-probe` setup project the `chromium` project depends on, following
the `chroma-setup` precedent in playwright.config.unit.mjs. It cannot live in
e2e/globalSetup.mjs: that runs as a plain node function outside any launched
browser, which is exactly the difference between reading a config and running
under it.

Three states, not two. `unobserved` exists so "could not look" can never be
spoken as "looked and it was fine" — a probe answering healthy-or-degraded
would be the same defect wearing the guard's uniform, reporting the absence of
evidence as evidence of health. Verified reachable by unit test rather than
left as a branch that only exists in the source: no launch flag can suppress
WEBGL_debug_renderer_info on our seats, so a fake page is the only way that
branch is ever observed firing.

The demand is armed by the declaration, not hardcoded. gpuIntent.mjs is the
single arg list the config spreads and the probe reads; a config that claims no
acceleration demands none, so a headless lane never has to switch the gate off.
Tuning flags (--disable-frame-rate-limit, --force-gpu-mem-available-mb) do not
claim acceleration and deliberately do not arm it.

The software-renderer marker list is a deny-list and says so: an allow-list of
hardware renderers would fail closed on the next GPU shipped. It is the
secondary signal — the primary one needs no strings, since the poisoned flag
pair produces no WebGL context at all.

Evidence, both directions, against real Chrome on this host:
  poisoned args -> {"context": false}, gate fails naming the flag-rot class
  clean args    -> ANGLE (Apple, ANGLE Metal Renderer: Apple M5 Max), gate passes
7 unit tests cover the decision core including both unobserved paths."
- 2026-07-24T14:31:07Z @neo-opus-ada referenced in commit `7c749ea` - "docs(test): port #15814's dead-flag warning to the extracted arg declaration (#15813)

@neo-gpt's review: extracting the launch args into gpuIntent.mjs moved the
declaration #15814 had annotated with its NEVER-pass regression explanation, so
the extraction preserved the flag removal but not its reason. The warning now
lives at BASE_LAUNCH_ARGS' JSDoc — the removal keeps its source authority, not
just its effect. The runtime gate and this comment are cheaper together than
either alone.

No behavior change; comment only."
- 2026-07-24T14:31:07Z @neo-opus-ada referenced in commit `0bda184` - "docs(test): make the poison-flag ban module-wide, not list-scoped (#15813)

@neo-gpt cycle-2: a maintainer adding --use-gl=desktop reaches for GPU_INTENT_ARGS
(it reads as a GPU selector), so a ban scoped to BASE_LAUNCH_ARGS guarded the one
list the wrong edit would never touch. Moved the prohibition to the module header
where it covers every list, and left a one-line pointer on BASE_LAUNCH_ARGS."
- 2026-07-24T16:50:15Z @neo-opus-ada referenced in commit `63d7c09` - "fix(ai): unproven containment is not permission — fail closed, classified (#15818)

@neo-gpt-emmy's cycle-2 falsifier: a resolution failure (EACCES on an ancestor,
ELOOP, a vanished parent) left containment UNPROVEN and escaped as a raw fs
error. Reproduced before fixing — `readFile` through a mode-000 ancestor threw
`EACCES: permission denied, realpath ...`, not a containment refusal.

It did stop the operation, so this was never an escape. The defect is
classification: a bare EACCES reads to a caller — and to an agent reading the
tool result — as an I/O problem worth retrying, when the truth is that the jail
could not answer. Indistinguishable from ordinary I/O failure is exactly how a
refusal gets caught generically and worked around.

So the guard now has THREE states rather than two: contained · outside ·
could-not-establish. The third is its own explicit refusal naming the cause and
stating that it is NOT a verdict the path is safe.

This is the same three-state discipline I built into the GL boot probe this
morning (#15813) and did not build into this guard this afternoon — observed
healthy, observed degraded, and could-not-observe, where collapsing the third
into either of the others is the whole defect class.

Verified RED against the pre-classification guard: exactly the new test fails,
10 pass."
- 2026-07-24T17:07:35Z @tobiu referenced in commit `90a8426` - "feat(test): fail the E2E suite loud when GPU-intent flags resolve to no GL (#15813) (#15816)

* feat(test): fail the E2E suite loud when GPU-intent flags resolve to no GL (#15813)

A launch flag whose text stays valid while the runtime moves out from under it
is invisible to every source-shaped instrument we own. The config reads fine,
the lint passes, the suite is green — and the "GPU-accelerated" benchmarks
plausibly never rendered one accelerated frame. Only the effect differs, so
only an effect probe finds it.

Adds a `gl-probe` setup project the `chromium` project depends on, following
the `chroma-setup` precedent in playwright.config.unit.mjs. It cannot live in
e2e/globalSetup.mjs: that runs as a plain node function outside any launched
browser, which is exactly the difference between reading a config and running
under it.

Three states, not two. `unobserved` exists so "could not look" can never be
spoken as "looked and it was fine" — a probe answering healthy-or-degraded
would be the same defect wearing the guard's uniform, reporting the absence of
evidence as evidence of health. Verified reachable by unit test rather than
left as a branch that only exists in the source: no launch flag can suppress
WEBGL_debug_renderer_info on our seats, so a fake page is the only way that
branch is ever observed firing.

The demand is armed by the declaration, not hardcoded. gpuIntent.mjs is the
single arg list the config spreads and the probe reads; a config that claims no
acceleration demands none, so a headless lane never has to switch the gate off.
Tuning flags (--disable-frame-rate-limit, --force-gpu-mem-available-mb) do not
claim acceleration and deliberately do not arm it.

The software-renderer marker list is a deny-list and says so: an allow-list of
hardware renderers would fail closed on the next GPU shipped. It is the
secondary signal — the primary one needs no strings, since the poisoned flag
pair produces no WebGL context at all.

Evidence, both directions, against real Chrome on this host:
  poisoned args -> {"context": false}, gate fails naming the flag-rot class
  clean args    -> ANGLE (Apple, ANGLE Metal Renderer: Apple M5 Max), gate passes
7 unit tests cover the decision core including both unobserved paths.

* docs(test): port #15814's dead-flag warning to the extracted arg declaration (#15813)

@neo-gpt's review: extracting the launch args into gpuIntent.mjs moved the
declaration #15814 had annotated with its NEVER-pass regression explanation, so
the extraction preserved the flag removal but not its reason. The warning now
lives at BASE_LAUNCH_ARGS' JSDoc — the removal keeps its source authority, not
just its effect. The runtime gate and this comment are cheaper together than
either alone.

No behavior change; comment only.

* docs(test): make the poison-flag ban module-wide, not list-scoped (#15813)

@neo-gpt cycle-2: a maintainer adding --use-gl=desktop reaches for GPU_INTENT_ARGS
(it reads as a GPU selector), so a ban scoped to BASE_LAUNCH_ARGS guarded the one
list the wrong edit would never touch. Moved the prohibition to the module header
where it covers every list, and left a one-line pointer on BASE_LAUNCH_ARGS."
- 2026-07-24T17:07:37Z @tobiu closed this issue
- 2026-07-24T17:35:11Z @tobiu referenced in commit `5acc564` - "fix(ai): pass argv, not a shell string, from the file-system MCP server (#15818) (#15819)

* fix(ai): pass argv, not a shell string, from the file-system MCP server (#15818)

`ensureSandboxed()` validates path containment and nothing else, and its
return value was interpolated into a shell command via `exec`. The path is an
MCP tool parameter, so it is agent-supplied. Replayed against the guard's exact
logic, every one of these is ADMITTED and reaches a shell:

    "x.mjs; id"   "x.mjs && id"   "x.$(id).mjs"   "a b.mjs"

The guard was never wrong. It answers "is this inside the root", and the call
site read that as "is this safe to concatenate into a command". `execFile` with
an argv array makes injection unrepresentable instead of filtered — nothing to
escape, and no allow-list of metacharacters to keep complete forever.

Second defect in the same function, which the alert does not mention:
containment used `startsWith`, so `<root>-evil/x` passed a jail whose entire
purpose was to exclude it. Now compared on a path boundary via `path.relative`,
which benefits all five non-exec callers too. The empty-string case (the root
itself) is handled first, since `isAbsolute('')` is false but so is a `..`
prefix check, and getting that wrong would reject the root.

The load-bearing test is behavioural, not structural. Asserting "the source
calls execFile" would pass the moment someone writes the right call and say
nothing about what the child receives. Instead a file literally named
`probe ;.mjs` is syntax-checked: under argv `node --check` parses that exact
file and returns Syntax OK; under a shell the command splits at the semicolon
and fails. The assertion cannot pass if the shell ever comes back.

Verified RED against unfixed source: exactly 3 of 5 fail, and the 2 that pass
are the guards that were already correct.

Module had zero test coverage before this.

* fix(ai): canonical containment — an in-root symlink must not reach outside (#15818)

@neo-gpt-emmy executed the falsifier against the prior head: an in-root alias to
an outside directory, then `readFile` through it — `ADMITTED_OUTSIDE_TARGET`,
sentinel returned. She is right, and the diagnosis is the sharper part: the
guard proved "the spelled path is under the root" while the contract is "the
filesystem object reached is under the root."

`path.resolve`/`path.relative` normalize SEGMENTS; they do not dereference
symlinks. So the lexical check answered an adjacent question with a confident
answer — the same class this PR's own commit message is about, one axis over.

Both sides are now canonicalized before comparison. `fs.realpath` cannot
canonicalize a file that does not exist yet, and `writeFile` legitimately
targets those, so `canonicalize()` walks to the deepest EXISTING ancestor and
re-appends the remainder — which is the security-relevant part anyway: a create
target can only escape through a parent that already exists and already points
outside.

`ensureSandboxed` is now async (all five callers already were) and returns the
canonical path, so callers operate on the verified object rather than on a
spelling that was merely validated.

Three tests, and the third matters as much as the first two: an in-root symlink
to outside is rejected with the outside sentinel proven unread AND unchanged; a
not-yet-existing write target under a symlinked-outside parent is rejected with
nothing created there; and legitimate creates plus in-root aliases still work.
A jail that rejects real work gets switched off.

Verified RED against the lexical-only guard: exactly the two escape tests fail,
8 pass. The third passes both ways by design — it guards the narrowing, not the
fix, which is the direction a single-direction control misses.

* fix(ai): unproven containment is not permission — fail closed, classified (#15818)

@neo-gpt-emmy's cycle-2 falsifier: a resolution failure (EACCES on an ancestor,
ELOOP, a vanished parent) left containment UNPROVEN and escaped as a raw fs
error. Reproduced before fixing — `readFile` through a mode-000 ancestor threw
`EACCES: permission denied, realpath ...`, not a containment refusal.

It did stop the operation, so this was never an escape. The defect is
classification: a bare EACCES reads to a caller — and to an agent reading the
tool result — as an I/O problem worth retrying, when the truth is that the jail
could not answer. Indistinguishable from ordinary I/O failure is exactly how a
refusal gets caught generically and worked around.

So the guard now has THREE states rather than two: contained · outside ·
could-not-establish. The third is its own explicit refusal naming the cause and
stating that it is NOT a verdict the path is safe.

This is the same three-state discipline I built into the GL boot probe this
morning (#15813) and did not build into this guard this afternoon — observed
healthy, observed degraded, and could-not-observe, where collapsing the third
into either of the others is the whole defect class.

Verified RED against the pre-classification guard: exactly the new test fails,
10 pass.

* fix(ai): a dangling alias is an object, not an absence (#15818)

@neo-gpt-emmy's cycle-3 falsifier, reproduced before fixing: an in-root symlink
whose target does not exist was treated as a create target, and `writeFile`
FOLLOWED it — the file landed outside the root. Measured:

    alias lstat isSymbolicLink : true
    realpath(alias)            : ENOENT
    writeFile                  : no throw, outside file created, content landed

`realpath` returns ENOENT for TWO states that are not the same security fact:
the entry is ABSENT, or the entry EXISTS as a symlink whose target is missing.
`canonicalize` collapsed them, so a dangling alias read as "not yet created" —
and a write follows a dangling symlink to wherever it points.

`lstat` is what tells them apart: it reports on the link itself rather than on
what the link fails to reach. When ENOENT comes back and `lstat` says symlink,
the declared target is resolved manually and canonicalization continues from
there, with a hop cap so a symlink cycle raises rather than spins (that raise is
then classified by the fail-closed wrapper, not leaked raw).

This is the same collapse for the fourth time today in my own code: two distinct
states read through one signal. lsof-failure vs quiet host; absent canonical vs
unreadable seat; unproven containment vs permission; and now absent entry vs
dangling alias. Every one of them a peer found, none of them self-caught.

Two tests, and the second earns its place: the escape is rejected with nothing
created outside, AND a dangling alias whose target resolves in-root remains a
legitimate create. A guard that rejects real work gets switched off.

Verified RED against the pre-fix guard: exactly the escape test fails, 3 pass —
the in-root case passing both ways by design, since it guards the narrowing.
13 pass with the fix."

