---
id: 16630
title: The MCP servers die on an undeclared heap ceiling the immune system cannot see
state: OPEN
labels:
  - bug
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-07T12:59:26Z'
updatedAt: '2026-08-07T15:03:41Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16630'
author: neo-opus-vega
commentsCount: 0
parentIssue: null
subIssues:
  - '[x] 16642 The MCP servers declare no V8 heap ceiling, and the fix has a false-green trap'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# The MCP servers die on an undeclared heap ceiling the immune system cannot see

Resolves nothing yet — filed from a live incident.

> ## ⛔ PRESCRIPTION AMENDED 2026-08-07 — PR #16634 closed unmerged. Read this box before implementing anything below.
>
> @neo-gpt returned **Drop+Supersede / `ticket-prescription-off`** on [PR #16634](https://github.com/neomjs/neo/pull/16634#pullrequestreview-4883941191). **The verdict is accepted; all three falsifiers were reproduced locally.** The incident in Context is real and the ceiling declarations are correct. **The diagnosis prescription in "The Problem §2" and the original AC-2 is dimensionally invalid and must not be built.**
>
> **The invalid part.** `memory_stats.usage` is **cgroup usage for the whole container** — V8 heap plus native allocations, `Buffer`s outside the heap, the binary, stacks, other processes. `--max-old-space-size` bounds **V8's old space alone**. Dividing the first by the second does not yield heap saturation: the ratio can be driven authoritative by memory V8 never touched, and can equally miss a real old-space exhaustion. `§2`'s "~66% of the V8 heap limit" line is that same conflation and is **struck** below.
>
> **And there is no in-place repair, which is what makes this terminal rather than a tweak.** Verified: the only V8 metrics anywhere in `ai/` are `process.memoryUsage().heapUsed`-family calls in `ai/scripts/benchmark/*`, all measuring the **local** process. Nothing collects `used_heap_size` / `heap_size_limit` for a **sibling container**, and nothing can over the Docker socket — the container has to expose them. **A dimension-matched V8 saturation fact requires a new observation channel that does not exist yet.** No denominator change reaches it.
>
> **Salvage map** (per the review, cited as required):
>
> | keep | discard |
> |---|---|
> | command-scoped `--max-old-space-size` for both MCP servers | the container-usage ÷ V8-cap denominator |
> | the no-`NODE_OPTIONS` rule + child-inheritance rationale, spec-asserted | `parseDeclaredHeapBytes`'s last-textual-match rule |
> | the *intent* to test the rendered command — reimplemented against `docker compose config` **output** as the test input | both denominator specs |
> | command parsing, **rebuilt fail-closed**: observable only when all declarations agree | the ADR-0025 §2.4 "effective ceiling" amendment |
>
> **Scope widened by one thing the review did not claim, found while reproducing its falsifier.** The `$$`→`$` render trap is **live in canonical `dev` today**, independent of PR #16634: mutating all 6 occurrences makes `docker compose --profile cloud config` exit **0** while rendering `node --max-old-space-size=6144 ""` for the **orchestrator** — a service that has declared its ceiling since #16459 and is not touched by this ticket's diff. So the rendered-command guard covers **three** services, and it is a pre-existing canonical defect rather than a new-code concern.

## Context

Operator report at 2026-08-07 ~12:45Z: `@neo-opus-grace` could not send A2A messages, timing out. My own `add_memory` had timed out at ~12:15Z, so it was not one seat.

**Root cause, measured:** `mc-server` killed itself at `2026-08-07T11:40:42Z`.

```
FATAL ERROR: Ineffective mark-compacts near heap limit
Allocation failed - JavaScript heap out of memory
```

Docker restarted it; healthy since; exactly **one** abort in the whole log window (04:07Z→now), so not a loop. MC was responsive when I measured it — five POSTs to the live MCP transport, all HTTP 401 in **17–21ms** (401 because I sent no bearer token, i.e. the transport answering correctly). The peer-visible symptom was almost certainly stale MCP sessions issued by the pre-restart process, which is a client-reconnect matter and not this ticket.

The operator's reaction is the reason this is filed: *"I actually thought we had it covered already."* **He is right — and it is worth being precise about what is covered, because the coverage is good and the gap is narrow.**

**Live latest-open sweep: latest 20 open issues checked at 2026-08-07T12:57:15Z.** A2A in-flight claim sweep at the same time: active claims are `#16515`/`#16626`/`#16623` (@neo-fable), `#16488`/`#16551` (@neo-opus-ada), `#16535`/`#16559` (@neo-opus-grace). No overlap.

## The Problem

**Nothing detected it, and three independent reasons had to line up.**

### 1. Churn requires three restarts. This was one.

`ContainerHealthDiagnosisService` has a `restart-churn` signal — added by #16462, ADR-0025's 2026-08-03 amendment, explicitly *"the first signal in the model that is a property across container incarnations rather than a point-in-time observation."* Exactly this class of evidence.

Its threshold is **3 restarts in a window**, and the reasoning in the code is sound: *"Three sits above ordinary transients … and far below a real loop, which reached 977."* One restart is deliberately not churn.

**But a heap-OOM exit is authoritative on the first occurrence.** A process that died of its own heap ceiling does not need a second sample to be diagnosable — the cause is in the log and it is unambiguous. Requiring three converts a knowable single event into an unknowable one, and a ceiling that fills over ~8 hours may *never* produce three-in-a-window while still killing the service repeatedly.

### 2. ⛔ AMENDED — the saturation signal cannot see this failure at all, and swapping its denominator does not fix that.

**What survives:** `memory-saturation` computes `usage / limit * 100` against the Docker container limit (`ContainerHealthDiagnosisService:1092`), read **36%** while the process was aborting, and therefore contributed nothing. That is true and it is the detection gap.

~~```
mc-server RSS 335–396 MiB / 1024 MiB container limit  =  ~36%   → nowhere near any threshold
mc-server RSS 335–396 MiB / ~560 MiB V8 heap limit    =  ~66%   → and it reached 100% at 11:40
```~~

⛔ **The second line is struck: it is the dimensional conflation.** `RSS` (or cgroup usage) over a **V8 old-space** cap is not a heap-saturation percentage — the numerator includes memory the cap does not govern. "~66%" was never a statement about the heap, and "it reached 100% at 11:40" is an inference from the abort, not an observation of that ratio.

**So the correct framing of this defect is narrower and harder:** the container-scoped signal is *dimensionally incapable* of seeing a V8 old-space exhaustion, at any denominator. It is not watching the wrong ceiling — it is watching the wrong **quantity**, and the right quantity is not currently observable for a sibling container. The successor slice is an observation channel, not arithmetic.

The honest detection story for this incident is therefore the **exit signature**, not saturation: a heap-OOM abort is authoritative at n=1 (§1), self-reported in the log, and needs no ratio. `§1` was always the stronger half of this ticket and is untouched by the review.

### 3. The ceiling that enforces is undeclared, so it is not merely unmonitored — it is unknowable to config.

| service | declared heap ceiling | container limit | effective ceiling |
|---|---|---|---|
| `orchestrator` | **`--max-old-space-size=6144`**, env-tunable via `NEO_ORCHESTRATOR_HEAP_MB` (`docker-compose.yml:399`) | 12 GiB | 6144 MiB — declared, *below* the container limit, correct direction |
| `mc-server` | **none** | 1 GiB | **~560 MiB, an emergent V8 heuristic** |
| `kb-server` | **none** | 1 GiB | **~560 MiB, an emergent V8 heuristic** |

**You cannot monitor a ceiling nobody wrote down.** `min(containerLimit, declaredHeapCeiling)` is uncomputable when the second term does not exist.

The orchestrator declares its ceiling *because* of #16459's 968-restart loop — the lesson was learned there and never propagated to the MCP servers. **`kb-server` carries the identical latent defect and has simply not hit it yet.**

## The Architectural Reality

- `ai/daemons/orchestrator/services/ContainerHealthDiagnosisService.mjs:1092` — `limit = Number(stats.memory_stats?.limit)`, the container-limit denominator.
- same file, `:10-19` — the nine live `DIAGNOSIS_KINDS`, including `memorySaturation` and `restartChurn`.
- same file, `:21-27` — the five `CONTAINER_HEALTH_ACTION_CLASSES`, **including `raiseCeiling`**.
- same file, `:719-725` — the **only** site prescribing `raiseCeiling`, gated on `storeMemoryFacts.length > 0` with reason `store-ceiling-exhaustion`. **Store-scoped.** `mc-server` is a service, not a store, so a service-class ceiling exhaustion has no route to the action that would fix it.
- `ai/deploy/docker-compose.yml:399` — the orchestrator's declared, knob-driven ceiling; the shape to copy.
- `ai/deploy/docker-compose.yml:248` / `:130` — mc-server and kb-server `deploy.resources.limits.memory: 1g`, with no corresponding heap declaration.
- `ProcessSupervisorService.mjs` — the only place in `ai/daemons/orchestrator/` aware of a V8 heap ceiling, and it governs the orchestrator's **in-process children**, not sibling containers (ADR-0025 §1.2 says so explicitly).

**The actuator is already capable.** Per #16596, `RecoveryActuatorService` `DEFAULT_ACTIONS` includes `reconfigure`, dispatching to `reconfigureComposeService({knob, knobValues, reason, target})`. So this is not a missing mechanism at either end — detect exists, act exists, and `raiseCeiling` exists. What is missing is a **declared ceiling to measure against and a knob to move.**

## Amended after @neo-opus-grace's independent read: canonical-wide, and NODE_OPTIONS is the REJECTED shape

**1. The gap is canonical, so every derived deployment inherits it.** Both `memory: 1g` services in `ai/deploy/docker-compose.yml` — `kb-server` (`:130`) and `mc-server` (`:248`) — declare no ceiling; the orchestrator (`:399`) is the *only* service in the file that declares one. Grace confirmed an external deployment's compose carries zero occurrences of `max-old-space-size` or `NODE_OPTIONS` across three 1 GiB services. **Nobody edited that gap in — it propagated.** So this is an upstream fix, not a plane-local one, and the fix is three services rather than one.

`kb-server` is the same shape as `mc-server`: V8 picks ~560 MiB inside a 1 GiB container and aborts with ~460 MiB unused. It has not hit the wall because its corpus is smaller — **timing, not immunity.**

**2. `NODE_OPTIONS` is the explicitly REJECTED mechanism, and I proposed it verbally before reading the precedent.** `docker-compose.yml:382`:

> `NODE_OPTIONS is inherited by every child and the container budget is silently multiplied by …`

So the ceiling must be `command:`-scoped per the orchestrator's shape, never an env var — an inherited `NODE_OPTIONS` would give every supervised child the parent's ceiling and multiply the budget. Recording the correction because the wrong version is the intuitive one and the next reader will reach for it too.

**3. A trap in the precedent that this fix must copy, not just its shape.** The same comment block explains that `$$SERVER_ENTRYPOINT` is double-escaped so Compose does *not* interpolate it at config time — and critically:

> `docker compose config` reports that as a warning and still exits 0, so an exit-code check passes while the rendered command is broken.

A `command:` added to `kb-server`/`mc-server` with single-`$` escaping renders as `node --max-old-space-size=<n>` with **no script**, and any CI check that only asserts `docker compose config` exit 0 will pass on it. That is a false green in the fix for a false-green ticket, so it gets its own AC below rather than a comment.

## The Fix

1. **Declare the heap ceiling for both MCP servers**, following the orchestrator's exact shape — a `command:`-scoped `--max-old-space-size` driven by an env knob (`NEO_MC_SERVER_HEAP_MB`, `NEO_KB_SERVER_HEAP_MB`), sized *below* the container limit so V8 aborts before the container OOM-kills. Per ADR-0019 the knob is a `leaf()`; `NEO_ORCHESTRATOR_HEAP_MB` is the sibling precedent.
2. **Fix the denominator.** A service-class `memory-saturation` fact must be computed against the **effective** ceiling — `min(containerLimit, declaredHeapCeiling)` — not the container limit alone. For a store the container limit *is* the effective ceiling, so store behaviour is unchanged.
3. **Make an undeclared ceiling itself diagnosable.** A Node service with no declared heap ceiling cannot have its saturation measured, and that is a condition the immune system should record rather than silently mis-measure. `record`-terminal, additive, in the style of the #16462 restart-churn amendment.
4. **Route service-class ceiling exhaustion to `raiseCeiling`**, which already exists and is currently reachable only from the store path.
5. **Consider whether 1 GiB is the right budget** for `mc-server` at all. It holds the graph and session state, the corpus is growing, and this ticket's fix makes it die *honestly* at its ceiling rather than mysteriously — which is necessary but not the same as having enough room.

**Decision Record impact:** `amends ADR 0025` (the detect model gains the effective-ceiling denominator and the undeclared-ceiling condition) and `aligned-with ADR 0026` (no new action class or privilege — `reconfigure` and `raiseCeiling` both already exist; only their reachability changes). Both amendments are additive, non-authoritative, and change no AC of either ADR — deliberately the same shape as the #16462 amendment, which is the precedent for extending the detect model without reopening its quorum.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `mc-server` / `kb-server` `command:` | `docker-compose.yml:399` (orchestrator precedent) | declared `--max-old-space-size` from an env knob, sized below the container limit | absent knob → current emergent default, i.e. today's behaviour | compose comment + ADR 0025 | heap OOM at 11:40:42Z with ~460 MiB of the container allowance unused |
| `NEO_MC_SERVER_HEAP_MB` / `NEO_KB_SERVER_HEAP_MB` | ADR-0019 `leaf()`; `NEO_ORCHESTRATOR_HEAP_MB` sibling | declarative leaf, env-overridable, no re-derivation | — | config leaf JSDoc | the orchestrator knob already exists and is reachable by the actuator |
| ~~`memory-saturation` fact~~ | ~~`ContainerHealthDiagnosisService:1092`~~ | ⛔ **VOID** — `min(containerLimit, declaredHeapCeiling)` against a cgroup numerator is a cross-scope ratio | — | — | ~~36% of 1024 vs ~66% of 560~~ — the second figure is the conflation, not evidence |
| **a V8 saturation fact** *(replaces the row above)* | `node:v8` `used_heap_size` / `heap_size_limit`; Docker stats documents its own usage as container/cgroup scope | emitted **only** from a V8-scoped numerator **and** V8-scoped capacity; no cross-scope pair permitted | **no fact at all** until the channel exists — silence beats a wrong authoritative percentage | ADR 0025 §2.4, once truthful | verified: no V8 metric is collected for any sibling container anywhere in `ai/`; the only heap reads are local-process benchmark calls |
| **container pressure fact** | `ContainerHealthDiagnosisService:1092`, unchanged | stays container usage ÷ container capacity, both scopes matching | — | ADR 0025 §2.4 | it was always dimensionally sound; only the reinterpretation was not |
| **declared-ceiling parsing** | `Config.Cmd` / `Config.Entrypoint` | **fail-closed**: observable only when every declaration agrees; divergent → `unknown` | `unknown`, never a pick | inline | `768` vs `256` in mc-server's two branches leaves the shipped spec 14/14 green while the parser reports the unexecuted branch |
| **rendered-command guard** | `docker compose config` **output** | the guard's input is the rendered artifact; covers all three Node services incl. `orchestrator` | — | inline + compose comment | `$$`→`$` × 6 renders `node --max-old-space-size=<n> ""` at exit 0 on canonical `dev`, spec 14/14 green |
| ~~an `undeclared-ceiling` condition~~ · ~~`raiseCeiling` reachability~~ | — | moved to **#16636** | — | — | see the moved-ACs section below |

## Acceptance Criteria

**Two slices, and they must not travel in one PR.** Slice A is a configuration + rendering repair with no observability claim. Slice B is a new observation channel. PR #16634 failed because it joined them and let A's correctness carry B's premise.

#### Slice A — declaration + rendering. Landable now.

- [ ] `mc-server` and `kb-server` each declare a heap ceiling via an env knob, `command:`-scoped, and a spec asserts the declared value is **strictly below** the container limit for each — a ceiling above the container limit converts a clean V8 abort into a container OOM-kill, which is a harder failure, not a fixed one.
- [ ] The ceiling is **NOT** `NODE_OPTIONS` — spec-asserted, with the child-inheritance reason in the assertion message so a future simplification back to an env var fails loudly.
- [ ] `kb-server` is fixed in the same change as `mc-server`. Its immunity is corpus size, not shape.
- [ ] **The rendered-command guard takes `docker compose config` OUTPUT as its test input** — the actual rendered artifact, not a `js-yaml` load of the source. **This AC replaces the one PR #16634 claimed to satisfy and did not:** its oracle read raw YAML, so `toContain('SERVER_ENTRYPOINT')` was true under either escaping. Mutating all 6 `$$`→`$` occurrences left the spec **14 passed** while `docker compose config` exited **0** rendering `node --max-old-space-size=<n> ""` — an empty script argument.
- [ ] **The guard covers all three Node services, including `orchestrator`.** Its exposure is pre-existing (declared since #16459) and reproduces on canonical `dev` today, so a two-service guard would leave the original instance unguarded.
- [ ] **Mutation-proven:** the `$$`→`$` mutation must turn the new guard red, and the mutation's *application* must be verified (an occurrence count), not assumed — a silent no-op substitution exits 0.

#### Slice B — dimension-matched V8 observation. Blocked on a channel that does not exist.

- [ ] **A V8-scoped numerator is obtainable for a sibling container before any heap-saturation fact is emitted.** `used_heap_size` / `heap_size_limit` per `node:v8`, exposed by the container itself; `memory_stats.usage` is not a substitute at any denominator. **No `memory-saturation` fact for a Node service may be computed from a cross-scope pair.** Design the channel first — this is the successor's real work.
- [ ] Container pressure keeps its honest form: **container usage ÷ container capacity**. The store path is unchanged by construction and asserted as a control.
- [ ] **Command parsing is fail-closed on ambiguity:** multiple `--max-old-space-size` declarations are observable **only when every value agrees**; divergent values report `unknown`, never a pick. Last-textual-match is rejected — `Config.Cmd` does not say which branch is executing, so a pick is a guess with a number attached.
  - **Falsifier that must go red:** setting mc-server's two branches to `768` and `256` currently leaves the spec **14 passed**, because it asserts count-and-upper-bound and never equality. The parser then reports `256` for a container running the `768` branch. The code comment claiming *"both are held equal by `DeclaredHeapCeilings.spec.mjs`"* asserted a guarantee the spec never made.
- [ ] **Evidence level stated explicitly:** the live recreate and diagnosis behaviour is **residual-live (L3)**. Unit receipts close Slice A's static contract only; neither slice closes this ticket on unit evidence alone.
- [ ] Any ADR-0025 amendment describes **what is actually measured**. The wording PR #16634 shipped — "effective ceiling" — encodes the conflation and must not be reintroduced.

#### Struck ACs

~~A service-class `memory-saturation` fact is computed against `min(containerLimit, declaredHeapCeiling)`.~~ ⛔ **Dimensionally invalid** — this is the prescription the review terminated. It is replaced by Slice B's first AC.

~~ADR 0025 carries the amendment line in its `Amended` row.~~ ⛔ Superseded: an amendment is owed only once there is a truthful claim to record. The version written for PR #16634 said "effective ceiling" and was withdrawn with it.

### Two ACs moved to #16636 — scope replaced, not dropped

The original AC list carried the **act** half as well:

> - A Node service with **no** declared heap ceiling produces an `undeclared-ceiling` record naming the service, and that record is `record`-terminal.
> - Service-class ceiling exhaustion reaches `raiseCeiling`; asserted by a fixture, with the existing `store-ceiling-exhaustion` route asserted unchanged.

Both now live in **#16636**, and the split is along ADR-0025-detect ↔ ADR-0026-act, which is the architecture's own seam rather than a convenience line. Two findings forced it, and both were discovered after the ACs were written:

1. **`raise-ceiling` is sanctioned by no ADR.** `grep -rn 'raiseCeiling' learn/` returns **0 hits** while the enum ships it at `ContainerHealthDiagnosisService.mjs:22`. It has no heal operation and is absent from `HEAL_ACTIONS`. So this AC is not "reach an existing action" — it is "reconcile the ADR with an action class the implementation already emits," which is an ADR-0026 §2.4 amendment. @neo-fable-clio is amending that exact section for #16596 right now; two PRs editing the matrix that sanctions what the actuator may do is the worst possible merge conflict, so hers goes first and #16636 rebases onto it.
2. **The AC as written was too flat.** "Route service-class exhaustion to `raiseCeiling`" is wrong for a service whose declared ceiling sits at the container limit — there is nothing to raise into, and a Node process's heap, unlike a store's resident corpus, genuinely is freed by a restart. The condition is **headroom**, not class. #16636 states it that way.

## Out of Scope

- **Grace's A2A timeouts.** Explained by stale MCP sessions from the pre-restart process; the remedy is a client reconnect. If a reconnect does not clear it, that is a separate fault and gets its own ticket rather than being absorbed here.
- **Whether ~560 MiB is a leak or a bounded working set.** That is #16463's question for the orchestrator and the same question deserves asking here, but this ticket makes the ceiling *declared and visible*; it does not characterise the growth curve. Raising a ceiling to hide unbounded growth would convert a fast failure into a slow one, which #16463 says explicitly.
- **Chroma's ceiling** — #16595 / #16596, store-scoped, already open and assigned to me.
- **The orchestrator's ceilings** — #16463, @neo-opus-grace's, which asks whether *declared* ceilings hold. This ticket is the inverse: ceilings that were never declared.
- **An external deployment's `mc-server` currently reporting `unhealthy`.** @neo-opus-grace measured it as a DIFFERENT fault with the same symptom word: `oomKilled: false`, `exitCode: 0`, and a Docker healthcheck `timeout: 10s` against an MCP handshake whose embedding write canary is allowed 30s (`embeddingWriteCanaryTimeoutMs`, default 30000) — provider aborts at 29.998s on the dot, every five minutes. A healthcheck deadline shorter than the canary it waits on can never pass. Flagged here only so a green or red result there is not read as evidence for or against this ticket's mechanism; it deserves its own ticket and is not claimed by this one.
- ~~**A new detect signal for heap-OOM exit codes specifically.** Tempting, and probably right eventually, but the ledger evidence for whether the effective-ceiling denominator alone closes the class does not exist yet. Adding two mechanisms and learning which one fired is worse than adding one.~~

  ⛔ **This exclusion's reasoning is void and the item is now IN scope** — it was excluded on the grounds that the denominator fix might close the class alone, and that fix no longer exists. With the saturation route dimensionally unavailable and its V8 replacement blocked on a channel nobody has built, **the abort signature is the only detection path that is both sound and available today.** A heap-OOM exit is self-reported, authoritative at n=1, needs no ratio, and requires no new observation channel. It is now the *leading* candidate for this ticket's detection half rather than a deferred nice-to-have. Note this reverses the "adding two mechanisms" argument entirely: there is currently **one** available mechanism, not two.

## Avoided Traps

- **Lowering the churn threshold from 3.** It would catch this and flood the ledger with ordinary transients, and the code's own reasoning for 3 is sound. The defect is not the threshold; it is that a heap abort is authoritative at n=1 and is being routed through a frequency signal at all.
- **Raising the container limit and calling it fixed.** The process would still self-abort at V8's heuristic default, which does not track the container limit. More container memory with no declared heap ceiling changes nothing about the failure.
- **A new "heap" diagnosis kind beside `memory-saturation`.** Two signals for one concern, free to disagree — the shape removed from `backup.mjs` (#16614) and from the parity lint (#16585) this same week. The existing signal needs the right denominator, not a sibling.

## Related

- #16636 — the **act half**, split out of this ticket's ACs 4 and 5. Blocked by #16596's ADR-0026 amendment.
- #16595 / #16596 — the **store-side** siblings, mine and open. #16596 established that detection measures against the container limit and that `RecoveryActuatorService` can already `reconfigure` a compose knob; its blocker is a hardcoded `memory: 2g`. This ticket's blocker is one layer in — not hardcoded, *undeclared*.
- #16463 — @neo-opus-grace's, the **orchestrator-side** sibling: ceilings that ARE configured, and whether they hold.
- #16462 — added the `restart-churn` signal and the precedent for amending ADR-0025's detect model additively.
- #16459 — the 968-restart loop that produced the orchestrator's declared ceiling in the first place.
- ADR 0025 / 0026 / 0027 — the immune system this extends rather than duplicates.

Origin Session ID: `4141258c-36d3-4788-b0c2-ab3ebe0867be`

Retrieval Hint: `query_raw_memories("undeclared V8 heap ceiling below container limit saturation denominator")` · `ContainerHealthDiagnosisService.mjs:1092` · the `11:40:42Z` mc-server abort.



## Timeline

- 2026-08-07T12:59:26Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-07T12:59:28Z @neo-opus-vega added the `bug` label
- 2026-08-07T12:59:28Z @neo-opus-vega added the `ai` label
- 2026-08-07T12:59:28Z @neo-opus-vega added the `architecture` label
- 2026-08-07T12:59:28Z @neo-opus-vega added the `agent-os` label
- 2026-08-07T13:27:06Z @neo-opus-vega cross-referenced by PR #16634
- 2026-08-07T13:47:27Z @neo-opus-vega cross-referenced by #16596
- 2026-08-07T14:09:05Z @neo-opus-vega referenced in commit `c119994` - "test(orchestrator): the ceiling fix gets a fact-level control pair and ADR-0025 carries its amendment (#16630)

Two of #16630's ACs that I had left open and mis-described as one.

FACT-LEVEL CONTROLS. The existing tests measure a PERCENT; the ticket's negative
control is about whether a FACT is emitted, and those are different claims — a
denominator can be right while the fact never reaches the emit path, and a percent
assertion cannot tell those apart. Added a pair against the real classifier:
mc-server (a DECLARED non-store in SERVICE_CLASS_BY_KEY, so the roster is
exercised rather than an unrecognised key defaulting into the same policy),
inspect carrying the declared 768 MB ceiling, two samples across the sustained
window, CPU pinned at ~4% so a CPU fact cannot satisfy the corroboration floor and
mask which signal fired.

- 400 MiB = 39% of the container limit, 52% of the ceiling: NO memory fact, status
  healthy. The sharper denominator must not make every service permanently
  saturated.
- 730 MiB = 71% of the container limit (silent under a 90% threshold, i.e. today's
  behaviour) and 95% of the declared ceiling: exactly one fact.

The pair is the point. "Comfortably inside both ceilings emits nothing" is
worthless alone — it reads green if the fixture never reached the memory branch at
all. The positive control moves ONE number in the same fixture, which is what makes
the silence mean what it says.

MUTATION-VERIFIED, and this is the AC's real requirement ("the test fails against
today's code") discharged at fact level rather than percent level: reverting the
denominator to the container limit (`calculateDockerMemoryPercent(sample, null)`)
fails the positive control at 1 failed / 9 passed, while the negative control stays
green — correct, since 400 MiB is under both thresholds either way. The edit was
applied with an occurrence-count assertion rather than a regex substitution,
because a silent no-op mutation exits 0 and would have let me publish a vacuous
guard as a verified one.

ADR-0025 AMENDMENT. §2.4's `memory %` was a ratio whose denominator the ADR never
named, so the implementation picked the container limit — right for a store, whose
footprint IS resident data, wrong for a Node service that aborts at its V8 heap
limit. Amendment lands as a §2.4 bullet plus the `Amended` row line, in the #16462
additive style the ticket named as precedent: no new detect signal, no new fact
type, no new recovery class, no new privilege, no action-set change, AC-1 through
AC-7 unchanged.

102 passed across the four affected suites.

Refs #16630"
- 2026-08-07T14:11:50Z @neo-opus-vega cross-referenced by #16636
- 2026-08-07T14:13:29Z @neo-opus-vega referenced in commit `f14cf61` - "test(orchestrator): the negative control asserts no fact at all, which is what the AC says (#16630)

The assertion filtered to memorySaturation while the AC says "produces no fact at
all" — a narrower guard that would pass while the sharper denominator quietly
emitted something else. Verified empirically rather than assumed: decision.facts is
genuinely empty for a service at 39% of its container limit and 52% of its declared
ceiling, so the stronger form holds and is now what ships. 10 passed.

Refs #16630"
- 2026-08-07T14:18:40Z @neo-opus-vega cross-referenced by #16566
- 2026-08-07T14:26:38Z @neo-fable-clio cross-referenced by #16637
- 2026-08-07T15:14:45Z @neo-opus-vega cross-referenced by PR #16640
- 2026-08-07T15:16:48Z @neo-opus-vega referenced in commit `c7f7010` - "test(deploy): the NODE_OPTIONS ban gets a positive control before its absence assertion (#16630)

Applying @neo-opus-grace's pattern from three of her own catches today: a guard
repaired at one site is not a guard, so sweep the file for its shape. Two of this
spec's assertions were rewritten to test properties instead of proxies, and a third
was left in the exact vacuity shape both of us keep hitting.

The NODE_OPTIONS ban is an ABSENCE assertion: match() returns null, `|| []` makes it
an empty array, toEqual([]) passes. If the pattern itself were wrong it would pass
just as green, forever, witnessing nothing — the same `[].every(...) === true` shape
Euclid found in this spec's predecessor and Grace found in her own invariant guard.

Now the matcher is proven to fire before its silence is trusted: it must match the
list form (- NODE_OPTIONS=...) and the mapping form (NODE_OPTIONS: ...), and it must
NOT match a comment naming NODE_OPTIONS — otherwise the guard would forbid its own
rationale and the next reader would delete the reason rather than the violation.

Also verified WHICH tests the escaping mutation reddens rather than only how many.
The four are the three per-service escaping assertions plus THE RENDERED ARTIFACT
test, so the render check is proven non-vacuous by name. Counting failures would
have left that unestablished while reading as though it were proven.

18 passed.

Refs #16630"
- 2026-08-07T15:19:50Z @neo-opus-vega cross-referenced by #16642
- 2026-08-07T15:20:27Z @neo-opus-vega added sub-issue #16642
- 2026-08-07T17:06:50Z @tobiu referenced in commit `257d961` - "Declare the MCP servers' V8 heap ceilings, guarded by the escaping that breaks (#16630) (#16640)

* fix(deploy): declare the MCP servers' V8 heap ceilings, guarded by the escaping that breaks (#16630)

Slice A of #16630 as amended after PR #16634 was closed unmerged on @neo-gpt's
Drop+Supersede. Configuration and rendering only: NO diagnosis change, no
denominator, no ADR amendment. The dropped half divided cgroup usage by a V8
old-space cap and called the result heap saturation; those are different scopes and
no denominator repairs that.

mc-server self-aborted at 2026-08-07T11:40:42Z with "Ineffective mark-compacts near
heap limit" while its container sat at 36% of a 1 GiB budget. No ceiling was
declared, so V8 chose a heuristic ~560 MiB and killed the process with ~460 MiB of
the container's own allowance unused. Because NODE aborted rather than the
container, ExitCode was 0, OOMKilled false and health healthy, so nothing surfaced
it. kb-server carries the identical shape and has only been spared by corpus size.

Both MCP servers now declare a command:-scoped --max-old-space-size at 768 MB under
the 1g limit. NOT NODE_OPTIONS: an env var is inherited by every child and silently
multiplies the container budget, which this file already rejects at :382.
mc-server needs it in BOTH branches of its conditional overlay command.

THE GUARD IS THE POINT OF THIS VERSION. PR #16634's spec asserted
command.includes('SERVER_ENTRYPOINT') and its body called that a rendered-command
test. Euclid falsified it: that substring is true under both the correct $$ and the
broken single-$ escaping, so the assertion could never distinguish them. It also
asserted ceiling COUNT and never ceiling EQUALITY. Both holes are now closed by
asserting the properties directly instead of proxies for them:

- the escaping itself: $$SERVER_ENTRYPOINT present, and a negative lookbehind
  forbidding any single-$ form, so a command mixing both cannot average out to green
- equality across branches via a Set of declared values, size 1
- strictly below the container limit
- NODE_OPTIONS never used
- the RENDERED artifact from `docker compose config` output, which is the only
  assertion whose input is Compose's own interpolation

MUTATION-PROVEN, both of Euclid's falsifiers:
- 6 x $$ -> $  =>  4 failed / 14 passed (was 14/14 green on the old spec)
- mc-server branches 768 vs 256  =>  red on the equality assertion (was green)

The render check corrected one of my own assumptions on its first run: `docker
compose config` does NOT print the post-interpolation command, it round-trips the
compose-canonical form, so a CORRECT file renders "$$SERVER_ENTRYPOINT" with the
escape intact. The single-$ form collapses to "" during that pass. So the
empty-script match is the discriminator and the escape-present assertion is its
positive control. The test went red against my first guess and the comment now
records what the renderer actually does.

The guard covers THREE Node services including orchestrator, whose exposure is
pre-existing: it has declared its ceiling since #16459 and the same mutation renders
`node --max-old-space-size=6144 ""` for it on canonical dev today. A two-service
guard would have left the original instance unguarded.

18 passed.

Refs #16630

* test(deploy): the NODE_OPTIONS ban gets a positive control before its absence assertion (#16630)

Applying @neo-opus-grace's pattern from three of her own catches today: a guard
repaired at one site is not a guard, so sweep the file for its shape. Two of this
spec's assertions were rewritten to test properties instead of proxies, and a third
was left in the exact vacuity shape both of us keep hitting.

The NODE_OPTIONS ban is an ABSENCE assertion: match() returns null, `|| []` makes it
an empty array, toEqual([]) passes. If the pattern itself were wrong it would pass
just as green, forever, witnessing nothing — the same `[].every(...) === true` shape
Euclid found in this spec's predecessor and Grace found in her own invariant guard.

Now the matcher is proven to fire before its silence is trusted: it must match the
list form (- NODE_OPTIONS=...) and the mapping form (NODE_OPTIONS: ...), and it must
NOT match a comment naming NODE_OPTIONS — otherwise the guard would forbid its own
rationale and the next reader would delete the reason rather than the violation.

Also verified WHICH tests the escaping mutation reddens rather than only how many.
The four are the three per-service escaping assertions plus THE RENDERED ARTIFACT
test, so the render check is proven non-vacuous by name. Counting failures would
have left that unestablished while reading as though it were proven.

18 passed.

Refs #16630

* fix(test): the render oracle separates availability from validity, and witnesses a rejection (#16642)

@neo-gpt's sole RA, and he is right. The oracle wrapped `docker compose config` in a
bare catch that labelled EVERY non-zero exit "docker unavailable", so a genuinely
invalid Compose file SKIPPED instead of failing. His falsifier: with Docker present
and a Compose-invalid service key, compose exits 1 while Playwright exits 0 with
17 passed / 1 skipped. The oracle failed open.

This is the class I spent the day correcting elsewhere and then wrote into my own
guard: a check routed through something that swallows the signal. A guard that
cannot distinguish "cannot run" from "ran and said no" is not a guard.

AVAILABILITY IS NOW A SEPARATE PROBE. composeCliAvailable() runs `docker compose
version`, which touches no project file and therefore cannot fail for a reason
belonging to the compose document — the one probe whose failure genuinely means
"cannot run". runComposeConfig() only answers what Compose said, and a non-zero exit
from it now THROWS with the captured stderr.

NEGATIVE WITNESS ADDED, per the RA. Before trusting the positive result the test
runs `config` against a file Compose is guaranteed to reject (`services.bad` as a
scalar) and asserts the rejection is reported. Without it the fail-open bug becomes
invisible again the moment anyone reintroduces a broad catch — the same reason the
NODE_OPTIONS matcher carries a positive control.

MUTATION-PROVEN with his exact shape, and getting the shape right took two attempts
worth recording. My first mutation added a second `depends_on` to kb-server, which
made the file YAML-INVALID — js-yaml threw at module load and Playwright reported
"No tests found", which is not his falsifier and would have proven nothing. The
correct mutation repoints kb-server's EXISTING depends_on at a nonexistent service:
verified YAML-valid (js-yaml parses OK) and Compose-invalid (rc=1). Under it the
suite is now 1 failed / 17 passed on THE RENDERED ARTIFACT, where before it was
17 passed / 1 skipped.

18 passed unmutated.

Refs #16642"

