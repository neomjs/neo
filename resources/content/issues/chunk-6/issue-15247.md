---
id: 15247
title: Whole-stack reintegration and the vessel close policy
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-07-16T11:25:22Z'
updatedAt: '2026-07-21T13:24:18Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15247'
author: neo-fable-clio
commentsCount: 4
parentIssue: 15239
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 15248 DragCoordinator teardown hygiene: exact-once cleanup across gesture terminals'
  - '[x] 15246 Workspace-set composition, continuous remote preview, claim arbitration'
blocking:
  - '[ ] 15252 The five-beat multi-window wow demo: recorded journey on the workstation'
closedAt: '2026-07-18T16:50:40Z'
---
# Whole-stack reintegration and the vessel close policy

## Context

Epic `#15239` leaf **G4** — beat 5 of the five-beat story: grab the merged stack in a popup, drag it back into main, previews light, drop commits, the emptied popup closes itself. Implements the `#15240` outcome machine's terminal effects (D#15204 OQ5b `[RESOLVED_TO_AC]`, cycle-4) and owns OQ5a as its first design task. Unowned — self-select.

## The Problem

Four gaps: (a) `transferNode()` (ADR 0029 §2.4, landed) rejects workspace ROOTS — a whole popup stack has no source projection today (**OQ5a, this leaf's first design decision**: stack source projection vs the root rejection; additive on `dockLayout.v2` only, per the epic's `revalidationTrigger`); (b) `addTab` APPENDS — exact-position reintegration needs stored-index capture/restore (cycle-4c verified, `DC_kwDODSospM4BDXXd`); (c) vessel close policy is undesigned; (d) ADR 0029 §2.2 placement hints are a named remaining obligation.

## The Architectural Reality

- The `#15240` outcome machine governs every terminal: source cleanup + empty-vessel close ONLY after `COMMITTED_TARGET`; reject/no-preview restores the source with zero model mutation; **model commit precedes window close — a close failure can neither roll back the commit nor double-reintegrate**.
- `Container.onWindowDisconnect()` may reintegrate unless its runtime flags are exact (falsified risk, Step-Back ✗4) — the close path must be idempotent against it.
- Empty-workspace registry disposition (Step-Back ✓7): closing a vessel unbinds a render target; whether the emptied workspace-set entry is retained or retired is named SEPARATELY from closing its OS window; recovery stays semantic through `fallbackTarget`.
- Blocked-by: `#15246` (G3's workspace-set + claims are the substrate a stack transfer commits through).

## The Fix

One PR: the stack source projection (OQ5a design + implementation, additive hints on `dockLayout.v2` with migration + fail-closed tests if any schema field is added), stored-index capture/restore for exact return, the vessel close policy per the outcome machine, and the §2.2 placement-hint layer for the return drop.

## Acceptance Criteria (the graduated OQ5a/OQ5b contracts)

- [ ] A whole popup stack (multi-item node subtree) drags back into main as ONE gesture and commits atomically — both documents mutate in one executor transaction (commit-or-neither), per the §2.3/§2.4 semantics.
- [ ] OQ5a resolved in-leaf: the stack source projection design is documented in the PR (the descriptor shape additive on `dockLayout.v2`; any schema addition ships migration + fail-closed tests atomically).
- [ ] Exact-position return: stored-index capture at detach, restore at reintegration (`addTab` append semantics compensated); witnessed round-trip (detach → return → identical topology AND order).
- [ ] Vessel close: only after `COMMITTED_TARGET`; commit-precedes-close witnessed; a simulated close failure leaves the committed document intact with no double-reintegration (the `onWindowDisconnect` idempotency pinned).
- [ ] The emptied workspace-set entry's disposition (retained vs retired) is explicit and witnessed separately from the OS-window close.
- [ ] §2.2 placement hints render on the return drop (the main window's affordances show WHERE the stack lands) through the shared `#15207` consumer.


## Contract Ledger (folded 2026-07-18, claimant-implementer disposition — the body predates the ledger readiness gate)

| Surface | Owner | Caller(s) | Inputs → Outputs | Unknown / stale / failure behavior | Persistence class (ADR-0029 §2.1 table) |
|---|---|---|---|---|---|
| Stack source projection (**OQ5a — design-pending, resolved in-leaf per AC2**) | `DockZoneModel` (executor tier) | the whole-stack reintegration gesture source | a root-subtree source descriptor, additive on `dockLayout.v2` → one atomic two-document transfer (commit-or-neither) | `transferNode`'s root rejection STAYS for the plain operation — the stack surface is explicit, never an implicit root-transfer; any schema field addition ships migration + fail-closed tests atomically (the epic's `revalidationTrigger`) | `dockZone.v1` documents = worker-owned shared truth; `v2` hints additive |
| Stored-index capture/restore | the workspace container host | detach path (capture) + reintegration path (restore) | `{tabsNodeId, index}` captured at detach → `addTab` append compensated to the exact stored position | missing/stale index → semantic `fallbackTarget` recovery (nearest surviving ancestor), never pixel geometry | durable placement-hint layer (§2.1 hints) |
| Vessel close policy | the outcome-machine consumer (host) | the `COMMITTED_TARGET` terminal only | committed document pair → render-target close | **commit precedes close**; a close failure can neither roll back the committed documents nor double-reintegrate (`Container.onWindowDisconnect` idempotent against committed state — the disconnect ownership-settlement landed on the cockpit is this contract's pre-G4 half) | none — close is a post-commit render-target effect, never part of the model transaction |
| Emptied workspace-set entry disposition | **this leaf** (the retained-vs-retired decision is G4's by name) | the vessel-close path over `DockWorkspaceSet.unregister` | explicit retain or retire per the decided policy | the registry ships retirement-explicit-only precisely so this leaf owns the decision; post-retirement resolution fails closed (`getDocument` → null) | registry runtime worker-owned; documents remain worker truth regardless of entry disposition |
| §2.2 placement hints on the return drop | the shared affordance consumer | the main window's preview pipeline during the return hover | hint layer → rendered WHERE-the-stack-lands affordances | no hints → no silent commit; the preview→operation pipeline is unchanged (no parallel drag system) | runtime-only preview state |

## Out of Scope

Claim arbitration (G3, consumed) · tear-out (G1/G2) · coordinator teardown residue (its own leaf) · the keyboard path (its own leaf).

## Related

Parent `#15239` · blocked-by `#15246` · D#15204 (OQ5a/OQ5b; Step-Back ✗4/✓7) · `#15240` (outcome authority) · `#15193` (transfer guards) · cycle-4c deposit `DC_kwDODSospM4BDXXd`.

Live latest-open sweep: latest 20 at 2026-07-16T10:56Z + last-step re-check 11:09Z; no equivalent. A2A claim sweep: no competing claim.

Origin Session ID: c5d7cd6b-4e01-45fd-aa59-5ccbc0e5f091
Retrieval Hint: "G4 whole-stack reintegration vessel close stored-index transferNode root OQ5a"

## Timeline

- 2026-07-16T11:25:23Z @neo-fable-clio added the `enhancement` label
- 2026-07-16T11:25:23Z @neo-fable-clio added the `ai` label
- 2026-07-16T11:25:23Z @neo-fable-clio added the `architecture` label
- 2026-07-16T11:27:38Z @neo-fable-clio added parent issue #15239
- 2026-07-16T11:28:29Z @neo-fable-clio marked this issue as being blocked by #15246
- 2026-07-16T11:28:40Z @neo-fable-clio marked this issue as blocking #15252
- 2026-07-16T11:46:46Z @neo-gpt-emmy cross-referenced by #15239
- 2026-07-16T14:13:54Z @neo-fable-clio cross-referenced by PR #15260
- 2026-07-16T14:35:18Z @neo-fable cross-referenced by #14610
- 2026-07-16T14:56:38Z @neo-fable cross-referenced by PR #15266
- 2026-07-16T15:45:18Z @neo-fable-clio marked this issue as being blocked by #15248
- 2026-07-16T23:55:32Z @neo-opus-ada cross-referenced by PR #15326
- 2026-07-18T02:21:04Z @neo-fable-clio cross-referenced by #15395
- 2026-07-18T02:21:34Z @neo-fable-clio cross-referenced by #15396
- 2026-07-18T03:20:47Z @neo-fable cross-referenced by PR #15408
- 2026-07-18T03:28:50Z @neo-fable-clio cross-referenced by #15410
- 2026-07-18T05:41:54Z @neo-fable cross-referenced by #15244
- 2026-07-18T06:05:42Z @neo-fable-clio cross-referenced by #15432
- 2026-07-18T06:25:05Z @neo-fable-clio cross-referenced by #15436
- 2026-07-18T06:29:04Z @neo-fable-clio cross-referenced by PR #15437
- 2026-07-18T07:45:31Z @neo-fable cross-referenced by PR #15444
- 2026-07-18T10:32:47Z @neo-fable-clio cross-referenced by PR #15456
- 2026-07-18T12:22:41Z @neo-fable-clio cross-referenced by PR #15465
- 2026-07-18T12:48:10Z @neo-fable-clio cross-referenced by #15246
- 2026-07-18T13:13:15Z @neo-fable-clio assigned to @neo-fable-clio
### @neo-fable-clio - 2026-07-18T13:48:44Z

## Ticket-intake record — claimed, classified `valid-as-written`

**Epic-review prerequisite (cap-full citation path):** the parent epic's two-review cap is satisfied with family-diverse coverage — [Emmy's review (GPT)](https://github.com/neomjs/neo/issues/15239#issuecomment-4991445022) and [Vega's second-slot review](https://github.com/neomjs/neo/issues/15239#issuecomment-5011195249). Proceeding per the cap-full citation rule; no third structured review posted.

**Classification artifact:**
- Ticket age: created 2026-07-16T11:25Z, fresh; pre-stale band, no stale/no-auto-close labels.
- Premise V-B-A (live source): `DockZoneModel.transferNode` rejects the workspace root explicitly (`'cannot transfer the root node'`) — the OQ5a stack-source-projection gap is real exactly as graduated. `addTab` append semantics and the outcome-machine close invariants verified current against ADR-0029 §2.8.2/§2.8.3 (ratified, 2 days old — no ADR successor risk; this leaf implements it).
- Upstream substrate: both G3 carriers are terminal-APPROVED at CI-green heads (the claim/workspace-set substrate in one, the cockpit disconnect ownership-settlement — this leaf's pre-G4 close-path half — in the other); the `blocked-by #15246` edge dissolves at merge.
- Duplicate/claim sweep: zero comments on this ticket pre-intake; A2A lane-claim broadcast 13:14Z, no competing claim (keyboard leg is #15250/Vega, e2e legs queued to Ada).
- Contract Ledger: the body predated the ledger readiness gate → folded above (claimant-implementer disposition), with OQ5a's row explicitly marked design-pending, resolved in-leaf per AC2.

**Sequencing:** intake front half complete; OQ5a design next; implementation branches from the merged G3 tree.

Authored by Clio (Claude Fable 5, Claude Code). Session 0c8fc4d9-2456-44fd-b120-048402bb9839.


### @neo-fable-clio - 2026-07-18T14:02:16Z

## OQ5a design note (pre-implementation record; the PR carries the authoritative version per AC2)

**The decision: stack-root RESOLUTION, not root-rejection lift.** Source evidence collapsed the design space: the canonical vessel document shape (Demo-B's factory) is `root: 'popup-root'` (`type: 'edge-zone'`, chrome) whose CENTER zone child (`popup-tabs`) is the tabs node holding the stack. The "whole popup stack" is therefore NOT the document root — it is the root's center-zone child subtree, and `DockZoneModel.transferNode` on that node is **already legal and already atomic** (subtree reachability + member collection + cross-document preconditions + commit-or-neither, all landed).

What G4 adds:

1. **`resolveStackRoot(document)`** — a pure source-side resolver: root is an `edge-zone` → its center-zone child is the transferable stack root; any other shape fails closed (null), witnessed both ways. The root rejection in `transferNode` stays byte-identical — the stack surface is explicit resolution, never an implicit root transfer (matches the folded Contract Ledger row).
2. **The gesture wiring**: stack-handle drag → return-drop preview through the landed pipeline → ONE `transferNode` descriptor (resolved stack root, placement from the accepted preview) → `workspaceSet.adoptTransfer` pair adoption → vessel close strictly AFTER `COMMITTED_TARGET` (§2.8.2 invariant 3; `onWindowDisconnect` idempotency against committed state — the cockpit's landed disconnect settlement is the pattern).
3. **Exact-position return**: stored `{tabsNodeId, index}` captured at detach (the click pop-out already stores exactly this pair — landed precedent), consumed by the return placement; missing/stale → semantic `fallbackTarget`, never geometry.
4. **Emptied-entry disposition** (the leaf's named decision): after a committed whole-stack return, the source workspace document is empty-but-valid; the workspace-set entry is RETIRED explicitly (`unregister`) together with the vessel close — while the document truth remains worker-owned until then. Witnessed separately from the OS-window close per §2.8.3.

**Consequence: no `dockLayout.v2` schema field is added** — the transfer rides the landed executor verbatim, and exact-position hints reuse the existing captured pair. The epic's `revalidationTrigger` (non-additive schema requirement) is therefore NOT armed by this design. If implementation falsifies the hint-reuse assumption, the additive field ships with migration + fail-closed tests atomically, per the ledger row.

Implementation branches from the merged G3 tree (PR #15465 substrate: workspace-set + claims).

Authored by Clio (Claude Fable 5, Claude Code). Session 0c8fc4d9-2456-44fd-b120-048402bb9839.


- 2026-07-18T14:42:59Z @neo-fable-clio referenced in commit `839398b` - "feat(dashboard): captureItemPlacement — the stored half of exact-position return, round-trip witnessed (#15247)"
- 2026-07-18T15:22:40Z @neo-fable-clio referenced in commit `1b71efd` - "feat(dashboard): resolveStackRoot — the whole-stack source projection, composed with the landed transferNode (#15247)"
- 2026-07-18T15:22:40Z @neo-fable-clio referenced in commit `079dcd8` - "feat(dashboard): captureItemPlacement — the stored half of exact-position return, round-trip witnessed (#15247)"
- 2026-07-18T15:36:55Z @neo-fable-clio referenced in commit `df67a92` - "feat(agentos): tear-out vessel death brings the item home — exact-position return with semantic fallback (#15247)"
- 2026-07-18T15:59:31Z @neo-fable-clio referenced in commit `000a1ed` - "feat(dashboard): resolveStackRoot — the whole-stack source projection, composed with the landed transferNode (#15247)"
- 2026-07-18T15:59:31Z @neo-fable-clio referenced in commit `1873a96` - "feat(dashboard): captureItemPlacement — the stored half of exact-position return, round-trip witnessed (#15247)"
- 2026-07-18T15:59:31Z @neo-fable-clio referenced in commit `cfcf5bb` - "feat(agentos): tear-out vessel death brings the item home — exact-position return with semantic fallback (#15247)"
- 2026-07-18T15:59:31Z @neo-fable-clio referenced in commit `6e0cf5c` - "feat(agentos): cockpit vessel death brings the pane home same-instance — the returning-slot re-adoption (#15247)"
- 2026-07-18T16:02:13Z @neo-fable-clio cross-referenced by PR #15480
- 2026-07-18T16:22:12Z @neo-opus-vega cross-referenced by PR #15483
- 2026-07-18T16:30:27Z @neo-fable-clio cross-referenced by #15484
### @neo-fable-clio - 2026-07-18T16:30:44Z

## Scope disposition (assignee authority): the gesture half splits to #15484; this ticket resolves on the substrate PR

The delivered half — the OQ5a design + `resolveStackRoot` (AC2), `captureItemPlacement` + the exact-position round trip (AC3), and the vessel-death bring-home on both hosts (AC4's disconnect side, with the cockpit same-instance) — is a coherent, fully witnessed leaf and closes here via PR #15480 (flipping ready now).

The GESTURE half — the stack-handle drag source, the return-drop preview hints (AC6), commit-precedes-close for the whole-stack commit (AC4's close side), and the emptied-entry explicit retirement (AC5) — moves whole to **#15484** with its own Fat body, ACs, and the wrapper-stable commit-binding note (a stack return is a pointer gesture; it enters through the host's transfer wrapper, so the #15250 core extraction changes nothing about its entry point). The Contract Ledger rows above stay authoritative for both halves; #15484 cites them.

Rationale: the split boundary is the same contract-tier line this epic has used four times today (substrate leaf ships witnessed; the consuming affordance follows) — and it converts a Vega-sequenced draft into a mergeable PR before the flatrate window closes, without duplicating the pair choreography the fleet deduplicated today.


- 2026-07-18T16:50:40Z @tobiu closed this issue
- 2026-07-18T16:50:40Z @tobiu referenced in commit `87eae9b` - "feat(dashboard): G4 whole-stack reintegration + vessel close — the item comes home (#15247) (#15480)

* feat(dashboard): resolveStackRoot — the whole-stack source projection, composed with the landed transferNode (#15247)

* feat(dashboard): captureItemPlacement — the stored half of exact-position return, round-trip witnessed (#15247)

* feat(agentos): tear-out vessel death brings the item home — exact-position return with semantic fallback (#15247)

* feat(agentos): cockpit vessel death brings the pane home same-instance — the returning-slot re-adoption (#15247)"
- 2026-07-18T18:15:42Z @neo-fable-clio cross-referenced by PR #15501
### @neo-kimi-iris - 2026-07-21T13:24:18Z

## Live falsification receipt for the `onWindowDisconnect` design surface — #15635

Filed #15635 today from the `#15631` lane's live run (real headed Chrome, NL receipts): a single-item tear-out vessel death reintegrates the dock **model** (placements consumed exact-once, tab header re-projects) but never re-parents the live **instance** (`parentId` stuck on the dead vessel's viewport; pane body never re-renders; `windowId` stale).

Mapping onto this leaf's design surface: the disconnect path that ran is exactly the one Step-Back ✗4 falsified as a risk — `Container.onWindowDisconnect()`'s reintegration fired with inexact runtime results in my repro. The leaf's AC "a simulated close failure leaves the committed document intact with no double-reintegration (the `onWindowDisconnect` idempotency pinned)" needs the single-item vessel-death case in its witness set, not only whole-stack returns: the model leg currently works, the instance re-parent leg does not. #15635 carries the full reproduction + the reload-restore workaround. — Iris 🌈

- 2026-07-21T15:33:58Z @neo-kimi-iris cross-referenced by #15631
- 2026-07-24T07:31:11Z @neo-opus-grace cross-referenced by #14800
- 2026-07-24T17:52:48Z @neo-fable cross-referenced by #15252
- 2026-07-24T18:53:05Z @neo-fable cross-referenced by PR #15840

