---
number: 14299
title: 'fix(ai): quarantine over-cap recovery rows (#14253)'
author: neo-gpt
state: OPEN
createdAt: '2026-06-28T19:33:04Z'
updatedAt: '2026-06-28T19:41:26Z'
closedAt: null
mergedAt: null
head: codex/14088-14253-corruption-recovery-gate
base: dev
url: 'https://github.com/neomjs/neo/pull/14299'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #14088
Resolves #14253
Related: #14039

Routes over-context data-integrity evidence away from the generic WAL-stall re-embed path. The classifier now recognizes `embeddingContextExceededCount`, the evidence assembler can carry `embedding-context-exceeded`, and the release gate pins both decisions: drain-stall is shape-equivalent to the existing provenance-blind coverage gap at the recovery layer, while over-cap rows quarantine instead of re-running `re-embed-missing`.

Evidence: L2 (focused Playwright unit/release-gate suite with isolated SQLite corruption injection, classifier/assembler/runner coverage, and mock actuator quarantine) -> L2 required (both close-target AC surfaces are unit-harness verifiable). No residuals.

## Deltas from ticket
- `#14088`: chose shape-equivalence for the recovery gate. Once a stalled deferred-embed drain leaves persisted rows without vectors, the coverage audit deliberately cannot distinguish provenance; the existing wal-stall gate is the recovery proof. A distinct drain-queue liveness signal remains prevention/early-warning scope, not a second recovery injector.
- `#14253`: resolves the heal-mode decision in code as `over-cap` -> `quarantine` for v13.1. No truncate-then-embed mutation was added; ADR-0027 makes quarantine the reversible safe-default when a row is not losslessly recoverable.

## Test Evidence
- `git diff --check`
- `npm run test-unit -- test/playwright/unit/ai/daemons/orchestrator/services/dataIntegrityModeClassifier.spec.mjs test/playwright/unit/ai/daemons/orchestrator/services/dataIntegrityEvidenceAssembler.spec.mjs test/playwright/unit/ai/daemons/orchestrator/services/DataIntegrityDiagnosisService.spec.mjs test/playwright/unit/ai/scripts/maintenance/CorruptionRecoveryGate.spec.mjs` -> 45 passed (31.2s)

## Post-Merge Validation
- [ ] Run the orchestrator data-integrity sweep in an environment with a live over-context fixture once a producer emits `embedding-context-exceeded` facts.

## Commits
- `b8d101ae75` — route over-cap data-integrity evidence to quarantine and pin the paired gate.

Authored by Euclid (GPT-5, Codex Desktop). Session current Codex Desktop thread.
