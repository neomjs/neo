---
id: 17150
title: Star-based DevIndex opt-in is dead by GitHub policy and blocks the issue path
state: OPEN
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-08-15T07:53:31Z'
updatedAt: '2026-08-15T09:04:39Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17150'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Star-based DevIndex opt-in is dead by GitHub policy and blocks the issue path

## Context

GitHub restricted the stargazers endpoint to repository **admins and collaborators** ([changelog, 2026-06-30](https://github.blog/changelog/2026-06-30-upcoming-access-restrictions-to-public-api-endpoints-and-ui-views/), titled "Upcoming", no published enforcement date). The rollout reached our `intake` App between `12:05:23Z` and `13:19:19Z` on 2026-08-14 — the last-green → first-red boundary of `#17131`.

Verified live, and it tracks collaborator status rather than repository visibility:

| probe | `neomjs/neo` (collaborator) | `devindex-opt-in` (`pull` only) | `devindex-opt-out` (`pull` only) |
|---|---|---|---|
| REST `/stargazers` | 200, 3 rows | **404** | **404** |
| GraphQL `stargazers` | returns edges | `edges: []` | — |
| REST repo root | 200 | 200 (`stargazers_count: 16`) | 200 |

Under the `intake` App token the GraphQL connection answers `Resource not accessible by integration`. This is permanent and by design — not an outage that clears.

`#17148` (PR #17149) fixes the *blast radius*: an intake denial no longer freezes corpus publication. It deliberately does **not** touch DevIndex behaviour, which is this ticket.

## The Problem

`OptIn.run()` has two independent intake mechanisms (`apps/devindex/services/OptIn.mjs`):

1. **Stargazers** — `// 1. Check Stargazers`, the paged `stargazers` GraphQL loop at `:49-113`. **Dead by policy.**
2. **Issues** — `// 2. Check Issues` → `processIssues()` at `:114`, the issue-template opt-in. **Still works.**

The star loop runs first and throws, so `processIssues()` is never reached. A permanently-dead half is taking the working half down with it: DevIndex opt-in is currently 100% unavailable when it could be ~50% available. The last green run shows both paths executing (`[OptIn] Checking for new opt-in requests...` then `[OptIn] Checking for new opt-in issue requests...`), so this is a regression in reachability, not a missing feature.

Note the asymmetry that hid this: `OptIn.mjs:78` already skips gracefully on `NOT_FOUND` / `Could not resolve`. A non-collaborator REST call returns 404 (which that branch would absorb), but the GraphQL connection returns `Resource not accessible by integration` — which is not in the branch, so it throws.

## The Architectural Reality

- `apps/devindex/services/OptIn.mjs:49-113` — the stargazer loop; `:76` the throwing call; `:77-83` the existing NOT_FOUND skip.
- `apps/devindex/services/OptIn.mjs:114` — `processIssues()`, unreachable behind it.
- `apps/devindex/services/GitHub.mjs:658` — where the GraphQL error is raised.
- `buildScripts/dataSyncPreflight.mjs:33-36` — `REQUIRED_REPOSITORIES` still declares `connection: 'stargazers'` for `devindex-opt-in` (added by `#17148`). If the star path is retired, that entry's connection must change with it or the preflight will deny forever on a read nothing performs.

## The Fix

**DECIDED 2026-08-15 by @tobiu: option B — restore access. Then FALSIFIED the same hour by direct experiment; option A is the remaining shape.**

@tobiu granted `administration: read` (and `organization administration: read`) on the `neomjs-devindex-intake` App. I listed `permission-administration: read` on the mint step and dispatched a `preflight_only` run — [run 31874111382](https://github.com/neomjs/neo/actions/runs/31874111382), side-effect-free by construction:

```
permission-administration: read          <- accepted by the mint step
[DataSync preflight] neomjs/devindex-opt-in attempt 1/3 failed
  (Resource not accessible by integration); retrying
[DataSync preflight] neomjs/devindex-opt-in DENIED (OptIn stargazer read)
[DataSync preflight] neomjs/devindex-opt-out reachable (OptOut issue read + close)
```

**The mint SUCCEEDING is what makes this conclusive.** `create-github-app-token` fails outright when asked for a permission the installation does not hold, so a successful mint proves the App genuinely held `administration: read` at probe time. Held, requested, minted — and the stargazer read was still denied, while the `issues` connection on the sibling repository stayed reachable in the same run. This is not "the grant did not take".

So the restriction is on **account status**, exactly as GitHub's wording says, and no App permission satisfies it. The workflow change was reverted rather than shipped (it would widen the intake identity for nothing), and the App grants should be revoked for the same reason.

**Option B is closed. Option A below is the work.**

**B (CHOSEN). Restore access.** Grant the `intake` App collaborator/admin-equivalent access on `neomjs/devindex-opt-in` and `neomjs/devindex-opt-out`.

> **The grant alone will NOT reach the pipeline — there are TWO places, and both must agree.**
> `.github/workflows/data-sync-pipeline.yml:96-97` mints the intake token with an EXPLICIT permission list:
> ```yaml
> permission-issues: write
> permission-metadata: read
> ```
> `actions/create-github-app-token@v3` mints exactly what is requested — the workflow's own comment says so: *"without this, v3 mints a token carrying every permission the installation holds"*. That cap is deliberate (it keeps least-privilege reviewable in the diff), but it means **widening the App installation while leaving these lines unchanged produces a token that still lacks the new permission**, and the failure is bit-identical to no grant at all. Whatever permission unlocks the stargazer read must be added here in the same change.

**A (fallback). Retire the star path.** Delete the stargazer loop, keep issue-based opt-in as the sole mechanism, set `REQUIRED_REPOSITORIES`' connection for `devindex-opt-in` to `issues`, and update any doc still saying "star the repo to opt in".

**Verification.** Note that the *current* preflight cannot confirm either outcome: it probes `repository{id}`, which resolves from metadata and passes whether or not stargazers is readable. `#17148` / PR #17149 is what makes the grant verifiable — it selects the connection the entry names and requires it to resolve. With that merged, `DATA_SYNC_PREFLIGHT_ONLY=true` verifies the credential topology with **no side effects** (it skips the collection stages, so OptOut comments on and closes nothing), which is the cheap iteration loop for getting the grant right.

Either way the star loop must stop being fatal to `processIssues()`.

## Acceptance Criteria

- [ ] A denied stargazer read no longer prevents `processIssues()` from running; issue-based opt-in works while the star path is unavailable.
- [ ] The unavailability is surfaced (logged//counted), not silently swallowed — a carve-out that quiets the guard would let the whole intake rot undetected.
- [ ] `REQUIRED_REPOSITORIES`' declared `connection` for `devindex-opt-in` matches whatever read the code actually performs after the decision.
- [ ] If the star path is retired, no user-facing doc still instructs "star to opt in".
- [ ] Unit coverage: stargazer denial ⇒ issue path still runs and its opt-ins are recorded.

## Out of Scope

- The pipeline blast-radius fix — `#17148` / PR #17149.
- The GitHub-side restriction itself; it is not appealable by us.

## Avoided Traps

- **Adding the denial string to the `NOT_FOUND` skip branch and calling it done.** One line, turns everything green, and converts a permanent capability loss into a silent no-op — the opt-in feature would rot with nobody noticing. The loss must be visible.
- **Assuming the whole opt-in feature is dead.** Only the star half is; `processIssues()` is intact and was working in the last green run.

## Related

- `#17148` / PR #17149 — the pipeline-side blast-radius fix that keeps the corpus publishing.
- `#17131` — the standing alarm, which stays red until this or a credential change resolves the intake.
- `#15744` — the two-identity least-privilege split that option B would reverse.

Retrieval Hint: "GitHub stargazers restricted to collaborators; DevIndex star opt-in dead, issue path still works"




## Timeline

- 2026-08-15T08:09:11Z @neo-gpt cross-referenced by PR #17149
- 2026-08-15T08:32:32Z @neo-fable-clio cross-referenced by #17153
### @neo-opus-ada - 2026-08-15T09:04:39Z

## Option B is VIABLE after all — `contents: write` is the collaborator signal

My earlier "no App permission satisfies it" was an over-generalisation from a single negative test, and @tobiu was right to push on it. GitHub's docs list **App installation access tokens** as supported for this endpoint, which should have stopped that claim. Four controlled probes, same App, same installation, one variable each — all `preflight_only`, all side-effect-free:

| # | requested intake permissions | `devindex-opt-in.stargazers` | run |
|---|---|---|---|
| 1 | `issues:write`, `metadata:read` (shipped) | **DENIED** | 31870344604 |
| 2 | + `administration:read` | **DENIED** | 31874111382 |
| 3 | + `contents:**write**` | **reachable** ✅ | 31875177082 |
| 4 | + `contents:**read**` | **DENIED** | 31875769230 |

`devindex-opt-out.issues` stayed reachable in every run, so the token was healthy throughout — the stargazer connection is the only thing moving.

**Probes 3 and 4 together are the finding**: write is required, read does not suffice. That matches the reporting claim that contents-write is how GitHub infers collaborator status, and now we have it first-party rather than second-hand.

**These results are only trustworthy because #17148 merged first.** The old preflight probed `repository{id}`, which resolves from metadata and would have printed `reachable` in all four rows. The instrument had to be fixed before the experiment could mean anything.

## What `contents: write` actually exposes

Measured, not assumed:

| repo | contents | size |
|---|---|---|
| `neomjs/devindex-opt-in` | `.github/`, `.gitignore` (184 B), `README.md` (1631 B) | 3 KB |
| `neomjs/devindex-opt-out` | `.github/`, `.gitignore` (184 B), `README.md` (1030 B) | 1 KB |

No code, no data. The DevIndex corpus lives in `neomjs/neo` under `apps/devindex/resources/data/`, which the intake App **cannot reach at all** — it has no installation there. So the grant's blast radius is a README, a `.gitignore` and issue templates across two 4 KB repos, recoverable from git history.

The property `#15744` exists to protect — the intake identity cannot publish to the main repository — is untouched by this. What does change is the workflow's claim that this token *"must never be able to write code anywhere"*: that sentence becomes false and must be rewritten rather than silently contradicted.

## Recommendation

**Take option B.** Grant `contents: write` (already granted), add `permission-administration: read`-free `permission-contents: write` to the mint step, and rewrite the intake-token comment to state precisely what the grant is for and what it costs. It preserves one-click star opt-in — materially lower friction than "file an issue from a template" for a community-facing index — at a cost of write access to 4 KB of non-code.

Scope of this ticket accordingly shifts from *retire the star path* to *restore access + document the trade*. The AC about `REQUIRED_REPOSITORIES`' connection now resolves as "stays `stargazers`", and the AC about `processIssues()` being unreachable behind a throwing star loop **still stands** — that is a real robustness defect regardless of whether access is restored, and it is what made a single denial take down both intake mechanisms.

Awaiting @tobiu's call on the trade before implementing; the permission line is not shipped and the experiment branch is not a merge candidate.

- 2026-08-15T09:26:39Z @neo-opus-ada cross-referenced by PR #17160

