---
number: 16841
title: >-
  docs(ai): truth-fold resolveMemorySaturationScope's docblock to shipped
  behavior (#16840)
author: neo-opus-vega
state: OPEN
createdAt: '2026-08-09T22:58:29Z'
updatedAt: '2026-08-09T23:25:53Z'
closedAt: null
mergedAt: null
head: agent/16840-saturation-scope-jsdoc
base: dev
url: 'https://github.com/neomjs/neo/pull/16841'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Resolves #16840

`resolveMemorySaturationScope`'s implementation was repaired in `#16630` Slice B after @neo-gpt falsified the envelope's scope authority. **Its docblock was not, and still taught the defect.**

Eleven lines apart, in the same function:

```
docblock:  container — no envelope, or an envelope explicitly reporting `not-node`
inline:    nodeCommand === false is the ONLY thing that licenses the container ratio,
           and the envelope deliberately does not count
```

And `nodeCommand` — the destructured parameter deciding the entire outcome — appeared nowhere in the `@param` list.

Evidence: L2 (docs truth-fold; the behavioural spec is the control and is unchanged) → L2 required. Residual: none.

## Deltas from ticket

None. One addition beyond the prescription: the docblock now records **both** reasons the envelope was demoted rather than stating the rule bare — a constraint without its rationale gets re-litigated as arbitrary, and this one was already re-derived once by falsifier.

## Test Evidence

`npm run test-unit -- ai/daemons/orchestrator/services/ContainerHealthDiagnosisService.spec --workers=1` → **87 passed**, unchanged. That is the control, not a demonstration: a docs-only change that moved a behavioural test would mean the change was not docs-only.

## Post-Merge Validation

- [ ] None. The rendered doclet is the artifact and it is produced from the committed source.

## Deltas

None beyond the one noted above.

Authored by Vega (@neo-opus-vega, Claude Opus 5, Claude Code). Origin Session ID: 4131135d-1b20-487f-9d23-d7213914246b.


## Comments

### `@github-actions` commented on 2026-08-09T22:58:39Z

## 🚨 Agent PR Body Lint Violation

@neo-opus-vega — your PR body on PR #16841 [QUARANTINED_URL: github.com] does not match the pull-request template structure.

**Required action**: read `.agents/skills/pull-request/SKILL.md` BEFORE editing the PR body. The skill points at:
  - Minimum-viable PR body structure: `.agents/skills/pull-request/references/pull-request-workflow.md §9`
  - Self-Identification mandate: `.agents/skills/pull-request/references/pull-request-workflow.md §5`

Do NOT compose a substitute template or hallucinate section headings. The validator
checks more structural anchors than this comment names. The only reliable path to
passing is reading the actual workflow file and following its structure.

**Diagnostic hint**: at least one recognized anchor like `close-relation audit (#16829): 5 orphaned item(s). Sanctioned repairs: (a) tick the delivered ACs on the ticket with their receipts; (b) annotate genuinely post-merge criteria on the ticket as `[L<N>-deferred — …]`; (c) split an L2-delivered leaf (#16776 pattern) and `Resolves` the leaf. Findings: #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] The `container` bullet names `nodeCommand === false` as the sole licence and states explicitly that the envelope does not count, even when unanimous. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] `nodeCommand` is documented in the `@param` list. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] The docblock records **why** — retained-sample staleness, and consumer independence from the producer's `identity-unknown` distinction — so the rule is not re-litigated as an arbitrary constraint. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] No behaviour change: `ContainerHealthDiagnosisService.spec` stays green unchanged, asserted as the control. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] **Evidence level: L2.** A docs truth-fold closes on the unchanged spec plus the rendered doclet.` is missing.

<details>
<summary>Visible anchors missing (full list)</summary>

- `close-relation audit (#16829): 5 orphaned item(s). Sanctioned repairs: (a) tick the delivered ACs on the ticket with their receipts; (b) annotate genuinely post-merge criteria on the ticket as `[L<N>-deferred — …]`; (c) split an L2-delivered leaf (#16776 pattern) and `Resolves` the leaf. Findings: #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] The `container` bullet names `nodeCommand === false` as the sole licence and states explicitly that the envelope does not count, even when unanimous. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] `nodeCommand` is documented in the `@param` list. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] The docblock records **why** — retained-sample staleness, and consumer independence from the producer's `identity-unknown` distinction — so the rule is not re-litigated as an arbitrary constraint. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] No behaviour change: `ContainerHealthDiagnosisService.spec` stays green unchanged, asserted as the control. | #16840 carries an unmet, un-annotated acceptance criterion this merge would close over: - [ ] **Evidence level: L2.** A docs truth-fold closes on the unchanged spec plus the rendered doclet.`

</details>

<sub>This is the CI tool-boundary lint companion to PR #11494's MCP `manage_pr_review` validator and PR #11502's `agent-pr-review-body-lint.yml` reviewer-side lint.
Resolves #11501.</sub>

---

