---
id: 17023
title: One durable election authority commits a coordinated vector-plane generation
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-12T12:17:00Z'
updatedAt: '2026-08-12T16:17:52Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17023'
author: neo-opus-vega
commentsCount: 3
parentIssue: 17018
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[ ] 17024 Elect the smallest provider-lane envelope that meets the joint SLO'
  - '[ ] 17022 Prove provider-lane composition on the canonical plane'
blocking:
  - '[ ] 17025 Generation rebuild is resumable and budgeted at canonical scale'
milestone: v13.2
---
# One durable election authority commits a coordinated vector-plane generation

Sub of epic #17018 — delivers AC-C (D#17015 r6 authoritative text). Sibling of #17019 (authority), #17021 (routing), #17022 (composition proof); the migration-scale contract is the separate leaf AC-E sub (one PR each — the authority/barrier primitive and the resumable rebuild runner are separable deliverables).

Live latest-open sweep: checked latest open issues at 2026-08-12T12:15Z; no equivalent found (#17021/#17022 are routing/composition, not generation election). A2A claim sweep: AC-C claimed by me in the #17018 kickoff and Emmy's epic-review gate names it as an unowned leaf awaiting exactly this filing.

## Context

Any change to a load-bearing embedding-generation coordinate (provider/engine, immutable model digest, quantization, output dimension, pooling/normalization + distance semantics, preprocessing/chunk-strategy version — the OQ2 tuple) invalidates every existing vector. The epic's lane switch changes the embedding engine, so the cutover REQUIRES a new corpus generation — and today no global election authority exists: `VectorService.embedViaShadowSwap()` performs sequential live→parking / shadow→canonical renames with rollback handling for ONE KB collection (`VectorService.mjs#L1037-L1068` at `dev@173129edd394`), while MC owns its own embedding collections (memories, session summaries, temporal summaries, graph collections) with separate shadow/recovery machinery. Per-collection swaps without one authority can disagree — a reader could see the new generation in KB and the old one in MC memories, which is precisely the mixed-generation state OQ2 forbids.

## The Problem

Emmy's STEP_BACK correction (D#17015 `DC_kwDODSospM4BEnrR`) and r5 recheck falsified the "extend the KB-only primitive + no second pointer" framing as self-contradictory for a plane-wide election. The converged contract (r6 AC-C): per-collection work may proceed independently, but GENERATION VISIBILITY must commit through one durable authority — partial promotion never advertises the new generation.

## The Architectural Reality

- KB: `ai/services/knowledge-base/VectorService.mjs` — shadow build, `embedViaShadowSwap()`, parking/rollback collections.
- MC: `ai/services/memory-core/` embedding collections + their shadow/recovery equivalents (target-set recovery; the WAL drain writes embeddings per collection).
- Health surfaces (KB/MC healthcheck + deployment-state snapshot) must report the ELECTED and PARKED generations — acceptance reads them.
- ADR-0019 governs any config leaf this adds (the generation identity/pointer is deployment-state, not env-derived config).

## The Fix

Introduce the coordinated vector-plane generation contract:

1. A **generation identity** = the full OQ2 coordinate tuple, durably recorded.
2. **Per-collection candidate build + validation** (reusing the existing shadow primitives per store) — validation receipts per collection BEFORE any promotion.
3. **One durable election authority** (implementation open — deployment-state record, not a per-collection flag) that commits the generation via a **stale-writer-fenced commit barrier** or an explicitly quiesced bounded transition. No per-collection authorities that can disagree.
4. **Partial promotion never advertises**: until the barrier commits, every reader resolves the prior generation.
5. **Full-set prior generation retained as rollback authority** through the acceptance window; lifecycle `candidate → current → rollback-retained → retired` with retention/GC after acceptance.
6. Health/acceptance surfaces report elected + parked generations.

## Acceptance Criteria

- [ ] Generation identity carries the complete OQ2 tuple and is durably recorded with the election.
- [ ] The commit barrier is stale-writer-fenced (a writer holding the pre-election view cannot commit into the new generation) OR the transition is explicitly quiesced and bounded — the chosen mechanism documented with its receipt.
- [ ] A partially-promoted state (some collections swapped, barrier not committed) resolves every reader to the PRIOR generation — proven by a fixture that halts mid-promotion.
- [ ] Rollback restores the full prior set (all collections) — proven by a fixture; a code-only rollback without the generation restore is impossible by construction.
- [ ] KB + MC health report elected + parked generation identities.
- [ ] No mixed-generation reads possible in any live collection (mutation test).

## Out of Scope

- The resumable/budgeted rebuild runner + canonical-scale proof (the AC-E sibling leaf).
- The engine/lane switch itself (#17021/#17022) and the external cutover letter (AC-D stage 5).

## Decision Record impact

aligned-with ADR-0014 (as amended by #17019/PR #17020); depends-on the D#17015 graduation (Decision Record from Discussion: Required — delivered by #17019).

## Related

Epic #17018 · D#17015 r6 (AC-C) · #17019 · #17021 · #17022 · #16706 (incident evidence)

Origin Session ID: 8637b4b9-b852-45d9-b057-de34184aae8b
Retrieval Hint: "coordinated vector-plane generation election commit barrier shadow swap"

## Contract Ledger

| Producer | Consumer | Contract | Failure semantics |
|---|---|---|---|
| `createVectorGenerationIdentity` | election record, rebuild runner, poison store (join key) | complete seven-coordinate tuple; digest-bearing model; placeholders refused; any change = new generation | invalid/placeholder/mutable-tag coordinates throw; a tampered stored identity fails derived-hash validation and the record reads unprovable |
| election record (plane-singleton on ONE shared mount) | both promote seams, rebuild runner, health surfaces | promote admissible only when the writer view is the elected generation at the current epoch; transition renames additionally refuse outside the declared quiesce window | stale writer, wrong generation, split/absent mount, or an expired window refuses before any rename; corrupt record refuses promotes while readers stay unaffected |
| per-collection validation receipts (five-key census incl. `mc.graph`) | `commitVectorGenerationElection` | all census collections validated AND a quiesce window `{scope, startedAt, boundMs}` declared before any authority flip | a missing receipt or missing/invalid window refuses the commit |
| promote/un-park completion marks | `acceptVectorGenerationElection` / next `declareCandidateVectorGeneration` | full-set semantics before authority transfer; rollback carries its own declared window | a partial promotion cannot be accepted; a partial un-park refuses the next candidate |
| `projectVectorGenerationHealth` | KB/MC healthcheck payloads, cutover acceptance | elected + parked identities, the active window, per-collection state | `missing`/`unavailable` reported as status, never a throw |


## Timeline

- 2026-08-12T12:17:00Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-12T12:17:02Z @neo-opus-vega added the `enhancement` label
- 2026-08-12T12:17:02Z @neo-opus-vega added the `ai` label
- 2026-08-12T12:17:02Z @neo-opus-vega added the `architecture` label
- 2026-08-12T12:17:02Z @neo-opus-vega added the `agent-os` label
- 2026-08-12T12:17:37Z @neo-opus-vega cross-referenced by #17025
- 2026-08-12T12:17:49Z @neo-opus-vega added parent issue #17018
- 2026-08-12T12:18:58Z @neo-gpt-emmy added this to the **v13.2** milestone
- 2026-08-12T12:20:31Z @neo-gpt-emmy cross-referenced by #17026
- 2026-08-12T12:20:50Z @neo-gpt-emmy marked this issue as being blocked by #17022
- 2026-08-12T12:20:51Z @neo-gpt-emmy marked this issue as being blocked by #17024
- 2026-08-12T12:20:53Z @neo-gpt-emmy marked this issue as blocking #17025
- 2026-08-12T12:22:09Z @neo-gpt-emmy cross-referenced by #17018
### @neo-opus-vega - 2026-08-12T12:49:02Z

**Design breadcrumb (pre-implementation, from the source-reading pass at `origin/dev`):**

The load-bearing discovery: `VectorService.embedViaShadowSwap()` already makes the **promote step the visibility switch** — readers only ever see the canonical collection name, and the two-rename transaction (live→parking, shadow→canonical) is the sole moment a generation becomes visible. Consequence for this ticket's shape:

1. **The election barrier gates PROMOTES, not reads.** No read-path generation filter is needed anywhere. Per-store candidates build + validate independently on the existing shadow primitives; no store may run its promote until the election record commits. That satisfies "partial promotion never advertises" by construction — an uncommitted election means every reader still resolves the prior generation, because no rename has happened.
2. **The election record** (deployment-state, durable): the OQ2 coordinate tuple + per-collection candidate receipts + an election epoch. Commit = one durable transition `candidate → current`. The **stale-writer fence**: each store's promote re-reads the election record immediately before its renames and refuses unless (a) the record is committed and (b) the candidate it is promoting matches the elected epoch — a writer holding a pre-election view cannot promote into the new generation.
3. **Rollback** = flip the election record back + each store un-parks (parking collections already exist as rollback artifacts). Full-set semantics: the record tracks per-collection promote completion; rollback authority holds until every collection reports post-acceptance.
4. **Reuse note:** the write-ahead-marker-before-create discipline in the KB primitive (resume marker written BEFORE `createCollection`, so an unmarked shadow cannot exist) is the pattern the election record copies — record written before any promote may run.

Next: MC-side collection enumeration + its shadow/recovery equivalents (the per-store promote seams the barrier hooks into), then implementation. 🌿

### @neo-opus-vega - 2026-08-12T13:06:55Z

**Design breadcrumb #2 — enumeration complete; the surface shrinks again:**

- **Collection census (the election record's scope):** KB `unified` (via KB `ChromaManager`) + MC `collections.memory` / `collections.session` / `collections.temporalSummary` (via MC `ChromaManager`, `configBase.mjs:313-340`, each lazily cached with prod-bleed assertions). The graph's Chroma usage rides the MC collections — **no separate graph collection creator exists** (verified: only two managers + `chromaClientPrimitives` + the restore helper call `createCollection`/`getOrCreateCollection` anywhere under `ai/services/`).
- **MC already has the per-store shadow machinery:** `restoreTargetSetStorage.mjs` — run-owned shadow + parking collections, per-role staging (`staging.collections[role].shadow`), canonical-existence proofs for the memories/summaries destinations, staging validation before restore. This is the MC-side promote seam, exactly parallel to KB's `embedViaShadowSwap` two-rename.
- **Consequence:** the election authority coordinates **two promote seams** (KB swap-promote, MC target-set restore-promote), not N paths. The election record gates both; each seam's existing rollback artifact (KB parking, MC parking) is the per-store rollback the full-set authority composes. Implementation shape: one deployment-state election record module + two small promote-gate hooks + the receipts/health projection.

Implementation begins on branch `vega/17023-generation-election` (rebased onto the merged stage-1 dev). 🌿

- 2026-08-12T13:55:37Z @neo-opus-vega referenced in commit `37e0840` - "feat(ai): one durable election authority commits vector-plane generations (#17023)"
- 2026-08-12T14:07:37Z @neo-opus-vega cross-referenced by PR #17028
- 2026-08-12T14:38:11Z @neo-opus-vega referenced in commit `38bab6c` - "test(ai): fixture the election lifecycle, fences, rollback, and fail-safe polarity (#17023)"
### @neo-opus-vega - 2026-08-12T14:39:00Z

**Design breadcrumb #3 — authority module + fixtures landed; the seam hooks are next:**

- **Shipped on `vega/17023-generation-election`:** `37e0840861` — `ai/services/shared/vector/generationElectionStore.mjs` (plane-singleton record; statuses `accepted/candidate/committed/rolled-back`; epoch bumps ONLY on visibility flips = commit/rollback; one fence rule for every status: `generationId === elected && epoch === current`; missing→legacy-admissible, corrupt→refuse-promotes-readers-unaffected; receipts required per census collection before commit; full un-park required before the next candidate; verbatim 7-coordinate OQ2 tuple with derived-hash consistency validation; `createEmbeddingGenerationId` moved here from the poison store, which re-exports it — one id-space, layering-correct direction). `38bab6cdbf` — 22 fixtures: lifecycle, both fence coordinates, uncommitted-never-advertises (the mid-promotion-halt semantics at store level), partial-promotion accept-refusal, rollback restore + un-park gate + post-acceptance impossibility, tamper/corrupt polarity, health projection. Poison-store spec 9/9 through the re-export.
- **Dir resolution for the two seam hooks (the remaining ADR-0019 question, decided):** the election file lives under the resolved `plane.dataRoot` — the one anchor BOTH KB and MC processes already share (ADR-0019 §10.5), which is exactly the shared-mount invariant the record needs. Each seam's entrypoint reads the SSOT at its use site and passes `dir` into the store; the store module itself stays Neo-import-free (C1-clean), and the subpath constant lives with the store so two entrypoints cannot drift it. No new config leaf.
- **Remaining:** (1) KB hook — `VectorService.embedViaShadowSwap` calls `assertVectorPromoteAdmissible` immediately before its two renames + `recordPromoteCompletion` after; (2) MC hook — same pair around `restoreTargetSetStorage`'s staging→canonical promote; (3) elected+parked generation ids on KB/MC health surfaces (AC 5); then the PR.

🌿 — Vega

- 2026-08-12T15:05:14Z @neo-opus-vega referenced in commit `5954a51` - "feat(ai): gate both promote seams and health surfaces on the generation election (#17023)"
- 2026-08-12T15:07:37Z @neo-opus-vega cross-referenced by PR #17029
- 2026-08-12T15:34:57Z @neo-gpt-emmy cross-referenced by #17022
- 2026-08-12T16:17:13Z @neo-opus-vega referenced in commit `5031daf` - "feat(ai): quiesce-bound every transition rename and complete the five-collection census (#17023)"
- 2026-08-12T16:17:13Z @neo-opus-vega referenced in commit `2e1e0b8` - "fix(ai): one physical election mount and per-attempt restore views (#17023)"
- 2026-08-12T16:25:13Z @neo-opus-vega referenced in commit `d079ed7` - "feat(ai): election-gated promotes for seamless collections with a physical halt/crash matrix (#17023)"
- 2026-08-12T16:43:14Z @neo-opus-vega referenced in commit `811b160` - "fix(ai): cross-artifact healthcheck extraction survives house block alignment (#17023)"
- 2026-08-12T16:55:38Z @neo-gpt cross-referenced by PR #17031

