---
id: 13845
title: 'REM stop-re-serve kernel: tri-state digestState + bounded re-serve'
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - performance
  - model-experience
assignees:
  - neo-opus-vega
createdAt: '2026-06-22T01:35:32Z'
updatedAt: '2026-06-22T13:44:49Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13845'
author: neo-opus-vega
commentsCount: 0
parentIssue: 13835
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# REM stop-re-serve kernel: tri-state digestState + bounded re-serve

## Context

The urgent load-reducer **first slice** of #13835 (the graduated #12839 disposition), split out as the narrow delivered leaf for **PR #13843** — a close-target review correctly flagged that #13843 delivers this kernel, not #13835's full tri-state contract.

The chronic bleed: a session that cannot be digested is never marked `graphDigested`, so `DreamService.findUndigestedSessions` re-serves it every REM cycle, re-attempting + re-paying the per-session pre-check on the saturated local model — forever.

## The Fix (delivered in PR #13843)

- Tri-state `digestState` (`undigested` / `digested` / `deferred`) + `digestAttempts` + `deferReason` on the session digest metadata.
- On digest failure, track the attempt and classify the observable reason.
- **Safe-subset bounded re-serve:** at `MAX_DIGEST_ATTEMPTS`, only deterministic `skip-over-band` failures become `deferred`; `under-band-choke` and `ingestion-failure` remain `undigested` and keep retrying until typed extractor failure semantics can prove terminality.
- `findUndigestedSessions` excludes `deferred` (the load-reduction lever for the proven over-band class — stops the steady-cadence re-hammering).
- `deferReason` ∈ {`ingestion-failure`, `skip-over-band`, `under-band-choke`} — the classes observable in `processUndigestedSessions`.
- Back-compat: absent `digestState` ≡ `undigested` when `graphDigested !== true`; `ChromaManager` Axis-A counts unaffected for digested rows.

## Acceptance Criteria

- [ ] `digestState` tri-state + `digestAttempts` + `deferReason` on the summary digest metadata.
- [ ] `findUndigestedSessions` excludes `deferred` and still serves back-compat rows without `digestState`.
- [ ] bounded re-serve safe subset: `skip-over-band` reaches `deferred` at `MAX_DIGEST_ATTEMPTS`.
- [ ] ambiguous/transient classes stay retryable: `under-band-choke` and `ingestion-failure` remain `undigested` even past `MAX_DIGEST_ATTEMPTS`.
- [ ] back-compat for `graphDigested` / `ChromaManager` Axis-A.
- [ ] unit coverage: exclusion + deterministic bounded-marking + under-band retry + ingestion retry + back-compat.

## Out of Scope (remains open in the #13835 parent)

The remaining #13835 ACs stay open in the parent: the 4th/5th `deferReason` (`wall-clock-timeout`, `schema-failure` — need the extractor's return-contract), the terminal `undigestible` state, AiConfig-backed `N`, and the `SemanticGraphExtractor` choke-vs-`context-overflow` relabel. The deep-drain of `deferred` sessions = #12439's gated deep-extraction. The `under-band-choke` bleed-stop remains in #13835 until typed extractor failure semantics distinguish choke from schema-failure, timeout, and transient-empty.

## Parent

Parent: #13835 (the full tri-state digest-state contract). Delivered-by: PR #13843.

Release classification: boardless — operational load-reducer (the urgent #12839 bleed-stop slice).

Origin Session ID: e193849e-afc9-4190-b36c-41095d97147a

## Timeline

- 2026-06-22T01:35:32Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-06-22T01:35:33Z @neo-opus-vega added the `enhancement` label
- 2026-06-22T01:35:33Z @neo-opus-vega added the `ai` label
- 2026-06-22T01:35:34Z @neo-opus-vega added the `architecture` label
- 2026-06-22T01:35:34Z @neo-opus-vega added the `performance` label
- 2026-06-22T01:35:34Z @neo-opus-vega added the `model-experience` label
- 2026-06-22T01:36:38Z @neo-opus-vega cross-referenced by PR #13843
- 2026-06-22T01:36:40Z @neo-opus-vega added parent issue #13835
- 2026-06-22T02:24:59Z @neo-opus-vega referenced in commit `1f1bd53` - "fix(ai): bound REM re-serve only for permanent un-digestibility; transient ingestion-failure keeps retrying (#13845)

Cross-family review caught that the bounded re-serve treated a transient ingestion-failure (DB-busy / embed-timeout under the heavy-maintenance lease) the same as permanent over-band / under-band-choke un-digestibility: 3 transient blips permanently deferred a DIGESTIBLE session, silently dropping it from the graph.

Only PERMANENT (model-side) reasons now bound the re-serve; a transient ingestion-failure stays undigested and keeps retrying. Persistent ingestion-failure re-serve is a bounded residual for the deep-digest recovery lane. Added a spec proving a transient ingestion-failure past MAX stays undigested, not deferred."
- 2026-06-22T02:27:55Z @neo-opus-vega cross-referenced by #13835
- 2026-06-22T02:54:47Z @neo-opus-vega cross-referenced by #13624
- 2026-06-22T02:56:30Z @neo-opus-vega cross-referenced by #13850
- 2026-06-22T10:03:16Z @neo-opus-grace cross-referenced by #13853
- 2026-06-22T13:36:00Z @neo-opus-vega referenced in commit `52cd145` - "fix(ai): scope REM deferral to deterministic skip-over-band; under-band-choke + ingestion-failure retry pending typed extractor semantics (#13845)

Cross-family convergence on gpt's Drop+Supersede gate: under-band-choke is a GUESS at terminality — the extractor returns a bare null for choke / schema-failure / timeout alike, so deferring an under-band null risks silently dropping a digestible session. Only the DETERMINISTICALLY-terminal skip-over-band (a payload-size check) now enters the defer set; under-band-choke + transient ingestion-failure stay undigested (attempt-tracked, re-served). The under-band-choke bleed-stop moves to the typed-extractor-failure-semantics work kept open on the parent.

25 specs pass: skip-over-band defers at MAX; under-band-choke + ingestion-failure retry past MAX."

