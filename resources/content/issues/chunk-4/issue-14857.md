---
id: 14857
title: 'Dock cross-zone tab drag: functional tab-into model move (hit-test → moveItem)'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-grace
createdAt: '2026-07-05T17:07:05Z'
updatedAt: '2026-07-05T20:18:56Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14857'
author: neo-opus-grace
commentsCount: 0
parentIssue: 13158
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 14769 Dock workspace cross-window participation: source hooks + target wiring'
closedAt: '2026-07-05T20:18:56Z'
milestone: v13.2
---
# Dock cross-zone tab drag: functional tab-into model move (hit-test → moveItem)

## Context

#14850 (merged) wired the dock's first gesture — a tab reorders WITHIN its own tabs node. This leaf delivers the next gesture: dragging a tab header OUT of its zone and **releasing it over a different zone** relocates the item in the committed `dockZone.v1` document — the "drop a tab into a new target drop zone" gesture.

**Scope note (narrowed 2026-07-05, per PR #14864 review):** this leaf is the **functional cross-zone model-move slice** — hit-test the zone under the pointer on drop → commit `moveItem` directly. The full drop-zone producer's `dockPreview.v1` production, the `DockPreview` drag-time hover affordance, `edge-*`/`split-*` placements, and the hit-test unit are split to **#14866** (which is what `#14769`'s `previewFor` seam binds to). This keeps the close-target honest: the PR delivers exactly this slice.

## The Fix (delivered)

**Reuse-and-route, no parallel drag system.** `src/dashboard/DockTabSortZone.mjs` (a dock-aware tab-header SortZone) rides the existing drag lifecycle and, on drop, fires a `dockCrossZoneDrop` event on its `tab.Container` (before `super` — the base drag-end + deferred re-projection would otherwise destroy the node). `DockLayoutAdapter.projectTabsNode` wires the listener beside the within-container `moveTo` one; the listener's closure holds the reducer (`context.onDockCrossZoneDrop`, threaded through `project()`), so it survives config cloning without a component-tree walk. `MainContainer.onDockCrossZoneDrop` hit-tests the rendered tabs zones (`getDomRect`) and commits `moveItem`; a same-zone drop is a no-op.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
|---|---|---|---|---|---|
| `DockTabSortZone.processDragEnd` | `Neo.draggable.tab.header.toolbar.SortZone`; ADR 0029 no-parallel-drag | Fires `dockCrossZoneDrop` on the tab.Container with release point + item id | No owner listener / non-numeric coords → no-op | JSDoc | e2e `DockCrossZoneDragNL` |
| `MainContainer.onDockCrossZoneDrop` reducer | `DockZoneModel.moveItem`; landed `applyDockZoneOperation`/`onDockZoneDocumentChange` | Hit-tests rendered zones → commits `moveItem` cross-zone | Same-zone / off-zone → no-op, fail-closed on `errors[]` | JSDoc | e2e worker-truth before/after |

## Decision Record impact

`aligned-with ADR 0029` (rides the existing SortZone lifecycle; converts to a semantic operation on drop). The `dockPreview.v1` preview→operation refinement rides #14866.

## Acceptance Criteria

- [x] A tab dragged out of its zone and dropped over a different zone commits `moveItem` to that zone (App-Worker truth), fail-closed. — delivered, PR #14864
- [x] Whitebox-e2e drives the native cross-zone gesture and asserts the model move (`strategy: main-tabs → terminal-tabs`), ships green in the PR. — delivered
- [x] `DockLayoutAdapter` + `DockZoneModel` unit specs stay green; within-container e2e regression green. — delivered
- [ ] Cross-family review.

## Out of Scope (→ #14866)

`dockPreview.v1` production · `DockPreview` hover-affordance rendering · `edge-*`/`split-*` placements · hit-test unit test · cross-window transfer (`#14769`).

## Related

Parent `#13158` · builds on `#14850` · follow-up `#14866` (the `dockPreview.v1` producer + rendering, which unblocks `#14769`) · authority ADR 0029 §2.3/§2.6.

Origin Session ID: 9e42a8de-4291-46fc-944e-92ceb0db1748
Retrieval Hint: "dock cross-zone tab drag DockTabSortZone dockCrossZoneDrop moveItem functional slice"


## Timeline

- 2026-07-05T17:07:05Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-07-05T17:07:06Z @neo-opus-grace added the `enhancement` label
- 2026-07-05T17:07:06Z @neo-opus-grace added the `ai` label
- 2026-07-05T17:07:07Z @neo-opus-grace added the `architecture` label
- 2026-07-05T17:07:17Z @neo-opus-grace added parent issue #13158
- 2026-07-05T17:07:22Z @neo-opus-grace marked this issue as blocking #14769
- 2026-07-05T17:07:38Z @neo-opus-grace cross-referenced by #14769
- 2026-07-05T17:13:09Z @neo-opus-grace referenced in commit `5da2b6f` - "test(dashboard): cross-zone dock drag falsifier — DoD for the drop-zone producer (#14857)

Sequences the whitebox-e2e journey EARLY (before the producer build): drag a tab
header out of its tabs node and drop it over a different zone; assert the committed
dockZone.v1 model relocates the item (moveItem to the target node).

Marked test.fixme — it FAILS today (proven: strategy stays in main-tabs; the
producer that turns drag geometry into a dockPreview.v1 is unbuilt). Flip to test()
in the producer PR so it ships green with the fix (gesture-proof guardrail)."
- 2026-07-05T17:56:27Z @neo-gpt added this to the **v13.2** milestone
- 2026-07-05T18:17:33Z @neo-opus-grace referenced in commit `a3e1a0e` - "feat(dashboard): dock cross-zone tab drag — drop-zone producer + green e2e (#14857)

Releasing a tab header into a DIFFERENT dock zone now relocates the item in the
committed dockZone.v1 document — the gesture beyond #14850's within-container reorder.

Reuse-and-route, no parallel drag system: a dock-aware tab-header SortZone
(DockTabSortZone) rides the existing drag lifecycle and, on drop, fires a
dockCrossZoneDrop event on its tab.Container carrying the release point + item id.
The adapter wires the listener beside the within-container moveTo one; its closure
holds the reducer (captured at projection time — the reliable seam, since Neo's
config clone strips functions and the owner id is unset at projection). The reducer
hit-tests the zone under the pointer and commits a semantic moveItem; a same-zone
drop is a no-op. project() now threads onDockCrossZoneDrop through the context.

Ships its whitebox-e2e green in the same PR (gesture-proof guardrail):
DockCrossZoneDragNL drags Strategy out of main-tabs onto the Terminal zone and
asserts App-Worker truth — strategy moves main-tabs -> terminal-tabs. The
within-container journey (DockDragDropNL) stays green; 123 dock unit specs pass."
- 2026-07-05T18:18:13Z @neo-opus-grace cross-referenced by PR #14864
- 2026-07-05T19:35:49Z @neo-opus-grace cross-referenced by #14866
- 2026-07-05T19:37:11Z @neo-opus-grace changed title from **Dock drop-zone producer: hit-test + dockPreview.v1 for cross-zone tab drag** to **Dock cross-zone tab drag: functional tab-into model move (hit-test → moveItem)**
- 2026-07-05T19:37:58Z @neo-opus-grace referenced in commit `8270780` - "feat(dashboard): dock cross-zone tab drag — drop-zone producer + green e2e (#14857)

Releasing a tab header into a DIFFERENT dock zone now relocates the item in the
committed dockZone.v1 document — the gesture beyond #14850's within-container reorder.

Reuse-and-route, no parallel drag system: a dock-aware tab-header SortZone
(DockTabSortZone) rides the existing drag lifecycle and, on drop, fires a
dockCrossZoneDrop event on its tab.Container carrying the release point + item id.
The adapter wires the listener beside the within-container moveTo one; its closure
holds the reducer (captured at projection time — the reliable seam, since Neo's
config clone strips functions and the owner id is unset at projection). The reducer
hit-tests the zone under the pointer and commits a semantic moveItem; a same-zone
drop is a no-op. project() now threads onDockCrossZoneDrop through the context.

Ships its whitebox-e2e green in the same PR (gesture-proof guardrail):
DockCrossZoneDragNL drags Strategy out of main-tabs onto the Terminal zone and
asserts App-Worker truth — strategy moves main-tabs -> terminal-tabs. The
within-container journey (DockDragDropNL) stays green; 123 dock unit specs pass."
- 2026-07-05T20:10:21Z @neo-opus-grace referenced in commit `774e409` - "feat(dashboard): dock cross-zone tab drag — tab-into model move + green e2e (#14857)

Releasing a tab header into a DIFFERENT dock zone now relocates the item in the
committed dockZone.v1 document — the gesture beyond #14850's within-container reorder.
This slice commits the model move DIRECTLY from the pointer hit-test; the dockPreview.v1
producer + DockPreview hover rendering + edge/split placements are the #14866 follow-up.

Reuse-and-route, no parallel drag system: a dock-aware tab-header SortZone
(DockTabSortZone) rides the existing drag lifecycle and, on drop, fires a
dockCrossZoneDrop event on its tab.Container. The adapter wires the listener beside
the within-container moveTo one; its closure holds the reducer (captured at projection
time — the reliable seam, since Neo's config clone strips functions and the owner id is
unset at projection). The reducer hit-tests the zone under the pointer and commits a
semantic moveItem; a same-zone drop is a no-op. project() threads onDockCrossZoneDrop
through the context.

Ships its whitebox-e2e green in the same PR (gesture-proof guardrail):
DockCrossZoneDragNL drags Strategy out of main-tabs onto the Terminal zone and asserts
App-Worker truth — strategy moves main-tabs -> terminal-tabs. The within-container
journey (DockDragDropNL) stays green; 123 dock unit specs pass."
- 2026-07-05T20:18:56Z @tobiu referenced in commit `236f15d` - "feat(dashboard): dock cross-zone tab drag — tab-into model move + green e2e (#14857) (#14864)

Releasing a tab header into a DIFFERENT dock zone now relocates the item in the
committed dockZone.v1 document — the gesture beyond #14850's within-container reorder.
This slice commits the model move DIRECTLY from the pointer hit-test; the dockPreview.v1
producer + DockPreview hover rendering + edge/split placements are the #14866 follow-up.

Reuse-and-route, no parallel drag system: a dock-aware tab-header SortZone
(DockTabSortZone) rides the existing drag lifecycle and, on drop, fires a
dockCrossZoneDrop event on its tab.Container. The adapter wires the listener beside
the within-container moveTo one; its closure holds the reducer (captured at projection
time — the reliable seam, since Neo's config clone strips functions and the owner id is
unset at projection). The reducer hit-tests the zone under the pointer and commits a
semantic moveItem; a same-zone drop is a no-op. project() threads onDockCrossZoneDrop
through the context.

Ships its whitebox-e2e green in the same PR (gesture-proof guardrail):
DockCrossZoneDragNL drags Strategy out of main-tabs onto the Terminal zone and asserts
App-Worker truth — strategy moves main-tabs -> terminal-tabs. The within-container
journey (DockDragDropNL) stays green; 123 dock unit specs pass."
- 2026-07-05T20:18:56Z @tobiu closed this issue

