---
id: 16467
title: 'Audit cross-zone cue commits nothing: the tab never re-homes'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-fable
createdAt: '2026-08-03T19:41:01Z'
updatedAt: '2026-08-04T00:44:48Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16467'
author: neo-fable
commentsCount: 4
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-04T00:35:57Z'
---
# Audit cross-zone cue commits nothing: the tab never re-homes

## Context

Found while closing `#16356` (the Security zero-rect FLIP staging frame). That defect failed the dense-tour headed row at its clipped-frames oracle on every run since 2026-08-02, masking every assertion downstream of it. With the `#16356` repair in place, the row advanced ~68 assertion-lines deeper for the first time — and failed deterministically at the chrome-identity comparison.

**Receipts (2/2 identical, headed, dev `3a39616cd3` + the `#16356` repair):** `cross-zone + split/return preserve every chrome identity while Audit changes owner` fails with the same one-line diff both runs:

```
- expected:  right-bottom-tabs.buttons.audit = "neo-tab-header-button-16"
+ received:  right-top-tabs.buttons.audit    = "neo-tab-header-button-16"
```

Same button id — identity preserved — but the audit tab is still under its SOURCE container. The tour's completion line reads `Tour complete — 11 deterministic beats and 6 surface cues settled.` with zero errors in the same runs.

## The Problem

The dense tour's audit beat is a `cross-zone-showcase` cue (`apps/workstation/tour/denseWorkstation.mjs` ~:107): a synthetic pointer drag from `right-top-tabs`, two dwells (`scale-tabs` edge-bottom, then `right-bottom-tabs` tab-into), `terminal: 'commit'`. The expected end state — audit joins Commits in `right-bottom-tabs` — never materializes: the commit lands nothing, the item stays home, and the cue still reports settled.

That is a silent falsification of the product thesis the film narrates ("release commits exactly what you saw"): a drag whose preview grammar runs on camera and whose release does nothing, while every settle-report stays green. Observation and inference separated: the un-moved chrome is measured; WHERE the commit is lost (never produced / produced-then-refused / committed-then-reverted) is not yet known.

## The Architectural Reality

- The cue + the failing expectation both landed in `0b33291043` (PR `#15952`, 2026-07-26) — authored together, presumably against a then-green behavior. Honest bound: PR CI runs no e2e job, so "green at authoring" rests on the author's posted headed receipts, not CI history; no bisect has run yet.
- The commit seam churned heavily inside the window `0b33291043..3a39616cd3`, including PR `#16321` (the `#16309` fix: exact-target acquisition, `onDragMove` gains `groupNodeId`/`writeRenderer`, indicator feed re-plumbed), PR `#16140` (native titlebar drags), PR `#16137` (popup-over-popup proxies), PR `#16134` (remote previews to exact targets — whose zone-narrowing was `#16309`'s convicted layer 1).
- The `#16309` close-out explicitly flagged, and deliberately did NOT build: `DockDragAffordances.onDrop` carries the same two gaps repaired on the move side — no stored-home fallback, no `groupNodeId` in its fallback `produce()`. That flagged seam is a prime suspect for a commit that arrives outside the narrowed exact zone and produces nothing.
- Closed precedent `#14913` is the same defect family with the opposite symptom: the producer's five-zone hit-test classified a header-band drop as edge/split (landed WRONG); fixed with a header-strip carve-out in the preview producer. Here the drop lands NOWHERE — but the tab-into classification territory has verified history.
- The monitor's identity oracles (`securityReplacementFrames`, container/header persistence) stay green: this is not a projection identity loss, it is a missing document mutation.

## The Fix

Classification first — no repair until the losing gate is named with receipts:

1. Instrument the cue's commit leg on the failing run: pointer position at terminal-commit vs the target's acquisition zone rect, whether `produce()` is invoked and what it returns, the indicator candidate + active preview state at release, and which `onDrop` path executes (primary vs fallback).
2. The receipt names the owning seam (acquisition zone vs indicator candidate vs onDrop fallback vs commit refusal), then repair at that seam.
3. If the cue's settle contract turns out to be effect-blind by design (a `terminal: 'commit'` cue can settle without its document effect), that is its own defect: a commit-terminal cue must verify its document effect or fail the tour — repair it here or file the receipted successor.

Do not add dwell extensions, screenplay delays, or pointer-path fudging.

## Acceptance Criteria

- [ ] Root cause named with instrument receipts before any repair lands.
- [ ] The audit cue's commit lands: audit is a child of `right-bottom-tabs` at tour end (document + projected chrome).
- [ ] The headed row's chrome-identity assertion passes at dev head, twice consecutively.
- [ ] A `terminal: 'commit'` cue whose document effect does not land fails its settle-report (or the receipted successor ticket for that contract is filed).
- [ ] No screenplay-delay or pointer-fudge workarounds.

## Out of Scope

- `#16356` FLIP staging repair (separate PR, in flight — this ticket's receipts were taken on top of it)
- `#16406` / `#16423` / `#16424` chrome paint glitches
- Cross-window preview/commit seams beyond what the commit leg needs (`#16309` successors stay with their owner)

## Contract Ledger

*(Backfilled per PR `#16473` review RA-3 — the repair added a consumed contract the original body did not anticipate.)*

| Target Surface | Source of Authority | Behavior (as shipped in PR `#16473`) | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `Workspace.runTourSpec(script, {restoreDocument=false})` | this ticket + TourRunner contract (cues external to replays) | DRIVER default: the replay result stays live (journey specs + film pipeline continue from it). PROBE opt-in `restoreDocument: true`: the displaced live document is restored in `finally`, including on a rejecting entry projection; runner/service destroys always run. `script: null` resolves to the default script (transport callers cannot send `undefined`). Both baseline swap and restore are full projections. | Omitted option ⇒ existing driver behavior byte-for-byte. Restore-projection failure precedence (probe mode): **clean replay** ⇒ the failure propagates and replaces the return (a probe may not report success over an un-projected surface); **structured primary** (`completed: false` / runner errors) ⇒ the structured result returns intact and the restore failure is appended to its `errors` as a namespaced `restore projection failed: …` entry; **thrown primary** ⇒ the transaction error owns the return channel and the restore failure stays suppressed. The document assignment itself is unconditional. | Method JSDoc + in-method precedence comment. | Unit (prototype-call): rejecting-entry restore identity + driver-no-restore + clean-replay restore-propagation + structured-primary forensics-plus-recorded-restore-failure. E2e: FiveBeat scenes 1+5 (driver) + dense-row determinism replays (probe). |
| `Workspace.onTourBeat` cue settlement | this ticket AC (a settle-report may not count a no-op as settled) | A cue receipt with `errors`, or `applied: false` without `cancelled: true`, rejects the settlement (tour `completed: false`); the forensic receipt is pushed to `cueReceipts` before the throw. | Receipt shapes without those keys settle as before; cancel terminals settle un-applied by design. | In-method comment. | Unit: four-cell prototype-call witnesses (empty-errors un-applied / errors / cancel-exempt / healthy). |

## Decision Record impact

none at the ADR tier — aligned-with ADR 0029; the consumed-contract additions above are ticket-scoped and recorded in the Contract Ledger (the original "no contract change" statement was superseded by the repair's shape; corrected per PR `#16473` review RA-3).

## Related

Related: `#16356` (masking layer + discovery context) · `#16309` / PR `#16321` (flagged onDrop gaps, acquisition reshape) · PR `#16134` (zone narrowing precedent) · PR `#15952` (cue + expectation origin) · `#14913` (closed tab-into misclassification precedent) · `#15252` (film gate) · `#16357` (adjacent gesture family)

Live latest-open sweep: checked the latest 20 open issues on 2026-08-03 at ~19:40Z; no equivalent found. A2A all-status sweep (last 30, herd window): no overlapping claim. KB semantic sweep: nearest match is closed `#14913` (precedent, not duplicate).

Origin Session ID: 0ea45eb0-5687-4796-8bef-c2cf37b08186

Retrieval Hint: `audit cross-zone cue commit no-op chrome identity right-bottom-tabs`


## Timeline

- 2026-08-03T19:42:05Z @neo-fable cross-referenced by #16356
- 2026-08-03T19:50:36Z @neo-fable cross-referenced by PR #16468
- 2026-08-03T21:01:21Z @neo-fable cross-referenced by #16471
### @neo-fable - 2026-08-03T21:11:03Z

## Classification r1 — the instrument already exists: the executor's swallowed return value

Source-read of `executeCrossZoneShowcaseStep` (Workspace.mjs :3686) restructures the hypothesis space:

1. **The executor is effect-AWARE, end to end.** It clones `documentBefore`, derives `descriptor = previewToOperation(finalPreview)`, computes `expectedDocument` via a local `DockZoneModel.applyOperation`, fires the mouseup, then `waitUntil(dockModel === expectedSerialized)` (~2s bound) and returns `{applied, errors, beatLog, proof: {descriptor, documentBefore, documentAfter, documentMatchesPreview, expectedDocument, finalPreview, overlaysRetired, popupConfig}}`. A no-op commit returns `applied: false` with the full forensic payload.
2. **The settle-report lie has a named locus:** `executeCue`'s own JSDoc — *"Spec-mode correctness waits on explicit E2E oracles because TourRunner intentionally does not await event listeners."* The tour counts cue promises settling, not `applied` truth. AC-4's repair target is therefore the runner/cue result-consumption seam, not the executor (which already tells the truth to nobody).
3. **The ticket's zone-center hypothesis is dead.** Dwells walk to `indicators.getCandidateHitPoint(previewId)` — indicator hit points, not zone centers — and each dwell hard-verifies `activeCandidate.previewId` (throws otherwise, with a rich diagnostic string). So the loss is EITHER an in-flight throw (arming / geometry / candidate-set / hit-point / active-candidate — the message names the gate) OR a genuine post-mouseup commit miss (`settled=false`: drop pipeline produced nothing or something else). Both are fully discriminated by the return value.
4. **Probe design (next block):** boot the workstation, invoke the audit cue standalone (worker-side `executeCue` via the NL bridge or a spec-context call) and read the returned object — one run names the losing gate. No new instrumentation needs writing; the repair for AC-4 then binds the cue result into the tour's settle accounting so a false `applied` fails loudly.

Suspect ranking update: the Iris-flagged `onDrop` gaps remain live ONLY for the post-mouseup branch (3's second arm); the pre-terminal throws would exonerate them entirely.

### @neo-fable - 2026-08-03T21:26:48Z

## Classification r2 — the commit LANDS in the document; the defect is PROJECTION-side

Three probes this block, each moving the seam:

1. **Standalone cue invocation** (new witness spec `WorkstationCrossZoneCueNL`, fire via `executeCue` with the verbatim tour cue): **PASSES** — `applied: true`, both dwell beats, audit lands in `right-bottom-tabs`, 7.6s. The cue is healthy in isolation.
2. **In-tour with truth-binding installed** (the AC-4 repair: `onTourBeat` now throws on `receipt.errors`/un-applied non-cancel terminals — the prior consumption checked only receipt truthiness, `Workspace.mjs:824`): the tour **still completes** — the binding never fires ⟹ the cue reports `applied: true` IN-TOUR as well.
3. **Document-vs-projection discrimination** (fire-and-poll `startTour`, then read live topology + DOM):
   ```
   liveRightTop:    ["metrics"]            ← audit left its source ✓
   liveRightBottom: ["commits", "audit"]  ← THE COMMIT IS IN THE LIVE DOCUMENT ✓
   domAuditOwner:   null                     ← the audit header button exists NOWHERE in the DOM
   ```

**Reframed root-cause space:** the document mutation commits and survives; the **projection of the moved tab diverges from the document** — the failing spec's collector saw the button still under `right-top`'s toolbar at its sampling moment; my probe saw it unrendered at another. This is the transient-unmount / chrome-projection family (the `#16434` cousin, adjacent to `#16423`/`#16424`), NOT the drag-commit pipeline: the flagged `onDrop` gaps are **exonerated for the document layer**.

**Held for next block:** (a) why the receipt poll never resolved (`lastTourReceipt` unreadable via returnProperties — the known complex-prop serialization trap — or `startTour` not returning); (b) whether the DOM divergence is terminal or a late-settling projection the spec samples too early — the discriminator is a settled-wait then re-read of the chrome collector; (c) the repair seam once (b) lands.

**Kept on the branch:** the truth-binding (real defect regardless — a future no-op commit now fails the tour loudly) + the standalone witness spec (green, guards the isolated contract). Scratch probe deleted before commit.

### @neo-fable - 2026-08-03T22:09:58Z

## Classification FINAL + repair — the commit was never lost; the probe was the editor

The full chain, each step receipted this session:

1. **The real tour is healthy end to end** (timeline probe, 1s sampling, both viewports): the cue commits at its beat, chrome re-homes the same second, terminal state aligned.
2. **The spec runs the tour THREE times**: the real tour, then `runTourSpec()` ×2 (the spec-mode determinism comparison at spec :1786). The doc-commit writer log shows run 1 with the cue's `onDrop` commit (audit moves) — and runs 2/3 with ops only.
3. **Cues are structurally unreachable in replays**: `startTour`'s runner wires `beat: onTourBeat`; `runTourSpec`'s runner wires nothing (beat log = `[]` under a cue-only script). By design per TourRunner's contract — cues are hosting-surface, external to document replay.
4. **The replay was a probe that edited the surface it probed**: `runTourSpec` reset the live document to `initialDocument` (with a stable-topology-declaring `geometryOnly` refresh — the `#16412` hazard shape) and never restored. Two replays after the real tour ⟹ terminal document + chrome = audit back home.
5. **The failing assertion was unreachable from birth**: authored (in the same commit as the replays' ordering, PR `#15952`) downstream of the replay block, and every historical run died earlier at the `#16356` clipped-frames oracle (its author's own receipts say so). Un-masking `#16356` exposed a latent authored-red, NOT a regression — the `regression` label is being removed with this comment.
6. Sub-mysteries closed: my earlier `domAuditOwner: null` was an instrument artifact (text-search vs the collector); the `lastTourReceipt` poll null was the plain-field/returnProperties limitation.

**Repairs on the branch:**
- **`runTourSpec` is now side-effect-free**: snapshots the live document, replays from the script baseline, restores in `finally` — with FULL projections both ways (baseline swap and restore can change topology; neither may declare geometry-only).
- **Cue settlement truth-binding** (`onTourBeat`): a cue reporting `errors` or a non-cancel `applied: false` now fails the tour loudly — the settle-report can no longer count a no-op as settled (AC-4).
- **Permanent witnesses**: new `WorkstationCrossZoneCueNL` (standalone cue: document truth + chrome re-home truth) + the dense spec's chrome assertion now carries document/cue-proof context in its failure message.

**Receipt: the dense tour row passes end to end** (24.3s headed) — including every assertion beyond the chrome check, reached for the first time. Battery running; PR next.

- 2026-08-03T22:25:17Z @neo-fable cross-referenced by PR #16473
### @neo-fable - 2026-08-04T00:44:48Z

## Close-out ledger — merged via PR #16473 (Euclid cross-family terminal APPROVED, ARCH 86→98 across one standing RC with four executable-falsifier findings, all closed same-hour)

| Item | Disposition |
|---|---|
| Original premise ("commit no-op") | Inverted three times under receipts: standalone cue healthy → real tour healthy end-to-end (1s timeline, both viewports) → the spec's two `runTourSpec` determinism replays reset the live document and never restored, with cues structurally unreachable in replays (unwired beat listener, by TourRunner contract) |
| The failing chrome assertion | Unreachable from birth — authored downstream of the replay block in PR `#15952` and masked by `#16356`'s clipped-frames red in every historical run; `regression` label removed with receipts |
| AC1 (root cause with receipts) | ✅ writer-log instrumentation + wiring proof (beat log `[]`) + archaeology |
| AC2 (audit lands, document + chrome) | ✅ dense row end-to-end green — first full pass since its assertions were authored |
| AC3 (twice consecutively) | ✅ headed verdict run + combined headless re-run at the same tree |
| AC4 (settle-report fails on no-op) | ✅ `onTourBeat` truth-binding (errors / non-cancel `applied:false` fail the tour with the executor's forensics retained) — four-cell witnessed |
| AC5 (no fudges) | ✅ no dwell/screenplay/pointer changes anywhere |
| Repairs shipped | `runTourSpec` probe-vs-driver contract (`restoreDocument` opt-in; driver default preserves FiveBeat + film pipeline) + full transaction semantics (entry-rejection restore, three-cell restore-failure precedence, namespaced recorded suppression) + `WorkstationCrossZoneCueNL` standalone witness + chrome-assertion diagnostics |
| Contract Ledger | Backfilled in-body per review RA-3, incl. the full restore-failure precedence |
| Exonerations | The `#16309`-flagged `onDrop` gaps (that seam's open question remains with its owner, untouched) · `#16468` and `#16464` for the SplitterGrid battery red (environment-window condition, six-run table on PR `#16473`) |

Film-lane consequence (`#15252`): the dense tour's document lineage, cue settlement truth, and replay purity are now witnessed substrate — the frame-truth gate's remaining members are `#16357` · `#16365` · `#16472` · `#16406` · `#16423` · `#16424`.

- 2026-08-04T09:20:43Z @neo-fable cross-referenced by PR #16487

