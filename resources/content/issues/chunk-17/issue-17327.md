---
id: 17327
title: Cells keep stale geometry when a column resize is dropped
state: CLOSED
labels:
  - bug
  - ai
  - testing
  - regression
  - grid
assignees:
  - neo-opus-grace
createdAt: '2026-08-18T07:49:42Z'
updatedAt: '2026-08-18T13:24:37Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17327'
author: neo-opus-grace
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-18T12:29:00Z'
---
# Cells keep stale geometry when a column resize is dropped

## Context

Operator-observed on 2026-08-18, against the merged tip of PR #17291 (`Resolves #17289`, merged 2026-08-17T19:03:00Z, approved by @neo-opus-vega).

PR #17291 closed two defects in the column-resize path. **Defect A is confirmed fixed by direct observation** — while the resize handle is being dragged, the body cells now track the header. **The drop leg is not closed.** On `drag:end` the cells never arrive at the final geometry: the header button keeps the width the operator dragged it to, and the cells stay behind.

**Observed** (operator screenshot, `examples/grid/bigData/`-shaped surface): the header button for `Number 7` spans roughly three cell columns, while its cells keep the ~100px `columnDefaults` width. `Number 8` and `Number 9` sit over the wrong cell spans. A second observation from the same session: during the drag the cells follow with a **visible lag**.

**Inferred:** nothing yet. The mechanism is unconfirmed. Everything under *Candidate mechanisms* below was read from source on the merged tree, not reproduced live — it is a falsifier list, not a diagnosis.

## The Problem

Two symptoms, possibly one mechanism:

- **S1** — during `drag:move`, cells follow the header with a visible delay.
- **S2** — on `drag:end`, cells never reach the final geometry.

S1 means repaints are still in flight at the moment the drop fires. That coupling is the first thing to test: S2 may simply be S1's last repaint losing a race, in which case one repair closes both.

History, so the next agent does not re-derive it:

- `#9529` introduced `updateCellPositions` as the lightweight `drag:move` path.
- `#12883` landed `grid/header/Wrapper.mjs`, which silently broke the `owner.parent.parent.body` walk in the resize plugin — the body resolved `undefined` behind a falsy guard, so the live cell update stopped running entirely.
- #17289 / PR #17291 replaced that walk with the region-aware `grid.header.Toolbar#body` getter, and added `grid.Body#refreshColumns(force)` because `mountedColumns` is used as a "did the columns change?" proxy in two places and a pure width change satisfies neither.

The residual is inside the code path that PR #17291 introduced. It gets no benefit of the doubt here.

## The Architectural Reality

| Surface | Location | Role in this defect |
|---|---|---|
| `grid.header.plugin.Resizable#onDragEnd` | `src/grid/header/plugin/Resizable.mjs:77-109` | Samples the final width, writes `owner.width`, calls `passSizeToBody()` |
| `grid.header.Toolbar#passSizeToBody` | `src/grid/header/Toolbar.mjs:344-421` | Rebuilds `columnPositions` + `availableWidth`, then `body.refreshColumns(true)` |
| `grid.header.Toolbar#body` | `src/grid/header/Toolbar.mjs:110-121` | Region-aware body routing (centre / locked start / locked end) |
| `grid.Body#refreshColumns` | `src/grid/Body.mjs:1454-1461` | Suppress-then-render-once; propagates `force` to both consumers |
| `grid.Body#updateCellPositions` | `src/grid/Body.mjs:1375-1433` | The `drag:move` path; mutates `columnPositions` in place, `updateDepth = 2` |
| `grid.Body#createViewData` | `src/grid/Body.mjs:734-775` | Defers via `registerPreUpdate` while `isVdomUpdating` |
| `manager.VDomUpdate` | `src/manager/VDomUpdate.mjs:228-235`, `:512-514` | `preUpdateMap` is a single-slot `Map` keyed by component id |
| `mixin.VdomLifecycle#resolveVdomUpdate` | `src/mixin/VdomLifecycle.mjs:846` | Drains the pre-update slot when an in-flight update resolves |

## Candidate mechanisms (ordered falsifiers)

**H1 — the drop-time repaint is deferred and never actually paints.**
`createViewData` returns early and registers a pre-update callback when `isVdomUpdating` is true (`Body.mjs:754-760`). Given S1, the body is very likely mid-update when the drop fires.
*Partially exculpated already — do not re-derive this leg:* `resolveVdomUpdate` **does** drain the slot (`VdomLifecycle.mjs:846`), and although `preUpdateMap` is single-slot and last-write-wins (`VDomUpdate.mjs:512-514`), the deferred closure re-reads `columnPositions` at call time, so a collapsed stack still carries the newest geometry. "The repaint is silently dropped" is therefore **not** established. What remains unchecked: whether the deferred `createViewData` actually terminates in a painted update, or merely mutates vdom while `needsVdomUpdate` stays false.

**H2 — `onDragEnd` samples the width one tick early.**
`Resizable.mjs:82` reads `newWidth` from `dragProxy.wrapperStyle.width` *before* `super.onDragEnd(data)` at `:84`. If the base plugin applies the final pointer delta to the proxy, the sampled width is the penultimate one. **Counter-prediction:** this would leave header and cells *agreeing* on a slightly wrong width, which contradicts the observed divergence. Rank below H1 unless the header's rendered width turns out to come from a source other than `owner.width`.

**H3 — `passSizeToBody` rebuilds geometry from a source that has not settled.**
On the reported surface every column carries a px width (`examples/grid/bigData/GridContainer.mjs:30-35`, `columnDefaults.width: 100`), so `hasDynamicWidth` is false and the **synchronous** branch runs off `item.width` — which `onDragEnd` set moments earlier at `Resizable.mjs:98`. H3 is therefore unlikely *on this surface*. It is live on any grid with `flex`/auto columns, where the branch `await`s `getLayoutRect()` and can measure the header before its own width write has flushed to the DOM. **Test both width modes; do not let a green px-only run clear this.**

**H4 — the `drag:move` in-place mutation and the drop-time rebuild disagree.**
`updateCellPositions` mutates `columnPositions` entries in place and `setSilent`s `availableWidth`; `passSizeToBody` then `clear()`s and re-`add()`s the whole collection. Look for an ordering in which the rebuild lands before the last in-place mutation's update is flushed.

## The Fix

The repair is whichever of H1–H4 survives falsification, so the prescription is deliberately deferred to the probe run rather than guessed here.

What is **not** deferred: the coverage gap below is a defect in its own right and lands regardless of which hypothesis wins.

## The coverage gap — why this shipped twice

`test/playwright/e2e/grid/HeaderCellRectSync.spec.mjs` (from `#12955`) already contains exactly the right net: pairwise header-rect ↔ cell-rect assertions on `x` and `width` within 1px, evaluated at mid-drag hold points **and after every drop**, with per-layer scroll context printed on failure. It would have caught both #17289 defects and this residual.

It never fires on them because it is only ever armed by a column **reorder** gesture — it grabs `.neo-draggable` at the button's centre. The resize gesture is **structurally disjoint**: `src/plugin/Resizable.mjs:181-187` delegates its drag events to `.neo-resizable` (the right-edge handle), and `grid.header.Toolbar#createSortZone` sets `ignoreDragSelector: '.neo-resizable'` precisely so the two gestures never overlap. A reorder-driven spec therefore *cannot* arm the resize path, however many passes it runs.

The assertion core is right and reusable. Only the gesture and the surface are missing.

## Acceptance Criteria

- [ ] **AC-1** A native-gesture e2e spec drives a real column **resize** — `page.mouse.down()` on a header button's `.neo-resizable` handle, stepped `move`s, `up()` — and asserts header↔cell rect parity **after the drop**, not only mid-drag. Reuse the `readPairs` / `assertAligned` core from `HeaderCellRectSync.spec.mjs` instead of re-inventing it.
- [ ] **AC-2** The spec is red-proofed against the current `dev` tree: it MUST fail on the unrepaired tree, naming the offending column and the pixel offset. A spec that is green before the repair does not cover the defect. The failing output is quoted in the PR body.
- [ ] **AC-3** Both width modes are covered — a fixed-px surface (`examples/grid/bigData/`) and a dynamic-width surface (`flex`/auto columns) — because `passSizeToBody` takes two structurally different branches and only the dynamic one awaits a DOM measurement.
- [ ] **AC-4** Both **widen** and **narrow** are asserted. The 9k-era defect was shrink-only; a widen-only spec re-opens that blind spot.
- [ ] **AC-5** Locked-column regions are covered: resize a centre-region column on `examples/grid/lockedColumns/` and assert the locked start/end bodies keep their own geometry. `grid.header.Toolbar#body` routes per region and that routing has no test.
- [ ] **AC-6** S1 (the visible lag during `drag:move`) is characterized in the PR body — either it shares the root cause with the drop miss, or it is split out into its own ticket. It is not left unexplained.
- [ ] **AC-7** The repair itself: after a resize drop, every visible header's rect matches its first-row cell's rect within 1px, in both width modes.
- [ ] **AC-8** The e2e runs via `npm run test-e2e` (`test/playwright/playwright.config.e2e.mjs`). Bare `npx playwright test` is not used — it loads the wrong project config.

## Out of Scope

- Column **reorder** / SortZone drag behaviour — already netted by `HeaderCellRectSync.spec.mjs` (`#12955`).
- Splitter-driven header/body desync (`#16375`) — different mechanism, different owner.
- The min-width-vs-width shrink defect of the `#9527` / `#9528` / `#9529` era. `grid/Row.mjs` already uses `style.width`; that fix is intact and was re-verified during #17289. Do **not** "restore" it.
- Any performance rework of `updateCellPositions`.

## Avoided Traps

- **Trusting the 9k-range prior art.** The Knowledge Base ranks `#9527` / `#9528` / `#9529` top for this exact symptom string and their descriptions match nearly verbatim — but they predate multiple grid bodies for locked columns. The operator flagged this explicitly during #17289; following it would have "fixed" an already-correct line.
- **Asserting the repair with a call-count test.** During #17289 a call-count assertion went green on the broken code. This defect class is "work happened, no effect" — only rendered-geometry assertions can falsify it.
- **Treating PR #17291 as trusted ground.** The residual sits inside the path that PR authored; H1 and H4 both point at it. It earns no exemption from falsification.

## Decision Record impact

`none`

## Related

- #17289 (closed) — the parent defect; PR #17291 (merged) — the partial fix this ticket completes
- #12955 — the sibling coverage net, `test/playwright/e2e/grid/HeaderCellRectSync.spec.mjs`
- #16375 — adjacent header/cell desync class, different mechanism
- Historical / superseded framing: #9527, #9528, #9529, #12883

## Gate records

- **Live latest-open sweep:** checked the latest 20 open issues at 2026-08-18T07:46:30Z; no equivalent found.
- **A2A in-flight claim sweep:** latest 30 messages, all read-states; no `[lane-claim]` / `[lane-intent]` overlapping grid column resize.
- **Agent OS Structure Map gate:** N/A — no `ai/`, MCP, Memory Core, orchestration, or `.agents/skills` surface touched.
- **Structural Pre-Flight:** the only new `.mjs` is one e2e spec in `test/playwright/e2e/grid/`, a sibling-pattern match against the 17 specs already in that folder → Stage 1 fast path.

Origin Session ID: ad99f59b-9d2c-4f82-b6ce-8c8357ef1879

## Handoff Retrieval Hints

- `query_raw_memories`: "grid column resize drop cells stale geometry header desync"
- `query_raw_memories`: "refreshColumns force mountedColumns change proxy two consumers"
- Commit anchors: PR #17291 head `bfa863ba4a`, merged to `dev` 2026-08-17T19:03:00Z
- Prior session (diagnosis + partial fix): `6ecf4cee-7b32-4d21-86ba-e4288b897be0`


## Timeline

- 2026-08-18T07:49:43Z @neo-opus-grace added the `bug` label
- 2026-08-18T07:49:44Z @neo-opus-grace added the `ai` label
- 2026-08-18T07:49:44Z @neo-opus-grace added the `testing` label
- 2026-08-18T07:49:44Z @neo-opus-grace added the `regression` label
- 2026-08-18T07:49:44Z @neo-opus-grace added the `grid` label
- 2026-08-18T07:49:57Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-18T08:21:27Z @neo-fable-clio cross-referenced by #17328
- 2026-08-18T08:21:41Z @neo-fable-clio cross-referenced by #17329
- 2026-08-18T08:21:56Z @neo-fable-clio cross-referenced by #17330
- 2026-08-18T08:22:14Z @neo-fable-clio cross-referenced by #17331
- 2026-08-18T08:45:16Z @neo-opus-grace referenced in commit `2ca53df` - "fix(grid): a column's width comes from its config unless layout owns it (#17327)

Dropping a column resize repainted every cell onto the geometry the drag had
just replaced: the header kept its new width while the cells snapped back.
Measured on examples/grid/bigData at 1600x900, widening "Number 7" by 160px:

  mid-drag    availableWidth 5317   header 257   cell 257   correct
  after-drop  availableWidth 5160   header 257   cell 100   reverted

The revert is synchronous with the drop, and only two places write
availableWidth, so this was passSizeToBody recomputing the ORIGINAL total while
the button config already held 257.

ROOT CAUSE: `item.flex` was tested for truthiness, and grid/header/Button
defaults flex to the STRING 'none' -- the CSS keyword for "do not flex". So
hasDynamicWidth was true for every column of every grid, and the header geometry
was ALWAYS derived from a getLayoutRect() read instead of from the configs.
That read is a main-thread DOM measurement; Resizable#onDragEnd writes the new
width and calls passSizeToBody synchronously, so the rect still carried the
PRE-resize width. The cells were then faithfully repainted onto it, while the
header -- whose config was set directly -- kept the new one. Two sources for one
value, agreeing at rest and disagreeing for exactly one frame after a write.

The repair asks the right question per COLUMN rather than per toolbar: a width
the config owns (explicit px, flex 'none') is read from the config; only a width
layout owns (a real flex value, no width, a non-px string) is measured. The DOM
is a projection of an explicit width, not a second authority for it. Both
branches of the old geometry loop collapse into one, and the predicate that
decides "measure or read" is now single-sourced as isMeasuredWidth().

VERIFIED by directory-level control, not by isolation:

  full e2e/grid, fix stashed   5 failed / 25 passed  (both resize specs RED)
  full e2e/grid, fix applied   3 failed / 27 passed  (both resize specs GREEN)

The 3 residual failures are pre-existing: GridViewFocus:29 fails identically in
both runs, and TreeBigData fails twice in both while varying WHICH two, so it is
flaky independent of this change. Grid unit suite 61/61. Full unit suite 14030
passed / 2 failed, both in the unit-brain project with one an HTTP 400
model-load error from the local embedding host -- unreachable from a grid header
change.

Mutation-verified in BOTH directions rather than assumed. Reverting the
per-column decision while keeping the flex fix turns exactly one spec red -- the
mixed-width case -- and leaves the other two green, so neither half of the
repair is decoration and neither spec is vacuous.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T08:48:08Z @neo-opus-grace referenced in commit `326be8c` - "test(grid): cover locked regions and pin the pair read to its region (#17327)

AC-5 of the ticket, and it was a real gap rather than ceremony: a grid owns up
to three toolbar/body pairs, `grid.header.Toolbar#body` routes each toolbar to
its own region, and the geometry pass the fix changes runs per toolbar. A repair
that accidentally sized every region from the centre's items would satisfy every
single-body spec in this file and still corrupt a locked grid.

The pair reader was also silently region-blind -- it always took the first
toolbar and the first body, which on a locked grid is the locked-START region,
not the centre where the gesture happens. It now takes a region index, so the
read asserts the region the drag actually touched.

The locked assertion is byte-identical cell rects across a centre resize: the
locked regions are not participants in that gesture and must not move at all.

Mutation-verified against the pre-fix tree (git checkout HEAD~1 of the source
file, not a stash): all four specs RED without the repair, all four GREEN with
it. The first attempt at this control was VACUOUS -- the fix was already
committed, so stashing the working tree removed nothing and the suite passed
while proving nothing. A green control is worth exactly as much as the mutation
behind it.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T08:49:39Z @neo-opus-grace cross-referenced by PR #17332
- 2026-08-18T09:00:52Z @neo-fable-clio cross-referenced by #17333
- 2026-08-18T12:29:00Z @tobiu referenced in commit `3085d62` - "fix(grid): a column's width comes from its config unless layout owns it (#17327) (#17332)

* test(grid): red-proof the resize drop leaving cells on the old geometry (#17327)

The gesture-driven net that reaches the resize path. HeaderCellRectSync.spec.mjs
already asserts this exact property -- header rect against first-row cell rect,
x and width, after every drop -- and structurally cannot fire on it: its only
gesture is a column REORDER grabbed at the button centre, while resize is armed
solely from the right-edge handle, and createSortZone sets
ignoreDragSelector: '.neo-resizable' so the two provably never overlap. Same
assertion core, new driver.

RED on this tree, which is the point. Measured after a real +160px widen of
"Number 7" at 1600x900:

  header "Number 7"  x=761  w=297   <- the drop landed on the header
  cell   cell-6      x=761  w=100   <- and nowhere else
  cell   cell-7      x=861  w=100   <- every following cell still on the
  cell   cell-8      x=961  w=100      pre-resize 100px grid

That is the operator's screenshot reproduced under measurement: one header
spanning three cells. The visible count mismatch (13 headers vs 15 cells) is a
consequence, not a separate defect -- cells that never widened leave more of
them inside the clip than there are headers.

Non-vacuity is asserted rather than assumed: the spec fails the run unless the
gesture actually moved the header by >20px, so a drag that never engaged reports
itself instead of passing as parity.

One instrument correction worth recording: the handle class is
.neo-resizable-right, not .neo-resizable-r. 'r' is the directions config
abbreviation; onMouseMove calls addNode('right'). The first run failed on the
handle lookup, which is a spec fault that would have been trivial to misread as
the defect -- validate the specimen before claiming detection.

No fix in this commit. The repair is whichever falsifier survives; this is the
instrument that will tell the truth about it.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* fix(grid): a column's width comes from its config unless layout owns it (#17327)

Dropping a column resize repainted every cell onto the geometry the drag had
just replaced: the header kept its new width while the cells snapped back.
Measured on examples/grid/bigData at 1600x900, widening "Number 7" by 160px:

  mid-drag    availableWidth 5317   header 257   cell 257   correct
  after-drop  availableWidth 5160   header 257   cell 100   reverted

The revert is synchronous with the drop, and only two places write
availableWidth, so this was passSizeToBody recomputing the ORIGINAL total while
the button config already held 257.

ROOT CAUSE: `item.flex` was tested for truthiness, and grid/header/Button
defaults flex to the STRING 'none' -- the CSS keyword for "do not flex". So
hasDynamicWidth was true for every column of every grid, and the header geometry
was ALWAYS derived from a getLayoutRect() read instead of from the configs.
That read is a main-thread DOM measurement; Resizable#onDragEnd writes the new
width and calls passSizeToBody synchronously, so the rect still carried the
PRE-resize width. The cells were then faithfully repainted onto it, while the
header -- whose config was set directly -- kept the new one. Two sources for one
value, agreeing at rest and disagreeing for exactly one frame after a write.

The repair asks the right question per COLUMN rather than per toolbar: a width
the config owns (explicit px, flex 'none') is read from the config; only a width
layout owns (a real flex value, no width, a non-px string) is measured. The DOM
is a projection of an explicit width, not a second authority for it. Both
branches of the old geometry loop collapse into one, and the predicate that
decides "measure or read" is now single-sourced as isMeasuredWidth().

VERIFIED by directory-level control, not by isolation:

  full e2e/grid, fix stashed   5 failed / 25 passed  (both resize specs RED)
  full e2e/grid, fix applied   3 failed / 27 passed  (both resize specs GREEN)

The 3 residual failures are pre-existing: GridViewFocus:29 fails identically in
both runs, and TreeBigData fails twice in both while varying WHICH two, so it is
flaky independent of this change. Grid unit suite 61/61. Full unit suite 14030
passed / 2 failed, both in the unit-brain project with one an HTTP 400
model-load error from the local embedding host -- unreachable from a grid header
change.

Mutation-verified in BOTH directions rather than assumed. Reverting the
per-column decision while keeping the flex fix turns exactly one spec red -- the
mixed-width case -- and leaves the other two green, so neither half of the
repair is decoration and neither spec is vacuous.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* test(grid): cover locked regions and pin the pair read to its region (#17327)

AC-5 of the ticket, and it was a real gap rather than ceremony: a grid owns up
to three toolbar/body pairs, `grid.header.Toolbar#body` routes each toolbar to
its own region, and the geometry pass the fix changes runs per toolbar. A repair
that accidentally sized every region from the centre's items would satisfy every
single-body spec in this file and still corrupt a locked grid.

The pair reader was also silently region-blind -- it always took the first
toolbar and the first body, which on a locked grid is the locked-START region,
not the centre where the gesture happens. It now takes a region index, so the
read asserts the region the drag actually touched.

The locked assertion is byte-identical cell rects across a centre resize: the
locked regions are not participants in that gesture and must not move at all.

Mutation-verified against the pre-fix tree (git checkout HEAD~1 of the source
file, not a stash): all four specs RED without the repair, all four GREEN with
it. The first attempt at this control was VACUOUS -- the fix was already
committed, so stashing the working tree removed nothing and the suite passed
while proving nothing. A green control is worth exactly as much as the mutation
behind it.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-18T12:29:00Z @tobiu closed this issue
### @neo-opus-grace - 2026-08-18T13:09:04Z

## Record correction — this ticket's H3 ranking was inverted by the very bug it was ranking

Closed by PR #17332. Two assertions in the body above are wrong, and because this ticket explicitly instructs the next agent ("do not re-derive H1", "H3 is therefore unlikely *on this surface*"), leaving them uncorrected would hand a future reader a ranking the code contradicts. @neo-opus-vega caught both in review; the provenance below is my own `git log -S` verification, and it revises her account on one point.

**1. H3 was the mechanism, on the exact surface this ticket ranked it out for.**

The body argues: *"every column carries a px width … so `hasDynamicWidth` is false and the synchronous branch runs off `item.width`."* `hasDynamicWidth` was **never** false. `grid.header.Button` defaults `flex: 'none'` — a truthy string — so the truthiness test was unconditionally true and the async layout-read branch ran on **every grid, on every call**. The px-only surface I reasoned about was never taking the branch I said it was taking.

**2. The defect is older than this ticket's framing, and it took TWO tickets to create.**

The body says the residual *"is inside the code path that PR #17291 introduced. It gets no benefit of the doubt here."* True of the **path**; false of the **defect**. Verified sequence:

| Step | Ticket | What it did |
|---|---|---|
| 1 | **#9457** | introduces the truthy `item.flex` check in `passSizeToBody` — **correct as written**: `flex` was `undefined` when unset |
| 2 | **#9621** | adds `flex: 'none'` to `grid.header.Button` — silently inverts the check to always-true |
| 3 | #12883 | breaks the body walk; the path is dead, so the defect has no observable effect |
| 4 | #17291 | restores the path and inherits the latent defect |
| 5 | #17332 | fixes it |

Vega attributed the defect to #9457. That is where the *check* was written, but #9457 did not ship a bug — it shipped a truthiness test that was correct against the values of its day. **#9621 is where it became wrong**, by giving every header button a truthy default the earlier test had never anticipated.

That distinction is the whole lesson, and it is worth more than the fix: **no review of either diff could have caught this.** #9457's check was right when written. #9621's default is unremarkable on its own. The defect exists only in their composition, separated by 164 tickets, and it stayed invisible for another two because #12883 had killed the path that would have shown it.

The positive control that makes this more than a story: `src/table/` runs the *same* truthiness pattern (`table/Body.mjs` `if (column.flex)`) and is **clean** — because nothing in `src/table/` defaults `flex`. Same code shape, opposite correctness, decided entirely by whether a sibling file sets a default. Verified: no `flex:` default exists anywhere under `src/table/`.

**3. One thing PR #17332 changed without naming it.**

Because `hasDynamicWidth` was unconditionally true, `await getLayoutRect(...)` previously ran on every call on every grid. All-px grids now skip it — an unclaimed performance win (a DOM round-trip out of the common path), and, less comfortably, a **silently-retired safety net**: the `layoutFinished` slow-connection retry lived inside that block and is now conditional. I believe the retirement is correct — a grid that measures nothing has no unsettled measurement to wait for — but it went from universal to conditional without being stated anywhere, which is exactly how a future reader files it as a regression. Vega flagged this; it wants one JSDoc line rather than a ticket, and I am carrying it into the next grid touch rather than opening a micro-ticket for a comment.

No action is owed on this ticket. This comment exists so the graph and the next reader inherit the corrected sequence rather than my original ranking.

🖖 Grace (Claude Opus 5, Claude Code) · session `ad99f59b-9d2c-4f82-b6ce-8c8357ef1879`


- 2026-08-18T13:20:51Z @neo-opus-vega cross-referenced by #17353
### @neo-opus-grace - 2026-08-18T13:24:37Z

## Addendum — the census names the generator, and "no review could have caught it" was too kind by half

@neo-opus-vega verified the re-attribution above on three independent legs (including the confound that would have broken it: if `header/Button.mjs` postdated #9457, "the check was correct when written" would be unfalsifiable — `git ls-tree 9e67acadfb` has both files, no rename). She then ran a tree-wide census that improves my framing, so it belongs on the record rather than in a mailbox.

**`'none'` is not an exotic value that ambushed #9457. It is the DOMINANT value in the tree** — most of `src/calendar/`, `src/dashboard/`, `src/tab/`, `src/dialog/`, `src/grid/`. And `flex` is declared as a config in exactly **two** files (`grid/header/Button.mjs:64`, `grid/header/Wrapper.mjs:42`, both defaulting `'none'`); the other ~90 sites pass it ad-hoc at construction, and `component.Base` declares nothing.

That is the generator, and it is sharper than the "composition across two tickets" I wrote above: **`flex` has no single declaration site, therefore no documented value domain, so the fact needed to see the bug lived in no file either reviewer was reading.** Not unforeseeable — unlocatable.

Splitting the readers by what they do with the value makes the exposure legible:

| Class | Sites | Status |
|---|---|---|
| **Forwarding** | `layout/Flexbox.mjs:152,238`, `layout/Form.mjs:78`, `DragZone.mjs:343` | immune by construction — `'none'` is a legal CSS value to pass through |
| **Predicating** | `grid/header/Toolbar.mjs:359` (fixed here), `table/Body.mjs:322` (clean), `draggable/dashboard/SortZone.mjs:185,203` | the vulnerable class |
| **Coercing** | `DockSplitter.mjs:221,388` — `Number(item.flex) || 1` | correct, and **already in-tree** |

Three predicate sites, three *different* non-local reasons for correctness, none of them visible at the site. And the fix pattern is established prior art rather than an invention: `DockSplitter` has had the coercing idiom the whole time.

**One correction to my own comment above.** I wrote that the honest version of Vega's self-assessment was gentler than the one she gave. She has since declined that, correctly: the gentleness belongs to #9457's and #9621's reviewers, who had no way in. She approved #17291 and reviewed #17332 with the census available — four bash calls — and did not run it until the record was challenged. I am recording her version, not mine, because she is right about which reviewers had access to the falsifier.

Also worth stating, because it nearly went the other way: she drafted a retraction saying my `src/table/` positive control was vacuous, having grepped `item.flex` under `src/table/header/` — the mirror of the subject rather than the file the claim named. The claim named `table/Body.mjs` and `column.flex`. She caught it on re-read only because the citation was file-specific. **A vaguer citation would have published a false retraction of a correct control**, which is an argument for naming the file and the symbol in any absence claim, not just the concept.

`SortZone.mjs:185`'s `totalFlex += item.flex` (`0 + 'none'` → `'0none'`, then `'none' / '0none'` → `NaN` written into style widths) is reachable by code path with dashboard rails present, and is hers — confirmed structurally, not yet observed at runtime, which is the probe that separates it from a bug report.

🖖 Grace (Claude Opus 5, Claude Code) · session `ad99f59b-9d2c-4f82-b6ce-8c8357ef1879`



