---
id: 14423
title: 'Docking design spec: perspectives, cross-window drag, SharedWorker seam'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-07-02T02:35:27Z'
updatedAt: '2026-07-02T07:44:03Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14423'
author: neo-fable-clio
commentsCount: 4
parentIssue: 13158
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-02T07:44:03Z'
---
# Docking design spec: perspectives, cross-window drag, SharedWorker seam

## Context

The operator re-ranked the docking lane (2026-07-02) as an agent-harness cornerstone: the bar is **Qt-Advanced-Docking-System-class WITH Neo's multi-window reality**, and the assessed gap is not another implementation leaf but the missing **design tier** (operator direction comment on the parent: IC_kwDODSospM8AAAABIcPOUw). The parent epic's own history evidences unconverged design: its scope was reframed to "Docking on the Infinite Canvas — QT superset + multi-window" (2026-06-15) and reverted to "QT-parity docking polish" (2026-06-21) — a title oscillation is a design question wearing a rename. This ticket is the design gate that settles the tier above the remaining leaves. Filed under the product-surface planning lane (`[lane-claim]` broadcast 2026-07-02T02:30Z; #13158 self-assigned).

## The Problem

The dockZone.v1 foundation is real and shipped (model contract, semantic-operation executor, split/tab adapter, drag preview, splitter resize, auto-hide model seam). But the remaining capability leaves (auto-hide UI `#13280`, named perspectives, grouped tab-drag/overflow) plus the multi-window bar share design questions that per-leaf contract settling cannot answer coherently:

- The persistence wrapper (`neo.harness.dockLayout.v1`) models `layoutId`/`title`/`revision`, but **perspective semantics** — what a perspective captures across MULTIPLE windows on one SharedWorker heap, restore into a *different* window topology, fail-closed restore — are unspecified.
- **Cross-window drag semantics** (a dock item dragged between Chromium popups sharing one App-Worker heap; boundary with the `#13028` OS-window reintegration leaf) are unowned by any leaf.
- The **SharedWorker seam** — which docking state is per-window render projection vs worker-owned shared truth — is implicit in the adapter but never stated as a contract.
- The **core-lift clause** (contract §Ownership Boundary: stays in `src/dashboard/` until a second non-dashboard consumer proves reuse) is approaching its trigger: the Institution Cockpit (#13444) and FM-UX surfaces (#13015) are becoming exactly that second consumer class. Fire, defer, or amend is a design decision, not a leaf decision.
- Operator assessment: parts of the existing interaction code are brittle — brittleness at the interaction layer is the symptom of missing design authority above it.

## The Architectural Reality

- Contract of record: `learn/agentos/HarnessDockZoneModel.md` (dockZone.v1 — §Split Projection, §Serializable vs Runtime State, §Ownership Boundary, §Demand Validation).
- Shipped substrate: `src/dashboard/DockZoneModel.mjs` (semantic ops incl. `resizeSplit`, `setItemAutoHidden`), `src/dashboard/DockLayoutAdapter.mjs`, `src/dashboard/DockSplitter.mjs`, `apps/agentos/view/DockPreview.mjs`.
- Verification surface: `examples/dashboard/dock/` (#13247 — slices landing).
- Multi-window substrate: Chromium popups on one SharedWorker App-Worker heap (ADR 0020 embodiment vessel); window-manager leaves #13025/#13028 are siblings, not children.
- Consumers-in-waiting: #13444 cockpit panes and #13015 FM-UX cards live INSIDE the docking shell; their container contract is this spec's downstream interface.

## The Fix

Produce the **docking design-tier specification** as a contract document in `learn/agentos/` (sibling-lift from `HarnessDockZoneModel.md`; exact filename settled at authoring per the sibling pattern — no new `.mjs`, so structural-pre-flight Stage 0 does not fire; if the cross-window seam proves to need binding decision authority beyond a contract doc, the spec escalates that single section to an ADR and says so explicitly). **One PR.** Sections the spec MUST settle:

1. **Layout model formalization** — the full dockZone.v1 state space incl. multi-window: worker-owned truth vs per-window projection (the SharedWorker seam), as a normative table.
2. **Named perspectives** — capture scope (single-window vs whole-topology), restore semantics into changed window topologies, fail-closed behavior, revision migration.
3. **Cross-window drag** — semantic-operation shape for item movement between windows, riding the existing preview→operation path; boundary with `#13028` consumed, not owned.
4. **Grouped drag + overflow** — the multi-item operation contract (leaf-implementable afterward).
5. **Core-lift clause disposition** — fire / defer-with-named-trigger / amend §Ownership Boundary, given the cockpit/FM consumer class.
6. **Container contract for embedded product surfaces** — the minimal interface #13444 panes and #13015 FM-UX cards consume (coordination: #13015 steward has the options DM, 2026-07-02).

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `learn/agentos/` docking design-tier contract doc (new) | ADR 0020 + `HarnessDockZoneModel.md` (dockZone.v1) | Adds the design tier above v1: multi-window layout model, perspectives, cross-window drag, SharedWorker seam, core-lift disposition | Per-leaf contract settling continues (status quo) | The doc IS the docs | Cross-family-reviewed spec PR; render-verified diagrams if included |
| `HarnessDockZoneModel.md` §Ownership Boundary | The contract's own clause | Spec dispositions the second-consumer trigger (cockpit/FM) | Clause unchanged | Amended §, iff disposition = amend | V-B-A against #13444/#13015 consumer reality |

## Decision Record impact

`aligned-with ADR 0020`. The spec itself declares whether the cross-window/SharedWorker seam requires a new ADR (escalation path named in The Fix); this ticket does not amend or supersede any ADR.

## Acceptance Criteria

- [ ] Spec PR merged containing all six sections, each with explicit contracts (not aspirations).
- [ ] SharedWorker seam stated as a normative table: worker-owned vs window-projected state classes.
- [ ] Perspective semantics define capture scope, cross-topology restore, and fail-closed behavior.
- [ ] Cross-window drag defined as semantic operations on the existing preview→operation path (no parallel drag system — inherited guardrail).
- [ ] Core-lift clause dispositioned with a named trigger (fire / defer-until-X / amend).
- [ ] Container-contract section names its two consumers (#13444, #13015) and their minimal interface.
- [ ] JSON-first persistence guardrail restated and applied to every new persisted shape (no `DOMRect`, screen coordinates, `windowId`, live components in layouts).
- [ ] Cross-family review from ≥1 non-fable family before merge.
- [ ] Unstarted leaves (perspectives, grouped-drag) reference the spec as upstream; in-flight `#13280` explicitly NOT gated on it.

## Out of Scope

- Implementation of the capability leaves (they follow, one Contract-Ledgered leaf per capability, per the parent's epic-review discipline).
- Executing the core `src/layout/Dock` lift (the spec dispositions it only).
- Window-manager leaves #13025 / #13028 (consumed as boundaries).
- #13015 stewardship or FM-UX implementation (only the container interface is specified here).
- Public performance claims (inherited `#13032` guardrail).

## Avoided Traps

- **Spec-as-epic sprawl** — rejected: one document, one PR, six named sections.
- **dockZone.v1 redesign** — rejected: the tier is additive on the shipped model (the parent's own rule).
- **Gating in-flight work** — rejected: `#13280` proceeds on its landed model seam; only unstarted leaves sequence behind the spec.
- **Importing Qt-ADS architecture wholesale** — rejected: Qt-ADS is the capability *bar*, not the design — its single-process, single-window-tree assumptions do not survive Neo's worker/multi-window reality (industry-friction-radar 3-step abstraction applies: extract the friction points, never the architecture).

## Related

Parent: #13158. Foundation: the `#13030` chain. Consumers: #13444, #13015. Boundaries: #13028, #13025. Verification: #13247. In-flight sibling: `#13280`.

Live latest-open sweep: checked latest 20 open issues at 2026-07-02T02:33Z; no equivalent found. A2A in-flight sweep: 30-message all-states scan at 02:33Z; only same-scope claim is my own `[lane-claim]` (02:30Z, first-claim-timestamp mine).

Origin Session ID: 4185a2ee-ff65-4544-9cc1-049d73e56649
Retrieval Hint: "docking design tier spec cross-window SharedWorker seam perspectives" · `query_summaries`: "product-surface planning lane #13158 claim"

## Timeline

- 2026-07-02T02:35:27Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-07-02T02:35:28Z @neo-fable-clio added the `enhancement` label
- 2026-07-02T02:35:28Z @neo-fable-clio added the `ai` label
- 2026-07-02T02:35:28Z @neo-fable-clio added the `architecture` label
- 2026-07-02T02:35:35Z @neo-fable-clio added parent issue #13158
- 2026-07-02T02:40:39Z @neo-fable-clio cross-referenced by #13158
### @neo-fable-clio - 2026-07-02T02:40:51Z

## AC addendum (from epic-review Stage 2.5, 2026-07-02)

Added authoring AC, per my epic review on the parent (https://github.com/neomjs/neo/issues/13158#issuecomment-4861698355):

- [ ] **`#13370` criteria-mapping pass:** fetch Discussion `#13370` (graduated 2026-06-15, quorum met → the "Infinite Canvas" reframe that was later title-reverted) and map each of its graduated criteria into a named spec section OR explicitly dispose of it (`superseded` / `out-of-scope-with-rationale`). No orphaned criteria between the reframe and the revert.

Also binding from the same review: the perspectives section V-B-As the **landed state of `#13147`/`#13153`** at authoring time (no June-comment trust).

— Clio (@neo-fable-clio), ticket author. Origin Session ID: 4185a2ee-ff65-4544-9cc1-049d73e56649

### @neo-fable-clio - 2026-07-02T02:43:51Z

## `#13370` criteria-mapping pass — COMPLETE (the Stage-2.5 AC)

Fetched Discussion `#13370` in full (graduated 2026-06-15; §6.2 quorum = Vega `[AUTHOR_SIGNAL]` + Euclid `[GRADUATION_APPROVED]` via Vega's provenance-preserving relay after the gh-credential misattribution was voided — the audit trail is intact and rather instructive).

**Finding: the orphaning suspected at epic-review is real.** The graduation target — reframe #13158 as the docking umbrella **+ file four leaf subs** (cross-window-contract seam · multi-window persistence · drag-to-dock integration · auto-hide/pin/perspectives-across-windows reconciliation) — executed only as the title reframe (2026-06-15T22:11Z), which was reverted (2026-06-21); **none of the four leaf subs were filed**, and the OQ3/4/5 acknowledgment ACs attached to no surviving artifact. This spec absorbs them:

| `#13370` graduated criterion | Disposition in #14423 |
|---|---|
| **Option-4 seam:** `CrossWindowDragTarget` manager-facing contract (named minimum: `sortGroup`, `windowId`, `acceptsRemoteDrag`, `onRemoteDragMove/Leave`, `onRemoteDrop`, `onRemoteDropOut`, native-titlebar hooks) + AC: `DragCoordinator` arbitrates WITHOUT importing `DockZoneModel`/`DockLayoutAdapter`/preview | **Spec §3 (cross-window drag semantics)** — the seam contract is the section's normative core; the dock-blind-coordinator AC carries verbatim |
| **OQ1 `[RESOLVED_TO_AC]`:** `neo.harness.windowPlacementHints.v1` separate hint layer keyed by item id (durable: identity, detached-vs-docked intent, owning `dockLayoutId`/perspective, semantic `fallbackTarget`; runtime-only: windowId, rects, SortZones) + semantic-recovery-not-geometry fallback | **Spec §1 + §2** — the SharedWorker-seam table inherits this durable/runtime line as its seed; perspectives capture-scope builds on the hint layer |
| **OQ2 `[RESOLVED_TO_AC]`:** the reusable shape is the CONTRACT, not a `dashboard.SortZone` lift | **Spec §3** — restated as a binding constraint |
| **OQ3 ack-AC:** `DockSplitter` vs `Neo.component.Splitter` reconcile | **Spec §1** (layout-model formalization) — dispositioned there |
| **OQ4 ack-AC:** web-docking-library parity sweep (Dockview / GoldenLayout / rc-dock / FlexLayout / Lumino); the cross-window live-object-permanence moat **stays a hypothesis until swept** | **Spec precedent section + AC** — sweep runs at authoring via the industry-friction-radar 3-step abstraction; the moat claim stays hypothesis-marked in all public text until validated |
| **OQ5 ack-AC:** auto-hide/pin/perspectives composition across detached windows | **Spec §2+§3 composition clause** — explicit behavior table for detached-window state of pinned / auto-hidden / perspective-owned items |
| **Graduation-target leaf subs** (seam · persistence · drag-to-dock integration · cross-window reconciliation) | **Post-spec decomposition** — filed as spec-downstream leaves per the parent's one-leaf-per-capability discipline; the spec defines their contracts |
| **Decision Record: OPTIONAL** (seam contract lives in `HarnessDockZoneModel.md`, not a new ADR) | **Preserved** — matches this ticket's declared escalation path exactly |

No criterion disposed as `superseded` or dropped: all eight map to named spec sections or post-spec decomposition. The spec's section list in the ticket body stands — `#13370` fills it with converged content rather than reshaping it.

**Liveness-invitation closure:** `#13370` §Unresolved Liveness invited the fable family's retroactive review of the unification + stewardship transition on reactivation. This mapping plus tonight's epic review (https://github.com/neomjs/neo/issues/13158#issuecomment-4861698355) are that review: **the one-cross-window-reality unification stands; the execution gap (reverted reframe, unfiled leaves) is real and is now owned by this spec.** Pointer comment going on `#13370`.

— Clio (@neo-fable-clio). Origin Session ID: 4185a2ee-ff65-4544-9cc1-049d73e56649

### @neo-fable-clio - 2026-07-02T02:48:57Z

## Session handover (Clio, sunset — solo-refresh) — spec-authoring pickup ramp

**Owner:** @neo-fable-clio (assigned; lane-claimed 2026-07-02T02:30Z). Next session is me — this ramp exists so the pickup costs one read.

**State:** ticket filed + parent-linked + epic-review gate cleared under my identity (parent review: https://github.com/neomjs/neo/issues/13158#issuecomment-4861698355) + `#13370` criteria-mapping COMPLETE (see prior comment — the mapping table IS spec-content, not just compliance) + branch `agent/14423-docking-design-spec` exists at dev-tip with zero commits (nothing to push).

**Pickup protocol, in order:**
1. Resume the branch (primary clone `/Users/Shared/clio/neomjs/neo`; note untracked `.claude/launch.json` was pre-existing — ignore).
2. **Fire `/guide-authoring` BEFORE the first sentence** — target is `learn/agentos/` (sibling-lift from `HarnessDockZoneModel.md`); the 9/10 bar + grounding discipline apply. No new `.mjs` → structural-pre-flight stays cold.
3. Grounding already banked in session 4185a2ee (query Memory Core if needed): full reads of `HarnessDockZoneModel.md`, epic #13158 + all comments incl. the operator direction (IC_kwDODSospM8AAAABIcPOUw — the bar: Qt-ADS-class WITH multi-window; brittleness assessment), Discussion `#13370` in full, ADR 0026 (for the two-worlds/controller vocabulary — do NOT conflate its "two-worlds" term with the product-taxonomy sense).
4. Draft the six sections from the ticket body; the `#13370` mapping table supplies §1–§3's converged content (Option-4 `CrossWindowDragTarget` contract verbatim, `windowPlacementHints.v1` durable/runtime line, dock-blind-`DragCoordinator` AC).
5. **At-authoring V-B-A obligations:** (a) landed state of `#13147`/`#13153` before writing §2 perspectives; (b) the OQ4 web-docking parity sweep (Dockview / GoldenLayout / rc-dock / FlexLayout / Lumino) via industry-friction-radar 3-step abstraction — the object-permanence moat stays hypothesis-marked until swept.
6. **Coordination state:** Grace has the #13280 sequencing question (her call: fixed-input vs open-question — DM 2bc3e04a, unanswered as of sunset); Ada has the FM-UX container-contract options (DM 5a80e8e3, unanswered). Neither blocks §1–§5; §6 (container contract) prefers Ada's answer but can draft interface-minimal without it.
7. One PR when the spec is real; cross-family review ≥1 non-fable family (AC).

**Do NOT at pickup:** trigger sandman; start implementation leaves; touch #13015 (Ada's stewardship).

— Clio. Origin Session ID: 4185a2ee-ff65-4544-9cc1-049d73e56649

### @neo-fable-clio - 2026-07-02T03:00:28Z

## AC addendum 2 — auto-hide UI contract enters the spec (leaf-owner sequencing decision, 2026-07-02)

@neo-opus-grace (owner of in-flight `#13280`) decided the sequencing on her leaf (A2A DM `b2d5e75a`, 2026-07-02T02:48Z): **hold `#13280` design-first** — the auto-hide UI contract is an **open question this spec settles**, not fixed-input; she implements `#13280` against the landed spec this week. Her stated rationale: the operator's design-first direction governs; `#13280` has no PR yet so nothing is lost; the auto-hide UX belongs to the coherent docking surface rather than an isolated fix that would constrain the design.

Two AC deltas follow:

- **Superseded:** the AC line *"in-flight `#13280` explicitly NOT gated on it"* — written when `#13280` was proceeding on its landed model seam. The leaf owner's own call inverts the sequencing: `#13280` is now **spec-downstream by owner decision**, joining the unstarted leaves. (Provenance: the original AC protected Grace's in-flight work from a gate she didn't choose; she chose it — the protection has no remaining object.)
- [ ] **Added:** the spec contains an **auto-hide UI contract section** — the implementable surface contract above the landed model seam (`setItemAutoHidden`, dockZone.v1): pinned/unpinned state semantics, edge-tab representation, reveal/dismiss interaction contract, overlay-vs-push disposition, and composition with perspectives + detached windows (the `#13370` OQ5 clause already bound for §2+§3 composes here). Section must be implementation-sufficient for `#13280` without further design decisions.

Section count: the ticket's six MUST sections + this one. All other ACs unchanged.

— Clio (@neo-fable-clio), ticket author. Origin Session ID: 2251c81c-1446-4723-86b3-479322bbcc95

- 2026-07-02T03:17:24Z @neo-fable-clio cross-referenced by PR #14425
- 2026-07-02T03:25:14Z @neo-fable-clio cross-referenced by #14427
- 2026-07-02T03:38:19Z @neo-fable-clio referenced in commit `a642fd4` - "docs(agentos): §3 seam contract — full source-side + native-window hook classes (#14423)"
- 2026-07-02T03:43:22Z @neo-fable-clio cross-referenced by #14426
- 2026-07-02T06:41:13Z @neo-fable cross-referenced by #13012
- 2026-07-02T06:57:15Z @neo-fable-clio referenced in commit `f3d4aa7` - "docs(agentos): re-home docking design authority as ADR 0029 (#14423)

Review cycle 3 (operator-flagged): the design tier settles seven shared questions, defines persisted schemas and semantic operations, and binds leaves to amend-first semantics — that crosses ADR 0005's ADR_REQUIRED threshold, so the authority moves from the public learning tree into learn/agentos/decisions/ as ADR 0029 with full head-matter, rejected options, and the section mapping (draft §N = ADR §2.N). Guide-tree registration replaced by the Decisions entry (net-zero nodes, lint 213 OK); ADR index row added to ArchitectureOverview.md per the map-as-pointer discipline."
- 2026-07-02T07:44:03Z @tobiu referenced in commit `464ca2b` - "docs(agentos): ADR 0029 — harness docking design tier above dockZone.v1 (#14423) (#14425)

* docs(agentos): docking design-tier spec above dockZone.v1 (#14423)

* docs(agentos): §3 seam contract — full source-side + native-window hook classes (#14423)

* docs(agentos): re-home docking design authority as ADR 0029 (#14423)

Review cycle 3 (operator-flagged): the design tier settles seven shared questions, defines persisted schemas and semantic operations, and binds leaves to amend-first semantics — that crosses ADR 0005's ADR_REQUIRED threshold, so the authority moves from the public learning tree into learn/agentos/decisions/ as ADR 0029 with full head-matter, rejected options, and the section mapping (draft §N = ADR §2.N). Guide-tree registration replaced by the Decisions entry (net-zero nodes, lint 213 OK); ADR index row added to ArchitectureOverview.md per the map-as-pointer discipline."
- 2026-07-02T07:44:04Z @tobiu closed this issue
- 2026-07-02T08:30:29Z @neo-fable cross-referenced by #13448

