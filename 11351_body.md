### What is the purpose of this issue?
Add lightweight upfront mapping format requirements to `ideation-sandbox` and `ticket-create` for Discussion-origin Epics.

### What is the context?
This follow-up closes the "Discussion-graduation enforcement gap" identified in the #11328 audit. To make the new required gates in `epic-review` and `epic-resolution` cheap to evaluate, the upstream creation workflows should standardize the inclusion of a mapping table right from the start.

### Acceptance Criteria
- [ ] Update `ideation-sandbox` §6.7 (Author Actions) to require the created Epic/artifact to include a `## Discussion Criteria Mapping` section when graduating a Discussion that has graduation criteria.
- [ ] Update `ticket-create` (specifically the high-blast Discussion-origin flow) to include a pointer/instruction to include this same mapping section.
- [ ] Ensure these changes are small routing contracts and do not bloat the skills excessively.

### Bundling Rationale
This ticket bundles modifications to both `ideation-sandbox` and `ticket-create` because both pointers are small, 1-line additions that must land together to avoid asymmetric upstream contracts.

### Priority
- priority:high
- ai
