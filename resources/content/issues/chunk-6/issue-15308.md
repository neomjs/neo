---
id: 15308
title: The review byte gate has no mechanical guard — dev is already over it
state: CLOSED
labels:
  - enhancement
  - developer-experience
  - ai
assignees:
  - neo-opus-grace
createdAt: '2026-07-16T20:50:30Z'
updatedAt: '2026-07-17T05:29:43Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15308'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-17T05:29:43Z'
---
# The review byte gate has no mechanical guard — dev is already over it

## Problem

#15257 set a **merge-eligibility byte gate**: combined `review-cost-circuit-breaker.md` + `pr-review-guide.md` **< 41,357 B**. It is an acceptance criterion on one PR and nothing else. Nothing enforces it.

**`dev` is in breach of it right now**, and nobody noticed:

| surface | circuit-breaker | guide | combined |
|---|---|---|---|
| baseline when the gate was set | 4,506 | 36,851 | **41,357** ← the gate |
| **`dev` today** | 4,506 | **36,986** | **41,492** ← **135 B over** |
| PR #15307's head | 2,207 | 36,544 | **38,751** ← passes, 2,606 B headroom |

The guide drifted **+135 B** after the baseline was measured, silently, and put `dev` past its own boundary. #15307 happens to bring it back under — but nothing stops the next guide edit from re-breaching it, and the next drift will be exactly as invisible as the one that already happened.

## Why this is worth machinery

#15257's whole thesis is that voluntary discipline fails under load, and that the fix is a *deterministic* refusal rather than a documented norm — that is precisely why `state-(b)` ("semantic blocker, **converging**") had to be deleted by name: it was a subjective escape that "authorized the loop it was meant to cap."

Leaving that ticket's one measurable boundary as a voluntary per-PR check reproduces the same shape one layer up. The +135 B drift is the proof: the honour system already lost once, quietly.

Verified there is no existing guard: `agent-pr-review-body-lint.yml` lints review **body** structure (micro-delta anchors), not file sizes; `41357` appears nowhere in code or CI (`git grep '41357' -- ':!*.md'` → only coincidental digits in `apps/devindex/resources/data/users.jsonl`).

## Acceptance Criteria

- [ ] CI fails when `.agents/skills/pr-review/audits/review-cost-circuit-breaker.md` + `.agents/skills/pr-review/references/pr-review-guide.md` combined exceed the gate — measured in bytes, reported as `actual / limit / headroom` so a near-miss is visible before it is a breach.
- [ ] The limit lives in **one** named place, cited by the guide rather than duplicated into it — a second copy of the number is a duplicated primitive that drifts (the failure this ticket is about).
- [ ] Red-then-green: the check fails against a deliberately over-budget fixture and passes at current head. A guard that cannot be shown failing is not a guard.
- [ ] The `dev` breach is resolved — either by #15307 merging (which lands 38,751 B) or, if that changes, by an explicit re-baseline with the new number recorded and justified.
- [ ] **Decide the boundary's scope explicitly**: is the gate about *these two files* or about *the pr-review skill's loaded surface*? If the latter, `SKILL.md` belongs in the sum and the number changes. Do not widen it silently.

## Out of scope

- Re-baselining the gate upward to accommodate drift. That inverts the point. If the limit is wrong, that is a decision with a rationale, not a CI edit.
- A general loaded-surface budget across all skills. Real, and much larger; this ticket only guards the boundary #15257 already set.

## Evidence

- `git show origin/dev:.agents/skills/pr-review/references/pr-review-guide.md | wc -c` → **36,986** (baseline 36,851).
- `git show 38018bb539ce:…` → circuit-breaker 2,207 + guide 36,544 = **38,751**.
- #15257 AC: *"Merge-eligibility byte gate: the combined two-file boundary is < 41,357 B after the replacement."*
- PR #15307 review (approved): the measurement table + this finding.

## Provenance

Surfaced while reviewing #15307 as the cross-family gate. I measured the wrong pair first (`SKILL.md` + guide, a comfortable pass) before reading #15257's AC and finding it names `circuit-breaker + guide` — both surfaces were green, which is exactly why the gate needs a machine that knows which two files it means.

Related: #15257 (set the gate) · PR #15307 (passes it, and is why the drift became visible)

## Timeline

- 2026-07-16T20:50:30Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-07-16T20:50:31Z @neo-opus-grace added the `enhancement` label
- 2026-07-16T20:50:31Z @neo-opus-grace added the `developer-experience` label
- 2026-07-16T20:50:31Z @neo-opus-grace added the `ai` label
- 2026-07-16T20:58:57Z @neo-opus-grace cross-referenced by PR #15307
- 2026-07-16T21:05:59Z @neo-gpt cross-referenced by #15309
- 2026-07-16T21:07:11Z @neo-opus-grace cross-referenced by PR #15310
- 2026-07-16T21:25:02Z @neo-opus-grace cross-referenced by PR #15311
- 2026-07-16T22:50:23Z @neo-opus-grace referenced in commit `1329b43` - "fix(build): enforce the graduated boundary strictly and cite the owner from the guide (#15308)

@neo-gpt-emmy's RA-1 and RA-2, both real, both repaired once.

RA-1 — the gate is `< 41,357 B` and equality was passing. The graduating decision reads
"the combined two-file boundary is `< 41,357 B`", and that number IS the pre-existing
baseline (4,506 + 36,851) the surface had to get *below* — so landing exactly on it is
the breach, not the last legal state. This ticket's own AC paraphrased it as "exceed",
and I implemented the paraphrase; the decision that set the number wins on drift.

Her phrasing of the defect is the one worth keeping: **the green test certified the
opposite boundary at equality.** The spec was named "exactly at the limit passes — the
contract is < limit" — stating the rule correctly and asserting its violation in the
same breath. That is worse than an untested boundary, because it looks like coverage.
The suite did not catch it; a reviewer reading the AC did.

The largest legal sum is now derived once as `limitBytes - 1` and the verdict, the
headroom and the overage all compare against that single relation:

- equality at 41,357 now FAILS (was: passed)
- headroom counts bytes an author may still ADD — 2,328 today, not 2,329 against an
  exclusive gate. "headroom 1" must not mean "the next byte fails".
- the overage is measured against the largest legal sum, so the historical breach reports
  OVER by 136, not 135. The +135 in the ticket's narrative is the guide's own growth
  (36,851 → 36,986) — an adjacent quantity that happened to coincide while the
  circuit-breaker file was unchanged.

Red-then-green by stashing the fix: all three assertions fail on the prior code
("EXACTLY at the limit FAILS", "OVER by 136", "headroom 2605"). A new spec pins one byte
UNDER the limit passing at headroom 0 — the boundary is now covered from both sides.

RA-2 — the guide now cites the canonical owner (`COMBINED_BUDGETS` /
`ai:check-substrate-size`) without repeating the number, closing the "one named place,
cited not duplicated" AC.

Worth recording: my first draft of that citation was a full paragraph and cost 682 B —
26% of the remaining headroom, spent documenting the budget it consumes. A pointer that
eats a quarter of the gate's slack is self-defeating, and Emmy asked for concise. The
shipped version costs 277 B.

7/7 specs green; the guard passes on its own repair at 39,028 B, headroom 2,328.

Refs #15308"
- 2026-07-16T23:05:46Z @neo-opus-grace referenced in commit `81c0ebd` - "fix(build): compress the §6.3 byte-gate pointer under the skill-growth cap (#15308)

@neo-gpt-emmy's terminal item 1. The pointer added for RA-2 grew the guide by 277 B and
tripped `lint-skill-manifest` twice over — the 250 B per-file oversized-workflow-map delta
AND the 250 B net skill-Markdown growth budget — by exactly 27 B each.

Recursive in a way worth recording: a citation documenting a byte budget breached a
different byte budget. The lint's own advice is the right one — a trigger pointer should
be pointer-sized — so the prose is compressed rather than justified via
`[skill-growth-justified]`. An escape hatch for 27 B would spend the exception on the one
case that does not need it.

Both gates now green: `lint-skill-manifest` OK, and the surface it points at sits at
38,977 B with headroom 2,379.

Refs #15308"
- 2026-07-17T04:47:55Z @neo-opus-grace referenced in commit `4dfb08e` - "fix(test): the guard's own spec built a shell command from an env-derived path (#15308)

CodeQL flagged this on the PR seven hours ago and neither the author (me) nor the
reviewer read it: `js/shell-command-injection-from-environment` at
CheckSubstrateSize.spec.mjs:57. The `CodeQL` and `Analyze (javascript)` checks both
report `pass` while the alert is posted as an inline review comment — green means the
analysis RAN, not that it found nothing. I read "checks green" as "clean" all night.

    execSync(`node ${scriptPath}`)   // scriptPath derives from import.meta.url

The finding is real, not an abstraction. Demonstrated against a path with a space:

    OLD execSync(`node ${p}`)       -> Cannot find module '.../scratchpad/has'
    NEW execFileSync(execPath,[p])  -> guard ran

The shell re-splits the interpolated path, so any checkout under a directory containing
a space or a shell metacharacter breaks. `execFileSync` with an argv array never invokes
a shell; `process.execPath` also pins the child to the Node binary running the suite
rather than whichever `node` is on PATH. This is the pattern PullRequestService.spec.mjs
already uses.

Impact stated accurately: BOTH arms fail loudly on such a checkout — the FAIL arm asserts
`EXCEEDS` and `OVER by 136 bytes`, the PASS arm asserts status 0, and a MODULE_NOT_FOUND
exit satisfies none of them. I first wrote that the status-1 assertion would make the FAIL
arm go green for the wrong reason; the output assertions refute that. It is a false RED on
a space-containing checkout plus a real shell-injection surface, not a false green.

7 passed."
- 2026-07-17T05:29:43Z @tobiu referenced in commit `59da047` - "fix(build): guard the graduated pr-review byte budget mechanically (#15308) (#15310)

* fix(build): guard the graduated pr-review byte budget mechanically (#15308)

#15257 graduated a merge-eligibility boundary — `review-cost-circuit-breaker.md`
+ `pr-review-guide.md` < 41,357 B — and it lived only as prose in a ticket. So it
drifted: the guide grew +135 B after the baseline was measured, silently, and dev
sat at 41,492 B over its own gate until #15307 happened to bring it back under.
Nothing failed, because nothing was watching.

This EXTENDS the existing guard rather than adding one. `substrate-size-guard`
already enforces byte budgets; it simply had no way to express this shape. Its
model is per-file (24 KiB, the Antigravity truncation limit), and a per-file limit
cannot say "these two files are read together, so their SUM is the cost." That gap
is why the boundary had nowhere mechanical to live. `COMBINED_BUDGETS` gives it
one, and #15308's own AC asked for exactly that: the limit in ONE named place, not
duplicated into the guide it measures.

The workflow's `paths:` now include both budgeted files. Without that the guard
would never RUN on a guide edit — and a guide edit IS the drift that happened. A
correct check that never fires on the change it exists to catch is not a guard.

Fails closed on a missing member: a budget that quietly drops a renamed file
measures a fiction and passes green.

Reports headroom, not just a verdict. The drift is gradual, so the shrinking margin
is the signal; by the time the verdict flips the substrate is already broken. dev
today: 38,751 B, headroom 2,606.

The limit is NOT re-baselined. It belongs to the decision that graduated it, and
raising it to make CI pass would invert the point. The failure text says so
explicitly, because the next person under time pressure will reach for the number
first.

Specs pin both arms and both directions: the historical breach reproduced exactly
(4,506 + 36,986 → OVER by 135), the repaired state passing with headroom, one byte
over fails, exactly at the limit passes, a missing member fails closed, and the
per-file arm still fails independently. They run against a synthetic root — asserting
on real file sizes would flip red the day someone edits the guide, which is
drift-coupling rather than a test.

Evidence: 6/6 green. Red-then-green confirmed by stashing the arm — 4 of the 6 fail
without it; the 2 that survive are the per-file arm and the at-limit case, which
correctly hold either way.

Resolves #15308 · Refs #15257, #15307

* fix(build): enforce the graduated boundary strictly and cite the owner from the guide (#15308)

@neo-gpt-emmy's RA-1 and RA-2, both real, both repaired once.

RA-1 — the gate is `< 41,357 B` and equality was passing. The graduating decision reads
"the combined two-file boundary is `< 41,357 B`", and that number IS the pre-existing
baseline (4,506 + 36,851) the surface had to get *below* — so landing exactly on it is
the breach, not the last legal state. This ticket's own AC paraphrased it as "exceed",
and I implemented the paraphrase; the decision that set the number wins on drift.

Her phrasing of the defect is the one worth keeping: **the green test certified the
opposite boundary at equality.** The spec was named "exactly at the limit passes — the
contract is < limit" — stating the rule correctly and asserting its violation in the
same breath. That is worse than an untested boundary, because it looks like coverage.
The suite did not catch it; a reviewer reading the AC did.

The largest legal sum is now derived once as `limitBytes - 1` and the verdict, the
headroom and the overage all compare against that single relation:

- equality at 41,357 now FAILS (was: passed)
- headroom counts bytes an author may still ADD — 2,328 today, not 2,329 against an
  exclusive gate. "headroom 1" must not mean "the next byte fails".
- the overage is measured against the largest legal sum, so the historical breach reports
  OVER by 136, not 135. The +135 in the ticket's narrative is the guide's own growth
  (36,851 → 36,986) — an adjacent quantity that happened to coincide while the
  circuit-breaker file was unchanged.

Red-then-green by stashing the fix: all three assertions fail on the prior code
("EXACTLY at the limit FAILS", "OVER by 136", "headroom 2605"). A new spec pins one byte
UNDER the limit passing at headroom 0 — the boundary is now covered from both sides.

RA-2 — the guide now cites the canonical owner (`COMBINED_BUDGETS` /
`ai:check-substrate-size`) without repeating the number, closing the "one named place,
cited not duplicated" AC.

Worth recording: my first draft of that citation was a full paragraph and cost 682 B —
26% of the remaining headroom, spent documenting the budget it consumes. A pointer that
eats a quarter of the gate's slack is self-defeating, and Emmy asked for concise. The
shipped version costs 277 B.

7/7 specs green; the guard passes on its own repair at 39,028 B, headroom 2,328.

Refs #15308

* fix(build): compress the §6.3 byte-gate pointer under the skill-growth cap (#15308)

@neo-gpt-emmy's terminal item 1. The pointer added for RA-2 grew the guide by 277 B and
tripped `lint-skill-manifest` twice over — the 250 B per-file oversized-workflow-map delta
AND the 250 B net skill-Markdown growth budget — by exactly 27 B each.

Recursive in a way worth recording: a citation documenting a byte budget breached a
different byte budget. The lint's own advice is the right one — a trigger pointer should
be pointer-sized — so the prose is compressed rather than justified via
`[skill-growth-justified]`. An escape hatch for 27 B would spend the exception on the one
case that does not need it.

Both gates now green: `lint-skill-manifest` OK, and the surface it points at sits at
38,977 B with headroom 2,379.

Refs #15308

* fix(test): the guard's own spec built a shell command from an env-derived path (#15308)

CodeQL flagged this on the PR seven hours ago and neither the author (me) nor the
reviewer read it: `js/shell-command-injection-from-environment` at
CheckSubstrateSize.spec.mjs:57. The `CodeQL` and `Analyze (javascript)` checks both
report `pass` while the alert is posted as an inline review comment — green means the
analysis RAN, not that it found nothing. I read "checks green" as "clean" all night.

    execSync(`node ${scriptPath}`)   // scriptPath derives from import.meta.url

The finding is real, not an abstraction. Demonstrated against a path with a space:

    OLD execSync(`node ${p}`)       -> Cannot find module '.../scratchpad/has'
    NEW execFileSync(execPath,[p])  -> guard ran

The shell re-splits the interpolated path, so any checkout under a directory containing
a space or a shell metacharacter breaks. `execFileSync` with an argv array never invokes
a shell; `process.execPath` also pins the child to the Node binary running the suite
rather than whichever `node` is on PATH. This is the pattern PullRequestService.spec.mjs
already uses.

Impact stated accurately: BOTH arms fail loudly on such a checkout — the FAIL arm asserts
`EXCEEDS` and `OVER by 136 bytes`, the PASS arm asserts status 0, and a MODULE_NOT_FOUND
exit satisfies none of them. I first wrote that the status-1 assertion would make the FAIL
arm go green for the wrong reason; the output assertions refute that. It is a false RED on
a space-containing checkout plus a real shell-injection surface, not a false green.

7 passed."
- 2026-07-17T05:29:43Z @tobiu closed this issue

