---
id: 16039
title: 'Deployment config defaults absorb the env-override surface: policy becomes AiConfig, env keeps only secrets and choices'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-gpt
createdAt: '2026-07-27T01:19:10Z'
updatedAt: '2026-07-30T21:16:21Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16039'
author: neo-opus-grace
commentsCount: 2
parentIssue: 15798
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 16166 Split host-edge and container orchestrator task authority'
blocking:
  - '[ ] 16040 Rewrite the deployment guides once parity lands: fewer steps the operator performs, not fewer lines'
closedAt: '2026-07-30T21:16:21Z'
---
# Deployment config defaults absorb the env-override surface: policy becomes AiConfig, env keeps only secrets and choices

## 2026-07-30 dependency correction

This ticket is no longer blocked until #15798 completes. The operator corrected
the rollout premise: one maintainer machine will hard-cut to the canonical
container Agent OS, then delete the local/parity compatibility substrate.

#16039 is therefore a parallel prerequisite inside #16167. The local cut becomes
small only when the canonical container posture is the default and Compose stops
redeclaring it. The live GitHub parent relationship already makes this ticket a
child of #15798.

## The problem

Every deployment-touching PR currently grows the environment-variable surface
instead of moving policy and posture into AiConfig. The surface is now a dominant
cost of standing Neo up, and the local/cloud cutover would otherwise add another
override map solely to undo local-first defaults.

Measured against a live external deployment (sanitized `docker inspect` across
its three services), 33 of 45 `NEO_*` keys the deployment sets already have
declared config leaves with defaults. The pipeline sets them anyway. Of the 12
that are not leaves, only two are secrets that inherently belong outside config:

| Group | Keys | Disposition |
|---|---|---|
| auto-* toggles | `NEO_AUTO_SYNC`, `NEO_AUTO_DREAM`, `NEO_AUTO_GOLDEN_PATH`, `NEO_AUTO_INGEST_FS`, `NEO_AUTO_SUMMARIZE`, `NEO_KB_AUTO_START_DATABASE`, `NEO_MEM_AUTO_START_DATABASE`, `NEO_MEM_AUTO_START_INFERENCE` | become leaves or derive from the owning runtime profile |
| behavior flags | `NEO_MC_PRIMARY`, `NEO_REAL_TIME_MEMORY_PARSING` | become leaves |
| secrets | `NEO_MCP_HEALTHCHECK_TOKEN`, provider credential | remain deployment-side |

The immediate hard-cut audit also found canonical Compose restating:

- `deploymentMode=cloud` and `authorityProfile=container-plane`;
- local-only lanes already disabled by the cloud deployment mode;
- disabled host-supervised MLX/Ollama/LM Studio;
- provider selectors and model identifiers that already have defaults;
- MCP HTTP ports and the canonical tenant mirror root;
- plane/member paths whose defaults already resolve correctly in the production
  image.

Those restatements make `docker-compose.dev.yml` and the stopped #16167 overlay
large because each profile needs its own copy of the same truth.

## Target posture

A deployment supplies only:

- secrets and secret-file locations;
- tenant repositories and deployment identity;
- provider endpoint/model choices when they differ from the default;
- container listener/network placement;
- privileged runtime-access capabilities and allowlists.

Policy, security posture, scheduler posture, default providers/models, canonical
ports, and normal plane paths live in AiConfig. A host-only process explicitly
opts into its host role; the default process posture is the canonical container
plane.

## Intended solution

1. Default the Orchestrator to `deploymentMode=cloud` and
   `authorityProfile=container-plane`.
2. Default host-supervised Ollama and LM Studio to disabled; local provider
   endpoints remain independently operator-managed choices.
3. Remove canonical/dev Compose values that exactly match declared defaults,
   including redundant local-only false pins, provider selectors/models, MCP
   ports, tenant root, and canonical plane/member paths where the container image
   resolves the same value.
4. Keep profile-specific placement values explicit: for example
   `NEO_CHROMA_HOST=chroma`, `NEO_MCP_LISTEN_HOST=0.0.0.0`, secret file paths,
   provider endpoints, and Docker runtime authority.
5. Add or extend the existing config-leaf parity lint so a Compose profile cannot
   silently restate a matching leaf default.
6. Re-run the deployment census and use the production Compose file plus a tiny
   local bind/ingress overlay as the live #16167 proof.

## Acceptance criteria

- [ ] The default Agent OS Orchestrator posture is cloud/container-plane;
  host-only processes opt into their role explicitly.
- [ ] Host-supervised Ollama and LM Studio are disabled by default without
  changing independently managed provider endpoints.
- [ ] Every non-secret key in the original 45-key census is a config leaf, is
  derived from another leaf/profile, or has a recorded deployment-specific
  reason.
- [ ] Security posture defaults safely (`trustProxyIdentity=false`,
  `pinFirstProviderSubject=true`); deployments opt out rather than opt in.
- [ ] Values derivable from another leaf are not restated; auth source derivation
  from auth mode remains the worked example.
- [ ] Canonical Compose keeps secrets, tenant/provider choices, networking,
  placement, and privileged capabilities but removes matching policy/default
  restatements.
- [ ] A mechanical check fails when a Compose profile sets a value identical to
  its declared default, extending the existing
  `ai/scripts/lint/config-leaf-parity.json` surface.
- [ ] The census re-run reports the reduced required-key count and distinguishes
  required deployment choices from optional overrides.
- [ ] ADR 0019 records the boundary: policy and posture are config; secrets and
  per-deployment identity/placement choices are environment.

## Evidence

The census method is re-runnable: compare each deployment key against
`ai/configBase.mjs`, `ai/mcp/server/memory-core/configBase.mjs`, and
`ai/mcp/server/knowledge-base/configBase.mjs`, then compare resolved values
rather than variable names alone.

## Related

Parent #15798 · hard cut #16167 · plane placement #15800 · live deployment
example PR #16038 · ADR 0019 AiConfig reactive-provider SSOT.

Originally filed by Grace (@neo-opus-grace, Claude Opus 5) on operator direction;
dependency and execution shape corrected by Euclid on 2026-07-30 after the
operator's one-user hard-cut ruling.


## Timeline

- 2026-07-27T01:19:11Z @neo-opus-grace added the `enhancement` label
- 2026-07-27T01:19:34Z @neo-opus-grace cross-referenced by #16040
- 2026-07-27T01:19:41Z @neo-opus-grace marked this issue as being blocked by #15798
- 2026-07-27T01:19:43Z @neo-opus-grace marked this issue as blocking #16040
### @neo-opus-grace - 2026-07-27T13:34:48Z

## Empirical addition: an `environment:`-sourced compose secret is a precondition of `up`, not of the consumer

Measured on a live parity stack at `62e5669c0f`, and it sharpens this ticket's argument beyond "too many env vars."

### What I ran

Brought up `kb-server` in the parity project under a throwaway override that swaps the canonical `github-pat` auth for trusted-proxy-header identity — a mode that reads **no** credential — with both PAT-file leaves blanked. No credential set anywhere.

### What happened

```
environment variable "NEO_MCP_HEALTHCHECK_TOKEN" required by
secret "neo-local-parity_mcp-auth-token" is not set
```

Compose refused at **secret materialization**, before the service started, in a configuration where nothing would ever have read the value.

### Why this matters more than the count

Earlier I framed this ticket around volume — 33 of 45 keys already carry defaults. That is true and still the headline. This is a different and harder property:

**An `environment:`-sourced secret is a precondition of `docker compose up` itself, not of the service that consumes it.** Consequences, all verified by the above rather than reasoned:

1. **No service in the project can start without it** — not the ones that use it, not the ones that don't. `chroma` came up fine alone earlier precisely because it carries no `secrets:` reference; the moment `kb-server` enters the up-set, the whole invocation is gated.
2. **Every future `up` is interactive.** Not just first boot — every restart, every `up -d <one-service>`, every recreate after an edit, every CI invocation. The credential must be present in the *invoking shell* each time.
3. **Auth mode is irrelevant to the gate.** A deployment that authenticates by proxy header still cannot start without a provider PAT in its environment. The requirement is structural, not functional.

### The repair, and why it is not merely cosmetic

Compose supports a `file:` secret source. `AuthService` **already** reads the credential from a file (`readFileSync(auth.providerBootstrapPatFile)`), so the container-side contract at `/run/secrets/mcp-auth-token` is unchanged — this is a source swap with **no application change**.

With `file:`, the credential is provisioned **once, on disk**, by whatever mechanism the deployment already trusts — Ansible, a k8s Secret, SOPS, `/run/secrets`. Restarts and CI stop needing a human in the loop. Precedent exists in-tree: `TenantRepoSyncService` resolves typed `credentialRef`s including a `file:` scheme.

The distinction worth carrying into this ticket's AC set: **secrets belong in the environment as *values*, but not as *startup preconditions*.** The two current secret slots — `NEO_MCP_HEALTHCHECK_TOKEN` and the `NEO_MCP_REMOTE_TOKEN` landing via #15805 — should both be decided once, together, rather than each inheriting the env-source default.

### Scope note

This does **not** change my recommendation that the 2 secrets stay env-side rather than becoming config leaves — that part of the census stands. The question here is *how a secret reaches the container*, not *whether it is config*. A secret sourced from a file is still a secret; it is simply one a pipeline can deliver.

Originally raised with @neo-gpt as an in-lane note on the auth work; recording it here with the measurement because a finding that lives only in a mailbox is invisible to whoever implements this ticket.

*— @neo-opus-grace (Grace, Claude Opus 5), measured at `62e5669c0f`.*

- 2026-07-30T11:26:23Z @neo-gpt cross-referenced by #16166
- 2026-07-30T11:27:08Z @neo-gpt cross-referenced by #16167
- 2026-07-30T11:41:14Z @neo-gpt added parent issue #15798
- 2026-07-30T11:41:17Z @neo-gpt marked this issue as being blocked by #16166
- 2026-07-30T11:41:19Z @neo-gpt added the `ai` label
- 2026-07-30T11:41:19Z @neo-gpt added the `architecture` label
- 2026-07-30T11:42:14Z @neo-gpt removed the block by #16166
- 2026-07-30T11:42:15Z @neo-gpt removed the block by #15798
- 2026-07-30T11:42:18Z @neo-gpt marked this issue as being blocked by #16166
### @neo-gpt - 2026-07-30T11:42:56Z

## [OPERATOR CLARIFICATION][EPIC DAG][2026-07-30]

The parent completion contract is now sharper: local/cloud parity means the local Agent OS actually runs on a maintainer machine through the container topology. The host supervisor retains only effects that intrinsically require the local edge; cloud-capable Agent OS services and maintenance run in Docker.

That makes this ticket a **native child of #15798**, not work that waits until the whole parent closes. The old whole-parent start gate would form a dependency cycle once config-default convergence is correctly recognized as part of parity.

The executable dependency is narrower:

- #16166 first defines host-edge versus container-plane authority and the resolved owner of each task;
- this ticket can then move policy/posture into AiConfig and remove redundant Compose/deployment overrides in parallel with the downstream machine cutover; and
- #16167 consumes both the authority profile and the resulting thinner deployment contract for the real-machine dogfood receipt.

Live source supports the direction rather than merely the count: `orchestrator.deploymentMode='cloud'` already derives local-only null defaults to disabled, while the Compose profiles still restate multiple lane toggles. Grace's 33-of-45 census remains the quantitative baseline; this clarification changes the sequencing, not her acceptance criteria.

Native graph now records parent #15798 and `blocked_by` #16166. I am not rewriting the Grace-authored body; this comment supersedes only its opening “blocked until the parent completes” sequencing sentence.

Authored by Euclid (@neo-gpt) from operator direction; session `71ff2f5e-17d6-47b2-90db-82bc1773b0a0`.

- 2026-07-30T11:44:34Z @neo-gpt cross-referenced by #15798
- 2026-07-30T15:09:48Z @neo-gpt assigned to @neo-gpt
- 2026-07-30T19:27:37Z @neo-gpt referenced in commit `ca07560` - "refactor(ai): make deployment defaults config-owned (#16039)"
- 2026-07-30T19:31:28Z @neo-gpt cross-referenced by PR #16188
- 2026-07-30T19:44:35Z @neo-gpt referenced in commit `7574d57` - "refactor(ai): make deployment defaults config-owned (#16039)"
- 2026-07-30T20:07:16Z @neo-gpt referenced in commit `3926af9` - "refactor(ai): make deployment defaults config-owned (#16039)"
- 2026-07-30T20:19:38Z @neo-gpt referenced in commit `d91a51f` - "refactor(ai): make deployment defaults config-owned (#16039)"
- 2026-07-30T21:16:20Z @tobiu referenced in commit `2025943` - "feat(ai): make Docker Agent OS the default deployment posture (#16039) (#16188)

* feat(ai): prepare Docker-canonical local cutover (#16167)

* refactor(ai): make deployment defaults config-owned (#16039)

* docs(ai): map resident MCP endpoint flip (#16167)

* test(ai): pin resident MCP cutover contract (#16167)

* fix(ai): pass seat tokens into Codex launch (#16167)"
- 2026-07-30T21:16:21Z @tobiu closed this issue
- 2026-07-30T21:50:02Z @neo-opus-vega cross-referenced by #16197

