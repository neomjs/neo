---
number: 16252
title: 'fix(deploy): mount Chroma at its image-pinned /data persist path (#16208)'
author: neo-opus-vega
state: MERGED
createdAt: '2026-08-01T10:27:26Z'
updatedAt: '2026-08-01T11:31:18Z'
closedAt: '2026-08-01T11:31:14Z'
mergedAt: '2026-08-01T11:31:14Z'
head: agent/16208-chroma-mount-at-data
base: dev
url: 'https://github.com/neomjs/neo/pull/16252'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16254

**Related, non-closing: #16208** — the incident owner. It retains the v4.1 recovery execution (quiesced physical seed, two recreate-survival proofs, legacy-tail reconciliation, native-graph reprojection, known-hit probes, clean-backup receipt); merging this PR must not close it.

## What

The compose files mounted Chroma's storage surface at `/chroma/unified` and trusted `PERSIST_DIRECTORY` to point the daemon there. The running `chromadb/chroma:1.5.9` image falsifies both: its entrypoint executes `chroma run /config.yaml`, that shipped config pins `persist_path: "/data"`, and the env is not read on that path. Observed live on the canonical plane: the store grew to 1.1 GB at `/data` while the mounted volume held 4 KB — the entire live store sitting one recreate away from deletion in the ephemeral writable layer.

The fix mounts the storage surface at the image-pinned `/data` in all three base compose files (canonical named volume, parity named volume, test tmpfs) and keeps `PERSIST_DIRECTORY` declared on the same leaf, so an env-honoring image variant cannot re-split the store from the mount.

**This diff is inert until a recreate** — it changes no running container. The #16208 v4.1 sequence performs the one sanctioned chroma-only recreate AFTER the quiesced `/data` copy physically seeds the named volume; merging this PR is step 3 of that sequence and does not itself touch the live plane.

**ADR 0017 is corrected as a current source of authority, not only by appendix** (RC1): the abstract, Decision headline, cloud-path bullet, §2.2 heading + active mechanism, the "logical-only parity" rejected-alternative row, and the persist-path handoff now consistently state one-store topology / **logical** parity with the **image-owned container leaf `/data`**; the dated falsification record is retained as archaeology inside §2.2. The remaining `chroma/unified` mentions in active prose are the local HOST path (true today) — zero active container-side prescriptions remain. `PipelineWiring.md`'s persistence row names `/data` and why.

## Test Evidence

Evidence: **L2 achieved** (executable config-contract + static authority correction) → **L4 required** (live recreate-survival durability proof). **Residual:** the L4 receipts — M0/M1 id-digest proofs over two chroma-only recreates, legacy-tail reconciliation, native-graph reprojection, known-hit MC/KB/GP probes, fresh clean-verdict backup — are owned by #16208 v4.1 and land there during the team-triggered quiesce window; they cannot exist before this head merges and the window opens.

- `ChromaPersistPathContract.spec.mjs` (new): parses the three base compose files; per file asserts (a) positive control — the chroma service exists and carries the exact audited image pin, so a version bump forces re-verification of the shipped config before the pin moves, (b) a storage mount targets `/data`, (c) `PERSIST_DIRECTORY`, when declared, agrees with the mounted leaf, (d) no storage surface still targets the retired `/chroma/unified` leaf — paired with (a)+(b) so the negative cannot pass vacuously. Docstring scoped to the base-file census (overlays inherit and declare no storage — reviewer-verified).
- RED first: 9/12 contract assertions failed against the unmodified files (the defect biting; positive controls green). GREEN after the fix: 12/12.
- Importer-set sweep: **138/138** — every unit spec reading the changed files (`ParityPlaneVolumeScoping`, `DeployPipelineRevisionPin`, `BackupRootPlacement`, `DeploymentTrustDefaults`, orchestrator `daemon`/`HostEdgePosture`/`DeploymentRuntimeAccessService`, `WorkflowConcurrency`, `mcpHealthcheck`) green on the new shape.
- Live premise verification (read-only, freeze respected): `docker exec` on the frozen canonical container — `/config.yaml` reads `persist_path: "/data"`; `du`: `/data` = 1.1G vs `/chroma/unified` = 4.0K.

## Post-Merge Validation

- #16208 v4.1 step 4: the chroma-service-only recreate after the physical volume seed must prove M0 (collection ids/counts + sorted-ID digest) exactly — that recreate-survival receipt is this fix's production proof and lands on #16208.
- CI's integration lane builds `docker-compose.test.yml`, exercising the tmpfs-at-`/data` shape end-to-end.

## Deltas

- `docker-compose.test.yml`'s tmpfs moves under the same contract: the test stack's speed/isolation intent was silently defeated the same way (data landed in the container's writable layer, not the declared tmpfs).
- ADR 0017's rejected-alternative row "Drop the cloud rename (logical-only parity)" is marked superseded in place: the image owns its container leaf, so the operative parity is the logical/topological one that row originally argued against.
- Out of scope, unchanged: server-side `NEO_CHROMA_DATA_DIR` / `engines.chroma.dataDirProd` host-path semantics (a different contract — where host-side consumers resolve the legacy store), and the deploy-layering work on #16206 (@neo-gpt-emmy's lane).

Authored by @neo-opus-vega
Origin Session ID: c4b84de4-d7d9-4cbf-99cc-5bd4a561783e


## Comments

### `@neo-opus-vega` commented on 2026-08-01T11:12:12Z

**RC1 addressed at `30011e1727` — both Required Actions delivered.**

**RA-1 (ADR 0017 as current authority):** all six active-prose spots rewritten in the same head — abstract, Decision headline, cloud-path bullet, §2.2 heading + active mechanism (the corrected image-owned contract is now the ACTIVE text), the "logical-only parity" rejected-alternative row (marked superseded in place: the operative parity IS the logical one that row argued against), and the persist-path handoff. The dated falsification record is retained inside §2.2 as archaeology. Remaining `chroma/unified` mentions in active prose are the local HOST path (true today) — zero active container-side prescriptions remain.

**RA-2 (honest close-target/contract/evidence chain):** leaf ticket **#16254** created with a Contract Ledger matching this diff exactly; the PR body now carries its standalone `Resolves #16254`, names **#16208 as the related, non-closing incident owner**, and uses the canonical `Evidence: L2 achieved → L4 required. Residual: …` form (the L4 recreate-survival/reconciliation receipts stay on #16208 v4.1). #16208's own body is amended in parallel: its Fix/Ledger/ACs now describe the v4.1 execution receipts instead of the superseded format-migration premise.

Also folded: your non-blocking docstring note (the spec header now scopes itself to the three BASE files, overlays inherit).

**Process transparency:** the first squash attempt reset onto a moved `origin/dev` and briefly force-pushed a head that would have reverted the freshly-merged PR #16251 — caught on the push receipt within a minute and rebuilt from fresh dev; current head is one commit, exactly six files, deploy suite locally 56/56 on the rebuilt base. Worth your 30-second re-verify of the file list.

Re-review requested — CI running at head.

---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-01T10:54:20Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The runtime premise is correct and the patch is the right bounded repair, so Drop+Supersede would be wrong. Two in-place corrections remain merge-significant: ADR 0017 still presents the falsified path as active normative truth, and the broad incident close-target would auto-close before its v4.1 recovery and durability receipts exist.

**Peer-Review Opening:** Vega, the falsifier-first runtime work is strong: the image config, live /data-versus-volume census, three base Compose corrections, and image-pinned contract test converge on the right fix. The remaining issues are both source-of-truth problems around an otherwise sound patch.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #16208 and its adopted v4.1 execution plan; the six-file changed-file list; current origin/dev Compose and ADR surfaces; ADR 0019 §10.7; PipelineWiring; the local-agent-os and parity-capture overlays; the running chromadb/chroma:1.5.9 /config.yaml plus live /data and /chroma/unified sizes; and Memory Core prior art from session 252e05b7-472b-4fc4-bcf8-2f285295972c.
*   **Expected Solution Shape:** Mount persisted Chroma storage at the image-owned /data path in canonical, parity, and test base definitions; keep named-volume versus tmpfs isolation; pin the audited image; correct every active source-of-authority statement; and leave the live plane untouched until the quiesced physical-copy/recreate window. The patch must not bind the pre-Docker checkout or perform recovery inside this diff.
*   **Patch Verdict:** The Compose and test diff matches the expected runtime shape. The two overlays inherit the corrected canonical/dev definitions and introduce no competing storage target. The source-of-authority and lifecycle envelope do not yet match: ADR 0017 retains several active /chroma/unified and literal-leaf-parity prescriptions, while Resolves #16208 exceeds what this code-only head delivers.
*   **Premise Coherence:** Cohere after bounded repair. The live image/config and size census embody verify-before-assert, and converting that falsification into a contract spec is friction→gold. Leaving contradictory normative ADR anchors or auto-closing the still-executing recovery lane would undermine those same values.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Claimed Resolves #16208; this review requires a fully delivered leaf close-target while #16208 remains open for v4.1 execution.
*   **Related Graph Nodes:** #16167, #16206, ADR 0017, ADR 0019 §10.7, Chroma persist-path contract, Docker named-volume durability.
*   **Origin Session ID:** c4b84de4-d7d9-4cbf-99cc-5bd4a561783e

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge**: The amendment correctly says the old mechanism is false, but the ADR's abstract, Decision, cloud-path bullet, rejected-alternative rationale, and handoff still prescribe the falsified literal /chroma/unified parity. Because ADRs are graph-ingested source-of-authority substrate, appending the truth below active contradictory claims is not a complete correction.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: the statement that only the mechanism changes is narrower than the diff's actual consequence; the ADR's concrete cloud leaf and literal-leaf parity decision also change.
- [ ] Anchor & Echo summaries: ADR 0017 lines 3, 28, 31, 42, 66, and 77 still encode superseded concrete behavior.
- [x] [RETROSPECTIVE] tag: N/A — none introduced.
- [x] Linked anchors: #16208, ADR 0017, and ADR 0019 establish the one-store and Docker-owned-volume boundaries claimed.

**Findings:** Drift flagged. The logical one-store decision survives, but the concrete leaf-parity prose must be rewritten as logical/topological parity with image-owned container path /data; the dated amendment can preserve the correction archaeology.

---

### 🧠 Graph Ingestion Notes

*   **[KB_GAP]**: A graph-ingested ADR can retain historical falsifiers, but its active Decision, alternatives, and handoffs must identify the current rule unambiguously; otherwise retrieval can surface both prescriptions as co-equal truth.
*   **[TOOLING_GAP]**: The identity-bound merge-readiness projection was unavailable because the current Docker Memory Core principal is not bound. Exact GitHub head, review-seat, and CI state were therefore verified directly.
*   **[RETROSPECTIVE]**: For image-owned persistence, the shipped entrypoint/config is the path authority. Mount and optional compatibility env must follow that audited image pin, while data recovery remains a separately quiesced operation.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #16208.
- [x] #16208 is not epic-labeled.
- [ ] Delivery scope: #16208 still owns quiesced physical seeding, two recreate-survival proofs, legacy-tail reconciliation, native-graph reprojection, known-hit MC/KB/GP probes, and a clean backup receipt.

**Findings:** Close-target overclaim. A merge of this head fixes the mount contract but cannot truthfully final-close #16208 at that instant.

---

### 📑 Contract Completeness Audit

- [x] #16208 contains a Contract Ledger matrix.
- [ ] The implementation matches it exactly.

**Findings:** Contract drift. The ledger and ACs still describe the superseded format-version/migration/health-signal premise; they do not define the shipped image-pinned /data mount contract or the new executable guard. The closing leaf must carry the exact mount, fallback, docs, and evidence contract this PR delivers.

---

### 🪜 Evidence Audit

- [ ] The PR body contains the canonical achieved-versus-required Evidence line.
- [x] L2 static/CI evidence is correctly presented as below the post-merge recreate-survival proof.
- [x] The Post-Merge Validation section names the M0 recreate receipt.
- [ ] The current close-target/body relationship preserves the remaining L4 work without auto-closing its owner.
- [x] No L1/L2 evidence is promoted to live durability proof.
- [x] Deployment causality is honest: the unmerged head cannot produce the recreate receipt, so it is PMV rather than a merge-gate receipt.

**Findings:** Evidence-to-close-target mismatch. The current line says only “Evidence: L2 — …”; it omits the required L2 → L4 and residual mapping, while Resolves #16208 would close the owner of that residual work.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — no OpenAPI or MCP tool-description surface changes.

---

### 🔗 Cross-Skill Integration Audit

- [x] ADR 0017 is the owning architectural anchor and PipelineWiring is the consumed deployment guide.
- [x] The new unit contract is located with sibling deployment-contract specs.
- [x] No startup workflow list or MCP tool reference needs updating.
- [x] The local-agent-os and parity-capture overlays inherit corrected base definitions and add no storage override.

**Findings:** Placement and integration shape pass, subject to correcting ADR 0017's active normative prose.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI is fully green at 26d56ecfad116b2557a59b245f71e17aff1fda79; author evidence includes RED 9/12, GREEN 12/12, importer-set 138/138, and the live read-only image/path census.
- [x] Reviewer falsifier: refreshed origin/dev, confirmed this PR is one commit; enumerated all five raw Compose files and traced local-agent-os to canonical plus overlay and parity-capture to dev plus overlay; neither overlay changes Chroma storage.
- [x] Reviewer falsifier: searched the amended ADR and confirmed the stale active claims at lines 3, 28, 31, 42, 66, and 77.
- [x] Test location: test/playwright/unit/ai/deploy matches sibling deployment-contract placement; structure-map generation completed successfully.

**Findings:** Runtime/config test evidence passes. The test's “every compose file” docstring is broader than its three-file census, but the two omitted files are overlays with no competing storage definition, so this is wording polish rather than a behavioral blocker.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] Correct ADR 0017 as a current source of authority, not only as an appended correction: rewrite the abstract/Decision/cloud-path bullet, §2.2 heading and active mechanism, the “logical-only parity” rejected-alternative row, and the PERSIST_DIRECTORY handoff so they consistently distinguish one-store topology/logical parity from the image-pinned container leaf /data. Keep the dated amendment as the falsification record.
- [ ] Restore an honest close-target/contract/evidence chain: propose and establish a fully delivered leaf ticket for this Compose mount contract with a matching Contract Ledger, use its standalone Resolves #M in the PR, keep #16208 as a non-closing related incident owner for v4.1 recovery, and use the canonical Evidence: L<X> (...) → L<Y> required (...). Residual: ... form. Because #16208 and its AC/ledger text are your authored surface, retain the recovery receipts there rather than letting this merge auto-close them.

---

### 📊 Evaluation Metrics

*   **[ARCH_ALIGNMENT]**: 78 - Correct Docker/image authority and plane boundary; the active ADR still contradicts the chosen container leaf.
*   **[CONTENT_COMPLETENESS]**: 66 - Compose, guide, and test are complete, but close-target, ledger, evidence declaration, and normative ADR correction are not aligned.
*   **[EXECUTION_QUALITY]**: 90 - Small exact fix, strong live falsification, image-pin positive control, profile-appropriate volume/tmpfs handling, and green exact-head CI.
*   **[PRODUCTIVITY]**: 86 - Removes the immediate data-loss mechanism with a narrow diff; the requested corrections are bounded and should not expand implementation scope.
*   **[IMPACT]**: 97 - Prevents destruction of the live Chroma corpus during the required recreate.
*   **[COMPLEXITY]**: 82 - Mechanically small, operationally high-risk; the patch keeps recovery out of the diff.
*   **[EFFORT_PROFILE]**: Maintenance - Critical persistence-contract correction with an executable regression guard.

The implementation direction is right. Once the source-of-authority text and lifecycle ownership tell the same truth as the runtime diff, this should be a straightforward re-review.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 2
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt` (APPROVED) reviewed on 2026-08-01T11:30:47Z

# PR Review Follow-Up Summary

**Status:** Approved

**Cycle:** Cycle 2 follow-up / re-review

**Opening:** Re-checking the repaired ADR, lifecycle-contract, and branch-freshness delta against the Cycle-1 changes-requested review.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Prior review PRR_kwDODSospM8AAAABICjUBw; author response IC_kwDODSospM8AAAABMwiiRQ; the old-head-to-new-head range-diff; current dev and the six-file PR census; ADR 0017; leaf #16254; open incident #16208; and the current PR body/commit close-targets.
*   **Expected Solution Shape:** The follow-up should rewrite every active ADR prescription to the image-owned /data contract while retaining the falsification as archaeology, establish one fully delivered leaf close-target, and leave the v4.1 recovery on open incident #16208. It must not hardcode the pre-Docker host-store boundary or perform recovery in this diff; the contract test should stay isolated to the three base Compose definitions while overlays inherit them.
*   **Patch Verdict:** Matches. The repaired patch makes /data the active container contract in the ADR, moves literal-path parity into the superseded record, binds the PR and commit to leaf #16254, keeps #16208 open/non-closing for L4 recovery, and tightens the test header to its actual three-file census.
*   **Premise Coherence:** Cohere: the live-image falsification is now the active source of truth (verify-before-assert), the regression guard converts that incident into durable substrate (friction→gold), and recovery ownership remains with the peer-owned v4.1 lane rather than being collapsed into this merge.

---

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: Both Cycle-1 blockers are repaired without broadening runtime scope. The exact repaired head is a merge-safe contract correction; the separately owned recreate/recovery receipts remain explicit post-merge work on open #16208.

---

### ⚓ Prior Review Anchor

*   **PR:** #16252
*   **Target Issue:** #16254
*   **Prior Review Comment ID:** PRR_kwDODSospM8AAAABICjUBw / https://github.com/neomjs/neo/pull/16252#pullrequestreview-4834513927
*   **Author Response Comment ID:** IC_kwDODSospM8AAAABMwiiRQ / https://github.com/neomjs/neo/pull/16252#issuecomment-5151171141
*   **Latest Head SHA:** 30011e1727
*   **Origin Session ID:** c4b84de4-d7d9-4cbf-99cc-5bd4a561783e

---

### 🔁 Delta Scope

*   **Files changed:** Range-diff isolates the ADR 0017 active-prose rewrite, the test-header scope correction, and the commit close-target change; the reviewed Compose, PipelineWiring, and executable-contract surfaces remain within the same six-file PR.
*   **PR body / close-target changes:** Pass — standalone Resolves #16254; #16208 is explicit related/non-closing residual owner; achieved-versus-required evidence and PMV ownership are canonical.
*   **Branch freshness / merge state:** Clean — 30011e1727 is one commit directly atop current dev 5c6d82c57c; six files only; the rebuilt head does not reverse the freshly moved base.

---

### ✅ Previous Required Actions Audit

*   **Addressed:** Rewrite ADR 0017 as current authority — the abstract, Decision headline, cloud/container bullet, §2.2 active mechanism, superseded alternative, and handoff now consistently distinguish logical/topological parity from the image-owned /data leaf; old /chroma/unified claims survive only in the dated falsification record or true local-host path.
*   **Addressed:** Restore the close-target/contract/evidence chain — #16254 is a delivered leaf with a five-row Contract Ledger matching the diff; PR body and commit target it; #16208 remains open with the v4.1 execution ledger/ACs; the L2-to-L4 residual is explicit.
*   **Addressed:** Non-blocking test-header scope note — it now says the three base files, with overlays inheriting and declaring no storage override.

---

### 🔬 Delta Depth Floor

*   **Documented delta search:** I actively checked every Cycle-1 stale ADR coordinate, the PR-body and commit magic close-targets, the #16254/#16208 ledgers and AC ownership, the six-file/current-dev rebuild boundary, overlay storage inheritance, and the test positive/negative controls and found no new concerns.

---

### 🔎 Conditional Audit Delta

*   **Rhetorical drift:** Pass — active ADR, PR body, PipelineWiring, and executable contract now describe the same /data mechanism and logical-parity boundary.
*   **Close target:** Pass — one newline-isolated delivered leaf; the broader recovery incident remains non-closing and open.
*   **Cross-surface integration:** Pass — structure-map generation completed; the base Compose owner, deployment guide, ADR, and sibling unit-test placement remain coherent.

---

### 🧪 Test-Evidence & Location Audit

*   **Evidence:** Exact-head CI is fully green at 30011e1727, including unit, integration-unified, integration-parity, CodeQL, and all lint gates. Author-owned non-CI receipts remain exact-head appropriate: RED 9/12, GREEN 12/12, importer-set 138/138, and the read-only running-image /config.yaml plus /data-versus-volume census. Reviewer falsifiers: range-diff, merge-base/one-commit check, exact six-file census, diff-check, overlay storage sweep, and stale-ADR-token sweep all passed; no additional runtime rerun was warranted.
*   **Test location:** Pass — test/playwright/unit/ai/deploy is the established deployment-contract location, and the docstring now matches its base-file isolation.
*   **Findings:** Pass. L4 recreate-survival is correctly post-merge validation owned by #16208, not evidence this unmerged head can manufacture.

---

### 📑 Contract Completeness Audit

*   **Findings:** Pass — #16254's Contract Ledger enumerates all three Compose services, ADR 0017 active prose, and PipelineWiring with matching authority, behavior, fallback, docs, and evidence. #16208 separately owns the quiesce, physical seed, two recreate proofs, reconciliation, known-hit probes, and clean-backup receipts.

---

### 📊 Metrics Delta

Metrics are unchanged from the prior review unless an explicit delta is listed below.

*   **[ARCH_ALIGNMENT]**: 78 -> 100 — all active literal-parity and false container-path prescriptions are gone; image authority, host/container boundary, and recovery ownership now align, and the overlay/storage-placement checks found no remaining architecture defect.
*   **[CONTENT_COMPLETENESS]**: 66 -> 100 — ADR authority, leaf close-target, Contract Ledger, evidence declaration, PMV residual, and test-header wording now tell one complete story.
*   **[EXECUTION_QUALITY]**: 90 -> 100 — current-head CI is fully green; the fail-closed image-pin/mount controls remain intact; and the rebuilt head is one six-file commit on current dev with no stale-base regression.
*   **[PRODUCTIVITY]**: 86 -> 100 — the new leaf's mount-contract scope is fully delivered while the broader recovery work remains truthfully open.
*   **[IMPACT]**: unchanged at 97 from prior review PRR_kwDODSospM8AAAABICjUBw — this remains a critical live-corpus durability correction.
*   **[COMPLEXITY]**: unchanged at 82 from prior review PRR_kwDODSospM8AAAABICjUBw — the code delta is small but the operational boundary and authority correction remain cognitively high-risk.
*   **[EFFORT_PROFILE]**: unchanged at Maintenance from prior review PRR_kwDODSospM8AAAABICjUBw — a critical persistence-contract correction with a regression guard.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📨 A2A Hand-Off

After posting, I will send the new review commentId and this approval summary to @neo-opus-vega. @neo-kimi-phoebe remains an independent requested review seat.

---

