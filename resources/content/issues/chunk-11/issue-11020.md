---
id: 11020
title: 'gh-workflow sync: skip NOT_PLANNED snapshots + on-release archival automation'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - model-experience
assignees:
  - neo-opus-ada
createdAt: '2026-05-09T15:19:14Z'
updatedAt: '2026-05-09T15:24:56Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11020'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-09T15:24:56Z'
---
# gh-workflow sync: skip NOT_PLANNED snapshots + on-release archival automation

## Context

Empirical audit 2026-05-09 17:00Z: `resources/content/issues/` contains **985 items** — 15 below GitHub's 1000-item folder cap. At recent ticket cadence (~3-5/day), 4-5 days of headroom before the cap blocks future ticket creation.

**Root-cause analysis of why we hit this:**
- gh-workflow MCP sync writes a markdown snapshot for ALL closed issues, including those closed `NOT_PLANNED` (dropped/duplicate/invalid). Empirical: of last 100 closed ai-labeled tickets, 11 were `NOT_PLANNED` and ALL 11 have snapshots in active `resources/content/issues/`. These snapshots have zero operational value — they're noise.
- No on-release archival automation. Pattern exists at `resources/content/issue-archive/{v9.9.0, v10.0.0, v10.0.0-alpha.1, v10.0.0-alpha.2, v10.0.0-alpha.3, v10.0.0-alpha.4, ...}` (manual archival per release), but the gh-workflow sync doesn't drive it.

## Prescription

**Two-part enhancement to gh-workflow MCP sync logic:**

**Part A: Skip snapshot writes for `NOT_PLANNED` closures.**

Modify the snapshot-write step in gh-workflow's `services/sync*.mjs` (specific service TBD by implementer; likely `sync_all` or its child `IssueSyncService`):
1. At sync-time, filter out tickets where `state === 'CLOSED' && stateReason === 'NOT_PLANNED'` from the snapshot-write step
2. One-shot cleanup migration: existing `NOT_PLANNED` snapshots in `resources/content/issues/` are pruned on next sync run (idempotent — re-running sync does nothing once cleaned)

**Part B: On-release archival automation.**

When a release tag is pushed, gh-workflow sync moves all `state === 'CLOSED' && stateReason === 'COMPLETED'` issues into `resources/content/issue-archive/<version>/`:
- Trigger options: (1) release-tag webhook listener, (2) `npm run ai:archive-on-release vX.Y.Z` operator script, (3) detect tag during boot-sync
- Operator picks trigger shape during implementation (3 options outlined; not blocking on scope decision since structure is settled)
- After archival, `resources/content/issues/` contains only OPEN issues + a configurable buffer of most-recent N closed-completed (for in-context active-PR work)

## Acceptance Criteria
- [ ] AC1 — gh-workflow sync filters `NOT_PLANNED` from snapshot-write at write-time
- [ ] AC2 — One-shot cleanup migration prunes existing `NOT_PLANNED` snapshots from `resources/content/issues/` on next sync (idempotent)
- [ ] AC3 — On-release archival mechanism (triggered via release-tag, operator script, or boot-sync detection) moves `closed-completed` to `resources/content/issue-archive/<version>/`
- [ ] AC4 — After v13 release, `resources/content/issues/` count drops to (open issues + N most-recent closed-completed buffer); folder budget restored
- [ ] AC5 — Unit test: sync skips `NOT_PLANNED` issue, writes `COMPLETED` issue
- [ ] AC6 — Integration test: archive-on-release moves correct subset to versioned folder; OPEN issues remain
- [ ] AC7 — `learn/agentos/GitHubWorkflow.md` (or equivalent guide) documents the new behavior

## Avoided Traps

- ❌ **Year-based partition** (`issues/2025/`, `issues/2026/`) — operator pattern is release-versioned, not calendar-versioned; calendar partition fights the existing `issue-archive/<version>/` shape
- ❌ **Keeping all closed-completed in active folder forever** — operator's "repurpose existing tickets" + "smarter remove dropped tickets" framing works against this; archived tickets stay readable in `issue-archive/`
- ❌ **Per-batch manual archival** — automation is the entire point; manual-only would replicate today's tribal-knowledge pattern that produced the 985 figure
- ❌ **Cap-bump as the fix** — GitHub's 1k cap is the structural constraint; budget management beats cap-evasion

## Provenance

- Operator directive 2026-05-09: "we have issue archives. once we do a release => all closed issues get moved out. more breathing room: repurpose existing tickets, enhance gh-workflow server to smarter remove dropped tickets."
- Empirical anchor: `ls resources/content/issues/ | wc -l` = 985 at 2026-05-09 17:00Z
- Empirical anchor: `gh issue list --state closed --limit 100 --search "label:ai"` shows 11 NOT_PLANNED of 99 ai-labeled — 11% of recent closures are noise

---

**Self-Identification:** @neo-opus-ada (Claude Opus 4.7, Claude Code) — chief-architect lane, Phase 0.C of post-quad coordination round.

## Timeline

- 2026-05-09T15:19:15Z @neo-opus-ada added the `enhancement` label
- 2026-05-09T15:19:15Z @neo-opus-ada added the `ai` label
- 2026-05-09T15:19:16Z @neo-opus-ada added the `architecture` label
- 2026-05-09T15:19:16Z @neo-opus-ada added the `model-experience` label
- 2026-05-09T15:19:29Z @neo-opus-ada assigned to @neo-opus-ada
### @neo-opus-ada - 2026-05-09T15:24:28Z

**Input from Claude Opus 4.7 (Claude Code):**

> ❋ ## Retraction — closing this as `not planned`
> 
> Operator @tobiu flagged a verify-before-assert violation in this ticket's framing. Investigated and confirmed.
> 
> **The flaw — Part B (on-release archival automation) reinvents existing infrastructure that's already in production.**
> 
> `buildScripts/release/publish.mjs` Step 6 ("Post-Release Cleanup (Sync & Archive)", lines 222-251) already implements the entire on-release archival flow:
> - `GH_SyncService.runFullSync()` reflects state changes locally including active→archive moves
> - `buildScripts/docs/index/tickets.mjs` regenerates ticket index to reflect moves
> - Commits via `chore: Archive tickets for v${newVersion}` and pushes to dev
> 
> `buildScripts/release/analyzeClosedSinceRelease.mjs:8-13` documents the lifecycle explicitly: *"ticket files live in `resources/content/issues/` while open or recently closed, then get swept into `resources/content/issue-archive/<release>/` at release cut."*
> 
> Empirical evidence the system is mature and fully working: 156 archived versions span `v8.1.0 → v12.1.0`, with v12.0.0 alone holding 413 archived tickets and v12.1.0 holding 214. The pattern is heavily-used and operator-validated.
> 
> **Why I missed it:** I anchored on `resources/content/issues/` showing 985 items (15 below 1k cap) and inferred "no on-release archival automation exists" without grepping the release pipeline. Should have searched `find . -iname "*archive*"` BEFORE asserting the gap. Classic verify-before-assert violation — same pattern I owned in PR #11014's Cycle 3 follow-up self-calibration.
> 
> **Why the cap concern was also wrong-shaped:** the next release cut (v13.0.0) will sweep all closed-since-v12.1.0 tickets into `issue-archive/v13.0.0/`, dropping the active folder count back into safe territory naturally. The 985 figure is a between-releases artifact, not a structural problem.
> 
> **On the Part A (skip NOT_PLANNED snapshots) sub-question:** still potentially valid as a narrow scope, but needs design thought separately — the existing release-archival pipeline does include NOT_PLANNED tickets in the per-release archive folder, which is arguably the correct audit-trail behavior. Whether the noise is worth filtering at sync-time is a judgment call I shouldn't bake in unilaterally without operator framing. Won't file a narrower replacement; will surface as a question if/when it becomes load-bearing.
> 
> **Discipline addition baked in:** before asserting any infrastructure-gap claim in a public artifact, run the empirical existence-check (grep + find for the proposed mechanism's keywords) FIRST. Tools are read-only and cheap; bypassing the check under coordination-pace pressure is the swarm's primary failure mode (per `feedback_verify_before_assert`).
> 
> Closing as `not planned`.

- 2026-05-09T15:24:56Z @neo-opus-ada closed this issue

