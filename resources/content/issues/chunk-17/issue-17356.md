---
id: 17356
title: The snapshot reports a service's health but never its resolved config
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-18T15:00:48Z'
updatedAt: '2026-08-19T05:53:11Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17356'
author: neo-opus-vega
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-19T05:53:11Z'
---
# The snapshot reports a service's health but never its resolved config

## Context

Measured friction from a live incident on an adopter deployment (2026-08-18). An embedding lane was at 96.7% of its memory ceiling and four tenant repos had been failing for four hours. `get_deployment_state_snapshot` answered every health question I had — usage bytes, limit bytes, pressure disposition, saturation facts, restart churn, provider lane shape, a log tail.

It could not answer *"what is this service's `NEO_KB_EMBEDDING_BATCH_SIZE` right now?"*

That mattered because an earlier deploy letter of ours had told the operator to set it to `1`, and I needed to know whether that instruction had been applied and never reverted.

**"Unknown" overstates it, and the precise version is the better argument for this ticket.** The *declared* value is readable — our compose declares `${NEO_KB_EMBEDDING_BATCH_SIZE:-5}`, and I read it. What is unreadable is whether that is what runs, across three divergence points **we ourselves created**: the client host is not a checkout, so its copy of `compose.yml` can lag the revision we are reading; the host `.env` is an override slot we published; and our own deploy letter instructed a value into it.

So the gap is not *we know nothing*. It is **we can read what we intended and cannot read what is running** — and reporting the declared value as the effective one is exactly the confident-wrong-answer this ticket exists to remove. It is also the answer I nearly gave.

So the one question I had to escalate to a human was a question about **our own** configuration, on a plane we author end to end.

> **This body was rewritten 2026-08-18 after the original Fix section proved wrong.** It prescribed a bridge-side `AiConfig` read for values owned by a different process. The reasoning and the corrected shape are preserved below under *The Architectural Reality*; the original is in `issuecomment-5330285332`.

## The Problem

`snapshot.services[]` carries `stats`, `memoryPressure`, `diagnosis`, `restartChurn`, `providerActivity`, `providerResidency`, `providerLaneShape` and `logs` — and no view of resolved configuration. `inspect` carries container identity and state but not `Config.Env`.

The gap is asymmetric in the way that matters: we can see what a service is *doing* and never what we *told it to do*. Diagnosis then proceeds against assumed inputs. In this incident the assumed input and the possible real input differed by 5×, on the exact knob that governs ingestion throughput.

There is a second-order cost. Because the value is unknowable remotely, the only way to confirm a config instruction landed is to ask the operator to run a `grep` — the pattern we have explicitly ruled out for anything we can self-serve. Config was the one carve-out, and a carve-out by omission rather than by decision.

## The Architectural Reality

**The process boundary is the load-bearing fact, and the first version of this ticket got it wrong.**

`batchSize` / `batchDelay` / `maxRetries` are **knowledge-base server** leaves (`ai/mcp/server/knowledge-base/configBase.mjs:693/:715/:735`). `DeploymentStateBridgeService` runs in the **orchestrator**. A bridge-side read for those paths resolves the *orchestrator's* config tree and publishes it under the KB server's name. On a deployment whose per-service `.env` diverges from the compose default — the only deployment anyone would consult this field for — that publishes a **confidently wrong number**.

That is strictly worse than the absence it replaces, and it is the same failure this ticket complains about: an absent field says *cannot answer* and gets checked; a wrong-process field says *answered* and does not.

**The channel already existed.** `heapObservation` is `provenance: 'self-reported'` — the owning process writes a file, the bridge reads and bounds it, and `unavailableReason` carries the reason when the process cannot answer (which is why `not-node` reads correctly on a non-Node container instead of looking like a broken probe). Resolved config is the second fact of that kind.

**`providerLaneShape` is the shape precedent** for a narrow declared-and-observed projection rather than an environment dump.

## The Fix (as shipped)

Four pieces, in dependency order:

1. **`ai/mcp/server/shared/helpers/resolvedConfigDisclosure.mjs`** — the disclosure boundary. A pure function over an already-resolved config object; reads no environment and imports no config singleton.
2. **`ai/mcp/server/shared/services/ResolvedConfigReporterService.mjs`** — the owning process projects its allowlisted subset and publishes it once, atomically, after `boot()`.
3. **`DeploymentStateBridgeService.readResolvedConfig`** — relays it as `services[].resolvedConfig`, beside `providerLaneShape`. Resolves nothing itself.
4. **`BaseServer.getResolvedConfigDisclosure`** — one hook returning `{config, allowlist}`; `kb-server` declares the three embedding knobs.

**Where the allowlist is enforced, and why it is not the relay.** The writer applies it before anything reaches disk, so an unallowlisted value never leaves the owning process. No downstream relay, snapshot copy, log or future consumer can surface what was never emitted, and there is no second place a filter has to be re-applied correctly.

**Validity is bounded by incarnation, not by elapsed time.** A heap number is resampled because it moves. Config is fixed at boot and runtime mutation of the shared tree is forbidden, so an old record is not degraded and refusing it on age would hide a correct answer. A restart *does* invalidate it — the container may have come back with different env — so a record stamped before the current incarnation start is `stale-incarnation`. An *unparseable* incarnation start does not discard the record: that is an instrument gap, and refusing on it would convert "cannot tell which incarnation" into "configuration unknown".

**Published after `boot()`, which is load-bearing rather than tidy.** `loadCustomConfig()` runs inside `boot()`, so publishing earlier would disclose the pre-overlay values — naming the defaults as this deployment's effective configuration, the exact false answer the channel replaces.

## The golden rule, and why it holds by construction

**These tools must never be able to read secrets.** Satisfied without relying on a filter:

1. **No environment access.** Nothing in the boundary reads `process.env`; it is a pure function over a config object handed in. There is no path from disclosure to the environment, so there is nothing for a filter to miss.
2. **Secrets are not config leaves.** Credentials arrive as `credentialRef: env:NAME` resolved at their point of use, deliberately outside the tree this projection can see.
3. **Allowlist, never denylist.** A denylist fails open on every future key. An allowlist's failure mode is a missing value — visible, harmless, fixed by a reviewed addition.
4. **No wildcards or prefix matching.** `embedding.*` would silently admit a future `embedding.apiKey`. Refused at load rather than left to review.
5. **Disclosure kinds as a second floor.** Every entry declares the primitive it may reveal, and there is no free `string` kind, because a free string is the shape a credential has. A path declared `number` cannot carry a token even after a refactor moves something unexpected behind it.

The `kind` is deliberately **not** the config leaf's type. The leaf already owns the value domain; restating `positiveInt` here would define one thing twice.

## Decision Record impact

`aligned-with ADR-0019` — every config read happens at its use site in the owning process; no parallel resolution path, no new leaf, no formula, no runtime mutation.

## Acceptance Criteria

- [x] **AC-1:** `snapshot.services[].resolvedConfig` reports the allowlisted subset, **self-reported by the owning service and relayed**, with no `process.env` read anywhere in the path and no bridge-side resolution of another process's config.
- [x] **AC-2 (security, fails first):** a spec asserts a non-allowlisted path — including one holding a credential-shaped string — is absent from the projection, and it is mutation-proven: replacing the projection with a full-subtree dump turns it red.
- [x] **AC-3:** the allowlist refuses wildcard and prefix entries at load, so `embedding.*`-style widening cannot be introduced without failing a test.
- [x] **AC-4:** an allowlisted path whose value violates its declared kind is omitted with a reason, not coerced and not emitted.
- [x] **AC-5:** the allowlist is frozen and is not derived from configuration or environment; a deployment cannot extend it.
- [x] **AC-6 (over-claim, twin of AC-2):** an entry the owning service has not reported is absent **with a reason**, never defaulted and never filled from the bridge's own tree. `disclosed` is `null` on every unavailable arm, never `{}`.
- [x] **AC-7:** ADR-0019 conformance over the diff, naming A1, A5, A6, B1, B3, B4 as checked.
- [x] **AC-8:** the incident question is answerable from the snapshot alone — the effective embedding batch size for a running service, with no operator involvement.
- [x] **AC-9:** the field is exercised through `collectServiceSnapshot`, not only through the reader in isolation — an isolated corpus cannot catch an unreachable call site.
- [x] **AC-10:** paths resolve against the **real** config proxy, with a control proving the proxy hides them from `in` while the values read fine.

## Out of Scope

- **Exposing `inspect.Config.Env`**, in whole or in filtered form. It starts from the surface containing every secret and tries to subtract.
- **Writing configuration through the snapshot.** Read-only; a remote config write is a different blast radius and needs its own decision.
- **Provider slot geometry** — `providerLaneShape` already covers it.
- **The 120-line log-tail limitation** — #17357.
- **Renaming the self-report directory's config key.** It is `heapObservation.dir` for historical reasons while its meaning is broader; the rename has its own callers.
- **`mc-server`'s declaration.** The hook exists and defaults to publishing nothing; adding its seed set wants its own incident-driven test of which values have been wanted.

## Avoided Traps

- **Filtering an environment dump.** Redaction inverts the safety default: every future secret is exposed until someone remembers to add a pattern. The industry-standard `SECRET_PATTERNS` regex list is precisely the denylist this refuses.
- **Making the allowlist configurable.** It reads as good practice and defeats the mechanism: a deployment that can extend the list can name a credential path.
- **Asserting the feature works without asserting what it refuses.** A test that only checks the allowlisted values appear passes equally against a full-subtree dump.
- **Trusting a plain-object fixture for a proxy-backed subject.** The walker decided presence with `in`, which reads `false` on a config proxy that has a `get` trap and no `has` trap — every fixture green, production silently disclosing nothing. Caught only by wiring it to the real config.
- **A second identity hook.** Reusing the heap channel's service key avoids two service keys for one process, which is the mis-attribution hazard that channel already documents.

## Related

- #17357 — startup facts aging out of the log tail; the sibling observability gap from the same incident.
- #17344 — one global transport leaf serving four servers whose transports diverged; same "config we authored is not observable" family.
- #17349 / #17345 — tenant-sync behaviours whose diagnosis this shortens.
- ADR-0019 `learn/agentos/decisions/0019-aiconfig-reactive-provider-ssot.md`.

## Handoff Retrieval Hints

- Retrieval Hint: `"resolved config self-reported allowlist leaf paths not env names"`
- Retrieval Hint: `"snapshot cannot answer effective batch size, asked the operator instead"`
- Retrieval Hint: `"config proxy has no has trap, in operator reads false while value resolves"`
- The patterns copied: `heapObservation` for the channel, `providerLaneShape` for the projection shape.

Origin Session ID: 9ccc2fa1-8843-4796-8e85-5e151c0392d2

— Vega (Claude Opus 5, Claude Code) 🌿


## Timeline

- 2026-08-18T15:00:49Z @neo-opus-vega added the `enhancement` label
- 2026-08-18T15:00:50Z @neo-opus-vega added the `ai` label
- 2026-08-18T15:00:50Z @neo-opus-vega added the `architecture` label
- 2026-08-18T15:00:50Z @neo-opus-vega added the `agent-os` label
- 2026-08-18T15:01:54Z @neo-opus-vega cross-referenced by #17357
- 2026-08-18T15:11:30Z @neo-opus-vega assigned to @neo-opus-vega
### @neo-opus-vega - 2026-08-18T15:20:57Z

## Design correction — the Fix section names a read the bridge structurally cannot do

Found by running the one probe I flagged before implementing. **This supersedes the "The Fix" section above**; I will fold it into the body rather than leave the correction only in a comment.

### What I got wrong

The body says the projection is "sourced from `AiConfig`" and "reads `AiConfig` at the use site". That is right for the *mechanism* and wrong for the *seed set*, because the two live in different processes:

- `batchSize` / `batchDelay` / `maxRetries` are **knowledge-base server** leaves — `ai/mcp/server/knowledge-base/configBase.mjs:693/:715/:735`.
- `DeploymentStateBridgeService` runs in the **orchestrator**.

So a bridge-side `AiConfig` read for those paths would resolve the *orchestrator's* config tree and publish it as the KB server's effective value. On a deployment where the two differ — which is the entire case this ticket was filed for, since a per-service `.env` override is what made the value unknowable — it would report a **confidently wrong number**. That is strictly worse than the current absence, and it is the same failure mode the ticket's own Context section complains about: I nearly answered "5" from the compose default when the real value might have been "1".

An absent field says *cannot answer*. A wrong-process field says *answered*, and nobody re-checks an answer.

### The corrected shape, and its precedent is in the same file

Cross-process facts already have a channel here: `heapObservation` (`DeploymentStateBridgeService.mjs:1356-1394`) is `provenance: 'self-reported'`, bounded by the bridge before publishing, and carries `unavailableReason` when the owning process cannot answer — which is why `unavailableReason: 'not-node'` reads correctly on a non-Node container instead of looking like a broken probe.

So:

1. **The owning service self-reports** its allowlisted resolved config. It reads its *own* `AiConfig` / config module at the use site — where A1's sanctioned form actually applies — so the value is authoritative by construction rather than by assumption.
2. **The bridge bounds and relays it**, exactly as it does for the heap observation. It never resolves another process's config.
3. **`provenance` is mandatory on every entry**, and absence carries a reason. `self-reported` and `observed-by-bridge` fail in opposite directions, and collapsing them is the mistake the heap-observation JSDoc already documents.

### What this changes in the ACs

- **AC-1** is amended: values are self-reported by the owning service and relayed, not read by the bridge.
- **New AC:** an entry whose owning service has not reported is absent **with a reason**, never defaulted and never filled from the bridge's own tree. This is the security-adjacent AC's twin — one refuses to over-disclose, this one refuses to over-claim.
- **AC-6** (ADR-0019 conformance) gains a specific check: no `AiConfig` read in the bridge for a path owned by another service.
- The allowlist itself is unchanged, and so is every clause of the golden rule: allowlist not denylist, no wildcards or prefix matching, disclosure-kind types, no `process.env` anywhere. Those were never the part that was wrong.

### Why I am recording this rather than quietly fixing it

The wrong version would have passed its own tests. A bridge-side read resolves *something* for those paths, the projection populates, AC-1 as originally written goes green, and the defect only surfaces on a deployment whose per-service config diverges — which is the one deployment anybody would consult it for. Worth the paragraph so the next reader knows the process boundary is load-bearing and not an implementation detail.

Also closing a hypothesis I broadcast: I suspected `VectorService.mjs:1167`'s bare `aiConfig` was a live ADR-0019 **B1** (exported config subtree). It is not — it is `import aiConfig from '../../mcp/server/knowledge-base/config.mjs'`, the KB server's own config module default export, which is the sanctioned pattern for KB services. No B1, no prerequisite ticket. The distinction between that `aiConfig` and the orchestrator's `AiConfig` is what surfaced the process-boundary problem above.

— Vega (Claude Opus 5, Claude Code) 🌿

- 2026-08-18T15:39:55Z @neo-opus-vega referenced in commit `b0da790` - "test(ai): the disclosure boundary, proven against configs that hold credentials (#17356)

Every fixture holds a credential beside the allowlisted knobs, deliberately. A
fixture containing only safe values cannot distinguish a working allowlist from a
full-subtree dump — both produce an identical passing projection — so the secret
is what makes the assertion able to fail at all.

Two cases exist to keep the security assertion honest rather than to test the
module. One pins the full-subtree shape and asserts the substring check catches
it, so trimming the credentials out of the other fixtures would be visible rather
than silently vacuous. The other demonstrates a denylist written before a key
existed leaking that key, which is the allowlist clause stated as evidence instead
of as a preference.

The over-disclose and over-claim arms are both covered: an unallowlisted value
never reaches the envelope, and an absent path is reported with a reason and
carries no value field, because a reader who cannot separate not-reported from
reported-the-default will read the second when only the first is true.

Coverage also includes the refactor case — a value that moved behind an
allowlisted path fails its declared kind instead of publishing — eight wildcard
and malformed path forms refused at load with a literal-path control so the guard
is not passing by refusing everything, the absence of a free string kind, the
enum length backstop, config-unavailable, and the frozen allowlist.

The spec imports Neo despite the module under test being a pure function: the
shared setup harness touches Neo.ns while wiring the main-thread mock, so a spec
that imports no framework leaves Neo unpopulated and setup throws before any
assertion runs.

11 passed via test/playwright/playwright.config.unit.mjs --workers=1."
- 2026-08-18T15:51:16Z @neo-opus-vega referenced in commit `b29da6a` - "feat(ai): a service publishes its own allowlisted resolved config (#17356)

The owning process has to be the one that answers. A deployment's health is
observable from outside over the Docker socket; its configuration is not, and
resolving another service's config in the orchestrator would publish the
orchestrator's numbers under that service's name. On a deployment whose
per-service .env diverges from the compose default — the only deployment anyone
would consult this for — that is a confidently wrong answer, which is worse than
absence: an absent field says cannot-answer, a wrong-process field says answered
and nobody re-checks an answer.

So this runs the transport the heap observation already runs, for the same reason:
the fact belongs to the process and only the process holds it.

The disclosure boundary is applied BEFORE the write, so an unallowlisted value
never leaves the process and no downstream relay, snapshot copy or log can
surface what was never emitted.

Written once rather than on a cadence: a heap number moves, resolved config does
not, and runtime mutation of the shared tree is forbidden. A lost file is
rewritten on the next boot while the reader reports absence with a reason, which
beats serving a stale value as current.

readConfig and readAllowlist are thunks, not default parameters. A default is
evaluated at call time outside the guard, so a throwing config getter would take a
booting service down — the one failure an observation lane must never cause. A
malformed allowlist degrades the channel and publishes nothing rather than
widening.

One test premise of mine was wrong and the code is better than it: I expected a
missing directory to fail, and the atomic writer resolves the parent chain with
mkdir(recursive). The spec now asserts the real behaviour, since a reporter that
needed its directory pre-created would go silent on exactly the deployment that
has never reported before.

Directory reuse is deliberate: it is the existing self-report channel, and its
config key is heap-specific only because heap was the first fact a service
published about itself. Renaming that key has its own callers and its own change.

8 passed for the reporter, 19 across the shared tree."
- 2026-08-18T16:06:59Z @neo-opus-vega referenced in commit `d1f2d1f` - "feat(ai): relay a service's self-reported resolved config into the snapshot (#17356)

The reader resolves nothing itself, and that is the whole contract. A deployment's
health is Docker-observable; its configuration is not, and the values that matter
during an incident belong to other services — resolving them here would publish
the orchestrator's own tree under another service's name. On a deployment whose
per-service env diverges from the compose default, which is the only deployment
anyone consults this for, that is a confidently wrong answer, and a wrong answer
is worse than an absent one: an absent field gets checked, an answered one does
not.

Validity is bounded by INCARNATION rather than by elapsed time, which is the
deliberate divergence from readHeapObservation. A heap number is resampled because
it moves, so age measures how well it still describes the process. Config is fixed
at boot and runtime mutation of the shared tree is forbidden, so an old record is
not degraded and refusing it on age would hide a correct answer. What does
invalidate it is a restart: the container may have come back with different env,
so a record stamped before the current incarnation started describes config that
no longer applies. There is no pairable equivalent because nothing puts this in a
ratio.

An unparseable incarnation start does NOT discard the record. That is an
instrument gap, and refusing on it would convert cannot-tell-which-incarnation
into configuration-unknown.

disclosed and omitted are null on every unavailable arm, never {} and never [].
An empty object reads as reported-and-disclosed-nothing, a different claim from
did-not-report, and collapsing them is how a reader believes a configuration was
checked when it never was.

Coverage includes a wiring test through collectServiceSnapshot, because an
isolated reader corpus cannot catch an unreachable call site: a reader that works
and is never called leaves every unit assertion green while the snapshot carries
nothing. Its stub declares a Node Cmd deliberately, so the arm it lands on is
 — proving the reader passed the identity gate and attempted the read —
rather than an early identity refusal that would prove much less.

111 passed."
- 2026-08-18T16:07:35Z @neo-opus-vega referenced in commit `dce278b` - "feat(ai): relay a service's self-reported resolved config into the snapshot (#17356)

The reader resolves nothing itself, and that is the whole contract. A deployment's
health is Docker-observable; its configuration is not, and the values that matter
during an incident belong to other services — resolving them here would publish
the orchestrator's own tree under another service's name. On a deployment whose
per-service env diverges from the compose default, which is the only deployment
anyone consults this for, that is a confidently wrong answer, and a wrong answer
is worse than an absent one: an absent field gets checked, an answered one does
not.

Validity is bounded by INCARNATION rather than by elapsed time, which is the
deliberate divergence from readHeapObservation. A heap number is resampled because
it moves, so age measures how well it still describes the process. Config is fixed
at boot and runtime mutation of the shared tree is forbidden, so an old record is
not degraded and refusing it on age would hide a correct answer. What does
invalidate it is a restart: the container may have come back with different env,
so a record stamped before the current incarnation started describes config that
no longer applies. There is no pairable equivalent because nothing puts this in a
ratio.

An unparseable incarnation start does NOT discard the record. That is an
instrument gap, and refusing on it would convert cannot-tell-which-incarnation
into configuration-unknown.

disclosed and omitted are null on every unavailable arm, never {} and never [].
An empty object reads as reported-and-disclosed-nothing, a different claim from
did-not-report, and collapsing them is how a reader believes a configuration was
checked when it never was.

Coverage includes a wiring test through collectServiceSnapshot, because an
isolated reader corpus cannot catch an unreachable call site: a reader that works
and is never called leaves every unit assertion green while the snapshot carries
nothing. Its stub declares a Node Cmd deliberately, so the arm it lands on is
ABSENT — proving the reader passed the identity gate and attempted the read —
rather than an early identity refusal that would prove much less.

111 passed."
- 2026-08-18T16:13:44Z @neo-opus-vega referenced in commit `8bda9bf` - "feat(ai): the kb-server declares and publishes its own embedding knobs (#17356)

Closes the loop: the boundary, the reporter and the relay existed but nothing
called them, so the snapshot field could never populate.

One hook returning both halves rather than two, for the reason the identity hook
beside it is one line: a server able to declare an allowlist separately from a
config source can half-integrate, naming values it never supplies or the reverse,
and both halves are useless alone. Identity reuses the heap channel's service-key
hook instead of adding a parallel one, because two service keys for one process is
the mis-attribution hazard that channel already documents.

Published after boot(), and here that is load-bearing rather than tidy:
loadCustomConfig() runs inside boot(), so publishing earlier would disclose the
PRE-OVERLAY values. That is not a missing report but a wrong one, naming the
defaults as this deployment's effective configuration -- exactly the false answer
this channel replaces. A late report reads as absent and gets checked; an early one
reads as authoritative and does not.

The seed set was chosen by one test: has not knowing this value cost an incident?
All three were needed during a live ingestion stall and none was answerable from
outside the container. Nothing is listed for completeness, because a path nobody
has wanted earns no disclosure and still has to be reviewed forever.

FIX to the already-committed walker, found only by wiring it to the real thing.
Presence was decided with the `in` operator, and a resolved config is a Proxy that
exposes leaves through a get trap with NO has trap -- so membership reads false
while the value resolves fine. Every plain-object fixture passed and production
would have disclosed nothing. Presence now follows the resolved value, and the
integration case asserts against the real config proxy with a control proving the
hazard is present rather than a config that happens to be plain.

The seed paths are top-level for the same reason: the leaves sit under data in the
config base and the proxy exposes them at the root, which is how every other
consumer reads them.

12 passed for the boundary, 727 across ai/mcp. The one failure there, neural-link
in McpServersHealth, was verified pre-existing by stashing these changes and
reproducing it on the clean tree."
- 2026-08-18T16:18:52Z @neo-opus-vega referenced in commit `b5c0a48` - "feat(ai): the disclosure boundary for self-reported resolved config (#17356)

A deployment's health is observable from outside the process; the configuration
it was given is not. During an incident that asymmetry means diagnosis proceeds
against assumed inputs, and the assumed value can differ from the real one by the
whole factor that matters, because a per-service .env overrides the compose
default with nothing reporting that it did.

The allowlist is enforced at the WRITER, not the relay. An unallowlisted value
never leaves the owning process, so no downstream relay, snapshot copy or future
consumer can surface what was never emitted. Filtering at the relay would leave
the full set in transit and make every new consumer a place the filter must be
re-applied correctly.

Four clauses carry the invariant by construction rather than by redaction: no
process.env read anywhere, so there is no path from disclosure to the environment
and no config-singleton import; allowlist never denylist, because a denylist
fails open on every key added after it was written; literal dot-paths only,
refused at load, because a match region wider than its intent admits keys nobody
reviewed; and a declared disclosure kind per entry, so a value that moved behind
an allowlisted path fails its kind instead of publishing.

The kind is deliberately coarser than the config leaf's type, which already owns
the value domain. There is no free string kind, because a free string is the
shape a credential has.

Omissions are returned beside disclosures. A reader who cannot tell an unreported
value from a reported default will read the second when only the first is true,
and a wrong value is worse than a missing one: a missing value gets checked, an
answered one does not.

Verified 17 adversarial cases against a config that CONTAINS credentials beside
the allowlisted knobs, and mutation-tested the security assertion: replacing the
projection with a naive full-subtree dump turns it red, so the assertion is
load-bearing rather than vacuous. A denylist mutation missing a later-added key
leaks, which is the allowlist clause stated as evidence."
- 2026-08-18T16:18:52Z @neo-opus-vega referenced in commit `4dc6f5c` - "test(ai): the disclosure boundary, proven against configs that hold credentials (#17356)

Every fixture holds a credential beside the allowlisted knobs, deliberately. A
fixture containing only safe values cannot distinguish a working allowlist from a
full-subtree dump — both produce an identical passing projection — so the secret
is what makes the assertion able to fail at all.

Two cases exist to keep the security assertion honest rather than to test the
module. One pins the full-subtree shape and asserts the substring check catches
it, so trimming the credentials out of the other fixtures would be visible rather
than silently vacuous. The other demonstrates a denylist written before a key
existed leaking that key, which is the allowlist clause stated as evidence instead
of as a preference.

The over-disclose and over-claim arms are both covered: an unallowlisted value
never reaches the envelope, and an absent path is reported with a reason and
carries no value field, because a reader who cannot separate not-reported from
reported-the-default will read the second when only the first is true.

Coverage also includes the refactor case — a value that moved behind an
allowlisted path fails its declared kind instead of publishing — eight wildcard
and malformed path forms refused at load with a literal-path control so the guard
is not passing by refusing everything, the absence of a free string kind, the
enum length backstop, config-unavailable, and the frozen allowlist.

The spec imports Neo despite the module under test being a pure function: the
shared setup harness touches Neo.ns while wiring the main-thread mock, so a spec
that imports no framework leaves Neo unpopulated and setup throws before any
assertion runs.

11 passed via test/playwright/playwright.config.unit.mjs --workers=1."
- 2026-08-18T16:18:52Z @neo-opus-vega referenced in commit `95b9752` - "feat(ai): a service publishes its own allowlisted resolved config (#17356)

The owning process has to be the one that answers. A deployment's health is
observable from outside over the Docker socket; its configuration is not, and
resolving another service's config in the orchestrator would publish the
orchestrator's numbers under that service's name. On a deployment whose
per-service .env diverges from the compose default — the only deployment anyone
would consult this for — that is a confidently wrong answer, which is worse than
absence: an absent field says cannot-answer, a wrong-process field says answered
and nobody re-checks an answer.

So this runs the transport the heap observation already runs, for the same reason:
the fact belongs to the process and only the process holds it.

The disclosure boundary is applied BEFORE the write, so an unallowlisted value
never leaves the process and no downstream relay, snapshot copy or log can
surface what was never emitted.

Written once rather than on a cadence: a heap number moves, resolved config does
not, and runtime mutation of the shared tree is forbidden. A lost file is
rewritten on the next boot while the reader reports absence with a reason, which
beats serving a stale value as current.

readConfig and readAllowlist are thunks, not default parameters. A default is
evaluated at call time outside the guard, so a throwing config getter would take a
booting service down — the one failure an observation lane must never cause. A
malformed allowlist degrades the channel and publishes nothing rather than
widening.

One test premise of mine was wrong and the code is better than it: I expected a
missing directory to fail, and the atomic writer resolves the parent chain with
mkdir(recursive). The spec now asserts the real behaviour, since a reporter that
needed its directory pre-created would go silent on exactly the deployment that
has never reported before.

Directory reuse is deliberate: it is the existing self-report channel, and its
config key is heap-specific only because heap was the first fact a service
published about itself. Renaming that key has its own callers and its own change.

8 passed for the reporter, 19 across the shared tree."
- 2026-08-18T16:18:53Z @neo-opus-vega referenced in commit `f4625e7` - "feat(ai): relay a service's self-reported resolved config into the snapshot (#17356)

The reader resolves nothing itself, and that is the whole contract. A deployment's
health is Docker-observable; its configuration is not, and the values that matter
during an incident belong to other services — resolving them here would publish
the orchestrator's own tree under another service's name. On a deployment whose
per-service env diverges from the compose default, which is the only deployment
anyone consults this for, that is a confidently wrong answer, and a wrong answer
is worse than an absent one: an absent field gets checked, an answered one does
not.

Validity is bounded by INCARNATION rather than by elapsed time, which is the
deliberate divergence from readHeapObservation. A heap number is resampled because
it moves, so age measures how well it still describes the process. Config is fixed
at boot and runtime mutation of the shared tree is forbidden, so an old record is
not degraded and refusing it on age would hide a correct answer. What does
invalidate it is a restart: the container may have come back with different env,
so a record stamped before the current incarnation started describes config that
no longer applies. There is no pairable equivalent because nothing puts this in a
ratio.

An unparseable incarnation start does NOT discard the record. That is an
instrument gap, and refusing on it would convert cannot-tell-which-incarnation
into configuration-unknown.

disclosed and omitted are null on every unavailable arm, never {} and never [].
An empty object reads as reported-and-disclosed-nothing, a different claim from
did-not-report, and collapsing them is how a reader believes a configuration was
checked when it never was.

Coverage includes a wiring test through collectServiceSnapshot, because an
isolated reader corpus cannot catch an unreachable call site: a reader that works
and is never called leaves every unit assertion green while the snapshot carries
nothing. Its stub declares a Node Cmd deliberately, so the arm it lands on is
ABSENT — proving the reader passed the identity gate and attempted the read —
rather than an early identity refusal that would prove much less.

111 passed."
- 2026-08-18T16:18:53Z @neo-opus-vega referenced in commit `02d716d` - "feat(ai): the kb-server declares and publishes its own embedding knobs (#17356)

Closes the loop: the boundary, the reporter and the relay existed but nothing
called them, so the snapshot field could never populate.

One hook returning both halves rather than two, for the reason the identity hook
beside it is one line: a server able to declare an allowlist separately from a
config source can half-integrate, naming values it never supplies or the reverse,
and both halves are useless alone. Identity reuses the heap channel's service-key
hook instead of adding a parallel one, because two service keys for one process is
the mis-attribution hazard that channel already documents.

Published after boot(), and here that is load-bearing rather than tidy:
loadCustomConfig() runs inside boot(), so publishing earlier would disclose the
PRE-OVERLAY values. That is not a missing report but a wrong one, naming the
defaults as this deployment's effective configuration -- exactly the false answer
this channel replaces. A late report reads as absent and gets checked; an early one
reads as authoritative and does not.

The seed set was chosen by one test: has not knowing this value cost an incident?
All three were needed during a live ingestion stall and none was answerable from
outside the container. Nothing is listed for completeness, because a path nobody
has wanted earns no disclosure and still has to be reviewed forever.

FIX to the already-committed walker, found only by wiring it to the real thing.
Presence was decided with the `in` operator, and a resolved config is a Proxy that
exposes leaves through a get trap with NO has trap -- so membership reads false
while the value resolves fine. Every plain-object fixture passed and production
would have disclosed nothing. Presence now follows the resolved value, and the
integration case asserts against the real config proxy with a control proving the
hazard is present rather than a config that happens to be plain.

The seed paths are top-level for the same reason: the leaves sit under data in the
config base and the proxy exposes them at the root, which is how every other
consumer reads them.

12 passed for the boundary, 727 across ai/mcp. The one failure there, neural-link
in McpServersHealth, was verified pre-existing by stashing these changes and
reproducing it on the clean tree."
- 2026-08-18T16:18:55Z @neo-opus-vega cross-referenced by PR #17362
- 2026-08-18T16:23:25Z @neo-opus-vega referenced in commit `17d0a04` - "chore(docs): regenerate the class hierarchy for the new reporter (#17356)

The freshness gate is not bookkeeping: `extends` is hashed into every Knowledge
Base chunk id, so committing a new class without regenerating this file makes the
next ingest re-identify the affected chunks and mark the existing corpus stale.

Generated rather than hand-edited, and the delta verified at token level rather
than by eye -- the file is one line, so a raw diff shows every service name in the
tree and proves nothing. Parsed comparison: exactly one token added,
Neo.ai.mcp.server.shared.services.ResolvedConfigReporterService, and zero removed.
The disclosure helper does not appear because it is pure functions, not a class."
- 2026-08-19T04:55:51Z @neo-opus-vega referenced in commit `d178c1d` - "chore(ai): merge dev and regenerate the class hierarchy (#17356)

The tracked class hierarchy conflicted because dev and this branch both
regenerated it. Resolved by regenerating from the merged source tree, which
is the only resolution that can be right: the file is derived, so neither
side's copy is authoritative once the sources are combined.

Verified the union rather than assuming it — the regenerated file drops
nothing from dev and adds exactly the one class this branch introduces
(Neo.ai.mcp.server.shared.services.ResolvedConfigReporterService)."
- 2026-08-19T05:09:05Z @neo-opus-grace cross-referenced by PR #17366
- 2026-08-19T05:27:57Z @neo-opus-vega referenced in commit `da0337d` - "fix(ai): an enum disclosure kind enumerates, so the second floor is membership (#17356)

Clause 4 promised that a declared kind stops a token reaching the wire even if a
refactor moves something unexpected behind an allowlisted path. That held for
`number` and `boolean`, which are structurally credential-proof. It did not hold
for `enum`, whose check was a 64-character length bound — and a credential
shorter than the bound passes a length bound.

An `enum` entry now declares its permitted `values`, refused at load when absent
or empty for the same reason a wildcard path is refused: an enum that does not
enumerate is a match region covering every string, arriving through the kind
instead of the path. Disclosure is membership in that set. A `values` list on
`number` or `boolean` is refused rather than ignored, because a decorative
constraint is worse than an absent one — the next reader believes it.

Verified by mutation rather than by inspection. Reverting the check to the old
length test discloses a 26-character credential-shaped string behind an
enum-declared path; the spec fixture is deliberately that size, because a
long-string fixture cannot tell a set check from a length check.

The fixture originally carried a realistic `glpat-` prefix and GitHub push
protection blocked it, classifying an invented value as a live GitLab token. It
now keeps the shape without the vendor grammar.

No production migration: every allowlist entry that ships is `number`, so the
only enum declarations in the tree were spec fixtures.

Found by @neo-opus-ada reviewing PR #17362."
- 2026-08-19T05:43:43Z @neo-opus-vega referenced in commit `45deab9` - "chore(ai): merge dev — both bridge readers land side by side (#17356)

#17366 merged first and added `readStartupLogHead` at the same insertion point
this branch adds `readResolvedConfig`. Both are pure additions, and both end in
an identical `catch` returning `unavailable(...)`, so git spliced the two methods
into one conflict instead of seeing two neighbours.

Resolved as "both", not "pick": took dev's file as the base and replayed this
branch's merge-base diff on top, so the surviving text is each side's own,
not a hand-merge of two method bodies. Same shape in the spec, where the two
`test.describe` blocks collided the same way.

Verified the union rather than assuming it — both call sites present
(`startup:` inside `summarizeLogs`, `resolvedConfig` beside the heap read),
both fields published on the record, and both spec blocks running:
118 in the bridge spec, 1926 across the shared and orchestrator trees.

The class hierarchy needed no regeneration this time; it merged clean."
- 2026-08-19T05:46:00Z @neo-opus-vega cross-referenced by #17371
- 2026-08-19T05:53:11Z @tobiu referenced in commit `2050825` - "feat(ai): a service reports its own resolved config, so nobody has to ask the operator (#17356) (#17362)

* feat(ai): the disclosure boundary for self-reported resolved config (#17356)

A deployment's health is observable from outside the process; the configuration
it was given is not. During an incident that asymmetry means diagnosis proceeds
against assumed inputs, and the assumed value can differ from the real one by the
whole factor that matters, because a per-service .env overrides the compose
default with nothing reporting that it did.

The allowlist is enforced at the WRITER, not the relay. An unallowlisted value
never leaves the owning process, so no downstream relay, snapshot copy or future
consumer can surface what was never emitted. Filtering at the relay would leave
the full set in transit and make every new consumer a place the filter must be
re-applied correctly.

Four clauses carry the invariant by construction rather than by redaction: no
process.env read anywhere, so there is no path from disclosure to the environment
and no config-singleton import; allowlist never denylist, because a denylist
fails open on every key added after it was written; literal dot-paths only,
refused at load, because a match region wider than its intent admits keys nobody
reviewed; and a declared disclosure kind per entry, so a value that moved behind
an allowlisted path fails its kind instead of publishing.

The kind is deliberately coarser than the config leaf's type, which already owns
the value domain. There is no free string kind, because a free string is the
shape a credential has.

Omissions are returned beside disclosures. A reader who cannot tell an unreported
value from a reported default will read the second when only the first is true,
and a wrong value is worse than a missing one: a missing value gets checked, an
answered one does not.

Verified 17 adversarial cases against a config that CONTAINS credentials beside
the allowlisted knobs, and mutation-tested the security assertion: replacing the
projection with a naive full-subtree dump turns it red, so the assertion is
load-bearing rather than vacuous. A denylist mutation missing a later-added key
leaks, which is the allowlist clause stated as evidence.

* test(ai): the disclosure boundary, proven against configs that hold credentials (#17356)

Every fixture holds a credential beside the allowlisted knobs, deliberately. A
fixture containing only safe values cannot distinguish a working allowlist from a
full-subtree dump — both produce an identical passing projection — so the secret
is what makes the assertion able to fail at all.

Two cases exist to keep the security assertion honest rather than to test the
module. One pins the full-subtree shape and asserts the substring check catches
it, so trimming the credentials out of the other fixtures would be visible rather
than silently vacuous. The other demonstrates a denylist written before a key
existed leaking that key, which is the allowlist clause stated as evidence instead
of as a preference.

The over-disclose and over-claim arms are both covered: an unallowlisted value
never reaches the envelope, and an absent path is reported with a reason and
carries no value field, because a reader who cannot separate not-reported from
reported-the-default will read the second when only the first is true.

Coverage also includes the refactor case — a value that moved behind an
allowlisted path fails its declared kind instead of publishing — eight wildcard
and malformed path forms refused at load with a literal-path control so the guard
is not passing by refusing everything, the absence of a free string kind, the
enum length backstop, config-unavailable, and the frozen allowlist.

The spec imports Neo despite the module under test being a pure function: the
shared setup harness touches Neo.ns while wiring the main-thread mock, so a spec
that imports no framework leaves Neo unpopulated and setup throws before any
assertion runs.

11 passed via test/playwright/playwright.config.unit.mjs --workers=1.

* feat(ai): a service publishes its own allowlisted resolved config (#17356)

The owning process has to be the one that answers. A deployment's health is
observable from outside over the Docker socket; its configuration is not, and
resolving another service's config in the orchestrator would publish the
orchestrator's numbers under that service's name. On a deployment whose
per-service .env diverges from the compose default — the only deployment anyone
would consult this for — that is a confidently wrong answer, which is worse than
absence: an absent field says cannot-answer, a wrong-process field says answered
and nobody re-checks an answer.

So this runs the transport the heap observation already runs, for the same reason:
the fact belongs to the process and only the process holds it.

The disclosure boundary is applied BEFORE the write, so an unallowlisted value
never leaves the process and no downstream relay, snapshot copy or log can
surface what was never emitted.

Written once rather than on a cadence: a heap number moves, resolved config does
not, and runtime mutation of the shared tree is forbidden. A lost file is
rewritten on the next boot while the reader reports absence with a reason, which
beats serving a stale value as current.

readConfig and readAllowlist are thunks, not default parameters. A default is
evaluated at call time outside the guard, so a throwing config getter would take a
booting service down — the one failure an observation lane must never cause. A
malformed allowlist degrades the channel and publishes nothing rather than
widening.

One test premise of mine was wrong and the code is better than it: I expected a
missing directory to fail, and the atomic writer resolves the parent chain with
mkdir(recursive). The spec now asserts the real behaviour, since a reporter that
needed its directory pre-created would go silent on exactly the deployment that
has never reported before.

Directory reuse is deliberate: it is the existing self-report channel, and its
config key is heap-specific only because heap was the first fact a service
published about itself. Renaming that key has its own callers and its own change.

8 passed for the reporter, 19 across the shared tree.

* feat(ai): relay a service's self-reported resolved config into the snapshot (#17356)

The reader resolves nothing itself, and that is the whole contract. A deployment's
health is Docker-observable; its configuration is not, and the values that matter
during an incident belong to other services — resolving them here would publish
the orchestrator's own tree under another service's name. On a deployment whose
per-service env diverges from the compose default, which is the only deployment
anyone consults this for, that is a confidently wrong answer, and a wrong answer
is worse than an absent one: an absent field gets checked, an answered one does
not.

Validity is bounded by INCARNATION rather than by elapsed time, which is the
deliberate divergence from readHeapObservation. A heap number is resampled because
it moves, so age measures how well it still describes the process. Config is fixed
at boot and runtime mutation of the shared tree is forbidden, so an old record is
not degraded and refusing it on age would hide a correct answer. What does
invalidate it is a restart: the container may have come back with different env,
so a record stamped before the current incarnation started describes config that
no longer applies. There is no pairable equivalent because nothing puts this in a
ratio.

An unparseable incarnation start does NOT discard the record. That is an
instrument gap, and refusing on it would convert cannot-tell-which-incarnation
into configuration-unknown.

disclosed and omitted are null on every unavailable arm, never {} and never [].
An empty object reads as reported-and-disclosed-nothing, a different claim from
did-not-report, and collapsing them is how a reader believes a configuration was
checked when it never was.

Coverage includes a wiring test through collectServiceSnapshot, because an
isolated reader corpus cannot catch an unreachable call site: a reader that works
and is never called leaves every unit assertion green while the snapshot carries
nothing. Its stub declares a Node Cmd deliberately, so the arm it lands on is
ABSENT — proving the reader passed the identity gate and attempted the read —
rather than an early identity refusal that would prove much less.

111 passed.

* feat(ai): the kb-server declares and publishes its own embedding knobs (#17356)

Closes the loop: the boundary, the reporter and the relay existed but nothing
called them, so the snapshot field could never populate.

One hook returning both halves rather than two, for the reason the identity hook
beside it is one line: a server able to declare an allowlist separately from a
config source can half-integrate, naming values it never supplies or the reverse,
and both halves are useless alone. Identity reuses the heap channel's service-key
hook instead of adding a parallel one, because two service keys for one process is
the mis-attribution hazard that channel already documents.

Published after boot(), and here that is load-bearing rather than tidy:
loadCustomConfig() runs inside boot(), so publishing earlier would disclose the
PRE-OVERLAY values. That is not a missing report but a wrong one, naming the
defaults as this deployment's effective configuration -- exactly the false answer
this channel replaces. A late report reads as absent and gets checked; an early one
reads as authoritative and does not.

The seed set was chosen by one test: has not knowing this value cost an incident?
All three were needed during a live ingestion stall and none was answerable from
outside the container. Nothing is listed for completeness, because a path nobody
has wanted earns no disclosure and still has to be reviewed forever.

FIX to the already-committed walker, found only by wiring it to the real thing.
Presence was decided with the `in` operator, and a resolved config is a Proxy that
exposes leaves through a get trap with NO has trap -- so membership reads false
while the value resolves fine. Every plain-object fixture passed and production
would have disclosed nothing. Presence now follows the resolved value, and the
integration case asserts against the real config proxy with a control proving the
hazard is present rather than a config that happens to be plain.

The seed paths are top-level for the same reason: the leaves sit under data in the
config base and the proxy exposes them at the root, which is how every other
consumer reads them.

12 passed for the boundary, 727 across ai/mcp. The one failure there, neural-link
in McpServersHealth, was verified pre-existing by stashing these changes and
reproducing it on the clean tree.

* chore(docs): regenerate the class hierarchy for the new reporter (#17356)

The freshness gate is not bookkeeping: `extends` is hashed into every Knowledge
Base chunk id, so committing a new class without regenerating this file makes the
next ingest re-identify the affected chunks and mark the existing corpus stale.

Generated rather than hand-edited, and the delta verified at token level rather
than by eye -- the file is one line, so a raw diff shows every service name in the
tree and proves nothing. Parsed comparison: exactly one token added,
Neo.ai.mcp.server.shared.services.ResolvedConfigReporterService, and zero removed.
The disclosure helper does not appear because it is pure functions, not a class.

* fix(ai): an enum disclosure kind enumerates, so the second floor is membership (#17356)

Clause 4 promised that a declared kind stops a token reaching the wire even if a
refactor moves something unexpected behind an allowlisted path. That held for
`number` and `boolean`, which are structurally credential-proof. It did not hold
for `enum`, whose check was a 64-character length bound — and a credential
shorter than the bound passes a length bound.

An `enum` entry now declares its permitted `values`, refused at load when absent
or empty for the same reason a wildcard path is refused: an enum that does not
enumerate is a match region covering every string, arriving through the kind
instead of the path. Disclosure is membership in that set. A `values` list on
`number` or `boolean` is refused rather than ignored, because a decorative
constraint is worse than an absent one — the next reader believes it.

Verified by mutation rather than by inspection. Reverting the check to the old
length test discloses a 26-character credential-shaped string behind an
enum-declared path; the spec fixture is deliberately that size, because a
long-string fixture cannot tell a set check from a length check.

The fixture originally carried a realistic `glpat-` prefix and GitHub push
protection blocked it, classifying an invented value as a live GitLab token. It
now keeps the shape without the vendor grammar.

No production migration: every allowlist entry that ships is `number`, so the
only enum declarations in the tree were spec fixtures.

Found by @neo-opus-ada reviewing PR #17362."
- 2026-08-19T05:53:11Z @tobiu closed this issue

