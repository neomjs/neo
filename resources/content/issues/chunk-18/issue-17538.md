---
id: 17538
title: 'The dock splitter''s visual language lives in apps, not the engine'
state: CLOSED
labels:
  - bug
  - ai
  - refactoring
assignees:
  - neo-opus-grace
createdAt: '2026-08-22T12:21:03Z'
updatedAt: '2026-08-22T13:52:23Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17538'
author: neo-opus-grace
commentsCount: 0
parentIssue: 17241
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-22T13:52:23Z'
---
# The dock splitter's visual language lives in apps, not the engine

## Context

The second leaf of #17241, split out for the same reason its sibling #17522 was: the parent covers the dock's whole visual language moving from `apps/` into the engine, and that is more than one PR can honestly close.

#17522 took the **rail tabs** — both apps declare the same structure and differ only in values, so it was mechanical. The **splitter** was deliberately left behind, because the engine's neutral default there was an open design decision rather than a refactor. That decision is now made and recorded on #17241 (@neo-fable, 2026-08-22): *option 3 — the engine ships a discoverable affordance; an app opts out by token.* This ticket implements it.

Filed on @neo-gpt's RA-1 against PR #17531, which correctly found that `Resolves #17241` over-closes the parent: the head delivers the splitter half only, while #17241 additionally retains the edge/band/rail chrome, the dark-theme home decision, and the cross-cutting net-LOC criterion. #17241 stays **open** as the two-leaf parent.

Live latest-open sweep: latest 20 open issues checked 2026-08-22T12:19:54Z; no equivalent — #17522 is the rail sibling, #17211 is the cockpit-ergonomics escalation that consumes this. A2A in-flight claim sweep: 30 most recent messages, newest 2026-08-22T01:24:41Z; no claim on the splitter scope.

## The Problem

`resources/scss/src/dashboard/Container.scss` is the engine home for `Neo.dashboard.Container`. Before this work it gave `.neo-dashboard-dock-splitter` `flex-shrink`, `position`, `touch-action`, `z-index`, a hit-target `::before` and a cursor — **no background, no border, no handle, no hover, no active**. Line 159 said so in place: *"Demo-consumable minimal hooks only — the real visual language lands with the F-tranche."*

`resources/scss/src/apps/workstation/Workspace.scss:31-82` — an *application* stylesheet — carried the entire affordance: band background, inset ring, a 36×2px `::after` grip, hover glow, active state.

So a consumer that adopts the dock inherits an **invisible drag target**. That is not a neutral default; it is the reported defect #17211 (finding 1) was filed about, and it is why the splitter is discoverable in the workstation and invisible in the FleetCockpit — the two are not styled by the same layer at all.

## The Architectural Reality

- `resources/scss/src/dashboard/Container.scss:113` — the engine splitter rule, structure only
- `Container.scss:14-27` — the existing `--dock-*` token precedent, **motion only**; paint is the gap
- `resources/scss/src/apps/workstation/Workspace.scss:31-82` — the app-owned affordance, already token-driven but reading `--workstation-*`, so its content is the promotion candidate almost verbatim and only its token names are app-specific
- `resources/scss/src/apps/agentos/fleet/FleetCockpit.scss:134` — FM's partial horizontal-splitter override

The workstation's active state carried `opacity: 1 !important`. An `!important` means a competing rule is being *fought* rather than resolved; the promotion must fix the specificity cause instead of carrying the escape hatch into the engine.

## The Fix

Three layers, and the middle one is the whole design:

| layer | owner | content |
|---|---|---|
| structure | engine, **untokened** | `flex-shrink`, `position`, `touch-action`, `z-index`, the hit-target `::before` |
| **affordance floor** | engine, **token defaults** | visible band, the 36×2 centred handle, hover raise, functional active — **without** the pill ring and outer glow |
| identity | app, **token values** | workstation re-adds ring + glow; FM declares `--dock-splitter-handle-size: 0` |

Ring and glow are workstation **signal-language** — they were born reading `--workstation-signal` — so shipping them as the engine default would push one consumer's identity onto every other. FM's flat splitter becomes an explicit `--dock-splitter-handle-size: 0`: a design statement that greps, rather than an absence that reads as an oversight.

Neutral defaults mix from `currentColor` rather than a literal grey — the engine cannot know the host palette, and a guess reads wrong on half of them.

SCSS token/skin layers only; zero CSS-in-JS.

## Contract Ledger Matrix

The consumed token surface. Source of authority for every row is `resources/scss/src/dashboard/Container.scss` (engine defaults) — each verified present in that file at `450ef52b68` except the `-active` handle row, which is the gap this ticket closes.

| Target surface | Proposed behavior | Fallback (no app tokens) | Evidence |
|---|---|---|---|
| `--dock-splitter-background` / `-hover` / `-active` | band paint per state | `color-mix(currentColor 9% / 16% / 26%, transparent)` | engine-only consumer renders a visible band |
| `--dock-splitter-radius` | band + handle rounding | `999px` | computed style |
| `--dock-splitter-handle-size` / `-thickness` | handle long / short axis; `size: 0` is the documented opt-out | `36px` / `2px` | FM opt-out grep + computed style |
| `--dock-splitter-handle-color` / `-hover` | handle paint, resting and hover | `color-mix(currentColor 45%, transparent)` / `currentColor` | computed style, both states |
| **`--dock-splitter-handle-color-active`** | **handle paint while dragging — the missing row** | **`var(--dock-splitter-handle-color-hover)`** | **computed style in the active state; workstation sets the value only** |
| `--dock-splitter-ring` / `-hover` / `-active` | identity slot | `none` — engine never fills it | guard asserts the `none` default |
| `--dock-splitter-handle-glow-hover` | identity slot | `none` — engine never fills it | guard asserts the `none` default |

## Decision Record impact

`aligned-with ADR 0029` (`learn/agentos/decisions/0029-docking-design.md` — renamed 2026-08-21 from `0029-harness-docking-design.md` by #17503; the harness misnomer is retired, persisted schema strings stay frozen). Presentation only — behavior and state ownership are unchanged.

## Acceptance Criteria

- [ ] `.neo-dashboard-dock-splitter` renders a discoverable affordance from the engine layer alone — asserted against `examples/dashboard/dock`, the consumer #17211 was filed about, with **no** app tokens set
- [ ] The engine's affordance floor is token defaults; ring and glow remain empty identity slots the engine never fills
- [ ] **No application stylesheet paints a splitter, including in its own nested state and pseudo blocks.** `&:active::after` is a state of the splitter itself, not a descendant, and must stay inside the census
- [ ] The census **rejects every visual property except an explicit allowlist of documented structural deviations** — a fixed five-property denylist lets `filter`, `outline` or `color` re-enter undetected
- [ ] The active handle is an engine token consumed by the engine rule; `Workspace.scss` sets a value and declares no `background`
- [ ] FM's flat splitter is an explicit `--dock-splitter-handle-size: 0`, not an omission
- [ ] No `!important` in the promoted engine rule; if one seems required, the specificity cause is fixed instead
- [ ] **Resting, hover and active computed styles are asserted in both apps and both themes**, including parity for the values meant to stay unchanged and FM's flat opt-out — rendered effect, not SCSS source shape
- [ ] Mutation: reintroducing nested app-side active paint reddens the census
- [ ] Mutation: removing the engine token block makes the engine-only consumer fail, so the test observes the **promotion** rather than an app override

## Out of Scope

- **The rail tabs** — #17522's leaf, delivered by PR #17524.
- The dock edge zone / row / band / rail chrome, the dark-theme home decision, and the cross-cutting net-LOC criterion. Those stay on #17241; this leaf does not close the parent.
- Dock behavior, drag mechanics, and the `DockSplitter` component API — presentation only.
- Whether FM *keeps* a flat splitter now that a real handle exists. That is @neo-fable-clio's call on FM's design language, and the engine answer does not depend on it.

## Avoided Traps

**Fixing FM by copying the workstation's SCSS into `FleetCockpit.scss`.** That is how the current state arose; a third copy makes the next consumer's bug certain.

**Promoting the workstation's tokens as-is.** `--workstation-signal` in an engine stylesheet inverts the dependency this work exists to fix.

**Asserting the promotion on SCSS source alone.** A source guard proves declarations exist and some properties are absent; it cannot prove token resolution, visible output, hover/active parity, selector load order, or consumer adoption. The close-target criterion is rendered effect.

**Verifying by screenshot.** The visual suite runs in no workflow and 8/8 goldens fail on an unmodified tree (#17518), so it cannot discriminate this change. Computed-style equality asserts the property rather than a rendering of it.

## Related

- #17241 — parent; retains the edge/band/rail chrome, the dark-theme home, and net LOC. Stays open. Whether it should convert from `enhancement` to `epic` now that it has two leaves is a taxonomy call left to its closeout, not decided here
- #17522 / PR #17524 — the rail-tab sibling leaf
- #17211 — cockpit ergonomics; its splitter-visibility finding is discharged by this leaf
- #17514 / PR #17515 — the Body-side architecture guide specifying the neutral-token shape
- PR #17531 — the in-flight implementation, retargeted here from #17241 on @neo-gpt's RA-1
- ADR 0029

Retrieval Hint: `dock splitter affordance floor engine token promotion Container.scss Workspace.scss active handle`

Origin Session ID: cb3eb9c7-875a-4eac-a716-02878fa535c5



## Timeline

- 2026-08-22T12:21:05Z @neo-opus-grace added the `bug` label
- 2026-08-22T12:21:05Z @neo-opus-grace added the `ai` label
- 2026-08-22T12:21:05Z @neo-opus-grace added the `refactoring` label
- 2026-08-22T12:21:13Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-22T12:21:16Z @neo-opus-grace added parent issue #17241
- 2026-08-22T12:24:26Z @neo-opus-grace referenced in commit `e2bdf86` - "fix(dashboard): the active handle becomes an engine token, and the census can see it (#17538)

Addresses @neo-gpt's RA-2 on PR #17531. Two defects, and the second is why the
first survived review-by-CI.

The promotion left one state behind. Workstation still carried
`&:active::after { background: var(--workstation-signal) }` — a direct paint on
an engine internal, in the one PR whose whole claim is that apps set values
only. The engine had `--dock-splitter-handle-color` and `-hover` but no
`-active`, so there was nowhere else for that colour to go. It now exists,
defaults to the hover value — dragging must never read as less engaged than
hovering — and the engine's own `:active` block consumes it. Workstation sets
the value and declares nothing.

The census could not see the violation. `splitterOwnDeclarations` discarded
every nested block at `d === 0`, and `&:active::after` is a state of the
splitter itself rather than a descendant, so the "no application stylesheet
paints a splitter" arm passed a tree that contained exactly that. Green CI was
a positive proof of the blind spot, not of the property.

The census now descends into self-state and pseudo blocks and skips real
descendants, distinguished by whether every comma-separated part starts with
`&` and carries no descendant combinator. It also walks at-rules, because
FleetCockpit's only structural deviation lives inside a `@container` query and
a top-level-only walk would have reported that file clean whatever it held.

The five-property denylist became an allowlist. `background`,
`background-color`, `border-radius`, `box-shadow` and `opacity` were the
properties someone had thought of; `filter`, `outline` and `color` were not. A
guard that only catches an enumeration confirms a memory instead of catching a
regression. Every non-custom property is now an offence unless it is a
documented structural deviation, of which there is currently one: FM's
`display: none`, which takes the splitter out of flow when a stacked split
makes its drag axis wrong.

Mutated against the real tree, not only fixtures: restoring the exact
`Workspace.scss` block reddens the file-scan arm with
`workstation/Workspace.scss -> background on .neo-dashboard-dock-splitter
&:active::after`. Three instrument controls ship with it — a descendant stays
out of scope, a declaration preceding a nested block is not swallowed, and a
splitter under an at-rule is still censused.

Retargeted to #17538, the splitter leaf, per RA-1; #17241 stays open as the
two-leaf parent."
- 2026-08-22T12:31:03Z @neo-opus-grace cross-referenced by #17241
- 2026-08-22T12:31:18Z @neo-opus-grace referenced in commit `033cc90` - "test(dashboard): the splitter affordance gets a rendered witness (#17538)

Addresses @neo-gpt's RA-3 on PR #17531. The layering census reads SCSS source;
it proves declarations exist and that no app paints a splitter. It cannot prove
the thing this leaf is about — that a consumer setting nothing gets a findable
drag target — because the reported defect was a splitter whose declarations all
existed and resolved to nothing visible.

Four arms on a real rendered splitter: the engine floor in both neo themes, the
active handle token, and the flat opt-out.

The controls are the arms. Each measures an exact token-derived value — 36px
long axis, 2px short axis — rather than "non-zero", because nothing else in the
document produces that pair, so an unloaded stylesheet cannot pass. The band is
compared against a plain sibling div in the same document and theme, which fixes
what "unpainted" computes to here instead of assuming how the browser serialises
transparency. The opt-out arm asserts its own precondition — the handle was 36px
before — so collapsing to 0px cannot be satisfied by a handle that was never
there.

Three fixture hazards from the rail-tab witness are carried across because each
produced a wrong green there. The test app pins both neo themes: the default set
is the legacy pair plus one neo theme, and half the variables the floor mixes
against never load in the legacy ones. The dashboard is a REAL
`dashboard/Container` rather than a div wearing `.neo-dashboard` — Neo loads a
class's stylesheet on instantiation, so faking the ancestor leaves the rule under
test out of the document. And transitions are frozen for the active measurement:
the engine transitions `background`, so reading after a state change returns the
start value and a frame later returns a blend.

Mutated, per the RA's own wording that the mutation must prove the engine floor
rather than an app override. Removing the engine's `:active` `&::after` consumer
reds the active arm — the app token stops arriving and the handle falls back to
rgb(240, 242, 240). Removing `background-color: var(--dock-splitter-background)`
reds the floor arm in BOTH themes, which is the pre-promotion state restated as
a measurement.

Bound honestly: this drives the engine classes the dock example mounts, not that
page. Cross-app computed-style parity is not claimed here."
- 2026-08-22T12:35:48Z @neo-opus-grace referenced in commit `2e05708` - "test(dashboard): both real apps, both themes, all three states (#17538)

Completes @neo-gpt's RA-3. The previous four arms proved the engine floor and
that the engine consumes the active token — but through a rule authored in the
spec, and a rule nobody ships proves nothing about either app.

Four more arms load the COMPILED artifacts instead: the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. The splitter then wears
`.workstation-workspace` or `.fm-fleet-cockpit` and is measured resting,
hovered and pressed, in both neo themes.

The parity claim is narrow and exact. Before this PR, `Workspace.scss` painted
`&:active::after { background: var(--workstation-signal) }` directly. The arm
requires the active handle to still compute to precisely that colour, now
arriving through `--dock-splitter-handle-color-active`. Removing the engine's
`:active` consumer reds it in both themes: the handle falls back to the hover
mix instead of the signal.

Three fixture defects were found by building this, each of which had produced a
green or a red for the wrong reason:

The app rule alone measures nothing. `--workstation-signal` is declared under
`:root .neo-theme-neo-*`, not in the app stylesheet, so loading only the rule
left every `color-mix()` an invalid substitution and the band computed to
transparent. Both layers load now, and a failed link is a loud reject rather
than a skip — silently missing tokens would measure the ENGINE floor while
reporting on the app.

`resting` was measured while hovering. The splitter renders at (0, 0) at 6x720,
so parking the pointer at the origin leaves it inside the element. The away
point is derived from the box, and all three readings now assert their own
pointer state, so the mistake cannot return silently. It surfaced only because
FM gives hover and active the same value — which is also why the three-step
ladder is asserted for workstation only. Asserting it for FM would assert a
design FM does not have.

A custom property comes back verbatim (`#0f766e`) while `backgroundColor` comes
back serialised (`rgb(15, 118, 110)`). String-comparing them reported a parity
failure on identical colours; the token is now resolved through the browser
before comparison."
- 2026-08-22T12:37:01Z @neo-opus-grace cross-referenced by PR #17531
- 2026-08-22T13:13:13Z @neo-opus-grace referenced in commit `02089de` - "test(dashboard): the named consumer and the pre-promotion values both get arms (#17538)

Closes @neo-gpt's RA-3, both clauses. His disposition was right on each: the
witness was real but drove classes I wired myself, and the residual narrowed
parity to the one value this PR moved rather than the unchanged-value boundary
the close target actually names.

The example is now driven as a PAGE. `examples/dashboard/dock` needs no
interaction — its initial dock document commits two splits, so both splitter
orientations render on load — and it sets no `themes`, so it boots under the
LEGACY `neo-theme-light`. That makes it the strongest available statement of the
floor rather than a weaker one: a consumer that opted into nothing, not even a
neo theme, still gets a painted band and a 36x2 handle. The arm asserts exact
token-derived geometry per orientation, because "non-zero" would also pass on a
stylesheet that failed to load and left the pseudo-element at `auto` — which is
precisely what this page rendered before the promotion. It also asserts no app
override is in play first, so the floor cannot be credited to someone else's
tokens.

Parity is now the real before/after. `PRE_PROMOTION_WORKSTATION` carries the
declarations verbatim from `git show
origin/dev:resources/scss/src/apps/workstation/Workspace.scss` — band, ring and
handle for resting, hover and active — and each is resolved through a probe in
the same scoped subtree rather than hard-coded as hex. That keeps it a
comparison instead of a transcription: a theme whose `--workstation-signal`
moved would move both sides, so the arm tests the relationship rather than a
frozen colour. All nine values match in both neo themes, which is the promotion
being value-preserving rather than merely plausible.

Mutated: dropping the engine handle size to 0 reds the example arm on the long
axis, and drifting workstation's resting band from 9% to 12% reds both parity
arms. A non-vacuity check requires each expectation to have resolved to a real
colour, since an unresolvable expression computes the same empty value on both
sides and equality alone would be two nothings matching.

Two mechanical notes worth keeping: `probe.style[camelCase] = value` rather than
`setProperty`, which expects kebab-case and silently no-ops on `backgroundColor`
— leaving the probe reading its inherited value and the comparison aimed at the
wrong thing. And the probe must live INSIDE the scoped subtree, or
`--workstation-signal` is undefined on both sides for the same reason."
- 2026-08-22T13:24:09Z @neo-opus-grace cross-referenced by #17539
- 2026-08-22T13:28:56Z @neo-fable-clio cross-referenced by #17543
- 2026-08-22T13:52:23Z @tobiu referenced in commit `5d23f96` - "The dock splitter's visual language lives in apps, not the engine (#17531)

* feat(dashboard): the engine ships a discoverable splitter, apps set identity (#17241)

Completes #17241 after the rail-tab leaf (#17522). Design decision from
@neo-fable, recorded on the ticket rather than left in A2A.

The engine had NO splitter paint — flex-shrink, position, touch-action,
z-index and a hit-target ::before. Both apps owned the entire visual
language, so a consumer adopting the dock got an INVISIBLE drag target. That
is the reported defect, not a neutral default, which is why the engine's
floor here is discoverability rather than blankness.

Three layers, and the middle one is the design:

  structure        engine, untokened
  affordance floor engine token DEFAULTS — band, 36x2 centred handle, plain
                   hover, functional active
  identity         app token values

The pill ring and outer glow are NOT in the floor. They were born reading
--workstation-signal, so they are consumer signal-language; shipping them as
the engine default would push one app's identity onto every other consumer —
this promotion's own defect pointed the other way. They ship as `none` and
workstation re-adds them by value. That split is Fable's correction to my
original two-layer reading, and it was a correction rather than a nuance.

Defaults mix against `currentColor`: the engine cannot know a host palette,
and a literal grey reads wrong on half of them.

FM's flat splitter becomes an explicit `--dock-splitter-handle-size: 0` — a
design statement that greps, rather than an absence that reads as oversight,
and one that survives the engine gaining a handle. Whether FM keeps flat now
that a discoverable handle exists is @neo-fable-clio's call on FM's design
language; the engine answer does not depend on it.

The workstation `opacity: 1 !important` is GONE, not moved. It meant a
competing rule was being fought; the engine now owns the active state at its
own specificity, so the conflict does not exist at this layer. Carrying the
escape hatch across would have laundered a specificity defect into the engine
under a no-visual-change banner. A guard arm now fails on any !important in
the promoted rule.

Also hoisted the floor above the nested rules: Sass emits trailing
declarations as a second same-selector block — valid, invisible in behaviour,
and confusing in the built output.

Five guard arms, three mutations, each reddening its own: an app re-painting
reports the file+property+selector; an engine-shipped ring fails the
identity-slot arm; an !important fails its own.

136 buildScripts, 50 component specs. Verified on compiled CSS: the engine
emits the floor with var() indirection, both apps emit only custom properties.

* fix(dashboard): the active handle becomes an engine token, and the census can see it (#17538)

Addresses @neo-gpt's RA-2 on PR #17531. Two defects, and the second is why the
first survived review-by-CI.

The promotion left one state behind. Workstation still carried
`&:active::after { background: var(--workstation-signal) }` — a direct paint on
an engine internal, in the one PR whose whole claim is that apps set values
only. The engine had `--dock-splitter-handle-color` and `-hover` but no
`-active`, so there was nowhere else for that colour to go. It now exists,
defaults to the hover value — dragging must never read as less engaged than
hovering — and the engine's own `:active` block consumes it. Workstation sets
the value and declares nothing.

The census could not see the violation. `splitterOwnDeclarations` discarded
every nested block at `d === 0`, and `&:active::after` is a state of the
splitter itself rather than a descendant, so the "no application stylesheet
paints a splitter" arm passed a tree that contained exactly that. Green CI was
a positive proof of the blind spot, not of the property.

The census now descends into self-state and pseudo blocks and skips real
descendants, distinguished by whether every comma-separated part starts with
`&` and carries no descendant combinator. It also walks at-rules, because
FleetCockpit's only structural deviation lives inside a `@container` query and
a top-level-only walk would have reported that file clean whatever it held.

The five-property denylist became an allowlist. `background`,
`background-color`, `border-radius`, `box-shadow` and `opacity` were the
properties someone had thought of; `filter`, `outline` and `color` were not. A
guard that only catches an enumeration confirms a memory instead of catching a
regression. Every non-custom property is now an offence unless it is a
documented structural deviation, of which there is currently one: FM's
`display: none`, which takes the splitter out of flow when a stacked split
makes its drag axis wrong.

Mutated against the real tree, not only fixtures: restoring the exact
`Workspace.scss` block reddens the file-scan arm with
`workstation/Workspace.scss -> background on .neo-dashboard-dock-splitter
&:active::after`. Three instrument controls ship with it — a descendant stays
out of scope, a declaration preceding a nested block is not swallowed, and a
splitter under an at-rule is still censused.

Retargeted to #17538, the splitter leaf, per RA-1; #17241 stays open as the
two-leaf parent.

* test(dashboard): the splitter affordance gets a rendered witness (#17538)

Addresses @neo-gpt's RA-3 on PR #17531. The layering census reads SCSS source;
it proves declarations exist and that no app paints a splitter. It cannot prove
the thing this leaf is about — that a consumer setting nothing gets a findable
drag target — because the reported defect was a splitter whose declarations all
existed and resolved to nothing visible.

Four arms on a real rendered splitter: the engine floor in both neo themes, the
active handle token, and the flat opt-out.

The controls are the arms. Each measures an exact token-derived value — 36px
long axis, 2px short axis — rather than "non-zero", because nothing else in the
document produces that pair, so an unloaded stylesheet cannot pass. The band is
compared against a plain sibling div in the same document and theme, which fixes
what "unpainted" computes to here instead of assuming how the browser serialises
transparency. The opt-out arm asserts its own precondition — the handle was 36px
before — so collapsing to 0px cannot be satisfied by a handle that was never
there.

Three fixture hazards from the rail-tab witness are carried across because each
produced a wrong green there. The test app pins both neo themes: the default set
is the legacy pair plus one neo theme, and half the variables the floor mixes
against never load in the legacy ones. The dashboard is a REAL
`dashboard/Container` rather than a div wearing `.neo-dashboard` — Neo loads a
class's stylesheet on instantiation, so faking the ancestor leaves the rule under
test out of the document. And transitions are frozen for the active measurement:
the engine transitions `background`, so reading after a state change returns the
start value and a frame later returns a blend.

Mutated, per the RA's own wording that the mutation must prove the engine floor
rather than an app override. Removing the engine's `:active` `&::after` consumer
reds the active arm — the app token stops arriving and the handle falls back to
rgb(240, 242, 240). Removing `background-color: var(--dock-splitter-background)`
reds the floor arm in BOTH themes, which is the pre-promotion state restated as
a measurement.

Bound honestly: this drives the engine classes the dock example mounts, not that
page. Cross-app computed-style parity is not claimed here.

* test(dashboard): both real apps, both themes, all three states (#17538)

Completes @neo-gpt's RA-3. The previous four arms proved the engine floor and
that the engine consumes the active token — but through a rule authored in the
spec, and a rule nobody ships proves nothing about either app.

Four more arms load the COMPILED artifacts instead: the app's own stylesheet
plus the theme-scoped token layer its values reference, both from `dist/**`,
the same files Neo loads at runtime. The splitter then wears
`.workstation-workspace` or `.fm-fleet-cockpit` and is measured resting,
hovered and pressed, in both neo themes.

The parity claim is narrow and exact. Before this PR, `Workspace.scss` painted
`&:active::after { background: var(--workstation-signal) }` directly. The arm
requires the active handle to still compute to precisely that colour, now
arriving through `--dock-splitter-handle-color-active`. Removing the engine's
`:active` consumer reds it in both themes: the handle falls back to the hover
mix instead of the signal.

Three fixture defects were found by building this, each of which had produced a
green or a red for the wrong reason:

The app rule alone measures nothing. `--workstation-signal` is declared under
`:root .neo-theme-neo-*`, not in the app stylesheet, so loading only the rule
left every `color-mix()` an invalid substitution and the band computed to
transparent. Both layers load now, and a failed link is a loud reject rather
than a skip — silently missing tokens would measure the ENGINE floor while
reporting on the app.

`resting` was measured while hovering. The splitter renders at (0, 0) at 6x720,
so parking the pointer at the origin leaves it inside the element. The away
point is derived from the box, and all three readings now assert their own
pointer state, so the mistake cannot return silently. It surfaced only because
FM gives hover and active the same value — which is also why the three-step
ladder is asserted for workstation only. Asserting it for FM would assert a
design FM does not have.

A custom property comes back verbatim (`#0f766e`) while `backgroundColor` comes
back serialised (`rgb(15, 118, 110)`). String-comparing them reported a parity
failure on identical colours; the token is now resolved through the browser
before comparison.

* test(dashboard): the named consumer and the pre-promotion values both get arms (#17538)

Closes @neo-gpt's RA-3, both clauses. His disposition was right on each: the
witness was real but drove classes I wired myself, and the residual narrowed
parity to the one value this PR moved rather than the unchanged-value boundary
the close target actually names.

The example is now driven as a PAGE. `examples/dashboard/dock` needs no
interaction — its initial dock document commits two splits, so both splitter
orientations render on load — and it sets no `themes`, so it boots under the
LEGACY `neo-theme-light`. That makes it the strongest available statement of the
floor rather than a weaker one: a consumer that opted into nothing, not even a
neo theme, still gets a painted band and a 36x2 handle. The arm asserts exact
token-derived geometry per orientation, because "non-zero" would also pass on a
stylesheet that failed to load and left the pseudo-element at `auto` — which is
precisely what this page rendered before the promotion. It also asserts no app
override is in play first, so the floor cannot be credited to someone else's
tokens.

Parity is now the real before/after. `PRE_PROMOTION_WORKSTATION` carries the
declarations verbatim from `git show
origin/dev:resources/scss/src/apps/workstation/Workspace.scss` — band, ring and
handle for resting, hover and active — and each is resolved through a probe in
the same scoped subtree rather than hard-coded as hex. That keeps it a
comparison instead of a transcription: a theme whose `--workstation-signal`
moved would move both sides, so the arm tests the relationship rather than a
frozen colour. All nine values match in both neo themes, which is the promotion
being value-preserving rather than merely plausible.

Mutated: dropping the engine handle size to 0 reds the example arm on the long
axis, and drifting workstation's resting band from 9% to 12% reds both parity
arms. A non-vacuity check requires each expectation to have resolved to a real
colour, since an unresolvable expression computes the same empty value on both
sides and equality alone would be two nothings matching.

Two mechanical notes worth keeping: `probe.style[camelCase] = value` rather than
`setProperty`, which expects kebab-case and silently no-ops on `backgroundColor`
— leaving the probe reading its inherited value and the comparison aimed at the
wrong thing. And the probe must live INSIDE the scoped subtree, or
`--workstation-signal` is undefined on both sides for the same reason."
- 2026-08-22T13:52:23Z @tobiu closed this issue

