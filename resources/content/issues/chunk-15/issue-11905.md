---
id: 11905
title: 'Sub 1: Layer 2 — heartbeat target resolver (BLOCKING; resolveSwarmHeartbeatTargets contract)'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - model-experience
assignees:
  - neo-opus-ada
createdAt: '2026-05-24T14:01:00Z'
updatedAt: '2026-05-24T15:56:28Z'
githubUrl: 'https://github.com/neomjs/neo/issues/11905'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-24T15:56:28Z'
---
# Sub 1: Layer 2 — heartbeat target resolver (BLOCKING; resolveSwarmHeartbeatTargets contract)

## Parent Epic

#11829 — Sub 1 of 5. **BLOCKING** for cross-family symmetric heartbeat delivery (AC1).

## Premise

`SwarmHeartbeatService.pulse()` currently invokes `checkSunsetted` + `idleOutNudge` per `this.identity` (single-identity binding at `ai/daemons/services/SwarmHeartbeatService.mjs:102` + `:129-131` + `:199` + `:224`). Per Epic AC1 + AC2: needs deployment-portable resolver so single Orchestrator lane loops over N resolved targets — NOT N service-instances; NOT direct identityRoots iteration.

## Prescription

Implement `resolveSwarmHeartbeatTargets()` with precedence chain per Epic AC2:
1. Explicit env/config target list
2. `orchestrator.swarmHeartbeat.targetSource ∈ {self | active-local-team | active-subscribers | disabled}`
3. Public default `self`
4. Opt-in `active-local-team` uses `ai/graph/identityRoots.mjs` filtered on `participationStatus === 'active'` (Neo team profile only)
5. Cloud / fork safety per [ADR 0014](https://github.com/neomjs/neo/blob/dev/learn/agentos/decisions/0014-cloud-deployment-topology-and-scheduler-task-taxonomy.md#L149-L159)

Wire `SwarmHeartbeatService.pulse()` to loop over resolved targets, calling `checkSunsetted` + `idleOutNudge` per target.

## Acceptance Criteria

- [ ] AC1: `resolveSwarmHeartbeatTargets()` exported pure-function with 5-step precedence chain
- [ ] AC2: `pulse()` invokes per resolved target (not just `this.identity`); orchestrator log shows N nudge lines where N = resolver output cardinality
- [ ] AC3: No-config external workspace defaults to `self` OR disables-with-log — NEVER silently fans out to Neo maintainer identities
- [ ] AC4: Unit tests in `test/playwright/unit/ai/daemons/orchestrator/services/SwarmHeartbeatService.spec.mjs` prove: no-config-defaults-self / explicit-target-list / active-local-team / unknown-target-fail-closed

## Avoided Traps (per Epic)

- ❌ Iterating directly over `identityRoots.mjs` IDENTITIES as heartbeat target source — registry is Neo-maintainer-team, NOT framework-portable; external consumers MUST NOT silently inherit Neo identities
- ❌ N service-instances instead of single-lane multi-target loop
- ❌ Hardcoding Neo maintainer identities into framework default

## Depends on

(none — first sub, no dependencies)

## Unblocks

- Sub 2 (#TBD): Layer 1 wake-content enrichment
- Sub 3 (#TBD): Layer 3 per-turn anti-pattern surface
- Sub 4 (#TBD): Layer 5 sunset-handoff pickup queue
- AC7 (Epic): cross-family symmetric validation — GPT's custom Codex nightshift-lifecycle-driver watchdog retirable once this lands

## Related

- Epic #11829 (parent)
- Discussion #11823 (graduation source — Cycle-2.7 `[GRADUATION_APPROVED]`)
- ADR 0014 cloud-deployment-topology profile-default precedent
- `ai/daemons/services/SwarmHeartbeatService.mjs:102/:129-131/:199/:224` — single-identity binding (the substrate gap this sub fixes)

**Authored by:** Claude Opus 4.7 (Claude Code)

## Contract Ledger (added per GPT PR #11913 cycle-1 RA2)

| Surface | Type | Default | Valid Values | Failure Semantics |
|---|---|---|---|---|
| `NEO_ORCHESTRATOR_SWARM_HEARTBEAT_TARGET_SOURCE` | env string | unset → null | `self` \| `active-local-team` \| `active-subscribers` \| `disabled` | invalid value → coerce to null + warn log (resolver then defaults to `'self'`) |
| `NEO_ORCHESTRATOR_SWARM_HEARTBEAT_TARGETS` | env CSV string | unset → null | comma-separated `@handle` list (handles auto-normalized to canonical `@<id>` form) | empty after normalize → null (falls through to `targetSource` semantics) |
| `orchestrator.swarmHeartbeat.targetSource` (AiConfig) | string\|null | `null` | same as env var above | same as env var above |
| Resolver precedence | function | `explicitTargets > targetSource > 'self'` | (precedence chain) | unknown source → fallback to `'self'` + warn log |
| `'self'` source | enum branch | returns `[selfIdentity]` | `selfIdentity: string\|null` | `null selfIdentity` → returns `[]` + info log naming `NEO_AGENT_IDENTITY` + `targetSource='disabled'` knobs (AC3 disables-with-log) |
| `'disabled'` source | enum branch | returns `[]` + info log | (none) | always `[]` + info log |
| `'active-local-team'` source | enum branch | reads `identityRoots.IDENTITIES` filtered on `type==='AgentIdentity'` AND `properties.participationStatus==='active'` | (none) | empty filter → returns `[]` (no log; substrate is empty by design) |
| `'active-subscribers'` source | enum branch | unions `selfIdentity` + injected provider output | provider: `() => Promise<String[]>` | missing provider → fallback to `'self'` + warn log + info log if `selfIdentity` is also null |
| `beforeSetIdentity` (SwarmHeartbeatService) | config-set hook | returns `null` on empty | string\|null | empty/null value → `null` (Sub 1 #11905 AC3 pivot: removed `DEFAULT_IDENTITY` fallback to prevent fork-leak) |
| `beforeSetTargetSource` (SwarmHeartbeatService) | config-set hook | returns `null` on empty/invalid | one of `VALID_TARGET_SOURCES` or null | invalid value → coerce to null + warn log (resolver then defaults to `'self'`) |
| `beforeSetExplicitTargets` (SwarmHeartbeatService) | config-set hook | returns `null` on empty/non-array | string[] | non-array OR empty array → `null`; per-element normalized via `normalizeAgentIdentityNodeId` |

**Public-contract surface count:** 3 env vars + 1 AiConfig key + 4 resolver enum branches + 3 config-set hooks = 11 named surface points. All 11 covered by tests in `test/playwright/unit/ai/daemons/orchestrator/scheduling/swarmHeartbeat.spec.mjs` (18 pure-function tests) + `test/playwright/unit/ai/daemons/orchestrator/services/SwarmHeartbeatService.spec.mjs` (5 integration tests + 1 AC3 fork-safety integration test).


## Timeline

- 2026-05-24T14:01:01Z @neo-opus-ada added the `enhancement` label
- 2026-05-24T14:01:01Z @neo-opus-ada added the `ai` label
- 2026-05-24T14:01:01Z @neo-opus-ada added the `architecture` label
- 2026-05-24T14:01:02Z @neo-opus-ada added the `model-experience` label
- 2026-05-24T14:01:35Z @neo-opus-ada cross-referenced by #11906
- 2026-05-24T14:02:33Z @neo-opus-ada cross-referenced by #11909
- 2026-05-24T14:02:56Z @neo-opus-ada cross-referenced by #11829
- 2026-05-24T14:07:50Z @neo-opus-ada cross-referenced by PR #11910
- 2026-05-24T14:12:57Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-05-24T14:31:37Z @neo-opus-ada cross-referenced by PR #11913
- 2026-05-24T14:52:09Z @neo-opus-ada cross-referenced by PR #11914
- 2026-05-24T14:58:15Z @neo-opus-ada referenced in commit `a35702d` - "feat(orchestrator): make AC3 null-self path observable + Contract Ledger (#11905)

Addresses GPT PR #11913 cycle-1 review:

RA1 — null-self disables-with-log observability:
The resolver's `'self'` branch now emits an info log when `selfIdentity` is null
before returning `[]`. AC3 wording is "defaults to 'self' OR disables-with-log
— NEVER silently fans out to Neo maintainer identities"; silent `[]` satisfied
the no-leak property but missed the disables-with-log property. The log names
both operator knobs (`NEO_AGENT_IDENTITY` env + `targetSource='disabled'`
config) so the misconfiguration is actionable. Extracted `selfFallback()` helper
so the unknown-source and active-subscribers-missing-provider branches share
the same observable behavior.

3 new unit tests assert the log fires in each fallback path (default 'self',
unknown source, active-subscribers without provider). Existing AC3 test
upgraded to also verify the log payload contains both knob names.

RA2 — Contract Ledger:
Appended a Contract Ledger matrix to ticket #11905 covering the 11 named
public-contract surface points (3 env vars + 1 AiConfig key + 4 resolver enum
branches + 3 config-set hooks). PR body will reference the ledger.

Test evidence: `npm run test-unit -- --grep "swarmHeartbeat|SwarmHeartbeat"`
→ 43 passed (was 41 before; +2 net new tests, +1 upgraded assertion).

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>"
- 2026-05-24T15:18:26Z @neo-opus-ada referenced in commit `3bcea60` - "chore(orchestrator): strip provenance + remove dead DEFAULT_IDENTITY export (#11905)

Addresses GPT PR #11913 cycle-3 review:

RA1 - Remove dead DEFAULT_IDENTITY export:
The constant + export were retained as "legacy" in cycle-1 but no external
consumer was found. Removed both the const definition and the named export.
Test docblock + test reference updated to drop the same name.

RA2 - Strip ticket/epic/AC/review-cycle/history anchors from added/edited
source + test prose:
The why-first/no-history-in-hot-path direction shipped via the compact-only
why-first guidance applies to runtime source comments and test names too.
Stripped anchors from the resolver JSDoc, selfFallback helper docblock,
SwarmHeartbeatService identity_/targetSource_/explicitTargets_ config blocks,
beforeSetIdentity and getPulseIdentities docblocks, the AiConfig
swarmHeartbeat slot, and the corresponding spec describe + test names.
Provenance now lives in git blame + PR/ticket/commit context where it can
support archaeology without bloating the hot path.

Net delta in this commit: +106 / -132 lines across the same 5 files; total
test count unchanged at 43 passing. No behavioral change.

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>"
- 2026-05-24T15:24:13Z @neo-opus-ada referenced in commit `b8c5a5e` - "chore(orchestrator): drop residual ticket anchor from getter docblock (#11905)

Removes the trailing `Sub 1 #11905 / Epic #11829 Layer 2` anchor that escaped
the cycle-3 sweep — Orchestrator.mjs `swarmHeartbeatExplicitTargets` getter
JSDoc. GPT cycle-3 parked-recheck flagged "one source-comment anchor remains";
this is it. Provenance stays in PR/ticket/commit context.

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>"
- 2026-05-24T15:56:28Z @tobiu closed this issue
- 2026-05-24T15:56:28Z @tobiu referenced in commit `6bb49d5` - "feat(orchestrator): add resolveSwarmHeartbeatTargets with 5-step precedence chain (#11905) (#11913)

* feat(orchestrator): add resolveSwarmHeartbeatTargets with 5-step precedence chain (#11905)

Adds the deployment-portable heartbeat target resolver per Epic #11829 AC2 / Sub 1.
The pure-function `resolveTargets({selfIdentity, targetSource, explicitTargets,
activeSubscribersProvider, logger})` exported from `scheduling/swarmHeartbeat.mjs`
walks a 5-step precedence chain:

  1. Explicit env target list (`NEO_ORCHESTRATOR_SWARM_HEARTBEAT_TARGETS`)
  2. `targetSource` enum (env override + AiConfig)
  3. Default `'self'` when nullish — deployment-portable safe default
  4. `'active-local-team'` reads `identityRoots.IDENTITIES` filtered on
     `participationStatus === 'active'` (Neo-team profile only)
  5. Cloud/fork safety per ADR 0014 — unknown sources fail-closed to `'self'`

`SwarmHeartbeatService.pulse()` now delegates target resolution to the new
resolver via `getPulseIdentities()`. The pre-#11905 `getPulseIdentities()`
union-with-WAKE_SUBSCRIPTION shape is preserved via opt-in `'active-subscribers'`.

AC3 fork-safety pivot: `beforeSetIdentity` no longer falls back to
`DEFAULT_IDENTITY = '@neo-gemini-pro'`. External forks with no
`NEO_AGENT_IDENTITY` env now silently no-op the per-identity pulse work rather
than leaking a Neo maintainer identity. The constant stays exported for any
backwards-compatible consumers but is no longer load-bearing.

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>

* feat(orchestrator): make AC3 null-self path observable + Contract Ledger (#11905)

Addresses GPT PR #11913 cycle-1 review:

RA1 — null-self disables-with-log observability:
The resolver's `'self'` branch now emits an info log when `selfIdentity` is null
before returning `[]`. AC3 wording is "defaults to 'self' OR disables-with-log
— NEVER silently fans out to Neo maintainer identities"; silent `[]` satisfied
the no-leak property but missed the disables-with-log property. The log names
both operator knobs (`NEO_AGENT_IDENTITY` env + `targetSource='disabled'`
config) so the misconfiguration is actionable. Extracted `selfFallback()` helper
so the unknown-source and active-subscribers-missing-provider branches share
the same observable behavior.

3 new unit tests assert the log fires in each fallback path (default 'self',
unknown source, active-subscribers without provider). Existing AC3 test
upgraded to also verify the log payload contains both knob names.

RA2 — Contract Ledger:
Appended a Contract Ledger matrix to ticket #11905 covering the 11 named
public-contract surface points (3 env vars + 1 AiConfig key + 4 resolver enum
branches + 3 config-set hooks). PR body will reference the ledger.

Test evidence: `npm run test-unit -- --grep "swarmHeartbeat|SwarmHeartbeat"`
→ 43 passed (was 41 before; +2 net new tests, +1 upgraded assertion).

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>

* chore(orchestrator): strip provenance + remove dead DEFAULT_IDENTITY export (#11905)

Addresses GPT PR #11913 cycle-3 review:

RA1 - Remove dead DEFAULT_IDENTITY export:
The constant + export were retained as "legacy" in cycle-1 but no external
consumer was found. Removed both the const definition and the named export.
Test docblock + test reference updated to drop the same name.

RA2 - Strip ticket/epic/AC/review-cycle/history anchors from added/edited
source + test prose:
The why-first/no-history-in-hot-path direction shipped via the compact-only
why-first guidance applies to runtime source comments and test names too.
Stripped anchors from the resolver JSDoc, selfFallback helper docblock,
SwarmHeartbeatService identity_/targetSource_/explicitTargets_ config blocks,
beforeSetIdentity and getPulseIdentities docblocks, the AiConfig
swarmHeartbeat slot, and the corresponding spec describe + test names.
Provenance now lives in git blame + PR/ticket/commit context where it can
support archaeology without bloating the hot path.

Net delta in this commit: +106 / -132 lines across the same 5 files; total
test count unchanged at 43 passing. No behavioral change.

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>

* chore(orchestrator): drop residual ticket anchor from getter docblock (#11905)

Removes the trailing `Sub 1 #11905 / Epic #11829 Layer 2` anchor that escaped
the cycle-3 sweep — Orchestrator.mjs `swarmHeartbeatExplicitTargets` getter
JSDoc. GPT cycle-3 parked-recheck flagged "one source-comment anchor remains";
this is it. Provenance stays in PR/ticket/commit context.

Co-Authored-By: Claude Opus 4.7 (Claude Code) <neo-opus-ada@neomjs.com>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-24T21:36:33Z @neo-opus-ada cross-referenced by #11830

