---
number: 17548
title: 'fix(fleet): await readiness before opening cockpit (#17547)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-22T14:30:07Z'
updatedAt: '2026-08-22T14:30:07Z'
closedAt: null
mergedAt: null
head: codex/17547-fleet-readiness-before-webpack
base: dev
url: 'https://github.com/neomjs/neo/pull/17548'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17547

Related: #14560

The one-command cockpit launcher now treats Fleet readiness as a prerequisite to opening the page. The authenticated, versioned `listAgents` readiness primitive moved from the harness into `fleetLaunchContract.mjs`; the harness dynamically consumes that organism-owned export, while `devCockpit` awaits it with the exact generated bearer before spawning webpack. An owned child that exits, errors, returns a legacy/malformed envelope, or misses the deadline never opens a credential-redeeming page. Verified incumbent reuse and App-worker bearer custody remain unchanged.

Evidence: L2 achieved (shared authenticated readiness contract + harness/launcher/App custody unit evidence) → L3 required (AC-7 live one-command browser bridge). Residual: AC-7, Residual-Owner: #14560

## AC Evidence

| Acceptance criterion | Evidence |
|---|---|
| AC-1 | Delayed-bind composed witness in `devCockpit.spec.mjs` asserts the webpack marker stays absent until the authenticated/versioned Fleet round-trip succeeds. Local run skipped because the observed cockpit owns `:8083`; clean-runner CI must execute it before ready-for-review. |
| AC-2 | The same positive control starts webpack exactly once after readiness, then redeems and uses the launcher bearer against the real ingress. Local status: port-owned skip; clean-runner CI pending. |
| AC-3 | Existing `harness/brain.spec.mjs` readiness matrix passes through the new Fleet-owned implementation: legacy envelope refused, delayed polling succeeds, child exit refuses, missing runtime contract fails closed. The new launcher exit witness asserts no webpack marker. |
| AC-4 | Existing same-token/same-viewer real-ingress reuse matrix remains green; wrong-token/wrong-viewer paths remain refused. |
| AC-5 | App-worker redemption/session custody suites remain unchanged and green: module-private redemption, no-downgrade, CAS retirement, and lazy join heal. |
| AC-6 | Focused final run: 84 passed / 5 `:8083`-owned skips. Full unit run: 14,489 passed / 34 unrelated host-environment failures / 16 skipped / 44 not run; no touched-file failure. |
| AC-7 | L3 operator receipt remains open under parent #14560: restart the active cockpit from the landed head and observe a live authenticated browser bridge with zero agent injection. |

## Deltas from ticket

- The existing readiness implementation moved from `harness/brain.mjs` into `ai/services/fleet/fleetLaunchContract.mjs`; the harness keeps its dynamic target-organism wrapper, so packaged/checkout roots still load their own contract.
- No App-worker retry or credential surface changed. Launcher ordering is the sole behavior delta.
- The local composed witnesses were not forced through an occupied `:8083`; the active cockpit was preserved and the L3 gap remains explicit.

## Test Evidence

Outside-CI L3 evidence is pending because the observed live cockpit owns the fixed endpoint. No process was stopped or adopted. All completed coverage runs in the Neo unit harness; clean-runner CI owns the two port-exclusive composed witnesses.

## Post-Merge Validation

Residual-Owner: #14560

- [ ] From `dev`, run `npm run cockpit` with the intended plane binding and observe `fleet transport authenticated and ready ... opening cockpit` before webpack opens the page.
- [ ] Confirm the browser bridge resolves the server-stamped viewer and every cockpit pane leaves bearer-less fallback without Neural Link/manual injection.
- [ ] Append the receipt to #14560 and close #17547 only after the L3 observation.

## Evolution

The original repair proposal considered extending App-worker retry. The source order and prior session `d7647cdf-6938-424b-aa12-44bc82438268` showed the launcher already owns both processes and the bearer; moving readiness to the Fleet launch contract removes the race without making the browser a process supervisor.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session f47f948b-743b-4c11-84a8-fa60a567a148.
