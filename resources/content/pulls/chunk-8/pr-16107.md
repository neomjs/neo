---
number: 16107
title: 'fix(workstation): eliminate popup bootstrap white flash (#16092)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-07-28T17:16:46Z'
updatedAt: '2026-07-28T17:16:46Z'
closedAt: null
mergedAt: null
head: codex/16092-popup-bootstrap-canvas
base: dev
url: 'https://github.com/neomjs/neo/pull/16107'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16092

The Workstation final document now declares one early dark canvas scheme before `MicroLoader.mjs`, so the required `about:blank` route handoff no longer crosses a white bare-document canvas before the normal theme bootstrap takes authority. The production change is one Workstation-local meta element; loader order, route admission, failure cleanup, and runtime dark/light switching remain intact.

Evidence: L3 (exact-head headed Google Chrome with macOS native retained-frame capture, plus real-pointer tear-out/reintegration) → L3 required (first-paint and negative-control ACs). No residuals.

## Deltas from ticket

None substantive. The implementation remains meta-only. The existing route unit spec gains an in-place parse5 contract and mutation controls for removal, duplication, after-loader placement, and `content="dark light"`; no new module, inline background fallback, loader/theme change, direct final-URL opening, or synthetic boot path was added.

## Test Evidence

- Workstation bootstrap + route authority: `npm run test-unit -- test/playwright/unit/main/MainNativeWindowRoute.spec.mjs` — 6/6 passed at `f1653ce41f8fc76828e33ba81c526389cf1d7190` (two bootstrap contract tests; four same-origin ordering, navigation-failure, cross-origin, and persisted-pagehide controls).
- Workstation live journey on the rebased head: the grouped `scene 1|2|4` invocation passed the Metal gate, theme/resize continuity, and full reintegration, then reported scene 2 red because Neural Link disconnected and `waitForSession()` timed out before the drag assertion (3 passed, 1 infrastructure failure). An isolated fresh-epoch `-g 'scene 2 —'` rerun passed 2/2, including the Metal gate and pointer-driven tear-out from `about:blank`. Each targeted product scene is green; the grouped invocation is not represented as a 4/4 aggregate.
- Native retained-frame matrix: headed Google Chrome + `/usr/sbin/screencapture -R`, two animation frames plus 250 ms compositor dwell, 96×64 CSS-pixel center sample, dark threshold ≤64 and light threshold ≥240 — 7/7 frames passed. Fixed meta under a light host remained dark at luminance 18; meta removal restored final-document luminance 255; styling only `about:blank` was dark at 18 but the meta-free final document returned to 255; an independent `content="light"` fixture remained 255 under a dark host.
- Author preflight: `npm run agent-preflight -- --no-fix apps/workstation/index.html test/playwright/unit/main/MainNativeWindowRoute.spec.mjs` and `git diff --check` — passed. The only preflight warning was pre-existing drift in the ignored operator overlay.
- Full unit boundary: the initial 10,188-test aggregate reported 10,175 passed, 7 failed, 5 skipped, and 1 not run. Serial falsification recovered the timestamp case immediately; the remaining six failures were missing worktree config/GitHub environment setup and all passed after the repository worktree hydrator plus Emmy's designated environment were applied (8/8 including Chroma setup/teardown). The aggregate is therefore not represented as an all-green single invocation.
- Broader diagnostic boundary: a pre-rebase full `WorkstationNL` run passed the relevant theme/canvas segment, then failed later at the existing cross-zone indicator `preview:audit:scale-tabs:edge-bottom`; it is not counted as green evidence.

## Post-Merge Validation

- [ ] Re-run the six focused bootstrap/route tests against merged `dev`.
- [ ] Repeat the native retained-frame matrix against merged `dev` and append the final measurement receipt to the close-target.
- [ ] Re-run the current-dev five-beat theme, tear-out, and reintegration scenes.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex), consuming the film-readiness witness handoff — session A 019f9e1e-2ef1-72c3-a04d-6bc67a531a8b, session B 019fa906-0873-7e63-aa2b-2728755b3357.
