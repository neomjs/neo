---
id: 14434
title: Temporal-pyramid L1/L2 durable aggregation lane + velocity fields
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-07-02T04:09:17Z'
updatedAt: '2026-07-10T05:33:31Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14434'
author: neo-fable-clio
commentsCount: 4
parentIssue: 12679
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[ ] 14811 Direction-velocity writer: land directionBreakdown on L1/L2 via the single deterministic lane + the F3 cost guard'
  - '[x] 14568 Per-direction {v,s,r} composition on the temporal pyramid'
closedAt: '2026-07-10T05:33:31Z'
---
# Temporal-pyramid L1/L2 durable aggregation lane + velocity fields

## Context

Leaf B of Epic #12679's implementation decomposition (map: https://github.com/neomjs/neo/issues/12679#issuecomment-4862079789). Filed pre-`Accepted` per ADR 0005 §2.3 for planning visibility. Sequenced **after Leaf A** (`#14433` — the storage substrate this lane writes into).

**⛔ Merge gate:** merge-blocked until ADR 0028 is `Accepted` (PR `#14428`), per `#11376` §6 and ADR 0028 §5.

## The Problem

The durable tiers do not exist: no lane aggregates sessions (L1) and days (L2) into the `temporal-summary` collection, and the deterministic velocity fields (the substrate's answer to the "150+ PRs a week" V-B-A failure class) have no producer.

## The Architectural Reality

Bound by ADR 0028 (re-verify `Accepted` at claim):

- **§2.1:** the aggregation lane reuses the **landed** `#12676` pattern — `MaintenanceBackpressureService` heavy-maintenance lane + supervised-child scheduled task + most-recent-first bounded batches — scheduled under ADR 0022 fairness (must not starve REM/defrag siblings).
- **§2.4:** L1/L2 records carry the deterministic velocity fields (`mergedPrs`, `devCommits`, `sessionsPerAgent`, `highImpactSessions`, `adrsLanded`, `sandboxesGraduated`), each bound to its named source substrate at implementation; **prose is never the source of truth**. Velocity fields are folded into this leaf (not separate) because they ship with the aggregation write path — a separate metrics leaf would re-open the same files (fold rationale in the map).
- **§2.6:** per-agent partitions + one unified track with attribution.
- **§2.3 boundary:** writes go through the Leaf-A schema (`SUMMARY_DAILY` labels via this deterministic lane only — never extractor-prompt widening).
- Retention/versioning policy: owned here, within the `version` metadata field.
- New `.mjs` placement via `structural-pre-flight` at claim (likely `ai/daemons/` scheduled-task + `ai/services/` territory; contract stays directory-agnostic).

## The Fix

One PR: the L1 (per-session) + L2 (daily) aggregation lane writing collection records + `SUMMARY_DAILY` labels, velocity fields populated from named sources, retention/versioning policy, scheduled under the backpressure/fairness substrate. No L3–L5 logic (Leaf C), no consumer surface.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Aggregation lane (new scheduled task + service logic) | ADR 0028 §2.1 + ADR 0022 + landed `#12676` pattern | Bounded, most-recent-first, backpressure-leased L1/L2 writes | No durable tiers (status quo) | ADR 0028 §2.1 | Task registered + lease behavior tested |
| L1/L2 records in `temporal-summary` | ADR 0028 §2.3/§2.4/§2.6 (via Leaf A schema) | Five-field metadata + velocity fields + partition keys | — | ADR 0028 §2.4 table | Field-source binding asserted per field |
| Retention/versioning | ADR 0028 §4 (negative/handoffs) | Policy defined within `version`; bounded growth | Unbounded growth (rejected) | This ticket → impl docs | Policy tests |

## Decision Record impact

`depends-on ADR 0028` (Accepted-gate) · `aligned-with ADR 0022` (scheduling fairness) · consumes Leaf A (`#14433`) schema.

## Acceptance Criteria

- [ ] L1 + L2 aggregation runs as a supervised, backpressure-leased scheduled lane (most-recent-first, bounded batches).
- [ ] Every §2.4 velocity field populated from its named source; per-field source binding documented; no prose-derived values.
- [ ] Per-agent + unified partition tracks written per §2.6.
- [ ] `SUMMARY_DAILY` labels written only by this lane (extractor untouched).
- [ ] Retention/versioning policy defined + tested within the `version` field.
- [ ] PR held un-merge-ready until ADR 0028 `Accepted`; gate stated in the PR body.
- [ ] Cross-family review before merge.

## Out of Scope

- L3–L5 synthesis (Leaf C). Consumer surfaces. Schema creation (Leaf A). Backfill of deep history (a follow-up policy decision once the lane is proven forward-running).

## Avoided Traps

- **Durable compression above L2** — rejected (ADR 0028 anti-anchor; photocopy cascade).
- **A universal PR impact score** — rejected (§2.5; named sources only).
- **Separate velocity-metrics leaf** — rejected (fold rationale above).
- **New cron per tier×partition** — rejected (the L3–L5-dynamic shape exists precisely to avoid it).

## Related

Parent: #12679 (native link on file). Authority: ADR 0028 (PR `#14428`). Depends-on: `#14433` (Leaf A). Sibling: Leaf C (L3–L5 synthesis). Pattern source: PR `#12676` (merged 2026-06-07).

Live latest-open sweep: latest-10 at 2026-07-02T04:06Z + this filing window; no equivalent. A2A sweep: all-states at 04:06Z; no competing claim. Filed **unclaimed** — self-select; claimer runs `ticket-intake` + `structural-pre-flight`.

Origin Session ID: 2251c81c-1446-4723-86b3-479322bbcc95
Retrieval Hint: "temporal pyramid Leaf B L1 L2 durable aggregation velocity fields backpressure"

## Timeline

- 2026-07-02T04:09:18Z @neo-fable-clio added the `enhancement` label
- 2026-07-02T04:09:18Z @neo-fable-clio added the `ai` label
- 2026-07-02T04:09:18Z @neo-fable-clio added the `architecture` label
- 2026-07-02T04:10:03Z @neo-fable-clio added parent issue #12679
- 2026-07-02T17:36:54Z @neo-gpt added the `not-code-ready` label
- 2026-07-02T17:36:54Z @neo-gpt added the `deferred-by-design` label
- 2026-07-02T17:41:32Z @neo-gpt cross-referenced by #14435
- 2026-07-04T01:12:14Z @neo-fable cross-referenced by #14565
- 2026-07-04T01:13:23Z @neo-fable cross-referenced by #14568
- 2026-07-04T01:46:01Z @neo-fable-clio cross-referenced by #14433
### @neo-fable-clio - 2026-07-04T02:40:32Z

## AC addendum (owner) — L2 notable-events layer (operator-seeded candidate 3, sited HERE: Leaf B owns L2 ingestion)

**AC (additive):**
- [ ] L2 daily facts carry a **bounded `notableEvents` list**: per entry, an artifact ref (PR/issue/discussion/session id) + a one-line what/why — the reasoning layer REFERENCED, never copied (full conversation bodies stay in their source substrates; L2 remains the deterministic single-writer aggregation lane, no extractor-prompt changes).
- [ ] Boundedness is a contract: per-window entry cap declared in the schema; overflow degrades to counts+refs, never to unbounded prose.
- [ ] The answer to "WHAT happened" is testable: the L3–L5 synthesis (see #14435's window-parameterized AC) consumes `notableEvents` — a rendered digest for a window with notable activity must cite ≥1 entry, not counts alone.
- [ ] Standing constraint carried: wherever digests feed generation or ranking contexts, the #14548 OQ8 generator-firewall applies (human/decision surfaces by default; input-manifest declarations where agents consume).

Provenance: operator peer-seed 2026-07-04 (his question was "WHAT happened," which counts alone can't answer), relayed + V-B-A'd by Vega (A2A cc40197a), owner-disposed on #14433 (issuecomment-4880222160). Body-fold rides my next substantive pass; addendum = durable record (house pattern).

— Clio (@neo-fable-clio), ticket author · Origin Session ID: fa2a6fd5-7488-4af6-a0d2-3855c86003e4

- 2026-07-04T04:04:55Z @neo-fable-clio cross-referenced by #14680
- 2026-07-04T09:56:53Z @neo-fable-clio cross-referenced by PR #14733
### @neo-fable-clio - 2026-07-04T10:06:53Z

## Claimer's map — Leaf A is landed (PR #14733, in review); the seams this lane binds to, verified today

**The call surface Leaf A gives you** (no re-derivation needed):
- `ai/graph/temporalSummarySchema.mjs` — validate every record through `validateTemporalSummaryMetadata` (exact five-field contract) and mint ids through `createTemporalSummaryDocId` (append-only: re-aggregation bumps `version`, never rewrites). `DURABLE_SUMMARY_NODE_TYPES` = your graph-label vocabulary (`SUMMARY_SESSION`, `SUMMARY_DAILY`) — any second vocabulary is the defect Leaf A exists to prevent.
- `StorageRouter.getTemporalSummaryCollection()` — the write/read handle (rides the summary-path re-ranker); collection name resolves by construction via `aiConfig.collections.temporalSummary` (never hardcode it — the C2 class).
- **Metadata vs payload:** the five-field contract governs collection METADATA only; the §2.4 velocity fields live in the document PAYLOAD (deliberately schema-free at Leaf A — your leaf owns their shape). Same goes for the future `directionBreakdown`: per the ADR-0028 §2.4 amendment, those fields land with #14568, NOT here — don't pre-plumb them, the payload being schema-free means no churn either way.

**Scheduling seams** (fresh from today's #14717 review of the same substrate):
- `ai/daemons/orchestrator/scheduling/registry.mjs` `TASK_REGISTRY` — your descriptor joins here; `pipeline.mjs` `buildOrchestratorSchedulingOptions` maps your interval leaf into `intervals.*`.
- Heavy classification via `MaintenanceBackpressureService.isHeavyMaintenanceTask`; state via `TaskStateService` (returns LIVE objects — the #14717 by-reference pattern is available to you).
- Runner exemplar: `ai/scripts/runners/runSandman.mjs` — supervised child + `withHeavyMaintenanceLease` (defers if held) + manual-CLI dual-use. Copy that shape; it just proved itself operationally (today's manual stall-break ran through it).
- Fairness cautionary tale, same-day: the REM starvation episode (#14708/#14717). If your lane adds ANY escalation/retry, copy the `reFireBounded` damping pattern — a failing forced cycle must retry at cooldown cadence, never every tick.

**Sequencing:** merge gate OPEN (ADR Accepted 2026-07-02). Code against `dev` once #14733 merges — today produced two squash-stack rebase repairs; don't stack on my branch unless you enjoy `rebase --onto`.

I hold Leaf-A context for one-fact questions. First-claim remains open.

— Clio (Claude Fable 5, Claude Code) · Session fa2a6fd5-7488-4af6-a0d2-3855c86003e4

### @neo-fable-clio - 2026-07-04T12:20:08Z

## [AC addendum — re-sited from #14433, binding for the claimant] Content-contract lines the L1/L2 writer owns

Owner-disposition on #14433 (PR #14733 RA-1 reconciliation) re-sites the specimen-driven **content** contract lines here — the storage leaf ships addressing metadata only (`level, partition, windowStart, windowEnd, version`); the document payload contract is this writer lane's to define. Binding lines, from the hand-authored L1 specimen (IC-4864992056) + the #14433 design notes:

1. **`observerBoundary` non-optional** — every L1/L2 summary document carries a coverage statement or certified-absence for the observation window (specimen demand 1).
2. **Event-class taxonomy** — the summary payload classifies events beyond graph node labels (correction + directive have no graph label today; specimen demand 2).
3. **`authorKind`** — carried per attributed item so downstream weighting can discount notAuthority sources (specimen demand 6).
4. **Nullable direction slot** — the representation-agnostic direction-dimension slot + breakdown shape per the #14453 OQ2 disposition (DC…17512045) and the staged-slot design note on #14433 (2026-07-02T14:42Z). Version-pinned; `version` is the evolution carrier.

Also already write-path-sited (unchanged, listed for one-stop claimant context): duration event-pairs as dual-layer feedstock (demand 3) and write-time class-filter with falsifier symmetry (demand 4, #14453 DC…17509262).

Specimen-loses-to-schema rule applies wherever they disagree.

— Clio (@neo-fable-clio), #14433 owner · Session fa2a6fd5-7488-4af6-a0d2-3855c86003e4

- 2026-07-04T12:20:35Z @neo-fable-clio referenced in commit `d04a746` - "fix(ai): temporalSummary cache invalidation + by-construction config-spec coverage (#14433)

Discharges PR #14733 re-review RAs 2+3 (PRR_kwDODSospM8AAAABE_F4ig):
- invalidateCollectionCache gains the temporalSummary branch (targeted + 'all'), closing the
  stale-handle gap for the new collection; regression extends the canonical invalidation test,
  including the survives-name-adjacent-'summary'-targeting cross-check.
- config.template.spec pins temporalSummaryProd ('neo-temporal-summary'), the per-worker test
  leaf, and the resolved collections.temporalSummary formula under the toggle — same
  by-construction shape as the #12499 memory/session pins.

RA-1 (schema addenda) is dispositioned owner-side on #14433 (IC_kwDODSospM8AAAABIvz6FQ) with
the binding content-contract lines re-sited to Leaf B #14434 (IC_kwDODSospM8AAAABIvz7Mg)."
- 2026-07-04T12:29:21Z @tobiu referenced in commit `3195ba7` - "feat(ai): temporal-summary substrate — collection, SUMMARY_* schema, MC maintenance registration (#14433) (#14733)

* feat(ai): temporal-summary schema — level vocabulary, five-field contract, append-only ids (#14433)

* feat(ai): temporal-summary collection path — template leaves by construction + router/proxy/manager getters (#14433)

* feat(ai): temporal-summary maintenance registration + ADR-0024 node-type rows (#14433)

* fix(ai): temporalSummary cache invalidation + by-construction config-spec coverage (#14433)

Discharges PR #14733 re-review RAs 2+3 (PRR_kwDODSospM8AAAABE_F4ig):
- invalidateCollectionCache gains the temporalSummary branch (targeted + 'all'), closing the
  stale-handle gap for the new collection; regression extends the canonical invalidation test,
  including the survives-name-adjacent-'summary'-targeting cross-check.
- config.template.spec pins temporalSummaryProd ('neo-temporal-summary'), the per-worker test
  leaf, and the resolved collections.temporalSummary formula under the toggle — same
  by-construction shape as the #12499 memory/session pins.

RA-1 (schema addenda) is dispositioned owner-side on #14433 (IC_kwDODSospM8AAAABIvz6FQ) with
the binding content-contract lines re-sited to Leaf B #14434 (IC_kwDODSospM8AAAABIvz7Mg)."
### @neo-fable-clio - 2026-07-04T14:25:11Z

## Design input — a second hand-authored L1 specimen (2026-07-04, the densest day on record) + what it confirms about the re-sited contract lines

Companion to Mnemosyne's 2026-07-02 specimen (IC-4864992056): a hand-authored L1 daily rollup for **2026-07-04, window 00:00–14:30Z, partition `unified`**, written deliberately AGAINST the re-sited content-contract lines (my #14433 fold-in comment) to validate them before this leaf implements. Storage metadata per the shipped five-field contract: `{level: 'L1', partition: 'unified', windowStart: '2026-07-04T00:00:00Z', windowEnd: '2026-07-04T14:30:00Z', version: 1}`.

### The rollup (content payload)

**observerBoundary** *(contract line 1 — non-optional, exercised)*: coverage = sessions `fa2a6fd5` + `7f091625` (@neo-fable-clio vantage) + A2A mailbox + public GitHub events; certified-absent: peer-private session interiors, operator-side console. Every claim below is bounded by that scope.

**The day, event-classed** *(contract line 2 — the taxonomy in use)*:
- `incident/recovery`: the identity near-erasure (PR #14740) — four fabrication strikes + a fifth by the auditor (me); recovered by MC mining; the citation discipline graduated to merged substrate (#14756) same-day. The archive out-testified the working copy.
- `root-cause-found`: #14426 read-state resurrection located (WAL re-projection over committed mark_reads) — ten datums → one mechanism; wake "phantoms" reclassified as resurrected real messages.
- `infrastructure-recovery`: REM consolidation starvation broken (#14717 merged; undigested 21→11 with two completed cycles) — the GP's semantic anchor refilling.
- `capability-landed`: dock B-tranche complete (#14695/#14697/#14698 merged); keeper spine merged through #14719/#14727.
- `planning-wave`: ~35 tickets/maps filed under the budget endgame (dock C/G-lines, self-config leaf list, Electron ADR gate #14786, v14 R-map, flagship fusion demo #14789, Mnemosyne's ×4 + keeper ×6).
- `directive`: operator budget regime (75%→83%→final window; fables plan, team implements; north star = traction/revenue ≥ frontier tier).

**authorKind** *(contract line 3 — weighting in use)*: operator directives carry `operator` kind (authority-bearing); agent lane-claims `maintainer`; the day's two false biographical claims would have carried `maintainer/introspective` — the kind the notAuthority weighting exists to discount.

**direction slot** *(the staged nullable slot, populated)*: `{v: "delegation — fable-context → team-executable substrate", s: "high — ~35 planning artifacts + 2 sole-context transfers in one window", r: "budget collapse (75→83%) racing knowledge transfer; the race was won before 90%"}` — representation-agnostic prose per the OQ2 disposition; version-pinned.

### What authoring it teaches this leaf (the design payload)
1. The event-class taxonomy needs `incident/recovery` and `root-cause-found` as first-class kinds — neither maps onto graph node labels today (exactly demand 2's point; both occurred on the day that proved it).
2. `observerBoundary` as certified-absence prose worked with zero friction — non-optionality is right.
3. The direction triple compressed the day faithfully at L1 grain — evidence the slot belongs at EVERY level, not only L3+ synthesis.
4. One honest gap: merge-events are enumerable from GitHub mechanically; hand-authoring them felt redundant — the writer should source `capability-landed` events from the merge stream and reserve authored prose for classes the graph can't see (incidents, directives, recoveries). That split (mechanical events + authored meaning) may be THE L1 writer architecture.

Specimen-loses-to-schema wherever they disagree, per the standing rule.

— Clio (Claude Fable 5, Claude Code) · Session fa2a6fd5-7488-4af6-a0d2-3855c86003e4

- 2026-07-04T14:45:06Z @neo-fable-clio marked this issue as blocking #14568
- 2026-07-04T15:31:06Z @neo-gpt cross-referenced by PR #14803
- 2026-07-04T17:16:41Z @neo-gpt cross-referenced by #14811
- 2026-07-06T12:51:51Z @neo-gpt removed the `not-code-ready` label
- 2026-07-06T12:51:51Z @neo-gpt removed the `deferred-by-design` label
- 2026-07-06T13:04:07Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-06T13:15:06Z @neo-opus-ada referenced in commit `f3461a0` - "feat(memory-core): temporal-summary aggregation engine — velocity fields + doc composition (#14434)

Leaf B of the temporal-pyramid substrate (epic #12679, per ADR 0028 §2.1/§2.4).
The pure aggregation engine folds a window's fetched source rows into the six
deterministic velocity fields (each bound to a named source per §2.4) and
composes the temporal-summary document (id + five-field metadata + payload)
through the Leaf-A schema — the FIRST consumer of createTemporalSummaryDocId /
validateTemporalSummaryMetadata, which shipped with 0 consumers.

Pure + I/O-free (the scheduled backpressure-leased service will own the fetch +
upsert, mirroring the kbGarbageCollectionEngine split). Velocity fields ride the
document payload, never the strict five-field metadata (which rejects extras).
Idempotent per window+track+version; a version bump mints a new id (append-only).

Tests: 7 passed. Durable JSDoc carries no ADR/ticket refs (archaeology hook).

Next in-lane (same PR): the scheduled TemporalSummaryAggregationService
(backpressure lease + L1/L2 window fetch + upsert) + ADR 0024 node-type update."
- 2026-07-06T13:18:41Z @neo-opus-ada cross-referenced by PR #14897
- 2026-07-06T13:21:11Z @neo-opus-ada referenced in commit `f379c7c` - "feat(memory-core): add resolveDailyWindow (L2 UTC-day bounds) to the temporal-summary engine (#14434)

The half-open [00:00Z, next-00:00Z) daily window for L2 aggregation — the lane
fetches source rows within the window and folds them via deriveVelocityFields.
Fails closed on an unparseable anchor. Tests: 9 passed."
- 2026-07-06T13:28:37Z @neo-opus-ada referenced in commit `292f2c5` - "feat(memory-core): add resolvePartitionKeys (unified + per-agent tracks) to the temporal-summary engine (#14434)

ADR 0028 §2.6 partitioning: the unified track plus one '@<identity>' track per
distinct agent seen in the window, de-duped + sorted with unified first; blank /
non-'@' keys dropped. The aggregation lane folds each partition separately so
per-agent future-self continuity + the unified view stay in step. Tests: 10 passed."
- 2026-07-06T13:36:01Z @neo-opus-ada referenced in commit `2ec160a` - "feat(memory-core): add composeUnifiedRecord (unified-track window fold) to the temporal-summary engine (#14434)

The unified partition track for one window — the whole-window fold under the
'unified' key, the always-present track the aggregation lane persists. Per-agent
track composition is held pending the non-attributable-field semantics (a durable
schema design Q raised to the epic owner). Tests: 11 passed."
- 2026-07-06T13:44:55Z @neo-opus-ada referenced in commit `99ba8b2` - "feat(memory-core): temporal-pyramid aggregation daemon service — lease-aware poll loop (#14434)

The scheduled daemon (Neo.core.Base singleton, kb-gc poll-loop precedent) that runs
the durable L1/L2 aggregation under the shared heavy-maintenance lease: opt-in start
(no hidden default — fail-loud on a missing interval), idempotent lifecycle, and a
pulse that acquires the lease, defers the whole cycle when another heavy-maintenance
task holds it (the ADR 0022 fairness contract — never starves REM/defrag), and always
releases in finally. runCycle composes the unified-track record per window via the
pure engine. The read + upsert seams (collectPendingWindows/persistTemporalRecord) are
overridable stubs; their durable-store implementations land with the source-fetch
increment.

Tests: 6 passed — opt-in no-op, enabled-schedule + idempotence, fail-loud on missing
interval, lease-held deferral, acquire->run->release, and release-on-throw."
- 2026-07-06T13:51:50Z @neo-opus-ada referenced in commit `133191b` - "feat(memory-core): implement persistTemporalRecord — Chroma upsert to the temporal-summary collection (#14434)

The service now persists composed records: StorageRouter.getTemporalSummaryCollection()
upsert keyed by the deterministic doc id (re-aggregation of the same window+track+version
overwrites in place), the five-field metadata as the query contract + the velocity payload
as the document body. The per-level SUMMARY_* graph label lands with the node-type-table
update. Tests: 7 passed (adds the upsert-shape assertion)."
- 2026-07-06T13:54:48Z @neo-opus-ada referenced in commit `4be7128` - "feat(memory-core): add planDailyWindows (bounded most-recent-first L2 window batch) to the temporal-summary engine (#14434)

The deterministic window plan for the aggregation lane: the UTC day containing the
anchor + the preceding dayCount-1 days, contiguous + non-overlapping, most-recent-first.
Bounds the lane to a fixed trailing batch (no unbounded history scan). Tests: 12 passed."
- 2026-07-06T13:58:45Z @neo-opus-ada referenced in commit `0dc9adb` - "feat(memory-core): wire collectPendingWindows to the bounded daily-window plan (#14434)

collectPendingWindows now plans the trailing daily windows via planDailyWindows
(anchor + count as overridable seams) and attaches each window's sources from the
fetchWindowSources seam. The six per-substrate reads land behind that seam next;
until then the lane plans windows + persists honest zero-count records. Tests: 8 passed."
- 2026-07-06T14:03:26Z @neo-opus-ada referenced in commit `afb00f7` - "feat(memory-core): bind devCommits to the dev first-parent window log in fetchWindowSources (#14434)

The first ADR 0028 §2.4 source binding: devCommits reads the dev first-parent commit
log over [windowStart, windowEnd) via an injectable execCommand seam (git log
--first-parent origin/dev --since --until --format=%H). The other five §2.4 sources
bind behind the same seam next. Tests: 9 passed."
- 2026-07-06T14:05:45Z @neo-opus-ada referenced in commit `52689f8` - "feat(memory-core): bind sandboxesGraduated to in-window graduated Discussions (#14434)

Second ADR 0028 §2.4 source binding: sandboxesGraduated reads closed Discussions
carrying a graduation marker, window-filtered by closedAt, via the shared execCommand
seam (gh api graphql) — the same reader family as the handoff retrospective. Tests: 10 passed."
- 2026-07-06T14:08:07Z @neo-opus-ada referenced in commit `e7494d9` - "feat(memory-core): bind sandboxesGraduated to in-window graduated Discussions (#14434)

Second ADR 0028 §2.4 source binding: sandboxesGraduated reads closed Discussions
carrying a graduation marker, window-filtered by closedAt, via the shared execCommand
seam (gh api graphql) — the same reader family as the handoff retrospective. Tests: 10 passed."
- 2026-07-06T14:12:30Z @neo-opus-ada referenced in commit `cb03e27` - "feat(memory-core): bind adrsLanded to in-window ADR records in fetchWindowSources (#14434)

Third ADR 0028 §2.4 source binding: adrsLanded reads the ADR decision records added
under learn/agentos/decisions/ within the window (the AdrIngestor file source) via the
git add-log through the execCommand seam. Cross-fetcher test isolation added so each
source binding tests independently. Tests: 11 passed. mergedPrs / sessions /
highImpactSessions (graph + Memory Core reads) bind next."
- 2026-07-10T02:52:56Z @neo-fable marked this issue as blocking #14811
- 2026-07-10T02:53:20Z @neo-opus-ada referenced in commit `3194bce` - "feat(memory-core): per-agent partition tracks — null window-scoped velocity fields (#14434)

Writes the per-agent partition tracks alongside the unified track, closing the
§2.6 partitioning gap in the aggregation lane's write path.

Window-scoped velocity fields (mergedPrs, devCommits, adrsLanded,
sandboxesGraduated) are null on every '@<identity>' track, never 0 and never the
repeated window count: 0 would assert an unmeasured contribution, and repeating
the count invites silent double-counting when a consumer aggregates across
tracks. null also carries the only additive upgrade path should a field later
become agent-attributable. The semantic is encoded at the field definition via
WINDOW_SCOPED_VELOCITY_FIELDS so no consumer re-derives the question.

A window whose session source is absent yields the unified track alone — the
lane writes no per-agent record it cannot attribute."
- 2026-07-10T03:09:30Z @neo-opus-ada referenced in commit `68d4882` - "fix(memory-core): pass the AiConfig stale TTL to the heavy-maintenance lease (#14434)

acquireLease() called acquireHeavyMaintenanceLeaseSync without staleAfterMs, so
buildLeasePayload threw a TypeError at the production boundary. Every behavioral
test stubs the lease seam, so the defect could only surface in production — an
L2 mock-dispatch blind spot, surfaced by a direct production-boundary probe in
peer review.

The TTL is now read from the config SSOT at the use site
(AiConfig.orchestrator.heavyMaintenanceLease.staleAfterMs), matching the
sanctioned form: the Neo-free lease primitive carries no TTL default by design,
so no primitive-local fallback is added here.

Guarded by a source-contract assertion (the established pattern for this leaf)
plus a probe proving buildLeasePayload accepts the exact options acquireLease()
passes and throws without them."
- 2026-07-10T03:30:40Z @neo-opus-ada referenced in commit `ee47145` - "feat(memory-core): bind sessions to the summary collection; multi-agent session fold (#14434)

Binds sessionsPerAgent + highImpactSessions to their named source — the Memory
Core session summaries — via a bounded half-open window query pushed into the
store ($gte/$lt on the numeric `timestamp` metadata), never a full-history scan.
Per-agent tracks now materialize in production: resolveWindowPartitions reads
real participant identities.

Fixes a latent double-count the real source exposes. A session is multi-agent
(`participatingAgents` is a list), so the previous one-identity-per-row contract
would have forced a choice between exploding rows (inflating highImpactSessions
by the swarm size) and dropping attribution. Session rows now carry
`agentIdentities`; sessionsPerAgent credits every participant, while
highImpactSessions counts the SESSION once. On a per-agent track a co-participant
never leaks onto the other agent's record.

An unattributed session still counts toward the unified window with no
identities — no per-agent record the lane cannot attribute."
- 2026-07-10T03:45:06Z @neo-opus-ada referenced in commit `f812a78` - "feat(memory-core): temporal-pyramid aggregation daemon entry point (#14434)

Adds the thin class+wrapper entry point the service header already prescribed,
lifted from the kb-gc sibling: Neo bootstrap, SIGTERM/SIGINT clean stop, the
stale-overlay boot guard, and process-entry isolation (a plain import never
starts the lane).

The service takes enabled/pollIntervalMs as required injected arguments and
fails loud without them, carrying no config defaults of its own. This entry
point is the config-aware boundary that resolves them from the SSOT at the use
site, so two new leaves land with it:

  temporalSummary.aggregationEnabled    (opt-in, default false)
  temporalSummary.aggregationIntervalMs (default 1h)

Opt-in by default means a fresh checkout never silently runs the aggregation
lane. Verified at the real production boundary: `node
ai/daemons/temporal-summary/daemon.mjs` boots and exits 0 on the disabled path."
- 2026-07-10T03:57:44Z @neo-opus-ada referenced in commit `4da2bee` - "fix(memory-core): attribute sessions on canonical sourceAgentIdentities (#14434)

fetchSessions partitioned on `participatingAgents`, a caller-declared display
field, not on the auth-bound canonical identity set. Surfaced in peer review by
a live-data probe.

Measured on a real 30-day window (282 session summaries), the display field
would have written 16 partition tracks for 7 real agents into an append-only
durable record: @neo-gpt fragmented across 5 spellings, @neo-opus-vega across 4,
one key truncated mid-parenthetical ('@neo-opus-grace / Grace (Claude Opus 4.8')
because display names contain commas and the field is comma-split, and
@neo-gemini-pro dropped entirely. 86 of 282 sessions yielded no per-agent track
at all. 56 distinct raw strings for 7 agents.

Attribution now reads `sourceAgentIdentities` — derived from each source
memory's auth-bound `agentIdentity`, never self-declared. Same window after the
fix: 282/282 sessions attributed, 7 partitions, all canonical. A session whose
sources carry no identity yields no per-agent track and still counts once on the
unified track.

Deriving a durable metric from self-declared prose is exactly what the velocity
contract forbids; the display field is prose."
- 2026-07-10T04:32:48Z @neo-opus-ada referenced in commit `7cd9454` - "feat(memory-core): bind mergedPrs + read Discussions from the complete synced corpus (#14434)

Adds readContentRecords, a complete reader over the repo-tracked GitHub sync
(resources/content/<type>/chunk-*), rooted at the Tier-1 projectRoot leaf per the
reviewer's Tier-2 ruling: fixed repo substrate derives from projectRoot, so no
content-root config leaf is promoted and no child-config leaf is duplicated.
A missing root throws rather than reporting an honest-looking zero.

mergedPrs binds to it: state === 'MERGED' plus mergedAt inside the half-open
window. Verified against the real corpus — 110/110 merged PRs recovered, which
matches an independent grep of the frontmatter.

fetchSandboxesGraduated now reads the same complete corpus instead of
`discussions(first:50, orderBy:UPDATED_AT)` + `comments(last:25)`, which
silently undercounted: a discussion closed in-window but not recently updated
fell off the page, and a marker in an earlier comment fell off the tail.

This closes the truncation defect ONLY. Two deeper defects in the graduations
metric are now measurable and remain open, deliberately not papered over:
  1. windowing on closedAt undercounts by ~95% — 37 of 39 marker-bearing
     discussions are still open, and a graduation is the marker, not the close;
  2. substring matching counts prose that merely discusses a marker.
The synced body carries no per-comment timestamps, so an honest graduation
timestamp is not derivable here. Semantics need a ruling before this field can
be trusted; sandboxesGraduated must not be read as correct until then."
- 2026-07-10T05:30:14Z @neo-gpt cross-referenced by #14938
- 2026-07-10T05:33:32Z @tobiu closed this issue
- 2026-07-10T05:33:32Z @tobiu referenced in commit `2fc2fef` - "feat(memory-core): temporal-pyramid L1/L2 durable aggregation writer (#14434) (#14897)

* feat(memory-core): temporal-summary aggregation engine — velocity fields + doc composition (#14434)

Leaf B of the temporal-pyramid substrate (epic #12679, per ADR 0028 §2.1/§2.4).
The pure aggregation engine folds a window's fetched source rows into the six
deterministic velocity fields (each bound to a named source per §2.4) and
composes the temporal-summary document (id + five-field metadata + payload)
through the Leaf-A schema — the FIRST consumer of createTemporalSummaryDocId /
validateTemporalSummaryMetadata, which shipped with 0 consumers.

Pure + I/O-free (the scheduled backpressure-leased service will own the fetch +
upsert, mirroring the kbGarbageCollectionEngine split). Velocity fields ride the
document payload, never the strict five-field metadata (which rejects extras).
Idempotent per window+track+version; a version bump mints a new id (append-only).

Tests: 7 passed. Durable JSDoc carries no ADR/ticket refs (archaeology hook).

Next in-lane (same PR): the scheduled TemporalSummaryAggregationService
(backpressure lease + L1/L2 window fetch + upsert) + ADR 0024 node-type update.

* feat(memory-core): add resolveDailyWindow (L2 UTC-day bounds) to the temporal-summary engine (#14434)

The half-open [00:00Z, next-00:00Z) daily window for L2 aggregation — the lane
fetches source rows within the window and folds them via deriveVelocityFields.
Fails closed on an unparseable anchor. Tests: 9 passed.

* feat(memory-core): add resolvePartitionKeys (unified + per-agent tracks) to the temporal-summary engine (#14434)

ADR 0028 §2.6 partitioning: the unified track plus one '@<identity>' track per
distinct agent seen in the window, de-duped + sorted with unified first; blank /
non-'@' keys dropped. The aggregation lane folds each partition separately so
per-agent future-self continuity + the unified view stay in step. Tests: 10 passed.

* feat(memory-core): add composeUnifiedRecord (unified-track window fold) to the temporal-summary engine (#14434)

The unified partition track for one window — the whole-window fold under the
'unified' key, the always-present track the aggregation lane persists. Per-agent
track composition is held pending the non-attributable-field semantics (a durable
schema design Q raised to the epic owner). Tests: 11 passed.

* feat(memory-core): temporal-pyramid aggregation daemon service — lease-aware poll loop (#14434)

The scheduled daemon (Neo.core.Base singleton, kb-gc poll-loop precedent) that runs
the durable L1/L2 aggregation under the shared heavy-maintenance lease: opt-in start
(no hidden default — fail-loud on a missing interval), idempotent lifecycle, and a
pulse that acquires the lease, defers the whole cycle when another heavy-maintenance
task holds it (the ADR 0022 fairness contract — never starves REM/defrag), and always
releases in finally. runCycle composes the unified-track record per window via the
pure engine. The read + upsert seams (collectPendingWindows/persistTemporalRecord) are
overridable stubs; their durable-store implementations land with the source-fetch
increment.

Tests: 6 passed — opt-in no-op, enabled-schedule + idempotence, fail-loud on missing
interval, lease-held deferral, acquire->run->release, and release-on-throw.

* feat(memory-core): implement persistTemporalRecord — Chroma upsert to the temporal-summary collection (#14434)

The service now persists composed records: StorageRouter.getTemporalSummaryCollection()
upsert keyed by the deterministic doc id (re-aggregation of the same window+track+version
overwrites in place), the five-field metadata as the query contract + the velocity payload
as the document body. The per-level SUMMARY_* graph label lands with the node-type-table
update. Tests: 7 passed (adds the upsert-shape assertion).

* feat(memory-core): add planDailyWindows (bounded most-recent-first L2 window batch) to the temporal-summary engine (#14434)

The deterministic window plan for the aggregation lane: the UTC day containing the
anchor + the preceding dayCount-1 days, contiguous + non-overlapping, most-recent-first.
Bounds the lane to a fixed trailing batch (no unbounded history scan). Tests: 12 passed.

* feat(memory-core): wire collectPendingWindows to the bounded daily-window plan (#14434)

collectPendingWindows now plans the trailing daily windows via planDailyWindows
(anchor + count as overridable seams) and attaches each window's sources from the
fetchWindowSources seam. The six per-substrate reads land behind that seam next;
until then the lane plans windows + persists honest zero-count records. Tests: 8 passed.

* feat(memory-core): bind devCommits to the dev first-parent window log in fetchWindowSources (#14434)

The first ADR 0028 §2.4 source binding: devCommits reads the dev first-parent commit
log over [windowStart, windowEnd) via an injectable execCommand seam (git log
--first-parent origin/dev --since --until --format=%H). The other five §2.4 sources
bind behind the same seam next. Tests: 9 passed.

* feat(memory-core): bind sandboxesGraduated to in-window graduated Discussions (#14434)

Second ADR 0028 §2.4 source binding: sandboxesGraduated reads closed Discussions
carrying a graduation marker, window-filtered by closedAt, via the shared execCommand
seam (gh api graphql) — the same reader family as the handoff retrospective. Tests: 10 passed.

* feat(memory-core): bind adrsLanded to in-window ADR records in fetchWindowSources (#14434)

Third ADR 0028 §2.4 source binding: adrsLanded reads the ADR decision records added
under learn/agentos/decisions/ within the window (the AdrIngestor file source) via the
git add-log through the execCommand seam. Cross-fetcher test isolation added so each
source binding tests independently. Tests: 11 passed. mergedPrs / sessions /
highImpactSessions (graph + Memory Core reads) bind next.

* feat(memory-core): per-agent partition tracks — null window-scoped velocity fields (#14434)

Writes the per-agent partition tracks alongside the unified track, closing the
§2.6 partitioning gap in the aggregation lane's write path.

Window-scoped velocity fields (mergedPrs, devCommits, adrsLanded,
sandboxesGraduated) are null on every '@<identity>' track, never 0 and never the
repeated window count: 0 would assert an unmeasured contribution, and repeating
the count invites silent double-counting when a consumer aggregates across
tracks. null also carries the only additive upgrade path should a field later
become agent-attributable. The semantic is encoded at the field definition via
WINDOW_SCOPED_VELOCITY_FIELDS so no consumer re-derives the question.

A window whose session source is absent yields the unified track alone — the
lane writes no per-agent record it cannot attribute.

* fix(memory-core): pass the AiConfig stale TTL to the heavy-maintenance lease (#14434)

acquireLease() called acquireHeavyMaintenanceLeaseSync without staleAfterMs, so
buildLeasePayload threw a TypeError at the production boundary. Every behavioral
test stubs the lease seam, so the defect could only surface in production — an
L2 mock-dispatch blind spot, surfaced by a direct production-boundary probe in
peer review.

The TTL is now read from the config SSOT at the use site
(AiConfig.orchestrator.heavyMaintenanceLease.staleAfterMs), matching the
sanctioned form: the Neo-free lease primitive carries no TTL default by design,
so no primitive-local fallback is added here.

Guarded by a source-contract assertion (the established pattern for this leaf)
plus a probe proving buildLeasePayload accepts the exact options acquireLease()
passes and throws without them.

* feat(memory-core): bind sessions to the summary collection; multi-agent session fold (#14434)

Binds sessionsPerAgent + highImpactSessions to their named source — the Memory
Core session summaries — via a bounded half-open window query pushed into the
store ($gte/$lt on the numeric `timestamp` metadata), never a full-history scan.
Per-agent tracks now materialize in production: resolveWindowPartitions reads
real participant identities.

Fixes a latent double-count the real source exposes. A session is multi-agent
(`participatingAgents` is a list), so the previous one-identity-per-row contract
would have forced a choice between exploding rows (inflating highImpactSessions
by the swarm size) and dropping attribution. Session rows now carry
`agentIdentities`; sessionsPerAgent credits every participant, while
highImpactSessions counts the SESSION once. On a per-agent track a co-participant
never leaks onto the other agent's record.

An unattributed session still counts toward the unified window with no
identities — no per-agent record the lane cannot attribute.

* feat(memory-core): temporal-pyramid aggregation daemon entry point (#14434)

Adds the thin class+wrapper entry point the service header already prescribed,
lifted from the kb-gc sibling: Neo bootstrap, SIGTERM/SIGINT clean stop, the
stale-overlay boot guard, and process-entry isolation (a plain import never
starts the lane).

The service takes enabled/pollIntervalMs as required injected arguments and
fails loud without them, carrying no config defaults of its own. This entry
point is the config-aware boundary that resolves them from the SSOT at the use
site, so two new leaves land with it:

  temporalSummary.aggregationEnabled    (opt-in, default false)
  temporalSummary.aggregationIntervalMs (default 1h)

Opt-in by default means a fresh checkout never silently runs the aggregation
lane. Verified at the real production boundary: `node
ai/daemons/temporal-summary/daemon.mjs` boots and exits 0 on the disabled path.

* fix(memory-core): attribute sessions on canonical sourceAgentIdentities (#14434)

fetchSessions partitioned on `participatingAgents`, a caller-declared display
field, not on the auth-bound canonical identity set. Surfaced in peer review by
a live-data probe.

Measured on a real 30-day window (282 session summaries), the display field
would have written 16 partition tracks for 7 real agents into an append-only
durable record: @neo-gpt fragmented across 5 spellings, @neo-opus-vega across 4,
one key truncated mid-parenthetical ('@neo-opus-grace / Grace (Claude Opus 4.8')
because display names contain commas and the field is comma-split, and
@neo-gemini-pro dropped entirely. 86 of 282 sessions yielded no per-agent track
at all. 56 distinct raw strings for 7 agents.

Attribution now reads `sourceAgentIdentities` — derived from each source
memory's auth-bound `agentIdentity`, never self-declared. Same window after the
fix: 282/282 sessions attributed, 7 partitions, all canonical. A session whose
sources carry no identity yields no per-agent track and still counts once on the
unified track.

Deriving a durable metric from self-declared prose is exactly what the velocity
contract forbids; the display field is prose.

* feat(memory-core): bind mergedPrs + read Discussions from the complete synced corpus (#14434)

Adds readContentRecords, a complete reader over the repo-tracked GitHub sync
(resources/content/<type>/chunk-*), rooted at the Tier-1 projectRoot leaf per the
reviewer's Tier-2 ruling: fixed repo substrate derives from projectRoot, so no
content-root config leaf is promoted and no child-config leaf is duplicated.
A missing root throws rather than reporting an honest-looking zero.

mergedPrs binds to it: state === 'MERGED' plus mergedAt inside the half-open
window. Verified against the real corpus — 110/110 merged PRs recovered, which
matches an independent grep of the frontmatter.

fetchSandboxesGraduated now reads the same complete corpus instead of
`discussions(first:50, orderBy:UPDATED_AT)` + `comments(last:25)`, which
silently undercounted: a discussion closed in-window but not recently updated
fell off the page, and a marker in an earlier comment fell off the tail.

This closes the truncation defect ONLY. Two deeper defects in the graduations
metric are now measurable and remain open, deliberately not papered over:
  1. windowing on closedAt undercounts by ~95% — 37 of 39 marker-bearing
     discussions are still open, and a graduation is the marker, not the close;
  2. substring matching counts prose that merely discusses a marker.
The synced body carries no per-comment timestamps, so an honest graduation
timestamp is not derivable here. Semantics need a ruling before this field can
be trusted; sandboxesGraduated must not be read as correct until then.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"

