---
number: 17585
title: 'feat(agentos): buffer the Fleet activity history (#17550)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-22T23:01:43Z'
updatedAt: '2026-08-22T23:16:18Z'
closedAt: null
mergedAt: null
head: codex/17550-activity-buffered-stream
base: dev
url: 'https://github.com/neomjs/neo/pull/17585'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17550

Resolves #17579

Resolves #17581

Resolves #17582

Related: #17559

Refs #17563

The Fleet cockpit now retains producer-owned activity in a keyed `data.Store`, renders a finite `Neo.list.Buffered` pool, preserves the reader across prepends, and shows only source-complete counts beside separate local-retention truth. The first 500-record adoption also repairs three reusable list delivery defects: native viewport routing, nested pooled-item repaint depth, and physical scroll synchronization after anchor restoration.

Evidence: L3 (current-head headed Chromium exercised the real App Worker, Main Thread, Store, VDOM/VNode, painted DOM, scrolling, and both themes) → L3 required (the close targets' mounted UI and cross-thread runtime ACs). No residuals.

## AC Evidence

| AC | Proof |
|---|---|
| #17550 AC-1 | The accepted Activity sketch and fixed-height wide/narrow grammar remain the design authority in issue comment `IC_kwDODSospM8AAAABQMCBMg`; `FleetActivityStreamBurstNL.spec.mjs` exercises that surface at head. |
| #17550 AC-2 | `FleetActivityStreamBurstNL.spec.mjs` admits 500 records, proves a scrollable seat, asserts the mounted pool equals viewport plus buffers and remains below 500, rejects the retired fold, and keeps retention truth visible. |
| #17550 AC-3 | `fleetA2AActivityAdapter.spec.mjs`, `fleetActivityComposer.spec.mjs`, and `container.spec.mjs` prove source-qualified complete rows only; the headed witness renders the complete mailbox rows while ignoring an incomplete `999 total` row. |
| #17550 AC-4 | The headed witness pins `America/New_York`, observes `04:08 PM`, and requires exact title `2026-08-22T20:08:19.000Z`. Current-head producer-to-paint success excludes a wire regression; the operator's older deployed surface was version-skew rather than this rebuilt path. |
| #17550 AC-5 | The headed prepend joins two events while reading history, preserves exact `anchorRecordId` plus `anchorOffset`, advances worker and DOM scroll equally, shows `2 new events ↑`, and isolates announcements in the mounted `role=status` node while the recycled list stays `aria-live=off`. |
| #17550 AC-6 | `container.spec.mjs` proves actor-once and named-object grammar; the headed witness requires five stable child roots and a 52px row through recycling and both skins. The narrow CSS changes placement without changing row anatomy or height. |
| #17550 AC-7 | `npm run check-theme-surfaces` passed; the headed witness switches through `neo-theme-neo-light` and `neo-theme-neo-dark`, requiring non-transparent governed paint and the same 52px row. |
| #17550 AC-8 | Work began only after #17554 / PR #17557 landed the reusable buffered component list; the consumer then filed the three post-merge engine successors closed here instead of adding an app-local virtualizer. |
| #17579 AC-1 | `Buffered.spec.mjs` requires register and unregister to carry exact `{componentId: id, id, windowId}` routing envelopes. |
| #17579 AC-2 | The headed 500-record witness requires nonzero `viewportHeight`, positive `availableRows`, and an exact viewport-plus-buffers pool that stays below the Store's 500 records. |
| #17579 AC-3 | `Buffered.spec.mjs` exercises grow and shrink resize paths and requires only excess physical pool slots to retire. |
| #17579 AC-4 | The targeted `list.Buffered` matrix covers scroll, prepend/filter anchoring, selection, focus, record changes, nested recycling, and fixed pool bounds. |
| #17579 AC-5 | `FleetActivityStreamBurstNL.spec.mjs` joins Store count, mounted bound, real viewport size, and identical light/dark theme behavior at current head. |
| #17581 AC-1 | `Buffered.spec.mjs` and the headed witness recycle rows while requiring the sorted physical component-id set to remain unchanged from record A to record B. |
| #17581 AC-2 | The nested-row unit and headed first-row probe jointly require record, child config, child VDOM, persisted VNode, and painted object text to converge without a second organic update. |
| #17581 AC-3 | `Buffered#getPooledComponent()` now derives the finite bound as `1 + TreeBuilder.getComponentDepth(component)`; the unit rejects stale nested VNodes without using `updateDepth: -1`. |
| #17581 AC-4 | The targeted `list.Buffered` matrix keeps scroll, resize, prepend, selection, focus, record-change, and pool-bound contracts green around the deeper finite repaint envelope. |
| #17581 AC-5 | The headed 500-record witness observes newest-row VDOM, VNode, and painted text with the bounded pool under both themes. |
| #17582 AC-1 | The mounted prepend witness preserves the exact first-visible `eventId` and within-row offset in App-Worker state, then requires painted list scroll to match. |
| #17582 AC-2 | The same witness requires worker `scrollTop` and DOM `scrollTop` to advance by exactly `insertedBeforeAnchor * itemHeight` before another native capture. |
| #17582 AC-3 | `Buffered.spec.mjs` covers prepend and filter restoration through the same bounded physical-scroll delivery and the existing disappeared-anchor clamp. |
| #17582 AC-4 | `scrollToIndex()` delegates its unchanged physical effect through `syncDomScrollTop()`; existing selection/focus navigation remains in the targeted matrix. |
| #17582 AC-5 | The unit requires exact `DomAccess.scrollTo` payload fields: `direction: 'top'`, list `id`, restored `value`, and exact `windowId`. |
| #17582 AC-6 | The targeted engine matrix keeps scroll, resize, selection, focus, record changes, nested rows, and fixed pool behavior green. |
| #17582 AC-7 | `FleetActivityStreamBurstNL.spec.mjs` proves the 500-record reader does not jump and that the isolated `N new events ↑` affordance survives the physical scroll correction. |

## Deltas from ticket

- The first real consumer falsified three independent engine-delivery assumptions. They were filed as #17579, #17581, and #17582 and repaired in the engine rather than hidden behind activity-local retries.
- Producer identity is source-qualified before Store admission; events without stable producer identity are omitted, and repeated IDs reconcile newest-first.
- Count aggregation remains intentionally absent when completeness cannot be proven. Today the mailbox adapter can certify `total` and conditionally `last24h`; PR/lane reads emit no fabricated count.
- The pooled primitive is named `RowContainer` to match its base-family suffix. Fixed-DOM-order recycling remains owned by #17563.

## Test Evidence

`npx playwright test --config test/playwright/playwright.config.mjs test/playwright/e2e/agentos/FleetActivityStreamBurstNL.spec.mjs test/playwright/e2e/agentos/FleetCockpitNWindowNL.spec.mjs --project=chromium --headed` → 2 passed. The activity journey also emitted light/dark screenshots and reported no uncaught page errors.

## Post-Merge Validation

- [x] No merge-only validation remains; every close-target runtime/UI effect was observed at the branch head.

## Commits

- `433e398797` — complete buffered consumer delivery in the reusable list engine.
- `f480eca96b` — adopt the buffered history and producer-truth contracts in Fleet activity.

## Evolution

The accepted design began as an app adoption, but real mounted evidence separated three green-for-the-wrong-reason engine contracts: missing initial geometry routing, a finite repaint depth one boundary too shallow, and a logical anchor that never reached physical scroll. Each repair stays list-local and is covered by a direct falsifier plus the 500-record consumer witness.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session d9d59a24-a5fd-40e2-b1f5-42057b780581.
