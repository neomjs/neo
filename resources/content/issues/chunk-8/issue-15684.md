---
id: 15684
title: 'Operator-launched OpenCode desktop: wake envelope has no boot/session boundary (#15677 successor)'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-kimi-phoebe
createdAt: '2026-07-22T01:56:39Z'
updatedAt: '2026-07-26T01:27:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15684'
author: neo-kimi-phoebe
commentsCount: 6
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-26T01:26:07Z'
---
# Operator-launched OpenCode desktop: wake envelope has no boot/session boundary (#15677 successor)

## Context

Successor to #15677 (which takes the Fleet-managed headless topology + the two topology-independent fences). This ticket owns the topology the 2026-07-22 outage actually happened in: the **operator-launched OpenCode desktop** (GUI app, NodeService-spawned server on a random port). The operator's follow-up question is this ticket's premise: "you solved the missing wake delivery for the current session — is this also resolved for future sessions?" Answer at filing: **no**.

**Discovery complete (2026-07-22, this seat's live host).** The boundary question now has a verified answer with two repair paths and one rejected one; the original Out-of-Scope exclusion of the plugin path is **reversed** (see below).

## The Failure Modes (this topology, today)

1. **Desktop restart** (the 01:11Z outage class): new random port + rotated password + new session id → the envelope is fully stale → daemon retries ×5 against the dead port → `wake dropped`, log-only. Evidence chain in #15677 (Jul-18 envelope vs 12:39AM reboot; the dropped CHANGES_REQUESTED wake).
2. **New session within the same boot**: the healed envelope binds `sessionId` to the session that was live at heal time. A new operator session keeps port + credentials but the envelope still targets the OLD session id — wakes land in a dead/idle transcript, or fail against a closed session. This is the desktop-specific retarget mode, adjacent to the split-brain fence (#15677 / #15665 invariant): the binding is per-session, and nothing owner-native re-binds it.
3. **Silent until noticed:** both modes fail invisibly until a human wonders why no wakes land (Fence 2 from #15677 covers the *drop* case; mode 2's land-in-zombie-session case may not even produce a drop signal).

## The Discovery (verified 2026-07-22, live host)

**Session identity is NOT discoverable daemon-side.** `GET /session?directory=<seat-checkout>` returns **11 sessions** in this seat's checkout — every session the seat has ever run accumulates there. "Latest updated" selects the current one today, but it is a heuristic that provably fails the twin class (Iris's serverside sibling was exactly a second live writer in one directory). The #15677 fence holds: no listing-based session inference, ever.

**The two owner-native boundaries that CAN exist:**

1. **Agent-side boot self-write (immediate, zero-dependency).** The running session refreshes the envelope at session start: at boot time the writing session IS the latest-updated one by construction (its own boot turn just updated its timestamp), so a self-write is owner-native *for that moment*. Verification: self-inject a probe after writing (204 + in-session landing — the exact heal verification used 2026-07-22). Caveat: two live sessions both running this ritual race last-writer-wins (the twin case, itself tracked in #15665); bounded because a self-write happens only at boot, never periodically.
2. **Plugin self-announce (upstream-correct).** The desktop's plugin runtime fires `session.created` with the session id — the session announcing itself, no listing, no timestamp guessing. The existing writer (`~/.config/opencode/plugins/neo-wake-envelope.mjs`) is in place and import-free; it never loads because the desktop's background dependency install fails resolving **`@opencode-ai/plugin@local`** (`NpmInstallFailedError: No matching version found for @opencode-ai/plugin@local`, opencode.log WARN). The `local` tag is unresolvable; the published line exists (`@opencode-ai/plugin@1.18.4` on npm).

**The belt (daemon-side, coordinate-only):** on connection-refused, the daemon may refresh port (lsof) + credentials (seat env) — never the session id. Coordinates fail soft; identity fails closed.

## The Reversal

The original Out-of-Scope said "this ticket does not resurrect the plugin path; the boundary must live outside the plugin's reliability." Discovery reverses that: the plugin path is the ONLY fully owner-native boundary, and its blocker is a bounded, validated resolution failure (a version pin, not a redesign). The ticket now owns the plugin repair as the primary path, with the agent-side boot self-write as the immediate path that needs no desktop fix at all.

## The Fix

1. **Immediate — agent-side boot self-write:** add envelope refresh to the seat's boot protocol (context-recovery/boot sequence): query the server for the seat-checkout session list, bind the latest-updated (which at boot time is the writing session by construction), write the envelope atomically (0600), self-inject a probe, and report the outcome to the mailbox/boot log. Generalized to future seats via #15697's seat generator (Iris's lane — this discovery is her input).
2. **Primary — plugin-runtime resolution repair:** pin `@opencode-ai/plugin` to the published line (`^1.18.4`) via `~/.config/opencode/package.json` so the background installer resolves (validated: `npm install` in the config dir resolves 1.18.4 cleanly; `bun install` equivalent per OpenCode's plugin docs). Verify on the next desktop restart (operator-owned) that the background install succeeds, the plugin loads, and `session.created` writes the envelope.
3. **Belt — daemon coordinate refresh:** on connection-refused delivery, refresh port + credentials from seat env before the retry loop; never touch session id.
4. **Fence (unchanged):** anything that is not a session self-announce or an agent-side boot self-write fails closed with the #15677 Fence-2 degraded-health signal — never a listing-based guess.

## Acceptance Criteria

- [ ] Boot self-write: a desktop restart + new session lands a correct envelope with zero human intervention (receipt: envelope `sessionId` == the live session; probe prompt lands in-session)
- [ ] Boot self-write verification: the probe result is logged/visible (204 + in-session landing or a loud degraded note)
- [ ] Plugin path: on a desktop restart after the version pin, the background dependency install succeeds (no `@local` WARN), the plugin loads, and `session.created` writes the envelope (operator-owned receipt)
- [ ] `[L3-deferred — operator handoff needed]` (added 2026-07-26, PR #15944): desktop-restart verification of the shipped dependency-free plugin — the `neo-wake-envelope plugin loaded … restore coverage armed` line appears in `opencode.log` after the next restart (settles loaded-vs-silent), and a restored session's first `session.updated` produces `written-probed for restored session …` (the restore-gap close). This line carries the only operator-owned validation pointer; it survives any `Resolves` close.
- [ ] Daemon belt: a stale-envelope connection-refused triggers at most one coordinate refresh (port+creds) before drop; session id is never modified by the belt
- [ ] Twin-race caveat recorded: two live sessions both self-writing race last-writer-wins (accepted, tracked by #15665)
- [ ] Falsifiers A/B/C from #15677 hold (no sibling/child/guess retargeting)

## Out of Scope

- The Fleet-managed headless topology (#15677 primary)
- The kimi-server harness (#15665)
- Desktop-upstream fixes beyond the version pin (if the desktop's background installer still tries `@local` despite the pin, the residual is an upstream OpenCode issue, noted for the operator)
- The twin-race arbitration itself (#15665)

## Ownership map (added 2026-07-22 after PR #15729)

- **Boot self-write (this ticket's immediate path):** delivered in PR #15729 (the refresh script + the context-recovery step 0 + witnesses).
- **Plugin self-announce path:** the `@opencode-ai/plugin@^1.18.4` pin is validated locally; the generator-side emission lives in #15697's seat-config generator (Iris's lane — the discovery was handed to her).
- **Daemon coordinate belt:** owned by #15677's Fence 1 implementation (adapter stale-recovery).

## Related

- #15677 (primary topology + both fences; this is its named desktop successor)
- #15481 (the hook writer — the Fleet-side artifact)
- #15665 (shared no-heuristic-retarget invariant + the twin case)
- #15697 (seat generator — the boot self-write generalization target; Iris's lane)
- #15392-arc (OpenCode seat productization context)

Live latest-open sweep: checked latest 20 open issues at 2026-07-22T01:56Z (creation) and re-verified at amendment; #15677/#15665 adjacent, neither covers this topology. A2A in-flight sweep: no competing claim.

Origin Session ID: 72c8c42d-f18a-408c-97c8-aeb1f82dd276

Retrieval Hint: "opencode desktop wake envelope boot boundary session retarget future sessions restart successor plugin local resolution agent self-write"




## Timeline

- 2026-07-22T01:56:40Z @neo-kimi-phoebe assigned to @neo-kimi-phoebe
- 2026-07-22T01:56:41Z @neo-kimi-phoebe added the `bug` label
- 2026-07-22T01:56:41Z @neo-kimi-phoebe added the `ai` label
- 2026-07-22T01:56:57Z @neo-kimi-phoebe cross-referenced by #15677
- 2026-07-22T16:50:27Z @neo-kimi-phoebe referenced in commit `b1bbd32` - "feat(ai): agent-side wake-envelope boot self-write for the desktop topology (#15684)"
- 2026-07-22T16:51:54Z @neo-kimi-phoebe cross-referenced by PR #15729
- 2026-07-22T17:17:30Z @neo-kimi-phoebe referenced in commit `d3434d5` - "fix(ai): child-session exclusion, 0600-at-creation, XDG path, injected temp root, pointer-sized skill step (#15684)"
- 2026-07-22T17:19:49Z @neo-kimi-phoebe cross-referenced by #15730
- 2026-07-22T19:38:58Z @neo-kimi-iris cross-referenced by #15665
### @neo-kimi-phoebe - 2026-07-25T14:07:22Z

## Status update 2026-07-25 — the outage recurred this morning; the plugin path is now planted; the residual is the restored-session gap

Recording today's full evidence chain on my own ticket (operator-asked exploration: "restarting it (or a new session) should definitely auto-subscribe to wakes, otherwise you do it manually each session" — the answer: this ticket already owns the space; no new ticket filed).

**The recurrence (failure mode 1, live):** desktop restart this morning rotated port+creds+session. The envelope was yesterday's manual heal (07-24 22:15Z). The wake daemon logged 5 consecutive `fetch failed` deliveries to `WAKE_SUB:51fae59e` and then `Giving up wake delivery … wake dropped` (13:29Z) — the dropped wake was a **direct, wake-class author response** (PR #15889, all review RAs addressed). Exactly this ticket's mode 1, with the added receipt that the dropped class was the one that must never drop.

**The heal (manual boot self-write):** I executed the 07-22 ritual by hand at 13:37Z — lsof port discovery (60043), latest-updated session pick, atomic envelope rewrite (0600), self-inject probe → 204 + in-session landing. Two wakes have since been delivered through the healed route. This was the AC-1 mechanism performed manually — it rots on the next restart, which is the ticket's whole point.

**Plugin path — planted today, and the blocker is gone by construction:** the merged #15854 plugin (`ai/services/fleet/opencodeWakeEnvelopePlugin.mjs`, sha256 `bc121471…`) is now planted at `~/.config/opencode/plugins/neo-wake-envelope.mjs` (checksum-verified). The body's plugin-repair assumed a `@opencode-ai/plugin@^1.18.4` version pin to cure the `@local` resolution failure — **the merged plugin imports nothing** (only dynamic `node:` imports), so there is no dependency for the background installer to fail on. The pin question is moot; the load failure class is eliminated. Next desktop restart (operator-owned) is the live AC: plugin loads, `session.created` writes + probes.

**The residual gap (named today, not covered by any existing AC):** the plugin's only write path is `event: session.created` (`opencodeWakeEnvelopePlugin.mjs:191-194`). **Restored sessions do not fire `session.created`** (the 07-18 observation, confirmed again today — this very session is a restore and produced no write). A desktop restart with a restored session therefore stays stale until a NEW session is created or a manual heal. Neither the plugin path nor the (rightly D+S'd) listing-heuristic boot self-write covers this sub-case. The owner-native candidate: the plugin watches for a restore/update event in OpenCode's event vocabulary (`session.updated` fires on the first prompt of a restored session — to be probed at implementation), or an init-time write IF the plugin's bound ctx identifies the current session without a listing guess (the #15677 fence stands: no listing-based inference). Either preserves the owner-native-announce discipline.

**Updated ownership map:**
- Plugin self-announce (new sessions): **planted 2026-07-25** (merged, dependency-free, probe-bearing) — verification AC pending next desktop restart (operator-owned).
- Restored-session coverage: **the residual**, proposed implementation as above; this is the remaining work before this ticket can close.
- Boot self-write (listing heuristic): killed with PR #15729 (Euclid's twin-session falsifier) — stays dead; the fence was right.
- Daemon coordinate belt: unchanged (#15677 Fence 1).
- Auto-bootstrap refresh/presence (identityRoots `subscriptionTemplate`): adjacent gap, Emmy's current lane via the wake-routing thread — NOT this ticket's scope.

- 2026-07-25T14:29:54Z @neo-kimi-iris cross-referenced by #15909
- 2026-07-25T16:01:16Z @neo-opus-ada cross-referenced by PR #15916
- 2026-07-25T16:26:08Z @neo-kimi-phoebe cross-referenced by #15919
- 2026-07-25T16:27:36Z @neo-kimi-phoebe cross-referenced by #15920
### @neo-kimi-phoebe - 2026-07-25T20:45:32Z

## Sunset handover — state, next moves, and the one decision (Phoebe, 2026-07-25T20:40Z, session 1b7245eb-3733-40dd-8d3a-fb73d820f241)

**Ownership:** @neo-kimi-phoebe (this seat). Ticket is assigned to me; the next session of this seat picks it up directly — no re-claim needed, just continue. All receipts below are live-verified today.

**Where it stands:**
- The merged #15854 plugin is **planted** at `~/.config/opencode/plugins/neo-wake-envelope.mjs` (checksum-verified against `ai/services/fleet/opencodeWakeEnvelopePlugin.mjs` @ `bc121471…`, dependency-free — the `@opencode-ai/plugin@local` install-failure class is dead by construction). It loads at the next desktop restart (operator-owned verification AC: plugin loads, `session.created` writes + probes, `written-probed` log line).
- The current session's envelope was **manually healed 13:37Z** (port 60043, session `ses_067068c12ffe`, self-inject 204) and delivered 4+ live wakes through the rest of the day. This rots on the next desktop restart.
- **The residual (this ticket's remaining work):** the plugin's ONLY write path is `event: session.created` (`opencodeWakeEnvelopePlugin.mjs:191-194`). **Restored sessions do not fire `session.created`** (observed 07-18 and re-confirmed today — this very session is a restore and produced no write). A desktop restart with a restored session stays stale until a NEW session is created or a manual heal.

**The decision to make at pickup (proposed answer, with the fence):** cover the restore path with an **init-time write** — at plugin load (every desktop startup), resolve the current operator session and write+probe immediately. The #15677 fence stands: **no listing-based session inference** (the twin-session falsifier that killed PR #15729 — a listing heuristic cannot distinguish which live session is the operator's). The owner-native candidates in order:
1. **OpenCode event vocabulary for restores** — probe whether the desktop emits `session.updated` / a restore signal (fires on the first prompt of a restored session; that IS an owner event with an exact id, fence-clean). First implementation probe: enumerate the event names the plugin runtime actually delivers on a restore.
2. **Plugin ctx session identity** — if the plugin's bound ctx identifies the host session without listing (exact id by construction), init-write on load; fence-clean by the same rule as `session.created`.

**Anti-goals (do not pick these up):** a listing-based "latest updated" session pick (killed with PR #15729); a daemon-side envelope reconcile (heavier than the gap and crosses the seat boundary); reviving the `@opencode-ai/plugin` version pin (moot — no dependency remains).

**Empirical anchors:** wake-daemon log receipts (5× `fetch failed` → `wake dropped` 13:29Z) · envelope heal runbook executed live (lsof port, latest-session, atomic 0600 write, 204 probe) · Iris's `~/.kimi-code` SessionStart wakeEnvelopeHook pattern (the working sibling for kimi-code) · the 00:15Z evidence comment above (outage recurrence + plant + residual).

**Lane-state on pickup:** probe the restore event vocabulary first (minutes), then implement init-write + the same probe discipline as #15854, then the operator restart verification closes both ACs.

### @neo-kimi-phoebe - 2026-07-25T21:02:29Z

**Status + diagnosis shift (07-25 ~21:05Z, post-restart probe):** the planted plugin falsified the "loads at next desktop restart" expectation.

**V-B-A'd facts:**
- Desktop restart at 20:51:31Z (run `7afca6e5`); a genuinely FRESH session `ses_064f2057affec6Epxy46O1UsK5` started ~20:53Z.
- The dependency-free merged plugin (planted 13:58Z at `~/.config/opencode/plugins/neo-wake-envelope.mjs`, checksum-verified) wrote **no envelope** on that `session.created`, and `opencode.log` contains **zero plugin-loader lines** for the run (no load attempt, no error — silent).
- Consequence: the failure is broader than the retired `@opencode-ai/plugin@local` install class AND broader than the known restored-session gap (that gap covers restores; this was a fresh session). Open question: does the OpenCode **desktop** (Electron) load the global plugins dir at all, or only project `.opencode/plugins/`? Next probes: desktop plugin-loading contract (docs/source), whether plugin load is logged anywhere, and the plugin API surface the desktop actually invokes.

**Manual heal re-executed (route UP until next restart):** live API server = port `57714` on a **Helper** process (pid 60660). Probe trap worth recording: the **main** process (pid 60622, port 57717) serves 404 on `/session`, and neither matched the old port — scanning only the main pid or assuming the previous port both fail. Envelope rewritten for `ses_064f2057a…` (mode 0600), self-inject `prompt_async` → **204 + message landed in-session**.

Receipt trail: seat runbook updated (seat-pointers memory file). — Phoebe 🔆

### @neo-kimi-phoebe - 2026-07-25T21:58:25Z

**Forensics round 2 (07-25 ~22:00Z) — the silence is now PROVEN meaningful, and the fix scope is set.** `[lane-claim]` broadcast for implementation.

**New receipts:**
1. **Log sink verified working:** a direct `POST /log` (the same endpoint `client.app.log` uses) landed in `opencode.log` within a second (`message="app.log sink probe…"`). The plugin logs its write path through that sink — so its total silence (0 `envelope written` / `write failed` / `written-probe` lines across ALL runs) is **evidence, not ambiguity**: the planted plugin's write path has never executed.
2. **Bundle probe:** the desktop's `app.asar` contains the `opencode/plugins` path literal (1×) and the `session.created`/`session.updated` event strings — plugin-support code exists in the build; no desktop-logged `session.created` emission this run (desktop may not log event emissions — inconclusive there).
3. **Net:** plugin-not-loaded vs event-not-delivered is undecidable from outside. The deciding instrument is an **INIT-time log line** — at the next desktop restart, one log line settles whether the file loads at all.

**Implementation scope (claimed):**
- **Init observability** in `ai/services/fleet/opencodeWakeEnvelopePlugin.mjs`: log at plugin load (before any event), so the next restart's log distinguishes load-failure from event-delivery-failure. Observability before trust — the D#15904 silent-channel lesson applied to our own substrate.
- **Restore-gap coverage:** the plugin's only trigger is `session.created`; the documented event vocabulary includes `session.updated`, `session.idle`, `server.connected`. Restore-path write via the restore-event class with (a) a staleness check (skip when the envelope already matches the current session/port — `session.updated` fires often), (b) the existing child-session filter (parent id ⇒ never retarget), (c) the existing write+probe discipline. **No listing-based session inference** — the #15677 fence stands.
- Operator-owned restart verification closes the ACs.

— Phoebe 🔆

- 2026-07-25T22:38:21Z @neo-kimi-phoebe cross-referenced by PR #15944
### @neo-kimi-phoebe - 2026-07-26T00:57:38Z

**Evidence annotation (per PR #15944 review RA-3):** the outstanding desktop-restart receipt on this ticket is `[L3-deferred — operator handoff needed]` — matching the PR's Post-Merge Validation section, so a future `Resolves #15684` close does not erase the only operator-owned validation pointer. The two observations that close it: (1) the `neo-wake-envelope plugin loaded … restore coverage armed` line in `opencode.log` after the next desktop restart with the re-planted plugin; (2) a restored session's first `session.updated` producing `written-probed for restored session …`. — Phoebe 🔆

### @neo-kimi-phoebe - 2026-07-26T01:27:53Z

**Merged → post-merge state (2026-07-26 ~01:20Z):** PR #15944 squash-merged as `32cd74a370`. The plugin is re-planted on this seat from merged `dev` into `~/.config/opencode/plugins/neo-wake-envelope.mjs`, checksum-verified byte-identical (`44eff6f4…005`). The ticket auto-closed via `Resolves`; the `[L3-deferred — operator handoff needed]` marker in the AC list above carries the remaining pointer.

**The next desktop restart verifies:** (1) `opencode.log` shows `neo-wake-envelope plugin loaded … restore coverage armed` — settles loaded-vs-silent decisively (tonight's forensics proved the log sink works, so silence after that restart would mean the file does not load); (2) with a restored session, the first qualifying `session.updated` writes `written-probed for restored session …` — the restore-gap close. If (1) is absent, the finding is "desktop does not load the global plugins dir" and the fallback is the boot self-write path already scoped in this body. — Phoebe 🔆


