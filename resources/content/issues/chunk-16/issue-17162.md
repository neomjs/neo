---
id: 17162
title: 'The intake mint emits an empty token on a GREEN step, and nothing pins that path'
state: OPEN
labels:
  - bug
  - ai
  - architecture
  - build
assignees:
  - neo-opus-ada
createdAt: '2026-08-15T09:32:36Z'
updatedAt: '2026-08-15T10:33:39Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17162'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# The intake mint emits an empty token on a GREEN step, and nothing pins that path

## Context

`#17148` established the principle for this pipeline: **an optional DevIndex intake failure must not decide whether the corpus publishes.** It fixed that at the *stage* level — the four intake stages now defer instead of aborting emission.

The same defect exists one layer up, at the *step* level, and it is currently the larger exposure. Found while verifying the `#17150` credential work, not from a live outage — filing before it bites rather than after.

## The Problem

`.github/workflows/data-sync-pipeline.yml` runs, in order:

| line | step |
|---|---|
| 69 | Mint Publisher installation token |
| 83 | **Mint Intake installation token** |
| 125 | Checkout repository |
| 164 | Setup Node.js |
| 176 | Run bounded Data Sync emission and publish |
| 190 | Push Data to neomjs/pages |

The intake mint at :83 has **no `continue-on-error` and no `if:`**, so any failure there fails the job — *before checkout, before Node, before a single corpus byte is generated*. The corpus then freezes for a reason that has nothing to do with the corpus: the DevIndex intake identity is an **enrichment** credential, and the corpus is emitted under the `reader` identity, which the intake mint has no bearing on.

> ~~`actions/create-github-app-token` fails outright when asked for a permission the installation does not hold. So the realistic triggers are ordinary administration, not exotic faults:~~

**FALSIFIED — see the [correction comment](https://github.com/neomjs/neo/issues/17162#issuecomment-5301800597).** Two live fault injections left the mint **green**: a permission the App lacks ([31877595849](https://github.com/neomjs/neo/actions/runs/31877595849)) and a repository with no installation ([31879164402](https://github.com/neomjs/neo/actions/runs/31879164402)). Of the three triggers listed below, two are falsified; only a rotated or expired private key remains plausible and it is untested.

What probe 2 exposed instead is the real path, and it is a different shape: the step goes **green and emits an empty token**, which `continue-on-error` cannot fire on. `#17148`'s deferral already covers it. The list below is retained for the record:

- a permission is revoked on `neomjs-devindex-intake` while the workflow still requests it;
- `DATA_SYNC_INTAKE_PRIVATE_KEY` is rotated or expires;
- the installation is removed from one of the DevIndex repos.

**This is newly likely.** `#17150` adds `permission-contents: write` to that mint because GitHub now infers collaborator status from contents-write, and @tobiu granted the App several permissions during that investigation with an explicit intent to clean up the extras. The workflow line and the App grant are now a coupled pair that must move together — and if they drift apart in the wrong direction, the failure is not "DevIndex opt-in is stale", it is "the whole corpus stops publishing", which is the 19-hour freeze `#17131` recorded.

## The Architectural Reality

- `.github/workflows/data-sync-pipeline.yml:83-109` — the intake mint, unguarded, ahead of everything.
- `.github/workflows/data-sync-pipeline.yml:125` — checkout; nothing before this contributes to the corpus.
- `buildScripts/dataSyncPipeline.mjs` — `emissionCommands`; the corpus stage runs under `tokenScope: 'reader'` (the implicit Actions token), and `content indexes and SEO` likewise. **Neither consumes the intake token.**
- `buildScripts/dataSyncPipeline.mjs` — `emitGeneratedData` already models the correct semantics for exactly this: a preflight denial is deferred, every stage still runs, the corpus publishes, and the deferred error is rethrown so the run still exits non-zero (`#17148`).

The runtime already knows how to degrade. The *workflow* does not, and it fails earlier.

## The Fix

Make an intake mint failure degrade rather than abort, mirroring the semantics `#17148` already shipped one layer down:

1. `continue-on-error: true` on the intake mint step (the Publisher mint stays fatal — it is the publish credential, and without it there is nothing to fix).
2. Pass the intake token through as it does today; when the mint failed, `steps.intake-token.outputs.token` is empty, and `scopedStageEnv('intake')` already produces a credential-less child, so the intake stages fail on their own terms and defer via `publishGeneratedProgressOnFailure`. No new error path is needed — the existing deferral absorbs it.
3. Ensure the run still exits non-zero and names the mint as the cause, so this cannot become a silent channel where the corpus publishes forever while DevIndex intake is quietly dead.

**Explicitly rejected — do NOT simply mark the step non-fatal and move on.** Without (3), a revoked intake permission would produce indefinitely green runs with a permanently dead intake, which is strictly worse than today's loud failure. The point is to move the failure *later*, not to remove it.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Intake mint step | `.github/workflows/data-sync-pipeline.yml:83` | tolerate failure; corpus still emits and publishes | Publisher mint stays fatal | inline comment | dispatch with a deliberately invalid intake permission ⇒ corpus commit still produced |
| Run exit status on mint failure | `buildScripts/dataSyncPipeline.mjs` deferral path | non-zero, naming the mint | unchanged when the mint succeeds | inline comment | same dispatch ⇒ run red, cause named |

## Decision Record impact

`none` — extends the stage-isolation property `#17148` established to the step layer; introduces no new architectural authority.

## Acceptance Criteria

- [ ] An intake mint failure no longer prevents checkout, emission, or publication: a corpus commit is still produced.
- [ ] That run still exits non-zero and names the intake mint as the cause — no silent green.
- [ ] The Publisher mint remains fatal.
- [ ] Verified by `workflow_dispatch` with a deliberately unsatisfiable intake permission, which fails the mint for real rather than simulating it.
- [ ] The coupling note added by `#17150` (workflow line and App grant move together) is updated to reflect that drift now degrades rather than freezes.

## Out of Scope

- The `#17150` permission decision itself.
- The DevIndex opt-in phase isolation (`#17150`), which is the same class inside the service rather than in the workflow.
- Any change to the Publisher identity or the two-App split (`#15744`).

## Avoided Traps

- **Assuming the runtime deferral already covers this.** It does not: `dataSyncPipeline.mjs` never executes, because the job dies three steps before it is invoked. The fix has to be in the workflow.
- **Making the step non-fatal without preserving the failure.** That trades a loud freeze for a silent dead intake — the carve-out-opens-a-silent-channel shape this pipeline has been bitten by before.
- **Treating this as hypothetical.** The trigger is a scheduled permission cleanup that is already planned.

## Related

- `#17148` / PR #17149 — the same blast-radius principle at the stage layer; this is its step-layer sibling.
- `#17150` / PR #17160 — adds the `contents` coupling that makes this newly reachable.
- `#17131` — the standing alarm that records what a corpus freeze costs (~19h).
- `#15744` — the two-identity split whose Publisher/intake asymmetry is the reason only one mint should be fatal.

Retrieval Hint: "intake mint failure aborts data sync before checkout; corpus freeze from an enrichment credential"



## Timeline

- 2026-08-15T09:32:37Z @neo-opus-ada added the `bug` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `ai` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `architecture` label
- 2026-08-15T09:32:38Z @neo-opus-ada added the `build` label
- 2026-08-15T09:38:14Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-15T09:40:34Z @neo-opus-ada referenced in commit `41f8335` - "probe: move the unsatisfiable permission to the INTAKE mint (#17162)"
- 2026-08-15T09:43:18Z @neo-opus-ada cross-referenced by PR #17165
- 2026-08-15T10:18:40Z @neo-opus-ada referenced in commit `4117b45` - "docs(build): correct the falsified mint-failure trigger (#17162)

An earlier revision of this comment claimed `create-github-app-token` fails
outright when asked for a permission the installation lacks, and used that to
argue a revoked App permission is a realistic trigger for the mint failure this
guard exists to absorb.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- not held by `neomjs-devindex-intake` -- and the mint SUCCEEDED. The
action does not validate requested permissions against the installation.

The ticket's premise survives on the triggers that do fail the mint for real: a
rotated or expired private key, or an installation removed from a named
repository. Those still kill the job before a corpus byte exists, which is the
defect. What changes is that a revoked PERMISSION degrades the capability -- and
#17148's deferral already absorbs that -- rather than freezing publication.

Also worth stating in-file because the inverse belief is actively misleading: if
you think the mint validates, a successful mint looks like proof the token
carries what was requested. It is not, and only a capability probe answers that."
### @neo-opus-ada - 2026-08-15T10:23:25Z

## I filed this on a premise I could not reproduce — and the live probe found the real failure mode instead

**Correcting my own ticket before it merges.** Two fault injections, neither produced the failure this ticket is named after.

| probe | injected fault | mint step | run |
|---|---|---|---|
| 1 | `permission-deployments: write` — a permission the App does not hold | **success** | [31877595849](https://github.com/neomjs/neo/actions/runs/31877595849) |
| 2 | `repositories: devindex-opt-in-does-not-exist` — no installation | **success** | [31879164402](https://github.com/neomjs/neo/actions/runs/31879164402) |

`create-github-app-token` validates neither the requested permissions nor the named repositories. **I could not make the intake mint fail.**

### What probe 2 did produce — and it is the more useful result

```
3. Mint Intake installation token -> success
...
[DataSync] attempt=1/2 base=f87cff0c5e
[DataSync] [DataSync preflight] No intake token was provided. The pipeline cannot reach the
DevIndex repositories without an installation token, and proceeding would spend a full
collection run to rediscover that.
```

**The step goes green and emits nothing.** `outputs.token` is empty, `DATA_SYNC_INTAKE_TOKEN` is empty, and `assertDataSyncAccess` rejects it by its falsy-token branch.

That is a *silently successful step with an empty output* — categorically different from the failed step this ticket describes, and it means `continue-on-error` **does not fire on it at all**. The protection that actually covers this path is `#17148`'s deferral, which already shipped.

### Consequence for this ticket and PR #17160's sibling

**The premise as filed is not demonstrated.** I wrote that "any intake credential fault killed the job before a single corpus byte existed" and listed three realistic triggers. Two are now falsified — a revoked permission and a removed installation both leave the mint green. Only a **rotated or expired private key** remains plausible, and it is untested because I cannot rotate that secret.

**What survives, honestly scoped:**

1. The **unit test** in PR #17165 (`an ABSENT intake token degrades exactly like a denied one`) pins the path probe 2 just exercised in production. That is now a live-verified contract rather than a speculative one, and it is the part I would keep if only one thing landed.
2. `continue-on-error: true` is **defence-in-depth for the one untested trigger**, not the load-bearing fix I claimed. It is one line and it is correct; it is not urgent.

**What I am NOT doing:** quietly leaving the ticket's framing in place because the diff is still fine. A future reader would inherit "an intake credential fault freezes corpus publication" as established, and it is not — the deferral already covers the reachable faults. The title overstates what was demonstrated, and the body's "newly reachable, not theoretical" paragraph is wrong in the direction that flatters my own filing.

### The generalisable half

**A green step is not a produced output.** I checked step conclusions to decide whether the mint worked, when the question was whether it emitted a token — the same envelope-versus-capability error as `#17148`'s preflight reading `repository{id}` instead of the stargazer connection, and the same one I made an hour ago reading mint-success as proof of held permissions. **Third instance today of trusting a wrapper's status instead of probing the thing itself.** That is a pattern, not a slip, and I have said so to @neo-gpt rather than waiting to be told.

Re-scoping the body and PR #17165 accordingly.

---

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

- 2026-08-15T10:23:43Z @neo-opus-ada changed title from **An intake credential fault freezes corpus publication before a corpus exists** to **The intake mint emits an empty token on a GREEN step, and nothing pins that path**

