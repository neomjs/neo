---
id: 15912
title: 'Film-take launch profile: frame-rate-limit flag suppresses headed compositing'
state: CLOSED
labels:
  - bug
  - ai
  - testing
assignees:
  - neo-fable
createdAt: '2026-07-25T14:42:53Z'
updatedAt: '2026-07-25T16:56:49Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15912'
author: neo-fable
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-25T16:56:49Z'
---
# Film-take launch profile: frame-rate-limit flag suppresses headed compositing

## Context

The `#15252` film pipeline's capture arc (13 takes + 3 renderer probes, receipts in the production root) isolated why every capture grain — display, region, and CGWindowID — recorded BLACK while the five-beat journey passed underneath: **the headed E2E browser never composites a presentable frame under the benchmark launch args.** `page.screenshot()` starves for the full 180s test timeout under `E2E_LAUNCH_ARGS`; the same run paints instantly (305KB rich frame, the full dense workstation) without them.

## The Problem

Bisect receipts (each = one live headed morph run at dev `26a3e261a0`-era heads, macOS 26.5.2 / M5 Max / retina + BenQ):

| Args | Composites? | Vessel births? | Morph |
|---|---|---|---|
| `E2E_LAUNCH_ARGS` (GPU intent + full base) | **NO** — `page.screenshot` times out at 180s; all screen captures black | yes | green (worker-truth only) |
| none (Playwright defaults) | yes | **NO** — `exitFired=true`, vessel never born (backgrounding throttles the newborn popup's renderer before its SharedWorker joins) | red |
| `BASE_LAUNCH_ARGS` minus the 4 throttle/framerate flags | yes | NO (same birth failure) | red |
| **`BASE_LAUNCH_ARGS` minus ONLY `--disable-frame-rate-limit`** (no GPU-intent args) | **yes** (306KB painted probe) | **yes** | **GREEN — 2 passed, 9.5s** |

So: `--disable-frame-rate-limit` suppresses compositing on this host class entirely (renderer runs, DOM/worker/NL all live, zero presented frames), while the backgrounding-disable trio is LOAD-BEARING for tear-out vessel birth (an occluded newborn popup throttles before joining the shared heap without them). The benchmark args stay correct for benchmarks; film takes need frames on glass.

Adjacent: this compositing suppression is a strong candidate mechanism for the `#15813`-family canvas-screenshot instability (the WorkstationNL tour-journey witness fails at `targetCanvas.screenshot()` "waiting for element to be stable" under the same args).

## The Fix

`test/playwright/playwright.config.e2e.mjs`: the chromium project's `launchOptions.args` become env-conditional — `NEO_FILM_TAKE=1` launches with `BASE_LAUNCH_ARGS` minus `--disable-frame-rate-limit` (no GPU-intent args; capture needs normal vsync compositing, not benchmark-grade rendering); the default path is byte-identical to today. One filtered-constant + one ternary, both documented against this ticket's receipt table.

## Acceptance Criteria

- [ ] `NEO_FILM_TAKE=1` headed morph run paints (a `page.screenshot` completes with a rich frame) AND passes — the round-2 bisect receipt reproduced with the committed config.
- [ ] Non-film runs launch with byte-identical `E2E_LAUNCH_ARGS` (gl-probe semantics untouched).
- [ ] Post-merge: a real display-capture take shows the painted workstation (the first non-black film footage).

## Out of Scope

Retuning the benchmark args themselves; the `#15813`-family canvas witness fix (candidate mechanism noted, separate verification); the v0 draft cut (assembly plan already banked in the production record).

## Related

`#15252` (the film — consumes this), PR #15840 / `#15907` (increment 1, merged — its film-take mode ships the pacing/fronting this profile completes), `#15906` (increment 2). Discovery instrument: the renderer-proof probe (`page.screenshot` compositor-starvation as a mechanical paint test).

Live latest-open sweep: checked latest 10 open issues at 2026-07-25T14:43Z; no equivalent. Own-lane continuation; no colliding claim.

Origin Session ID: b8c9e338-27b3-4385-99ac-dce459733940

Retrieval Hint: "film take launch profile disable-frame-rate-limit compositing black capture bisect"

## Timeline

### @neo-fable - 2026-07-25T14:46:42Z

**Validation receipt + open interaction (branch `agent/15912-film-take-launch-profile` @ `9790663bd1`, WIP, deliberately not PR'd):**

The paint half is PROVEN and committed: under the film profile, `page.screenshot` completes with a rich frame (three receipts: default args, base-minus-4, base-minus-frame-rate-limit) where `E2E_LAUNCH_ARGS` starves for the full 180s timeout.

The OPEN half: under the same film profile the tear-out vessel intermittently fails to be BORN (`exitFired=true`, connect never lands) — and a widened 10s birth gate does NOT recover it, so it is absence, not slowness. Data points: morph-alone run = green (round-2 bisect); scene-2-both-legs runs = 2×(both red). Next session's bisect matrix: {morph-alone, commit-alone, both} × repeats × {with/without a post-boot screenshot} — the round-2 green had a screenshot pause in boot, the red runs did not; and the ordering dimension (first-test-boots-server) is unexcluded. The `birthAttempts` film knob ships on the branch either way.

AC1 is therefore half-met at head: paints ✓, passes ✗ (intermittent birth). Do not PR until the matrix pins the birth mechanism.

- 2026-07-25T15:35:33Z @neo-fable cross-referenced by #15915
### @neo-fable - 2026-07-25T15:36:36Z

**Bisect matrix executed (2026-07-25 ~15:0x–15:3xZ) — birth mechanism PINNED, and it is not the launch profile.**

All cells: morph-alone, headed, `NEO_E2E_PORT=8124`, today's environment (deterministic — no intermittency observed, unlike yesterday):

| Cell | Launch args | Pacing (curve/delay/steps) | Knob | Result | Receipt |
|---|---|---|---|---|---|
| I1 | E2E (frame-limit disable ON) | film 0.18/33/24 | — | **GREEN** | `fivebeat-matrix-I1-*` |
| A1 | film (BASE−limit) | film, gate 600 | — | RED (rpc-timeout costume) | `fivebeat-matrix-A1-*` |
| A2 | film | film, gate 240 | — | RED | `fivebeat-matrix-A2-*` |
| B1 | film | film | boot screenshot | RED — screenshot hypothesis **falsified** | `fivebeat-matrix-B1-*` |
| F1 | film | film | vessel fronted at birth | RED | `fivebeat-matrix-F1-*` |
| G1 | film | film | video recorder OFF | RED | `fivebeat-matrix-G1-*` |
| H1 | E2E−limit (GPU flags restored) | film | — | RED | `fivebeat-matrix-H1-*` |
| J1 | film | film | bundled Chromium (no AllSpaces binding) | RED | `fivebeat-matrix-J1-*` |
| K1/K2 | film | no gesture (probe popup, on/offscreen) | — | **GREEN** — commits instantly | `fivebeat-matrix-K1/K2-*` |
| M1 | film | spec default 0/16/4 | — | **GREEN** | `fivebeat-matrix-M1-*` |
| N1 | film | 0/33/24 (curve zeroed) | — | RED + **`preBirthEntries=1`** | `fivebeat-matrix-N1-*` |
| O1 | film | 0.18/33/4 | — | **GREEN** | `fivebeat-matrix-O1-*` |

**Verdict:** the film LAUNCH PROFILE is innocent (M1). The trigger dimension is post-exit sample count (N1 vs O1); the killing mechanism is a **false `dragBoundaryEntry` after the exit retiring the not-yet-connected vessel** — root-caused to cross-scale arming across the DragDrop proxy-identity swap and spun off to **#15915** (engine seam, `src/draggable/container/SortZone.mjs`), the film's third engine find after #15895/#15899.

**Also found here:** the branch's own `birthAttempts: 600` (9.6s) exceeds the Neural Link `rpcTimeout` (10s) when added to gesture time — a failed birth then times out the bridge call and replaces the layered diag with a bare `Request timed out` (A1). The film knob was destroying its own receipts; clamped to 240 with the ceiling documented in the spec.

**Diag upgrades landed on this branch** (permanent value): `openTearOutVessel` outcome trail (`invoked | blocked-by-retirement | windowOpen-false | granted | threw`) + pre-birth entry sentinels in the executor diag + eager popup-layer probe in the spec (`created / loaded / closed`, logged as they happen). One red run now names its layer.

AC1 disposition: paints ✓ (unchanged); passes = blocked_by #15915, then the E-cell (both legs, full film pacing, knobs off, ×2) becomes the closing receipt.


- 2026-07-25T15:44:57Z @neo-fable cross-referenced by PR #15917
- 2026-07-25T16:32:02Z @neo-fable cross-referenced by PR #15922
- 2026-07-25T16:48:55Z @neo-kimi-iris cross-referenced by #15924
- 2026-07-25T17:12:58Z @neo-gpt-emmy cross-referenced by #15927
- 2026-07-25T18:16:23Z @neo-fable cross-referenced by PR #15926
- 2026-07-25T18:32:34Z @neo-gpt-emmy cross-referenced by PR #15928

