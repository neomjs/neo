---
id: 12157
title: 'KB release zip must be collection-scoped, never the whole .neo-ai-data'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-opus-ada
createdAt: '2026-05-28T23:44:03Z'
updatedAt: '2026-05-29T01:26:58Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12157'
author: neo-opus-ada
commentsCount: 0
parentIssue: 12153
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-29T01:26:58Z'
---
# KB release zip must be collection-scoped, never the whole .neo-ai-data

## Context

Sub of #12153 — **highest priority: latent privacy leak.** `uploadKnowledgeBase.mjs` zips the whole `.neo-ai-data` (private memories, the 199 MB graph SQLite, the mailbox), and `downloadKnowledgeBase.mjs` extracts it over the user's `.neo-ai-data` (clobbers their MC). Post-unification there is no clean KB folder to zip, so the artifact MUST be collection-scoped.

## The Architectural Reality

- `ai/scripts/maintenance/uploadKnowledgeBase.mjs:43-44,83`: `sourceDir='.neo-ai-data'`, `zip -r neo-ai-data.zip .neo-ai-data`. Set 2026-04-06 (commit `87069c0bd9`). The shipped v12.1.0 asset is the clean 2.2 MB `chroma-neo-knowledge-base.zip` — the script's output name never matched the published asset (the path is effectively unreleased/broken), which is why it's *latent* not active.
- `downloadKnowledgeBase.mjs`: fetches `neo-ai-data.zip` (404 vs the published asset) → soft-fails to local sync; the npm `prepare` hook runs it on every install.
- `buildScripts/release/publish.mjs:219` invokes `uploadKnowledgeBase`.

## The Fix

- Export **only** the `neo-knowledge-base` collection (daemon ETL → portable JSONL or a collection snapshot) into the release artifact — never the data dir. Defense-in-depth: assert the artifact contains no MC collections / no `sqlite/`.
- Download/ingest collection-scoped into the user's KB — never a dir-restore over `.neo-ai-data`.
- Reconcile the asset name (one canonical name across upload/download/publish).

## Contract Ledger

| Surface | Authority | Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| KB release artifact | `uploadKnowledgeBase.mjs` | collection-scoped `neo-knowledge-base` export | n/a | guide | `uploadKnowledgeBase.mjs:83` |
| KB ingest (user) | `downloadKnowledgeBase.mjs` | collection-scoped import; never clobber `.neo-ai-data` | local `ai:sync-kb` | guide | `downloadKnowledgeBase.mjs` |

## Acceptance Criteria

- [ ] Release artifact contains ONLY `neo-knowledge-base`; verified to exclude memory/sessions/graph + `sqlite/`. *(post-merge: inspect a built artifact)*
- [ ] User ingest never overwrites an existing memory-core.
- [ ] Upload/download/publish asset name reconciled.

## Out of Scope

The store rename (parent sub 2) — though this lands cleaner after it.

## Related

#12153 (parent), #12152.

Origin Session ID: efd8dc2e-2052-4089-814a-ab22cd8c6a62

## Timeline

- 2026-05-28T23:44:04Z @neo-opus-ada added the `bug` label
- 2026-05-28T23:44:05Z @neo-opus-ada added the `ai` label
- 2026-05-28T23:44:25Z @neo-opus-ada added parent issue #12153
- 2026-05-29T00:28:58Z @neo-opus-ada cross-referenced by #12153
- 2026-05-29T00:43:20Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-05-29T00:57:13Z @neo-opus-ada cross-referenced by PR #12160
- 2026-05-29T01:15:34Z @neo-gpt cross-referenced by PR #12159
- 2026-05-29T01:18:35Z @tobiu referenced in commit `7cedc3b` - "docs(ai): ADR 0017 — single flat unified Chroma store + dev/prod parity (#12154) (#12159)

Adds ADR 0017 amending ADR 0003 (Chroma Topology — Unified Only): preserves the
one-daemon core decision and records the store's concrete shape — a single flat
persist store named `unified`, identical local and cloud, with KB/MC/tenant
separation enforced by collection + metadata (isolation), collection-scoped
export (shipping), and the KB-as-cache/MC-as-store backup model (recovery) —
never by directory or daemon split. Retires the federated-era two-folder local
layout (`knowledge-base/` + stale `memory-core/`).

Grounded in a 6-agent plan stress-test of Epic #12153 + a live store/daemon/
cloud-compose audit. Encodes the verified cloud fact (the chromadb image
persists to WORKDIR-relative /chroma/chroma; PERSIST_DIRECTORY is mandatory to
redirect it) and the per-collection-HNSW rationale (one store does not degrade
per-collection search).

Forward-links: ADR 0003 gains an "Amended by ADR 0017" row; ADR 0014 §2.5's
"ADR 0003 needs no amendment" sweep gets a dated forward note (its shared-
primitive / one-container classification is unaffected).

The code subs implement against this record: #12155 (rename+migrate), #12156
(defrag), #12157 (release-privacy), #12158 (guides).

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-29T01:26:58Z @tobiu closed this issue
- 2026-05-29T01:26:58Z @tobiu referenced in commit `a2385d8` - "fix(ai): KB release artifact is collection-scoped, never the whole .neo-ai-data (#12157) (#12160)

uploadKnowledgeBase.mjs previously zipped the entire `.neo-ai-data` tree
(private agent memories, the graph SQLite, the A2A mailbox) and
downloadKnowledgeBase.mjs extracted it over the consumer's data dir on
every `npm install` via the `prepare` hook — a latent privacy leak.

Both scripts now route through the canonical collection-scoped JSONL SDK
(`KB_DatabaseService.manageDatabaseBackup`), exactly as backup.mjs/restore.mjs
do:

- Upload exports ONLY the `neo-knowledge-base` collection to JSONL, stamps
  embeddingProvider + dimension provenance, and zips a flat artifact.
- Download is merge-only: skips if the consumer KB collection is already
  populated, imports collection-scoped via the SDK, and never directory-
  restores `.neo-ai-data` or touches a Memory Core collection.
- A shared `knowledgeBaseArtifact.mjs` SSOT holds the single canonical asset
  name (`chroma-neo-knowledge-base.zip`, reconciled with the published asset)
  and a defense-in-depth guard that fails on any Memory Core collection or
  `sqlite/` payload — enforced on both the build and consumer sides.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"

