---
id: 16694
title: Plain harness start self-supplies the fleet transport — zero bearer coordination
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-08-08T13:28:03Z'
updatedAt: '2026-08-09T16:52:18Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16694'
author: neo-fable-clio
commentsCount: 2
parentIssue: null
subIssues:
  - '[x] 16708 Harness UI-mode transport: self-supply mechanics + ownership/observation split'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Plain harness start self-supplies the fleet transport — zero bearer coordination

**Priority: HIGH (operator friction→gold, 2026-08-08).** First live operator run of the Electron harness, verbatim: *"1 command is a never ending process, so i can not add a second command into that terminal... 2 terminals: getting the same token is non-trivial. friction->gold => we can not ask other users to do this manually."*

## Context

The first Lane-B operator run (#14560 §04 arc) produced the empirical chain now on #16693: shell boots ✓, fleet transport runs ✓, auth enforced ✓ — cockpit offline, because the shell's process env carried no `NEO_FLEET_BEARER` (hash-verified: empty on the shell side, present on the server side). `harness/main.mjs:78` self-generated an ephemeral bearer; every renderer→IPC→main→HTTP request 401'd; the renderer's fail-closed path honestly showed the static roster. **The security design worked; the coordination model is the defect.** The interim recipe (one shared env export across processes) is a diagnosis artifact, not a product behavior.

## The Problem

`npm start` (UI-only) boots the window and spawns nothing. The fleet transport must be started externally, and the app↔transport handshake requires the SAME `NEO_FLEET_BEARER` in two independently-launched processes. A human cannot reasonably hand-carry a 43-char one-time secret between terminals — and a foreground dev server occupies its terminal, so the one-terminal flow does not exist either. The §04 PoC bar (@tobiu starts an agent from the UI) is unreachable while the handshake needs a hand-carried env var.

## The Architectural Reality

- `harness/main.mjs` already holds the resolved bearer (`main.mjs:78`, `resolveFleetBearer`) and already owns the complete fleet-spawn machinery on the brain path: child spawn with `env: {NEO_FLEET_BEARER: fleetBearerToken, NEO_FLEET_PORT}` plus `awaitFleetReady` (readiness = a real `listAgents` wire round-trip, never PID existence).
- `probeExistingFleetServer` (fleetLaunchContract) owns the reuse semantics: same-token+same-viewer probe → reuse; anything else is a named refusal.
- The quit path already drains "exactly the child tree this harness owns" — the ownership set exists.
- The packaged product profile already boots its own transport; UI-only checkout start is the ONLY topology demanding manual coordination.

## The Fix

In UI-only mode (`npm start`, no brain leg), the shell self-supplies its transport:

1. Probe `:8083` (or `NEO_FLEET_PORT`) via `probeExistingFleetServer` with the shell's bearer: **same-bearer server → reuse** (a dev's own coordinated transport keeps working).
2. **Port free → spawn** `devFleetServer` as an owned child with the shell's bearer + port env (the brain path's exact spawn shape), await fleet readiness, register in the owned-children set so the existing quit drain tears it down.
3. **Foreign/mismatched listener → do NOT adopt, do NOT exit**: the window boots to the current honest offline state with the probe's named refusal logged — a UI must not brick because a foreign process squats the port (contrast: the brain path's fail-closed exit stays correct for organism boot).
4. `NEO_HARNESS_SMOKE=1` keeps UI-only legs spawn-free (the smoke's isolation contract — a dev machine may carry a live transport; the fleet leg's evidence lives in `smoke:brain` and the live run).

## Acceptance Criteria

*(Restructured 2026-08-08 during PR #16696 review cycle 2, per the reviewer's close-target rule: the pre-merge-provable mechanics moved to the sub-leaf **#16708** (the PR's `Resolves` target, carrying the Contract Ledger for the delivered surfaces); THIS umbrella stays open until its live receipt lands. The cycle-1 AC-split annotation is preserved below in spirit — the amendment history is in the PR thread.)*

- [ ] **→ #16708 (sub-leaf, delivered by PR #16696):** transport self-supply mechanics — three-outcome composition witnessed, ownership≠observation split witnessed, live spawn receipt, quit drain, smoke spawn-free.
- [ ] **This umbrella's remaining AC (operator-run class):** the renderer-side receipt — the cockpit renders the LIVE roster over the wire in the one-command flow on the operator machine (lands with the operator's run and/or `#16699`'s auto-recheck instrumentation). Close this ticket only on that receipt.

## Out of Scope

- The brain legs' plane-aware ATTACH-OR-OWN re-triage — #16693 owns it (this leaf is its UI-only sibling, split per the #16693 scope note).
- Packaged-product profile (already self-supplying).
- Any bearer-prompt UX (`credentialProvider` surface) — the point is needing none.

## Avoided Traps

- **Exiting the shell on a foreign listener** (the brain path's semantics) — correct for organism boot, wrong for a UI window; the honest-offline fallback is the existing, tested behavior.
- **A root npm wrapper script exporting one env for both processes** — batches the friction instead of removing it; the packaged app (Finder double-click) could never use it.
- **Auto-spawn in smoke mode** — violates the smoke isolation contract on dev machines with live transports.

## Related

- #16693 (sibling: brain-leg plane-aware attach; carries today's empirical receipt) · #14560 Lane B (the operator run) · #13015 (transport ownership) · #15798 (topology context).

Creation-gate record: live latest-10 sweep 2026-08-08T13:27Z — #16693 is the adjacent sibling, split per its own scope note; no equivalent open ticket. Self-assigning; implementation starts immediately (operator-prioritized).

Origin Session ID: 9b6352cd-0db2-4a10-8254-36e710c67e1d
Retrieval Hint: "harness UI-only start self-supplies fleet transport bearer zero coordination"



## Timeline

- 2026-08-08T13:28:04Z @neo-fable-clio added the `enhancement` label
- 2026-08-08T13:28:04Z @neo-fable-clio added the `ai` label
- 2026-08-08T13:28:05Z @neo-fable-clio added the `architecture` label
- 2026-08-08T13:28:10Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-08-08T13:35:22Z @neo-fable-clio cross-referenced by PR #16696
- 2026-08-08T13:35:36Z @neo-fable-clio cross-referenced by #16693
- 2026-08-08T13:41:43Z @neo-fable-clio cross-referenced by #16699
- 2026-08-08T14:15:04Z @neo-fable-clio referenced in commit `23081cc` - "fix(harness): teardown ownership splits from Brain observation — UI transport is drain-owned, never a Brain claim (#16694)"
- 2026-08-08T14:38:43Z @neo-fable-clio cross-referenced by #16708
- 2026-08-08T14:39:05Z @neo-fable-clio added sub-issue #16708
- 2026-08-08T14:40:38Z @neo-fable-clio referenced in commit `3ad92a5` - "test(harness): the UI transport owner composition becomes witnessable; #16708 close-target split (#16694)"
- 2026-08-08T14:55:42Z @tobiu referenced in commit `0a675e2` - "feat: plain harness start self-supplies the fleet transport (#16694) (#16696)

* feat(harness): plain start self-supplies the fleet transport — zero bearer coordination (#16694)

* fix(harness): teardown ownership splits from Brain observation — UI transport is drain-owned, never a Brain claim (#16694)

* test(harness): the UI transport owner composition becomes witnessable; #16708 close-target split (#16694)"
- 2026-08-08T15:24:40Z @neo-fable-clio cross-referenced by PR #16711
- 2026-08-08T16:59:00Z @neo-fable-clio cross-referenced by PR #16721
- 2026-08-08T19:57:16Z @neo-fable-clio cross-referenced by #16746
### @neo-fable-clio - 2026-08-09T10:59:27Z

## [live-receipt — renderer side, agent-run class] The cockpit renders the LIVE roster over the wire; my own card is on it

Operator-goal receipt (2026-08-09 ~10:57Z, operator machine, this checkout at `dev`):

**Composition** — webpack dev server (`:8080`, harness-launched) + `devFleetServer` in **plane mode** (`NEO_FLEET_PLANE_BASE=http://127.0.0.1:3102`): boot log verified `mailbox/compose/catch-up seams bound to the containerized plane … (viewer @neo-fable-clio verified plane-side; host graph not consulted)` and `authenticated app<->fleet transport listening on http://127.0.0.1:8083/fleet (viewer: @neo-fable-clio, bearer: supplied)`. The single-viewer invariant held end-to-end: my plane bearer's server-resolved subject IS the boot viewer.

**Injection** — Neural Link attach to the running cockpit (session `998d9845`, runtime freshness current), then `ViewportController.wireFleetBridge({url, bearerToken})` per the worker-realm injector seam — the launch-contract bearer handed in memory, never URL/file/log. Ack `true`; `rosterSourceMode: 'selected'`.

**Wire truth** — `FleetCockpit` reached `gridAdapterState: 'live'`, `rosterWired: true`, `gridDegradedReason: null`, poll generation advancing (13+ reads). Empty registry first rendered the TRUE ZERO state (`server connected · fleet registry empty — define agents to go live` — the honest banner, not sample data). Then `defineAgent {githubUsername: neo-fable-clio, harnessType: claude-code}` over the authenticated transport (credential-free public definition) → next poll → **`FLEET · 1 AGENTS`: the @neo-fable-clio card renders with avatar, `all sources nominal`, launchable control.**

**Fail-honest residuals, named by the surface itself:**
- Card state `benched / offline`: runtime axis honestly `off` (Fleet does not own my process — I launched myself), wake axis `unknown` with reason `delivery-lane liveness is not exposed by the containerized plane yet` (the boot log's `delivery axes honest-unknown pending a plane vouching surface`). My graph presence IS fresh (`record_turn_presence` receipt `41a0e0e6`, fresh until 11:23Z) — the roster's online-light awaits the plane vouching surface, which is named work, not a defect here.
- Activity/mailbox seam degraded live: `a2a: plane list_messages failed` — the running plane (`92c0a49fda`) predates the #16773 `list_messages` convergence fix (merged to dev after the rebuild). The degradation banner + last-known data + stale marker rendered exactly as designed. Redeploy timing is the plane owners' call.

**Umbrella disposition:** this receipt proves the renderer side live over the wire on the operator machine — but via the NL-injection flow, not the one-command flow this ticket exists to deliver for the browser/harness path. The remaining AC (operator-run class, one-command, zero bearer coordination) stays OPEN and is the close target.

— Clio (@neo-fable-clio; Fable 5, Claude Code) 📜 · Session `7b51208b-bfd4-4372-94c6-49f6242e709d`

- 2026-08-09T11:11:32Z @neo-fable-clio cross-referenced by #16744
- 2026-08-09T11:55:52Z @neo-fable-clio cross-referenced by #16737
- 2026-08-09T12:29:06Z @neo-fable-clio cross-referenced by PR #16781
- 2026-08-09T12:36:36Z @neo-fable-clio cross-referenced by #16741
### @neo-fable-clio - 2026-08-09T16:52:18Z

## [SUNSET HANDOVER → successor session] Mission: finish the FM live-data roster TODAY — exact state, restart recipe, three candidate finish-lines

**Operator directive at sunset (2026-08-09 ~18:50 local):** the FM live-data roster must be FINISHED today; the prior session (mine) hit context exhaustion. This comment is the cold-pickup ramp.

### TRUE current state (verify, don't trust)

The roster ran LIVE today — 9 defined agents with real presence bands from the containerized plane (receipts upthread: IC…N80VQw + the 9-agent screenshot beat) — **but the composition was SESSION-BOUND and is dead now**: the `devFleetServer` ran as my session's background process, webpack as my preview server, and the bearer was NL-injected into an App Worker that has since been abandoned. What SURVIVES: (1) the **9 agent definitions** in the host fleet registry (`AiConfig.fleet.dataDir`/registry.json — disk, session-independent; all credential-free/display-only), (2) all merged code on dev (S1 compose, S3 presence axis `55219f40d8`, S4 witness), (3) the canonical plane at `55219f40d8` with the **fleet profile at first light** (Emmy's rebuild — composed `fleet-server` running, its own Fleet volume EMPTY).

### The restart recipe (15 min to re-live, proven twice today)

1. Ports probe: `lsof -nP -iTCP:8083 -sTCP:LISTEN` + 8080 (foreign-server trap).
2. Webpack via `.claude/launch.json` `neo-dev-server` (preview_start; autoPort lands 8080).
3. Fresh transport bearer (generate via `ai/mcp/server/shared/helpers/localBearer.mjs`, 600-perm scratch file, reference-only in commands) → `NEO_FLEET_BEARER="$(cat …)" NEO_FLEET_PLANE_BASE="http://127.0.0.1:3102" NEO_FLEET_PLANE_BEARER="$NEO_MCP_REMOTE_TOKEN" node ai/services/fleet/devFleetServer.mjs` (bg). Boot log MUST say `viewer @neo-fable-clio verified plane-side`.
4. Open `apps/agentos/index.html`, NL attach (`healthcheck` → sessionId), `find_instances` ViewportController → `call_method wireFleetBridge({url: 'http://127.0.0.1:8083/fleet', bearerToken})`.
5. `record_turn_presence start` → poll → cards carry bands.

### The three candidate finish-lines (ask the operator which, or default to A)

- **A — the one-command flow (THIS ticket's remaining AC):** `npm run cockpit` boots both processes, but the browser bearer hand-off still rides the NL/Electron seam — closing THAT gap (launcher→page bearer hand-off without an agent, e.g. a one-time loopback handshake endpoint the launcher serves and the page fetches same-origin, or the Electron path #16708 already delivered) = the zero-coordination AC delivered. Design constraint: never a URL param, never persisted (see `installFleetBridge` contract).
- **B — the composed cutover (C2, `#16743`, Emmy claimed):** cockpit dials the plane's composed fleet-server + PAT admission; definitions must land in the COMPOSED registry (its volume is empty — re-run the 9 tokenless `defineAgent` calls against it). If Emmy lands C2 today, THIS is the canonical finish.
- **C — the demo-stable middle:** recipe above + a `launch.json` cockpit entry so the operator can front the live roster on demand — weakest, only if time runs out.

### Warnings / do-NOT-pick-up

- **`#16812` (my PR, review pending):** memory-core response-budget fix — CI was pending at sunset; if RC arrives, the author loop is the successor's ONLY inherited non-FM duty. Do NOT start new non-FM lanes — today's operator correction: FM first.
- **MC mailbox/save flakiness:** long `add_memory` payloads LOSE (size-correlated, evidence on `#16808`) — keep saves compact; verify sends via outbox before any resend; unread-counter inflated (~410) — never bulk mark_read (`#16541` evidence).
- **Fold-gated:** the S2/S4/S3 edge repair waits on Emmy's D#16764 fold — check before touching `#16736`/`#16738`/`#16739` edges.
- **Kimi seats** (Iris S7, Phoebe) return ~18:20 local; Grace owns stability (route findings to `#16706`); the FM guide `#16798` is open (backstop lane, not today's priority).

— Clio (@neo-fable-clio; Fable 5, Claude Code) 📜 · Sunset of session `7b51208b-bfd4-4372-94c6-49f6242e709d`


