---
number: 17530
title: 'feat(agentos): add zero-residue extraction inventory (#17525)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-08-22T00:15:59Z'
updatedAt: '2026-08-22T00:23:51Z'
closedAt: null
mergedAt: null
head: codex/17525-agentos-extraction-inventory
base: dev
url: 'https://github.com/neomjs/neo/pull/17530'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17525

Related: #17500

The first blocking AgentOS extraction inventory is now executable rather than prose: one read-only diagnostic composes the existing launch-root closure and durable-plane-opener authorities, reconciles them against an exact-identity registry, and refuses either missing authority or stale authority. It inventories 160 script modules, 116 root commands, 69 workflow occurrences, 63 durable-plane opener concerns, 18 config authorities, 12 unresolved closure edges, the one current subprocess launch, and the explicit wave-one Engine custody boundaries. Every row has one of `edge`, `cloud`, `shared`, `stays-engine`, or `retire`; both residue directions are zero at `ae21f730015b7483aeafd0447a273380e62947c0`.

Evidence: L1 achieved (clean exact-head source census + SHA-bound CLI receipt with zero bidirectional residue) → L1 required (Epic inventory covenant). The 11-case unit suite adds mutation controls for missing/stale/duplicate/invalid authority, command-target conflict, dirty-SHA refusal, workflow occurrence shape, and AST subprocess discovery.

## Deltas from ticket

- Live `origin/dev` moved during filing: the baseline changed 157→159 scripts, 115→116 root commands, and 73→74 launch roots before implementation began. The registry includes the diagnostic's own governed row, so the exact PR-head script population is 160.
- Final custody never follows current closure implicitly. All 160 script modules, all 116 root commands, all 69 workflow occurrences, and the subprocess launch are explicit registry identities; closure/target shape is evidence and conflict checking only.
- Current orchestrator `shared-primitive` maps to Cloud, not C′'s conditional pure `shared/` package. Only one independently verified pure cross-plane helper is admitted as `shared`.
- A dirty checkout cannot publish a commit-SHA receipt. Library tests may opt into dirty derivation, but the CLI fails until the source is clean.

## Lifecycle / Sunset

The diagnostic and registry are migration substrate, not a permanent fourth authority. Their own rows are `retire`: after plane manifests and the permanent membership/closure/denial gate own the moved population, and the Engine deletion/cut receipt consumes the final inventory, this diagnostic, registry, and focused spec delete in that cut series. No root `package.json` script was added.

## Test Evidence

- `node ai/scripts/diagnostics/agentOsExtractionInventory.mjs` at `ae21f730015b7483aeafd0447a273380e62947c0` → **OK**, 160 / 116 / 69 / 63 / 18 / 12 / 1 populations, `disk - authority: 0`, `authority - disk: 0`.
- `npm run test-unit -- test/playwright/unit/ai/scripts/diagnostics/agentOsExtractionInventory.spec.mjs` → **11 passed**.
- `npm run agent-preflight -- --no-fix --change-class capability ...` → **all requested gates passed**; the existing machine-local `ai/config.mjs` stale-overlay warning remains out of scope.
- Full-suite probe (not claimed green): **14,442 passed / 34 failed** on the live maintainer machine. Failures span host daemon/deploy state; one was independently reproduced as `check-retired-primitives` scanning ignored `ai/deploy/.neo-ai-data/backups` and matching archived fixture prose (captured via the defect channel). The touched focused suite is green; clean-runner CI remains the full-suite gate.

## Post-Merge Validation

None owed: this is a static current-head proof. Before any relocation leaf starts, the exact PR-head receipt is linked on the parent Epic.

## Signal Ledger

| Family | Signal | Anchor | Disposition |
|---|---|---|---|
| GPT / author | `[AUTHOR_SIGNAL]` by `@neo-gpt-emmy` | [D#17489 author signal](https://github.com/orgs/neomjs/discussions/17489#discussioncomment-18110122) | current |
| Claude / non-author | `[GRADUATION_APPROVED]` by `@neo-opus-vega` | [Epic review `IC_kwDODSospM8AAAABQHA4IQ`](https://github.com/neomjs/neo/issues/17500#issuecomment-5376063521) against corrected `DC_kwDODSospM4BFFq_` | current; timing/root correction explicitly revalidated |

## Unresolved Dissent

No architectural dissent remains on the corrected first-wave authority. Repository naming remains unresolved, but this inventory uses topology roles rather than a repository name and freezes none.

## Unresolved Liveness

- Kimi re-poll at `2026-08-22T00:14:39Z`: Phoebe and Iris are roster-active but dark; no current signal is inferred.
- Gemini re-poll at `2026-08-22T00:14:39Z`: `@neo-gemini-pro` remains operator-benched.

No-signal is liveness, never consent.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session bbd4f722-ca03-4269-a88e-29555b12b9f9.
