---
id: 14668
title: 'Perspective restore into changed topology: reconciliation + the fail-closed contract'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-grace
createdAt: '2026-07-04T03:49:50Z'
updatedAt: '2026-07-10T05:58:52Z'
githubUrl: 'https://github.com/neomjs/neo/issues/14668'
author: neo-fable-clio
commentsCount: 3
parentIssue: 13158
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-10T05:58:52Z'
---
# Perspective restore into changed topology: reconciliation + the fail-closed contract

## Context
Tree line **B5** (#13158 map) — the HARD half of restore (fable-tier in the triage): applying a captured perspective when the window topology CHANGED (fewer/more windows than capture time). ADR 0029 §2.2's restore-semantics table governs; #14653 owns the unchanged-topology case.

## The Problem
Changed-topology restore is where designs lie: silently dropping panes, spawning surprise windows, or half-applying are all worse than refusing. §2.2's contract: deterministic reconciliation for the coverable subset + an explicit, RENDERED fail-closed result for what cannot restore (the Demo-B scene G4 shows this honestly — it is a feature, not an apology).

## The Architectural Reality
Reconciliation = mapping captured window-slots onto live windows by shape affinity (never ids), then per-window #14653 restore plans; the uncoverable remainder returns a structured `unrestored` set (items + reason classes). No window CREATION in v1 (spawning is a §2.3/#13028-boundary decision — out of scope, documented). Blocked_by #14653 + B3.

## The Fix
One PR: the slot-mapping reconciler (pure) + composed restore + the fail-closed result shape + specs (shrunk topology: best-coverage mapping + unrestored set; grown topology: no surprise spawns; determinism).

## Acceptance Criteria
- [ ] Slot mapping is deterministic + id-free (spec).
- [ ] Unrestored set carries reason classes; NOTHING silently drops (spec: item count conservation — restored + unrestored = captured).
- [ ] No window creation in v1 (review-checkable).
- [ ] Composes #14653 per-window (no duplicate restore logic).
- [ ] Cross-family review.

## Out of Scope
Window spawning · unchanged-topology path (#14653) · UI rendering of the fail-closed result (Demo-B G4 + B7 consume the shape).

## Related
Parent #13158 (line B5) · blocked_by #14653 + B3 leaf · authority ADR 0029 §2.2 · consumed by #14590 G4. Sweeps: 03:47Z clean.
Origin Session ID: fa2a6fd5-7488-4af6-a0d2-3855c86003e4
Retrieval Hint: "perspective restore changed topology reconciliation fail closed unrestored"

## Timeline

- 2026-07-04T03:49:51Z @neo-fable-clio added the `enhancement` label
- 2026-07-04T03:49:51Z @neo-fable-clio added the `ai` label
- 2026-07-04T03:49:51Z @neo-fable-clio added the `architecture` label
- 2026-07-04T03:52:23Z @neo-fable-clio added parent issue #13158
- 2026-07-04T04:40:40Z @neo-fable-clio cross-referenced by PR #14695
- 2026-07-04T04:44:25Z @neo-fable-clio cross-referenced by PR #14697
- 2026-07-04T04:48:30Z @neo-fable-clio cross-referenced by #14667
- 2026-07-04T04:51:32Z @neo-fable-clio cross-referenced by PR #14698
- 2026-07-04T07:15:02Z @neo-fable-clio cross-referenced by #14652
### @neo-fable-clio - 2026-07-04T13:55:28Z

## [spec-transfer — the slot-reconciliation half, sole-context dump] Changed-topology restore: how captured slots map onto a different window set

Final pre-shutdown transfer (the `#14653` planner comment marks the fingerprint gate as this leaf's boundary; here is the algorithm on the far side of that gate — everything else about this leaf is already in the body):

**Inputs:** captured envelope (`captureScope: 'topology'`, `windowDocuments` slot-map `{slotId → document}`, `windowFingerprint` = the slot-ordered `w[t|t]` composition) + the CURRENT topology (live window set + per-window documents).

**Step 1 — slot matching is shape-greedy, never id-based:** compute each current window's `dockShape.v1` term and each captured slot's term (both via `computeShapeFingerprint` on the persisted trees — the coherence rule). Match in three passes: (1) exact shape-term equality (stable-sort both sides, pair in order — deterministic on ties); (2) same top-level orientation + tab-count within ±1 (the "close enough to restore INTO" tier — the per-window `#14653` planner closes the residual gap); (3) unmatched. Never match on windowId — ids are runtime-only by the ADR-0029 §2.1 table, and captures deliberately carry none.

**Step 2 — the three outcome classes (all structured, none silent):**
- **Matched pairs** → per-window `#14653` planner (fingerprint gate per-pair: exact-match pairs skip straight to apply; tier-2 pairs run the full plan).
- **Captured-but-unmatched slots** (fewer windows now) → `unrestored: [{slotId, document, reason: 'no-candidate-window'}]` — the caller decides (spawn a window via the landed `#13446` ops, or surface the choice); this leaf does NOT auto-spawn windows (§2.1 boundary — window creation is an embodiment act, not a restore side-effect).
- **Current-but-unmatched windows** (more windows now) → `untouched: [windowRef…]` — restore never mutates a window the capture doesn't describe (object permanence; the dual of `#14653`'s `surplus`).

**Step 3 — degenerate identity (regression-pin this):** a topology capture of ONE window must restore byte-equivalently to the single-window path — `topo([doc]) ≡ win(doc)` modulo scope+fingerprint schema fields (the landed spec already pins the capture side; pin the restore side the same way).

**Result shape:** `{restored: [{slotId, windowRef, applied, errors}], unrestored, untouched, errors}` — fail-closed per-window (one window's failure never rolls back another's success; report, don't cascade — multi-window atomicity is explicitly NOT promised by the ADR and faking it would require cross-window transactions that don't exist).

**Spec recipe:** same-shape 2-window round-trip · shrunk topology (2→1: one restored + one unrestored) · grown topology (1→2: one restored + one untouched) · tier-2 near-match restores through the planner · degenerate identity · per-window fault isolation (forced failure in window A, window B still restores).

First-claim wins; `#14653` (or its claimant) is the natural sequel-owner.

— Clio (Claude Fable 5, Claude Code) · Session fa2a6fd5-7488-4af6-a0d2-3855c86003e4

- 2026-07-04T17:26:32Z @neo-gpt cross-referenced by #14773
- 2026-07-04T23:02:45Z @neo-gpt cross-referenced by #14649
- 2026-07-04T23:18:28Z @neo-gpt added the `not-code-ready` label
- 2026-07-04T23:18:28Z @neo-gpt added the `needs-design` label
- 2026-07-05T20:51:43Z @neo-opus-grace cross-referenced by PR #14871
- 2026-07-05T20:52:04Z @neo-opus-grace cross-referenced by #14653
- 2026-07-06T12:49:32Z @neo-gpt removed the `not-code-ready` label
- 2026-07-06T12:49:32Z @neo-gpt removed the `needs-design` label
- 2026-07-10T03:37:06Z @neo-opus-grace assigned to @neo-opus-grace
### @neo-opus-grace - 2026-07-10T03:38:06Z

## Intake + design contract (claimed; blockers live-verified CLOSED: #14653 shipped via PR #14871, B3 = #14667 closed)

Branch `agent/14668-changed-topology-restore` cut from dev. Design pinned to ADR-0029 §2.2's changed-topology table before implementation:

**Shape — a pure reconciler module (planner-sibling statics), composing, never duplicating:**

1. `mapWorkspaceSlots(captured, live)` — captured workspace-slots ↔ live windows by SHAPE AFFINITY, id-free per the AC: affinity = item-catalog overlap (Jaccard over `dockItemId` sets) with structural tie-breakers (node-type multiset), ties broken by stable captured-order — deterministic by construction, spec-pinned with permuted inputs.
2. Coverable subset → per-window plans through the LANDED `DockRestorePlanner.restoreToward(current, captured)` (#14653's primitive — zero duplicate restore logic, the composition AC).
3. `unrestored` result set with reason classes (enum proposal, review-checkable): `no-live-window` (shrunk topology), `validation-failed` (§2.2 row 1 — any failure fails the WHOLE restore closed, untouched), `unmapped-slot` (affinity below floor). Conservation invariant spec: `restored + unrestored === captured` item counts, no silent drops.
4. §2.2 row 3 honored structurally: NO window creation paths exist in the module (grown-topology spec asserts zero spawn calls; excess live windows' documents untouched per row 4).

**Specs planned:** shrunk topology best-coverage mapping + reasoned remainder · grown topology no-spawn + excess-untouched · determinism under input permutation · fail-closed validation (whole-restore atomicity) · composition proof (planner invoked per mapped window, plan shapes equal #14653's).

Implementation next block — the contract above is the review surface; drift from it gets called out, not absorbed.

*Process note: Fable 5 window (tail). Investigation branch for #14911 and PR #14923 are independent lanes.* — Grace

- 2026-07-10T03:45:43Z @neo-opus-grace cross-referenced by PR #14925
### @neo-opus-grace - 2026-07-10T03:45:56Z

**Contract amendment (flagged per the posted contract's own rule) + delivery:** implementation shipped as PR #14925 matching the design contract above, with ONE addition — a `displaced` result set (`[{itemId, liveIndex}]`): the planner's shape-mismatch deferral routes those pairs to wholesale adoption, and live-only items absent from the adopted document must be REPORTED, not silently dropped — the ticket's no-silent-drop principle applied to the live side too. Reason-class enum shipped exactly as proposed; conservation invariant asserted in all six specs; 202/202 dashboard suite. — Grace (Fable 5 window)

- 2026-07-10T04:30:59Z @neo-opus-grace referenced in commit `b4796cc` - "fix(dashboard): reconciler cycle-1 — envelope authority, optimal assignment, reason-true branching (#14668)"
- 2026-07-10T05:08:47Z @neo-opus-grace referenced in commit `72da282` - "fix(dashboard): reconciler cycle-2 — total slot validation, content-stable ties, complete-output uniqueness (#14668)"
- 2026-07-10T05:26:34Z @neo-opus-grace cross-referenced by PR #14936
- 2026-07-10T05:57:07Z @neo-gpt cross-referenced by #14945
- 2026-07-10T05:58:52Z @tobiu referenced in commit `a916bca` - "feat(dashboard): changed-topology restore reconciler — affinity mapping + fail-closed contract (#14668) (#14925)

* feat(dashboard): changed-topology restore reconciler — affinity mapping + fail-closed contract (#14668)

* fix(dashboard): reconciler cycle-1 — envelope authority, optimal assignment, reason-true branching (#14668)

* fix(dashboard): reconciler cycle-2 — total slot validation, content-stable ties, complete-output uniqueness (#14668)"
- 2026-07-10T05:58:52Z @tobiu closed this issue

