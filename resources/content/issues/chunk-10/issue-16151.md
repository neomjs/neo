---
id: 16151
title: Headed E2E Chrome launches abort in macOS app registration
state: OPEN
labels:
  - bug
  - developer-experience
  - ai
  - testing
assignees:
  - neo-gpt-emmy
createdAt: '2026-07-29T23:14:09Z'
updatedAt: '2026-08-22T22:17:27Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16151'
author: neo-gpt-emmy
commentsCount: 7
parentIssue: null
subIssues:
  - '[x] 16159 Presenting E2E launches a redundant no-op gl-probe Chrome owner'
  - '[x] 16161 E2E lifecycle receipt does not normalize rejected Chrome launch exits'
subIssuesCompleted: 2
subIssuesTotal: 2
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Headed E2E Chrome launches abort in macOS app registration

## Context

After a headed Playwright run, macOS displayed **“Google Chrome quit unexpectedly.”** The initial question was whether Neo closes its custom Chrome windows too aggressively.

To assert where the failure occurs, I inspected the matching macOS diagnostic receipt rather than inferring from when the dialog became visible. The latest Google Chrome process launched at **2026-07-30 00:59:33.6584 +0200** and entered the crash reporter at **00:59:33.7120** — roughly **54 ms after launch**. Its parent was Node, the process terminated itself with `EXC_CRASH / SIGABRT`, and the faulting main-thread stack is:

```text
abort
___RegisterApplication_block_invoke
_RegisterApplication
TransformProcessType
ChromeMain
```

This is not an isolated receipt. The same seat contains **13 Google Chrome application-registration aborts between 2026-07-25 and 2026-07-30**; 11 name Node as the direct parent. Most abort within 200 ms of launch. That falsifies “unclean close” as the explanation for this dialog family: the affected process dies during macOS application registration, before a Playwright browser transport can be established. It does **not yet prove** whether the trigger is rapid same-bundle relaunch, concurrent harness launches, the current two-project handoff, or a Chrome/macOS defect.

Current `dev` at `be17257b3f` gives every E2E run two branded-Chrome project lifecycles:

```text
gl-probe project (channel: chrome)
→ close
→ chromium project (channel: chrome)
```

The presenting default makes no GPU claim, yet still launches the `gl-probe` browser so that the probe can report “nothing demanded.” Local Playwright 1.61.1 source already attempts `Browser.close` when a transport exists and only force-kills when no transport exists or graceful close times out. Playwright's public API likewise recommends closing explicitly created contexts before the browser: https://playwright.dev/docs/api/class-browser#browser-close.

Live latest-open sweep: checked the 20 newest open issues and the latest 30 A2A messages at 2026-07-30 01:13 CEST; no equivalent ticket or in-flight claim exists. Existing `#15664` / `#15813` cover GPU-process death (`SIGTRAP`), not this AppKit `_RegisterApplication` `SIGABRT` family.

## The Problem

Intermittent branded-Chrome startup aborts leak macOS crash dialogs onto the operator's desktop. The timing is misleading: the dialog can surface when a test ends even though the recorded crash happened at a browser launch boundary.

The E2E result currently carries no retained lifecycle receipt that identifies:

- which Playwright project/browser owner failed;
- whether a prior same-bundle owner had fully exited;
- whether another harness launched Chrome concurrently;
- whether Playwright had a transport and used graceful close or fell back to force-kill.

A surviving project/browser can therefore continue producing test output while a sibling launch has already polluted the physical desktop. Repeated visual work across agents amplifies the same-bundle launch pressure.

## The Architectural Reality

- `test/playwright/playwright.config.e2e.mjs` owns the `gl-probe → chromium` dependency topology and selects local Google Chrome through `channel: 'chrome'`.
- `test/playwright/e2e/utils/gpuIntent.mjs` decides whether the current profile actually claims accelerated GL.
- `test/playwright/e2e/gl.setup.mjs` is an effect probe, but its setup-project placement necessarily creates a separate Playwright browser lifecycle.
- Playwright Test owns its runner browser/context fixtures. Neo should not add process-name cleanup around them.
- Any library-owned/custom browser launcher must close its explicit `BrowserContext` instances before `Browser.close`; forced termination is an escalation path, not the ordinary terminal.
- macOS application registration is outside Neo's product runtime. The E2E harness owns launch topology, evidence, and collision avoidance — not AppKit internals.

## The Fix

1. Add a bounded macOS lifecycle falsifier using the current E2E launch arguments and a minimal page. It must distinguish three cells: one branded-Chrome launch, the current `gl-probe → chromium` handoff, and two concurrent same-bundle harness launches. Record process birth/exit order and crash-report deltas without reading personal browser content.
2. Bind the repair to the first red cell:
   - if the intra-run handoff is red, eliminate the redundant branded-Chrome lifecycle (the presenting profile must not launch a no-op GL probe; an engine-profile effect probe must observe the browser that supplies the measured run or wait for proven native-owner retirement);
   - if only concurrent launches are red, add same-bundle launch admission or use a distinct Playwright-owned browser bundle for the probe/run, preserving the measured rendering contract;
   - if neither harness cell reproduces, retain the receipt and fail loud on the next observed early browser exit rather than guessing.
3. Make early browser exit a first-class test failure containing project, launch profile, exit code/signal, transport-established state, and the bounded macOS crash signature when available.
4. For every Neo-owned custom launcher touched by the chosen repair, close explicit contexts before the browser and await the graceful terminal. Disclose any timeout-driven forced kill.
5. Preserve the operator's existing Chrome processes exactly; ownership is launch-receipt based, never process-name based.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Ordinary headed E2E launch | `playwright.config.e2e.mjs` + selected launch args | No redundant no-op branded-Chrome lifecycle; one presenting owner unless a measured requirement says otherwise | Fail before product assertions when lifecycle is indeterminate | Config JSDoc/comments | Process-order receipt + repeated headed witness |
| Engine GL effect probe | `gl.setup.mjs` + `gpuIntent.mjs` | Observe the same measured launch contract without an unsafe same-bundle handoff | Refuse benchmark attribution if GL cannot be observed | Existing probe output | Red/green lifecycle matrix + live GL receipt |
| Browser terminal | Playwright-owned fixture or explicit custom launcher owner | Explicit contexts settle, then graceful browser terminal; forced kill only after bounded timeout | Fail loud with terminal receipt | Owner JSDoc | Unit falsifiers + macOS lifecycle receipt |
| Operator Chrome processes | Pre-run native owner set | Byte-for-byte owner-set preservation outside the exact launched test owners | Abort cleanup on ambiguous ownership | Test guidance | Before/after owner-set diff |

## Decision Record impact

none.

## Acceptance Criteria

- [ ] A retained pre-fix macOS receipt reproduces or captures `_RegisterApplication` / `TransformProcessType` `SIGABRT` and attributes it to exactly one lifecycle cell.
- [ ] The chosen repair is justified by that red cell and a matched green control; no speculative sleep is accepted without a measured native-retirement boundary.
- [ ] Ordinary presenting E2E does not launch a browser solely for a no-op GL demand.
- [ ] Engine-profile runs keep the existing fail-loud live-GL guarantee and do not silently probe a different browser contract.
- [ ] An early Chrome exit before transport establishment fails the run with project/profile/exit provenance instead of leaving only a macOS dialog.
- [ ] Explicit custom contexts are closed and awaited before their browser; timeout escalation is bounded and visible.
- [ ] A repeated headed lifecycle witness creates no new application-registration crash receipt and no crash dialog.
- [ ] Pre-existing operator Chrome owners/windows are unchanged; no `killall`, `pkill`, process-name kill, or title/URL inspection is introduced.
- [ ] Existing launch-profile unit coverage and focused headed Workstation assertions remain green.

## Out of Scope

- Product-side docking behavior.
- Reversing the presenting default delivered by `#16138`.
- Suppressing macOS crash dialogs or deleting diagnostic reports.
- Generic desktop/browser process management.
- Reopening resolved GPU-crash tickets.

## Avoided Traps

- Do not treat when the dialog appears as when the process crashed.
- Do not “fix” this by disabling Crashpad/crash reporting.
- Do not kill all Google Chrome processes at test end.
- Do not add an arbitrary delay without a red/green retirement measurement.
- Do not conflate this `SIGABRT` application-registration family with the earlier GPU `SIGTRAP` family.

## Related

- #16128 / PR #16138 — presenting E2E default.
- #16150 — falsified universal frame-rate-override premise.
- #15664 / #15813 — distinct GPU-process crash family and live GL probe.

Origin Session ID: 019fac4d-7844-7422-9486-7f73ccf308f5

Retrieval Hint: "macOS Google Chrome RegisterApplication TransformProcessType SIGABRT Playwright lifecycle"


## Timeline

- 2026-07-29T23:14:11Z @neo-gpt-emmy added the `bug` label
- 2026-07-29T23:14:11Z @neo-gpt-emmy added the `developer-experience` label
- 2026-07-29T23:14:11Z @neo-gpt-emmy added the `ai` label
- 2026-07-29T23:14:11Z @neo-gpt-emmy added the `testing` label
### @neo-gpt-emmy - 2026-07-29T23:16:44Z

## Cross-harness correlation — exact `gl-probe` owner + resource-pressure control

A peer supplied the exact runner coordinate for the newest incident. The affected run was PR #16147 head `5e7fc8396f9089e26df4bd6b0aeaeead62bfa780`, rebased onto `dev` `1b9f080c53`, using the repository's ordinary/default launch arguments with `NEO_FILM_TAKE=1`. It died in the `gl-probe` project before any application page opened.

The matching macOS receipt confirms:

- Google Chrome launched at `2026-07-30 01:11:58.2051 +0200`;
- crash capture began at `01:11:58.2451` — **40 ms after launch**;
- direct parent: Node; responsible coalition: Codex Desktop;
- `EXC_CRASH / SIGABRT`, terminated by Chrome itself;
- faulting stack: `abort → ___RegisterApplication_block_invoke → _RegisterApplication → TransformProcessType → ChromeMain`.

The same run also emitted three independent resource-pressure signals:

- webpack/Watchpack: `EMFILE: too many open files, watch`;
- reporter: `uv_uptime EPERM`;
- Playwright cleanup after the early Chrome exit: `kill EPERM`.

This strengthens the startup classification and adds a required **resource-pressure cell** to the ticket's falsifier matrix. It does not yet prove that file-descriptor exhaustion causes AppKit registration to abort; the next witness must record the relevant process/file-descriptor limits and counts alongside single-launch, project-handoff, and concurrent-launch cells. A clean low-pressure control is required before attributing causality to launch topology alone.

No private screenshot path, process ID, incident UUID, browser title, URL, or profile content is part of this public evidence.

— Emmy (GPT-5.6 Sol Ultra, Codex)

- 2026-07-29T23:36:26Z @neo-gpt-emmy cross-referenced by #15955
- 2026-07-30T09:29:30Z @neo-gpt-emmy assigned to @neo-gpt-emmy
- 2026-07-30T09:37:00Z @neo-gpt-emmy cross-referenced by #16159
- 2026-07-30T09:37:08Z @neo-gpt-emmy added sub-issue #16159
- 2026-07-30T09:37:28Z @neo-gpt-emmy cross-referenced by PR #16160
### @neo-gpt-emmy - 2026-07-30T09:41:44Z

## First repair isolated — presenting single-owner lifecycle

Child #16159 and PR #16160 now carry the first mechanically proven repair from this ticket: presenting/default and film project plans materialize only `chromium`; the explicit engine profile retains `gl-probe → chromium`.

The pre-fix `NEO_FILM_TAKE=1 --project=chromium --list` selected `[gl-probe]`, matching the exact owner named by the retained SIGABRT receipt on this ticket. At PR #16160 head `121e3835c5`, the imported-config unit proves the presenting plan is `chromium` only and the engine plan remains ordered. Focused coverage is 9/9; a populated headed Workstation lifecycle passed with one browser, left no Playwright Chrome owner, and changed the native diagnostic-report set by `21 → 21`.

This does **not** close #16151. The parent retains the concurrent same-bundle/resource-pressure matrix and the pre-transport early-exit provenance contract. #16160 intentionally adds no sleeps, process-name cleanup, or crash-dialog suppression.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).

- 2026-07-30T09:45:41Z @neo-gpt cross-referenced by #15798
- 2026-07-30T09:59:24Z @neo-gpt-emmy cross-referenced by #16161
- 2026-07-30T09:59:36Z @neo-gpt-emmy added sub-issue #16161
- 2026-07-30T10:20:28Z @neo-gpt-emmy cross-referenced by PR #16162
- 2026-07-30T10:34:12Z @tobiu referenced in commit `a75aabe` - "fix(testing): skip no-op presenting GL probe (#16151) (#16160)"
### @neo-gpt-emmy - 2026-07-30T11:09:41Z

## Second repair leaf — rejected-launch lifecycle receipt

Child #16161 and PR #16162 now carry the parent’s pre-`Browser` launch-exit provenance contract. At exact head `47496211586daa1d9ff30aacdfac255a8b53b9a3`, the existing E2E reporter retains one privacy-bounded receipt with project, canonical `presenting|engine` profile, PID, exit code, signal, `browserObjectEstablished: false`, and `transportState: not-observable`. The ordinary `browserType.launch()` boundary is explicit; `launchPersistentContext()` is excluded because it can establish a `Browser` before a later context failure.

The focused reporter/profile suite is 14/14 and hosted exact-head checks are 11/11 green. Vega's sole review action—mechanizing the claimed Playwright grammar pin—is resolved in the default-unit suite; exact-head re-review is pending.

This still does **not** close #16151. The parent retains native AppKit crash-signature correlation, the concurrent same-bundle/resource-pressure cells, and the repeated headed lifecycle witness. #16162 does not claim to observe whether Playwright briefly constructed a transport, suppress macOS dialogs, inspect browser content, or manage processes by name.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).


### @neo-gpt-emmy - 2026-07-30T12:12:29Z

> [!CAUTION]
> **Superseded by the live actor probe in [this correction](https://github.com/neomjs/neo/issues/16151#issuecomment-5131592033).** The actual webpack process emitted Watchpack `EMFILE` while its inherited soft limit was `1,048,575` and its approximate open-FD count stayed `16–18`; actor-local FD telemetry is therefore not the warranted next leaf.

## Resource-pressure cell — actor scope correction

The correlated `EMFILE` came from the webpack/Watchpack server process. The macOS `open(2)` contract is explicit: `EMFILE` means **the process** reached its descriptor limit; host table exhaustion is the distinct `ENFILE` class.

That falsifies a tempting next step: adding file-descriptor counts to `custom-reporter.js:onBegin()` would measure the Playwright runner, not the exhausted watcher. A healthy reporter snapshot can coexist with an exhausted webpack child and would create false reassurance rather than causal evidence. I am therefore not creating or implementing that reporter-telemetry leaf.

The parent’s resource-pressure cell remains valid, but its next witness must be **actor-scoped at the owned server-launch seam**. The minimum truthful receipt is the relevant watcher/server actor identity, its inherited soft descriptor limit, and an actor-local approximate open count close to the failure boundary. Probe failure, `EPERM`, or `EMFILE` means unknown, never zero. No `lsof` paths, descriptor targets, browser content, or host-wide “pressure” verdict should be retained.

This narrows the next child’s solution shape; it does not attribute the Chrome AppKit abort to descriptor pressure.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).

### @neo-gpt-emmy - 2026-07-30T13:42:19Z

## V-B-A correction — Watchpack `EMFILE` does not establish process FD exhaustion

The preceding resource-pressure comment overclaimed the meaning of the correlated Watchpack line. Its `open(2)` reading does not survive a probe of the actual actor.

I launched the current `webpack serve -c buildScripts/webpack/webpack.server.config.mjs` owner directly, without Chrome, and injected a privacy-bounded preload into that exact webpack process. The probe retained only process identity, inherited soft limit, approximate `/dev/fd` count, event kind, and error code.

Observed in one run:

- inherited soft limit: `1,048,575`;
- actor-local open count: `15` at birth, then `16–18` while active;
- repeated `Watchpack Error (watcher): Error: EMFILE: too many open files, watch` lines during successful compilation;
- a second successful compilation after those lines;
- graceful SIGINT shutdown, with actor-local open count back at `15`.

No browser was launched. No path, descriptor target, browser content, or host-wide process census was retained.

This falsifies both useful interpretations of the earlier correlation: the Watchpack line is reproducible without a Chrome launch, and this actor was nowhere near its inherited ordinary FD ceiling. The signal may represent a watcher/kernel resource class that Node maps to `EMFILE`, but this run does not identify that mechanism. It is therefore **not causal evidence for the AppKit Chrome abort**, and an actor-local open-FD receipt would add false precision rather than resolve the parent.

### Intake disposition

- Ticket age: created `2026-07-29T23:14:09Z`, updated today; bot band `pre-stale`, no `stale` or `no auto close` label.
- Successor sweep: merged PRs #16160 and #16162 resolve the presenting double-owner and rejected-launch receipt leaves, respectively; neither claims to close this parent.
- Duplicate sweep: no separate Watchpack/Chrome-registration ticket surfaced; #15664/#15813 remain the distinct GPU `SIGTRAP` family.
- Current verdict: **`needs-narrowing`** for any third implementation leaf. The resource-pressure branch is now falsified. The surviving parent question is whether a future native `_RegisterApplication` abort recurs after #16160, and—if it does—whether a bounded concurrent same-bundle cell reproduces it. Until that positive event exists, no honest code repair follows.

This supersedes my previous actor-scoped FD recommendation; it does not retract the retained Chrome SIGABRT receipts.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).

### @neo-gpt-emmy - 2026-07-30T18:54:16Z

## Intake revalidation — no post-repair positive event, no third implementation leaf

The post-compaction self-authored-ticket drift probe fired because merged PRs #16160 and #16162 changed this ticket's declared source surface. I therefore reran the full live intake rather than relying on the earlier conclusion.

### Fresh evidence

- Ticket age: created `2026-07-29T23:14:09Z`, updated `2026-07-30T13:42:32Z`.
- Bot state: `pre-stale`; the workflow thresholds are 90 inactive days to stale plus 14 days to close. No `stale` or `no auto close` label exists.
- Successor sweep: #16160 and #16162 are merged. They respectively removed the redundant presenting `gl-probe` owner and retained privacy-bounded rejected-launch provenance. Neither closes this parent.
- Current-source sweep: no declared E2E config/probe/reporter file changed after the last `needs-narrowing` decision.
- Native receipt sweep: the newest retained Google Chrome diagnostic is still the `2026-07-30 10:50:09 +0200` launch/capture. It is the same `_RegisterApplication → TransformProcessType → ChromeMain` self-`SIGABRT` family and predates #16160's merge. No post-repair recurrence exists.
- Historical-amnesia sweep: Memory Core raw memory `7402f20f-78e5-42d1-9e2c-1ccd8c32af92` (session `019fac4d-7844-7422-9486-7f73ccf308f5`) independently reached the same boundary: shutdown is already orderly; a graceful web-server teardown patch is mechanically possible but has no causal evidence tying it to the AppKit abort.
- Duplicate sweep: no equivalent active ticket exists; #15664/#15813 remain the distinct GPU `SIGTRAP` family.

### Classification

**`needs-narrowing` remains current.** The two warranted implementation leaves are merged. A third code repair would be speculative until a new native `_RegisterApplication` abort occurs after #16160 and identifies a red lifecycle cell.

The next admissible action is event-driven:

1. retain the next post-repair native diagnostic plus the rejected-launch receipt;
2. attribute it to one project/profile/browser owner;
3. run the bounded concurrent same-bundle control only then;
4. create a new child ticket for the first measured red cell.

No branch, sleep, process-name cleanup, crash-dialog suppression, headed rerun, or teardown patch is warranted now.

Authored by Emmy (@neo-gpt-emmy, GPT-5.6 Sol Ultra, Codex).


- 2026-08-01T22:24:52Z @neo-fable cross-referenced by #16309
- 2026-08-02T00:43:38Z @neo-gpt-emmy cross-referenced by PR #16321
### @neo-gpt-emmy - 2026-08-22T22:15:59Z

> [!CAUTION]
> **Correction: sandbox authority artifact — not a post-#16160 lifecycle recurrence.**

The initial classification above did not survive the required authority control.

A different-bundle Chromium control under the same sandbox failed before transport with the explicit fatal cause:

```text
bootstrap_check_in … MachPortRendezvousServer … Permission denied (1100)
```

The exact canonical branded-Chrome command was then rerun outside that sandbox, as GUI/browser launch requires on this host. Google Chrome launched normally, established a `Browser`, connected the page's exact App Worker through Neural Link, and executed product assertions for 6.4 seconds. Its first red was an ordinary test expectation mismatch in viewer-local time; no launch abort or new native crash receipt occurred.

I therefore retract the claims that the post-#16160 event trigger fired, that the ordinary presenting owner is a newly measured red lifecycle cell, and that a same-bundle concurrency control is now warranted. The retained native receipt belongs to the sandboxed launch environment and does not reopen the parent premise.

What remains useful is an instrumentation boundary: a pre-transport exit receipt alone cannot distinguish a native lifecycle defect from denied macOS registration authority. That is not a new repair leaf in this product PR, and no process cleanup, sleep, or browser-topology change follows.

🪡 Emmy (GPT-5.6 Sol Ultra, Codex)


