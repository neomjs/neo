---
id: 13818
title: REM consolidation-liveness watchdog — alarm on a stalled dream cycle
state: OPEN
labels:
  - enhancement
  - ai
  - architecture
  - model-experience
assignees:
  - neo-opus-vega
createdAt: '2026-06-21T20:41:23Z'
updatedAt: '2026-06-21T21:39:09Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13818'
author: neo-opus-vega
commentsCount: 1
parentIssue: 13624
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# REM consolidation-liveness watchdog — alarm on a stalled dream cycle

## Context

Live `get_rem_pipeline_state` this session: **undigested 316** (~stable from ~326 = not draining) + **`recentCycles: []`** (no REM cycle recorded) while the Golden Path handoff regenerated fresh (golden-path decoupled by #13783). The orchestrator is **alive but the consolidation half is silently stalled** — the operator's *"orchestrator does still not fully work."* #13551 (`embedDrainLivenessWatchdog`) watches the **embed-drain** WAL backlog exactly this way (in the recovery incident the embed-drain died silently and no alarm fired for ~8 days); **nothing watches the REM / consolidation cycle**. ADR 0023 AC-3 (consolidation-liveness: *observable, never assumed-green*) mandates closing this gap.

## The Problem

A stalled REM cycle produces **no alarm**: the forecast keeps regenerating (decoupled) while the graph rots — the *"green-but-rotting"* anti-pattern (ADR 0023 §5). `recentCycles: []` is invisible until a human notices stale routing. Process-alive ≠ consolidation-draining (the same lesson #13551 encodes for embed-drain).

## The Architectural Reality

Mirror `ai/daemons/orchestrator/scheduling/embedDrainLivenessWatchdog.mjs` (#13551) — **read-only, never-fail, one-shot-latch**, pure `getDueTask`:
- **Read primitive:** `ai/services/memory-core/helpers/remRunStateStore.mjs` — the last successful REM-cycle timestamp (the `recentCycles` source) + the undigested backlog count (`get_rem_pipeline_state` axes).
- **Stall signal (mirrors embed-drain's `oldestAgeMs > thresholdMs`):** *time-since-last-successful-REM-cycle > threshold* **AND** *undigested backlog > 0*. Fail-soft (a read fault → no alarm, never a false stall, never a throw into the pipeline).
- **Register** in `registry.mjs` as a read-only `health-check` with `backpressure: 'none'` + empty `dependencies` (the #13551 precedent — never takes a heavy lease).
- **Config leaves** (`remConsolidationWatchdogCheckMs`, staleness threshold) per ADR 0019.
- **One-shot latch:** alarm on stall-onset, latched, cleared on a healthy check.

## Decision Record impact

`aligned-with` ADR 0023 (AC-3 consolidation-liveness — this is the observability execution). Sub of #13624 (orchestrator heavy-maintenance backlog doesn't drain — the observability angle; "peers pick up the angles").

## Acceptance Criteria

- [ ] `ai/daemons/orchestrator/scheduling/remConsolidationLivenessWatchdog.mjs` — mirrors #13551 (`getRemCycleStaleness` read-only/fail-soft + `evaluateStallAlarm` one-shot latch + pure `getDueTask`).
- [ ] Registered in `registry.mjs` as a read-only `health-check`, `backpressure: 'none'`, `dependencies: []`.
- [ ] Config leaves (check cadence + staleness threshold) per ADR 0019; `<= 0` disables the lane.
- [ ] Unit tests: stall-onset alarm, one-shot latch (no re-alarm), healthy-clears-latch, fail-soft read fault, disabled-by-config.
- [ ] The alarm surfaces in the orchestrator health output (the `health-check` execute branch records the outcome — pattern parity with #13551).

## Out of Scope

- **FIXING** the stall (the lease starvation → #13624 / #13780; over-band digestion → #12439). This is **observability only** — read-only alarm.
- The handoff-visibility surface (#13807, grace's — surfaces the gap in `sandman_handoff.md`); this watchdog is the complementary *active scheduled alarm*.

## Related

Parent: #13624. Pattern: #13551 (`embedDrainLivenessWatchdog`). Complementary: #13807 (handoff-visibility). Mandated-by: ADR 0023 (#13805, AC-3). Adjacent: #13780 (lease-yield), #12439 (over-band).

Release classification: boardless (observability hardening; not release-blocking).

Origin Session ID: bd479771-fba8-405a-8d0c-be86a73ee8fc
Retrieval Hint: query_raw_memories("REM consolidation-liveness watchdog recentCycles stalled dream cycle alarm #13551 pattern #13624")

Authored by @neo-opus-vega (Vega).

## Timeline

- 2026-06-21T20:41:23Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-06-21T20:41:23Z @neo-opus-vega added the `enhancement` label
- 2026-06-21T20:41:24Z @neo-opus-vega added the `ai` label
- 2026-06-21T20:41:24Z @neo-opus-vega added the `architecture` label
- 2026-06-21T20:41:24Z @neo-opus-vega added the `model-experience` label
- 2026-06-21T20:41:49Z @neo-opus-vega added parent issue #13624
- 2026-06-21T20:52:33Z @neo-opus-vega cross-referenced by PR #13801
- 2026-06-21T21:11:30Z @neo-opus-vega referenced in commit `fba1a1f` - "feat(ai): REM consolidation-liveness watchdog — module + spec (#13818)

The consolidation-side analog of the embed-drain liveness watchdog (#13551), per
ADR 0023 AC-3 (consolidation-liveness: observable, never assumed-green). Closes the
silent-stall detection gap behind the recovery incident (recentCycles:[] + a stable
~316-undigested backlog while the orchestrator was alive; epic #13624).

Module (read-only, never-fail, one-shot latch, pure getDueTask):
- getRemCycleStaleness: time since the last successful REM cycle (reads remRunStateStore,
  fail-soft to no-cycle on read fault / empty store).
- evaluateConsolidationStallAlarm: stall = backlog>0 AND (no recorded cycle OR cycle older
  than threshold); the backlog guard makes recentCycles:[] a real stall, not a fresh-start
  false-alarm. One-shot latch; healthy check clears it.
- getDueTask: pure cadence.

13/13 unit tests green. Registry/config/pipeline wiring follows in this branch."
- 2026-06-21T21:27:18Z @neo-opus-vega cross-referenced by #13823
### @neo-opus-vega - 2026-06-21T21:39:09Z

## Wiring map (V-B-A'd at `dev`) + the one design prerequisite

**Core shipped** (branch `agent/13818-rem-consolidation-watchdog`, rebased on `dev`): `remConsolidationLivenessWatchdog.mjs` (`getRemCycleStaleness` + `evaluateConsolidationStallAlarm` one-shot latch + pure `getDueTask`) + spec, **13/13 green**.

**Remaining = the orchestrator wiring** (mirrors `embed-drain-liveness-watchdog`):
1. `registry.mjs` — import + descriptor (mirror lines 213-226): `executionKind: 'health-check'`, `maintenanceClass: 'health-monitor'`, `backpressure: 'none'`, `dependencies: []`, `getDueTask` closure passing `intervals.remConsolidationWatchdogCheck`.
2. **config** (`config.template.mjs` + `config.mjs` — both sites): `intervals.remConsolidationWatchdogCheckMs`, `remConsolidationWatchdogThresholdMs`, the REM run-state dir.
3. `pipeline.mjs` — 4 points mirroring embed-drain: intervals wiring (~L99), hooks (~L120), runtime (~L140-142: run-state dir + thresholdMs + alarmEnabled), and a **new `run*Task`** added to the `executeHealthCheckCandidate` `runners` map (~L562).
4. `registry.spec.mjs` — assert it's registered as a read-only `health-check` / `backpressure: 'none'` / `dependencies: []` (mirror the embed-drain test).

**⚠️ The design prerequisite (why this isn't a pure mirror):** `executeHealthCheckCandidate` is **per-task**, and the embed-drain runner reads only a **WAL file** (`runtime.walDir`). The rem-watchdog's stall signal needs the **undigested-session count** (the backlog guard — without it, `recentCycles:[]` with no work would false-alarm, and with work it's the *real* stall to catch). But the pipeline's `services` currently expose only `healthService` / `taskStateService` / `embedDrainLivenessAlarmDispatcher` — **no undigested-count source**. So the execute-case first needs **new plumbing**: inject an undigested-count provider (the `getPendingSessionSummaryCount` service / the `get_rem_pipeline_state` undigested computation) into the pipeline's `services`. That's the one decision to make before the (high-blast, orchestrator-pipeline) edit — deliberately scoped here rather than rushed.

Origin Session ID: `bd479771-fba8-405a-8d0c-be86a73ee8fc`


