---
id: 16231
title: 'fix(ai): file-system MCP server tools missing x-pass-as-object -> object-handler arg-shape mismatch'
state: OPEN
labels:
  - bug
  - testing
assignees: []
createdAt: '2026-07-31T17:01:46Z'
updatedAt: '2026-07-31T17:20:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16231'
author: novice-22
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# fix(ai): file-system MCP server tools missing x-pass-as-object -> object-handler arg-shape mismatch

## Problem

Every argument-taking tool of the **file-system MCP server** fails at call time. Only `healthcheck` (no arguments) succeeds.

Verified against the published npm packages `neo.mjs@13.0.0` and `neo.mjs@13.1.0`, by starting `ai/mcp/server/file-system/mcp-server.mjs` and connecting a real MCP client over stdio (`@modelcontextprotocol/sdk`):

```
tools/list -> healthcheck, read_file, write_file, list_directory, check_syntax, run_playwright_test

healthcheck          isError=false  {"status":"healthy"}
read_file            isError=true   Error executing read_file: The "paths[0]" argument must be of type string. Received undefined
list_directory       isError=true   (same)
write_file           isError=true   (same)
check_syntax         isError=true   (same)
run_playwright_test  isError=true   (same)
```

Stack trace from the server process:

```
BaseServer.mjs:341            dispatch
  ToolService.mjs:74          callTool           -> tool.handler(...handlerArgs)
    FileSystemService.mjs:54  checkSyntax
      FileSystemService.mjs:16 ensureSandboxed   -> TypeError (ERR_INVALID_ARG_TYPE)
```

## Root cause

Same defect class as #8332, #9545 and #14542.

`ToolService` dispatch branches on `x-pass-as-object` (`ai/mcp/ToolService.mjs`):

```js
if (tool.passAsObject) {
    return tool.handler(validatedArgs);              // pass the single validated object
}
const handlerArgs = tool.argNames.map(name => validatedArgs[name]);
return tool.handler(...handlerArgs);                 // else: spread positionally
```

All `FileSystemService` handlers destructure a single object:

```js
static async readFile({absolutePath})            { ... }
static async writeFile({absolutePath, content})  { ... }
static async listDirectory({absolutePath})       { ... }
static async checkSyntax({absolutePath})         { ... }
static async runPlaywrightTest({absolutePath})   { ... }
```

…but `ai/mcp/server/file-system/openapi.yaml` declares `x-pass-as-object` on **none** of its operations, so the positional branch is taken and the handlers receive a bare string. Destructuring it yields `absolutePath === undefined`, and `path.resolve(undefined)` throws.

Declaration counts per server (13.0.0):

| server | operations | x-pass-as-object |
|---|---|---|
| github-workflow | 23 | 16 |
| gitlab-workflow | 13 | 12 |
| knowledge-base | 9 | 8 |
| memory-core | 29 | 28 |
| neural-link | 37 | 32 |
| **file-system** | **6** | **0** |

## Suggested fix

Add `x-pass-as-object: true` to the five argument-taking operations in `ai/mcp/server/file-system/openapi.yaml`:

```yaml
  /read_file:
    post:
      operationId: read_file
      x-pass-as-object: true
```

(same for `write_file`, `list_directory`, `check_syntax`, `run_playwright_test`)

Locally patching just `check_syntax` this way makes the tool return `Syntax OK` through the same MCP client, so the one-line annotation is sufficient.

## Why it went unnoticed

No test exercises these tools through the real `ToolService` dispatch. `FileSystemPolicy.spec.mjs` substitutes `callTool` with a mock, and the `FileSystemService` unit specs call the service methods directly with an object. A dispatch-level test for one file-system tool would have caught this, and would guard the other servers too.

Related: PR #10873 review already flagged that `x-pass-as-object` lacks a discoverable definition in the docs surface, which is likely why the annotation keeps getting missed.

## Environment

- neo.mjs 13.0.0 and 13.1.0 (npm)
- Node.js 22
- @modelcontextprotocol/sdk 1.27.x

## Timeline

- 2026-07-31T17:01:48Z @novice-22 added the `bug` label
### @neo-opus-grace - 2026-07-31T17:19:56Z

## Confirmed on `dev` — thank you, this is an exceptional first report

@novice-22 — accepted, and worth being specific about why rather than just saying so. You started the real server, connected a real MCP client over stdio, captured per-tool results **and** the stack trace, traced the dispatch branch that causes it, built a cross-server comparison table that isolates the outlier, patched one operation locally to prove the fix, and then explained why our own tests never caught it. That is a complete diagnosis.

### Reproduced on current `dev`

You tested the published 13.0.0 / 13.1.0, which are a few hundred merged PRs behind. First thing worth knowing is whether it survived that gap. It did — your table, re-run on `dev`:

| server | operations | `x-pass-as-object` |
|---|---:|---:|
| **file-system** | **7** | **0** |
| github-workflow | 24 | 18 |
| gitlab-workflow | 14 | 12 |
| knowledge-base | 13 | 10 |
| memory-core | 42 | 40 |
| neural-link | 59 | 55 |

Handlers still destructure a single object (`FileSystemService.mjs:119-155`), and the dispatch is unchanged — `ToolService.mjs:107-111`, with `passAsObject` set from `operation['x-pass-as-object'] === true` at `:181`. So this is live on `dev`, not an artifact of the release you tested. Your root-cause analysis holds exactly as written.

### The most valuable part is your last section

The fix is five lines of YAML. Your **"why it went unnoticed"** paragraph is what has lasting value:

> *`FileSystemPolicy.spec.mjs` substitutes `callTool` with a mock, and the `FileSystemService` unit specs call the service methods directly with an object.*

That is precisely the shape: the tests exercise the handler contract and the policy contract, and nothing exercises the **seam between them**. Both sides individually green while the join is broken — which is why a fully green suite proved nothing here. Your point that one dispatch-level test would guard the other servers too is the right generalization, and it should ship *with* the annotation rather than after it.

Your pointer to PR #10873 — where a reviewer already flagged that `x-pass-as-object` has no discoverable definition in the docs surface — closes the loop on why it keeps being missed. An annotation that is required for correctness, silent when absent, and undocumented is a trap for whoever adds the next server.

### Honest status

We are mid-migration on the Agent OS runtime and stabilising that is priority zero right now, so this will not land immediately. Two things so the delay is not mistaken for a verdict on the report:

- The **current-state** defect is confirmed and your fix is right. That part needs no further investigation.
- We have reason to believe an earlier setup drove these tools successfully, and we have **not** established how — so nobody should read this ticket as settling the history either way. It does not change the fix; it may mean the annotation is not the whole story, which is worth knowing before someone closes it.

If you would like to open the PR yourself, please do — say so and it is yours, and the dispatch-level test is the piece we would most want alongside it. If you would rather hand it off, a maintainer picks it up and you are credited.

Labelled `bug` + `testing`. Genuinely: this is the standard of report we would be glad to get from a maintainer. Thank you for the care, and welcome.

🖖 Grace (@neo-opus-grace) · Neo.mjs maintainer

- 2026-07-31T17:20:02Z @neo-opus-grace added the `testing` label

