---
number: 15281
title: 'feat(fleet): throttle-state read producer — the honest axis (#15272)'
author: neo-opus-grace
state: OPEN
createdAt: '2026-07-16T16:43:18Z'
updatedAt: '2026-07-16T16:52:27Z'
closedAt: null
mergedAt: null
head: grace/15272-throttle-producer
base: dev
url: 'https://github.com/neomjs/neo/pull/15281'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #15272

The throttle axis of the S2 telltale taxonomy — and the honest half of the pair: the ticket's first design task was naming a trustworthy truth source, and the evaluation's binding conclusion is that **none exists in the platform yet**. Every candidate was falsified at source before this contract shipped: the lifecycle service's `failureReason` is content-generic (spawn/error-class strings at `FleetLifecycleService.mjs:447/:654`) and a rate-limited harness typically keeps RUNNING (parked), producing no failure record at all; stderr content is deliberately not retained (byte counts only, secret-hygiene design); A2A self-reports are prose, not producer-grade telemetry; provider usage APIs need credentials + polling + per-provider adapters; harness session files are heterogeneous and undocumented.

So `fleetThrottleStateAdapter.mjs` ships exactly what the AC prescribes for that outcome: the DTO contract (`none | overage | rate-limited | unknown`), taxonomy clamping (a future producer cannot smuggle a fifth state or a typo past the telltale), the capability envelope keeping "we cannot see" distinguishable from "nothing is throttled", and the **injected-reader seam the future watchdog-signals producer plugs into** (the follow-up `FleetManager`'s own JSDoc has named since the runtime-status view landed). With nothing injected — today's platform truth — every row is honestly `unknown` under `degraded/none`, never a fabricated `none`. `FleetManager.fleetThrottleStatus()` exposes the roster snapshot turnkey via the `throttleStateOptions` plain-field seam.

Evidence: L3 achieved (full unit coverage: taxonomy clamping, the platform-default unknown path as its own named spec, the watchdog flip-target path, partial-failure degradation, wiring) → L3 required (producer-contract claims, all mechanically provable). Residual: none — the watchdog-signals producer is the named follow-up that flips the default, not an AC of this leaf.

## Deltas from ticket

- None substantive. The truth-source evaluation demanded by the AC concluded all-candidates-falsified; per the ticket's own fail-honest clause, every sub-state ships `unknown` with the rationale documented at the module (behaviorally, at the seam the successor flips).

## Test Evidence

- `npx playwright test --config=test/playwright/playwright.config.mjs fleetThrottleStateAdapter FleetManager.spec` — **31 passed** (7 new adapter specs + 2 new `fleetThrottleStatus` wiring specs; 22 pre-existing FleetManager specs untouched-green on this base).
- Directly touched surfaces: `ai/services/fleet/` adapter + facade + their specs; no app/UI surface (the telltale render is #15273's leaf).

## Post-Merge Validation

- [ ] Trivial-rebase check against the wake sibling (PR #15280): both PRs insert adjacent import/field/method hunks into `FleetManager.mjs`/its spec — semantically independent; whichever lands second rebases in one command.
- [ ] The watchdog-signals producer (follow-up) injects `resolveThrottleState` and the platform default flips — the seam's integration proof.

## Review routing note

Cross-family primary on CI-green; Emmy holds #15273 (the consumer of both axes) — the pair reviews naturally together with the wake sibling PR #15280 (CI-green, requested).

Related: #15271 (the orthogonal wake sibling, PR #15280) · #15273 (consumer) · #13015 (parent epic).

Authored by Grace (Claude Fable 5, Claude Code). Session f7a6fdb7-8667-45d2-8d43-cbd3f16c6027.
