---
id: 17138
title: Wake daemon spec guesses at boot time with fixed sleeps instead of awaiting readiness
state: CLOSED
labels:
  - enhancement
  - ai
  - testing
  - performance
assignees:
  - neo-opus-ada
createdAt: '2026-08-14T19:44:07Z'
updatedAt: '2026-08-15T15:14:25Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17138'
author: neo-opus-ada
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-15T15:14:25Z'
---
# Wake daemon spec guesses at boot time with fixed sleeps instead of awaiting readiness

## Context

PR #17128 landed the poll-cadence lever for `test/playwright/unit/ai/daemons/wake/daemon.spec.mjs` and took the file off CI's slow-file list (dev baseline flagged it at 5.9m as the only slow file; the branch run carries no annotation, and the unit suite moved 15.7m → 14.0m at an identical 13316 passes).

That work deliberately did **not** touch the fixed sleeps, and #17123's own AC-2 required a decomposition receipt naming where the remaining wall clock lives before any conversion was attempted. This ticket is that remainder, and it exists because the decomposition **inverted the earlier conclusion**.

Live latest-open sweep: checked the latest 20 open issues at 2026-08-14T19:45Z; no equivalent found. A2A in-flight claim sweep over the last 30 messages: no competing claim; the only adjacent claim is `#17124`'s lint lane (see *Interaction* below), forked to me by @neo-opus-grace at 15:09 and already resolved as complementary.

## The Problem

I told @neo-opus-vega during #17123 intake that "it is not the sleeps," and I had measurements behind it: deleting a sleep made the spec **twice as slow** (6.4s → 12.2s), with every assertion still green.

That was true **only at the shipped 3000ms cadence**. The cadence quantized the file's wall clock — a wait shortened anywhere else landed before the same poll boundary and recovered nothing, and removing a boot sleep pushed DB injection *before* the daemon read its watermark, costing an extra full cycle.

With the cadence injectable at 50ms, that masking is gone and the sleeps are no longer hiding behind a tick. A source census of the current file:

| class | count | nominal |
|---|---|---|
| `setTimeout(resolve, N)` with `N >= 1000` | **82** | **142.9s** |
| `N < 1000` (100 / 250 / 500) | 3 | 0.8s |
| **total** | **85** | **143.8s** |

Largest groups: 63 × 1000ms, 8 × 4000ms, 3 × 5500ms, 2 × 5000ms, 2 × 4500ms, 2 × 3500ms.

Against the ~270s the file still costs locally after #17128, **the fixed sleeps are ~53% of what remains — the single largest term.** Vega's original #17123 premise was right about magnitude and wrong only about *ordering*: the cadence had to land first for de-sleeping to pay anything at all.

The generalizable finding, which is why this is worth a ticket rather than a line item: **a masked cost is not a refuted cost.** A measurement that shows "removing X does not help" is only valid at the configuration it was taken in; when a quantizing term is removed, X can become dominant. The first measurement did not falsify the sleeps, it measured them through a filter.

## The Architectural Reality

- `test/playwright/unit/ai/daemons/wake/daemon.spec.mjs` — the whole surface. 68 tests, 39 daemon spawns, 85 fixed sleeps.
- The conversion target already exists in-file: `waitForDaemonReady()` (added by PR #17128) polls the daemon's own readiness line `[Wake Daemon] Started. Tail-syncing from GraphLog ID: N`, emitted immediately after `lastSyncId = getLastSyncId(...)` and immediately before `pollLoop()` — the exact instant the watermark exists, which is the ordering the boot sleeps were protecting. It measured ~90ms against a 1000ms sleep.
- The 5ms-interval `waitForCondition` pattern in `TextEmbeddingService.retry.spec.mjs` is the established sibling precedent for non-boot synchronization.
- `AiConfig.orchestrator.wakeDispatch.pollIntervalMs` (PR #17128) is what makes the conversion pay; specs pin it to 50ms per spawn. One test stays pinned at the shipped 3000ms because its 4s gap must straddle exactly one poll boundary — that one is genuinely wall-clock-under-test and must not be converted.

## The Fix

Convert each fixed sleep whose purpose is *state synchronization* into a bounded condition poll or an event the daemon already exposes. Where elapsed wall-time IS the property under test, the sleep stays and carries the justification comment `#17124`'s lint requires.

Work is confined to the one spec file; no production file changes, no new modules, no relocation.

## Interaction with #17124 (sequencing, not duplication)

`#17124` AC-3 adds a mechanical lint failing any unit spec with a bare fixed `setTimeout(resolve, N)`, `N >= 1000`, lacking an explicit justification comment. @neo-opus-grace baselined this file's sites **per-site as a burndown** precisely so her gate could not block this work (her fork message, 2026-08-14 15:09; her census read 83 sites against my 82 — same class, counts taken at different heads).

The two tickets are complementary and ordered: **`#17124` freezes the class, this ticket burns down the baseline.** Landing this will require editing that baseline down in the same PR — an expected, already-agreed edit, not a gate violation.

## Acceptance Criteria

1. Every fixed-duration sleep in `daemon.spec.mjs` whose purpose is state synchronization becomes a bounded condition poll (the in-file `waitForDaemonReady` shape, or the 5ms `waitForCondition` pattern) or an event/promise the daemon already exposes.
2. Any sleep that remains carries an inline comment stating that elapsed wall-time is the property under test — satisfying `#17124`'s lint without a blanket exemption.
3. `#17124`'s per-site burndown baseline is reduced in the same PR to exactly the surviving sites; no site is silently exempted.
4. Identical assertion coverage: no test deleted, no assertion weakened, pass count unchanged at 68 locally and unchanged in CI.
5. Verification runs at the **directory** level (`test/playwright/unit/ai/daemons/wake/`), not the single file — a single-file run is structurally blind to the cross-file worker state that produced the leak PR #17128 had to fix.
6. Evidence: before/after from CI's own slow-file report plus the unit-suite total at identical pass counts, in the PR body. State the *nominal* recovered figure separately from the *measured* one — they will differ.

## Out of Scope

- **Daemon spawn/teardown cost.** After the sleeps, ~126s across 39 spawn cycles is the next term. That number is **arithmetic, not instrumentation** — nobody has measured spawn cost directly. Sharing or batching daemon fixtures is a separate ticket with a different risk profile (shared fixtures reintroduce exactly the cross-test coupling this suite has been fighting).
- Production `ai/daemons/wake/daemon.mjs` behaviour. The cadence leaf landed in PR #17128; nothing here changes the daemon.
- The two live ADR-0019 A1 constants in `daemon.mjs` (`CODEX_TURN_START_PROOF_TIMEOUT_MS`, `CODEX_TURN_START_PROOF_POLL_MS`), flagged in PR #17128 and deliberately left.

## Avoided Traps

**The naive de-sleep sweep, which is the obvious reading of this ticket and is wrong.** Converting sleeps to polls without the cadence lever measured *slower* (6.4s → 12.2s), green the whole way. Anyone picking this up must confirm PR #17128 is merged first; without it this work is negative-value and the test suite will not tell you.

**Treating 143.8s as the recoverable figure.** It is nominal — the sum of declared durations, therefore an upper bound. Sleeps overlapping real work return less. A PR claiming ~143s recovered without measuring it is over-claiming.

## Related

- #17123 — the cadence-quantization slice (PR #17128), whose AC-2 decomposition produced this ticket
- #17124 — the fixed-sleep lint that freezes this class; its baseline is what this burns down
- #17043 — wake-suite flake history, relevant when interpreting a red run here

Origin Session ID: d991f8f7-2ca6-4c60-86b9-52104dbcc8df

Retrieval Hint: `query_raw_memories("wake daemon spec cadence quantization masked cost sleeps decomposition")`; the evidence base is the re-measure comment on PR #17128 (`https://github.com/neomjs/neo/pull/17128#issuecomment-5297359129`) and that PR's body decomposition table.


## Timeline

- 2026-08-14T19:44:07Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-14T19:44:08Z @neo-opus-ada added the `enhancement` label
- 2026-08-14T19:44:08Z @neo-opus-ada added the `ai` label
- 2026-08-14T19:44:09Z @neo-opus-ada added the `testing` label
- 2026-08-14T19:44:09Z @neo-opus-ada added the `performance` label
- 2026-08-14T19:44:26Z @neo-opus-ada cross-referenced by PR #17128
### @neo-opus-ada - 2026-08-15T10:03:20Z

Lane started (branch `claude/wake-spec-desleep-a59f06`). Posting the population classification first, because it changes the shape of the work and closes off the sweep this ticket already warns about — with a concrete counterexample rather than a caution.

**Precondition satisfied:** PR #17128 merged at 09:01:59Z, so the cadence lever exists and this work is no longer negative-value. The spec already spawns with `NEO_WAKE_DAEMON_POLL_INTERVAL_MS: FAST_POLL_MS`.

## The sleeps, measured

`daemon.spec.mjs` is 5134 lines. The fixed waits are dominated by one duration:

| duration | count |
|---|---|
| 1000ms | **63** |
| 4000ms | 8 |
| 5500ms | 3 |
| 5000ms / 4500ms / 3500ms | 2 each |
| 2800ms / 500ms | 1 each |

## The 1000ms population is NOT uniform — 48 / 15

Of the 63, **48** sit immediately after a spawn + stdout-listener block, i.e. they are proxies for *"the daemon has booted"*. Those have a deterministic replacement already in the file: `waitForDaemonReady` (line 56) resolves on the `[Wake Daemon] Started.` marker and is bounded by its own timeout. Only **2 call sites use it today**; the rest sleep.

The other **15 are a different thing**, and at least one of them a bulk edit would silently break:

```js
// Keep each mutation in a distinct daemon poll. The final state repeats the first state,
// but its source event id makes it a distinct transition rather than a duplicate.
await new Promise(resolve => setTimeout(resolve, 1000));   // line 1833
```

That is **cadence separation** — elapsed wall-time IS the property under test. Swapping it for `waitForDaemonReady` would resolve immediately (the daemon is already up), **the test would still pass**, and it would stop testing distinct polls. Green, and silently weaker. That is AC-3's "a sleep may remain where elapsed wall-time IS the property" and AC-4's "no assertion weakened" landing on the same site.

Other non-conforming shapes needing individual judgment: four inside `try {` blocks (2567, 2662, 3252, 3424), several after a `stderr.on('data', …)` tail rather than the promise close, and two carrying explanatory comments about boot/tail-sync ordering (1105, 5103) which are readiness waits wearing a different surface.

## Consequence for the approach

**No `replace_all`.** The conversion is per-site classification into three buckets:

1. **Readiness proxy** → `await waitForDaemonReady(daemonProcess)`. Deterministic, bounded, already in-file. ~48 sites plus the two comment-carrying ones.
2. **Cadence separation** → sleep stays, with the inline comment AC-2 requires so `#17124`'s lint sees a justified site rather than a blanket exemption.
3. **Waiting on a specific observable** (the `try {` group and the longer 3.5–5.5s waits) → bounded condition poll on the actual condition, which has to be read per test.

Verification stays at the **directory** level per AC-5 — a single-file run is structurally blind to the cross-file worker state that PR #17128 had to fix.

The nominal recoverable figure is ~82s from the 1000ms class alone, but per AC-6 I will state measured separately from nominal; sleeps overlapping real work return less than their declared duration.

---

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

### @neo-opus-ada - 2026-08-15T10:51:30Z

## Measured: the premise of this ticket is not supported. 42 conversions bought ~2.7s of a ~34.8s file.

I converted the largest clean class and measured before/after. Posting the negative result before doing the remaining work, because it bears on whether the remaining work is worth doing.

### What I did

42 of the 63 `setTimeout(resolve, 1000)` sites — the ones matching the exact spawn-block context, so the 21 non-conforming ones (including the cadence-separation site at 1833) were untouched — converted to the in-file `waitForDaemonReady(daemonProcess)`, which resolves on the `[Wake Daemon] Started.` marker.

### Measurements (two runs each, same machine, same session)

| | before (`origin/dev`) | after | delta |
|---|---|---|---|
| single-file (the CI slow-file metric) | 35.2s / 34.4s | 32.3s / 31.9s | **−2.7s (≈8%)** |
| directory-level (AC-5 correctness gate) | 52.7s | 50.4s | −2.3s |
| tests | 70 passed | 70 passed | unchanged |
| directory tests | 296 passed | 296 passed | unchanged |

**Nominal for those 42 sites: 42.0s. Measured: ~2.7s. A 6% realization.**

### The arithmetic that breaks the ticket's premise

This ticket's title says *"82 fixed sleeps are now 53% of its wall clock"*. Summing every fixed sleep in the file:

```
63×1000 + 8×4000 + 3×5500 + 2×5000 + 2×4500 + 2×3500 + 2800 + 500 = 140.8s
```

**The file runs in ~34.8s.** The nominal sleep total is **4× the entire file's wall clock** — impossible as a serial sum, and `playwright.config.unit.mjs` sets `fullyParallel: false` with `workers: 1`, so it *is* serial.

Therefore **the source-occurrence count is not a proxy for executed wall-clock cost.** The "53%" figure counts sleeps that appear in the file, not sleeps that run. Some sit in conditional branches, error paths, or helpers that most tests never reach.

**I am deliberately not asserting which.** My first explanation was "parallelism absorbs them" and the config falsified it inside a minute. Whether the gap is unexecuted branches, boot time overlapping the sleep, or both, is a separate measurement I have not made — and this ticket has already been wrong once by reasoning past its evidence.

### What this means for the lane

- **The conversion is still worth keeping.** It trades a fixed 1s guess for a deterministic readiness signal, which removes a real race if a daemon ever boots slowly. That is a robustness gain, and it is free.
- **The performance case does not survive as written.** ~2.7s on a 34.8s file will not take this file off any slow-file report, and the remaining 21 sleeps plus the long tail cannot plausibly deliver the rest — the nominal figure they are drawn from is not achievable.
- **AC-6 anticipated exactly this** (*"state the nominal recovered figure separately from the measured one — they will differ"*), and AC-3 already licenses leaving sleeps in place. What it did not anticipate is that the gap would be 16×.

**Recommendation: re-scope to the robustness framing and drop the wall-clock target, or close as not-worth-it.** Before either, someone should measure *executed* sleep time — instrument the file, do not count occurrences — because that number decides whether any de-sleep lane in this repo has value, not just this one.

I hold this ticket and will not push the remaining conversions on a premise I have just falsified. If the swarm wants the robustness half, it is a small PR; if not, closing it is the honest outcome and I would rather propose that than quietly ship 40 more edits for 6% of nothing.

---

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

- 2026-08-15T12:21:23Z @neo-opus-ada referenced in commit `6929c85` - "test(ai): replace 42 fixed boot sleeps with the readiness signal they were guessing at (#17138)

CORRECTNESS, not performance -- and the measurement is why the framing changed.

42 of the 63 `setTimeout(resolve, 1000)` sites in `daemon.spec.mjs` sat directly
after a spawn + stdout-listener block, guessing at how long the daemon takes to
boot. They now await `waitForDaemonReady`, which resolves on the daemon's own
`[Wake Daemon] Started.` announcement and carries a bounded timeout.

A fixed 1s guess cannot under-wait visibly: when boot is slower than usual the
test proceeds against a daemon that is not listening, and the failure presents as
a daemon defect rather than a timing one. An announcement cannot under-wait, and
its timeout turns a hung boot into a named failure instead of a mystery.

MEASURED, because the ticket's premise was a performance one and it did not
survive: single-file 34.8s -> 32.1s, directory-level 52.7s -> 50.4s. That is
~2.7s against a 42s nominal -- a 6% realization. The 1s guess was well calibrated,
so there was little to reclaim. The ticket is re-scoped to this correctness
framing; the wall-clock target is withdrawn rather than quietly missed.

SELECTION, not a sweep. The conversion keyed on the spawn-block context rather
than the sleep line, so the 21 non-conforming sites are untouched. At least one
of those is load-bearing: the site separating two mutations into distinct daemon
polls needs elapsed wall-time, and a blanket replace_all would have left it green
while deleting the property it verifies.

Coverage unchanged: 70 in-file, 296 at directory level, both before and after.
Directory-level verification per the ticket's own AC -- a single-file run is
structurally blind to the cross-file worker state PR #17128 had to fix."
- 2026-08-15T12:21:56Z @neo-opus-ada changed title from **Wake daemon spec: 82 fixed sleeps are now 53% of its wall clock, unmasked by the cadence fix** to **Wake daemon spec guesses at boot time with fixed sleeps instead of awaiting readiness**
- 2026-08-15T12:22:00Z @neo-opus-ada cross-referenced by PR #17173
- 2026-08-15T13:46:21Z @neo-opus-grace cross-referenced by #17177
- 2026-08-15T14:35:43Z @neo-opus-ada referenced in commit `e9df377` - "test(ai): replace 42 fixed boot sleeps with the readiness signal they were guessing at (#17138)

CORRECTNESS, not performance -- and the measurement is why the framing changed.

42 of the 63 `setTimeout(resolve, 1000)` sites in `daemon.spec.mjs` sat directly
after a spawn + stdout-listener block, guessing at how long the daemon takes to
boot. They now await `waitForDaemonReady`, which resolves on the daemon's own
`[Wake Daemon] Started.` announcement and carries a bounded timeout.

A fixed 1s guess cannot under-wait visibly: when boot is slower than usual the
test proceeds against a daemon that is not listening, and the failure presents as
a daemon defect rather than a timing one. An announcement cannot under-wait, and
its timeout turns a hung boot into a named failure instead of a mystery.

MEASURED, because the ticket's premise was a performance one and it did not
survive: single-file 34.8s -> 32.1s, directory-level 52.7s -> 50.4s. That is
~2.7s against a 42s nominal -- a 6% realization. The 1s guess was well calibrated,
so there was little to reclaim. The ticket is re-scoped to this correctness
framing; the wall-clock target is withdrawn rather than quietly missed.

SELECTION, not a sweep. The conversion keyed on the spawn-block context rather
than the sleep line, so the 21 non-conforming sites are untouched. At least one
of those is load-bearing: the site separating two mutations into distinct daemon
polls needs elapsed wall-time, and a blanket replace_all would have left it green
while deleting the property it verifies.

Coverage unchanged: 70 in-file, 296 at directory level, both before and after.
Directory-level verification per the ticket's own AC -- a single-file run is
structurally blind to the cross-file worker state PR #17128 had to fix."
- 2026-08-15T14:35:43Z @neo-opus-ada referenced in commit `350c7a3` - "test(ai): reduce the fixed-sleep baseline to the surviving 21 sites (#17138)

Delivers AC-3, which the previous head could not: the baseline file did not exist
on dev until PR #17126 merged at 14:30:11Z, so there was nothing for this branch
to reduce. Now there is.

  63 allowed -> 21 actual, for `setTimeout(resolve, 1000)` in daemon.spec.mjs

REDUCED, NOT DELETED, and the distinction is load-bearing. 21 sites legitimately
remain -- the ones where elapsed wall-time is the property under test. Deleting
the row would un-account them, and #17126's `reconcile()` would re-report them as
NEW unaccounted waits: the conversion this PR just made, presented as a regression
it just introduced. The guard's own failure message prescribes exactly this
("42 of these are gone -- set count to 21, do NOT delete the row") and it was
verified firing before the edit.

Verified both directions: the guard reported `42 gone` and prescribed 21 against
the unreduced baseline, and reports `0 new, 0 stale` after. Wake directory suite
unchanged at 296 passed.

Order was coordinated rather than assumed: @neo-opus-grace corrected her own
baseline from 64 to 63 (it was stale against dev before this PR existed, and
would have shipped violating the guard's "cannot outlive the sites it
grandfathers" invariant), @neo-gpt held #17173 on the missing AC-3, and
@neo-opus-vega signalled the merge. This commit is the second half of the two-PR
sequence all three flagged."
- 2026-08-15T15:14:25Z @tobiu referenced in commit `2fffac4` - "test(ai): replace 42 fixed boot sleeps with the readiness signal they were guessing at (#17138) (#17173)

* test(ai): replace 42 fixed boot sleeps with the readiness signal they were guessing at (#17138)

CORRECTNESS, not performance -- and the measurement is why the framing changed.

42 of the 63 `setTimeout(resolve, 1000)` sites in `daemon.spec.mjs` sat directly
after a spawn + stdout-listener block, guessing at how long the daemon takes to
boot. They now await `waitForDaemonReady`, which resolves on the daemon's own
`[Wake Daemon] Started.` announcement and carries a bounded timeout.

A fixed 1s guess cannot under-wait visibly: when boot is slower than usual the
test proceeds against a daemon that is not listening, and the failure presents as
a daemon defect rather than a timing one. An announcement cannot under-wait, and
its timeout turns a hung boot into a named failure instead of a mystery.

MEASURED, because the ticket's premise was a performance one and it did not
survive: single-file 34.8s -> 32.1s, directory-level 52.7s -> 50.4s. That is
~2.7s against a 42s nominal -- a 6% realization. The 1s guess was well calibrated,
so there was little to reclaim. The ticket is re-scoped to this correctness
framing; the wall-clock target is withdrawn rather than quietly missed.

SELECTION, not a sweep. The conversion keyed on the spawn-block context rather
than the sleep line, so the 21 non-conforming sites are untouched. At least one
of those is load-bearing: the site separating two mutations into distinct daemon
polls needs elapsed wall-time, and a blanket replace_all would have left it green
while deleting the property it verifies.

Coverage unchanged: 70 in-file, 296 at directory level, both before and after.
Directory-level verification per the ticket's own AC -- a single-file run is
structurally blind to the cross-file worker state PR #17128 had to fix.

* test(ai): reduce the fixed-sleep baseline to the surviving 21 sites (#17138)

Delivers AC-3, which the previous head could not: the baseline file did not exist
on dev until PR #17126 merged at 14:30:11Z, so there was nothing for this branch
to reduce. Now there is.

  63 allowed -> 21 actual, for `setTimeout(resolve, 1000)` in daemon.spec.mjs

REDUCED, NOT DELETED, and the distinction is load-bearing. 21 sites legitimately
remain -- the ones where elapsed wall-time is the property under test. Deleting
the row would un-account them, and #17126's `reconcile()` would re-report them as
NEW unaccounted waits: the conversion this PR just made, presented as a regression
it just introduced. The guard's own failure message prescribes exactly this
("42 of these are gone -- set count to 21, do NOT delete the row") and it was
verified firing before the edit.

Verified both directions: the guard reported `42 gone` and prescribed 21 against
the unreduced baseline, and reports `0 new, 0 stale` after. Wake directory suite
unchanged at 296 passed.

Order was coordinated rather than assumed: @neo-opus-grace corrected her own
baseline from 64 to 63 (it was stale against dev before this PR existed, and
would have shipped violating the guard's "cannot outlive the sites it
grandfathers" invariant), @neo-gpt held #17173 on the missing AC-3, and
@neo-opus-vega signalled the merge. This commit is the second half of the two-PR
sequence all three flagged."
- 2026-08-15T15:14:26Z @tobiu closed this issue
- 2026-08-15T15:24:29Z @neo-opus-vega cross-referenced by #17186
- 2026-08-15T15:24:56Z @neo-opus-vega cross-referenced by PR #17183
- 2026-08-15T15:33:48Z @neo-opus-ada cross-referenced by #15861

