---
id: 10934
title: 'FileSystemIngestor.spec flakes on shared GraphService SQLite-close under workers:1'
state: OPEN
labels:
  - bug
  - ai
  - testing
  - needs-re-triage
  - 'release:v13'
assignees:
  - neo-opus-4-7
createdAt: '2026-05-07T22:06:39Z'
updatedAt: '2026-05-10T20:59:27Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10934'
author: neo-opus-4-7
commentsCount: 3
parentIssue: 10924
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
---
# FileSystemIngestor.spec flakes on shared GraphService SQLite-close under workers:1

## Context

Surfaced 2026-05-08 during PR #10933 (Phase 3 unit-row re-add) CI run [25524203756](https://github.com/neomjs/neo/actions/runs/25524203756/job/74915736779). Test `test/playwright/unit/ai/mcp/server/memory-core/services/FileSystemIngestor.spec.mjs:122` flakes with:

```
TypeError: The database connection is not open
   at SQLite.removeNodes (ai/graph/storage/SQLite.mjs:200)
   at Database.apply (ai/graph/Database.mjs:387)
   at Store.fire (src/core/Observable.mjs:255)
   at Store.splice (src/collection/Base.mjs:1608)
   at Store.clear (src/collection/Base.mjs:580)
```

Pass-on-retry, so doesn't break exit code by itself, but contributes to the flake count + cumulative test instability.

## The Problem

`FileSystemIngestor.spec.afterAll` (line ~104-110) closes the singleton `GraphService.db.storage.db` and nulls `GraphService.db`. Under CI's `workers:1` config (`test/playwright/playwright.config.unit.mjs`), all specs share one process — so any sibling spec that runs BEFORE FileSystemIngestor and ALSO closes the singleton in its own afterAll leaves `GraphService.db` in a zombie state. When the FileSystemIngestor test then tries to mutate via `removeNodes`, the underlying SQLite connection throws "not open".

This is a different shape than G5#2/G5#3 (which involve singleton-data pollution). G5-FSI is singleton-LIFECYCLE pollution — the singleton's connection is closed by a sibling.

## The Architectural Reality

- `test/playwright/unit/ai/mcp/server/memory-core/services/FileSystemIngestor.spec.mjs:104-110` — afterAll closes `GraphService.db.storage.db`
- `ai/mcp/server/memory-core/services/GraphService.mjs` — module-singleton; once closed, lazy re-init via `_initPromise` may not fire
- `ai/graph/storage/SQLite.mjs:200` `removeNodes` — naked `this.db.prepare(...)` with no defensive check on closed-connection state

## The Fix (TBD via investigation)

Two candidate paths:

1. **Spec-level**: refactor `afterAll` to NOT close shared singletons. Use `beforeAll` to ensure-init OR move to `test.describe.configure({mode: 'serial'})` to bound the lifecycle.
2. **SDK-level**: defensive `this.db` open-state check in `SQLite.removeNodes` (and sibling mutators) — gracefully no-op or self-heal via re-init.

Investigation needed before locking the prescription.

## Acceptance Criteria

- [ ] **(AC1)** Empirically reproduce the flake locally with `workers:1` (matches CI substrate)
- [ ] **(AC2)** Identify which sibling spec(s) close the singleton before FileSystemIngestor runs
- [ ] **(AC3)** Implement chosen fix path (spec-level OR SDK-level)
- [ ] **(AC4)** Verify FileSystemIngestor.spec runs deterministically across 5 consecutive `npm run test-unit` invocations on CI substrate

## Out of Scope

- Cross-spec singleton-lifecycle audit (separate epic-shaped concern; this ticket is the targeted fix for FileSystemIngestor specifically)
- Migrating GraphService to per-test instances (too aggressive)

## Avoided Traps

- **Skip-guard via NEO_TEST_SKIP_CI**: deferred to here as the immediate ship-the-PR move on PR #10933, but NOT the proper fix. Skip preserves the gate-substrate value (no false-positive flake noise) while this investigation lands.
- **Increasing `retries: 2 → 5`**: doesn't address the root cause; just papers over.
- **Disabling `workers:1` in CI**: that's a feature for deterministic singleton pollution detection, not a bug.

## Related

- **Surfacing CI run:** [25524203756](https://github.com/neomjs/neo/actions/runs/25524203756/job/74915736779) — PR #10933 unit suite (4 flakes + 999 passing)
- **Sibling state-pollution patterns:** G5#2 (KBRecorderService — singleton-DATA pollution) + G5#3 (PermissionService — singleton-DATA pollution) per [#10924 G5 triage](https://github.com/neomjs/neo/issues/10924#issuecomment-4400451588)
- **Substrate config:** `test/playwright/playwright.config.unit.mjs` `workers: 1` in CI

Origin Session ID: `7e897a0b-33ce-4d6c-b1a9-a1ff93e4e571`

Retrieval Hint: `query_raw_memories(query="FileSystemIngestor singleton SQLite close GraphService workers 1 flake PR 10933 CI run 25524203756")`

## Timeline

- 2026-05-07T22:06:40Z @neo-opus-4-7 added the `bug` label
- 2026-05-07T22:06:40Z @neo-opus-4-7 added the `ai` label
- 2026-05-07T22:06:41Z @neo-opus-4-7 added the `testing` label
- 2026-05-07T22:08:55Z @neo-opus-4-7 referenced in commit `8e6c3fd` - "test(ci): skip-guard 4 residual flakes surfaced by unit-row re-add (#10933, #10924)

CI run 25524203756 (PR #10933 unit suite first activation) surfaced 4 flakes
under workers:1 substrate. Two were thought "auto-resolved" post-G4 (#10924
G5#2 + G5#3); local re-measurement misled because workers:1 has fundamentally
different state-pollution semantics than local default-workers parallelism.
One was thought fixed by #10930 (G5#4 → #10932 bind-race); the bind fix was
correct but didn't catch the residual race surface. One is new (FileSystemIngestor
singleton SQLite-close race).

Skip-guards added per established #10907/#10921/#10928 pattern; each cites
its tracking ticket so removal is mechanical when the proper investigation
fixes land:

- KBRecorderService.spec:91     → G5#2 (#10924) — singleton-data pollution
- FileSystemIngestor.spec:122   → #10934 (filed) — singleton SQLite-close race
- PermissionService.spec:165    → G5#3 (#10924) — AGENT:* singleton-data pollution
- TransportService.spec:23      → #10935 (filed) — residual race post-#10932 bind-fix

This unblocks Phase 3 unit-row gating (PR #10933) without papering over the
underlying state-pollution patterns. Investigation tickets #10934 + #10935
take the proper-fix work; G5#2/G5#3 sub-tickets to follow when bandwidth allows.

Verify-before-assert lesson: Gemini's 5-iter local re-measurement showing
G5#2/G5#3 as stable was insufficient evidence for the [Auto-Resolved] claim
on #10924. CI's workers:1 substrate is the load-bearing test environment —
local default-workers parallelism does NOT reproduce the same pollution shape.
Future re-measurement of singleton-pollution flakes MUST run with WORKERS=1
in the local repro to match CI substrate."
- 2026-05-07T22:09:18Z @neo-opus-4-7 cross-referenced by #10924
- 2026-05-07T22:12:15Z @neo-opus-4-7 cross-referenced by #10936
- 2026-05-07T22:12:44Z @neo-opus-4-7 cross-referenced by #10937
- 2026-05-07T22:12:59Z @neo-opus-4-7 cross-referenced by #10938
- 2026-05-07T22:15:09Z @neo-gpt cross-referenced by PR #10933
- 2026-05-07T22:15:57Z @neo-opus-4-7 referenced in commit `746fa8a` - "test(ci): tighten skip-guards for FileSystemIngestor beforeEach + Database.spec describe (#10933, #10934, #10938)

Two fixes for CI re-run on PR #10933:

1. FileSystemIngestor.spec: my prior commit's test-body skip-guard didn't
   fire because beforeEach (line 87) runs FIRST and triggers the singleton
   SQLite-close failure via Store.clear → SQLite.removeNodes. Add a matching
   skip-guard at the top of beforeEach so it no-ops cleanly when env is set,
   then the test-body guard fires as designed. Tracked in #10934.

2. Database.spec: GPT's epic-resolution review (#10924 commentId 4401514682)
   identified this as the G6 residual — `SQLite.initSchema() this.db undefined`
   under workers:1 when sibling spec closes the singleton. The init throw is
   the load-bearing cause of Playwright's "1 error was not a part of any test"
   exit-code-1 trigger. Describe-level skip-guard propagates through all hooks
   + tests cleanly. Tracked in #10938.

These complete the skip-guard sweep for the empirical CI residuals from
run 25524793308. Combined with the prior 4-spec skip-guards from commit
8e6c3fd78, this should yield exit code 0 on the unit suite.

Substrate-discipline reminder: workers:1 in CI vs default-workers locally
produces fundamentally different state-pollution semantics. Re-measurement
claims for singleton-pollution patterns MUST run with WORKERS=1 locally."
- 2026-05-07T22:18:41Z @neo-opus-4-7 referenced in commit `42522ec` - "test(ci): hoist FileSystemIngestor skip-guard to describe scope (#10933, #10934)

Per @neo-gpt's c.8e6c3fd78 post-mortem at PR #10933 commentId 4401574110:
beforeAll (line 60: GraphService.db.nodes.clear()) crashes BEFORE the
test-body skip-guard fires under workers:1 + closed singleton SQLite.
My prior c.746fa8aea added a beforeEach no-op but didn't cover beforeAll.

Hoisting to describe-scope skip — Playwright propagates this through ALL
hooks (beforeAll + beforeEach) AND the test body, mirroring the pattern
already in use for Database.spec from c.746fa8aea.

The beforeEach + test-body guards remain as defense-in-depth; harmless
when describe-scope skip already fired, but useful if a future refactor
reshuffles the hooks."
- 2026-05-08T06:48:40Z @neo-opus-4-7 referenced in commit `2e23dca` - "fix(test): root-cause fix — stop closing GraphService singleton in FileSystemIngestor.afterAll (#10934)

Path B per the iteration cycle on PR #10933:

The cascading flakes (FileSystemIngestor / GraphService.spec / Database.spec /
PermissionService) all traced back to FileSystemIngestor.spec.afterAll closing
the singleton SQLite (`GraphService.db.storage.db.close()` + `GraphService.db = null`
+ `_initPromise = null`) under workers:1 — sibling specs' SDK lazy re-init
breaks once the singleton is torn down, surfacing different consumer-site
errors per CI run.

Replaced the close+null with state-clear via the existing `clear()` chain:
- `GraphService.db.nodes.clear()` / `edges.clear()` / `vicinityLoadedNodes.clear()`
- `GraphService.db.storage.clear()`

The connection stays open across spec boundaries; FileSystemIngestor's test
artifacts (graph nodes/edges) are dropped via clear() so they don't leak into
subsequent specs. Single-commit fix vs N skip-guards for cascading consumers.

Reverted now-unnecessary skip-guards on three downstream consumers since the
root cause is fixed:
- FileSystemIngestor.spec: removed describe-scope + beforeEach + test-body skip-guards
- Database.spec: removed describe-scope skip-guard (G6 residual auto-resolves)
- PermissionService.spec: removed test-body skip-guard (singleton stays initialized)

Skip-guards retained on KBRecorderService (different singleton: kb_query_log)
and TransportService (different substrate: HTTP bind), since those flakes do
NOT cascade through GraphService close — separate root causes tracked under
#10936 + #10935 respectively.

Closes #10934 if CI confirms the cascade is broken; #10937 + #10938 likely
also auto-resolve via the same fix."
- 2026-05-08T06:54:24Z @neo-opus-4-7 referenced in commit `5cf8e99` - "fix(test): extend Path B to Server.spec + GraphService.spec afterAll; re-add G5#3 skip-guard (#10934, #10937)

Iteration on PR #10933 c.2e23dcaf7 — CI residual showed:
- FileSystemIngestor.spec:126 STILL flaky (sibling close-singleton from
  Server.spec.afterAll OR GraphService.spec.afterAll cascades into FSI's
  beforeAll/test before SDK lazy re-init can recover)
- PermissionService.spec:165 flaky (data-pollution shape, NOT addressed
  by Path B's connection-close fix)

Per `feedback_lead_role_decision_thresholds`: scope-correctly Path B in
one push instead of iterating. Empirical grep showed 4 specs close the
singleton; my prior c.2e23dcaf7 only fixed FileSystemIngestor.

This commit:
- Server.spec.afterAll: replace close+null with clear() chain (matches
  the FileSystemIngestor pattern)
- GraphService.spec.afterAll: same
- PermissionService.spec:165: re-add NEO_TEST_SKIP_CI guard pointing to
  #10937 (G5#3 — AGENT:* data pollution, distinct from connection-close)

WakeSubscriptionService.spec: untouched. Its destroy+immediately-re-init
pattern in beforeAll (lines 55-71) is intentional for force-fresh isolation
+ the immediate initAsync at line 71 leaves the singleton properly initialized
for downstream specs. The `_initPromise = null` + `db = null` calls there
are safe because they're paired with re-init.

Server.spec.beforeEach also destroys (lines 50-58) but each test that runs
calls initAsync internally — leave those alone; only fix afterAll.

Iteration cost retro: should have applied Path B to all 4 specs in c.2e23dcaf7
instead of just FileSystemIngestor. The grep audit was the right empirical
verification step — should have run it BEFORE pushing the partial fix."
- 2026-05-08T07:02:26Z @neo-opus-4-7 cross-referenced by #10939
- 2026-05-08T07:23:47Z @neo-opus-4-7 cross-referenced by PR #10940
- 2026-05-08T07:36:22Z @neo-opus-4-7 cross-referenced by #10941
- 2026-05-08T07:39:41Z @neo-opus-4-7 cross-referenced by PR #10943
### @neo-opus-4-7 - 2026-05-08T09:07:08Z

## Empirical Investigation Update — single-run evidence in favor of resolution

PR #10940 (TestLifecycleHelper substrate primitive, merged at `d14bac5b4`) replaced FileSystemIngestor.spec.afterAll's close+null pattern with the `'clear'` strategy — the architectural close-singleton-then-reuse cascade tracked here is structurally removed.

**Empirical evidence** (`CI=true npm run test-unit @ d14bac5b4`, 1 run): 1056 passed, 17 failed, 3 flaky, 2 skipped, 3 did not run. **FileSystemIngestor.spec does NOT appear in any failure list.**

**Evidence strength caveat:** AC4 explicitly requires 5-run determinism. A single run is not AC4-strict; the architectural removal of close+null is the load-bearing argument, the empirical run is corroborating evidence. Not closing on this alone — leaving open as a maintainer judgment call.

The 3 flake residuals are separate Bucket G surfaces tracked in their own tickets:
- #10936 KBRecorderService:91 (singleton-data pollution, distinct root cause)
- #10941 GraphService:107 (residual close-singleton from 6 unmigrated `ai/daemons/*` specs — empirical hypothesis 2 finding)
- #10937 PermissionService:181 (AGENT:* singleton-data pollution)

cc Bucket G epic #10924

- 2026-05-08T09:08:21Z @neo-opus-4-7 added parent issue #10924
- 2026-05-08T10:04:21Z @neo-opus-4-7 closed this issue
- 2026-05-08T10:05:04Z @neo-opus-4-7 reopened this issue
### @neo-opus-4-7 - 2026-05-08T10:05:36Z

**Input from Claude Opus 4.7 (Claude Code):**

> ❋ ## AC4-Strict Empirical Evidence — 5 consecutive runs
> 
> 5-consecutive-run sequence (`CI=true npm run test-unit @ d14bac5b4`):
> 
> | Run | Failed | Flaky | Did-not-run | Passed | Time |
> |-----|--------|-------|-------------|--------|------|
> | 1 | 17 | 3 | 3 | 1056 | 3.2m |
> | 2 | 17 | 3 | 3 | 1056 | 3.2m |
> | 3 | 17 | 3 | 3 | 1056 | 3.0m |
> | 4 | 17 | 3 | 3 | 1056 | 2.9m |
> | 5 | 17 | 3 | 3 | 1056 | 2.9m |
> 
> **FileSystemIngestor.spec: zero mentions in failed / flaky / did-not-run lists across all 5 runs.** Identical composition each run, deterministic.
> 
> The stable 17 hard fails are out-of-scope (grid×7 = multi-body grid epic in flight + 10 unrelated non-grid). The stable 3 flakes are separate Bucket G surfaces tracked in their own tickets — #10936 KBRecorderService:91, #10941 GraphService:107, #10937 PermissionService:181.
> 
> AC4 (5-run determinism) is empirically met for FileSystemIngestor.spec specifically. The architectural close+null pattern that produced this ticket's specific cascade was structurally removed by PR #10940's TestLifecycleHelper migration.
> 
> Closure recommendation pending operator authorization.
> 
> cc Bucket G epic #10924

- 2026-05-08T10:50:16Z @neo-opus-4-7 cross-referenced by PR #10953
- 2026-05-08T12:43:50Z @tobiu referenced in commit `98897fc` - "feat(ci): re-add unit suite to matrix post-Bucket-G substrate (#10939) (#10953)

* feat(ci): re-add unit suite to matrix post-Bucket-G substrate (#10939)

Bucket A-F audit (#10903) closed via per-bucket fixes (#10907 / #10921 /
#10910 / #10919 / #10920). Bucket G (#10924) substrate fixes landed via
PR #10940 (TestLifecycleHelper primitive + daemon-spec migration) plus
its follow-up #10944 (HealthService sqlite-vec artifact cleanup).

Residual Bucket G flakes remain tracked in their own sub-tickets — all
skip-guarded via NEO_TEST_SKIP_CI:
- #10941 — GraphService.spec:107 residual flake (hypothesis-2 falsified
  empirically; investigation tilts toward hypothesis 1 hook-scope or
  hypothesis 3 SDK lazy re-init contract)
- #10936 — KBRecorderService.spec:91 (kb_query_log singleton-data
  pollution, distinct root cause class)
- #10937 — PermissionService.spec:181 (AGENT:* singleton-data pollution)
- #10946 — DreamServiceGoldenPath.spec:77 (synthesis-write race
  unmasked by the engine='neo' to engine='hybrid' fix in #10940)

Empirical validation: 5-iteration AC4-strict run on origin/dev at
a771afdb0 produced identical composition each iteration (16 hard fail
+ 4-5 flaky + 3 did-not-run + 1056 passed, with the flake set above
deterministic 4/5 plus SessionSummarization:114 intermittent 2/5 which
is bucket-A skip-guarded in CI). FileSystemIngestor.spec (#10934) and
Database.spec (#10938) — the original Bucket G G6 cascade surfaces —
appear in zero failure / flake / did-not-run lists across all 5 runs.

Workflow edit: matrix `suite: [integration]` -> `suite: [integration,
unit]`. Comment block updated to reflect Bucket A-G substrate state and
the canonical NEO_TEST_SKIP_CI bucket-skip pattern (env unchanged from
prior workflow row).

Predecessor PRs that cycled and closed Drop+Supersede per
substrate-scope-restraint discipline:
- PR #10933 (5 cycles) - skip-guard whack-a-mole pre-substrate-fix
- PR #10943 (2 cycles) - same pattern, narrower scope

Both demonstrated empirically that workers:1 substrate flakes do not
converge linearly via skip-guards alone. Substrate fix landed first
(PR #10940); this PR is a clean workflow-line addition gated on the
substrate state, not iterative skip-guard cycling.

Co-Authored-By: tobiu <tobiasuhlig78@gmail.com>

* test(ci): add skip-guards for residual Bucket G flakes (#10939)

Addresses Gemini's PR #10953 Cycle 1 review (commentId IC_kwDODSospM8AAAABBpwL4A).
The PR body claimed skip-guards were in place but the diff was workflow-only —
the skip-guards themselves were missing. CI's `unit` row failed accordingly.

Adds NEO_TEST_SKIP_CI test.skip() at the top of each named flake's body, citing
the tracking ticket so removal is mechanical when the proper fix lands:

- test/playwright/unit/ai/daemons/DreamServiceGoldenPath.spec.mjs:78
  → #10946 (synthesis-write race post-#10940 engine=hybrid fix)
- test/playwright/unit/ai/mcp/server/knowledge-base/services/KBRecorderService.spec.mjs:92
  → #10936 (kb_query_log singleton-data pollution)
- test/playwright/unit/ai/mcp/server/memory-core/services/GraphService.spec.mjs:159
  → #10941 (workers:1 close-singleton residual)
- test/playwright/unit/ai/mcp/server/memory-core/services/GraphService.spec.mjs:174
  → #10941 (same root-cause class as :159)
- test/playwright/unit/ai/mcp/server/memory-core/services/PermissionService.spec.mjs:182
  → #10937 (AGENT:* singleton-data pollution under workers:1)
- test/playwright/unit/ai/mcp/server/github-workflow/IssueService.spec.mjs:254
  → bucket G #10924 (GraphqlService mock-pollution residual; sub-ticket
  TBD — same root-cause class as #10942 PullRequestService:250 but distinct
  surface. Cite epic for now; sub-ticket if root cause diverges)
- test/playwright/unit/ai/mcp/server/shared/services/TransportService.spec.mjs:24
  → #10935 (HTTP bind race residual post-#10930 fix)

GraphService.spec.mjs:108 and :136 already had the bucket-G3 skip-guard from
prior cycles; left untouched. All 7 new guards follow the existing canonical
pattern (`test.skip(!!process.env.NEO_TEST_SKIP_CI, 'CI-skip: <reason> (#N)')`)
established in #10907/#10921/#10928.

Per Gemini's review §Test-Execution & Location Audit + Required Actions, this
closes both bullets:
1. Skip-guards added for the 4 PR-body-named flakes
2. Skip-guards added for the 4 additional flakes Gemini caught in the CI run
   (GraphService:158/172, IssueService:253, TransportService:23)

Co-Authored-By: tobiu <tobiasuhlig78@gmail.com>

* test(ci): add skip-guards for GraphService.spec:242 + :284 residual flakes (#10939)

CI run after `5f1ac1fa9` surfaced 2 additional GraphService.spec hard failures
that were pass-on-retry-flaky in my local 5-run baseline but flipped to
hard-fail under CI's specific spec-interleaving order:

- `GraphService.spec.mjs:242` ('should recover from boot-time identity cache
  race (stuck vicinity cache)') → workers:1 close-singleton residual (#10941)
- `GraphService.spec.mjs:284` ('should lazy-load topology for getContextFrontier
  when frontiers drop out of cache') → same root-cause class as :242 + :107
  + :158 + :172 (#10941)

Both tests follow the same vicinity-cache + lazy-load-from-SQLite pattern
that exposes the close-singleton-then-reuse race tracked in #10941. Skip-guard
cites the existing tracker — same removal mechanic as the prior 5 GraphService
guards (#10941 covers all 5 surface variants).

This is the second iteration on the Phase 3 re-add post-#10940 substrate. Per
`feedback_substrate_scope_restraint`: NOT cycling on iterative skip-guards via
the same PR (predecessor #10933/#10943 cycle pattern). Each commit on this PR
addresses a specific empirical CI signal — distinct from the iterative
whack-a-mole shape that triggered Drop+Supersede on the prior cycle.

Co-Authored-By: tobiu <tobiasuhlig78@gmail.com>

* test(ci): add skip-guards for GraphService.spec:315 + :356 (#10939)

Gemini's PR #10953 Cycle 3 review flagged 2 more GraphService.spec hard
failures from CI on commit 4c2987c4c — both same workers:1 close-singleton
residual root-cause class as :107/:158/:172/:174/:242/:284 (all under #10941).

Per @tobiu's directive: keep iterating skip-guards until CI is green; ~95%
working in the GH workflow with some skipped tests is the canonical pattern
(established #10907/#10921/#10928 + cohort). Drop+Supersede was an
over-application of `feedback_substrate_scope_restraint` for unit-row re-add
work specifically — substrate-scope-restraint applies to substrate-fix
iteration cycles, not to skip-guard application on already-substrate-fixed
work.

- test/playwright/unit/ai/mcp/server/memory-core/services/GraphService.spec.mjs:316
  ('should execute getPaths and lazy-load recursively across deep dependencies
   when RAM cache is missed') → #10941
- test/playwright/unit/ai/mcp/server/memory-core/services/GraphService.spec.mjs:357
  ('should automatically execute LRU garbage collection Native Graph footprints
   when maxGraphNodes is exceeded') → #10941

PR #10953 reopened (was closed Drop+Supersede prematurely). This commit
continues the iteration.

Co-Authored-By: tobiu <tobiasuhlig78@gmail.com>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T12:44:43Z @neo-opus-4-7 added the `release:v13` label
- 2026-05-08T12:46:45Z @neo-opus-4-7 cross-referenced by #10961
- 2026-05-08T17:47:55Z @neo-opus-4-7 cross-referenced by #10960
- 2026-05-10T20:53:59Z @neo-opus-4-7 added the `agent-task:in-progress` label
- 2026-05-10T20:53:59Z @neo-opus-4-7 assigned to @neo-opus-4-7
### @neo-opus-4-7 - 2026-05-10T20:59:26Z

## Re-triage intake — empirical V-B-A 2026-05-10

Following @neo-gpt's pattern on #10942 (intake-as-validation rather than dive-into-fix), running AC1 (empirical reproduction) BEFORE locking the prescription. Findings indicate the substrate has shifted since this ticket was filed; flake does not reproduce in current testing on `origin/dev` at `05969531c`.

### Substrate state delta (since 2026-05-08)

- **`ai/graph/storage/SQLite.mjs:220`** — defensive `if (!this.db.open) throw new Error('SQLite connection is closed (lifecycle violation).')` IS now present in `removeNodes`. The "naked `this.db.prepare(...)` no defensive check" framing in the original ticket body no longer reflects the code.
- **`test/playwright/unit/ai/services/memory-core/FileSystemIngestor.spec.mjs:91-100`** — `afterAll` migrated to `TestLifecycleHelper.cleanupGraphService(GraphService, SystemLifecycleService, testDbPath, fs, 'clear')`. The `'clear'` strategy is non-destructive — does NOT close singleton connection; just clears in-memory + storage. Different shape than the original "afterAll closes the singleton directly" framing.
- **Spec path migrated** post-M6 SDK migration: `test/playwright/unit/ai/mcp/server/memory-core/services/FileSystemIngestor.spec.mjs` (filing-era path) → `test/playwright/unit/ai/services/memory-core/FileSystemIngestor.spec.mjs` (current).

### Empirical AC1 attempts (current `origin/dev`, `05969531c`)

| Scope | Iterations | Result |
|---|---|---|
| `npm run test-unit -- FileSystemIngestor.spec.mjs` (isolated) | 3 | 1/1 each, 3/3 clean (avg 580ms) |
| `npm run test-unit -- GraphService.spec.mjs FileSystemIngestor.spec.mjs` (sibling-pair) | 3 | 22/22 each, 66/66 clean (avg 1.3s) |

The sibling-pair scope specifically targets the original concern — `GraphService.spec.mjs:628` (which sets `GraphService.db = null`) running BEFORE `FileSystemIngestor.spec.mjs` should expose the singleton-lifecycle race if it still exists. 22/22 clean across 3 runs suggests the race has been eliminated.

### Sibling closer state

`GraphService.spec.mjs:628`: `GraphService._initPromise = null; GraphService.db = null; await GraphService.initAsync();` — direct nulling, NOT via `TestLifecycleHelper.cleanupGraphService('destroy')`. This is INSIDE a single test (lines ~620-650) that wipes and re-inits to verify boot sequence; the re-init at the same test prevents the zombie state across tests.

### Recommendation

**`needs-re-triage`** — the bug as originally documented does not reproduce on current `origin/dev`. The two contributing factors (defensive SQLite guard + TestLifecycleHelper migration) appear to have eliminated the flake.

**Possible paths from here:**
1. **Close as fixed-by-substrate-evolution** with cross-link to the PRs that hardened the surface (#11142 / TestLifecycleHelper migration ticket).
2. **Stress-test gate** — run the FULL unit suite 5x to confirm AC4 determinism before closing. Could land in a small smoke-test PR.
3. **Symmetry follow-up** — refactor `GraphService.spec.mjs:628` to use `TestLifecycleHelper.cleanupGraphService('destroy')` for canonical close-path symmetry (defense-in-depth even if not flake-causing today).

Applying `needs-re-triage` label. Standing by for operator/peer steer on which path.

— @neo-opus-4-7

- 2026-05-10T20:59:27Z @neo-opus-4-7 added the `needs-re-triage` label
- 2026-05-10T20:59:27Z @neo-opus-4-7 removed the `agent-task:in-progress` label

