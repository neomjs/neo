---
id: 12812
title: Intra-run progress logs for session-summary + mini-summary backfill (long-run visibility)
state: CLOSED
labels:
  - enhancement
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-06-09T08:14:34Z'
updatedAt: '2026-06-11T06:34:04Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12812'
author: neo-opus-grace
commentsCount: 1
parentIssue: 12740
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-11T06:34:04Z'
---
# Intra-run progress logs for session-summary + mini-summary backfill (long-run visibility)

## Context

Follow-up to #12810 (which added the **at-trigger** backlog depth). @tobiu: long summary tasks (15-30 min) currently emit only `Starting X` → silence → `completed` / `watchdog-killed` — no progress. Other heavy tasks (file ingest, embeddings) show granular `5/30 done`. We want the same for **session summaries (each)** and **mini-summaries (every ~5)** so a 15-30 min run gives real output, not silence.

## V-B-A — why the operator is currently blind (root cause)

The ProcessSupervisor spawns the lifecycle children (`summarize-sessions.mjs`, `backfill-memory-summaries.mjs`) with `stdio: ['ignore', 'ignore', 'pipe']` (`ai/daemons/orchestrator/services/ProcessSupervisorService.mjs:353`) — **child STDOUT is IGNORED; only STDERR is captured + re-logged** as `[ProcessSupervisor] {label} stderr: …` (`:241-250`). Consequences:

- The children's `console.log` progress (e.g. `summarize-sessions.mjs`'s lines) is **discarded** — invisible in the orchestrator.log the operator watches.
- The memory-core `logger` (`ai/mcp/server/memory-core/logger.mjs`) writes to a file sink (`mc-server*.log`) **+** stderr, gated by `stderrMode: 'debug'`. So `logger.*` progress reaches orchestrator.log only if its level routes to stderr.
- `SessionService.summarizeSessions` already logs `Processing batch X/Y` (`:1216`, `logger.info`) at batch-start — but it is invisible in orchestrator.log if `logger.info` does not reach stderr (the likely reason the operator doesn't see it today).
- `MemoryService.backfillMiniSummaries` has **no intra-loop progress log at all**.

## The Fix

1. **Mini-summary backfill** (`MemoryService.backfillMiniSummaries`): emit a progress log every ~5 processed rows — `miniSummary backfill: N/total (U updated, D deferred)` — reusing the `processed` counter added by the #12802 run-budget.
2. **Session summary** (`SessionService.summarizeSessions`): emit a per-session **completion** log — `session summarization: N/total done (<sessionId>)` — after each `summarizeSession` resolves (the `chunk.map` at `:1218`).
3. **Visibility routing (the key decision):** since child stdout is ignored, progress MUST reach **STDERR** (the captured channel) to land in the operator's orchestrator.log view. Confirm/adjust so info-level progress reaches stderr (verify `stderrMode` handling in `ai/mcp/server/shared/logger.mjs`), OR switch the supervisor child stdio to capture stdout (`stdio[1]='pipe'`). Decide at implementation; target: progress visible alongside the `[ProcessSupervisor] Starting …` lines.

## Decision Record impact

`none` — orchestrator/service logging.

## Acceptance Criteria

- [ ] Mini-summary backfill emits a progress log every ~5 rows (`N/total` + updated/deferred).
- [ ] Session summarization emits a per-session completion log (`N/total`).
- [ ] The progress is **visible in the operator's orchestrator.log view** (not only the `mc-server` file sink) — verified against the stdout-ignore / stderr-capture routing.
- [ ] Unit coverage where feasible (loop progress emission); the routing may need a manual/integration check.

## Out of Scope

- The summary subsystem's drain/bound (#12746 / #12802), timeout (#12804 / #12805), and at-trigger depth (#12810) — all shipped.

## Related

- #12740 (epic). #12810 (at-trigger backlog depth — this is the intra-run complement).

Live latest-open sweep: checked latest open issues + dup-search at 2026-06-09T08:14:03Z; no progress-log dup.

Authored by Claude Opus 4.8 (Claude Code)

## Timeline

- 2026-06-09T08:14:35Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-06-09T08:14:36Z @neo-opus-grace added the `enhancement` label
- 2026-06-09T08:14:36Z @neo-opus-grace added the `ai` label
- 2026-06-09T08:15:12Z @neo-opus-grace added parent issue #12740
- 2026-06-09T17:13:08Z @neo-opus-grace cross-referenced by #12823
- 2026-06-09T19:15:33Z @neo-opus-grace cross-referenced by #12828
- 2026-06-10T10:52:01Z @neo-opus-vega cross-referenced by #12833
- 2026-06-10T22:15:32Z @neo-opus-vega cross-referenced by #12872
- 2026-06-10T23:05:07Z @neo-fable cross-referenced by #12696
- 2026-06-11T01:26:51Z @neo-opus-vega unassigned from @neo-opus-grace
- 2026-06-11T01:26:54Z @neo-opus-vega assigned to @neo-opus-vega
### @neo-opus-vega - 2026-06-11T01:26:56Z

**`[lane-override]` reassignment audit-trail** (#11537 §AC8)

**Previous assignees:** `@neo-opus-grace`
**New assignees:** `@me`
**Reason:** Reassigned from @neo-opus-grace per his explicit A2A handoff (MESSAGE:e685cb93, 2026-06-11) — he is not on it. #12812 is the summary-backfill progress-logging lane; it's my summary lane and tonight's prefill/lease/backlog investigation (the ~31× REM-starvation finding + the ~387-session blind backlog) supplies the operator-visibility "why".

*Audit-trail per AGENTS.md §6.5 — `acknowledgedReassign` reason persistence. Graph-ingested via Retrospective daemon comment-scan path.*

- 2026-06-11T01:47:18Z @neo-opus-vega cross-referenced by PR #12885
- 2026-06-11T02:17:53Z @neo-opus-vega referenced in commit `522570d` - "fix(memory-core): emit summary/backfill progress to stderr directly, not via a config-Provider mutation (#12812)

Addresses the B4 review: drops the Memory_Config.data.debug=true activation (consumer mutation of the read-only reactive Provider SSOT per ADR 0019) from both lifecycle children. The 3 progress lines now write directly to stderr (the channel ProcessSupervisor captures into orchestrator.log), [INFO]-prefixed for the child-stderr level parser — targeted (no debug-flood), Provider untouched."
- 2026-06-11T02:20:30Z @neo-opus-vega cross-referenced by #12889
- 2026-06-11T06:34:04Z @tobiu referenced in commit `9bcd7bc` - "feat(memory-core): intra-run progress logs for session-summary + miniSummary backfill (#12812) (#12885)

* feat(memory-core): intra-run progress logs for session-summary + miniSummary backfill (#12812)

backfillMiniSummaries logs every ~5 rows + a completion tally; summarizeSessions logs per-session completion. The lifecycle children enable debug logging so memory-core logger output reaches stderr (the channel ProcessSupervisor captures into orchestrator.log; child stdout is ignored) — mirrors runSandman.mjs / syncKnowledgeBase.mjs. Operator-visibility is verified at runtime per the ticket's manual/integration note.

* fix(memory-core): emit summary/backfill progress to stderr directly, not via a config-Provider mutation (#12812)

Addresses the B4 review: drops the Memory_Config.data.debug=true activation (consumer mutation of the read-only reactive Provider SSOT per ADR 0019) from both lifecycle children. The 3 progress lines now write directly to stderr (the channel ProcessSupervisor captures into orchestrator.log), [INFO]-prefixed for the child-stderr level parser — targeted (no debug-flood), Provider untouched."
- 2026-06-11T06:34:04Z @tobiu closed this issue
- 2026-06-12T02:38:01Z @neo-opus-grace cross-referenced by #12943
- 2026-06-12T03:14:13Z @neo-opus-vega cross-referenced by PR #12952

