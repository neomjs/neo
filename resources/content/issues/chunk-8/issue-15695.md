---
id: 15695
title: Measure restore-empty-target staging and promotion at 5k/20k
state: CLOSED
labels:
  - bug
  - ai
  - testing
  - performance
assignees:
  - neo-gpt-emmy
createdAt: '2026-07-22T09:45:56Z'
updatedAt: '2026-07-24T11:32:29Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15695'
author: neo-gpt-emmy
commentsCount: 7
parentIssue: null
subIssues:
  - '[x] 15755 Add the disposable restore target-set meter'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[x] 15740 Implement orchestrator-owned restore-empty-target action'
closedAt: '2026-07-24T11:32:29Z'
---
# Measure restore-empty-target staging and promotion at 5k/20k

## Context

The original fresh-host investigation is measured in [the phase receipt](https://github.com/neomjs/neo/issues/15695#issuecomment-5049531530): ordinary restore preserved explicit vectors, made zero embedding-provider calls, and did not reproduce a provider-dependent hang. The first material boundary was whole-file vector retention plus store transport; 20,000-row runs reached 1.23–1.33 GiB importer RSS before counting Chroma.

That evidence unblocked #15691 and #15692. Both fixes are now merged: bundle/row admission completes before mutation, and stored-vector import streams in bounded batches.

Discussion #14032 subsequently graduated the narrower `restore-empty-target` contract at body SHA-256 `9b3139f6678dca536407e3d5f0d426df83f9a28d281781a7e404a2cb692d684c`. The remaining scale gate is no longer the manual importer: #15740 must stage, validate, promote, and revalidate one run-owned Memory Core target set containing memories Chroma, summaries Chroma, and the SQLite graph. #15740 is blocked from merge until this ticket supplies that evidence.

## The Problem

#15692 bounds each vector-import batch, but it does not price the complete recovery unit:

- two isolated Chroma staging targets;
- a run-owned SQLite graph staging target;
- pre-promotion validation of all three targets;
- ordered memories → summaries → graph promotion;
- post-promotion target-set validation and cleanup/containment overhead;
- Node, Chroma, SQLite, temporary-disk, and total wall-time cost.

The prior single-import receipt cannot be reused as proof for this wider action. Conversely, a mock-only benchmark cannot prove the exact #15740 implementation is cloud-safe.

## Investigation Contract

Build the disposable measurement harness now, then run its final evidence pass against the exact #15740 implementation head before that PR may merge.

1. Use fresh, run-owned destinations and disposable bundle/store roots only. No live Memory Core data may be opened or mutated.
2. Measure two explicit fixture profiles:
   - `5k-target-set`: 5,000 stored-vector rows in memories and 5,000 in summaries;
   - `20k-target-set`: 20,000 stored-vector rows in memories and 20,000 in summaries.
3. Include a deterministic graph fixture in both profiles and report its exact node/edge counts and serialized bytes. Do not imply a one-to-one relationship between vector and graph rows.
4. Execute and time the exact action phases separately: admission, stage memories, stage summaries, stage graph, validate all staged targets, promote memories, promote summaries, promote graph, revalidate production, and terminal settlement.
5. Record peak Node heap/RSS, Chroma RSS, SQLite/process RSS where separable, temporary-disk high-water mark, first/last progress receipt, per-phase row counts, and observed maximum vector batch size.
6. Trace provider entrypoints and prove zero embedding or re-embedding calls. Provider state is not a benchmark axis for the final action because it is not a dependency.
7. Exercise one interrupted pre-promotion control and one post-first-promotion reconciliation control. These controls measure retained staging/disk/memory cost; #15740 owns correctness of the state machine.
8. Repeat the final measurement on the exact #15740 PR head. Synthetic or seam-only runs remain controls and cannot satisfy the merge gate.

## Acceptance Criteria

- [ ] A disposable harness measures the complete memories + summaries + graph target set without touching live data
- [ ] The 5k profile contains 5,000 explicit-vector rows per Chroma destination and reports exact graph cardinality/bytes
- [ ] The 20k profile contains 20,000 explicit-vector rows per Chroma destination and reports exact graph cardinality/bytes
- [ ] Admission, each staging phase, staged validation, each ordered promotion, production revalidation, and settlement have separate timings and progress receipts
- [ ] Peak Node/Chroma/SQLite RSS, Node heap, temporary-disk high-water mark, row counts, and maximum observed vector batch size are reported
- [ ] Provider tracing proves zero embedding and re-embedding calls
- [ ] Pre-promotion interruption and post-first-promotion reconciliation controls report retained-resource cost
- [ ] The final receipt names the exact #15740 implementation head; mock-only evidence is explicitly non-authoritative
- [ ] Any unsafe scaling or unbounded phase is routed to its owning fix before #15740 merge; no provider gate is introduced from correlation

No arbitrary wall-time or RSS pass threshold is invented here. The merge decision must use the measured 5k→20k scaling curve, the bounded-batch witness, and the declared cloud deployment envelope rather than a threshold without provenance.

## Decision Record Impact

None. This ticket measures the `restore-empty-target` contract graduated by Discussion #14032 and governed by the #15739 ADR-0027 amendment. It cannot change action authority.

## Authority Boundary

- The orchestrator classifier remains the only selector of `restore-empty-target`.
- `DataRecoveryActuatorService` / #15740 owns persistent recovery mutation.
- This ticket owns disposable measurement only.
- Stored-vector restore is provider-free.
- Embedding and eventual re-embedding remain separately classified and Orchestrator-driven.

## Out of Scope

- Implementing or widening the #15740 action contract
- Implementing the #15639 bootstrap selector
- `restore-shadow-fill`, generic count-loss promotion, or journal replay
- Knowledge Base restore
- Production mutation
- Adding an embedding-provider preflight

## Avoided Traps

- **Reusing the old importer receipt** — it omits graph staging and ordered target-set promotion.
- **Mock-only green benchmark** — useful as a control, not exact-head merge evidence.
- **Node-only memory accounting** — hides Chroma, SQLite, and temporary-disk cost.
- **One end-to-end stopwatch** — cannot locate a scale cliff or retained-resource phase.
- **Provider-state matrix** — the final action has no provider dependency; trace zero calls instead.
- **Invented performance threshold** — report the scaling curve and deployment envelope before choosing a bound.

## Dependencies And Routing

- #15740 may implement while this measurement lane is open, but its PR may not merge until the exact-head receipt lands here.
- #15739 remains the ADR merge-order gate for #15740.
- #15639 consumes the admitted action only after #15739 and #15740 settle.

## Related

- Discussion #14032
- #15639
- #15691
- #15692
- #15739
- #15740

Origin Session ID: `cb60301d-74a4-4024-b80d-2f7efdbf9cd1`

Retrieval Hint: "restore-empty-target 5000 20000 memories summaries graph staging promotion peak memory"


## Timeline

- 2026-07-22T09:46:52Z @neo-gpt-emmy cross-referenced by #15639
- 2026-07-22T09:46:54Z @neo-gpt-emmy cross-referenced by #15689
- 2026-07-22T09:46:55Z @neo-gpt-emmy cross-referenced by #15640
- 2026-07-22T09:50:12Z @neo-gpt-emmy cross-referenced by PR #15690
### @neo-gpt-emmy - 2026-07-22T15:54:41Z

## Ticket intake — `valid-as-staged-investigation`

- **Currency / ownership:** same-day ticket; live assignee list is empty, and the current open issue/PR queue contains no competing implementation or claim.
- **Current-source falsifier:** the premise survives. KB still accumulates each JSONL file in `records[]` before 500-row writes; MC does the same before 250-row writes and retains file-wide merge sets. `validateBundle()` streams only the first non-empty row, so it cannot attribute a late-row stall before import.
- **Prior-art sweep:** Memory Core receipt `b3da60a9-de69-4731-ba33-c128e5805246` records a real 22,636-row / 1.2 GB JSONL recovery class and the V8 whole-string failure that forced streaming verification. That does not prove today's importer stalls at 5k, but it makes the bounded matrix materially warranted.
- **Provider premise:** explicit-vector restore remains provider-free (`reEmbed=false` on MC; KB passes stored embeddings). Provider healthy/cold/unreachable is therefore a dependency-census axis, not permission to re-embed.
- **ADR fit:** continue. Diagnostics measure and classify; the orchestrator remains the only owner of recovery/re-embedding action selection.

### Staging constraint

The manual importer matrix is executable now. The opt-in first-boot row is **`NOT_YET_MEASURED — BLOCKED_BY #15639`** until that controller exists; it must not be simulated by moving orchestration into this ticket. I will drive the current manual/DatabaseService phase receipts first and keep the blocked row explicit.

The investigation must produce evidence for #15691/#15692 rather than implementing either prescription opportunistically: first stalled boundary, peak buffered state, provider-request census, and a minimal disposable reproducer. Verdict: proceed.

### @neo-gpt-emmy - 2026-07-22T17:50:17Z

## Fresh-host restore matrix — measured receipt

**Head:** `6587d96cfe`  
**Host:** Darwin arm64, Node `v25.9.0`  
**Isolation:** every run used a fresh Node process, `UNIT_TEST_MODE=true`, disposable SQLite/WAL/log paths, and either a strict in-process collection seam or Neo's own private Chroma test-daemon helper with a unique temp data directory + collection. The daemon was heartbeat-gated and torn down after each run. No live store or production bundle was opened.

Fixtures contain 4,096-dimensional explicit vectors and `reEmbed=false`. The zero-valued JSON representation is deliberately compact (20k ≈ 158 MiB on disk), so parsed vector cardinality is exact while wire-byte cost is conservative versus ordinary floating-point exports.

### 1. Provider axis: falsified as a restore dependency

MC merge, 5,000 valid rows, strict store seam:

| Provider stub | Wall | Peak heap delta | Imported | Provider calls |
| --- | ---: | ---: | ---: | ---: |
| healthy | 171.6 ms | +171.4 MiB | 5,000 | **0** |
| cold (would delay if called) | 174.4 ms | +173.7 MiB | 5,000 | **0** |
| unreachable (would throw if called) | 175.8 ms | +173.7 MiB | 5,000 | **0** |

The provider state did not change the path because neither importer invoked `TextEmbeddingService.embedText(s)`. The removed restore-wide provider gate remains falsified.

### 2. Scale + store transport

These rows used a real private Chroma daemon. Memory is the **Node importer only**; Chroma ran in a separate process, so total deployment memory was higher.

| Entry / importer | Mode | Rows | Wall | Peak Node heap | Peak Node RSS | First store request |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `runRestore()` → MC | merge | 5,000 | 3.748 s | 395.9 MiB | 685.6 MiB | after all 5,000 lines |
| MC service | merge | 5,000 | 3.693 s | 335.3 MiB | 582.5 MiB | after all 5,000 lines |
| MC service | replace | 5,000 | 3.702 s | 404.9 MiB | 689.0 MiB | after all 5,000 lines |
| MC service | merge | 20,000 | 14.747 s | **882.8 MiB** | **1,232.9 MiB** | after all 20,000 lines |
| KB service | merge | 20,000 | 13.408 s | **930.4 MiB** | **1,332.2 MiB** | after all 20,000 lines |

The instrumented readline/store boundary is decisive: no store request occurred until EOF. Mock-store controls showed the same shape at 20k (MC: 696.6 MiB heap / 921.5 MiB RSS; KB: 685.9 MiB / 934.2 MiB), proving that whole-file parse retention exists independently of Chroma latency. Real Chroma then adds serialization/transport pressure.

Both 20k runs completed on this host, so I did **not** reproduce an actual hang. The measured 1.23–1.33 GiB importer RSS, before counting Chroma, is nevertheless a falsifiable constrained-container dead-end mechanism and validates #15692's bounded-streaming premise.

### 3. Late-row corruption boundaries

At row 5,000, with the provider unreachable and still zero provider calls:

- **MC missing / non-array / mixed-dimension vector:** imports 4,999, reports `failed: 1`, persists 4,999. A real-Chroma missing-vector run confirmed the same result.
- **KB missing vector, real Chroma:** fails after **4,500 rows were already persisted**; wall 3.188 s, peak 364.8 MiB heap / 646.4 MiB RSS. The current KB importer has no MC-equivalent full vector-validity partition.
- **Corrupt final JSON row:** both importers read all 5,000 lines, retain the full valid prefix, then fail before the first store write (≈204 MiB heap / 406–411 MiB RSS in the control).
- In `replace` mode, current source truncates before the importer performs this full-file work. Full compatibility/parse validation before mutation therefore remains a safety requirement, not merely an optimization.

### Phase/status verdict

- **Manual `ai:restore`: measured.** The six-phase `runRestore()` witness completed; the embedded-import phase owns essentially all material time/memory.
- **First-boot orchestration:** `NOT_YET_MEASURED — BLOCKED_BY #15639` (the controller does not exist yet).
- **Post-restore lane startup:** `NOT_YET_MEASURED — BLOCKED_BY #15639`; it must remain a separate receipt after the controller lands.
- **Provider requests:** zero in every compatible-vector and malformed-row run.
- **Original symptom:** exact off-host hang unreproduced here; old re-embedding explanation falsified.
- **First material boundary:** whole-file buffering plus Chroma serialization, with a conservative 20k fixture already exceeding 1.2 GiB Node RSS.

### Routing

1. **#15692 is now evidence-backed and unblocked:** stream bounded batches through parse → validate → write, emit progress/checkpoints, and avoid a file-wide `records[]` / merge set.
2. **#15691 owns the safety preflight:** fully validate parseability, vector presence/shape/dimension, and bundle compatibility before any replace truncation or KB partial write.
3. **#15694 stays action-scoped:** this investigation provides no warrant for a restore-wide embedding probe.
4. I am leaving the #15639-dependent rows explicitly incomplete rather than simulating controller behavior in this ticket.

Minimal reproducer: generate a JSONL file of 20,000 records with 4,096-number explicit vectors, replace the importer collection owner with a receipt wrapper (or a uniquely named collection on the repository's disposable Chroma test daemon), sample `process.memoryUsage()` every 5 ms, and record readline lines observed at the first `get/add/upsert`. The first call occurs only after line 20,000.

— Emmy (GPT-5.6 Sol Ultra, Codex)

- 2026-07-22T17:58:59Z @neo-gpt-emmy cross-referenced by #15692
- 2026-07-22T18:01:47Z @neo-gpt-emmy cross-referenced by #15693
- 2026-07-22T18:07:04Z @neo-kimi-phoebe cross-referenced by PR #15732
- 2026-07-22T18:13:52Z @neo-gpt-emmy cross-referenced by PR #15733
- 2026-07-22T20:44:06Z @neo-gpt-emmy cross-referenced by #15739
- 2026-07-22T20:45:54Z @neo-gpt-emmy cross-referenced by #15740
- 2026-07-22T22:31:26Z @neo-gpt-emmy cross-referenced by PR #15743
### @neo-gpt-emmy - 2026-07-23T12:57:29Z

## Intake continuation — `valid-as-staged-measurement`

**Live authority / source falsifier**

- `origin/dev` is `ee43360bfd2375ea752e5f5c8311ed7cd165cec5` after #15753 merged.
- The ADR-0027 target-set amendment is merged through #15743, so the memories → summaries → graph phase order, `committed` eligibility boundary, and exact-#15740 scale gate are current authority.
- The implementation head does **not** exist yet: live `DataRecoveryActuatorService.applyHeal()` is still collection-scoped, while `healActionDispatch.mjs` still carries the retired `restore-delta-merge` vocabulary and no `targetSet` seam. #15740 is open, unassigned, and has no PR.

**Prior-art / collision sweep**

- #15691 and #15692 are prerequisites, not duplicates; their admission and bounded-batch mechanics are already merged.
- #15740 owns the actuator action; this ticket cannot implement or widen it.
- #15693 is the older provisional broad restore/delta reservation. Its collision/delta premise was superseded by the narrower graduated `restore-empty-target` contract; it is not authority for this measurement and is not a second implementation lane.
- The live open-PR search found no `restore-empty-target` implementation or competing measurement harness.
- Memory Core pre-brief has no graph node for `15695`; the origin session `cb60301d-74a4-4024-b80d-2f7efdbf9cd1` remains the relevant decision genealogy. Knowledge Base ticket retrieval does not contain #15695/#15740 yet, so it contributes no current authority.

**ADR successor-risk:** `adr-aligned`. This lane instruments the accepted contract and cannot select an action, mutate production, or redefine completion.

**ROI:** positive. Building the recorder/sampler and deterministic disposable controls now removes instrumentation work from #15740's critical path. Waiting for the actuator would force measurement mechanics and privileged recovery logic into one review cycle.

### Executable slice now

1. Add a disposable target-set measurement driver under the established `ai/scripts/benchmark/` substrate.
2. Fix the profile and report contract to the ticket's 5k/20k row counts, deterministic graph cardinality/bytes, named phase timings/progress, Node/store RSS, temporary-disk high-water mark, batch maximum, and provider-entrypoint census.
3. Unit-pin ordering, high-water aggregation, interruption/reconciliation control receipts, and the non-authoritative evidence label through injected synthetic controls.
4. Keep the exact action adapter and final real-store runs explicit:
   - `NOT_YET_MEASURED — BLOCKED_BY #15740 implementation head`

Synthetic/seam evidence will be labeled `control` and cannot satisfy the merge gate. Verdict: proceed with the instrument now; do not claim the final scale result until the exact #15740 head exists.

— Emmy (GPT-5.6 Sol Ultra, Codex)

- 2026-07-23T13:21:54Z @neo-gpt-emmy cross-referenced by #15755
- 2026-07-23T13:26:40Z @neo-gpt-emmy cross-referenced by PR #15756
### @neo-gpt-emmy - 2026-07-23T13:27:24Z

## Harness PR + synthetic-control receipt

**Harness leaf:** #15755  
**PR:** #15756  
**Exact harness head:** `287d34264e39d0d25ecc069061e0d51535ffbf27`

The disposable instrument is now in review. It fixes the two profiles, canonical target-set phases, progress/resource/provider report contract, interruption/reconciliation controls, and the injection seam for the eventual real recovery action.

### Real disposable file controls

All four runs used 4,096-dimensional explicit-vector fixtures under internally created OS-temporary roots. Every profile records 64 graph nodes, 63 graph edges, and 23,354 serialized graph bytes.

| Profile / scenario | Status | Wall ms | Node heap HWM bytes | Node RSS HWM bytes | Logical temp-file HWM bytes | Batch max | Provider calls |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 5k / full | completed | 902 | 51,435,560 | 173,228,032 | 166,397,154 | 250 | 0 |
| 20k / full | completed | 3,489 | 96,092,808 | 267,419,648 | 665,447,159 | 250 | 0 |
| 5k / interrupt before promotion | interrupted | 624 | 50,610,848 | 165,511,168 | 166,397,154 | 250 | 0 |
| 5k / reconcile after memories | completed | 901 | 59,330,592 | 183,369,728 | 166,397,154 | 250 | 0 |

The interruption checkpoint follows staged target-set validation and reports 166,397,154 retained logical bytes. The reconciliation checkpoint follows memories promotion and reports the same retained logical bytes. The full 20k control observes a 250-row maximum for both vector collections rather than conflating file cardinality with request size.

### Evidence boundary

These runs copy deterministic files and trace only the synthetic adapter seam. They start no Chroma or SQLite process. Therefore:

- Chroma RSS: `NOT_MEASURED`
- SQLite/shared-process RSS: `NOT_MEASURED`
- production embedding/re-embedding entrypoint census: `NOT_MEASURED`
- cloud deployment envelope comparison: `NOT_MEASURED`
- exact action scaling verdict: `NOT_YET_MEASURED — BLOCKED_BY #15740 implementation head`

The zero provider-call count belongs only to the declared synthetic seam; no production zero-call inference is made. Every report mechanically emits `authoritative=false` and `mergeGateSatisfied=false`. The compact zero-vector file representation also makes these controls instrument-validation receipts, not production disk-cost estimates.

#15695 remains open. Once #15740 has a clean PR head, its action adapter must drive this meter for both profiles and append the real Chroma/SQLite/provider/deployment receipt here before that PR may merge.

— Emmy (GPT-5.6 Sol Ultra, Codex)

- 2026-07-23T14:45:23Z @neo-gpt-emmy cross-referenced by PR #15757
### @neo-gpt-emmy - 2026-07-23T14:46:14Z

## Exact #15740 action receipt — real Chroma + SQLite

**PR:** #15757  
**Exact clean head:** `859b799f70e35f6e54cf07ff54832d5968ece88d`  
**Harness:** #15756, plus the exact-head production adapter in #15757  
**Host:** Darwin arm64, Node `v25.9.0`

Both reports recorded `repositoryHead === implementationHead`, `dirty=false`, `evidenceClass=exact-head-candidate`, and `status=completed`. Each run created its own OS-temporary bundle, Chroma persist root, collections, graph SQLite target, strict ledger, and production/staging paths. The adapter stopped its Chroma process with `exited=true`, `forced=false`, and `groupEmpty=true`; no live Memory Core path was opened.

### Target-set result

| Profile | Vector rows | Graph | Wall ms | Node heap HWM | Node RSS HWM | Chroma RSS HWM | Logical temp-disk HWM | Batch max | Provider calls |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 5k | 5,000 memories + 5,000 summaries | 64 nodes / 63 edges / 23,354 B | 44,070 | 201,818,192 B (192.5 MiB) | 475,381,760 B (453.4 MiB) | 347,766,784 B (331.7 MiB) | 271,531,896 B (259.0 MiB) | 250 | **0** |
| 20k | 20,000 memories + 20,000 summaries | 64 nodes / 63 edges / 23,354 B | 97,764 | 199,320,744 B (190.1 MiB) | 462,012,416 B (440.6 MiB) | 856,244,224 B (816.6 MiB) | 1,034,367,761 B (986.4 MiB) | 250 | **0** |

The fixtures use 4,096-dimensional explicit vectors. The production storage adapter accepts explicit vectors only; its sole embedding-capable collaborator is the guarded Chroma dummy embedding function. The trace covered `dummy_embedding_function.generate` and recorded zero calls in both profiles. No embedding or re-embedding dependency was introduced.

SQLite RSS is intentionally non-separable: `better-sqlite3` executes inside the measured Node adapter process and is included in Node RSS. Chroma is a separate process and produced 478 samples at 5k and 1,419 samples at 20k. Node and Chroma values are independent high-water marks and must not be summed as though they occurred in the same sample.

### Per-phase timings

| Exact production phase | 5k ms | 20k ms |
| --- | ---: | ---: |
| admission | 555 | 1,606 |
| stage memories | 10,269 | 20,622 |
| stage summaries | 10,446 | 20,849 |
| stage graph | 9 | 5 |
| validate staged target set | 7,313 | 13,549 |
| promote memories | 5,521 | 13,424 |
| promote summaries | 3,849 | 13,517 |
| promote graph | 7 | 5 |
| revalidate production | 5,508 | 13,455 |
| terminal settlement | 4 | 2 |

Every phase emitted separate start/completion receipts with its actual row counts. The first progress receipt was `admission/started`; the last was `terminal-settlement/completed`. All three staging targets validated before the first production promotion. Both runs strict-appended the full eight-transition chain, settled `committed`, and reported `serviceEligible=true`.

### Scaling and declared cloud envelope

At 4× vector rows:

- wall time rose 2.22×;
- Node heap and RSS remained effectively flat;
- Chroma RSS rose 2.46×;
- logical temporary disk rose 3.81×;
- vector request size remained bounded at 250 for both collections.

No unbounded Node-retention phase or provider call appeared. The material 20k envelope is Chroma RSS (816.6 MiB) plus 986.4 MiB of logical temporary disk.

Against the committed deployment limits, the isolated 20k action reached:

- 43.0% of the [1 GiB orchestrator limit](https://github.com/neomjs/neo/blob/859b799f70e35f6e54cf07ff54832d5968ece88d/ai/deploy/docker-compose.yml#L167-L170), leaving 583.4 MiB before unrelated daemon baseline;
- 39.9% of the [2 GiB Chroma limit](https://github.com/neomjs/neo/blob/859b799f70e35f6e54cf07ff54832d5968ece88d/ai/deploy/docker-compose.yml#L39-L43), leaving 1,231.4 MiB before unrelated Chroma baseline.

This is the requested action-specific scale receipt, not a claim that full steady-state container co-residency has been measured. The first cloud execution should compare cgroup baseline + action telemetry with those declared limits; no arbitrary local threshold is invented here.

### Provenance and verdict

Report SHA-256:

- 5k: `ddb6c26cb87164c610586b402341fa2fc1f3abccc710460c3b7ea63354004c88`
- 20k: `b4c248376ce5aff61903c56a98d601448df3a6000355811ab60add51d97a7b44`

The 5k/20k exact-action merge-gate evidence is now present on the exact #15757 head. The reports deliberately retain `authoritative=false` / `mergeGateSatisfied=false`: publication plus maintainer review—not the meter itself—grants authority. I found no unsafe scaling or unbounded phase that needs a new owning fix before review.

— Emmy (GPT-5.6 Sol Ultra, Codex)


### @neo-gpt-emmy - 2026-07-23T15:43:52Z

## Exact #15740 action receipt refresh — post-review head

**PR:** #15757  
**Exact clean head:** `b61528dcdf6b86fde6eaa5d569a8ba3b8a4099e1`  
**Reason for refresh:** the head now contains the requested crash-window containment for a vector promotion interrupted after canonical → parking but before shadow → canonical.  
**Host:** Darwin arm64, Node `v25.9.0`

Both published reports recorded `repositoryHead === implementationHead`, `dirty=false`, `evidenceClass=exact-head-candidate`, and `status=completed`. Each run created its own OS-temporary bundle, Chroma persist root, collections, graph SQLite target, strict ledger, and production/staging paths. The adapter stopped its Chroma process with `exited=true`, `forced=false`, and `groupEmpty=true`; no live Memory Core path was opened.

An initial sandboxed 5k attempt was rejected before report publication because `ps` was unavailable and the recorder correctly refused a candidate with zero separable Chroma RSS samples. The published runs below used enabled process inspection and include 357 / 1,454 Chroma samples.

### Crash-window containment proof

`npm run test-unit -- test/playwright/unit/ai/services/memory-core/helpers/restoreTargetSetStorage.spec.mjs test/playwright/unit/ai/services/memory-core/helpers/restoreEmptyTargetOperation.spec.mjs` — **24 passed**.

The parameterized storage tests construct both realistic interruption states:

- memories: ledger `staged`, canonical renamed to parking, shadow still present;
- summaries: memories already promoted and recorded, summaries canonical renamed to parking, summaries shadow still present.

For both roles, reconciliation returns unsafe without inferring a transition, the controller settles `failed-contained` with `serviceEligible=false`, a second resume is terminal/idempotent, and both shadow + parking remain intact. Fresh-target inspection classifies a missing canonical as non-fresh. Pre-promotion cleanup also requires positive store proof before deleting any shadow.

### Target-set result

| Profile | Vector rows | Graph | Wall ms | Node heap HWM | Node RSS HWM | Chroma RSS HWM | Logical temp-disk HWM | Batch max | Provider calls |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 5k | 5,000 memories + 5,000 summaries | 64 nodes / 63 edges / 23,354 B | 24,206 | 201,342,512 B | 451,428,352 B | 343,392,256 B | 271,468,720 B | 250 | **0** |
| 20k | 20,000 memories + 20,000 summaries | 64 nodes / 63 edges / 23,354 B | 98,226 | 199,786,320 B | 462,602,240 B | 855,539,712 B | 1,034,193,943 B | 250 | **0** |

The fixtures use 4,096-dimensional explicit vectors. The production storage adapter accepts explicit vectors only; its sole embedding-capable collaborator is the guarded Chroma dummy embedding function. The trace covered `dummy_embedding_function.generate` and recorded zero calls in both profiles.

SQLite RSS is intentionally non-separable: `better-sqlite3` executes inside the measured Node adapter process and is included in Node RSS. Chroma is a separate process. Node and Chroma values are independent high-water marks and must not be summed as though they occurred in the same sample.

### Per-phase timings

| Exact production phase | 5k ms | 20k ms |
| --- | ---: | ---: |
| admission | 315 | 930 |
| stage memories | 4,875 | 20,708 |
| stage summaries | 5,155 | 21,536 |
| stage graph | 5 | 4 |
| validate staged target set | 3,352 | 13,708 |
| promote memories | 3,338 | 13,601 |
| promote summaries | 3,340 | 13,610 |
| promote graph | 4 | 4 |
| revalidate production | 3,356 | 13,568 |
| terminal settlement | 2 | 3 |

Every phase emitted separate start/completion receipts with its actual row counts. All three staging targets validated before the first production promotion. Both runs strict-appended the full eight-transition chain, settled `committed`, and reported `serviceEligible=true`.

### Scaling and declared cloud envelope

At 4× vector rows:

- wall time rose 4.06×;
- Node heap remained flat (0.99×) and Node RSS rose 1.02×;
- Chroma RSS rose 2.49×;
- logical temporary disk rose 3.81×;
- vector request size remained bounded at 250 for both collections.

Against the committed deployment limits, the isolated 20k action reached 43.1% of the [1 GiB orchestrator limit](https://github.com/neomjs/neo/blob/b61528dcdf6b86fde6eaa5d569a8ba3b8a4099e1/ai/deploy/docker-compose.yml#L167-L170) and 39.8% of the [2 GiB Chroma limit](https://github.com/neomjs/neo/blob/b61528dcdf6b86fde6eaa5d569a8ba3b8a4099e1/ai/deploy/docker-compose.yml#L39-L43). This remains an action-specific high-water receipt, not a claim about full steady-state container co-residency.

### Provenance and verdict

Report SHA-256:

- 5k: `658bce3ff64324d18fe83cc03e50993c05b6cadaf73d68cc87b5ac161fe2eaea`
- 20k: `a32c8004c542f9aa547d333d0e10761dc82dc753d22d479a77fcfef04a863246`

The exact-action scale candidate is refreshed on the current #15757 head. Both reports deliberately retain `authoritative=false` / `mergeGateSatisfied=false`: publication plus maintainer review—not the meter itself—grants authority.

— Emmy (GPT-5.6 Sol Ultra, Codex)

### @neo-gpt-emmy - 2026-07-24T11:31:16Z

## Resolution audit — complete

Live revalidation on 2026-07-24 confirms this measurement gate is fully discharged.

| Acceptance surface | Evidence | Verdict |
|---|---|---|
| Disposable complete target-set harness | PR #15756 merged at exact reviewed head `287d34264e`; run-owned OS-temporary bundle, Chroma, graph, ledger, production, and staging paths | PASS |
| 5k profile | 5,000 explicit-vector rows in each of memories + summaries; graph = 64 nodes / 63 edges / 23,354 B | PASS |
| 20k profile | 20,000 explicit-vector rows in each of memories + summaries; graph cardinality/bytes unchanged and independently reported | PASS |
| Per-phase timing + progress | Admission, both vector stages, graph stage, staged validation, all three ordered promotions, production revalidation, and terminal settlement reported separately with first/last receipts | PASS |
| Resource high-water marks | Node heap/RSS, separable Chroma RSS, in-process SQLite attribution, logical temporary disk, row counts, and observed batch maximum all reported | PASS |
| Provider-free action | Production adapter traced the only embedding-capable collaborator; both profiles recorded zero embedding/re-embedding calls | PASS |
| Interruption + reconciliation controls | The harness receipt reports both controls and retained resource cost; 5k interruption/reconciliation each retained 166,397,154 logical bytes | PASS |
| Exact action head | Final receipt names `b61528dcdf6b86fde6eaa5d569a8ba3b8a4099e1`; that is PR #15757's reviewed and merged head. Synthetic reports remained explicitly non-authoritative. | PASS |
| Unsafe/unbounded routing | No unbounded Node-retention phase or provider dependency appeared. Vega's review did find a destructive two-rename crash window; it was contained with regression coverage at `b61528dcdf`, re-reviewed APPROVED, and merged before this closure. | PASS |

The final 5k→20k curve is preserved as evidence, not converted into an invented threshold: wall `4.06×`, Node heap `0.99×`, Node RSS `1.02×`, Chroma RSS `2.49×`, logical temp disk `3.81×`, maximum vector batch `250`, provider calls `0`.

All nine ACs are satisfied with no residual measurement gate. Closing as completed.



