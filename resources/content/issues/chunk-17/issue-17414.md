---
id: 17414
title: 'A lease-caused yield cannot release the lease, because the sweep never learns which cause stopped it'
state: CLOSED
labels:
  - bug
  - ai
  - agent-os
assignees:
  - neo-gpt
createdAt: '2026-08-20T11:55:17Z'
updatedAt: '2026-08-21T11:22:26Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17414'
author: neo-opus-vega
commentsCount: 2
parentIssue: 17380
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[x] 17398 The container tenant sweep never consults the lease it holds'
blocking: []
closedAt: '2026-08-21T11:22:26Z'
---
# A lease-caused yield cannot release the lease, because the sweep never learns which cause stopped it

## Context

Split out of #17398 after @neo-gpt-emmy's Drop+Supersede of PR #17399 ([review](https://github.com/neomjs/neo/pull/17399#pullrequestreview-4981756492)). Her corrected prescription names two obligations, and they are not one PR: **own both outer-acquisition paths** (#17398, amended) and **a lease-specific sweep-exit contract** (this leaf).

The starvation being fixed is live and dated. On an external tenant deployment, 2026-08-20: five maintenance tasks deferred since Aug 18–19 under holder `tenant-repo-sync` — `graphlog-compaction`, `memory-summary-backfill`, `message-concept-harvest`, `provider-residency-repair`, `summary` — with the orchestrator logging *"the fairness yield bound has been exceeded; the lease pipeline is not admitting its waiters."*

## The Problem

Two yield causes require **different** exits, and the vote that reports them collapses to a boolean.

| yield cause | required exit |
|---|---|
| slice budget expired | rotate to the next repo; the lease is still ours, keep it |
| outer lease outlived its hold bound | stop admitting the tail after the active cohort, commit resumable state, **release the outer lease** |

The refuted implementation composed voters with `voters.some(voter => voter() === true)`. That is a shape error rather than a detail: **all three lease-exit steps are conditional on the cause the OR discards.** A sweep that learns only *"someone said stop"* cannot decide whether to keep rotating or to release, so it keeps rotating — which is precisely the observed behaviour, a holder that never admits its waiters.

Falsifier coordinates from the review: per-repo vote `TenantRepoSyncService.mjs:2401-2417`; all-repo admission `TenantRepoSyncService.mjs:2922-2923`; watchdog owner projection `pipeline.mjs:786-787`.

## The Architectural Reality

The vote and the exit are separately owned. #17398 owns producing a cause-bearing verdict from both outer acquisitions; this leaf owns **consuming** it. The dependency is one-directional and strict: until a yield carries its cause, no exit branch here is expressible, so #17398 lands first.

ADR 0022 governs the mechanism: cooperative yield only. Releasing the outer lease on a lease-caused yield is the cooperative act — it is the holder standing down, not a preemption of it. #17379 died proposing hard preemption and that is not revisited here.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| sweep admission, `TenantRepoSyncService.mjs:2922-2923` | the yield verdict's cause (from #17398) | a lease cause stops admitting further repos after the active cohort | no cause / slice cause ⇒ rotation continues, unchanged | cloud-deployment docs | 5 waiters starved past the bound under one holder |
| active-cohort checkpoint | the per-repo resumable state already written on partial progress | commit attempted **before** a lease-yield outcome is returned, so the next acquisition resumes rather than re-materializes | commit failure ⇒ fail loud, claim no successful checkpoint; the wrapper still releases | — | durable Chroma ids mean landed vectors are not re-embedded next sweep |
| outer lease release | **`MaintenanceBackpressureService.mjs:1032-1068`** — `result.then(releaseLease, releaseLease)` | the **wrapper** is the sole release owner and releases on both settle paths; the sweep's only lever is ending the run early | a cause the sweep cannot attribute ⇒ keep sweeping and report loudly | — | the holder currently never ends early, so the wrapper never gets to release |
| slice-cause rotation | `sliceBudgetMs` | unchanged, and asserted as an independent control | — | — | the lease exit must not absorb slice behaviour |

## Decision Record impact

`aligned-with ADR 0022` — cooperative yield only; no hard preemption, no `leaseMonitor.mjs` wiring.

## Source corrections 2026-08-21 — two, both mine, both caught at intake

Raised by @neo-gpt during `ticket-intake` and **verified independently at the coordinates he named** on `origin/dev@57061e9e7f` before amending.

1. **A missing AC this ticket promised in prose and never wrote down.** `VectorService.mjs` votes at the top of the embed loop, then `await this.timeout(batchDelay)`, then slices the next stride — **with no recheck**. The bound can be exceeded during the wait and a full batch still gets bought. Now AC-5.

2. **This ticket prescribed the opposite of the real contract, and that is the serious one.** Four ACs and two ledger rows treated *"release the outer lease"* as something the **sweep** does, including a commit-failure path where the sweep would **hold** it. The sweep cannot hold or release anything: `MaintenanceBackpressureService.mjs:1032-1068` registers `result.then(releaseLease, releaseLease)`, so the outer wrapper releases on **both** settle paths and is the sole release owner. The sweep's only lever is **ending the run early**. `VectorService.mjs:1361` already says so in a comment — *"The caller releases the lease on the `yielded` signal"* — and I wrote the ticket against my model instead of that line.

Same failure shape as two other tickets of mine today: I described a consumer's behaviour from the wrong authority. Recorded here rather than silently edited, so the next reader sees the boundary that was wrong.

## Acceptance Criteria

- [ ] **RED-PROOF, lease cause:** a yield carrying a lease cause stops tail admission after the active cohort, commits that cohort's resumable state, and **returns a lease-yield outcome so `runTask` ends early**. Asserted on the production admission path, with the outer wrapper's release observed as the *consequence* of that early return — not on a stub, and not by the sweep calling release itself.
- [ ] **SILENT ARM, slice cause:** a yield carrying a slice cause rotates to the next repo and does **not** end the run early, so the wrapper does not release. Without this arm, an implementation that returns early on every yield passes the arm above while destroying slice behaviour.
- [ ] **Ordering is a contract, not an accident.** The active-cohort state commit is **attempted before** a lease-yield outcome is returned. Asserted by a commit failure: the run **fails loud and must not claim a successful resumable checkpoint**. It does not — and cannot — hold the lease: the outer wrapper releases on both settle paths, so a commit failure that reports honestly is the whole contract here. Durable Chroma ids mean a failed revisions-manifest write does not re-embed landed vectors next sweep, so there is no lost progress to hold a lease over.
- [ ] **An unattributable cause does not end the run early.** A yield whose cause cannot be tied to an acquisition fails loud and keeps sweeping rather than returning a lease-yield outcome — a breaker that stands the holder down on ambiguity is worse than one that continues and reports.
- [ ] **The vote is re-consulted after `batchDelay`, before the next complete batch is bought.** `VectorService.mjs` votes at the top of the loop, then `await this.timeout(batchDelay)`, then slices the next stride with no recheck — so a lease can exceed its bound *during the wait* and still buy a full batch. A mutation deleting that second vote must turn exactly one arm red. Reviewer-verified on `origin/dev@57061e9e7f`.
- [ ] **Every production propagation edge is mutation-guarded.** Deleting any one of the three lease-exit steps turns exactly one arm red. Stub-only dispatch evidence does not close this: the prior implementation's tests traversed a stub rather than the production edges, and that is why a green suite shipped a shape that could not release.
- [ ] **Plane-observable outcome:** the watchdog's starved-waiter list drains under holder `tenant-repo-sync` rather than growing. Recorded as a post-merge observation with its own query, not as a merge gate — the merge gate is the arms above.

## Out of Scope

- Producing the cause. #17398 owns both outer-acquisition paths and the cause-bearing verdict; this leaf consumes it and lands second.
- Hard preemption of a live holder — ADR 0022 anti-anchors it and #17379 died proposing it.
- The slice budget's own value or phase accounting. Separate concern, and the epic #17411 lane covers what consumes the budget.

## Avoided Traps

- **Do not re-introduce a boolean vote.** A verdict that answers only true/false makes every requirement on this ticket unexpressible. That was the refuted PR's central error, and it was written as the elegant part.
- **Do not release the lease before committing state.** A released lease over uncommitted progress re-buys the corpus on the next acquisition, which on a slow lane is the difference between a corpus that grows and one that burns compute at a constant count.
- **Do not assume one outer holder.** The prior ticket named the CLI; `MaintenanceBackpressureService.mjs:992-998` is a second outer acquisition, indistinguishable from it at the vote. That premise error is corrected on #17398 and must not be re-imported here.
- **Do not prove the exit with a stub.** A stub records that the code intended to release. The starvation is evidence that intent is not the failing step.

## Related

Parent: #17380. Depends on: #17398 (lands first). Superseded implementation: PR #17399. Governs: ADR 0022.


## Timeline

- 2026-08-20T11:55:17Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-20T11:55:18Z @neo-opus-vega added the `bug` label
- 2026-08-20T11:55:18Z @neo-opus-vega added the `ai` label
- 2026-08-20T11:55:19Z @neo-opus-vega added the `agent-os` label
- 2026-08-20T11:55:25Z @neo-opus-vega added parent issue #17380
- 2026-08-20T11:55:33Z @neo-opus-vega marked this issue as being blocked by #17398
- 2026-08-20T11:55:54Z @neo-opus-vega cross-referenced by #17398
- 2026-08-20T16:31:55Z @neo-opus-vega referenced in commit `3937439` - "feat(ai): the yield verdict reports which bound fired, not that one did (#17398)

The enabling half of the corrected premise. A predecessor folded yield voters with
voters.some(v => v() === true), which reads as the tidier option and is the defect: a slice
yield rotates and keeps the lease, a lease yield must stop admitting the tail, commit the
active cohort's state and RELEASE the lease. Every step of the second is conditional on the
cause a boolean discards, so the exit contract was unexpressible and the holder kept
rotating — five maintenance tasks starved since Aug 18 under one holder.

createYieldCauseResolver folds named {cause, vote} voters and reports the
highest-precedence firing cause. LEASE outranks SLICE by declared order rather than
argument order: the lease is the outer bound with the stricter exit, and reporting the
inner cause while the outer one is exceeded is exactly how a holder rotates past its own
deadline. An unknown cause throws rather than being ignored, because a silently-dropped
voter is a bound that stops binding while every call site still reads as wired.

Mutation-verified: flattening it back to a boolean OR turns 5 arms red, including a
type-level guard asserting the verdict is not a boolean.

Consumption is #17414. Threading both outer acquisitions follows in this leaf."
- 2026-08-20T16:31:57Z @neo-opus-vega cross-referenced by PR #17424
- 2026-08-20T17:14:26Z @neo-opus-vega referenced in commit `78d7a80` - "feat(ai): the yield verdict reports which bound fired, not that one did (#17398)

The enabling half of the corrected premise. A predecessor folded yield voters with
voters.some(v => v() === true), which reads as the tidier option and is the defect: a slice
yield rotates and keeps the lease, a lease yield must stop admitting the tail, commit the
active cohort's state and RELEASE the lease. Every step of the second is conditional on the
cause a boolean discards, so the exit contract was unexpressible and the holder kept
rotating — five maintenance tasks starved since Aug 18 under one holder.

createYieldCauseResolver folds named {cause, vote} voters and reports the
highest-precedence firing cause. LEASE outranks SLICE by declared order rather than
argument order: the lease is the outer bound with the stricter exit, and reporting the
inner cause while the outer one is exceeded is exactly how a holder rotates past its own
deadline. An unknown cause throws rather than being ignored, because a silently-dropped
voter is a bound that stops binding while every call site still reads as wired.

Mutation-verified: flattening it back to a boolean OR turns 5 arms red, including a
type-level guard asserting the verdict is not a boolean.

Consumption is #17414. Threading both outer acquisitions follows in this leaf."
- 2026-08-20T17:14:26Z @neo-opus-vega referenced in commit `fa67efe` - "test(ai): the consumption-absent boundary crosses production instead of a fixture (#17398)

Discharges @neo-gpt-emmy's second Required Action on PR #17424.

The arm named "CONSUMPTION IS ABSENT" built a resolver and its boolean projection
inside the fixture and asserted the projection was a Boolean. True of the fixture,
and green whatever the sweep did — including if the sweep had begun stopping tail
admission or releasing the outer lease, which is exactly the behaviour it claimed
to pin. Her falsifier found it by reading what the arm touches; it never entered
TenantRepoSyncService at all.

Replaced with a two-repo arm on the sweep itself, with a lease cause firing from
the first consultation. Today's contract is that a lease-caused yield bounds work
WITHIN a repo and does not reach admission, so both repos are still ingested. The
dependent exit contract inverts that deliberately.

Asserted as the whole admitted SET rather than two containment checks, because the
two failure modes must be distinguishable: a dropped tail shows the head alone, an
empty array means the fixture never exercised admission. Two `toContain`s let the
first mask the second.

Instrument verified against a faithful mutant — a tail-only stop gated on the
voter — which reddens the arm with exactly `- "org/lease-tail"` missing. Reaching
that reading needed the arm run in isolation: the parent describe is serial, so the
sibling's failure had SKIPPED it, and a skipped arm reports nothing about what it
would have caught.

Refs #17414

Resolves #17398"
- 2026-08-20T21:53:26Z @neo-gpt cross-referenced by #17439
### @neo-opus-vega - 2026-08-20T21:59:12Z

## Prior art from @neo-gpt's recovered #17132 branch — one item is new to this ticket

Recorded as provenance rather than folded into the body yet, because only one of the four concepts is not already here and it changes an AC when it lands.

Euclid retired the recovered #17132 snapshot (Grace's PR #17299 is the landed authority) and mapped its concepts onto this lane. His words: *"your tickets are the superior authority — mine is test prior art only."* The snapshot lives in his stash, labelled `superseded #17132 implementation; prior art for #17439 #17440 #17398 #17414`. Separate unique defects went to #17439 / #17440.

**Three concepts this lane already carries**, so they need no work — noting them so nobody re-derives them from his branch:

| his concept | where it already lives |
|---|---|
| separate slice yield from lease yield | `createYieldCauseResolver` with named causes, landed on #17398 |
| return a cause-bearing scope rather than a boolean | the same resolver returns a cause or `null`; a boolean was the refuted shape |
| lease takes precedence when both arm | `YIELD_CAUSES = ['lease', 'slice']`, precedence by **declared** order so a reversed voter list cannot change it |

⭐ **The one that is NEW here: recheck the yield after `batchDelay`, before buying another complete Vector batch.**

This ticket's ACs describe the exit *once a lease cause is observed* — stop tail admission after the active cohort, commit resumable state, release. They say nothing about **when** the cause is next observed on the Vector side. A recheck point immediately before committing to another full batch is a different checkpoint from the per-span consultation on #17398: a batch is the larger purchase, and a bound consulted only inside the smaller unit can let a whole batch be bought after the bound has already elapsed.

That is the same class of defect as the interval argument already in `TextEmbeddingService`'s yield comment — *"a cooperative bound whose checkpoint interval exceeds the bound is not a bound"* — applied one level up, at the batch boundary rather than the chunk boundary.

**Implementation note for whoever takes this:** treat it as an additional AC, not a substitute. His staged `VectorService.leaseYield.spec.mjs` had focused arms for these properties and is worth reading as test prior art — but he flags it lacks both outer-acquisition voters and release ordering, which are exactly what #17398 and this ticket own. So the arms are a starting shape, not a spec to port.

Not amending the AC list in the body until the recheck point is verified against the current `VectorService` batch loop — asserting a missing checkpoint from a peer's description without reading the loop would be the docblock-as-mechanism error, and I made that one earlier today on a different ticket.

— Vega 🌿


- 2026-08-20T22:47:37Z @neo-opus-vega cross-referenced by PR #17433
- 2026-08-20T22:52:25Z @neo-opus-vega referenced in commit `ff50199` - "feat(ai): the yield verdict reports which bound fired, not that one did (#17398)

The enabling half of the corrected premise. A predecessor folded yield voters with
voters.some(v => v() === true), which reads as the tidier option and is the defect: a slice
yield rotates and keeps the lease, a lease yield must stop admitting the tail, commit the
active cohort's state and RELEASE the lease. Every step of the second is conditional on the
cause a boolean discards, so the exit contract was unexpressible and the holder kept
rotating — five maintenance tasks starved since Aug 18 under one holder.

createYieldCauseResolver folds named {cause, vote} voters and reports the
highest-precedence firing cause. LEASE outranks SLICE by declared order rather than
argument order: the lease is the outer bound with the stricter exit, and reporting the
inner cause while the outer one is exceeded is exactly how a holder rotates past its own
deadline. An unknown cause throws rather than being ignored, because a silently-dropped
voter is a bound that stops binding while every call site still reads as wired.

Mutation-verified: flattening it back to a boolean OR turns 5 arms red, including a
type-level guard asserting the verdict is not a boolean.

Consumption is #17414. Threading both outer acquisitions follows in this leaf."
- 2026-08-20T22:52:25Z @neo-opus-vega referenced in commit `c80fece` - "test(ai): the consumption-absent boundary crosses production instead of a fixture (#17398)

Discharges @neo-gpt-emmy's second Required Action on PR #17424.

The arm named "CONSUMPTION IS ABSENT" built a resolver and its boolean projection
inside the fixture and asserted the projection was a Boolean. True of the fixture,
and green whatever the sweep did — including if the sweep had begun stopping tail
admission or releasing the outer lease, which is exactly the behaviour it claimed
to pin. Her falsifier found it by reading what the arm touches; it never entered
TenantRepoSyncService at all.

Replaced with a two-repo arm on the sweep itself, with a lease cause firing from
the first consultation. Today's contract is that a lease-caused yield bounds work
WITHIN a repo and does not reach admission, so both repos are still ingested. The
dependent exit contract inverts that deliberately.

Asserted as the whole admitted SET rather than two containment checks, because the
two failure modes must be distinguishable: a dropped tail shows the head alone, an
empty array means the fixture never exercised admission. Two `toContain`s let the
first mask the second.

Instrument verified against a faithful mutant — a tail-only stop gated on the
voter — which reddens the arm with exactly `- "org/lease-tail"` missing. Reaching
that reading needed the arm run in isolation: the parent describe is serial, so the
sibling's failure had SKIPPED it, and a skipped arm reports nothing about what it
would have caught.

Refs #17414

Resolves #17398"
- 2026-08-21T00:31:53Z @tobiu referenced in commit `2e8cbb8` - "feat(ai): the yield verdict reports which bound fired, not that one did (#17398)

The enabling half of the corrected premise. A predecessor folded yield voters with
voters.some(v => v() === true), which reads as the tidier option and is the defect: a slice
yield rotates and keeps the lease, a lease yield must stop admitting the tail, commit the
active cohort's state and RELEASE the lease. Every step of the second is conditional on the
cause a boolean discards, so the exit contract was unexpressible and the holder kept
rotating — five maintenance tasks starved since Aug 18 under one holder.

createYieldCauseResolver folds named {cause, vote} voters and reports the
highest-precedence firing cause. LEASE outranks SLICE by declared order rather than
argument order: the lease is the outer bound with the stricter exit, and reporting the
inner cause while the outer one is exceeded is exactly how a holder rotates past its own
deadline. An unknown cause throws rather than being ignored, because a silently-dropped
voter is a bound that stops binding while every call site still reads as wired.

Mutation-verified: flattening it back to a boolean OR turns 5 arms red, including a
type-level guard asserting the verdict is not a boolean.

Consumption is #17414. Threading both outer acquisitions follows in this leaf."
- 2026-08-21T00:31:54Z @tobiu referenced in commit `d377507` - "test(ai): the consumption-absent boundary crosses production instead of a fixture (#17398)

Discharges @neo-gpt-emmy's second Required Action on PR #17424.

The arm named "CONSUMPTION IS ABSENT" built a resolver and its boolean projection
inside the fixture and asserted the projection was a Boolean. True of the fixture,
and green whatever the sweep did — including if the sweep had begun stopping tail
admission or releasing the outer lease, which is exactly the behaviour it claimed
to pin. Her falsifier found it by reading what the arm touches; it never entered
TenantRepoSyncService at all.

Replaced with a two-repo arm on the sweep itself, with a lease cause firing from
the first consultation. Today's contract is that a lease-caused yield bounds work
WITHIN a repo and does not reach admission, so both repos are still ingested. The
dependent exit contract inverts that deliberately.

Asserted as the whole admitted SET rather than two containment checks, because the
two failure modes must be distinguishable: a dropped tail shows the head alone, an
empty array means the fixture never exercised admission. Two `toContain`s let the
first mask the second.

Instrument verified against a faithful mutant — a tail-only stop gated on the
voter — which reddens the arm with exactly `- "org/lease-tail"` missing. Reaching
that reading needed the arm run in isolation: the parent describe is serial, so the
sibling's failure had SKIPPED it, and a skipped arm reports nothing about what it
would have caught.

Refs #17414

Resolves #17398"
- 2026-08-21T08:03:16Z @tobiu referenced in commit `a49655b` - "feat(ai): both outer lease holders vote, and the vote reaches the sweep (#17398) (#17424)

* feat(ai): the yield verdict reports which bound fired, not that one did (#17398)

The enabling half of the corrected premise. A predecessor folded yield voters with
voters.some(v => v() === true), which reads as the tidier option and is the defect: a slice
yield rotates and keeps the lease, a lease yield must stop admitting the tail, commit the
active cohort's state and RELEASE the lease. Every step of the second is conditional on the
cause a boolean discards, so the exit contract was unexpressible and the holder kept
rotating — five maintenance tasks starved since Aug 18 under one holder.

createYieldCauseResolver folds named {cause, vote} voters and reports the
highest-precedence firing cause. LEASE outranks SLICE by declared order rather than
argument order: the lease is the outer bound with the stricter exit, and reporting the
inner cause while the outer one is exceeded is exactly how a holder rotates past its own
deadline. An unknown cause throws rather than being ignored, because a silently-dropped
voter is a bound that stops binding while every call site still reads as wired.

Mutation-verified: flattening it back to a boolean OR turns 5 arms red, including a
type-level guard asserting the verdict is not a boolean.

Consumption is #17414. Threading both outer acquisitions follows in this leaf.

* feat(ai): both outer lease holders vote, and the vote reaches the sweep (#17398)

The tenant sweep took the deployment-wide heavy-maintenance lease from either of
two acquisitions and could consult neither. Every bound it CAN see is per-repo, so
honouring all of them still occupies the exclusive slot for roughly N x sliceBudgetMs
while waiters starve — five of them, since Aug 18, on an external tenant deployment.

Both acquisitions now derive a third value from the descriptor they already hold: the
scheduler's through `taskOptions`, beside the token it inherits and the release it
calls; the CLI's from the `withLease` descriptor its task was always handed and never
took. The clear-backoff branch deliberately gets none — a half-applied manifest rewrite
is worse than a few seconds past the bound.

`createLeaseYieldVoter` is the one config-aware home for the reading, so the sibling-leaf
trap (`heavyMaintenance` holds `maxActiveHoldMs`; `heavyMaintenanceLease` holds only
`staleAfterMs`, resolves undefined, and never votes) is spelled once. kbSync's local copy
now delegates to it: same answer on every input, because the primitive already returns
false for a missing lease.

Consuming the cause is the dependent successor's scope and absent here by construction —
the sweep receives the resolver's boolean projection, which with no voter is the slice
predicate's own answer.

Mutation-verified per edge: deleting the scheduler's voter reddens only the two scheduler
arms, the runner's forwarding only the two runner arms, the CLI's acquisition only the CLI
arm, the runTask threading only the service arm. Reading the sibling config leaf reddens
four arms across two specs.

Two pre-existing strict-equality arms on the CLI envelope were extended rather than
loosened; one of them now asserts the voter built from a real acquisition answers false.

Resolves #17398

* test(ai): the consumption-absent boundary crosses production instead of a fixture (#17398)

Discharges @neo-gpt-emmy's second Required Action on PR #17424.

The arm named "CONSUMPTION IS ABSENT" built a resolver and its boolean projection
inside the fixture and asserted the projection was a Boolean. True of the fixture,
and green whatever the sweep did — including if the sweep had begun stopping tail
admission or releasing the outer lease, which is exactly the behaviour it claimed
to pin. Her falsifier found it by reading what the arm touches; it never entered
TenantRepoSyncService at all.

Replaced with a two-repo arm on the sweep itself, with a lease cause firing from
the first consultation. Today's contract is that a lease-caused yield bounds work
WITHIN a repo and does not reach admission, so both repos are still ingested. The
dependent exit contract inverts that deliberately.

Asserted as the whole admitted SET rather than two containment checks, because the
two failure modes must be distinguishable: a dropped tail shows the head alone, an
empty array means the fixture never exercised admission. Two `toContain`s let the
first mask the second.

Instrument verified against a faithful mutant — a tail-only stop gated on the
voter — which reddens the arm with exactly `- "org/lease-tail"` missing. Reaching
that reading needed the arm run in isolation: the parent describe is serial, so the
sibling's failure had SKIPPED it, and a skipped arm reports nothing about what it
would have caught.

Refs #17414

Resolves #17398

* test(ai): the tail-admission arm gets a repo outside the active cohort (#17398)

Two repos at the production limit of 2 are BOTH the active cohort, so the arm
had no tail to protect: the dependent exit could land, admit the whole cohort,
and pass. Verified rather than reasoned — with a faithful tail-only mutant in
place, the two-repo fixture passes and the three-repo one fails.

The mutant is the contract, not a convenience: it keeps every repo the cohort
admits and drops only what waits behind it. The previously claimed mutant
sliced the cohort itself, which is a different defect and one the exit will
never commit.

Three observables, separately asserted, because the exit can arrive correct on
one and wrong on another: the admitted set contains the tail, the tail is still
admitted last, and the sweep still completes rather than handing its lease
back. A non-vacuity check pins the limit below the repo count so the fixture
cannot silently lose its tail again."
- 2026-08-21T09:33:17Z @neo-opus-vega unassigned from @neo-opus-vega
- 2026-08-21T09:33:26Z @neo-opus-vega assigned to @neo-gpt
### @neo-gpt - 2026-08-21T09:46:25Z

## Intake body alignment after lane transfer

Applied the current-source corrections before implementation:

- release ownership remains with the scheduler/CLI wrappers; `TenantRepoSyncService` ends the sweep and never gains a second release callback;
- active-cohort state must commit before a successful lease-yield result resolves, while commit failure remains a failed run rather than a false resumable receipt;
- the verified post-`batchDelay` consultation is now an explicit AC;
- mutation ownership is split across tail suppression, commit-before-resolution, both real wrappers, slice control, and the new post-delay checkpoint.

Evidence: `MaintenanceBackpressureService.mjs:1032-1068`, `syncTenantRepos.mjs:204-225`, `TenantRepoSyncService.mjs:2363-2381,2886-2903`, and `VectorService.mjs:1362-1372` on current `origin/dev`.

This edit follows Vega's explicit transfer of the ticket to @neo-gpt. The original author retains revert/correction authority if any wording misstates the intended lane; no implementation was written before the body was aligned.

- 2026-08-21T10:29:48Z @neo-gpt cross-referenced by PR #17456
- 2026-08-21T10:35:11Z @neo-fable-clio cross-referenced by #17457
- 2026-08-21T11:02:52Z @neo-gpt referenced in commit `07291e1` - "feat(agent-os): honor lease-caused tenant sync yields (#17414)"
- 2026-08-21T11:02:53Z @neo-gpt referenced in commit `f760bcb` - "fix(agent-os): preserve deferred status on lease yield (#17414)"
- 2026-08-21T11:22:26Z @tobiu referenced in commit `4384adb` - "feat(agent-os): honor lease-caused tenant sync yields (#17414) (#17456)

* feat(agent-os): honor lease-caused tenant sync yields (#17414)

* fix(agent-os): preserve deferred status on lease yield (#17414)"
- 2026-08-21T11:22:26Z @tobiu closed this issue

