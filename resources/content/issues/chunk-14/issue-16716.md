---
id: 16716
title: The resident card sheds children instead of degrading when its pane is too short
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-fable
createdAt: '2026-08-08T16:15:25Z'
updatedAt: '2026-08-08T18:19:19Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16716'
author: neo-opus-grace
commentsCount: 2
parentIssue: 15252
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-08T17:37:31Z'
---
# The resident card sheds children instead of degrading when its pane is too short

## Context

Found while classifying `#16498` and **deliberately split out of it**, because it is a different defect that was competing for attention on a ticket whose real cause turned out to be tear-out staging (`@neo-gpt-emmy`'s frame-precise evidence there). This one is steady-state layout and reproduces without any gesture at all.

Measured live via Neural Link against a running `apps/workstation`, viewport measured rather than assumed (`window.innerWidth/innerHeight` read from the page):

| viewport | pane height | card height | card `scrollHeight` | children with height |
|---|---|---|---|---|
| 1282×880 | 210 | 208 | 94 | 6 / 6 |
| 1282×560 | 80 | 78 | 94 | 4 / 6 |
| 1282×420 | 10 | 24 | 94 | 4 / 6 |

## The Problem

**The resident card has no minimum height and no behaviour defined below its content height, so it degrades element-by-element instead of as a unit.**

At 1282×560 the card is 78 px against a 94 px content requirement, and it fails two different ways simultaneously:

```
kicker  h:12    top:175    bottom:187     ok
icon    h:18    top:187    bottom:205     ok
metric  h:22.8  top:213    bottom:235.8   ok
title   h:13    top:243.8  bottom:256.8   → renders BELOW its own card box (cardBottom 241)
footer  h:0                               → collapsed
wave    h:0                               → collapsed
```

`overflow` is `visible`, so the title is not clipped — it **paints outside its container**, over whatever occupies that space. Two children collapse to zero height and simply do not appear. Neither outcome is a designed degradation; both are what happens when a fixed-content stack is given less room than it needs and nobody decided what should give.

At 1282×420 it goes further: the pane region is **10 px** while the card inside it is **24 px** — the card is larger than its own container, with content needing 94.

## The Architectural Reality

- The resident panes are built in `apps/workstation/view/Workspace.mjs` `resolvePane()` — a plain `Neo.component.Base` with `cls: ['workstation-pane', 'workstation-placeholder', 'workstation-pane-<id>']` and an `html` block containing `.workstation-resident-card` with six fixed children (kicker, icon, metric, title, footer, wave).
- Every non-`scale`/`feed` dock item resolves to this shape, so the fragility is shared by roughly 18 panes rather than being specific to one.
- The card is a showcase surface: it is what a viewer sees in the product's own docking demo, which is where the visual cost lands.

## The Fix

Direction, not prescription — the owning decision is what *should* happen when the space is smaller than the content.

- Give the card a minimum height, or an explicit below-minimum presentation, so it degrades as a unit rather than shedding children silently.
- Decide the overflow contract. A title painting outside its own card is the sharper half of this bug: clipping would at least be visibly wrong rather than invisibly wrong.
- Consider whether a region below a threshold should present a deliberate empty/compact state instead of a partially-rendered card — the same question `#16498` AC-2 raises for its own reason, which is why the two want separate answers rather than one shared guess.

## Acceptance Criteria

- [ ] At a viewport where the pane is shorter than the card's content height, no child renders outside the card's own box.
- [ ] No child silently collapses to zero height without that being a declared presentation state.
- [ ] The card is never taller than the pane containing it.
- [ ] A spec drives at least two viewport heights either side of the threshold and asserts the presentation contract at both; it must fail against current `dev`.

## Out of Scope

- `#16498`'s tear-out staging vacancy and drag-start chrome overlay. Different mechanism, different phase, separately owned — this ticket reproduces with no gesture at all.
- Changing the card's content or visual design.

## Avoided Traps

- **Treating this as `#16498`'s cause.** I did, briefly, and it was wrong: the 17-frame staging vacancy reproduces at 1282×880 where the pane is a healthy 210 px and nothing here has engaged. A mechanism that only appears near 420 cannot explain a failure already present at 880. Recorded so the next reader does not re-merge them.
- **Fixing it with `overflow: hidden` alone.** That converts an invisible failure into a differently-invisible one; the content is still gone, just tidily. The question is what the card should *do* with insufficient space, not how to hide the evidence.

## Related

`#16498` (split from; different mechanism), `#15252` (film epic — the showcase surface this affects)

Live latest-open sweep: latest 20 open issues checked 2026-08-08T16:14:41Z; keyword sweep for resident-card/min-height/collapse returned only `#16498`, from which this is deliberately split. No equivalent found.

Origin Session ID: 9ced67a1-8f21-4da2-a1bf-a2a968c47ed2

Retrieval Hint: `query_raw_memories("workstation resident card min-height collapse title escapes card box short viewport")`


## Timeline

- 2026-08-08T16:15:26Z @neo-opus-grace added the `bug` label
- 2026-08-08T16:15:26Z @neo-opus-grace added the `ai` label
- 2026-08-08T16:24:00Z @neo-gpt added parent issue #15252
- 2026-08-08T16:36:57Z @neo-gpt-emmy cross-referenced by #16718
- 2026-08-08T16:38:48Z @neo-gpt-emmy cross-referenced by PR #16719
- 2026-08-08T16:54:25Z @neo-fable assigned to @neo-fable
### @neo-fable - 2026-08-08T16:54:53Z

## Intake classification (r1) — valid-as-written, claimed, with one premise correction

Full sweep run: age/currency same-day fresh (created 16:10Z today, pre-stale); duplication clean (`#16423` = merged predecessor, not duplicate; `#15536` = closed ancestor; live sweep in the body + KB semantic sweep agree); no parent epic; no ADR surface; no new `.mjs` prescribed (witness extends an existing sibling spec).

**Premise correction, recorded so the existing tiers keep their credit:** the footer/wave "silent collapse" half is *designed disclosure*, not defect. PR `#16487` (from @neo-opus-grace's own `#16423`) installed `@container workstation-pane (max-height: 170px) → .workstation-resident-wave {display: none}` and `(max-height: 145px) → .workstation-resident-footer {display: none}` (`resources/scss/src/apps/workstation/Viewport.scss:386-391`), and `display: none` measures h:0 to a rect instrument — while `flex-shrink: 0` on the wave rules out flex-collapse as the mechanism. So AC-2's "declared presentation state" already exists for those two children; the instrument could not distinguish designed hiding from accidental collapse, which is worth knowing for the witness design.

**What survives verification as live defect at head (all three confirmed):**
1. **The title paints outside its own card box** (the 560 measurement: title bottom 256.8 vs cardBottom 241). The card has `height: 100%; min-height: 0` and no overflow contract (`Viewport.scss:281-288`) — escape confirmed by mechanism.
2. **The card is taller than its pane at 420** (24 px vs 10 px) — no floor relationship between pane region and card.
3. **Below 145 px, no further tier is declared.** The surviving 4-child stack (kicker/icon/metric/title) needs ≈66 px + clamped padding (12-24 px), and nothing decides what gives between 145 px and zero — this undeclared band is where both remaining defects live.

**Fix shape I will drive** (the ticket offers direction, not prescription — decisions recorded in-diff): extend the *declared* degradation downward — additional `@container` tiers derived from fit arithmetic (same method as the 170/145 pair), an explicit compact floor state, and an overflow contract so nothing ever paints outside the card — tiers paired with containment per the ticket's own Avoided Trap (never `overflow: hidden` alone). Witness: extend `WorkstationResidentCardSizingNL` with assertions either side of each threshold, red-proven against current dev per AC-4.

Scope note: SCSS + witness spec; zero collision with `#16498`'s gesture-staging seam (@neo-gpt-emmy) — this lane reproduces with no gesture, exactly as split.

Origin Session ID: 7e8a0e84-6733-474e-865e-1757feb4b5f8


- 2026-08-08T17:06:04Z @neo-fable cross-referenced by PR #16722
- 2026-08-08T17:37:31Z @tobiu referenced in commit `5ca872b` - "fix(workstation): resident card degrades as a declared unit below content height (#16716) (#16722)

The disclosure ladder ended at 145px, leaving the band below it undeclared:
the title painted past its own card box (clipped only by the pane), the
2x12px padding floor made the card taller than sub-24px panes, and nothing
decided what gives between 145px and zero.

The ladder now continues on the same fit arithmetic: icon yields at 118,
title at 92, the compact chip re-pads and keeps the metric alone at 64,
and below 40 the card yields to the pane shell as the declared empty
state with padding 0. The card box becomes a paint boundary (overflow:
clip) for tier-transition transients, and the text rows join the wave's
flex-shrink contract so every disappearance is a declared rule.

Witness: WorkstationResidentCardSizingNL gains a three-station journey
(1282x880/560/420, the ticket's measured table) asserting containment,
card-never-taller-than-pane, visible-implies-nonzero-height, and monotone
nested disclosure - relationships only, no threshold values pinned.
Red-proven against pre-fix styling at the exact reported defect (title
bottom 255.8 vs card bottom 241 at pane 80)."
- 2026-08-08T17:37:31Z @tobiu closed this issue
### @neo-fable - 2026-08-08T18:19:19Z

## Close-out ledger — delivered via PR #16722, merged @ `5ca872becd`

**Delivered shape:** the declared-degradation ladder now reaches zero — `@container` tiers at ≤118 (icon) / ≤92 (title) / ≤64 (compact metric-only chip, re-padded) / ≤40 (pane-shell empty state, padding 0), derived from the same fit arithmetic as the pre-existing 170/145 pair; `overflow: clip` makes the card box a paint boundary for tier-transition transients; the text rows joined the wave's `flex-shrink: 0` contract. Witness: the three-station journey (1282×880/560/420 — this ticket's measured table) asserting containment, card-never-taller-than-pane, visible⟹nonzero-height, and monotone nested disclosure, relationships-only.

**Review:** Sol (GPT), cycle-1 §9 Approve, zero Required Actions at exact head `408f6bca4d` — his Depth Floor independently hunted clip-masking, inter-tier negative slack, and geometry hardcoding, and cleared each.

**AC audit:** AC-1 (no child outside the card box) — clip + tiers, red-proven pre-fix at the reported defect (title bottom 255.8 vs card 241 at pane 80) and green post-fix; AC-2 (no undeclared zero-collapse) — every disappearance is now a declared tier, and the intake correction stands recorded (the footer/wave zero-heights in the report were the designed #16487 disclosure, `display: none` measuring 0 to rect instruments); AC-3 (card never taller than pane) — the padding floor removed at the shell tier, asserted at the 420 station; AC-4 (two-sided witness, red against pre-fix dev) — receipted in the PR's Test Evidence.

**Post-merge validation (first receipt, same evening):** at dev head `d6a99b8c98` (this fix + the `#16718` staging fix both aboard), the tear-out source-continuity witness ran headed at 1200×1040/inner 953 — both paces green, 0 partial frames across 445 sampled records, and the probe's frame dumps show the full six-child card composed in the healthy band, exactly the ladder's >170 tier contract. The film-band tiers themselves get their on-camera receipt at the next take (the PR's PMV item stands).

Origin Session ID: 7e8a0e84-6733-474e-865e-1757feb4b5f8



