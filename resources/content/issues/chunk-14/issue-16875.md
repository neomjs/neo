---
id: 16875
title: 'A persistence failure re-embeds the identical set on every sweep, invisibly'
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-opus-ada
createdAt: '2026-08-10T11:16:40Z'
updatedAt: '2026-08-10T12:55:08Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16875'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-10T12:55:08Z'
---
# A persistence failure re-embeds the identical set on every sweep, invisibly

## Context

`#16780` AC-2 asks that a persistence failure not re-select and re-embed the identical set unboundedly, and that the non-convergence be *detected and reported rather than repeated*.

**The loop is real and I verified it rather than reasoning from the code shape.** `embedChunks` computes an embedding and upserts **inside the same `try`**, so a failed write discards work the provider was already paid for, the batch is marked failed, and the next sweep re-selects exactly those chunks — they are not in the collection — and pays for them again.

Live latest-open sweep at 2026-08-10T11:16:10Z across the latest 20 open issues; no equivalent found.

## The Problem

**The loop is not the finding. Its invisibility is.**

From provider load alone this is indistinguishable from an ingestion making progress: continuous inference, a corpus that does not grow, and every per-sweep receipt honestly reporting a handled failure. Nothing lies. There is simply no surface at which "this sweep did the same work as the last one" is expressible, so an operator watching a plane in this state sees a busy, healthy-looking system.

That is the shape `#16780` opened with — four cores against an empty corpus — and it is a shape a deployment can sit in for months.

## The Architectural Reality

`ai/services/knowledge-base/VectorService.mjs` — `embedChunks`. The embed and the `collection.upsert` share one `try`, so persistence failure discards a paid-for vector rather than retaining it for the retry.

That structure is **not** what this ticket proposes to change. Retaining vectors across a failed write is a different design with its own cost, and the ticket's ask is *detection*, not *survival*.

## The Fix

Coverage that pins the loop as a property, plus its converging control.

- **The loop:** three sweeps against a failing collection re-submit the same three distinct texts. Asserted on what was **submitted**, because the submission is the cost — a provider call happens whether or not the write lands.
- **The control:** a sweep whose writes persist submits each text exactly once and the next sweep has nothing to do. Without this, the first spec proves only that `embedChunks` embeds things; *"re-submitted"* means nothing without a case that does not.

The *reporting* half of AC-2 is supplied by the re-embed ratio (`#16866`): repeated content drives it above 1 while distinct content holds it at 1. This ticket makes the loop a pinned property so that ratio has something whose absence is meaningful.

## Acceptance Criteria

Delivered on `ada/16780-nonconvergence-detection`. `4 passed`.

- [x] A persistence failure re-embeds the identical set on every sweep — three sweeps, three re-submissions of three distinct texts, asserted at the **submission** boundary.
- [x] **Control:** a converging sweep submits each text exactly once and the next sweep has nothing to do. Without it the criterion above is satisfied by any code that embeds at all.
- [x] The failing collection models a real failure — `upsert` throws — rather than silently recording a no-op, so the retry and failure accounting are exercised.
- [x] Coverage fails against a converging implementation and passes against the looping one; the two specs are each other's falsifier.

## Out of Scope

- **Changing `embedChunks` to retain vectors across a failed write.** A different design with its own cost, and AC-2 asks for detection rather than survival.
- **The reporting surface** — `#16866`, and its cross-process exposure is `@neo-gpt`'s repair.
- **Why persistence fails** — store health is `#16795` / `#16227` territory.

## Avoided Traps

- **Asserting on `embedded` counts.** A sweep can report zero embedded while still having paid for the provider call; the submission is the cost, so the assertion is on what was sent.
- **A loop test with no converging control.** It would pass against any implementation that embeds, which is the "test that cannot fail" shape.
- **Modelling failure as a silent no-op.** A collection that accepts and discards would exercise a path production does not have — the same permissive-double defect corrected on `#16870`.

## Related

`#16780` (parent — AC-2), `#16866`, `#16843`.

Origin Session ID: 87f453f9-aa80-4487-9ed1-b5d91e052c43

Retrieval Hint: `persistence failure unbounded re-embed loop converging control submission boundary`

Authored by Ada (Claude Opus 5, Claude Code).


## Timeline

- 2026-08-10T11:16:41Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-10T11:16:42Z @neo-opus-ada added the `bug` label
- 2026-08-10T11:16:42Z @neo-opus-ada added the `ai` label
- 2026-08-10T11:18:03Z @neo-opus-ada cross-referenced by PR #16876
- 2026-08-10T12:10:41Z @neo-opus-ada referenced in commit `888d017` - "test(knowledge-base): drive non-convergence through the production selector (#16875)

Review found the previous spec could not falsify its own claim, and that is
correct. It ran its OWN loop and handed embedChunks the full corpus every sweep,
so the repetition it "detected" was authored by the test; its control
discriminated with corpus.filter(!landed.has(id)), a filter that also lived in
the test. A detect-and-stop repair landing in production selection was invisible
to both arms, so a CORRECT implementation stayed green.

Proven rather than argued: under a detect-and-stop mutation of
selectResumableChunks, the old spec passes 2/2 and the new one fails.

Both arms now run one production path. readCollectionIds asks the collection
what landed, selectResumableChunks decides what remains, embedChunks embeds
exactly that. No selection logic lives in the spec, so the numbers move when
production changes. One collection factory serves both arms with a single
variable - whether the upsert persists - because two hand-written doubles can
drift into differing on something a reader cannot see.

The third test asserts the reporting terminal, which a process-local observer
could not express: the recorder and the reader are different OS processes. The
shared identity ledger removes that boundary, so the provider is stubbed BELOW
the seam - stubbing embedTexts skips the ledger write and certifies the stub
instead of the path.

The collection double refuses what ChromaDB refuses. A permissive double deletes
the store's mandatory refusals and every downstream assertion becomes a property
of the double."
- 2026-08-10T12:11:25Z @neo-gpt-emmy cross-referenced by #16879
### @neo-opus-ada - 2026-08-10T12:12:11Z

## AC-4 was ticked before it was true — recorded rather than quietly corrected

**AC-4** reads *"Coverage fails against a converging implementation and passes against the looping one; the two specs are each other's falsifier."* It was ticked at PR open. It was **not true**, and @neo-gpt found it in review on PR #16876.

The original spec ran its own sweep loop and handed `embedChunks` the full corpus every pass, so the repetition it measured was authored by the test; its control discriminated with `corpus.filter(chunk => !landed.has(chunk.id))`, a filter that also lived in the test. Production's sweep selector is neither — it is `VectorService.embed()` reading `existingIds` off the collection and selecting only what is missing, which **is** the convergence mechanism. The two specs were each other's falsifier only with respect to code I wrote in the test file, so **a correct detect-and-stop implementation kept them green.**

### The tick is now earned, and here is the receipt

I mutated `selectResumableChunks` into a detect-and-stop implementation (remember every id already selected; never select it twice) and ran both specs against the same mutation:

```
detect-and-stop mutation applied to ai/services/knowledge-base/helpers/resumableEmbedding.mjs

  OLD spec  ->  2 passed        <- vacuous: a correct implementation kept it green
  NEW spec  ->  1 failed        <- expected [3, 3, 3], production selected [3, 0, 0]
```

The mutation was reverted; `git diff` on that file is empty. Delivered in `888d017821` on PR #16876.

### What changed structurally

Both arms now call one shared production path: `readCollectionIds` asks the collection what landed, `selectResumableChunks` decides what remains, `embedChunks` embeds exactly that. No selection logic lives in the spec, so the numbers move when production changes. The only variable between arms is whether the upsert persisted.

A third test was added for the reporting terminal, which **was not writable when this ticket was filed**: the observer was process-local and structurally blind to the KB producer. #16866 / PR #16867 repaired that (merged `69aaeabcd1`), so the loop is now assertable as `ratio > 1` for `source: 'knowledge-base'` while a converging run holds it at exactly `1`.

### Why this is recorded instead of silently fixed

A reader who sees only the current state cannot distinguish a criterion that was always satisfied from one that was claimed early and later earned. The second kind is worth being able to find — a vacuous guard is more dangerous than no guard, because it reports protection that is not there.

- 2026-08-10T12:17:30Z @neo-opus-ada referenced in commit `1955386` - "test(knowledge-base): bind the DEPLOYED sweep entry point, not just its primitives (#16875)

Production implements the same selection rule TWICE, and the previous commit
covered the wrong one for the claim being made. VectorService.embed() carries its
own inline filter over existingIds; selectResumableChunks is a separate helper
used by the shadow-swap resume path. Binding only the helper leaves the deployed
sweep unverified.

A fourth test drives embed() itself across successive sweeps: it reads its own
corpus off disk, reads existing ids off the collection, decides what to embed and
writes. Nothing in the spec selects anything. Failing persistence pays the
provider for the same corpus twice; restored persistence selects nothing on the
second sweep.

Red-proof, and it discriminates the two selectors. Mutating embed()'s inline
filter to ignore existingIds turns the converging arm from [3, 0] into [3, 3]:
the new test fails while the three primitive-level tests stay green, because they
bind the other selector. Mutation reverted; git diff on VectorService.mjs is
empty."
- 2026-08-10T12:55:08Z @tobiu referenced in commit `a565ad6` - "test(knowledge-base): pin persistence failure as an unbounded re-embed loop (#16875) (#16876)

* test(knowledge-base): pin persistence failure as an unbounded re-embed loop (#16780)

AC-2's premise, verified rather than assumed. embedChunks computes an embedding
and upserts inside the same try, so a persistence failure discards work the
provider was already paid for, marks the batch failed, and the next sweep
re-selects exactly the same chunks — they are not in the collection — and pays
again. Three sweeps, three re-submissions of three distinct texts.

The loop is not the finding; its INVISIBILITY is. From provider load alone it is
indistinguishable from an ingestion making progress: continuous inference, a
corpus that does not grow, and every per-sweep receipt reporting a handled
failure. That is a shape a deployment can sit in for months.

Asserted on what was SUBMITTED, because the submission is the cost.

The converging control is the load-bearing half: a sweep whose writes persist
submits each text exactly once and the next sweep has nothing to do. Without it
the first spec proves only that embedChunks embeds things — 're-submitted' means
nothing without a case that does not.

* test(knowledge-base): drive non-convergence through the production selector (#16875)

Review found the previous spec could not falsify its own claim, and that is
correct. It ran its OWN loop and handed embedChunks the full corpus every sweep,
so the repetition it "detected" was authored by the test; its control
discriminated with corpus.filter(!landed.has(id)), a filter that also lived in
the test. A detect-and-stop repair landing in production selection was invisible
to both arms, so a CORRECT implementation stayed green.

Proven rather than argued: under a detect-and-stop mutation of
selectResumableChunks, the old spec passes 2/2 and the new one fails.

Both arms now run one production path. readCollectionIds asks the collection
what landed, selectResumableChunks decides what remains, embedChunks embeds
exactly that. No selection logic lives in the spec, so the numbers move when
production changes. One collection factory serves both arms with a single
variable - whether the upsert persists - because two hand-written doubles can
drift into differing on something a reader cannot see.

The third test asserts the reporting terminal, which a process-local observer
could not express: the recorder and the reader are different OS processes. The
shared identity ledger removes that boundary, so the provider is stubbed BELOW
the seam - stubbing embedTexts skips the ledger write and certifies the stub
instead of the path.

The collection double refuses what ChromaDB refuses. A permissive double deletes
the store's mandatory refusals and every downstream assertion becomes a property
of the double.

* test(knowledge-base): bind the DEPLOYED sweep entry point, not just its primitives (#16875)

Production implements the same selection rule TWICE, and the previous commit
covered the wrong one for the claim being made. VectorService.embed() carries its
own inline filter over existingIds; selectResumableChunks is a separate helper
used by the shadow-swap resume path. Binding only the helper leaves the deployed
sweep unverified.

A fourth test drives embed() itself across successive sweeps: it reads its own
corpus off disk, reads existing ids off the collection, decides what to embed and
writes. Nothing in the spec selects anything. Failing persistence pays the
provider for the same corpus twice; restored persistence selects nothing on the
second sweep.

Red-proof, and it discriminates the two selectors. Mutating embed()'s inline
filter to ignore existingIds turns the converging arm from [3, 0] into [3, 3]:
the new test fails while the three primitive-level tests stay green, because they
bind the other selector. Mutation reverted; git diff on VectorService.mjs is
empty."
- 2026-08-10T12:55:08Z @tobiu closed this issue

