---
id: 12886
title: 'Neural Link: drag-motion observability — agents must SEE live drag choreography'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable
createdAt: '2026-06-11T01:52:56Z'
updatedAt: '2026-06-11T06:48:22Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12886'
author: neo-fable
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-11T06:48:22Z'
---
# Neural Link: drag-motion observability — agents must SEE live drag choreography

## Context

**Operator PRIO 0** (2026-06-11, verbatim: "PRIO 0 => new NL tools to enable you to see it"): tonight's grid-DnD regression cluster (#12883 — wrong mid-drag header motion, broken overdrag auto-scroll, **column duplication on drop**) shipped through FOUR blind verification layers — unit specs (visual write path stubbed), two independent live probes (final-order assertions only), and two formal cross-family reviews. Agents currently have NO way to perceive the motion layer: the thing a human user experiences first is the thing the swarm cannot see at all. This is the substrate fix: give agents eyes on live drag choreography, then fix #12883 WITH sight.

## The Problem

Three perception gaps, each mapping to a regression class that escaped tonight:

1. **Mid-drag geometry over time** — headers animate via `wrapperStyle.left` on absolutely-positioned items (`container.SortZone#updateItem`); nothing can sample where elements ACTUALLY are during a drag window. (Regression class: "headers move to wrong positions, not even close".)
2. **Drag-pipeline trajectory** — `get_drag_state` is a point-in-time snapshot; there is no trace of the drag lifecycle (per-move `currentIndex` transitions, `switchItems` calls with arguments, `indexMap` evolution, overdrag/scroll activations, final `moveTo` arguments). (Regression classes: burst-walk misbehavior, broken overdrag scroll — and it permanently supersedes the temp-logging instrumentation #12880's AC1 wanted.)
3. **Engine↔DOM consistency** — no tool asserts that a container's `items[]`, `vdom.cn`, and the REAL DOM children agree. (Regression class: **drop-duplication** — a column existing twice is exactly a three-surface disagreement, currently invisible until a human looks.)

## The Fix (tool surfaces — shapes for intake validation, split at implementation if needed)

1. **`observe_motion`** — `{componentIds|selector, durationMs, intervalMs}` → time-series of client rects (+ relevant style/transform), sampled main-thread-side during the window, returned as a trace. Agent drives the drag (any dispatch path) while the recorder runs; the trace IS the mid-drag assertion surface.
2. **`get_drag_trace`** — ring-buffer instrumentation in `container.SortZone` (+ grid subclass): per drag, record move-events (clientX/Y, computed target), `currentIndex` transitions, `switchItems(i,j)` calls, scroll activations, `onDragEnd` resolution (fromIndex/toIndex/locked verdict). Tool returns the last N drags' traces. Negligible cost when idle; permanent falsifier substrate.
3. **`verify_component_consistency`** — `{componentId}` → diff report across `items[]` / `vdom.cn` / live DOM childNodes (ids, order, count). Generic beyond grids — the duplication detector for any container.

All three route through the existing app-worker NL channel (no dependency on #12884's window-registration gap, which remains the `simulate_event` routing fix).

## Acceptance Criteria

- [ ] AC1: `observe_motion` returns a usable trace for a devindex header drag (manual or dispatched), revealing the #12883 wrong-position motion
- [ ] AC2: `get_drag_trace` captures a full drag lifecycle incl. burst-walk switch sequences and overdrag scroll activations
- [ ] AC3: `verify_component_consistency` detects a synthetically induced items/vdom/DOM mismatch (and the live duplication case while it exists)
- [ ] AC4: tool descriptions within MCP budget (§5.3); whitebox-e2e + neural-link skill payloads updated to route to these tools
- [ ] AC5: the #12883 fix PR uses these tools as its evidence layer (the doctrine: no more blind motion fixes)

## Out of Scope

- The #12883 fix itself (separate, gated on these eyes per operator directive).
- `simulate_event` routing for non-shared apps (#12884).
- Screenshot/pixel-level perception (rect-trace is the right granularity for this defect class; pixel tools are a later layer if rects prove insufficient).

## Decision Record impact

none (extends the existing NL tool surface; ADR 0019-conformant config reads).

## Related

Operator directive thread: #12883 (the consumer). #12884 (sibling NL gap). #12807 (@neo-opus-ada's simulatability — dispatch side; this is the perception side). Umbrella: the whitebox-e2e doctrine proven tonight.

Release classification: ON the board — operator PRIO 0; gates the #12883 fix which gates the cut.

Origin Session ID: 567191c3-16f6-4235-914c-b51fc94d1514
Retrieval Hint: `query_raw_memories("drag motion observability NL eyes PRIO 0 duplication")`

Live latest-open sweep: latest 12 open checked 01:48Z + #12883/#12884 filed by me since; no equivalent. A2A sweep: clean; lane operator-routed to @neo-fable.

## Timeline

- 2026-06-11T01:52:57Z @neo-fable assigned to @neo-fable
- 2026-06-11T01:52:58Z @neo-fable added the `enhancement` label
- 2026-06-11T01:52:58Z @neo-fable added the `ai` label
- 2026-06-11T01:52:58Z @neo-fable added the `architecture` label
- 2026-06-11T01:54:23Z @neo-fable cross-referenced by #12883
### @neo-fable - 2026-06-11T01:58:00Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ## Implementation blueprint (assignee; end-to-end pattern traced live on dev tonight — execution starts here)
> 
> **The op flow (verified by source read):** `ai/mcp/server/neural-link/openapi.yaml` (tool def) → `toolService.mjs` `serviceMapping` (tool → service method) → `ai/services/neural-link/<X>Service.mjs` thin relay: `ConnectionService.call(sessionId, '<op>', params)` → bridge → app-worker `src/ai/Client.mjs` op map → `src/ai/client/<X>Service.mjs` implementation → main thread (when DOM needed) via `Neo.Main.importAddon({name, windowId})` + `Neo.main.addon.X` remotes. Canonical siblings: `get_drag_state` (server `InteractionService.getDragState`, pure relay) and `get_dom_rect` (worker `client/ComponentService.getDomRect({componentIds})` → `component.getDomRect(componentIds)` — batched array support exists).
> 
> **Tool 1 — `observe_motion`** (no new files): server relay beside `getDragState`; worker impl beside `getDomRect`: clamp `durationMs ≤ 10000`, `intervalMs ≥ 16`; loop sampling the batched rect path per tick, accumulate `[{t, rects}]`, return the trace. The agent drives the drag through any dispatch path while the recorder runs.
> 
> **Tool 2 — `get_drag_trace`**: instrumentation in `src/draggable/container/SortZone.mjs` — module-level capped ring (last ~5 drags): record on `onDragStart` (zone/owner ids, snapshot geometry), per `onDragMove` (clientX/Y, computed `targetIndex`, `currentIndex`), per `switchItems(i,j)`, per `scrollToIndex` (the overdrag-scroll observability the operator named), and `onDragEnd` resolution (fromIndex/toIndex). Grid subclass appends the lock-verdict event (`positional region`, `column.locked` mutation). Worker op returns + optionally clears. Negligible idle cost; permanent falsifier substrate (supersedes all temp-logging).
> 
> **Tool 3 — `verify_component_consistency`**: worker-side three-surface read — `container.items` ids; `vdom.cn` componentIds; real DOM children ids via a minimal `getChildNodeIds({id, windowId})` added to `src/main/DomAccess.mjs` (no children-read remote exists today — grep verified; trivial main-thread addition, existing file). Diff membership/order/count pairwise; return mismatch report. THE duplication detector, generic for all containers.
> 
> **Constraints:** openapi descriptions single-line within §5.3 budget; structural pre-flight = fast-path (zero new files, sibling-pattern matches throughout); NL MCP **server restart required** after server-side changes (operator/harness op — the runtime-freshness healthcheck will flag it); unit coverage for the pure parts (differ, ring buffer), whitebox rig for `observe_motion` against a real drag (composes with #12883's falsifier protocol).
> 
> **Sequencing:** this gates the #12883 fix (operator PRIO-0). The pending PR-#12882-revert decision affects #12883's baseline but NOT this ticket — eyes get built either way.

- 2026-06-11T02:00:34Z @neo-gpt cross-referenced by #12696
- 2026-06-11T02:13:09Z @neo-fable cross-referenced by #12887
- 2026-06-11T02:13:55Z @neo-fable cross-referenced by PR #12888
- 2026-06-11T02:26:37Z @neo-fable cross-referenced by PR #12890
- 2026-06-11T02:42:42Z @neo-fable referenced in commit `9a1d09f` - "docs(ai): route drag-motion perception tools in skill payloads (#12886)"
- 2026-06-11T06:38:32Z @neo-fable referenced in commit `cf3e250` - "feat(ai): add NL drag-motion observability tools (#12886)"
- 2026-06-11T06:38:32Z @neo-fable referenced in commit `99d9e68` - "docs(ai): route drag-motion perception tools in skill payloads (#12886)"
- 2026-06-11T06:48:22Z @tobiu referenced in commit `ae22396` - "feat(ai): add NL drag-motion observability tools (#12886) (#12890)

* feat(ai): add NL drag-motion observability tools (#12886)

* docs(ai): route drag-motion perception tools in skill payloads (#12886)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-11T06:48:22Z @tobiu closed this issue
- 2026-06-11T06:51:45Z @neo-fable cross-referenced by #12880
- 2026-06-11T07:17:30Z @neo-fable cross-referenced by PR #12891
- 2026-06-11T08:35:57Z @neo-fable cross-referenced by PR #12892
- 2026-06-11T10:08:54Z @neo-opus-grace cross-referenced by PR #12894
- 2026-06-11T14:00:14Z @neo-fable cross-referenced by PR #12901

