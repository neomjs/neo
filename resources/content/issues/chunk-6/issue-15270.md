---
id: 15270
title: 'FM cockpit per-agent mailbox pane — read-only AgentDetail tab (S1 view half, D#15249)'
state: OPEN
labels:
  - enhancement
  - design
  - ai
assignees:
  - neo-opus-vega
createdAt: '2026-07-16T15:06:50Z'
updatedAt: '2026-07-16T22:21:14Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15270'
author: neo-fable
commentsCount: 1
parentIssue: 14560
subIssues:
  - '[x] 15316 FM cockpit mailbox pane + Fleet read seam — the delivered S1 view half (split from #15270)'
subIssuesCompleted: 1
subIssuesTotal: 1
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy:
  - '[ ] 15320 Authenticate Fleet HTTP ingress and bind viewer identity'
  - '[x] 15269 Fleet read adapter: per-agent A2A mailbox mirror — read-only, viewer-admitted, paginated (S1 Brain half, D#15249)'
blocking: []
---
# FM cockpit per-agent mailbox pane — read-only AgentDetail tab (S1 view half, D#15249)

## Context

The view half of surface S1 from the graduated FM design-first record (D#15249, closed RESOLVED → #15254 Surface Registry). Object permanence ruled the placement: the mailbox belongs to the agent object → a **tab inside `AgentDetail`**. Dock-liftability is a later follow-up under the dock grammar, deliberately NOT this leaf.

## The Problem

The operator's daily loop today is reading raw A2A traffic by hand (wake daemon off = manual mailbox checks). The detail view (#14608) renders status/freshness panes but no communication surface.

## The Architectural Reality

- Consumes the Fleet mailbox read adapter (#15269 — blocked-by edge set); renders, never fetches raw MailboxService itself.
- Graduated design direction (binding): **flat-chronological with thread-collapse** where `partOfThread` exists; **freshness labels the PANE** (data-as-of), never rows — messages are immutable timestamped facts, per-row freshness is noise.
- **Read-only in v13.2**: no mark-read affordance ANYWHERE in the pane (the record's MUST-NOT: operator mark-read swallows peer handoffs); reply-as-operator deferred to v13.3 as chat's companion (the record pre-answers the convergence: chat is the actionable EVOLUTION of this pane, never a parallel surface — the registry entry names it).
- Windowed/paginated per the record's density bound (thread-collapse alone is not a bound); honest degraded/admission-denied states.
- `apps/**` gate: data via `data.Store`/records where applicable, `state.Provider` scoping, zero CSS-in-JS; design primitives from `TOKENS.md`/`CARD-CONTRACT.md`.

## Acceptance Criteria

- [ ] `AgentDetail` gains a mailbox tab rendering the selected resident's active-inbox mirror via #15269: flat-chrono, thread-collapsed, paginated, pane-level data-as-of freshness label.
- [ ] Zero mutation affordances (no mark-read, no reply) — structurally absent from the vdom, asserted in a spec.
- [ ] Honest states: admission denied → named denial state; adapter unavailable → degraded state; empty inbox → empty state (never fake rows, never fake success).
- [ ] Both themes; design sign-off by @neo-opus-grace per the #15242 AC pattern; the #15254 registry entry is the durable citation.
- [ ] Unit specs for the render states + an NL e2e touching the tab on the live module.

## Out of Scope

The adapter itself (#15269) · dock-liftability of the pane · chat/actionable evolution (v13.3) · archive browsing.

## Related

Epic #14560 (parent — the surface epic) · #15269 (blocked-by: the Brain half) · D#15249 (graduated record, S1 tokens + boundary ACs) · #15254 / PR #15255 (registry) · #14608 (the detail view this extends).

Sweeps: latest-open checked 2026-07-16T15:05Z (latest #15267) — no equivalent. A2A herd-window clean.

Origin Session ID: b42c5667-db78-4a0c-83a0-ce66c95ee0d9
Retrieval Hint: "FM cockpit mailbox pane AgentDetail tab read-only S1"

## Timeline

- 2026-07-16T15:06:51Z @neo-fable added the `enhancement` label
- 2026-07-16T15:06:52Z @neo-fable added the `design` label
- 2026-07-16T15:06:52Z @neo-fable added the `ai` label
- 2026-07-16T15:08:23Z @neo-fable added parent issue #14560
- 2026-07-16T15:08:50Z @neo-fable marked this issue as being blocked by #15269
- 2026-07-16T15:11:44Z @neo-fable cross-referenced by #15254
- 2026-07-16T16:49:54Z @neo-opus-vega cross-referenced by PR #15285
- 2026-07-16T16:52:54Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-07-16T17:02:31Z @neo-opus-vega referenced in commit `a1f6995` - "feat(agentos): MailboxMessage model + pane-owned AgentMailbox store — the mailbox tab data plane (#15270)"
- 2026-07-16T17:06:16Z @neo-opus-vega referenced in commit `5a5e1cc` - "feat(agentos): MailboxPane — the read-only S1 mailbox tab view with four honest states (#15270)"
- 2026-07-16T17:14:36Z @neo-opus-vega referenced in commit `367b1fb` - "test(agentos): MailboxPane render-state battery + explicit collapse seed on snapshot replace (#15270)"
- 2026-07-16T17:18:48Z @neo-opus-vega referenced in commit `2b5fbb9` - "feat(agentos): AgentDetail grows the Status/Mailbox tab structure — the countless mailbox tab seated (#15270)"
- 2026-07-16T17:20:41Z @neo-opus-vega referenced in commit `c5e8a85` - "style(agentos): mailbox tab styles — rows, thread chips, honest-state tones over the fm token layer (#15270)"
- 2026-07-16T21:15:10Z @neo-opus-vega referenced in commit `aa03744` - "test(agentos): the Mailbox tab proven live — drill → tab → injected snapshot → real thread toggle (#15270)

The mounted whitebox-e2e the pane's unit battery cannot give: a REAL DOM click
drills a resident, activates the countless "Mailbox" tab, and the pane renders
the honest `unobserved` state — no wiring injects snapshots yet, and the spec
asserts that truth rather than fabricating rows.

An adapter-shaped snapshot then enters through the possession seam
(`setProperties` on the mounted pane — exactly the wiring's contract) and the
whole render chain runs over the real DOM: rows newest-first, the collapsed
thread head carrying its `+N earlier` chip, and a real click on the thread head
expanding it inline — proving the `data-thread-id` → dataset → delegated-listener
path end-to-end. Read-only stays structural and is asserted live: zero
button/input/anchor renders anywhere inside the pane.

The drill target is the dedicated native `fm-card-drill` Button, not the avatar.
The avatar is a handler-less Image, so the gesture the sibling drill spec uses is
a silent no-op — chased with a console/network probe (no failing request; the
click simply lands on nothing) before reading the card's own wiring. Reported
separately: that sibling spec cannot pass on dev today, and the e2e battery does
not gate CI, so it went unseen.

Evidence: green locally against the live app worker at NEO_E2E_PORT."
- 2026-07-16T21:21:54Z @neo-opus-vega referenced in commit `ddfce62` - "feat(agentos): MailboxMessage model + pane-owned AgentMailbox store — the mailbox tab data plane (#15270)"
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `265ba9f` - "feat(agentos): MailboxPane — the read-only S1 mailbox tab view with four honest states (#15270)"
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `dfd36cb` - "test(agentos): MailboxPane render-state battery + explicit collapse seed on snapshot replace (#15270)"
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `1eab0b4` - "feat(agentos): AgentDetail grows the Status/Mailbox tab structure — the countless mailbox tab seated (#15270)"
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `53c3e41` - "style(agentos): mailbox tab styles — rows, thread chips, honest-state tones over the fm token layer (#15270)"
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `15040d0` - "test(agentos): the Mailbox tab proven live — drill → tab → injected snapshot → real thread toggle (#15270)

The mounted whitebox-e2e the pane's unit battery cannot give: a REAL DOM click
drills a resident, activates the countless "Mailbox" tab, and the pane renders
the honest `unobserved` state — no wiring injects snapshots yet, and the spec
asserts that truth rather than fabricating rows.

An adapter-shaped snapshot then enters through the possession seam
(`setProperties` on the mounted pane — exactly the wiring's contract) and the
whole render chain runs over the real DOM: rows newest-first, the collapsed
thread head carrying its `+N earlier` chip, and a real click on the thread head
expanding it inline — proving the `data-thread-id` → dataset → delegated-listener
path end-to-end. Read-only stays structural and is asserted live: zero
button/input/anchor renders anywhere inside the pane.

The drill target is the dedicated native `fm-card-drill` Button, not the avatar.
The avatar is a handler-less Image, so the gesture the sibling drill spec uses is
a silent no-op — chased with a console/network probe (no failing request; the
click simply lands on nothing) before reading the card's own wiring. Reported
separately: that sibling spec cannot pass on dev today, and the e2e battery does
not gate CI, so it went unseen.

Evidence: green locally against the live app worker at NEO_E2E_PORT."
- 2026-07-16T21:21:55Z @neo-opus-vega referenced in commit `b6408fd` - "fix(agentos): the mailbox degrade line stops blaming the source for the adapter's refusals (#15270)

The mirror adapter's fail-closed refusals — an unbound request identity, an
asserted viewer that does not match the binding, an inadmissible namespace
subject — all arrive as `admission.state: 'unavailable'` beside
`capability.state: 'degraded'`, shaped identically to a genuine Memory Core
outage. The pane cannot tell them apart, and it was guessing:

  "Mailbox source degraded: asserted viewerIdentity does not match the bound
   request identity"

That sends an operator to debug Memory Core over a refusal the adapter made
about the VIEWER. The line now states only what this view actually knows — no
rows, and the owner's reason verbatim — rather than naming a cause it cannot
see. One neutral word instead of a fifth pane state: the reason text was always
doing the real work, and the sentence around it was the only thing lying.

Pinned per refusal reason, asserting the source-outage attribution cannot come
back and that a refusal shows neither rows nor a page window. Stash-verified
against the old copy.

Evidence: 318 agentos specs green."
- 2026-07-16T21:23:12Z @neo-opus-vega referenced in commit `a3a8ddd` - "feat(agentos): MailboxMessage model + pane-owned AgentMailbox store — the mailbox tab data plane (#15270)"
- 2026-07-16T21:23:12Z @neo-opus-vega referenced in commit `aeaadc6` - "feat(agentos): MailboxPane — the read-only S1 mailbox tab view with four honest states (#15270)"
- 2026-07-16T21:23:12Z @neo-opus-vega referenced in commit `16cf690` - "test(agentos): MailboxPane render-state battery + explicit collapse seed on snapshot replace (#15270)"
- 2026-07-16T21:23:13Z @neo-opus-vega referenced in commit `5c95f6e` - "feat(agentos): AgentDetail grows the Status/Mailbox tab structure — the countless mailbox tab seated (#15270)"
- 2026-07-16T21:23:13Z @neo-opus-vega referenced in commit `d0e18a1` - "style(agentos): mailbox tab styles — rows, thread chips, honest-state tones over the fm token layer (#15270)"
- 2026-07-16T21:23:13Z @neo-opus-vega referenced in commit `f703299` - "test(agentos): the Mailbox tab proven live — drill → tab → injected snapshot → real thread toggle (#15270)

The mounted whitebox-e2e the pane's unit battery cannot give: a REAL DOM click
drills a resident, activates the countless "Mailbox" tab, and the pane renders
the honest `unobserved` state — no wiring injects snapshots yet, and the spec
asserts that truth rather than fabricating rows.

An adapter-shaped snapshot then enters through the possession seam
(`setProperties` on the mounted pane — exactly the wiring's contract) and the
whole render chain runs over the real DOM: rows newest-first, the collapsed
thread head carrying its `+N earlier` chip, and a real click on the thread head
expanding it inline — proving the `data-thread-id` → dataset → delegated-listener
path end-to-end. Read-only stays structural and is asserted live: zero
button/input/anchor renders anywhere inside the pane.

The drill target is the dedicated native `fm-card-drill` Button, not the avatar.
The avatar is a handler-less Image, so the gesture the sibling drill spec uses is
a silent no-op — chased with a console/network probe (no failing request; the
click simply lands on nothing) before reading the card's own wiring. Reported
separately: that sibling spec cannot pass on dev today, and the e2e battery does
not gate CI, so it went unseen.

Evidence: green locally against the live app worker at NEO_E2E_PORT."
- 2026-07-16T21:23:13Z @neo-opus-vega referenced in commit `4da1ff2` - "fix(agentos): the mailbox degrade line stops blaming the source for the adapter's refusals (#15270)

The mirror adapter's fail-closed refusals — an unbound request identity, an
asserted viewer that does not match the binding, an inadmissible namespace
subject — all arrive as `admission.state: 'unavailable'` beside
`capability.state: 'degraded'`, shaped identically to a genuine Memory Core
outage. The pane cannot tell them apart, and it was guessing:

  "Mailbox source degraded: asserted viewerIdentity does not match the bound
   request identity"

That sends an operator to debug Memory Core over a refusal the adapter made
about the VIEWER. The line now states only what this view actually knows — no
rows, and the owner's reason verbatim — rather than naming a cause it cannot
see. One neutral word instead of a fifth pane state: the reason text was always
doing the real work, and the sentence around it was the only thing lying.

Pinned per refusal reason, asserting the source-outage attribution cannot come
back and that a refusal shows neither rows nor a page window. Stash-verified
against the old copy.

Evidence: 318 agentos specs green."
- 2026-07-16T21:24:31Z @neo-opus-vega cross-referenced by PR #15312
- 2026-07-16T21:57:19Z @neo-opus-vega referenced in commit `8974780` - "fix(agentos): a subject's mail cannot render under another agent's name (#15270)

Two fail-open honesty defects the review falsified on the exact head. Both are
bugs in shipped behavior, not gaps in unbuilt work.

**Subject possession.** AgentDetail re-seated only `record` and retained the
mailbox snapshot, so drilling resident A and then resident B rendered A's inbox
under B's name — mail attributed to an agent who never received it. A snapshot is
one SUBJECT's mail; it now cannot survive a re-seat onto a different resident, and
the pane drops to its honest `unobserved` until the wiring supplies this subject's
snapshot. A same-subject re-seat (a roster refresh restamping the record) keeps it,
or every poll would blank a correct pane.

**The unrecognized envelope failed OPEN.** `getPaneState()` returned `empty` for
`{}`, a torn payload, or any shape without the producer's rows array — rendering
"No active messages for @x", a confident and honest-LOOKING empty inbox fabricated
out of something the pane never understood. `empty` is a claim about the subject's
mail and may only be made when the producer actually said so; an unrecognized
envelope is `unobserved`. The producer's own empty answer is still explicitly
empty — the guard must not swallow the real zero-state.

Both are the same shape as the review finding that caught the adapter this
morning: a fact rendered about a subject that the surface was never entitled to
make about them.

Evidence: 291 agentos specs green, incl. the possession matrix (different-subject
drops, same-subject keeps) and a five-envelope fail-closed battery."
- 2026-07-16T22:01:08Z @neo-opus-vega referenced in commit `5ca2a28` - "feat(fleet): the mailbox mirror gets its Brain-side read seam (#15270)

The first half of the production path the review correctly found missing:
`readFleetMailboxMirror` existed only in its adapter, its JSDoc and its own
tests, so nothing could ever hand the cockpit a real snapshot.

`fleetMailboxMirror({subjectAgentId, limit, offset})` joins `fleetActivity` as a
read-observe verb over an injected `mailboxMirrorSource`, mirroring that slot's
DI contract exactly: the wiring owns the identity binding and read permissions,
so the SOURCE holds `resolveBoundIdentity` and the viewer-bound `listMessages`.
This bridge never imports MailboxService and never authors an admission fact — it
routes. Admission stays the Memory Core primitive's fail-closed CAN_READ_INBOX_OF
gate, decided one layer down.

The unwired degrade runs through the adapter's OWN pure half rather than a
hand-built literal, so the not-wired snapshot is shape-identical to a live one and
cannot drift from it. That matters beyond tidiness: the consuming pane now fails
closed on an envelope it does not recognize, so a bridge that degraded into a
DIFFERENT shape would be read as unrecognized rather than as honestly unavailable.
It never returns an empty inbox for a missing feed — "this agent has no mail" and
"we cannot see this agent's mail" are different claims, and only the producer is
entitled to the first.

Evidence: 253 fleet service specs green, incl. verbatim subject/bounds forwarding
and the unwired envelope asserting unavailable-not-granted, zero rows, and the
exact live key set."
- 2026-07-16T22:04:09Z @neo-opus-vega referenced in commit `a31c85d` - "feat(agentos): the mailbox tab reads its own subject through the Fleet seam (#15270)

The Body half of the production path. The drill now READS: seating a resident
calls the authenticated `fleetMailboxMirror` verb and hands the returned snapshot
to the pane. The pane still renders and never fetches; this view owns the read
because it owns the drill, and it stays shell-agnostic so a popped-out inspector
reads exactly like a docked one. The Body never touches MailboxService — the
source behind the verb holds the identity binding, and admission stays the Memory
Core primitive's own fail-closed gate.

Race-safe by generation, not by hope. A drill is a gesture and the read is async,
so across a fast A→B drill the in-flight read for A resolves AFTER B is seated;
assigning it would render A's inbox under B's name — the possession defect
re-entering through the back door, invisible to the possession guard because the
record was already correct. A monotonic generation AND a re-checked subject must
both hold before a snapshot lands, and the spec proves it by resolving A's read
LAST and asserting A's mail never reaches B's pane.

Fail-closed throughout: an absent verb or a throw leaves the pane honestly
`unobserved`. It never fabricates a snapshot, and never renders "no mail" for a
read that did not happen.

Evidence: 294 agentos specs green, incl. the verb-fed drill (every read scoped to
the drilled subject), the ordered-race falsifier, and the fail-closed matrix."
- 2026-07-16T22:09:41Z @neo-opus-vega referenced in commit `30b81c1` - "fix(agentos): thread collapse becomes a native button — the pane's only affordance was mouse-only (#15270)

The sole interaction was a delegated `click` on a plain div. A div owns no
Enter/Space and no tab stop, so the pane's ONLY affordance was unreachable by
keyboard — the same defect #14619 retired on the card drill, shipped again one
surface over. The toggle is now a real `<button>` naming its state with
`aria-expanded` + an `aria-label`, and the listener delegates to the BUTTON
rather than the row: a row-wide listener is an interactive region with no tab
stop.

The read-only spec had to be corrected to allow it, and the correction is the
point. It asserted zero `button|input|textarea|select|a` anywhere — which reads
as stricter but is the wrong bar: it conflated "no MUTATION verbs" (the record's
actual MUST-NOT) with "no interactive elements", and that conflation is exactly
what forced the toggle onto a div. Read-only constrains what an operator can
CHANGE, never whether they can reach what they can already see. The bar is now
mutation: no data-entry element, no mutation label, and the one admissible
control must carry `fm-mail-thread-toggle`.

The e2e now activates it by KEYBOARD — focus, then Enter — driving the real
data-thread-id → dataset → delegated-listener path. A div would swallow that
press; this is the assertion that would have caught the defect, where the prior
`.click()` sailed straight past it.

Evidence: 295 agentos specs green + the NL e2e green against the live app worker."
- 2026-07-16T22:15:11Z @neo-opus-vega referenced in commit `a04917a` - "feat(agentos): the mailbox page window can MOVE — row 51 becomes reachable (#15270)

Pagination was a range label. The pane rendered "1–50" and offered no way to see
message 51: a window with no transitions is a claim about the data, not access to
it, and the adapter's bounded `limit`/`offset` had no consumer.

Both edges are native buttons carrying the offset that reaches their page. The
pane REQUESTS (`pageRequest`) and never fetches — the owner holds the read seam
and the subject, so it performs the bounded re-read through the SAME path as the
drill. That routing is deliberate: a page request answered after the operator
drilled elsewhere inherits the generation latch and subject re-check for free,
rather than paging resident A's inbox into resident B's pane.

A step is DISABLED, never hidden, at the range edge — a control that vanishes
teaches the operator the surface is inconsistent, while a disabled one says "this
edge is the end". `next` is offered only on a FULL page, because a short page is
the producer telling us it ran out. A denial or degrade hides the bounds AND the
transitions together: a window over data you cannot see is a lie either way.

Evidence: 297 agentos specs green + the NL e2e green against the live app worker,
incl. the disabled-edge matrix and the pane-requests-never-fetches assertion."
- 2026-07-16T22:16:53Z @neo-opus-vega referenced in commit `7530832` - "fix(agentos): the mailbox freshness chip ages — a fresh mailbox could stay fresh forever (#15270)

Freshness is time-relative everywhere in this cockpit, but the mailbox chip
classified against the clock only when something re-rendered the pane. The owner's
aging loop re-ran `applyPaneFreshness` for the four status panes and never touched
the mailbox tab, so a `fresh` mailbox stayed fresh indefinitely while its snapshot
silently rotted — a stale claim rendered as current, which is the exact failure the
freshness vocabulary exists to prevent, sitting inside the surface that invented the
vocabulary.

The chip now ages off the SAME owner timer. One timer, two consumers: a second
interval would age one surface twice and give the cockpit two clocks to disagree
with each other. Aging re-labels and never drops data — the rows are immutable
timestamped facts; only the honesty of the label moves.

Evidence: 298 agentos specs green, incl. a 5-minute wall-clock decay against an
unchanged snapshot asserting the data survives the re-label."
- 2026-07-16T22:19:13Z @neo-opus-vega cross-referenced by #15316
- 2026-07-16T22:19:19Z @neo-opus-vega added sub-issue #15316
### @neo-opus-vega - 2026-07-16T22:21:14Z

## Narrowed at review — this ticket now owns the authenticated last mile

PR #15312's Cycle-1 review falsified the premise that AC1's "via #15269" was a wiring gap. It is an **authentication boundary**. Recording the disposition here so the ticket carries its own why rather than leaving it in a PR thread.

**Split:** #15316 (sub) captures exactly the scope PR #15312 delivers — the pane + the Fleet read seam. **This ticket stays open** owning the remainder.

**The falsifier**, V-B-A'd independently from both sides ([reviewer's scope correction](https://github.com/neomjs/neo/pull/15312#issuecomment-4997077214)):

- `dispatchFleetRequest` receives no request-bound identity.
- `fleetBridgeServer` is loopback-only but **unauthenticated**, and emits `Access-Control-Allow-Origin: *`.
- `installFleetBridge` sends only `Content-Type` — no viewer credential crosses the browser→Fleet boundary.
- The local-bearer MCP precedent authenticates *possession* without resolving an identity, so it does not supply the missing viewer fact.

**Loopback locality is not a viewer identity.** #15269's landed RA-2 contract is explicit: no binding ⇒ no admission claim. Binding a fixed viewer, or trusting caller-supplied text, would turn local reachability into ambient `CAN_READ_INBOX_OF` authority — the caller-authored-provenance shape that #15269 and #15309 both exist to kill, rebuilt one layer lower and blessed by a green test. So the `mailboxMirrorSource` stays deliberately **unwired and fail-closed**: `fleetMailboxMirror` degrades through the adapter's own pure half and reads as honestly `unavailable`, never as an empty inbox.

**What this ticket now owns — the successor security boundary, in order:**

1. Authenticated Fleet viewer ingress (browser→Fleet), replacing `Access-Control-Allow-Origin: *` + unauthenticated loopback.
2. Per-request `RequestContext` binding, so a viewer fact exists to bind.
3. `mailboxMirrorSource` wiring at the fleet-server boot (the composition point exists — `devFleetServer` already reaches memory-core in-process).
4. The first end-to-end admitted read: grant → rows; revoke → the named denial panel.

**Delivered and no longer owed here** (all on #15316 / PR #15312, green): the pane's four honest states with fail-closed envelope validation, subject possession + generation safety, bounded paging, wall-clock freshness, accessible native view-only controls, structural absence of mailbox mutations, and the `fleetMailboxMirror` read seam itself.

Step 1 is a real security surface with its own reviewers, and it is not this leaf's to invent at the end of a UI ticket. Whoever picks it up: the seam is already there and fails closed — you are supplying the viewer, not the plumbing.

— Vega (@neo-opus-vega) · Claude Opus 4.8, Claude Code


- 2026-07-16T22:30:45Z @neo-opus-vega referenced in commit `a8b4152` - "fix(fleet): put fleetMailboxMirror on the wire — the read seam could not cross it (#15270)

Self-caught before the terminal review: the read verb existed on the Node-side
bridge and was NOT on `FLEET_WIRE_METHODS`. That list is the SSOT for both ends —
`dispatchFleetRequest` rejects anything off it, and `createFleetRegistryBridge`
generates exactly one browser method per entry. So the App-Worker bridge had no
`fleetMailboxMirror` at all, and the pane's `typeof bridge.fleetMailboxMirror !==
'function'` guard returned early forever.

The seam was Node-side only: a verb the browser could never name. Worse, the
omission failed closed and SILENT — from the pane's side an unreachable verb is
indistinguishable from a wired-but-empty mailbox, which is exactly the
honest-looking failure this surface exists to prevent. I had also attributed the
e2e's "feed not wired" state to a missing registryBridge without checking; this
allowlist is at least as likely the real cause, and I had not verified either.

Safe by construction today: the verb is read-observe with no lifecycle-write
authority (R3), its snapshot is body-free, admission is gated one layer down, and
its source stays deliberately unwired — so crossing the wire can only ever answer
an honest `unavailable`. Being on the list is what makes the seam real rather
than a claim.

The allowlist pin now asserts it, with the silent-failure reasoning recorded so
the next verb author does not repeat the omission.

Evidence: 253 fleet service specs green."
- 2026-07-16T23:03:17Z @neo-gpt cross-referenced by #15320
- 2026-07-16T23:04:01Z @neo-gpt marked this issue as being blocked by #15320
- 2026-07-17T00:24:09Z @neo-opus-grace cross-referenced by #15331
- 2026-07-17T04:47:30Z @tobiu referenced in commit `e770d1f` - "feat(agentos): FM cockpit per-agent mailbox pane — the read-only AgentDetail tab (#15316) (#15312)

* feat(agentos): MailboxMessage model + pane-owned AgentMailbox store — the mailbox tab data plane (#15270)

* feat(agentos): MailboxPane — the read-only S1 mailbox tab view with four honest states (#15270)

* test(agentos): MailboxPane render-state battery + explicit collapse seed on snapshot replace (#15270)

* feat(agentos): AgentDetail grows the Status/Mailbox tab structure — the countless mailbox tab seated (#15270)

* style(agentos): mailbox tab styles — rows, thread chips, honest-state tones over the fm token layer (#15270)

* test(agentos): the Mailbox tab proven live — drill → tab → injected snapshot → real thread toggle (#15270)

The mounted whitebox-e2e the pane's unit battery cannot give: a REAL DOM click
drills a resident, activates the countless "Mailbox" tab, and the pane renders
the honest `unobserved` state — no wiring injects snapshots yet, and the spec
asserts that truth rather than fabricating rows.

An adapter-shaped snapshot then enters through the possession seam
(`setProperties` on the mounted pane — exactly the wiring's contract) and the
whole render chain runs over the real DOM: rows newest-first, the collapsed
thread head carrying its `+N earlier` chip, and a real click on the thread head
expanding it inline — proving the `data-thread-id` → dataset → delegated-listener
path end-to-end. Read-only stays structural and is asserted live: zero
button/input/anchor renders anywhere inside the pane.

The drill target is the dedicated native `fm-card-drill` Button, not the avatar.
The avatar is a handler-less Image, so the gesture the sibling drill spec uses is
a silent no-op — chased with a console/network probe (no failing request; the
click simply lands on nothing) before reading the card's own wiring. Reported
separately: that sibling spec cannot pass on dev today, and the e2e battery does
not gate CI, so it went unseen.

Evidence: green locally against the live app worker at NEO_E2E_PORT.

* fix(agentos): the mailbox degrade line stops blaming the source for the adapter's refusals (#15270)

The mirror adapter's fail-closed refusals — an unbound request identity, an
asserted viewer that does not match the binding, an inadmissible namespace
subject — all arrive as `admission.state: 'unavailable'` beside
`capability.state: 'degraded'`, shaped identically to a genuine Memory Core
outage. The pane cannot tell them apart, and it was guessing:

  "Mailbox source degraded: asserted viewerIdentity does not match the bound
   request identity"

That sends an operator to debug Memory Core over a refusal the adapter made
about the VIEWER. The line now states only what this view actually knows — no
rows, and the owner's reason verbatim — rather than naming a cause it cannot
see. One neutral word instead of a fifth pane state: the reason text was always
doing the real work, and the sentence around it was the only thing lying.

Pinned per refusal reason, asserting the source-outage attribution cannot come
back and that a refusal shows neither rows nor a page window. Stash-verified
against the old copy.

Evidence: 318 agentos specs green.

* fix(agentos): a subject's mail cannot render under another agent's name (#15270)

Two fail-open honesty defects the review falsified on the exact head. Both are
bugs in shipped behavior, not gaps in unbuilt work.

**Subject possession.** AgentDetail re-seated only `record` and retained the
mailbox snapshot, so drilling resident A and then resident B rendered A's inbox
under B's name — mail attributed to an agent who never received it. A snapshot is
one SUBJECT's mail; it now cannot survive a re-seat onto a different resident, and
the pane drops to its honest `unobserved` until the wiring supplies this subject's
snapshot. A same-subject re-seat (a roster refresh restamping the record) keeps it,
or every poll would blank a correct pane.

**The unrecognized envelope failed OPEN.** `getPaneState()` returned `empty` for
`{}`, a torn payload, or any shape without the producer's rows array — rendering
"No active messages for @x", a confident and honest-LOOKING empty inbox fabricated
out of something the pane never understood. `empty` is a claim about the subject's
mail and may only be made when the producer actually said so; an unrecognized
envelope is `unobserved`. The producer's own empty answer is still explicitly
empty — the guard must not swallow the real zero-state.

Both are the same shape as the review finding that caught the adapter this
morning: a fact rendered about a subject that the surface was never entitled to
make about them.

Evidence: 291 agentos specs green, incl. the possession matrix (different-subject
drops, same-subject keeps) and a five-envelope fail-closed battery.

* feat(fleet): the mailbox mirror gets its Brain-side read seam (#15270)

The first half of the production path the review correctly found missing:
`readFleetMailboxMirror` existed only in its adapter, its JSDoc and its own
tests, so nothing could ever hand the cockpit a real snapshot.

`fleetMailboxMirror({subjectAgentId, limit, offset})` joins `fleetActivity` as a
read-observe verb over an injected `mailboxMirrorSource`, mirroring that slot's
DI contract exactly: the wiring owns the identity binding and read permissions,
so the SOURCE holds `resolveBoundIdentity` and the viewer-bound `listMessages`.
This bridge never imports MailboxService and never authors an admission fact — it
routes. Admission stays the Memory Core primitive's fail-closed CAN_READ_INBOX_OF
gate, decided one layer down.

The unwired degrade runs through the adapter's OWN pure half rather than a
hand-built literal, so the not-wired snapshot is shape-identical to a live one and
cannot drift from it. That matters beyond tidiness: the consuming pane now fails
closed on an envelope it does not recognize, so a bridge that degraded into a
DIFFERENT shape would be read as unrecognized rather than as honestly unavailable.
It never returns an empty inbox for a missing feed — "this agent has no mail" and
"we cannot see this agent's mail" are different claims, and only the producer is
entitled to the first.

Evidence: 253 fleet service specs green, incl. verbatim subject/bounds forwarding
and the unwired envelope asserting unavailable-not-granted, zero rows, and the
exact live key set.

* feat(agentos): the mailbox tab reads its own subject through the Fleet seam (#15270)

The Body half of the production path. The drill now READS: seating a resident
calls the authenticated `fleetMailboxMirror` verb and hands the returned snapshot
to the pane. The pane still renders and never fetches; this view owns the read
because it owns the drill, and it stays shell-agnostic so a popped-out inspector
reads exactly like a docked one. The Body never touches MailboxService — the
source behind the verb holds the identity binding, and admission stays the Memory
Core primitive's own fail-closed gate.

Race-safe by generation, not by hope. A drill is a gesture and the read is async,
so across a fast A→B drill the in-flight read for A resolves AFTER B is seated;
assigning it would render A's inbox under B's name — the possession defect
re-entering through the back door, invisible to the possession guard because the
record was already correct. A monotonic generation AND a re-checked subject must
both hold before a snapshot lands, and the spec proves it by resolving A's read
LAST and asserting A's mail never reaches B's pane.

Fail-closed throughout: an absent verb or a throw leaves the pane honestly
`unobserved`. It never fabricates a snapshot, and never renders "no mail" for a
read that did not happen.

Evidence: 294 agentos specs green, incl. the verb-fed drill (every read scoped to
the drilled subject), the ordered-race falsifier, and the fail-closed matrix.

* fix(agentos): thread collapse becomes a native button — the pane's only affordance was mouse-only (#15270)

The sole interaction was a delegated `click` on a plain div. A div owns no
Enter/Space and no tab stop, so the pane's ONLY affordance was unreachable by
keyboard — the same defect #14619 retired on the card drill, shipped again one
surface over. The toggle is now a real `<button>` naming its state with
`aria-expanded` + an `aria-label`, and the listener delegates to the BUTTON
rather than the row: a row-wide listener is an interactive region with no tab
stop.

The read-only spec had to be corrected to allow it, and the correction is the
point. It asserted zero `button|input|textarea|select|a` anywhere — which reads
as stricter but is the wrong bar: it conflated "no MUTATION verbs" (the record's
actual MUST-NOT) with "no interactive elements", and that conflation is exactly
what forced the toggle onto a div. Read-only constrains what an operator can
CHANGE, never whether they can reach what they can already see. The bar is now
mutation: no data-entry element, no mutation label, and the one admissible
control must carry `fm-mail-thread-toggle`.

The e2e now activates it by KEYBOARD — focus, then Enter — driving the real
data-thread-id → dataset → delegated-listener path. A div would swallow that
press; this is the assertion that would have caught the defect, where the prior
`.click()` sailed straight past it.

Evidence: 295 agentos specs green + the NL e2e green against the live app worker.

* feat(agentos): the mailbox page window can MOVE — row 51 becomes reachable (#15270)

Pagination was a range label. The pane rendered "1–50" and offered no way to see
message 51: a window with no transitions is a claim about the data, not access to
it, and the adapter's bounded `limit`/`offset` had no consumer.

Both edges are native buttons carrying the offset that reaches their page. The
pane REQUESTS (`pageRequest`) and never fetches — the owner holds the read seam
and the subject, so it performs the bounded re-read through the SAME path as the
drill. That routing is deliberate: a page request answered after the operator
drilled elsewhere inherits the generation latch and subject re-check for free,
rather than paging resident A's inbox into resident B's pane.

A step is DISABLED, never hidden, at the range edge — a control that vanishes
teaches the operator the surface is inconsistent, while a disabled one says "this
edge is the end". `next` is offered only on a FULL page, because a short page is
the producer telling us it ran out. A denial or degrade hides the bounds AND the
transitions together: a window over data you cannot see is a lie either way.

Evidence: 297 agentos specs green + the NL e2e green against the live app worker,
incl. the disabled-edge matrix and the pane-requests-never-fetches assertion.

* fix(agentos): the mailbox freshness chip ages — a fresh mailbox could stay fresh forever (#15270)

Freshness is time-relative everywhere in this cockpit, but the mailbox chip
classified against the clock only when something re-rendered the pane. The owner's
aging loop re-ran `applyPaneFreshness` for the four status panes and never touched
the mailbox tab, so a `fresh` mailbox stayed fresh indefinitely while its snapshot
silently rotted — a stale claim rendered as current, which is the exact failure the
freshness vocabulary exists to prevent, sitting inside the surface that invented the
vocabulary.

The chip now ages off the SAME owner timer. One timer, two consumers: a second
interval would age one surface twice and give the cockpit two clocks to disagree
with each other. Aging re-labels and never drops data — the rows are immutable
timestamped facts; only the honesty of the label moves.

Evidence: 298 agentos specs green, incl. a 5-minute wall-clock decay against an
unchanged snapshot asserting the data survives the re-label.

* fix(fleet): put fleetMailboxMirror on the wire — the read seam could not cross it (#15270)

Self-caught before the terminal review: the read verb existed on the Node-side
bridge and was NOT on `FLEET_WIRE_METHODS`. That list is the SSOT for both ends —
`dispatchFleetRequest` rejects anything off it, and `createFleetRegistryBridge`
generates exactly one browser method per entry. So the App-Worker bridge had no
`fleetMailboxMirror` at all, and the pane's `typeof bridge.fleetMailboxMirror !==
'function'` guard returned early forever.

The seam was Node-side only: a verb the browser could never name. Worse, the
omission failed closed and SILENT — from the pane's side an unreachable verb is
indistinguishable from a wired-but-empty mailbox, which is exactly the
honest-looking failure this surface exists to prevent. I had also attributed the
e2e's "feed not wired" state to a missing registryBridge without checking; this
allowlist is at least as likely the real cause, and I had not verified either.

Safe by construction today: the verb is read-observe with no lifecycle-write
authority (R3), its snapshot is body-free, admission is gated one layer down, and
its source stays deliberately unwired — so crossing the wire can only ever answer
an honest `unavailable`. Being on the list is what makes the seam real rather
than a claim.

The allowlist pin now asserts it, with the silent-failure reasoning recorded so
the next verb author does not repeat the omission.

Evidence: 253 fleet service specs green.

* fix(agentos): a bare rows array is not the producer's envelope (#15316)

Carried RA-2, first half. `getPaneState()` checked only `Array.isArray(rows)`, so
the reviewer's exact falsifiers rendered:

  {rows: []}          -> "No active messages for @x"
  {rows: [message]}   -> a stranger's message list

Both fabricate a claim about this agent's mail out of a shape the pane never
recognized. The producer emits `{capability, admission, rows, page}` on EVERY
state — its own degrades included, because the not-wired snapshot is built by the
same pure half as a live one. So a payload missing any member did not come from
that producer, and it now fails closed to `unobserved` BEFORE denied/degraded can
lend it their authority.

Structural recognition only: the pane checks that the envelope IS the producer's,
never what the producer decided inside it. Each single missing member is pinned on
its own, so a future producer that drops one cannot slip through on the strength
of the other three.

Still open from the same packet and NOT in this commit: admitted-subject
validation (a `granted` snapshot for Vega must not render under Ada). That one
needs a canonical mailbox identity on the roster row — Fleet `agentId` is the
registry id, not the mailbox identity, so comparing it to `admission.subjectAgentId`
would compare incompatible IDs. The DTO already carries `githubUsername`;
`mapRosterRow` drops it. Landing separately rather than bolting a second identity
grammar onto the Body.

Evidence: 298 agentos specs green, incl. the reviewer's two exact falsifiers.

* fix(agentos): the mailbox asks the envelope who it is about (#15316)

Carried RA-2, second half — the reviewer's exact falsifier: a `granted` snapshot
for Vega assigned onto Ada's pane rendered Vega's mail under Ada's name. The
possession guard and the generation latch both protect the SEQUENCE and neither
reads the envelope; Ada's record was already correct when it landed, so every
guard was satisfied. The envelope has to be asked who it is ABOUT.

The comparison could not use `agentId`. That is the Fleet registry key (`vega`)
while a mailbox subject is an AgentIdentity node id (`@neo-opus-vega`), and for
custom / multi-instance residents the two need not correspond at all — comparing
them would either never match or match the wrong resident. The roster DTO already
carried `githubUsername`; `mapRosterRow` dropped it. It now rides the record as
the resident's mailbox identity authority, and the READ asks with it too: aiming
the read with the registry key requested a subject that does not exist, so any
answer would have been about someone else.

Validation is scoped by what a state CLAIMS, not applied flatly:

- `rows` / `empty` are claims about THIS resident's mail → a verified subject is
  required. An unattributed zero is "no mail for someone" rendered as "no mail for
  HIM".
- `denied` names the subject inside its own sentence → unattributed, it would put
  a name there the producer never admitted.
- `degraded` reports on the READ, not the mail. The adapter's own refusals
  legitimately resolve NO subject, so gating them would swallow the honest reason
  and leave a silent pane. It is the one state that may speak unattributed.

A resident with no identity authority is honestly unverifiable: no read is issued
and nothing renders — never an implicit pass.

The fixtures now mirror production (the DTO carries the identity authority beside
the registry key); tests opt INTO the unverifiable case by passing null.

Evidence: 552 specs green (agentos + fleet), incl. granted/empty/denied-about-
another-resident and the no-identity-authority case.

* fix(agentos): the sample roster carries the mailbox identity authority (#15316)

Consequence of the admitted-subject guard, and the interesting part is what it
exposed. The sample seed carried no `githubUsername`, so with the guard live the
mailbox tab could never verify ANY resident in the dev app — permanently
`unobserved`. Honest, and useless.

The seed's `agentId` values happen to BE GitHub usernames (`neo-opus-grace`,
`neo-gpt`), which is exactly the coincidence the reviewer warned not to lean on:
`FleetControlBridge` defines the registry id as "defaults to githubUsername", so
they coincide until a custom or multi-instance resident arrives and they silently
do not. Falling back to `agentId` when the authority is absent would have made the
guard pass by comparing whatever happened to be there — the bug wearing a green
test. The seed now carries the authority explicitly, mirroring the production DTO.

The NL e2e aims its injected snapshot with the resident's `githubUsername` rather
than the registry key, so the fixture speaks the production contract: a snapshot
admitted for anyone else renders nothing, which is the whole point of the guard.

Evidence: 552 specs green (agentos + fleet) + the NL e2e green against the live
app worker.

* fix(fleet): the producer answers hasMore — an exactly-full final page trapped the operator (#15316)

Carried RA-3, and the trap is real. `atEnd` was inferred as `count < limit`, so a
FULL page always enabled Next. When that page was exactly the final one, the next
read returned an empty window at a positive offset — and the pane rendered a
global "No active messages" while hiding the page strip entirely, leaving the
operator stranded with no Previous and a fabricated empty-inbox claim.

`count === limit` is ambiguous by construction: the producer returns a slice with
no total, so a full page cannot distinguish "more follows" from "that was exactly
the last one". The consumer was guessing, and one of its two guesses was a dead
end.

The adapter now probes ONE row past the page and reports `page.hasMore` — the
producer answering rather than the consumer inferring. No count query, no total,
one extra row: `limit + 1` is what the question costs, `limit` is still what the
caller gets. The pane consumes the fact instead of deriving it.

The read bound and the page bound now differ by exactly one, deliberately, and
both are pinned so the divergence reads as intent rather than drift.

Evidence: 553 specs green (agentos + fleet) + the NL e2e green, incl. the
exactly-full-final-page falsifier — the case where count and limit agree and only
the probe knows the truth.

* fix(agentos): the page steps compose button.Base — I rebuilt the primitive by hand (#15316)

The design review's finding, and it is the sharper half of my own: I wrote "same
discipline as the card drill's native-button target" — but AgentCard reaches its
native <button> THROUGH `button.Base`. I read that contract, agreed with it, and
then reproduced the OUTCOME by hand with raw `{tag:'button'}` vdom. Measured: 4
raw button tags in this file against 0 in all of shipped apps/agentos/view/fleet/,
where `button.Base` is imported by FleetCockpit and AgentCard. That is the
`apps/**` gate: compose real components, never raw interactive vdom.

The glyphs were a second vocabulary for an idiom that already ships one —
`toolbar.Paging` steps with `iconCls: 'fa fa-angle-left'`. The steps now compose
`button.Base` with that vocabulary. Not adopting the toolbar wholesale (too heavy
for a tab pane), which was the reviewer's explicit latitude; the narrow ruling was
that the glyphs must not invent a vocabulary, and they no longer do.

The diagnosis underneath was the useful part: I had reported the affordances have
"no considered visual state" and the chip "may want a focus/hover treatment it
doesn't have". Those were not missing STATES, they were a missing PRIMITIVE —
handler wiring, iconCls, disabled styling and the neo-selection opt-in all live in
`button.Base` already. Composing it supplies them.

AND composing it exposed a defect I had asserted away. My JSDoc claimed "a
disabled control cannot fire". False: `component.Base`'s `disabled` adds the
`neo-disabled` CLASS, that styling carries no `pointer-events: none`, and
`button.Base.onClick` invokes `handler` without consulting it. A disabled step
still fires on click AND on keyboard Enter — the state is VISUAL, not semantic. At
the last page that reopens the exact trap `hasMore` closed: an empty window at a
positive offset, reachable by the operator it looks shut to. The handler now
refuses at a closed edge, and the e2e asserts `neo-disabled` rather than the
native matcher, which would fail on a control the framework considers disabled.

Remaining raw tags: the 2 thread-toggle variants inside the vdom row list. Raised
with the reviewer rather than guessed — `src/table/header/Button.mjs` emits a raw
tag INSIDE a src primitive (legitimate), while this cockpit's shipped row
precedent is component-per-row (AgentCard), and the two point at different fixes.

Evidence: 300 agentos specs green + the NL e2e green, incl. a closed edge refusing
an activation that the primitive would have let through.

* docs(fleet): the mailbox seam stops calling this transport authenticated (#15316)

The reviewer's JSDoc truth repair, which I had missed while landing his other
literals. Three of my own present-tense claims outran the evidence:

- "Rides the AUTHENTICATED registryBridge" (the mirror verb)
- "admission-gated one layer down" (the wire allowlist)
- "calls the AUTHENTICATED fleetMailboxMirror read verb, whose source holds the
  identity binding" (the app read)

None of that is true today. The Fleet HTTP surface binds no viewer identity and
authenticates nothing, so no admission can be attributed through it — which is
precisely why the source stays unwired and every call answers `unavailable`. The
prose claimed the destination while standing at the start. Each site now says what
it is: a staged, inert seam whose live read waits on authenticated viewer ingress
plus per-request identity binding.

This is the same defect class as the code it documents. A snapshot that says
"granted" without a bound viewer and a JSDoc that says "authenticated" without an
authenticated transport are one mistake wearing two hats — asserting the state you
intend instead of the state you have.

Ticket refs stripped from the durable comments per the archaeology gate: the
successor boundary is described by behavior, not by a number that rots.

Noted, NOT touched: `getBootIdentity`, `fleetActivity` and `fleetRoster` carry the
identical "authenticated registryBridge" phrase on dev. The same V-B-A that
falsified mine falsifies theirs — but they are shipped verbs outside this leaf's
scope, so they get reported rather than silently rewritten.

Evidence: 554 specs green (agentos + fleet).

* test(agentos): the fail-closed read assertion now outlives the read it proves absent (#15316)

Applying a reviewer's catch on a peer PR to my own spec within the hour. Reviewing
#15318 I noted its author had added a deliberate wait before a negative assertion
— "allow a wrongly-restored click route to commit" — because a negative that reads
too early passes for the wrong reason. Mine had the same hole.

The drill ISSUES an async mirror read. Asserting `snapshot === null` the instant
after the tab click proved nothing: it ran before that read could land, so it
would have stayed green with a snapshot arriving one tick later. The assertion I
believed was pinning "a fail-closed read assigns NOTHING" was actually pinning "we
looked before anything could happen".

A negative assertion has to outlive the thing it proves absent. It now waits, so
the null it reports is a finding rather than a coincidence.

Evidence: NL e2e green against the live app worker.

* fix(agentos): presence is not permission — an unavailable admission rendered "No active messages" (#15316)

Two carried RA-2/RA-3 properties from the terminal pass. Both real; the first is
the fail-open I thought I had closed.

**RA-2.** My envelope guard checked that `{capability, admission, rows, page}` were
PRESENT and stopped there. The reviewer's literal falsifier:

  {capability:{state:'wired'}, admission:{state:'unavailable', subjectAgentId:'@x'},
   page:{}, rows:[]}   ->  reached `empty`

A `wired` capability beside an `unavailable` admission is a read that never
happened. Its zero rows mean "we could not look" — and the pane reported that as
"No active messages for @x", the outcome of a read nobody performed. The same
shape with rows reached `rows`. Presence of the four members was never the
producer saying anything; only `granted` over `wired` is a mail claim, and the
page must be real numbers, since `{}` yields `NaN–NaN` bounds and NaN offsets — a
window invented out of absent values and rendered as fact.

**RA-3.** The a11y regression is mine, introduced while FIXING composition: moving
from raw `{tag:'button'}` to `button.Base` dropped the `aria-label` the raw tags
carried. Icon-only chevrons with no accessible name — the chevron IS the label to
a sighted operator and nothing at all to anyone else. And `disabled` adds only the
`neo-disabled` class: no native attribute, no aria. So the closed edge was
announced as ENABLED. The operator who most needs the boundary to be honest is
exactly the one it lied to.

Now all three, because each alone is a different half-truth: the class carries the
look, `aria-disabled` carries the meaning, the handler guard carries the refusal.

Named via `changeVdomRootKey` rather than a `vdom` config — `button.Base` builds
its own tree and a config would fight the primitive for ownership of it.

Still open, deliberately: Grace's composition RC on the 2 thread-toggle variants
inside the vdom row list. Her ruling, not my guess.

Evidence: 302 agentos specs green + the NL e2e green, incl. the reviewer's exact
shape and the aria state tracking bounds across a page change.

* test(agentos): audit the LAST unpinned claim — the freshness chip (#15316)

Hunting the class instead of the specimen, for once. Every other producer-derived
claim on this pane was fail-open at least once today: `empty` fabricated from a
torn envelope, then `empty` fabricated again from an `unavailable` admission — the
same bug at two layers, with a "fix" of mine in between. The pattern was never the
shape; it was that I stop at the first guard which makes the reported falsifier go
green, and never ask where else the class lives.

So I asked. The chip is the last surface here that renders an assertion derived
from the producer: it reads `capability.capturedAt` and renders currency. A
snapshot whose timestamp is absent or unparseable must not become "fresh" —
that would be currency invented from a value nobody supplied.

It doesn't. `classifyPaneFreshness` already degrades to `unobserved` for anything
it cannot place in time — "a claim we cannot place in time is never rendered as
current". The audit came back CLEAN, which is the point: this commit is the pin,
not a fix. The behavior my whole honest-state design leans on is now a witness
rather than an assumption, across six unplaceable timestamp shapes.

Note the boundary it proves: the rows still render while the age is unknown. The
producer authorized the mail; only the currency claim is unavailable. Failing the
rows closed there would be the opposite error — refusing data the producer DID
grant because one adjacent fact was missing.

Evidence: 18 mailboxPane specs green.

* docs(agentos): correct disabled pointer semantics (#15316)

* docs(agentos): the routed gates DO close — the guard covers direct entry only (#15316)

The disabled-step claim overshot twice, and the second wording still overshot
after correcting the first.

V-B-A of both sources:
- resources/scss/src/Global.scss:25 — `.neo-disabled` DOES set `pointer-events:
  none`. My original "carries no pointer-events" was false.
- src/manager/DomEvent.mjs:182 — `if (!component || (eventName !== 'resize' &&
  component.disabled)) { break }` breaks the listener walk before every
  non-resize listener. A keyboard Enter on a native button arrives as a click on
  that same route, so it stops there too.

So "keyboard activation can still reach the handler" is false for any ROUTED
activation. I had read `button.Base.onClick` and asserted about the whole path.

The honest residual is DIRECT entry: `onClick` never consults the config, so a
programmatic call reaches `handler` with the routed gates bypassed. That is what
`requestPage`'s check covers — defense in depth, not the only gate.

The spec comment was the same error wearing a fixture: it calls
`pane.onPrevPageClick()` directly while claiming that is "exactly what a keyboard
user does". It is precisely the entry a keyboard user does NOT take. The test is
correct and stays; only its description was lying about what it witnesses.

The AT half of the residual (no native `disabled` → announced enabled, still
focusable) is `aria-disabled`'s job, already pinned at mailboxPane.spec.mjs:470.

Evidence: 303/303 agentos unit specs pass (comment/title-only; no behavior delta).

* docs(agentos): the pane stops asserting the Button layer's contract as its own (#15316)

Reviewing @neo-gpt-emmy's native-Button-disabled PR falsified two claims here —
both mine, both written hours ago, and this PR exists precisely to correct false
disabled-semantics claims, so shipping it with a fresh one would be a poor joke.

The e2e comment said Neo's `disabled` "never" projects a native attribute, and
`applyPageStep`'s JSDoc said a styled-shut control is "still reachable". Once the
Button layer projects a native `disabled`, both are false: the control leaves the
tab order and is announced disabled.

The deeper error is the same one in both: I stated a neighbouring LAYER's contract
as a fact about mine, then depended on it. A pane has no business asserting what
Button.Base does or does not emit. Rewritten to hold either way — `neo-disabled` is
`component.Base`'s contract and is what this pane may rely on; `toBeDisabled()` is
deliberately not asserted because that pins the Button layer's projection, which is
the Button layer's to pin.

The class-alone announcement gap is still real and still why `aria-disabled` is set
here. When a native `disabled` lands on the Button layer, that call becomes
redundant rather than wrong — noted at the method that would own the cleanup, so
the next reader finds the disposition instead of rediscovering the interaction.

No behavior change; the e2e asserts the class, which holds in both worlds.

Evidence: 303/303 agentos unit specs green.

* docs(agentos): stop asserting an unmerged PR as fact, and stop over-crediting aria-disabled (#15316)

@neo-gpt's non-gating correction, and both overstatements are the same error I
committed the previous fix to remove — borrowing a neighbouring layer's fact and
depending on it. I removed the false version and wrote a confident replacement.

The e2e said buttons "are gaining" a native disabled attribute. That is an UNMERGED
PR (#15327) stated as present-tense reality. If it lands the sentence is premature;
if it changes shape the sentence is wrong; either way the comment is asserting
someone else's unshipped work. Cut — the reason to assert `neo-disabled` stands on
its own: it is what `component.Base` applies, so it is what this pane may rely on,
and `toBeDisabled()` asserts the Button layer's contract rather than ours.

The applyPageStep triad over-credited `aria-disabled`: "the class carries the look;
aria-disabled carries the meaning; the handler guard carries the refusal" reads as
though the three cover the edge between them. They do not. `aria-disabled`
ANNOUNCES — it does not remove the control from the tab order, so a keyboard user
still lands on a closed edge. That is not a defect (they are told, correctly, that
it is closed), but the residual has to be stated rather than implied away. It also
now says which entry the handler guard covers — direct, since a routed activation
is already stopped upstream — instead of leaving "refusal" to sound total.

Comments only. No behavior change; the e2e asserts the class, which is unaffected.

Evidence: 303/303 agentos unit specs green.

* docs(agentos): correct disabled matcher rationale (#15316)

* docs(agentos): scope disabled test contract (#15316)

* test(agentos): prove the ARIA announcement reaches the DOM, not just the vdom (#15316)

The closed edge's announcement was pinned ONLY as `prev.vdom['aria-disabled']` — a
vdom assertion. This repo has a documented gotcha where a vdom attribute write does
not flush to a mounted node, so a unit test asserting `.vdom` passes while the DOM
is broken. That is the whole failure of the night in miniature: the assertion proves
the object, not the thing the operator's screen reader would read. Nothing anywhere
proved the attribute mounts — grep for `aria-disabled` under e2e/ returned only a
comment.

`toBeDisabled()` closes it. Playwright honours `aria-disabled` for roles in
`kAriaDisabledRoles` (verified at playwright-core's coreBundle: `button` is the
second entry), so on a control carrying no native attribute it passes off the pane's
own `aria-disabled`. That property is what makes it worth asserting here, not a
reason to avoid it.

The pass is unambiguous: `neo-disabled` is a CLASS and Playwright does not read it
for disabled-ness, and this head emits no native `disabled`. So the matcher can only
be reading aria — the green cannot come from anywhere else. Both matchers stay,
because neither subsumes the other: the class pins the cross-component contract, the
matcher pins the announced state live.

Found while V-B-A'ing @neo-gpt's correction that `toBeDisabled()` accepts BOTH native
and aria — he was right, and it has a consequence beyond the wording: the matcher
would already have passed here, which means it was available as mounted evidence the
whole time and I had reasoned my way out of using it.

Evidence: FleetMailboxTabNL e2e 1/1 passed against the real DOM (chromium, 3.0s).

* docs(agentos): a native disabled does not universally remove focus — the root tag decides (#15316)

The JSDoc said a native `disabled` is the control's own contract to state — then
asserted, parenthetically, what it would do: "take it out of the tab order". That is
true for a `<button>` root and FALSE for an anchor one, where the attribute has no
semantics at all. Refusing to assume a neighbouring layer's contract and then
narrating its behavior anyway is not refusing.

Found via @neo-gpt-emmy's #15327: `Button.Base` roots are polymorphic —
`updateTag()` renders `<a>` whenever `url` or a non-editing `route` is set. She
caught it in the head I had already APPROVED, after I swept every Button subclass
and declared the class covered. The subclass axis was clean; the hole was the base's
own tag axis, one level up from where I aimed the falsifier.

This pane's page steps are plain buttons, so the claim happened to hold for them.
That is exactly what makes it worth removing: a universal that is accidentally true
here reads as verified and is not.

Comments only; no behavior change.

Evidence: 18/18 mailboxPane unit specs green.

(The first version of this message claimed 24/24. I typed the number before reading
the runner output, in a commit about removing unverified claims. Amended after
catching it; the run says 18. Recorded rather than quietly corrected, because a
number I did not read is the same defect as a mechanism I did not check.)

---------

Co-authored-by: neo-gpt <neo-gpt@neomjs.com>"

