---
number: 17687
title: >-
  fix(agentos): remove the misleading token-limit header from wake prompts
  (#17685)
author: neo-preview
state: OPEN
createdAt: '2026-08-24T08:20:19Z'
updatedAt: '2026-08-24T08:21:04Z'
closedAt: null
mergedAt: null
head: fix/17685-remove-wake-token-header
base: dev
url: 'https://github.com/neomjs/neo/pull/17687'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17685

## The problem

The `[session-context: N tokens, gate at M]` header rendered into wake prompts reads as a context-occupancy gauge while actually measuring last-turn `input + cache.read` against the wake-deferral threshold — thresholds sized from Kimi-K3 economics that do not transfer across model families. Live specimen: a `231K/250K` header was read as "88% of context used" (operator-corrected; real window 1M, ~23%), which then drove a wrong operational decision.

## The fix

Remove the display block from `formatLocalWakeDigest` and its now-dead `formatContextTokens` helper. The context gate POLICY is untouched — oversized wakes still defer silently; the envelope still carries probed `sessionContext` for any future instrument. Only the misleading rendering dies.

## AC Evidence

| AC | Proof |
|---|---|
| AC-1 | Display block removed; no `[session-context:` line can be emitted regardless of probe state |
| AC-2 | Receiver spec arms asserting the *envelope payload* field remain green — instrument survives rendering removal |
| AC-3 | Full wake suite 316 passed at this head |

## Deltas from ticket

The stale spec arm asserting the line's presence/order was removed rather than inverted — its replacement already exists as the receiver-side envelope-payload arms.

## Test Evidence

Full wake suite 316 passed (`ai/daemons/wake`, workers=1) including both retained receiver envelope-payload arms.

Evidence: L2 (unit arms over digest formatting + envelope payload) → L2 required (ACs govern prompt rendering only). Residual: none.

Authored by Eos (ox-alpha, OpenCode). Session b644277f-7fcf-4079-a363-a7f9099a4566.

## Post-Merge Validation

None — rendering-only removal; the deferral policy and envelope instrumentation are covered by retained unit arms at this head.
