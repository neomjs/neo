---
id: 16515
title: 'Measured debt census of the ai folder: duplication, boundaries, placement'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-fable
createdAt: '2026-08-04T20:41:47Z'
updatedAt: '2026-08-08T11:46:36Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16515'
author: neo-fable
commentsCount: 6
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-07T12:49:56Z'
---
# Measured debt census of the ai folder: duplication, boundaries, placement

## Context

Operator assessment during the same friction→gold discussion (2026-08-04): *"imho the debt inside the ai folder is extreme."* Concurred with first receipts measured live; this ticket converts the assessment into an instrument so follow-up work is evidence-ranked instead of vibes-ranked. Live latest-open sweep at filing: latest 15 checked, no equivalent census lane; `#16488`/`#16495` are single-boundary instances the census will contextualize, not duplicates.

## The Problem

`ai/` is 682 `.mjs` files / ~228k lines, and the visible sediment starts at its root: FIVE config-ish entries (`config.mjs`, `config.template.mjs`, `configBase.mjs`, `ConfigProvider.mjs`, `planeConfig.mjs`), `agent/` + `demo-agents/` + `Agent.mjs`, `services/` + `services.mjs`, `docs/` + a root `sdk-manifest.md`. Known systemic classes with prior receipts: module-scope heavy imports making the SDK boundary non-load-bearing (`#16488`: chromadb at module scope forces a Brain-tier package to merely load; ~20 external readers block the clean init refactor), placement drift severe enough that the structural-pre-flight skill exists because of `ai/` misfilings (PR `#11008` lineage), and the ADR-0019 forbidden-pattern catalog existing at all. First duplication family already measured and ticketed: four lock/lease implementations (`#16514`).

The debt's cost is not size — it is that boundaries are not load-bearing, so every change's blast radius is unknowable without archaeology.

## The Architectural Reality

- Instruments exist: `npm run --silent ai:structure-map -- --files --loc` (the placement gate), `rg` for pattern families, ADR-0019 as the config-authority lens, `learn/agentos` ADRs as the placement authority.
- Hard constraint: a downstream production plane deploys from this tree — the census is READ-ONLY; no moves, no renames, no import changes ride this lane. Prescriptions become tickets, each individually deployment-safe.

## The Fix

A census artifact (ticket comment series or one `learn/agentos`-adjacent report, driver's call) with four measured chapters:

1. **Duplication families** — pattern census (locks/leases done → `#16514`; candidates: retry/backoff shapes, atomic-write/tmp-rename, config readers, health/receipt emitters, path resolution), each with file list + call-site count + rule-of-three verdict.
2. **Boundary violations** — module-scope imports crossing the SDK/service boundary (the `#16488` class, full set), import cycles, `ai/`→`src/` and reverse reaches outside sanctioned entrypoints.
3. **Placement drift** — files whose home contradicts the structure-map/ADR authority (the scripts-vs-daemons-vs-services axis), root-level sediment disposition (incl. the five config entries — audited UNDER ADR-0019's SSOT lens, which may bless some of them; the census records WHY per entry).
4. **Ranked follow-up set** — each finding priced (blast radius × fix cost × deployment risk) and shaped into pool tickets; the ranking IS the deliverable that makes every later move cheaper.

## Acceptance Criteria

- [ ] All four chapters published with reproducible commands per claim (every number re-derivable by any peer).
- [ ] Zero tracked-file mutations from this lane (read-only census; deployment redline untouched).
- [ ] Follow-up tickets filed for the top-ranked findings (rule-of-three families and boundary classes at minimum), each one-PR-shaped or an explicitly licensed split.
- [ ] The ADR-0019 lens applied to every config-surface claim (critical gate: read the ADR before authoring that chapter).

## Out of Scope

- Executing any consolidation/move (each rides its own ticket).
- `#16488`/`#16495`/`#16514` themselves (in flight; the census references and ranks around them).

## Decision Record impact

none by the census itself; individual findings may propose `amends` cases, each carried by its own follow-up ticket.

## Related

`#16514` (first family, filed from the same discussion), `#16488`, `#16495`, ADR-0019, the structural-pre-flight skill lineage (PR `#11008`).

Origin Session ID: 1913de09-6dc0-4d1e-a9a3-b51c33b46cdc

Retrieval Hint: `query_raw_memories("ai folder debt census duplication families boundary violations placement drift read-only")`


## Timeline

- 2026-08-04T20:41:48Z @neo-fable added the `enhancement` label
- 2026-08-04T20:41:49Z @neo-fable added the `ai` label
- 2026-08-04T20:41:49Z @neo-fable added the `architecture` label
- 2026-08-07T10:50:22Z @neo-fable assigned to @neo-fable
### @neo-fable - 2026-08-07T11:50:43Z

## Census — baseline + Chapter 1 (duplication families), measured 2026-08-07 at dev `4d8e9ead75`

Read-only lane per the ticket's deployment redline; every number re-derivable by the command beside it.

**Baseline:** 688 `.mjs` files, 230,518 lines (`find ai -name "*.mjs" | wc -l` / `… -exec cat {} + | wc -l`). Root sediment unchanged from filing: 5 config-ish entries + `Agent.mjs` + `services.mjs` + `sdk-manifest.md` (`ls ai/*.mjs ai/*.md`).

### Chapter 1 — duplication families (rule-of-three verdicts)

| family | measure | command | verdict |
|---|---|---|---|
| retry/backoff shapes | **31 files** carry own retry/backoff vocabulary | `rg -l "backoff\|maxRetries\|retryDelay" ai -g '*.mjs' \| wc -l` | rule-of-three EXCEEDED ×10 — consolidation candidate, but classify per-shape first (poll-retry vs transport-retry vs lease-retry are different contracts; a single util that blurs them would be worse) |
| atomic write (tmp→rename) | **13 files** hand-roll `renameSync` atomicity | `rg -l "renameSync\(" ai -g '*.mjs'` | rule-of-three EXCEEDED — strongest single-util candidate in the census: the contract is uniform (write-tmp, rename-over), the failure modes are identical, and the call sites span services/daemons/wake |
| `fileURLToPath(import.meta.url)` boilerplate | **128 files / 132 occurrences** | `rg 'fileURLToPath\(import\.meta\.url\)' ai -g '*.mjs' \| wc -l` | the volume king, but LOW-value consolidation: it is idiomatic node ESM, one line, zero drift risk — recording as measured-and-declined (a shared helper would add an import to save a line) |
| lock/lease implementations | 13 modules (Vega's measured census) | see #16514 | already owned — ranked around, not re-measured |
| healthcheck/receipt emitters | **65 files** mention healthcheck surfaces | `rg -li "healthcheck" ai -g '*.mjs' \| wc -l` | needs a shape-census pass before any verdict (mentions ≠ implementations); chapter-4 ranking input only |

### Chapter 1½ — the root config-entry consumer map (raw import counts, NO authority judgment yet)

| entry | importers | command |
|---|---|---|
| `config.mjs` | **156 files** | `rg -l "config\.mjs'" ai -g '*.mjs' \| wc -l` |
| `configBase.mjs` | 21 | same pattern |
| `ConfigProvider.mjs` | 19 | same |
| `config.template.mjs` | **11** | same |
| `planeConfig.mjs` | 11 | same |

Raw observation only: five config surfaces, five distinct consumer sets, and the deploy-template artifact has 11 importers. **The authority audit of these five entries is Chapter 3 and is gated on the ADR-0019 read (critical gate 10) — no blessing/indicting here.** The `config.mjs` importer pattern match also catches `config.template.mjs'` suffix hits; the exact-entry split rides Chapter 3's per-entry audit.

Next chapters: (2) boundary violations (module-scope SDK-crossing imports, the `#16488` class — Ada's retirement lane in flight, census will rank around it), (3) placement drift + the five-entry audit under the ADR-0019 lens, (4) the ranked follow-up set. Chapters land as separate comments; each is independently consumable.

### @neo-fable - 2026-08-07T12:08:31Z

## Census — Chapter 2 (boundary reaches), measured 2026-08-07 at dev `4d8e9ead75`

| boundary | measure | command | reading |
|---|---|---|---|
| module-scope external-SDK imports | **29 import statements** (chromadb / openai / @google / @modelcontextprotocol / @anthropic at module scope) | `rg -n "^import .*(chromadb\|openai\|@google\|@modelcontextprotocol\|@anthropic)" ai -g '*.mjs' \| wc -l` | the `#16488` class in full; Ada's in-flight retirement lane (#16488) owns the canonical fix pattern — the census RANKS AROUND it and hands her the complete file set on request rather than duplicating the lane |
| `ai/` → `src/` reaches | **217 of 688 files (31.5%)** import from the Body tree | `rg -l "from '.*\.\./src/" ai -g '*.mjs' \| wc -l` | measurement, NOT a violation count: the Brain consuming the Body's class system (`Neo.mjs`, `core/Base`, `setupClass`) is design; the census question is the SANCTIONED-ENTRYPOINT vs DEEP-REACH split (a daemon importing component internals is a different animal than a service extending `core.Base`). That split is a per-import classification pass — chapter 4 decides whether it's worth its own ticket or rides the placement chapter |
| `src/` → `ai/` reverse reaches | **0 files** | `rg -l "from '.*\.\./ai/" src -g '*.mjs' \| wc -l` | **the hemisphere boundary holds perfectly one-way** — the Body never imports the Brain. Worth naming as the census's cleanest finding: whatever debt lives inside `ai/`, it does not leak into the engine |

Import-cycle detection deferred to chapter 4 tooling assessment (needs `madge`-class analysis or a small script; not a grep). Chapter 3 (placement drift + the five root config entries under the ADR-0019 lens) remains gated on the ADR read per critical gate 10.

### @neo-fable - 2026-08-07T12:48:08Z

## Census — Chapter 3 (the five root config entries under the ADR-0019 lens + placement drift), 2026-08-07 at dev `fed9bf6e7d`

Gate 10 honored: ADR-0019 read in full before this chapter; every disposition below cites its section.

### 3a. The five entries — each has a NAMED role, and the structure is healthier than the raw counts suggested

| entry | ADR role | disposition |
|---|---|---|
| `config.template.mjs` | the CANONICAL template (§2.1: overlay = "thin child of deltas"; both register `Neo.ai.Config`, first-wins gatekeep) | SANCTIONED as-is; its 11 importers are the template's legitimate audience (tests per C3's rule + arbitration sites) |
| `config.mjs` | the operator OVERLAY (thin child of deltas) | the chapter-1½ "156 importers" DECOMPOSES: the import-specifier histogram (`rg -o "from '[^']*[Cc]onfig[^']*\.mjs'" ai -g '*.mjs' \| sort \| uniq -c`) shows the dominant consumed surfaces are the PER-SERVER Tier-2 configs (memory-core 40, knowledge-base 28, github-workflow 15, gitlab 4) — the realm-chain design working, not 156 files on one root. Root-overlay true consumer count needs resolved-path analysis; structurally NOT the sediment it looked like |
| `configBase.mjs` | the leaf-declaration base (§10: the `plane` subtree home; §5.5 chicken-and-egg clause covers config-layer helpers) | 16 direct importers, path-shapes consistent with config-file consumption — sanctioned pending a runtime-stray sample in chapter 4 |
| `ConfigProvider.mjs` | THE PRIMITIVE (§6.2: consumers "do not need" it — only primitive-changers and the config files constructing the tree) | 20 importers at per-server-config path depths — the tree constructing itself, sanctioned; a runtime-stray sample rides chapter 4 |
| `planeConfig.mjs` | the COLLAPSED twin (§10.1: one exported constant + pure functions; the twin shape RETIRED 2026-07-25 with receipts) | ADR-curated within the last two weeks — lowest re-audit priority in the set |

**Net chapter-3a verdict: the root's five config entries are not sediment — they are the ADR's own architecture with documented roles.** The census's contribution is the decomposition receipt; the ticket's original "FIVE config-ish entries" framing (mine) overstated the drift.

### 3b. One LIVE violation class found: C3 ×7

**7 test files import the overlay (`ai/config.mjs`) instead of the canonical template** (`rg -l "\.\./config\.mjs'|/ai/config\.mjs'" test -g '*.mjs'` — initTier1ConfigMigration, ContentPolicy, TenantRepoSyncService, orchestrator daemon.spec, initServerConfigs.spec +2). ADR §3 tags this class `[live: #11976]`; disposition against that ticket's live state lands in chapter 4's ranking.

### 3c. Non-config root sediment (structure-map lens, dispositions only)

- `Agent.mjs` + `agent/` + `demo-agents/`: three agent-named surfaces at/near root — needs an ownership read (chapter 4 candidate, small).
- `services.mjs` + `services/`: same pattern-pair; `services.mjs` likely a barrel — verify + disposition in chapter 4.
- `docs/` + root `sdk-manifest.md`: one doc artifact outside its folder — trivial placement item, batch into any adjacent cleanup.
- `initServerConfigs.mjs` (13 importers from config files): §5.5's config-layer-helper clause plausibly sanctions it; verify its Neo-import-freedom in chapter 4.

Chapter 4 (ranked follow-ups + ticket filings) closes the census.

- 2026-08-07T12:49:02Z @neo-fable cross-referenced by #16628
- 2026-08-07T12:49:27Z @neo-fable cross-referenced by #16629
### @neo-fable - 2026-08-07T12:49:55Z

## Census — Chapter 4 (ranked follow-up set) — the census is COMPLETE

Ranking axis per the ticket: blast radius × fix cost × deployment risk. Every finding below is either FILED, DISPOSED, or explicitly ranked-and-parked with its claimable prescription.

| rank | finding | disposition |
|---|---|---|
| 1 | **C3 re-accumulation** — 7 live overlay-imports in tests, #11976 CLOSED, class unguarded (not in the ADR-0019 §7 lint set) | **FILED → #16628** (repair + lint extension + ADR tag sync; pool) |
| 2 | **tmp-rename atomic-write family** — 13 hand-rolls, uniform contract | **FILED → #16629** (one owned primitive + batched migration; pool) |
| 3 | retry/backoff — 31 files | PARKED with prescription: a CLASSIFICATION pass first (poll-retry vs transport-retry vs lease-retry are different contracts); consolidating before classifying would blur real semantics. Claimable as-is from this comment; rule-of-three exceeded ×10 so the pass is worth a session |
| 4 | ai→src sanctioned-vs-deep split — 217 reach files | PARKED with prescription: per-import classification (entrypoint-class reads of `Neo.mjs`/`core/Base`/`setupClass` = design; deep component-internal reaches from daemons = findings). Needs a small script, not grep; possibly `madge`-class tooling shared with the deferred import-cycle scan |
| 5 | root sediment placement batch — `Agent.mjs`/`agent/`/`demo-agents/` ownership read, `services.mjs` barrel verify, `sdk-manifest.md` → `docs/` | PARKED as one tiny batch (each individually deployment-safe); below filing threshold alone, natural rider on any adjacent `ai/` cleanup PR |
| 6 | healthcheck emitters — 65 mention-files | DISPOSED note-only: mentions ≠ implementations; revisit only if a concrete healthcheck bug motivates the shape-census |
| 7 | `fileURLToPath` ×128 | DISPOSED measured-and-declined (chapter 1: idiomatic one-liner; a helper costs more than it saves) |
| — | lock/lease ×13 modules | OWNED elsewhere (#16514, Vega) — ranked around throughout |
| — | module-scope SDK imports ×29 | OWNED elsewhere (#16488, Ada's retirement lane) — the census's full file set stands ready in chapter 2 on request |

**AC audit:** all four chapters published with reproducible commands ✓ · zero tracked-file mutations from this lane ✓ (read-only throughout; the two filings are tickets, not tree changes) · follow-up tickets filed for the top-ranked findings ✓ (#16628, #16629) · ADR-0019 lens applied to every config-surface claim, read-gate honored before chapter 3 ✓.

**The census's one-line summary:** the debt is real but SHAPED — the root config "sediment" turned out to be the ADR's own architecture (chapter 3's decomposition), the hemisphere boundary is perfectly clean one-way (chapter 2's zero), and the actionable residue is two filed tickets, two parked classification passes, and one guard gap that let a cleaned class regrow. Closing as delivered.

### @neo-fable - 2026-08-07T12:49:56Z

Delivered — all four ACs met per the chapter-4 audit above; the census artifact is the four chapter comments on this ticket.

- 2026-08-07T12:49:56Z @neo-fable closed this issue
- 2026-08-08T11:45:16Z @neo-fable cross-referenced by PR #16678
### @neo-fable - 2026-08-08T11:46:35Z

## Census correction — chapter 3b/4's C3 finding was an instrument error (2026-08-08, the census author)

The C3 row claimed "7 overlay imports re-accumulated under `test/` after #11976". Intake on the follow-up ticket #16628 falsified it: the census instrument was `rg -l "\.\./config\.mjs'|/ai/config\.mjs'"` — a string grep that lists files *mentioning* the pattern. All 7 hits at head `ac1c197648` are test-SUBJECT occurrences (fixture strings written to temp roots, lint-spec fixture sources, bootstrap path-lists, a forbidden-asset-path assertion, one comment). The precise instrument — `lint-config-template-ssot.mjs`'s AST-resolving test config-authority rule, shipped with #12451 — reports **0 violations**, and a corrected synthetic probe red-proves it fires on a real overlay import.

What survived the correction is narrower and real, delivered via #16628 / PR #16678: the lint's CI workflow path-filtered on config surfaces only, so the test-scanning rule could never fire on an introducing PR (scanned ⊆ watched now holds), and ADR-0019 §3/§7 under-reported the shipped coverage.

Method note for future census chapters: `rg -l` answers "which files mention X", never "which files DO X" — import-semantics questions need the AST instrument (or at minimum import-statement-anchored patterns), and any derived count belongs next to the command that produced it so the reader can see which question was actually asked.


