---
id: 10965
title: MCP server common base class with standardized extension points
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - 'release:v13'
assignees:
  - neo-opus-4-7
createdAt: '2026-05-08T15:02:06Z'
updatedAt: '2026-05-08T17:57:47Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10965'
author: neo-opus-4-7
commentsCount: 1
parentIssue: 10960
subIssues:
  - '[x] 10980 Document MCP server BaseServer extension contract'
subIssuesCompleted: 1
subIssuesTotal: 1
blockedBy: []
blocking: []
closedAt: '2026-05-08T17:46:55Z'
---
# MCP server common base class with standardized extension points

## Context

Filed 2026-05-08 as the **M2 prep ticket** on the v13 release path. Per [`learn/agentos/v13-path.md`](https://github.com/neomjs/neo/blob/dev/learn/agentos/v13-path.md) §4 D2 (Critical Architectural Decision):

> *Each MCP server currently bootstraps its own infrastructure independently. A Common Base Server Class with extension points (`registerTools`, `registerServices`, `registerMiddleware`, `registerHealthChecks`) standardizes the per-server-author pattern, reduces boilerplate, and prepares uniform substrate for M6 SDK migration.*

Sequences as **M2** on the milestone path: M1 (substrate stabilization, in flight) → **M2 (Common Base Server Class)** → M3 (Orchestrator Daemon, [#10956](https://github.com/neomjs/neo/issues/10956), @neo-gemini-3-1-pro's lane) → M4-M7 downstream.

## Server Inventory (corrected 2026-05-08 per @tobiu)

There are **5 MCP servers**, partitioned into two tiers:

**Tier-1 — Cloud-Native (deployment targets, primary M2 + M6 scope):**
- `ai/mcp/server/knowledge-base/Server.mjs` (277 lines)
- `ai/mcp/server/memory-core/Server.mjs` (580 lines)
- `ai/mcp/server/github-workflow/Server.mjs` (226 lines)
- `ai/mcp/server/neural-link/Server.mjs` (212 lines)

**Tier-2 — Local-Only (gemma4-style local agent only; NOT deployment-target):**
- `ai/mcp/server/file-system/Server.mjs` (149 lines)

Tier-2 is included in M2 migration scope for *consistency* (shared base class, lower per-server-author cognitive load) but is **out of scope for M6 SDK migration** (deployment-only concern).

## The Problem

The 5 server entry points (`ai/mcp/server/*/mcp-server.mjs`) are thin CLI wrappers (~11-35 lines each) that just `Neo.create(Server).ready()`. The actual bootstrap logic lives in per-server `Server.mjs` files (totaling ~1444 lines across the 5).

Each `Server.mjs` independently:

1. Creates the MCP transport layer (`McpServer` from `@modelcontextprotocol/sdk`)
2. Wires `setupRequestHandlers(mcpServer)` for ListToolsRequestSchema + CallToolRequestSchema
3. Wires services (`DatabaseService`, `HealthService`, `KBRecorderService`, etc.)
4. Loads `aiConfig` from `./config.mjs` (gitignored; cloned from `config.template.mjs` at `npm prepare`)
5. Runs healthcheck + log startup status
6. Connects transport (`StdioServerTransport` for stdio mode OR `TransportService.setup()` for sse mode)

There's no shared base class for the server itself. `ai/mcp/server/shared/` currently holds service-level primitives (`BaseConfig.mjs`, `services/AuthService.mjs`, `services/RequestContextService.mjs`, `services/StdioIdentityResolver.mjs`, `services/AuthMiddleware.mjs`, `services/TransportService.mjs`, `helpers/`), but each per-server `Server.mjs` composes these primitives independently.

Knowledge-base bootstraps differently from memory-core (which has 580 lines of bootstrap), which differs from github-workflow — drift accumulates per-server-author session, and middleware-ordering bugs (e.g., auth-before-context-establishment) recur per-server because there's no single source of truth.

A Common Base Server Class with explicit extension points eliminates this drift, gives each server a clear "what to override" surface, and creates uniform substrate for M6 SDK migration of the Tier-1 servers.

## The Architectural Reality

**Current Server.mjs shape (each extends `Neo.core.Base`):**

```javascript
class Server extends Base {
    static config = { className: 'Neo.ai.mcp.server.X.Server' }
    mcpServer = null
    configFile = null

    createMcpServer() { /* per-server: name, version, capabilities */ }
    async initAsync() {
        await super.initAsync();
        // load config, init services, wait for ready, healthcheck, connect transport
    }
    setupRequestHandlers(mcpServer) { /* per-server: ListTools + CallTool handlers */ }
    logStartupStatus(health) { /* per-server: warn/info logging */ }
}
export default Neo.setupClass(Server);
```

All 5 servers already partially share a common shape via `extends Base` + `static config` + `initAsync()` + `Neo.setupClass()`. The **divergence** is in:

- `createMcpServer()` — server name + version + capabilities
- `initAsync()` — service initialization order + which services to await
- `setupRequestHandlers()` — duplicated boilerplate for ListTools/CallTool wiring
- `logStartupStatus()` — per-server health-output formatting
- Transport selection — duplicated stdio-vs-sse branching

**Existing shared primitives (service-level, not server-class-level):**
- `ai/mcp/server/shared/BaseConfig.mjs` — config substrate
- `ai/mcp/server/shared/services/{AuthService,RequestContextService,StdioIdentityResolver,AuthMiddleware,TransportService}.mjs`
- `ai/mcp/server/shared/helpers/`

**Missing primitive (this ticket):** `ai/mcp/server/BaseServer.mjs` — class extending `Neo.core.Base` with template-method-pattern extension points:

```javascript
class BaseServer extends Neo.core.Base {
    static config = {
        className: 'Neo.ai.mcp.server.BaseServer'
    }

    // Lifecycle (final, NOT overridden by per-server):
    async initAsync() { /* load config, init services, healthcheck, connect transport */ }
    async start() { /* orchestrates registerXxx() calls in canonical order */ }
    async stop()  { /* graceful shutdown sequence */ }
    setupRequestHandlers(mcpServer) { /* ListTools + CallTool boilerplate */ }
    logStartupStatus(health) { /* canonical startup-status logging */ }

    // Extension points (per-server overrides):
    getServerMetadata()              { /* { name, version, capabilities } */ }
    registerTools(toolService)       { /* per-server tool surface */ }
    registerServices(serviceRegistry){ /* per-server service wiring */ }
    registerMiddleware(chain)        { /* per-server middleware additions (append, not replace) */ }
    registerHealthChecks(healthService) { /* per-server healthcheck shape */ }
}
```

Each per-server `Server.mjs` becomes a thin extension overriding only the 4-5 hooks. Bootstrap order, transport setup, default middleware, healthcheck-aggregation, and ListTools/CallTool wiring become base-class concerns.

## The Fix

1. **Create `ai/mcp/server/BaseServer.mjs`** with the extension points + canonical lifecycle methods + JSDoc `@summary`. Class extends `Neo.core.Base` per Neo conventions.
2. **Migrate each of the 5 servers** to extend `BaseServer`:
   - Tier-1 (4 cloud-native): full migration in scope
   - Tier-2 (file-system, local-only): migration in scope for consistency (lower priority; can defer if Tier-1 surfaces unforeseen complexity)
   - Each migration: lift current bootstrap code into the appropriate `registerXxx()` override; remove duplicated transport/middleware/healthcheck wiring; verify healthcheck output unchanged before/after
3. **Document the extension contract** in `learn/agentos/tooling/Introduction.md` (or new `MCPServerBaseClass.md` if scope warrants).
4. **Test surface:** unit specs for the base class (`test/playwright/unit/ai/mcp/server/BaseServer.spec.mjs`), plus per-server smoke tests verifying behavior unchanged after migration.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `Neo.ai.mcp.server.BaseServer` class | This ticket; v13-path.md D2 | Provides extension points + canonical bootstrap order; final `initAsync`/`start`/`stop`/`setupRequestHandlers` | None — inheritance is the contract | `learn/agentos/tooling/MCPServerBaseClass.md` (new) or §extension in Introduction.md | Per-server `Server.mjs` extends BaseServer; unit specs cover extension-point invocation |
| Per-server `Server.mjs` | This ticket | Each becomes a thin extension overriding the hooks | If migration breaks, revert per-server file | Per-server JSDoc `@summary` describes extension shape | Per-server smoke tests verify healthcheck + tool surface unchanged |
| `getServerMetadata()` | Per-server `createMcpServer()` content | Returns `{ name, version, capabilities }`; base class composes McpServer instance | If override missing, base throws at `initAsync` | JSDoc | McpServer instance has correct name/version/capabilities |
| `registerTools(toolService)` | OpenAPI 3.0 spec already canonical | Per-server populates toolService from its `openapi.yaml` | If no openapi.yaml, override returns no-op | Tool-registration guide | OpenAPI spec parses; tool count in healthcheck matches openapi.yaml operations |
| `registerServices(serviceRegistry)` | Per-server services in `*/services/` | Per-server registers domain services + invokes `initAsync()` | If a service fails initAsync, throw at `start()` | JSDoc | Service count in healthcheck matches per-server expectations |
| `registerMiddleware(chain)` | `shared/services/AuthMiddleware.mjs` already canonical | Base class adds auth + request-context middleware in canonical order; per-server appends | Per-server cannot reorder base middleware | JSDoc | Middleware order assertion in unit specs |
| `registerHealthChecks(healthService)` | Per-server `services/HealthService.mjs` (varies) | Per-server contributes healthcheck blocks; base class aggregates | If override returns nothing, base provides empty block | JSDoc | Healthcheck JSON shape unchanged before/after migration |

## Acceptance Criteria

- [ ] **AC1:** `ai/mcp/server/BaseServer.mjs` created, extends `Neo.core.Base` per Neo class-system conventions, has JSDoc `@summary` on every method.
- [ ] **AC2:** Class exposes the extension hooks: `getServerMetadata`, `registerTools`, `registerServices`, `registerMiddleware`, `registerHealthChecks`. Default implementations are no-ops returning the input collection unchanged (or sentinel for `getServerMetadata`).
- [ ] **AC3:** Canonical lifecycle methods `initAsync()`, `start()`, `stop()` orchestrate the registration flow in deterministic order; per-server overrides cannot reorder. Double-start throws.
- [ ] **AC4:** All 4 Tier-1 cloud-native servers (`knowledge-base`, `memory-core`, `github-workflow`, `neural-link`) migrated to extend `BaseServer`.
- [ ] **AC5:** Tier-2 `file-system` server migrated to extend `BaseServer` (consistency with Tier-1; can defer to a follow-up ticket if Tier-1 surfaces unforeseen complexity).
- [ ] **AC6:** Each per-server `Server.mjs` reduced significantly post-migration (rough target: drift below ~80 lines per file is the success signal, not a hard cap; the ~1444 total LOC across 5 Server.mjs files should drop substantially).
- [ ] **AC7:** Per-server healthcheck JSON shape unchanged after migration (snapshot diff captured in PR evidence).
- [ ] **AC8:** Per-server tool count + tool surface unchanged (verified via integration smoke against `openapi.yaml`).
- [ ] **AC9:** Unit specs at `test/playwright/unit/ai/mcp/server/BaseServer.spec.mjs` cover: (a) extension hook invocation order, (b) middleware aggregation, (c) graceful start/stop sequence, (d) error-on-double-start, (e) `getServerMetadata` missing-override throws.
- [ ] **AC10:** Documentation: either new `learn/agentos/tooling/MCPServerBaseClass.md` OR §-section added to `learn/agentos/tooling/Introduction.md` describing the extension contract + when to override which hook.
- [ ] **AC11:** No regression in CI: integration row + unit row both green post-migration.

## Out of Scope

- **M3 Orchestrator daemon** — separate primitive, sibling to bridge per v13-path D3, tracked via [#10956](https://github.com/neomjs/neo/issues/10956). The Orchestrator daemon does NOT extend `BaseServer` (different class hierarchy — daemons vs servers).
- **M6 SDK migration per-server** — this ticket creates the *internal* class-hierarchy substrate; M6 exposes per-server SDKs for *external* consumption, scoped to **Tier-1 only**. Separate tickets per Tier-1 server, sequenced after M2 lands.
- **Config-substrate cleanup** — tracked via [#10822](https://github.com/neomjs/neo/issues/10822). M2 ingests existing `BaseConfig` shape; does NOT change the config layer.
- **Importable-defaults config refactor** — separate downstream ticket. M2 creates the substrate; refactoring `aiConfig from './config.mjs'` → `aiConfigDefaults from './config.template.mjs'` + optional override layer is a separate per-server change. Coordinated with [#10964](https://github.com/neomjs/neo/issues/10964) (clean-checkout deploy bootstrap), which uses Path A (Docker/entrypoint generates `config.mjs`) for immediate clean-deployment unblock.
- **Runtime Factory pattern** — the per-process LOCAL servers still use `isPrimary` for single-writer guard; the REMOTE Factory (`RequestContextService` + `Mcp-Session-Id`) operates at a different layer. M2 is class-hierarchy-only, not concurrency-substrate.
- **Single-writer enforcement audit** — tracked via [#10186](https://github.com/neomjs/neo/issues/10186). M2 doesn't add or remove single-writer behavior.
- **Bridge daemon** — pre-existing separate daemon process; not an MCP server; not affected by this ticket.

## Avoided Traps / Gold Standards Rejected

- **Rejected: skip the base class, use composition only.** Composition would scatter the bootstrap-order discipline across each server (re-creating today's drift). Inheritance + final lifecycle methods + open extension points enforces order at the language level.
- **Rejected: factory function returning a configured server.** A factory hides the extension surface and breaks Neo's class system conventions. Inheritance is more discoverable for downstream maintainers + matches `Neo.core.Base` patterns elsewhere.
- **Rejected: collapse `registerTools` and `registerServices` into one hook.** Tools and services have different lifecycles — tools are static (defined at openapi.yaml load time); services may be dynamic (initAsync). Separate hooks keep the contract explicit.
- **Rejected: bake middleware ordering into `registerMiddleware`.** Per-server overrides should APPEND to base middleware, not replace it. Base class controls canonical-order; per-server adds at end.
- **Rejected: re-implement Express/Hono-style routing in BaseServer.** MCP transport is JSON-RPC over stdio + HTTP; routing is dictated by the MCP spec, not the BaseServer. Routing logic stays in transport-layer services.
- **Rejected: pre-empt M6 SDK boundary inside M2.** M6 exposes a stable external SDK surface; M2 standardizes internal class hierarchy. Coupling them creates premature lock-in.
- **Rejected: exclude file-system server from M2 because it's local-only.** The substrate gain (uniform base class, single source of truth for MCP boilerplate) outweighs the slight cost of including the Tier-2 server. Excluding it would leave one stale-shape server lurking that future server-authors would clone-copy.
- **Rejected: refactor config-import shape inside M2.** Importable-defaults config refactor is a separate per-server change. Coupling it with BaseServer migration would balloon scope. Path A (Docker/entrypoint) covers #10964's immediate need; importable-defaults can come after M2 substrate lands.

## Related

- **Parent epic:** [#10960](https://github.com/neomjs/neo/issues/10960) — v13 release tracking (canonical M-milestone tracking)
- **Grandparent epic:** [#9999](https://github.com/neomjs/neo/issues/9999) — Cloud-Native Knowledge & Multi-Tenant Memory Core (v13 main)
- **Strategic anchor:** [`learn/agentos/v13-path.md`](https://github.com/neomjs/neo/blob/dev/learn/agentos/v13-path.md) §4 D2 + §6 M2 (closed PR [#10958](https://github.com/neomjs/neo/pull/10958))
- **Sibling milestones:**
  - [#10956](https://github.com/neomjs/neo/issues/10956) (M3 Orchestrator daemon, @neo-gemini-3-1-pro)
  - [#10952](https://github.com/neomjs/neo/issues/10952) (M1 deployment integration, closed via #10962)
  - [#10964](https://github.com/neomjs/neo/issues/10964) (M1 deployment hardening, @neo-gpt) — coordinates with M2 on `package.json` runtime-deps split + config-bootstrap path
- **Adjacent (informational, NOT blockers):**
  - [#10822](https://github.com/neomjs/neo/issues/10822) (config-substrate cleanup)
  - [#10186](https://github.com/neomjs/neo/issues/10186) (MCP concurrency audit)

Origin Session ID: `005b6edf-85d8-4980-9e17-486b6b8bed3f`

Retrieval Hint: `query_raw_memories(query="MCP server common base class extension points registerTools registerServices registerMiddleware registerHealthChecks v13 M2 milestone Tier-1 cloud-native Tier-2 local-only")`

## Timeline

- 2026-05-08T15:02:08Z @neo-opus-4-7 added the `enhancement` label
- 2026-05-08T15:02:08Z @neo-opus-4-7 added the `ai` label
- 2026-05-08T15:02:08Z @neo-opus-4-7 added the `architecture` label
- 2026-05-08T15:02:09Z @neo-opus-4-7 added the `release:v13` label
- 2026-05-08T15:02:22Z @neo-opus-4-7 added parent issue #10960
- 2026-05-08T15:06:56Z @neo-opus-4-7 assigned to @neo-opus-4-7
- 2026-05-08T15:27:58Z @neo-opus-4-7 cross-referenced by PR #10966
- 2026-05-08T15:34:33Z @neo-opus-4-7 referenced in commit `1e915e8` - "fix(ai): move BaseServer out of shared/ + match Neo namespace convention (#10965)

Per @tobiu's review correction: (1) Neo's class system enforces
filepath-to-className matching, so BaseServer.mjs at
`ai/mcp/server/shared/BaseServer.mjs` cannot use `Neo.mcp.server.Base`.
(2) `shared/` is for cross-cutting *services* (composable primitives),
not class-hierarchy ancestors. BaseServer is an inheritance ancestor;
its natural location is parent-of-server-dirs.

Two changes:
- Moved file: ai/mcp/server/shared/BaseServer.mjs → ai/mcp/server/BaseServer.mjs
- Moved spec: test/playwright/unit/ai/mcp/server/shared/BaseServer.spec.mjs
  → test/playwright/unit/ai/mcp/server/BaseServer.spec.mjs
- className updated: 'Neo.mcp.server.Base' → 'Neo.ai.mcp.server.BaseServer'
- Class JSDoc tag updated to match
- Spec describe-block titles updated to new className for consistency
- Import paths in spec adjusted (shallower by one level)
- TransportService import path adjusted: './services/' → './shared/services/'

26/26 unit tests still pass (842ms).

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>"
- 2026-05-08T15:44:13Z @tobiu referenced in commit `77f71e7` - "feat(ai): MCP server common base class scaffold + unit specs (#10965) (#10966)

* feat(ai): MCP server common base class scaffold + unit specs (#10965)

Ships the M2 architectural primitive for v13 release path: a shared base
class for the 5 MCP servers (knowledge-base, memory-core, github-workflow,
neural-link, file-system) with template-method extension hooks and a
canonical bootstrap lifecycle.

Per-server migration deferred to PR2-6 so each migration lands in
isolated review surface — knowledge-base first (smallest cloud-native),
memory-core last (most complex, 580-line current Server.mjs has stdio
identity resolution + wake substrate + sibling concurrency log).

BaseServer.mjs (~370 lines):
- Required override hooks: getServerMetadata, getToolService
- Optional override hooks: getDependentServices, getHealthExemptTools,
  getHealthService, wrapDispatch, logStartupStatus, buildRequestContext
  (SSE), onSessionClosed (SSE)
- Lifecycle hooks (no-op defaults): beforeMcpServerInit, beforeHealthcheck,
  afterHealthcheck, afterTransportConnected
- Building blocks (callable from overridden initAsync): loadCustomConfig,
  createMcpServer, setupRequestHandlers, formatToolResult/Health/Tool,
  waitForDependentServices, runHealthcheckAndLogStatus, connectTransport
- Default initAsync orchestrates building blocks in deterministic order

BaseServer.spec.mjs (26 unit tests, 918ms locally):
- Required-override-throw paths
- Optional-hook defaults
- formatToolResult shapes (object-success, error-key, primitives)
- formatHealthError + formatToolError shapes
- setupRequestHandlers wiring through mock McpServer
- ListTools + CallTool dispatch + health-gate apply / skip / error-format
- wrapDispatch override threading
- Default initAsync canonical sequence assertion
- waitForDependentServices ordering + idempotency
- loadCustomConfig no-op-when-unset / loads-when-set

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>

* fix(ai): move BaseServer out of shared/ + match Neo namespace convention (#10965)

Per @tobiu's review correction: (1) Neo's class system enforces
filepath-to-className matching, so BaseServer.mjs at
`ai/mcp/server/shared/BaseServer.mjs` cannot use `Neo.mcp.server.Base`.
(2) `shared/` is for cross-cutting *services* (composable primitives),
not class-hierarchy ancestors. BaseServer is an inheritance ancestor;
its natural location is parent-of-server-dirs.

Two changes:
- Moved file: ai/mcp/server/shared/BaseServer.mjs → ai/mcp/server/BaseServer.mjs
- Moved spec: test/playwright/unit/ai/mcp/server/shared/BaseServer.spec.mjs
  → test/playwright/unit/ai/mcp/server/BaseServer.spec.mjs
- className updated: 'Neo.mcp.server.Base' → 'Neo.ai.mcp.server.BaseServer'
- Class JSDoc tag updated to match
- Spec describe-block titles updated to new className for consistency
- Import paths in spec adjusted (shallower by one level)
- TransportService import path adjusted: './services/' → './shared/services/'

26/26 unit tests still pass (842ms).

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T16:07:59Z @neo-opus-4-7 cross-referenced by #10970
- 2026-05-08T16:13:34Z @neo-opus-4-7 cross-referenced by PR #10973
- 2026-05-08T16:22:16Z @neo-opus-4-7 cross-referenced by PR #10974
- 2026-05-08T16:28:30Z @neo-opus-4-7 cross-referenced by PR #10975
- 2026-05-08T16:33:33Z @neo-opus-4-7 cross-referenced by PR #10976
- 2026-05-08T16:39:55Z @tobiu referenced in commit `b368cbb` - "feat(ai): migrate github-workflow/Server to extend BaseServer (#10965) (#10974)

PR3 of the M2 series — third per-server consumer of the BaseServer
substrate (after PR1 scaffold via #10966 and PR2 knowledge-base via
#10973).

github-workflow/Server.mjs reduced 226 → 110 lines (51% reduction).

Cleaner than PR2 because github-workflow doesn't need any new BaseServer
extension hooks — the existing override surface covers it:
- getServerMetadata: name + version + capabilities
- getToolService: {listTools, callTool}
- getDependentServices: [SyncService] — startup sync against GitHub
- getHealthService: HealthService
- getHealthExemptTools: ['healthcheck'] — equivalent to original
  substring-match since openapi.yaml only declares one canonical
  `/healthcheck` operation
- afterHealthcheck(health): conditionally pre-fetches + caches GitHub
  viewer permission when health is not unhealthy (canonical-sequence
  preserves original semantic ordering)
- logStartupStatus(health): github-workflow-specific gh-CLI-tip on
  unhealthy + degraded/healthy paths

No SSE branching needed — github-workflow is stdio-only; BaseServer's
default connectTransport handles it.

All 39 existing github-workflow unit specs pass post-migration (1.6s).

Refs #10965. Per-server migration sequence per #10965 PR Plan: PR1
scaffold (#10966 merged), PR2 knowledge-base (#10973 in review), PR3
github-workflow (this), PR4 neural-link, PR5 memory-core, PR6 file-system.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T16:40:48Z @neo-opus-4-7 cross-referenced by PR #10977
- 2026-05-08T16:43:36Z @tobiu referenced in commit `6e8607c` - "feat(ai): migrate neural-link/Server to extend BaseServer + boot() seam (#10965) (#10975)

PR4 of the M2 series — fourth per-server consumer of the BaseServer
substrate. Validates the abstraction against a custom-bootstrap-order
consumer (transport-before-services) — predicted in #10965 to surface
design pressure, exercised here.

neural-link/Server.mjs reduced 212 → 149 lines (30% reduction; less
than PR2's 50% because neural-link has the custom boot order +
getCurrentTurnId export + bridgeCwd member that survives migration).

BaseServer refactor — `boot()` as overridable seam:
- `initAsync()` factored to chain super.initAsync() + delegate to boot()
- `boot()` contains the canonical bootstrap sequence (loadCustomConfig
  → beforeMcpServerInit → createMcpServer → waitForDependentServices
  → beforeHealthcheck → runHealthcheckAndLogStatus → afterHealthcheck
  → connectTransport → afterTransportConnected) and is overridable by
  subclasses with non-canonical bootstrap orders
- Backwards-compatible: existing canonical consumers (knowledge-base
  PR2, github-workflow PR3) inherit boot() unchanged

neural-link override:
- Custom boot order: load config → mcpServer → connectTransport (EARLY,
  before ConnectionService) → ConnectionService.ready (non-fatal try/
  catch preserved) → healthcheck + log
- Rationale (preserved from existing code): connect transport early so
  MCP-client handshake succeeds even when the Bridge is down or still
  spawning. Canonical order would race ConnectionService.ready against
  client connections during Bridge spawn.
- bridgeCwd member preserved for ConnectionService.cwd assignment
- _turnId module counter + getCurrentTurnId() export preserved;
  callTool wrapper increments per-dispatch

Tests:
- BaseServer.spec.mjs: 26 → 27 unit tests; 1 new for boot-override
  scenario (verifies subclass override skips canonical sequence
  cleanly; only invoked building blocks fire)
- All 7 existing neural-link unit specs pass post-migration (961ms)

Refs #10965. Per-server migration sequence: PR1 scaffold (#10966
merged), PR2 knowledge-base (#10973 in review), PR3 github-workflow
(#10974 in review), PR4 neural-link (this), PR5 memory-core, PR6
file-system.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T16:45:08Z @tobiu referenced in commit `2405528` - "feat(ai): migrate memory-core/Server to extend BaseServer + beforeToolDispatch hook (#10965) (#10977)

PR5 of the M2 series — final per-server consumer migration. Memory Core
is the most complex (580-line original; identity resolution, sibling
concurrency, wake substrate, custom boot order, identity-spoof guard).

memory-core/Server.mjs reduced 580 → 417 lines (~28% reduction; less
than other migrations because of irreducible per-server complexity:
resolveStdioIdentity, bindAgentIdentity, logSiblingConcurrency, +
custom boot order with stdio-identity-resolution between dependent-
services and healthcheck per #10249).

BaseServer extension (load-bearing for this migration):
- New optional override hook `beforeToolDispatch({toolName, args})`
  fires BEFORE the healthcheck gate. Throws bubble to outer CallTool
  try/catch and route to formatToolError (NOT formatHealthError,
  semantic distinction matters). Default no-op.
- Memory Core uses it for AuthMiddleware.validateNoIdentitySpoof per
  ticket #10145 — must fail-fast on identity-spoofing requests before
  any other processing.

memory-core's boot() override (preserves all original semantics):
1. loadCustomConfig
2. WakeSubscriptionService.init() — pre-mcpServer per #10437
3. createMcpServer (override chains super + CoalescingEngineService.
   addMcpServer for experimental neo-wake-substrate broadcast)
4. InferenceLifecycleService.ready + SessionService.ready
5. Stdio identity resolution + HealthService.setStdioIdentityState +
   wake-subscription auto-bootstrap (fire-and-forget IIFE per #10438
   single-error-boundary discipline) — BEFORE healthcheck so the
   boot snapshot reflects bound identity state per #10249
6. runHealthcheckAndLogStatus + logSiblingConcurrency
7. connectTransport + logIdentityStatus (stdio only)

Hooks used:
- getServerMetadata (with experimental neo-wake-substrate capability)
- getToolService, getHealthService, getHealthExemptTools
- wrapDispatch (RequestContextService.run binding for stdio identity)
- beforeToolDispatch (AuthMiddleware identity-spoof guard)
- buildRequestContext (SSE per-request OIDC binding)
- onSessionClosed (SSE: queueSummarizationJob + CoalescingEngine cleanup)
- logStartupStatus (Memory Core flavored output)
- createMcpServer (override chains super for CoalescingEngineService bind)

Helpers preserved (memory-core-private):
- resolveStdioIdentity, bindAgentIdentity (with #10241 retry pattern)
- logIdentityStatus, logCollectionStats, logSiblingConcurrency

Tests:
- BaseServer.spec.mjs: 27 → 28 unit tests; 1 new for beforeToolDispatch
  (verifies hook fires BEFORE health gate, throws route to
  formatToolError NOT formatHealthError)
- 258 existing memory-core unit specs pass (5 pre-existing Bucket G
  flakes unrelated to migration: GraphService #10941, PermissionService
  #10937, SessionSummarization)

Refs #10965. M2 per-server migration sequence COMPLETE: PR1 scaffold
(#10966 merged), PR2 knowledge-base (#10973 approved), PR3 github-workflow
(#10974 approved), PR4 neural-link + boot() seam (#10975 approved), PR5
memory-core (this), PR6 file-system (#10976 in review).

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T17:04:06Z @neo-opus-4-7 referenced in commit `70767ed` - "feat(ai): migrate knowledge-base/Server to extend BaseServer (#10965)

PR2 of the M2 series — first per-server consumer of the BaseServer
substrate landed via #10966. Validates the abstraction with a real
consumer before tackling the more complex servers (memory-core 580 LOC,
neural-link transport-before-services order).

knowledge-base/Server.mjs reduced 277 → 139 lines (~50% reduction):
- Removed: createMcpServer, initAsync, setupRequestHandlers (all
  inherited from BaseServer; the 120+ lines of ListTools/CallTool/result-
  formatting boilerplate moves out)
- Kept: KB-specific logStartupStatus + logCollectionStats (override of
  BaseServer's default for KB-flavored health output)
- Added: getServerMetadata, getToolService, getDependentServices,
  getHealthService, getHealthExemptTools (override hooks)
- Added: onHealthGateFailure (KB-specific: KBRecorderService.log of
  failed tool dispatch, preserving existing telemetry shape)

BaseServer extended (load-bearing for this migration):
- New optional override hook `onHealthGateFailure({toolName, args, error,
  t0})` fires when the healthcheck gate rejects a tool call, BEFORE the
  formatHealthError envelope is returned. Default no-op. Lets per-server
  augment with telemetry without overriding setupRequestHandlers wholesale.
- t0 = Date.now() captured at handler entry so telemetry can compute
  duration_ms = Date.now() - t0.

Tests:
- BaseServer.spec.mjs: 26 → 28 unit tests; 2 new for onHealthGateFailure
  (hook fires with correct context shape, default-no-op confirmed)
- All 21 existing knowledge-base unit specs pass post-migration

Refs #10965. Per-server migration sequence per #10965 PR Plan: PR2 done
(this), PR3 github-workflow next, PR4 neural-link, PR5 memory-core, PR6
file-system.

Co-Authored-By: Claude Opus 4.7 <neo-opus-4-7@neomjs.com>"
- 2026-05-08T17:08:34Z @tobiu referenced in commit `dfb71b4` - "feat(ai): migrate knowledge-base/Server to extend BaseServer (#10965) (#10973)

PR2 of the M2 series — first per-server consumer of the BaseServer
substrate landed via #10966. Validates the abstraction with a real
consumer before tackling the more complex servers (memory-core 580 LOC,
neural-link transport-before-services order).

knowledge-base/Server.mjs reduced 277 → 139 lines (~50% reduction):
- Removed: createMcpServer, initAsync, setupRequestHandlers (all
  inherited from BaseServer; the 120+ lines of ListTools/CallTool/result-
  formatting boilerplate moves out)
- Kept: KB-specific logStartupStatus + logCollectionStats (override of
  BaseServer's default for KB-flavored health output)
- Added: getServerMetadata, getToolService, getDependentServices,
  getHealthService, getHealthExemptTools (override hooks)
- Added: onHealthGateFailure (KB-specific: KBRecorderService.log of
  failed tool dispatch, preserving existing telemetry shape)

BaseServer extended (load-bearing for this migration):
- New optional override hook `onHealthGateFailure({toolName, args, error,
  t0})` fires when the healthcheck gate rejects a tool call, BEFORE the
  formatHealthError envelope is returned. Default no-op. Lets per-server
  augment with telemetry without overriding setupRequestHandlers wholesale.
- t0 = Date.now() captured at handler entry so telemetry can compute
  duration_ms = Date.now() - t0.

Tests:
- BaseServer.spec.mjs: 26 → 28 unit tests; 2 new for onHealthGateFailure
  (hook fires with correct context shape, default-no-op confirmed)
- All 21 existing knowledge-base unit specs pass post-migration

Refs #10965. Per-server migration sequence per #10965 PR Plan: PR2 done
(this), PR3 github-workflow next, PR4 neural-link, PR5 memory-core, PR6
file-system.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T17:09:12Z @tobiu referenced in commit `f3340b1` - "feat(ai): migrate file-system/Server to extend BaseServer (#10965) (#10976)

PR6 of the M2 series — fifth and final per-server consumer of the
BaseServer substrate. file-system is the Tier-2 local-only server
(gemma4-style local-agent surface, NOT a deployment target).

file-system/Server.mjs reduced 149 → 61 lines (~60% reduction —
biggest of the M2 migrations because file-system has no aiConfig, no
logger module, no health service, no dependent services).

Migration shape:
- extends BaseServer
- Inline `stderrLogger` shim routes all log levels to console.error
  (file-system has no dedicated logger module unlike Tier-1 servers;
  stdio MCP protocol requires log output on stderr to avoid corrupting
  the stdout JSON-RPC channel)
- getServerMetadata: 'neo-file-system' + version + capabilities
- getToolService: {listTools, callTool}
- All other override hooks use BaseServer defaults (no health gate,
  no dependent services, default canonical boot sequence)

Refs #10965. M2 per-server migration sequence complete:
- PR1 #10966 BaseServer scaffold (merged)
- PR2 #10973 knowledge-base (eligible-for-merge)
- PR3 #10974 github-workflow (in review)
- PR4 #10975 neural-link + boot() seam (in review)
- PR5 (memory-core, pending)
- PR6 file-system (this)

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-08T17:11:14Z @neo-gpt cross-referenced by PR #10978
- 2026-05-08T17:13:17Z @neo-opus-4-7 cross-referenced by #10980
- 2026-05-08T17:13:27Z @neo-opus-4-7 added sub-issue #10980
- 2026-05-08T17:18:07Z @neo-gemini-3-1-pro referenced in commit `1901fbf` - "docs(agentos): add MCPServerBaseClass documentation (#10965)"
- 2026-05-08T17:18:16Z @neo-gemini-3-1-pro cross-referenced by PR #10981
- 2026-05-08T17:30:57Z @neo-gemini-3-1-pro referenced in commit `187a8ec` - "docs(agentos): add MCPServerBaseClass documentation (#10965)"
- 2026-05-08T17:39:37Z @tobiu referenced in commit `0d09943` - "docs(agentos): add MCPServerBaseClass documentation (#10965) (#10981)

Co-authored-by: neo-gemini-3-1-pro <neo-gemini-3-1-pro@users.noreply.github.com>"
### @neo-opus-4-7 - 2026-05-08T17:46:49Z

# M2 substrate complete — closing as completed

All 11 ACs satisfied across the M2 PR series merged 2026-05-08. Final landing snapshot:

## AC verification

- **AC1** `ai/mcp/server/BaseServer.mjs` created — ✅ via [PR #10966](https://github.com/neomjs/neo/pull/10966) (PR1 scaffold)
- **AC2** Class exposes the extension hooks — ✅ via PR1 + extended in PR2/PR4/PR5:
  - Required: `getServerMetadata`, `getToolService`
  - Optional: `getDependentServices`, `getHealthExemptTools`, `getHealthService`, `wrapDispatch`, `logStartupStatus`, `buildRequestContext`, `onSessionClosed`
  - Added during migration cycles: `onHealthGateFailure` (PR2 #10973 for KB telemetry), `boot()` overridable seam (PR4 #10975 for non-canonical bootstrap), `beforeToolDispatch` (PR5 #10977 for memory-core's identity-spoof guard)
- **AC3** Canonical lifecycle methods (`initAsync`, `start`, `stop` orchestration) — ✅ default `boot()` seam handles canonical sequence; subclasses override `boot()` for custom orders (neural-link, memory-core)
- **AC4** All 4 Tier-1 cloud-native servers migrated:
  - knowledge-base — ✅ [PR #10973](https://github.com/neomjs/neo/pull/10973) (-50% LOC)
  - github-workflow — ✅ [PR #10974](https://github.com/neomjs/neo/pull/10974) (-51% LOC)
  - neural-link — ✅ [PR #10975](https://github.com/neomjs/neo/pull/10975) (-30% LOC, introduced `boot()` seam)
  - memory-core — ✅ [PR #10977](https://github.com/neomjs/neo/pull/10977) (-28% LOC, introduced `beforeToolDispatch` hook)
- **AC5** Tier-2 file-system server migrated — ✅ [PR #10976](https://github.com/neomjs/neo/pull/10976) (-60% LOC, biggest reduction)
- **AC6** Per-server LOC reduction:
  - knowledge-base 277→139 (50%)
  - memory-core 580→417 (28%)
  - github-workflow 226→110 (51%)
  - neural-link 212→149 (30%)
  - file-system 149→61 (60%)
  - **Total: ~1444 → ~876 LOC across 5 server entry points**
- **AC7** Healthcheck JSON shape unchanged — ✅ verified via 258 memory-core unit specs + 21 knowledge-base + 39 github-workflow + 7 neural-link specs all passing post-migration
- **AC8** Tool count + tool surface unchanged — ✅ verified via per-server unit specs covering tool-service contracts
- **AC9** Unit specs for BaseServer — ✅ 31 specs at `test/playwright/unit/ai/mcp/server/BaseServer.spec.mjs` covering hook invocation order, middleware aggregation (where applicable), graceful start/stop, error-on-double-start, plus the per-hook tests added during migration cycles (`onHealthGateFailure`, `boot()` override, `beforeToolDispatch`)
- **AC10** Documentation — ✅ [PR #10981](https://github.com/neomjs/neo/pull/10981) added `learn/agentos/tooling/MCPServerBaseClass.md` + cross-link from `Introduction.md`; reconciled stale BaseServer.mjs JSDoc to reference `boot()`. (Tracked via dedicated follow-up ticket #10980, closed by #10981.)
- **AC11** No CI regression — ✅ all 4 CI checks GREEN on each merge; integration row honest substrate (no NEO_TEST_SKIP_CI gating)

## Cross-PR review state (all cross-family approved before merge)

- PR1 (mine) — Gemini approved
- PR2 (mine) — Gemini approved
- PR3 (mine) — GPT approved
- PR4 (mine) — Gemini approved
- PR5 (mine) — Gemini approved
- PR6 (mine) — GPT approved
- AC10 doc PR (Gemini's #10981) — both me + GPT approved (after a Cycle 1+2 calibration pass on prose-rigor)

## Downstream impact

- M6 SDK migration (per-server, Tier-1 only) **unblocked** — when M6 lane picks up, each server has a uniform extension surface to expose externally
- Future per-server-author cognitive load **reduced** — single doc + per-server reference table covers the abstraction
- Substrate-pillar audit dimension validated for cross-family review (per `feedback_architectural_pillar_review_floor` discipline retro from this session)

Closing as completed. M2 substrate complete in dev.

— Claude


- 2026-05-08T17:46:55Z @neo-opus-4-7 closed this issue
- 2026-05-08T17:47:55Z @neo-opus-4-7 cross-referenced by #10960
- 2026-05-08T18:12:55Z @neo-opus-4-7 cross-referenced by #10982
- 2026-05-08T19:38:19Z @neo-opus-4-7 cross-referenced by #10986
- 2026-05-08T20:48:45Z @neo-opus-4-7 cross-referenced by #10991
- 2026-05-08T20:59:03Z @neo-opus-4-7 cross-referenced by PR #10992
- 2026-05-08T21:02:09Z @neo-opus-4-7 cross-referenced by #10993
- 2026-05-08T21:06:43Z @neo-opus-4-7 cross-referenced by #10994
- 2026-05-08T21:17:50Z @neo-opus-4-7 cross-referenced by #10996
- 2026-05-08T21:22:46Z @neo-opus-4-7 cross-referenced by PR #10995

