---
id: 17439
title: Resumed repo slices forget previously settled embeddings
state: OPEN
labels:
  - bug
  - ai
  - architecture
  - agent-os
assignees:
  - neo-gpt
createdAt: '2026-08-20T21:53:25Z'
updatedAt: '2026-08-21T09:31:57Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17439'
author: neo-gpt
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
---
# Resumed repo slices forget previously settled embeddings

## Context

Recovered while auditing a superseded implementation of #17132 after Grace's merged PR #17299. The slice budget and repo rotation are shipped; the resumed-slice accounting property is not.

Current source at `origin/dev@b75e5d005961` establishes the mismatch:

- `IngestionService.mjs:330` sets `summary.ingested = embeddableChunks.length`: the accepted corpus presented on this sweep.
- Incremental `VectorService.embed()` returns `embedded: embedResult.embedded` at `:2622`: work newly landed in this call only.
- `TenantRepoSyncService.buildCorpusOutstandingObservation()` at `:553` derives outstanding from accepted minus that current-call embedded count.

A resumed call can therefore forget durable IDs landed by earlier slices. This is a source-proven arithmetic defect; the required production-composition red below owns its behavioral proof.

Live latest-open sweep: checked the latest 20 open issues at 2026-08-20T21:53:24.789Z; no equivalent found. Recent A2A claim sweep: last 30 messages, no overlapping claim. Semantic KB and live GitHub searches returned no equivalent ticket.

## The Problem

For a 100-chunk accepted corpus:

1. Slice 1 lands 20; the current arithmetic reports `100 - 20 = 80` outstanding.
2. Slice 2 re-enters with the same accepted corpus. VectorService internally excludes the 20 durable IDs, but returns only the 20 newly landed in slice 2.
3. The caller again reports `100 - 20 = 80`.

The corpus advances while the operator-visible remainder stands still. Different per-slice throughput can even make the number move independently of durable progress.

This is not permission to redefine `ingested`. #17132's amended AC-5 correctly preserves `ingested = accepted`; collapsing it to landed makes the existing outstanding equation identically zero. The missing terms are authoritative cumulative `settled` and `remaining`.

## The Architectural Reality

- `VectorService.embedChunks()` already knows pre-embedded IDs, newly landed IDs, skipped IDs, and the resume selection.
- The shadow-swap path already folds `alreadyEmbedded` into its result; the incremental path returns only current-call `embedded`.
- `IngestionService` owns the consumed summary contract and currently exposes accepted `ingested`, newly landed `embeddingsGenerated`, and `yielded`.
- `TenantRepoSyncService` persists `corpusOutstanding`; `DeploymentStateBridgeService` projects the repo rows.

The fix belongs in the existing Vector → Ingestion → TenantRepo result chain. No new persistence store is needed.

## The Fix

1. Return exact non-negative `settled` and `remaining` counts from both VectorService strategies, derived from durable selection/landed IDs rather than current-call arithmetic.
2. Propagate those fields through the Ingestion summary without changing `ingested = accepted` or `embeddingsGenerated = newly landed`.
3. Build `corpusOutstanding` from authoritative `remaining`; a missing/malformed producer field is unobserved and must not receive a whole-group fallback credit.
4. Project per-repo settled/remaining on partial-progress rows and document the resumed-slice semantics.
5. Keep lease-vs-slice cause work in #17398/#17414.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `VectorService.embed()` result | durable collection IDs + resume selection + IDs landed this call | returns `settled` and `remaining` for incremental and shadow-swap paths | malformed counts fail loud at the internal boundary; never infer full settlement | JSDoc | strategy-parity arms |
| `IngestionService` summary | Vector result | preserves `ingested=accepted`, `embeddingsGenerated=newly-landed`, adds cumulative `settled/remaining` | absent producer fields remain unobserved; no `group.length` credit | OpenAPI/JSDoc | multi-group + no-op arms |
| `corpusOutstanding` | `summary.remaining` | monotonic across clean resumed slices for one unchanged corpus generation | invalid/missing value projects `null`, never a reassuring zero | cloud-deployment guide | two-sweep production composition |
| deployment repo row | persisted checkpoint + current slice outcome | exposes settled/remaining beside `partial-progress` | missing values remain unobserved | TenantIngestionModel | snapshot projection arm |

Decision Record impact: none — this repairs accounting inside existing result and deployment-observation contracts.

## Acceptance Criteria

- [ ] Against current `dev`, a production-composition witness with four due repos at `concurrencyLimit=2` proves repo A resumes across two slices from durable landed IDs and currently reports a non-decreasing or stale remainder.
- [ ] After repair, A's `remaining` strictly decreases when the second clean slice lands new IDs, and reaches zero only when the accepted corpus is settled.
- [ ] A no-op resume over an already-settled corpus reports `remaining: 0` from durable IDs, not from a current-call zero.
- [ ] Incremental and shadow-swap paths use the same `settled/remaining` meanings.
- [ ] `summary.ingested` remains the accepted term; a mutation redefining it as landed turns a guard red.
- [ ] Missing, fractional, negative, or internally inconsistent counts fail closed and cannot project zero outstanding.
- [ ] The Ingestion/OpenAPI/JSDoc and deployment-guide contracts match the shipped fields.
- [ ] No new durable state is introduced.

## Out of Scope

- Lease-vs-slice cause and exit semantics: #17398 and #17414.
- Durable-fence receipt eligibility: separate follow-up to #17139.
- Changing `sliceBudgetMs`, semaphore admission, or provider batching.
- Redefining `ingested` as landed.

## Avoided Traps

- **Subtracting current work from lifetime work.** A per-call delta is not cumulative settlement.
- **Crediting the whole group on absent data.** Under-counting re-offers work; over-counting suppresses it forever.
- **Porting a stale source blob.** The recovered implementation predates later Vector, tenant-sync, and checkpoint changes; only the property and focused arms are reusable.

## Related

- #17132 / PR #17299 — shipped slice fairness; this is a post-merge accounting follow-up.
- #17410 — closed live observation that outstanding did not move; its checkpoint-deadlock premise was withdrawn, but the symptom matches this arithmetic.
- #17398 / #17414 — distinct cause-bearing lease/slice work.

Origin Session ID: 5227e910-6f64-46da-a0d1-d6fb1cdfbb9f

Retrieval Hint: `query_raw_memories("resumed tenant repo slice settled remaining outstanding accounting")`; source anchors `IngestionService.mjs:330`, `VectorService.mjs:2622`, `TenantRepoSyncService.buildCorpusOutstandingObservation`.


## Timeline

- 2026-08-20T21:53:26Z @neo-gpt added the `bug` label
- 2026-08-20T21:53:27Z @neo-gpt added the `ai` label
- 2026-08-20T21:53:27Z @neo-gpt added the `architecture` label
- 2026-08-20T21:53:27Z @neo-gpt added the `agent-os` label
- 2026-08-20T21:53:28Z @neo-gpt cross-referenced by #17440
- 2026-08-20T21:59:13Z @neo-opus-vega cross-referenced by #17414
- 2026-08-20T22:39:27Z @neo-opus-vega cross-referenced by PR #17441
- 2026-08-20T23:34:37Z @neo-opus-vega cross-referenced by #17443
- 2026-08-21T09:31:57Z @neo-gpt assigned to @neo-gpt

