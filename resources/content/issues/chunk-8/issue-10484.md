---
id: 10484
title: OpenAiCompatible stream fallback for non-SSE JSON chat completions
state: CLOSED
labels:
  - bug
  - ai
assignees:
  - neo-gpt
createdAt: '2026-04-29T10:05:52Z'
updatedAt: '2026-06-03T09:58:35Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10484'
author: neo-gemini-3-1-pro
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-03T09:58:35Z'
---
# OpenAiCompatible stream fallback for non-SSE JSON chat completions

## Context

This ticket was originally bundled as LM Studio Tri-Vector pipeline crash + SQLite chunking refactor. Current V-B-A shows that bundle is stale:

- The SQLite chunking / `SQLITE_IN_CLAUSE_BATCH_SIZE` work was already delivered by #10463 / PR #10464 and #10484-linked PR #10486.
- Empty-provider-output detection and downstream graph-extractor friction routing were later improved by #12091 / #12113-era work.
- The remaining provider-contract gap is still real: current `ai/provider/OpenAiCompatible.mjs` requests `stream: true` and only yields SSE `choices[0].delta.content`. If an OpenAI-compatible server replies with a non-SSE JSON chat-completions payload, e.g. `choices[0].message.content`, the stream parser yields no content.

## Problem

OpenAI-compatible providers can return either streaming SSE chunks or a plain JSON chat-completions object. Neo currently handles the SSE delta path only. For local providers such as LM Studio / llama.cpp that may respond with a monolithic JSON object under `response_format: {type: "json_object"}` or degraded streaming support, this can collapse into an empty generated payload.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
|---|---|---|---|---|---|
| `OpenAiCompatible.stream()` SSE path | Current provider contract | Continue yielding `choices[0].delta.content` from `data:` SSE frames. | Malformed individual SSE frames remain ignored as today. | JSDoc if behavior clarified | Existing behavior preserved by focused provider test |
| `OpenAiCompatible.stream()` non-SSE JSON path | OpenAI-compatible chat-completions response shape | If the response body is a plain JSON object, yield `choices[0].message.content` when present. | Empty / missing content yields no chunks; invalid JSON should not corrupt valid SSE behavior. | Provider JSDoc | New unit test with JSON response fixture |
| `OpenAiCompatible.generate()` aggregate result | Existing `generate()` delegates to `stream()` | Aggregates the non-SSE yielded content exactly like SSE content and returns `{content, raw.message.content}`. | Provider errors still throw through. | No extra docs unless JSDoc changes | Focused generate/stream coverage |
| SQLite chunking / bridge-daemon GraphLog insertions | #10463 / PR #10464 / PR #10486 | Already resolved; not part of this residual ticket. | None. Do not re-open bridge-daemon chunking under this ticket. | N/A | Current source/tests + closed PRs |

## Acceptance Criteria

- [ ] `OpenAiCompatible.stream()` preserves existing SSE `delta.content` behavior.
- [ ] `OpenAiCompatible.stream()` yields `choices[0].message.content` for non-SSE JSON chat-completions responses.
- [ ] `OpenAiCompatible.generate()` aggregates that non-SSE content through its existing stream delegation.
- [ ] Focused unit coverage exercises both SSE and non-SSE JSON response shapes.
- [ ] No SQLite chunking or bridge-daemon scope is included; that stale slice remains closed by prior PRs.

## Related

- #10463 / PR #10464 — SQLite chunking implementation.
- PR #10486 — externalized SQLite chunking limit from the original bundled ticket.
- Provider-adjacent later work: #12059, #12090, #12114, #12264.

Origin Session ID: original #10484 context; body narrowed by @neo-gpt on 2026-06-03 after live source and PR verification.

## Timeline

- 2026-04-29T10:26:13Z @neo-gemini-3-1-pro cross-referenced by PR #10486
- 2026-04-29T12:03:07Z @neo-gemini-3-1-pro referenced in commit `8f8188e` - "test(ai): replace sqlite chunking load test with deterministic helper assertion

Related to #10484. Replaces the slow, non-deterministic daemon subprocess
load test with a deterministic mock-based unit test to strictly verify the
chunking behavior of getNodesData and getEdgesData against
SQLITE_IN_CLAUSE_BATCH_SIZE."
- 2026-04-29T12:16:45Z @neo-gemini-3-1-pro referenced in commit `9dc4ee9` - "test(ai): remove trailing whitespace (#10484)"
- 2026-04-29T12:22:25Z @neo-gemini-3-1-pro referenced in commit `820076a` - "test(ai): remove trailing whitespace (#10484)"
- 2026-04-29T12:53:27Z @tobiu referenced in commit `7e1bbc0` - "fix(ai): externalize SQLite chunking limit to prevent parameter overflow (#10484) (#10486)

* fix(ai): externalize SQLite chunk batch size to prevent parameter limit crash (#10484)

* test(ai): replace sqlite chunking load test with deterministic helper assertion

Related to #10484. Replaces the slow, non-deterministic daemon subprocess
load test with a deterministic mock-based unit test to strictly verify the
chunking behavior of getNodesData and getEdgesData against
SQLITE_IN_CLAUSE_BATCH_SIZE.

* test(ai): remove trailing whitespace (#10484)

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-01T16:03:15Z @neo-gpt cross-referenced by #10587
- 2026-05-03T14:53:43Z @neo-gpt cross-referenced by #10647
- 2026-05-03T15:03:08Z @neo-gpt cross-referenced by #10651
- 2026-05-03T21:19:12Z @neo-gpt cross-referenced by #10666
### @neo-gpt - 2026-06-03T01:03:39Z

**Input from GPT-5 (Codex Desktop):**

> ● [ARCH_ALIGNMENT] Ticket-triage verdict: retrospective challenge fails; routing to `needs-re-triage` instead of applying normal labels.
> 
> V-B-A checks run:
> - Live issue body fetched from GitHub; there are no clarifying comments and no labels.
> - Current `ai/daemons/bridge/queries.mjs` already chunks `getNodesData()` / `getEdgesData()` by `SQLITE_IN_CLAUSE_BATCH_SIZE`, with coverage in `test/playwright/unit/ai/daemons/bridge/daemon.spec.mjs`. That maps to the SQLite half of this ticket and was closed separately by #10465/#10466.
> - Current `ai/services/graph/SemanticGraphExtractor.mjs` and `ai/services/graph/TopologyInferenceEngine.mjs` already detect empty provider output and emit existing `context-overflow` friction, shipped by #12091/#12113.
> - Current `ai/provider/OpenAiCompatible.mjs` still parses only SSE `delta.content`; a valid non-SSE OpenAI-compatible JSON response with `choices[0].message.content` would not be captured by the provider stream parser. That residual is a provider-contract issue, but it is not cleanly represented by this bundled ticket.
> 
> Retrospective six-stage result:
> - Premise: partially real, but stale in two of three ACs.
> - Prescription: fails as written because it bundles a provider fallback with already-resolved SQLite bridge-daemon work.
> - Substrate: mixed provider, bridge-daemon, and REM graph-extractor surfaces in one ticket.
> - Consumer: agent/Dream Pipeline, but the needed provider contract is not separated from downstream extractor telemetry.
> - Service-boundary: fails; bridge-daemon SQL chunking and OpenAI-compatible provider response parsing should not travel as one implementation lane.
> - Decision-record impact: none identified.
> 
> Recommended next shape: either close this as superseded/invalid after maintainer confirmation, or rewrite/split into a narrow provider-ticket for `OpenAiCompatible.stream()` non-SSE JSON handling with a Contract Ledger that names the exact response shapes (`data:` SSE delta vs plain JSON `choices[0].message.content`) and the expected fallback/error behavior.
> 
> Triaged per `ticket-triage` skill. Applying only `needs-re-triage`; no primary/secondary labels applied because the premise failed the retrospective challenge.

- 2026-06-03T01:03:46Z @neo-gpt added the `needs-re-triage` label
- 2026-06-03T07:48:41Z @neo-gpt changed title from **Fix LM Studio Monolithic JSON Payload Crash & Refactor SQLite Batch Size** to **OpenAiCompatible stream fallback for non-SSE JSON chat completions**
- 2026-06-03T07:48:47Z @neo-gpt removed the `needs-re-triage` label
- 2026-06-03T07:48:53Z @neo-gpt added the `ai` label
- 2026-06-03T07:48:53Z @neo-gpt added the `bug` label
- 2026-06-03T08:19:23Z @neo-gpt assigned to @neo-gpt
- 2026-06-03T08:26:04Z @neo-gpt cross-referenced by PR #12428
- 2026-06-03T09:58:35Z @tobiu referenced in commit `6ddfc4f` - "fix(ai): handle non-sse OpenAI-compatible JSON (#10484) (#12428)

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-06-03T09:58:36Z @tobiu closed this issue

