---
id: 12116
title: 'ConsumerFrictionHelper: loud-fail on undefined contextLimitTokens (close NaN-silent-skip hole exposed by SessionService:612 fallback removal)'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - model-experience
assignees:
  - neo-opus-4-7
createdAt: '2026-05-27T22:33:56Z'
updatedAt: '2026-05-28T06:39:29Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12116'
author: neo-opus-4-7
commentsCount: 0
parentIssue: 12101
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-05-28T06:39:29Z'
---
# ConsumerFrictionHelper: loud-fail on undefined contextLimitTokens (close NaN-silent-skip hole exposed by SessionService:612 fallback removal)

## Premise

PR #12115 retired the `SessionService.mjs:612` hidden default fallback (`aiConfig.openAiCompatible?.contextLimitTokens || 32768`) per the operator's strict `feedback_no_hidden_default_fallbacks` contract. The deletion exposed a latent contract-gap downstream: `ConsumerFrictionHelper.invokeWithGuardrail` does NOT validate `contextLimitTokens` at function entry.

When `contextLimitTokens` is `undefined` (the very scenario the operator contract is designed to surface):

```js
// ai/services/memory-core/helpers/ConsumerFrictionHelper.mjs:336-341
const effectiveSafeTokens = Number.isFinite(safeProcessingLimitTokens)
    ? safeProcessingLimitTokens
    : Math.floor(contextLimitTokens * DEFAULT_SAFE_FRACTION);  // undefined * 0.75 = NaN
                                                                // Math.floor(NaN) = NaN
if (inputTokensEstimate > effectiveSafeTokens) {  // (number > NaN) === false
    // NEVER FIRES — pre-check silently skipped
}
```

Guardrail pre-check silently skipped, invocation proceeds unguarded, subsequent friction telemetry stores `undefined` for `contextLimitTokens` (silent corruption of the friction record downstream). This is a **hidden default fallback in disguise** — same anti-pattern class PR #12115 removed at SessionService:612, just one substrate-layer deeper in the helper.

## Substrate shape

`emitConsumerFriction` (line 197) validates `symptom`, `emissionPoint`, `suggestionKind`, `serviceDomain` via `TypeError` throw. The same loud-fail discipline should extend to `contextLimitTokens` since the JSDoc explicitly marks it `// durable contract` (line 54). `safeProcessingLimitTokens` remains optional with documented default-derivation; only `contextLimitTokens` becomes required-and-validated.

## Prescription

1. **`invokeWithGuardrail`**: validate `contextLimitTokens` at entry with `TypeError('invokeWithGuardrail: contextLimitTokens must be a finite number')` when `typeof contextLimitTokens !== 'number' || !Number.isFinite(contextLimitTokens)`.
2. **`emitConsumerFriction`**: same validation on `input.contextLimitTokens` at entry.
3. **Unit tests** asserting the TypeError for both helpers under missing-config (stub `aiConfig` with no `localModels.chat.contextLimitTokens`).

## Contract Ledger

| Surface | Before | After | Consumer |
|---|---|---|---|
| `invokeWithGuardrail` `contextLimitTokens` validation | Implicit — NaN propagation silently skips pre-check; downstream friction record gets undefined | Explicit `TypeError` loud-fail at function entry | Direct callers + cascading downstream consumers of friction records |
| `emitConsumerFriction` `contextLimitTokens` validation | None (JSDoc says required, no runtime check) | Explicit `TypeError` loud-fail at function entry | All friction-emit call sites |

## Acceptance Criteria

- [ ] AC1: `invokeWithGuardrail` throws `TypeError` when `contextLimitTokens` is `undefined`, `null`, `NaN`, `Infinity`, or non-number
- [ ] AC2: `emitConsumerFriction` throws `TypeError` on same conditions
- [ ] AC3: Error messages name the specific contract so call-site debugging is unambiguous
- [ ] AC4: Unit tests cover all 5 invalid-input cases for both helpers
- [ ] AC5: No regression on the 4 consumer sites migrated by PR #12115 — all pass `aiConfig.localModels.chat.contextLimitTokens` which IS a finite number when overlays are synced

## Related

- Parent: Epic #12101 (greenfield aiConfig substrate redesign)
- Discovered by: PR #12115 retiring `SessionService.mjs:612` hidden default fallback (`|| 32768`)
- Sibling: #12114 (role-keyed context-limits — this ticket hardens the helper-layer contract that #12114's consumer migration relies on)

## Discipline anchor

The `|| 32768` fallback at SessionService:612 (now deleted) was the visible-layer contract violation. The NaN-silent-skip at ConsumerFrictionHelper.mjs:336-341 is the invisible-layer counterpart — same anti-pattern class, just one substrate-layer deeper. PR #12115 closes the visible-layer instance; this ticket closes the invisible-layer instance. Both together satisfy the operator's contract that "config IS SSOT; ban `||`/`??` substitution".

## Timeline

- 2026-05-27T22:33:57Z @neo-opus-4-7 added the `enhancement` label
- 2026-05-27T22:33:58Z @neo-opus-4-7 added the `ai` label
- 2026-05-27T22:33:58Z @neo-opus-4-7 added the `architecture` label
- 2026-05-27T22:33:58Z @neo-opus-4-7 added the `model-experience` label
- 2026-05-27T22:34:03Z @neo-opus-4-7 added parent issue #12101
- 2026-05-27T22:51:08Z @neo-opus-4-7 cross-referenced by #12117
- 2026-05-27T23:07:12Z @neo-opus-4-7 cross-referenced by PR #12119
- 2026-05-28T00:31:56Z @neo-opus-4-7 cross-referenced by #11791
- 2026-05-28T00:32:41Z @neo-opus-4-7 assigned to @neo-opus-4-7
- 2026-05-28T00:37:54Z @neo-opus-4-7 referenced in commit `e2bf604` - "fix(memory-core): loud-fail invokeWithGuardrail on undefined contextLimitTokens (#12116)

PR #12061 / #12115 retired the SessionService.mjs:612 hidden default
fallback (aiConfig.openAiCompatible?.contextLimitTokens || 32768) per
the operator's strict no-hidden-default-fallbacks contract. The
deletion exposed a latent contract-gap downstream: when
contextLimitTokens reaches invokeWithGuardrail as undefined, the
Math.floor(undefined * 0.75) = NaN computation made
effectiveSafeTokens = NaN, then `inputTokensEstimate > NaN === false`
silently skipped the Angle-2 pre-check. The invocation proceeded
unguarded and emitConsumerFriction stored undefined for
contextLimitTokens in the friction record - hidden default fallback
in disguise, one substrate-layer deeper than the SessionService:612
fix.

Mirror the existing TypeError throw discipline in emitConsumerFriction
(lines 198-212) per the operator-codified loud-fail contract: validate
contextLimitTokens at function entry, throw TypeError with the bad
value in the message so operators can branch on the substrate-level
hole rather than chase silent invocation drift downstream.

safeProcessingLimitTokens remains optional with the documented
75%-of-contextLimitTokens default-derivation; only contextLimitTokens
becomes required-and-validated since the JSDoc explicitly marks it
"// durable contract".

Tests: new spec exercises undefined, null, 0, negative, NaN, +Infinity,
-Infinity, and stringly-numeric "10000" - all throw at entry with the
diagnostic message including the bad value; invocationFn never runs;
aggregator stays empty. 19/19 ConsumerFrictionHelper.spec.mjs tests
pass against the new fix.

Both production callers (SemanticGraphExtractor.mjs:127 and
SessionService.mjs:612) pull contextLimitTokens from
aiConfig.localModels.chat.contextLimitTokens (template default 262144,
env-bindable via NEO_LOCAL_MODELS_CHAT_CONTEXT_LIMIT_TOKENS per
config.template.mjs:185 + :630), so the loud-fail only fires when an
operator overlay (ai/config.mjs) explicitly omits or nulls the key -
which IS the operator-contract scenario this fix is designed to
surface, not regress."
- 2026-05-28T00:40:49Z @neo-opus-4-7 cross-referenced by PR #12121
- 2026-05-28T01:01:20Z @neo-opus-4-7 referenced in commit `1b979bf` - "fix(memory-core): extend loud-fail to emitConsumerFriction direct surface (#12116)

Cycle-1 review caught the missing second contract surface: the cycle-1
commit validated only the invokeWithGuardrail wrapper entry, leaving
the direct emitConsumerFriction call path silently corrupting friction
records when callers passed undefined contextLimitTokens. Cross-family
reviewer ran a direct falsifier on the checked-out branch and proved
the gap empirically.

#12116 AC2 explicitly requires the same loud-fail discipline on
emitConsumerFriction itself. #12116 AC4 requires unit coverage on
BOTH helpers; cycle-1 covered only the wrapper.

Mirror the same TypeError throw at emitConsumerFriction entry, placed
after the existing enum-validation block and before the aggregator
mutation. Aggregator stays untouched on throw (verified by the new
test asserting getAggregatedFrictions().length === 0 after 8 bad-value
throws).

New spec at line 367 covers the 8 bad-value matrix (undefined / null /
0 / -1 / NaN / +Infinity / -Infinity / stringly-numeric '10000') on
the direct emitConsumerFriction path. The pre-existing
invokeWithGuardrail loud-fail spec is renamed to mark its AC1 anchor
explicitly. 20/20 tests pass."
- 2026-05-28T06:39:29Z @tobiu referenced in commit `5d99900` - "fix(memory-core): loud-fail invokeWithGuardrail on undefined contextLimitTokens (#12116) (#12121)

* fix(memory-core): loud-fail invokeWithGuardrail on undefined contextLimitTokens (#12116)

PR #12061 / #12115 retired the SessionService.mjs:612 hidden default
fallback (aiConfig.openAiCompatible?.contextLimitTokens || 32768) per
the operator's strict no-hidden-default-fallbacks contract. The
deletion exposed a latent contract-gap downstream: when
contextLimitTokens reaches invokeWithGuardrail as undefined, the
Math.floor(undefined * 0.75) = NaN computation made
effectiveSafeTokens = NaN, then `inputTokensEstimate > NaN === false`
silently skipped the Angle-2 pre-check. The invocation proceeded
unguarded and emitConsumerFriction stored undefined for
contextLimitTokens in the friction record - hidden default fallback
in disguise, one substrate-layer deeper than the SessionService:612
fix.

Mirror the existing TypeError throw discipline in emitConsumerFriction
(lines 198-212) per the operator-codified loud-fail contract: validate
contextLimitTokens at function entry, throw TypeError with the bad
value in the message so operators can branch on the substrate-level
hole rather than chase silent invocation drift downstream.

safeProcessingLimitTokens remains optional with the documented
75%-of-contextLimitTokens default-derivation; only contextLimitTokens
becomes required-and-validated since the JSDoc explicitly marks it
"// durable contract".

Tests: new spec exercises undefined, null, 0, negative, NaN, +Infinity,
-Infinity, and stringly-numeric "10000" - all throw at entry with the
diagnostic message including the bad value; invocationFn never runs;
aggregator stays empty. 19/19 ConsumerFrictionHelper.spec.mjs tests
pass against the new fix.

Both production callers (SemanticGraphExtractor.mjs:127 and
SessionService.mjs:612) pull contextLimitTokens from
aiConfig.localModels.chat.contextLimitTokens (template default 262144,
env-bindable via NEO_LOCAL_MODELS_CHAT_CONTEXT_LIMIT_TOKENS per
config.template.mjs:185 + :630), so the loud-fail only fires when an
operator overlay (ai/config.mjs) explicitly omits or nulls the key -
which IS the operator-contract scenario this fix is designed to
surface, not regress.

* fix(memory-core): extend loud-fail to emitConsumerFriction direct surface (#12116)

Cycle-1 review caught the missing second contract surface: the cycle-1
commit validated only the invokeWithGuardrail wrapper entry, leaving
the direct emitConsumerFriction call path silently corrupting friction
records when callers passed undefined contextLimitTokens. Cross-family
reviewer ran a direct falsifier on the checked-out branch and proved
the gap empirically.

#12116 AC2 explicitly requires the same loud-fail discipline on
emitConsumerFriction itself. #12116 AC4 requires unit coverage on
BOTH helpers; cycle-1 covered only the wrapper.

Mirror the same TypeError throw at emitConsumerFriction entry, placed
after the existing enum-validation block and before the aggregator
mutation. Aggregator stays untouched on throw (verified by the new
test asserting getAggregatedFrictions().length === 0 after 8 bad-value
throws).

New spec at line 367 covers the 8 bad-value matrix (undefined / null /
0 / -1 / NaN / +Infinity / -Infinity / stringly-numeric '10000') on
the direct emitConsumerFriction path. The pre-existing
invokeWithGuardrail loud-fail spec is renamed to mark its AC1 anchor
explicitly. 20/20 tests pass.

---------

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-05-28T06:39:29Z @tobiu closed this issue
- 2026-05-29T01:44:54Z @neo-opus-4-7 cross-referenced by #12101

