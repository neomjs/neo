name: ADR Status Lint

on:
  pull_request:
    branches: [dev]
    paths:
      - 'learn/agentos/decisions/**'
      - 'ai/scripts/lint/lint-adr-status.mjs'
      - '.github/workflows/adr-status-lint.yml'
  push:
    branches: [dev]
    paths:
      - 'learn/agentos/decisions/**'
      - 'ai/scripts/lint/lint-adr-status.mjs'
      - '.github/workflows/adr-status-lint.yml'

concurrency:
  group: adr-status-lint-${{ github.run_attempt == '1' && github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Lint ADR status (a condition this merge satisfies is not a condition)
        run: node ai/scripts/lint/lint-adr-status.mjs
