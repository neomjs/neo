---
id: 12947
title: Verify cross-body row-window sync (availableRows vs mounted rows)
state: CLOSED
labels:
  - bug
  - ai
  - grid
assignees:
  - neo-fable-clio
createdAt: '2026-06-12T02:54:41Z'
updatedAt: '2026-06-12T03:41:22Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12947'
author: neo-fable-clio
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-12T03:41:22Z'
---
# Verify cross-body row-window sync (availableRows vs mounted rows)

## Context

Split leaf from #12930 AC4 ("Secondary row-window desync either resolved by the same fix (with evidence) or split into its own leaf with the captured data"). The id-migration seam fix landed via PR `#12938` (`c3d372054`) and #12930 closes on its regression net; this leaf carries the captured secondary so it cannot silently vanish with the parent's close.

`Release classification:` boardless — verification residual of the `#12930` family; escalates to the board only if the live recount shows the invariant still broken post-`c3d372054`.

## The Problem (captured data, 2026-06-11 ~23:0xZ, operator devindex session `948c9bba`)

All three region bodies reported `availableRows: 16` / `startIndex: 0` while mounting **18 / 22 / 22** rows (center / locked-start / locked-end). Each body was internally consistent (its own vdom == DOM), but the cross-body invariant — same row window across regions for one logical grid — was broken.

**Staleness caveat (why this is a VERIFY ticket, not a FIX ticket):** the capture predates the entire `#12938` fix family. The duplication corruption active at capture time held BOTH cell/row generations in the DOM, which plausibly inflates mounted counts — the desync may be an artifact of the now-fixed defect rather than an independent seam. No post-fix recount exists.

## The Architectural Reality

- `src/grid/Body.mjs` derives `availableRows` from container height / rowHeight and the mounted row set from `startIndex` + buffer math (`createViewData` path).
- Since the multi-body migration (`#9486` / `36b45d700`), `src/grid/View.mjs#syncBodies` owns the cross-body window orchestration (startIndex/visibleRows pushed into each body under `skipCreateViewData`) — the invariant's single writer.
- Possible legitimate variance to rule IN/OUT during verification: per-region `bufferRowRange` application could make mounted counts differ from `availableRows` by design; 18 vs 22/22 across regions of the SAME grid is the suspicious part, not mounted > available per se.

## The Fix (verification protocol)

1. On a post-`c3d372054` build (devindex or the `#12936` `examples/grid/lockedColumns` fixture from PR `#12945`), connect Neural Link and read all three bodies: `availableRows`, `startIndex`, `bufferRowRange`, `items.length`, DOM row count.
2. Sample at rest → after vertical scroll → after a column drag (the original corruption trigger).
3. **Clean** (counts coherent under one documented rule): close this ticket with the recount evidence.
4. **Dirty**: re-scope this ticket into a fix leaf with the fresh capture; board it (the grid-corruption family was flagged release-blocking in the `#12696` triage override).

## Acceptance Criteria

- [ ] Post-fix recount executed on all three bodies across the three sample points, numbers recorded on this ticket.
- [ ] Either closed-clean with evidence, or re-scoped to a fix leaf with fresh capture + board attachment.

## Out of Scope

- The id-migration seam (fixed, `c3d372054`; regression net on #12930).
- The view-level stale-baseline update race (#12939) and the in-flight wedge (#12946) — sibling lanes.

## Related

- Parent context: #12930 (AC4 origin) · Fix family: PR `#12938` · Siblings: #12939, #12946 · Fixture: #12936 / PR `#12945`

Live latest-open sweep: checked latest 20 open issues at 2026-06-12T02:55Z; no equivalent (`#12946` = vdom in-flight wedge, different layer). A2A in-flight sweep: clean — no competing claim on the row-window scope.

Origin Session ID: c29b5ccc-d711-4cde-8401-32d1e4bbc4f1

Retrieval Hint: "cross-body row-window desync availableRows mounted rows recount post-fix"

## Timeline

- 2026-06-12T02:54:42Z @neo-fable-clio added the `bug` label
- 2026-06-12T02:54:42Z @neo-fable-clio added the `ai` label
- 2026-06-12T02:54:42Z @neo-fable-clio added the `grid` label
- 2026-06-12T02:56:57Z @neo-fable-clio cross-referenced by PR #12948
- 2026-06-12T02:57:42Z @neo-fable cross-referenced by #12946
- 2026-06-12T03:01:53Z @neo-fable cross-referenced by PR #12945
- 2026-06-12T03:28:32Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-06-12T03:40:44Z @neo-fable-clio cross-referenced by #12954
### @neo-fable-clio - 2026-06-12T03:41:13Z

**Input from Claude Fable 5 (Claude Code):**

> ❋ ❋ ❋ ## Recount executed — invariant CLEAN post-fix; closing per AC branch 3. New unrelated finding split to #12954
> 
> **Surfaces & taint protocol:** merged dev tip `d6d6a1c75`, own checkout served on :8091, fresh Chromium sessions via the e2e NL fixture (the operator's wedged tab untouched per @neo-fable's warning; the Claude-preview browser was excluded after it reproduced the `#12946` init-wedge — evidence posted there, `IC_kwDODSospM8AAAABF19mfw`).
> 
> ### Sample set 1 — `examples/grid/lockedColumns` (3 region bodies, 8-record store), rest / wheel / column-drag
> 
> All three bodies agree at every sample point: `availableRows 32`, `startIndex 0`, `bufferRowRange 3`. `verify_component_consistency` = `consistent: true, mismatches: []` on the view AND all three bodies, **including post-drag** (the original corruption trigger). vdom == items == DOM at every layer.
> 
> - Caveat 1: the 8-record store cannot scroll vertically in a 32-row viewport, so the scrolled-window condition rode sample set 2.
> - Caveat 2 → **#12954**: locked-end `body-3` mounts 0 of 8 rows (stable, internally consistent, *consistently empty*) — a distinct row-creation defect, NOT a window-sync violation: its window state (32/0/3) agrees perfectly with its siblings. Split out with full numbers.
> 
> ### Sample set 2 — devindex on the same tree (1000+ rows; single body since `#12935` removed its locks), rest / wheel-400 / wheel-2400
> 
> | sample | scrollTop | startIndex | availableRows | buffer | items | DOM rows |
> |---|---:|---:|---:|---:|---:|---:|
> | rest | 0 | 0 | 18 | 1 | 20 | 20 |
> | scrolled | 400 | 8 | 18 | 1 | 20 | 20 |
> | deep-scrolled | 2400 | 48 | 18 | 1 | 20 | 20 |
> 
> **`mounted == availableRows + 2×bufferRowRange` exactly (20 = 18 + 2), at every scroll depth, with startIndex tracking scrollTop precisely.** The capture-era anomaly — mounted EXCEEDING the formula (18/22/22 vs availableRows 16) and differing per body — does not reproduce anywhere post-fix.
> 
> ### Verdict
> 
> The pre-`#12938` desync was an **artifact of the fixed duplication family** (both cell/row generations living in the DOM inflated the counts), exactly as this ticket's staleness caveat predicted. Closing clean per AC: numbers recorded, invariant verified on both available surfaces.
> 
> **Honest residual:** scroll × multi-body *combined* is unsampleable on current surfaces (fixture can't scroll; devindex has no regions). #12954's AC 3 carries that re-check for when a locked-end-configured overflow surface exists.
> 
> Per the `#12946` sequencing agreement: these numbers double as the pre-fix baseline for @neo-fable's wedge/race lane — cross-referenced there.
> 
> — **Clio**, @neo-fable-clio 📜 (Session c29b5ccc-d711-4cde-8401-32d1e4bbc4f1)

- 2026-06-12T03:41:22Z @neo-fable-clio closed this issue
- 2026-06-12T04:15:51Z @neo-fable cross-referenced by #12958

