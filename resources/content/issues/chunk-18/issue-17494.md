---
id: 17494
title: The docs namespace walker consults inherited properties
state: CLOSED
labels:
  - bug
  - ai
  - build
  - security
assignees:
  - neo-opus-grace
createdAt: '2026-08-21T18:46:22Z'
updatedAt: '2026-08-21T23:17:48Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17494'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-21T23:17:48Z'
---
# The docs namespace walker consults inherited properties

## Context

CodeQL alert **113**, `js/prototype-pollution-utility`, at `buildScripts/docs/generateDocsJson.mjs:91`.

**This is not one of the two alerts @tobiu described as deliberate.** That exemption covers `src/Neo.mjs:563,565`, where the framework enriches prototypes on purpose. This is a third instance of the same rule in a **different file** — a docs generator with no intent to touch prototypes anywhere — and it surfaced only because I re-read the live alert queue instead of working from a carried list.

Two items I had been carrying turned out to be stale in the same pass, worth recording so nobody re-opens them: the `js/cors-permissive-configuration` alert from #15818's list is **no longer open**, and Ada's parked `ai/daemons/wake/queries.mjs:84` fixed-temp-path defect is **already remediated** — `writeFileAtomicSync` now uses `${absolute}.${pid}.${randomUUID()}.tmp` with `flag: 'wx'` and a `finally { rmSync }`, verified at source rather than from its comment.

Live latest-open sweep: checked latest 8 open issues at 2026-08-21T18:5x UTC; `gh search issues` for `setNamespace OR generateDocsJson OR prototype-pollution` returned zero. No equivalent exists.

## The Problem

`buildScripts/docs/generateDocsJson.mjs:80-93`:

```js
function setNamespace(tree, names, value) {
    names = Array.isArray(names) ? names : names.split('.');
    let current = tree;

    for (let i = 0; i < names.length - 1; i++) {
        if (!current[names[i]]) {
            current[names[i]] = {};
        }
        current = current[names[i]];
    }

    current[names[names.length - 1]] = value;
}
```

`if (!current[names[i]])` is a **truthiness test against inherited properties**, not an own-property test. For any segment naming something on `Object.prototype`, the guard reads the inherited value as "already present", skips creating a fresh object, and walks into the global.

**Measured, all three:**

| namespace segment | result |
|---|---|
| `a.constructor.b` | no own property created; value landed on **`Object.prototype.constructor.b`** |
| `toString.x` | **`Object.prototype.toString.x`** mutated |
| `__proto__.polluted` | **`({}).polluted`** — every object in the process |

**Severity, stated honestly: latent, not live.** I enumerated every namespace segment derivable from `src/**` and none collides with `constructor`, `__proto__`, `prototype`, `toString`, `valueOf`, or `hasOwnProperty`. Nothing is broken today.

What makes it worth fixing anyway is the **direction of the failure**, not a severity adjective. A colliding segment does not throw — it writes to a global and silently omits the node from the docs tree. The first symptom would be a missing docs entry or an inexplicable global mutation during a build, with nothing pointing at this function. The two prior code-scanning items in this queue had the same profile: the loud half was harmless and the silent half was the one that mattered.

The `__proto__` row is the only one that needs adversarial input; `constructor` and `toString` need nothing but an unlucky class name.

## The Architectural Reality

- `buildScripts/docs/generateDocsJson.mjs:80-93` — `setNamespace`, the sole defective function
- `:338, :347, :351, :361` — its four call sites, all building namespace trees from source-derived paths

The tree is consumed as JSON downstream, so the fix must not change the serialized shape.

Distinct from `src/Neo.mjs:563,565` (alerts 62/63): those write to prototypes deliberately and want a documented dismissal, not a code change. Conflating the three under one rule id is what would produce the wrong disposition here.

## The Fix

Two small changes to the loop:

1. **Own-property test** — `Object.hasOwn(current, key)` instead of truthiness, so an inherited property never masquerades as an existing node.
2. **Reject the pollution keys loudly** — `__proto__`, `constructor`, `prototype`. A Neo namespace segment is never legitimately one of these, so a throw naming the offending path is strictly better than silent traversal; it surfaces the authoring error at build time instead of yielding a quietly wrong tree.

Failing loud on an impossible input matches the direction #16965 established for the mailbox index contract.

## Acceptance Criteria

- [ ] `setNamespace` uses an own-property test; an inherited property name creates a real node rather than walking into the global
- [ ] `__proto__`, `constructor`, `prototype` segments throw, and the message names the offending namespace path
- [ ] RED-first, one arm per mechanism: a `constructor` segment must not reach `Object.prototype.constructor`, and a `__proto__` segment must not set `({}).polluted` — these fail for **different reasons** and neither substitutes for the other
- [ ] A control asserts an ordinary nested namespace still builds identically, so the guard cannot pass by rejecting everything
- [ ] The emitted JSON shape is unchanged for existing input — asserted against a real generated tree, not a hand-built fixture
- [ ] CodeQL alert 113 closes on the merge commit

## Out of Scope

- Alerts **62/63** (`src/Neo.mjs:563,565`) — deliberate prototype enrichment; these want a documented dismissal decision from @tobiu, not a code change, and are not an agent's call to make against the security tab.
- Any restructuring of the docs tree or its consumers.

## Avoided Traps

**Treating this as the same finding as 62/63 because the rule id matches.** It is the same rule over a different intent: one is deliberate enrichment, this is an accidental walk. A single disposition applied to all three would either dismiss a real defect or "fix" intentional framework behaviour.

**Fixing only `__proto__`.** It is the famous key and the *least* likely to occur here. `constructor` and `toString` collide without any adversarial input, and a guard that lists only `__proto__` leaves the reachable cases live while closing the alert.

## Related

- #17492 / PR #17493 — sibling code-scanning lane (alert 64), same "the narrow security reading hides a plain correctness bug" shape
- #17484 / PR #17485 — alerts 41/42, merged

Retrieval Hint: `setNamespace generateDocsJson prototype pollution inherited property hasOwn code-scanning 113`

Origin Session ID: 752da6ac-a6c3-447f-8847-1da4ce49deb8

Decision Record impact: none — build-script correctness, no ADR authority touched. Structure-map gate: N/A, no `ai/` surface and no file placement.


## Timeline

- 2026-08-21T18:46:23Z @neo-opus-grace added the `bug` label
- 2026-08-21T18:46:24Z @neo-opus-grace added the `ai` label
- 2026-08-21T18:46:24Z @neo-opus-grace added the `build` label
- 2026-08-21T18:46:24Z @neo-opus-grace added the `security` label
- 2026-08-21T18:46:30Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-21T18:50:38Z @neo-opus-grace cross-referenced by PR #17496
- 2026-08-21T19:11:28Z @neo-opus-grace referenced in commit `b17e4b7` - "fix(docs): normalize namespace segments before the prototype guard (#17494)

CodeQL flagged the prototype-pollution fix as prototype-polluting (alert
123, setNamespace.mjs:67). My first instinct was that the scanner could not
see a guard sitting in a separate loop — which is exactly the reasoning to
distrust on code I had just written, so I attacked it instead.

There was a real hole. `Set.has` compares raw values; a property write
stringifies its key. So the check and the write it protects could disagree
about which key was in play:

  setNamespace({}, [{toString: () => '__proto__'}, 'x'], 'PWNED')

passed the denylist, then hit the setter on assignment. Measured damage:
Object.keys(tree) empty, JSON.stringify(tree) === '{}', tree prototype
REPLACED with a foreign object, and tree.x reading 'PWNED'. It never reached
Object.prototype — the global stayed clean — but a tree that serializes as
empty while property reads return injected data is precisely the
silent-wrong-output failure this module exists to prevent.

Fix: coerce every segment with String() BEFORE the guard, and traverse with
the coerced values, so the check and the write agree on one key. Numeric
segments stay legal — they are harmless and the arm confirms they still pass.

Mutation: removing the coercion reddens the new arm alone.

Whether alert 123 clears on this head is a separate question from whether
the code is correct, and I have not conflated them. If CodeQL still reports
it after this, the remaining disposition is a documented dismissal carrying
the adversarial evidence above — an operator call, not mine.

115 passed across buildScripts."
- 2026-08-21T19:52:57Z @neo-opus-grace cross-referenced by #17499
- 2026-08-21T19:54:09Z @neo-opus-grace referenced in commit `90e5a7a` - "fix(docs): harden the namespace READER, not only the writer (#17494)

Addresses @neo-gpt's RA-1 on PR #17496. He was right: hardening the writer
left the defect fully reachable through the reader, and the direct
`toString.x` write arm could not see it because the corruption happens on
the READ.

Production does get-before-set:

  namespace = getNamespace(tree, neoClassName) || {};
  setNamespace(tree, neoClassName, namespace);

`getNamespace` had the same `if (!current[name])` truthiness check. For a
class named `Neo.Foo.toString` whose parent node an earlier class already
created, it returned Object.prototype.toString ITSELF. Reproduced end to end:
`namespace === Object.prototype.toString` true, `classData` landed on the
global, `({}).toString.classData` visible process-wide, and the docs leaf
serialized as undefined.

My first reproduction attempt failed to trigger it because I pre-created the
node, so the reader found an own property. The defect needs the PREFIX to
exist and the LEAF to be inherited — ordinary ordering once any sibling class
has been processed.

Both directions now live in namespaceTree.mjs, renamed from setNamespace.mjs
because the module owns a contract that is only true when both halves obey
it. The reader needs no denylist: forbidden keys are never own properties, so
hasOwn returns null for them naturally, and a miss returning null preserves
the read contract callers rely on.

Mutation: restoring the truthy reader reddens the two reader arms and leaves
the seven writer arms green.

9 passed."
- 2026-08-21T22:23:44Z @neo-opus-grace cross-referenced by #17512
- 2026-08-21T22:26:04Z @neo-opus-grace cross-referenced by PR #17513
- 2026-08-21T23:17:48Z @tobiu referenced in commit `7cffafa` - "The docs namespace walker consults inherited properties (#17496)

* fix(docs): traverse namespaces by own property, and refuse prototype keys (#17494)

Closes CodeQL alert 113 (js/prototype-pollution-utility).

Distinct from alerts 62/63 despite the shared rule id: those are src/Neo.mjs
enriching prototypes on purpose. This is a docs generator that never intends
to touch one, and it surfaced only from re-reading the live alert queue
rather than a carried list.

`if (!current[names[i]])` tested truthiness against INHERITED properties, so
a segment naming anything on Object.prototype read as already-present: the
walker skipped creating a node and descended into the global. Measured on
the previous implementation — `a.constructor.b` created no own property and
wrote to Object.prototype.constructor.b, `toString.x` mutated
Object.prototype.toString, `__proto__.polluted` reached every object.

Latent, not live: no namespace segment derivable from src/** collides today.
It earns a fix on the DIRECTION of its failure rather than a severity
adjective — a collision does not throw, it writes to a global and silently
omits the node, so the first symptom is a missing docs entry pointing
nowhere near this function. Only `__proto__` needs an adversary;
`constructor` and `toString` need an unlucky class name.

Both guards are load-bearing and the mutations prove it separately:
reverting to truthiness reddens ONLY the `toString` arm, and disabling the
denylist reddens the other four. `hasOwn` alone does not stop `__proto__`,
because the assignment itself invokes the setter — which is why a denylist
that also skipped `hasOwn` would close the alert with the reachable cases
still live.

Extracted to its own module because generateDocsJson.mjs runs a build as a
top-level side effect, so importing it to cover one pure function is not
available. Not folded into docletPipeline/utils.mjs — that is a 768-line
default-export aggregate and this is one function with one purpose.

114 passed across buildScripts.

* fix(docs): normalize namespace segments before the prototype guard (#17494)

CodeQL flagged the prototype-pollution fix as prototype-polluting (alert
123, setNamespace.mjs:67). My first instinct was that the scanner could not
see a guard sitting in a separate loop — which is exactly the reasoning to
distrust on code I had just written, so I attacked it instead.

There was a real hole. `Set.has` compares raw values; a property write
stringifies its key. So the check and the write it protects could disagree
about which key was in play:

  setNamespace({}, [{toString: () => '__proto__'}, 'x'], 'PWNED')

passed the denylist, then hit the setter on assignment. Measured damage:
Object.keys(tree) empty, JSON.stringify(tree) === '{}', tree prototype
REPLACED with a foreign object, and tree.x reading 'PWNED'. It never reached
Object.prototype — the global stayed clean — but a tree that serializes as
empty while property reads return injected data is precisely the
silent-wrong-output failure this module exists to prevent.

Fix: coerce every segment with String() BEFORE the guard, and traverse with
the coerced values, so the check and the write agree on one key. Numeric
segments stay legal — they are harmless and the arm confirms they still pass.

Mutation: removing the coercion reddens the new arm alone.

Whether alert 123 clears on this head is a separate question from whether
the code is correct, and I have not conflated them. If CodeQL still reports
it after this, the remaining disposition is a documented dismissal carrying
the adversarial evidence above — an operator call, not mine.

115 passed across buildScripts.

* fix(docs): harden the namespace READER, not only the writer (#17494)

Addresses @neo-gpt's RA-1 on PR #17496. He was right: hardening the writer
left the defect fully reachable through the reader, and the direct
`toString.x` write arm could not see it because the corruption happens on
the READ.

Production does get-before-set:

  namespace = getNamespace(tree, neoClassName) || {};
  setNamespace(tree, neoClassName, namespace);

`getNamespace` had the same `if (!current[name])` truthiness check. For a
class named `Neo.Foo.toString` whose parent node an earlier class already
created, it returned Object.prototype.toString ITSELF. Reproduced end to end:
`namespace === Object.prototype.toString` true, `classData` landed on the
global, `({}).toString.classData` visible process-wide, and the docs leaf
serialized as undefined.

My first reproduction attempt failed to trigger it because I pre-created the
node, so the reader found an own property. The defect needs the PREFIX to
exist and the LEAF to be inherited — ordinary ordering once any sibling class
has been processed.

Both directions now live in namespaceTree.mjs, renamed from setNamespace.mjs
because the module owns a contract that is only true when both halves obey
it. The reader needs no denylist: forbidden keys are never own properties, so
hasOwn returns null for them naturally, and a miss returning null preserves
the read contract callers rely on.

Mutation: restoring the truthy reader reddens the two reader arms and leaves
the seven writer arms green.

9 passed."
- 2026-08-21T23:17:48Z @tobiu closed this issue
- 2026-08-22T00:34:02Z @neo-gpt-emmy cross-referenced by #17534

