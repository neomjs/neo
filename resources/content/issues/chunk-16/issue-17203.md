---
id: 17203
title: The theme-staleness guard teaches the manual rebuild and never mentions watch-themes
state: CLOSED
labels:
  - enhancement
  - ai
  - build
assignees:
  - neo-opus-grace
createdAt: '2026-08-15T18:41:08Z'
updatedAt: '2026-08-24T23:21:20Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17203'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-24T23:21:20Z'
---
# The theme-staleness guard teaches the manual rebuild and never mentions watch-themes

> **CORRECTED BY THE OPERATOR BEFORE ANY WORK STARTED — the original prescription was wrong.**
>
> *"A new branch needs ONE theme build. Afterwards `npm run watch-themes` => done."*
>
> `watch-themes` exists (`node ./buildScripts/helpers/watchThemes.mjs`) and is the durable answer. So auto-rebuilding inside `globalSetup` is the **wrong fix**: it would duplicate a watcher that already solves this continuously, for every consumer — not just this harness.
>
> **I proposed automating something that is already automated, because I reached for a fix without checking whether the capability existed.** That is the same assert-before-verify failure I spent the day cataloguing in code, applied to my own enablement proposal — and it landed one hour after I wrote a memory about friction awareness. Recorded rather than quietly rewritten.
>
> **SECOND CORRECTION — my cost figure was invented too.** I wrote "~30s" for the theme build. Measured on this machine: **1.29s real**. Off by more than 20×, and I had put it in this ticket and a Discussion comment as though it were measured.
>
> That changes the reasoning, not just a digit. My argument against auto-rebuild was *"it would bolt 30s onto every run"* — at 1.3s that objection collapses. **The conclusion survives on a different reason than the one I gave:** the watcher already exists, is incremental and near-instant, and serves every consumer (dev server, browser, any suite) rather than one harness. A conclusion whose stated justification collapsed is one to re-derive, not to keep — so it is re-derived here, and the teaching-surface finding below is the part that never depended on the number.
>
> **The real defect is sharper than the one I filed.** The guard's message names only the one-off command:
>
> ```
> Rebuild first:
>   node ./buildScripts/build/themes.mjs -f -n -e dev
> ```
>
> It never mentions `watch-themes`. So every time it fires it **teaches the manual workflow** — and it fires precisely at the moment someone is least likely to go looking for a better one. I hit it twice today, typed the printed command twice, and did not learn the watcher existed until told. An error message is a teaching surface, and this one teaches the worse habit with perfect reliability.
>
> The ACs below are replaced by the corrected set at the bottom.

Refs #17201
Refs D#17085

## Problem

A recurring shape across our tooling: a guard **detects** a condition precisely, **knows the exact command** that resolves it, prints that command, and then **throws so a human or agent retypes it.**

Instance found today, `test/playwright/visual/globalSetup.mjs`:

```js
if (newestScss > newestCss) {
    throw new Error(
        'visual harness: the built theme CSS is OLDER than the newest SCSS source — a baseline over ' +
        'stale artifacts is a poisoned golden. Rebuild first:\n' +
        '  node ./buildScripts/build/themes.mjs -f -n -e dev'
    )
}
```

The detection is exactly right and the reasoning is correct — a baseline over stale artifacts *is* a poisoned golden. But the resolution is deterministic, idempotent, and already written down two lines below the throw. I hit this twice in one session, ran the printed command verbatim both times, and exercised no judgment either time.

`check-block-alignment` is the same shape with a real caveat and is filed separately as #17201; that one needs its fixer scoped before it can auto-apply. **This one has no such caveat** — a theme rebuild is idempotent and whole-tree by nature, so there is nothing to iron out.

## Why this is the cheapest class of improvement we have

It removes cost while **keeping the guarantee completely intact**. The same condition is detected by the same code; only the disposition changes from *refuse* to *repair*. There is no risk calculus, no incident-currency question, no judgment about whether the rule still earns its place — the rule is untouched.

## The Architectural Reality

- The throw exists to prevent a poisoned baseline. Rebuilding satisfies that intent **more** directly than refusing does: after a rebuild the artifacts are fresh, which is the state the guard wants.
- Cost is honest either way. Refusing costs a 30s rebuild **plus** a failed run, a round-trip, and the reader's attention. Auto-running costs the 30s.
- The failure mode of auto-running is bounded and visible: the build is idempotent, so the worst case is a no-op that logs.
- This is not "remove the guard". Anything that cannot deterministically compute its own fix keeps throwing.

## Out of Scope

- `check-block-alignment` — same verb, real blocker, filed as #17201.
- Any guard whose fix requires a decision. The test is *deterministic and idempotent*, not merely *known*.
- Rewriting the guards' detection logic. Detection is what these get right.

## Acceptance Criteria (SUPERSEDED — see the corrected set below)

- [ ] The visual harness rebuilds stale theme CSS itself and proceeds, rather than throwing with the command in the message. A run against deliberately stale artifacts completes green with no author action.
- [ ] The no-built-CSS-at-all branch behaves the same way — it prints the identical command today, so it is the identical case.
- [ ] Auto-repair is announced, not silent: the run logs that it rebuilt and why, so a slow first run is explained rather than mysterious.
- [ ] A repair that **fails** still fails the run, loudly, with the original guidance — this converts a refusal into an attempt, never into a suppression.
- [ ] A sweep records every other guard in `buildScripts/` and `test/` that prints a fix command in its failure text, with a keep/convert verdict per instance and the deterministic-and-idempotent test applied. The sweep output goes in the PR body so the population is a measurement, not an impression.

## Evidence class

Read at source on `dev` 2026-08-15: `globalSetup.mjs` staleness and no-CSS branches, both printing `node ./buildScripts/build/themes.mjs -f -n -e dev`. Friction observed live twice in one session, resolved identically both times with no judgment exercised. Sibling instance and its scoping blocker diagnosed in #17201.

Live latest-open sweep at 2026-08-15T18:31:40Z plus the tickets filed since; nothing equivalent open. A2A sweep: no competing claim.

## Decision Record impact

`none`.

🖖 Authored by Grace (Claude Opus 5, Claude Code). Session b17338dd-b474-494f-b08c-683044de2ddb. Filed after the operator's coffee-machine framing — the point being that I hit this twice today, typed the printed command both times, and did not once ask why I was the one typing it.


## Acceptance Criteria (corrected)

- [ ] The staleness and no-CSS messages name the **durable** setup first — one build, then `npm run watch-themes` — and keep the one-off command as the fallback for a single run. The message teaches the workflow that stops it recurring, not the one that resolves this instance.
- [ ] The same correction lands wherever else the one-off build command is printed as guidance, found by sweeping for it rather than by memory.
- [ ] No auto-rebuild is added to `globalSetup` — but on the correct grounds: the watcher already covers it and serves every consumer, not because a rebuild is expensive (it is 1.29s here). If a belt-and-braces net is ever wanted, cost is not the argument against it.
- [ ] Where a new-branch first build is genuinely required, that sequencing is stated once, in the place a session actually starts, rather than discovered from a failed test run.
- [ ] The `check-block-alignment` convert in #17201 is re-examined against this same question before implementation: is there an existing capability that makes the conversion unnecessary? The lesson generalises — **check for the tool before building the tool.**



## Timeline

- 2026-08-15T18:41:08Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-15T18:41:10Z @neo-opus-grace added the `enhancement` label
- 2026-08-15T18:41:10Z @neo-opus-grace added the `ai` label
- 2026-08-15T18:41:10Z @neo-opus-grace added the `build` label
- 2026-08-15T18:44:11Z @neo-opus-grace changed title from **Guards that know the fix and make you type it anyway** to **The theme-staleness guard teaches the manual rebuild and never mentions watch-themes**
- 2026-08-15T20:23:11Z @neo-fable-clio cross-referenced by #17209
- 2026-08-15T23:12:34Z @neo-fable-clio cross-referenced by #17222
- 2026-08-24T21:07:00Z @neo-opus-grace cross-referenced by #17201
- 2026-08-24T21:07:19Z @neo-opus-grace cross-referenced by PR #17735
- 2026-08-24T21:29:41Z @neo-opus-grace referenced in commit `4753f00` - "fix(visual): the printed theme guidance is executable as printed (#17203)

@neo-gpt-emmy's RA-1, verified independently at exact head. The guidance I shipped could
not be run as written, in two separate ways.

'npm run build-themes' expands to 'themes.mjs -f'. Without -n the script falls into its
Inquirer theme/environment prompts (themes.mjs:71, 'if (!programOpts.noquestions)') — the
reader gets an interactive question where they expected a rebuild.

And 'watch-themes' holds a recursive fs.watch and is designed to stay up, so chaining it
with '&&' hands the author a blocked terminal instead of returning them to the run they
came here to fix.

The recovery line is now DEVELOPMENT_THEME_BUILD_COMMAND, imported from the same module
this file already reads mtimes from rather than retyped. Retyping is how the broken form
got here: the constant exists precisely because the flag set is not obvious, and the e2e
preflight already prints it for the same reason. watch-themes is listed separately and
marked long-running.

The AC-2 sweep missed this and the miss is instructive: I grepped the literal path
'buildScripts/build/themes.mjs', which is structurally blind to every instance that
references the constant instead. Re-swept by CLAIM rather than by token — the surviving
guidance sites use DEVELOPMENT_THEME_BUILD_COMMAND, and watchThemes.spec.mjs:283 already
pins that exact recovery string, so this file now matches the house idiom instead of
inventing a second one.

Emmy's framing is the keeper: a command in an error message is an API, and its
interactivity, process lifetime and terminal ownership are part of the contract.

Control: touching an SCSS file fires the stale branch; the printed text is read back off
the thrown error and both lines are now runnable as shown.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-24T21:31:01Z @neo-opus-grace referenced in commit `1ef4fa7` - "docs(agents): the theme slot is conditional, single-authority, and carries a sunset (#17203)

@neo-gpt-emmy's RA-2. The Step-0 line I added was unconditional 475-byte prose on the
MANDATORY boot path — every session paid for it, including the majority that never touch a
rendered surface — with no retirement trigger and no slot-rationale in the PR body. That is
the substrate-accretion defense, and I skipped it.

Three repairs, one per failing property:

CONDITIONAL — gated on SCSS / theme / rendered-surface lanes, so the load is paid by the
sessions it serves rather than by all of them.

SINGLE AUTHORITY — it no longer repeats the build command. The visual guard owns that text
and prints it when it fires; duplicating it here would have created the second stale copy
this very PR exists to prevent one file over.

SUNSET — retire once a watcher starts with the dev server, because at that point nothing
needs starting by hand and the slot is pure cost.

Net: 475 bytes unconditional becomes ~250 conditional.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-24T23:21:20Z @tobiu referenced in commit `76701ee` - "fix(visual): the theme guard teaches the durable setup, not the one-off rebuild (#17203) (#17735)

* fix(visual): the theme guard teaches the durable setup, not the one-off rebuild (#17203)

Both branches of the visual harness's theme guard printed only the one-off build command.
That resolves the instance and guarantees the next one: run it, edit SCSS again, meet the
identical failure. I hit this twice in one session and typed the printed command both times
without once asking why I was the one typing it.

The guidance now names the durable fix first — build once, then watch-themes, which serves
every consumer of the built CSS rather than this harness alone — and keeps the one-off as
the labelled fallback for a single run.

Deliberately NOT auto-rebuilding, and the JSDoc records the correct grounds: the watcher
already covers it. Cost is not the argument, since the build is ~1.3s; a repair bolted into
one harness's setup would be a second mechanism for a solved problem, reachable only by
authors who happen to run visual tests.

The no-CSS branch additionally names its real cause. It is the first-run state on a fresh
clone or a newly checked-out branch, because dist/development/css is gitignored and never
travels with a branch — previously the message said 'build first' and left the reader to
work out why the artifacts were missing at all.

AGENTS_STARTUP.md Step 0 states the sequencing once, where a session actually starts,
rather than leaving it to be discovered from a failed test run.

Control: touching an SCSS file makes it newer than the built CSS; the stale branch fires
with the new guidance. Both messages were rewritten, found by sweeping for the command
rather than from memory — the sweep shows every other hit is a real invocation or a doc,
never guidance in failure text.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* fix(visual): the printed theme guidance is executable as printed (#17203)

@neo-gpt-emmy's RA-1, verified independently at exact head. The guidance I shipped could
not be run as written, in two separate ways.

'npm run build-themes' expands to 'themes.mjs -f'. Without -n the script falls into its
Inquirer theme/environment prompts (themes.mjs:71, 'if (!programOpts.noquestions)') — the
reader gets an interactive question where they expected a rebuild.

And 'watch-themes' holds a recursive fs.watch and is designed to stay up, so chaining it
with '&&' hands the author a blocked terminal instead of returning them to the run they
came here to fix.

The recovery line is now DEVELOPMENT_THEME_BUILD_COMMAND, imported from the same module
this file already reads mtimes from rather than retyped. Retyping is how the broken form
got here: the constant exists precisely because the flag set is not obvious, and the e2e
preflight already prints it for the same reason. watch-themes is listed separately and
marked long-running.

The AC-2 sweep missed this and the miss is instructive: I grepped the literal path
'buildScripts/build/themes.mjs', which is structurally blind to every instance that
references the constant instead. Re-swept by CLAIM rather than by token — the surviving
guidance sites use DEVELOPMENT_THEME_BUILD_COMMAND, and watchThemes.spec.mjs:283 already
pins that exact recovery string, so this file now matches the house idiom instead of
inventing a second one.

Emmy's framing is the keeper: a command in an error message is an API, and its
interactivity, process lifetime and terminal ownership are part of the contract.

Control: touching an SCSS file fires the stale branch; the printed text is read back off
the thrown error and both lines are now runnable as shown.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* docs(agents): the theme slot is conditional, single-authority, and carries a sunset (#17203)

@neo-gpt-emmy's RA-2. The Step-0 line I added was unconditional 475-byte prose on the
MANDATORY boot path — every session paid for it, including the majority that never touch a
rendered surface — with no retirement trigger and no slot-rationale in the PR body. That is
the substrate-accretion defense, and I skipped it.

Three repairs, one per failing property:

CONDITIONAL — gated on SCSS / theme / rendered-surface lanes, so the load is paid by the
sessions it serves rather than by all of them.

SINGLE AUTHORITY — it no longer repeats the build command. The visual guard owns that text
and prints it when it fires; duplicating it here would have created the second stale copy
this very PR exists to prevent one file over.

SUNSET — retire once a watcher starts with the dev server, because at that point nothing
needs starting by hand and the slot is pure cost.

Net: 475 bytes unconditional becomes ~250 conditional.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-24T23:21:20Z @tobiu closed this issue

