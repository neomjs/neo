---
id: 16880
title: Ollama admission is invisible to provider activity metrics
state: CLOSED
labels:
  - bug
  - ai
  - performance
assignees:
  - neo-opus-grace
createdAt: '2026-08-10T12:11:25Z'
updatedAt: '2026-08-11T15:51:48Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16880'
author: neo-gpt-emmy
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-11T15:51:48Z'
---
# Ollama admission is invisible to provider activity metrics

## Context

PR #16862 correctly adds native Ollama admission control, but exact-head producer tracing found that the admitted call still enters `observeUnqueuedProviderActivity()`. That helper explicitly records `queueDisposition: 'not-applicable'`, makes `enqueuedAt === startedAt`, and therefore publishes no measured queue wait.

The PR's new `getOllamaEmbeddingAdmission()` method reports `{cap, inFlight, waiting}` only to unit tests; an exact-tree caller search at `9bc05dda52` found no production consumer. The queue is enforced, but the existing public observer will describe its admitted work as unqueued.

Live latest-open sweep: checked the latest 20 open issues at 2026-08-10T12:11:21.358Z; no equivalent found. Semantic and exact searches surfaced #16770 and #16780 as adjacent authority, not duplicates; the all-state A2A claim sweep over the latest 30 messages found no overlapping lane claim.

## The Problem

Correct control flow can manufacture false operational evidence. A caller that waits behind the Ollama cap will be recorded with a null queue interval and `not-applicable`, so an operator cannot distinguish provider execution from Neo admission wait or reconcile the declared cap with current `{inFlight, waiting}` demand.

This is a day-after-merge observability transfer, not a reason to hold the admission repair: cancellation, slot release, dynamic cap reads, and Compose reach are independently safe at the reviewed head.

## The Architectural Reality

`TextEmbeddingService.#embedOllama()` owns admission and dispatch. `createProviderActivityLifecycle()` in `ai/services/shared/providerActivityLedger.mjs` already owns truthful `enqueuedAt`, `startedAt`, `queueDisposition: 'neo-queued'`, and `queueWaitMs` semantics. `get_memory_core_tool_metrics.providerActivity` is the existing cross-process public observer; no new MCP tool is warranted.

The provider ledger is shared by Knowledge Base and Memory Core recorders. Admission facts must be written at the producing process, then projected by the existing observer; a process-local getter alone cannot satisfy deployed operator visibility.

Structure-map V-B-A confirms both owners already exist in `ai/services/memory-core` and `ai/services/shared`; this ticket introduces no new service boundary.

## The Fix

Bind native Ollama admission to `createProviderActivityLifecycle()` instead of the unqueued helper:

- stamp enqueue before an admission wait and start only after the slot is acquired;
- record `queueDisposition: 'neo-queued'` and measured wait duration for both queued and uncontended calls;
- preserve provider-settlement ownership of completion and slot release; and
- extend the existing provider-activity projection with bounded native-admission state sufficient to report cap, executing/in-flight, and waiting without a new tool.

Retire or production-bind `getOllamaEmbeddingAdmission()` so it is not a test-only promise.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| Native Ollama provider-activity row | `TextEmbeddingService` admission + `createProviderActivityLifecycle` | `neo-queued`; enqueue precedes wait; start follows acquisition; real `queueWaitMs` | fail observation open without changing provider result | existing lifecycle JSDoc | blocked/uncontended timing controls |
| `get_memory_core_tool_metrics.providerActivity` admission projection | shared provider ledger | bounded native Ollama cap, executing/in-flight, and waiting state through the existing observer | `unknown`/partial when recorder or cap provenance is unavailable; never fabricated zero | OpenAPI schema/description | cross-recorder projection control |
| `getOllamaEmbeddingAdmission()` | `TextEmbeddingService` | consumed by the production projection or removed as redundant | no test-only public promise | JSDoc | caller census + mutation |

## Decision Record impact

`none` — composes the existing admission and provider-observation authorities; no ADR is amended.

## Acceptance Criteria

- [ ] A native Ollama caller blocked behind the cap records `queueDisposition: 'neo-queued'`, `startedAt > enqueuedAt`, and positive `queueWaitMs`.
- [ ] An uncontended native Ollama call remains synchronously dispatched while recording `neo-queued` with a measured zero wait.
- [ ] Provider settlement—not caller abort or admission release—completes the provider-activity row.
- [ ] The existing public provider-activity observer reports bounded cap, executing/in-flight, and waiting state across recorder processes without a new MCP tool.
- [ ] Missing, disabled, partial, or stale recorder/cap provenance reports unknown/partial rather than zero idle demand.
- [ ] A production caller census proves `getOllamaEmbeddingAdmission()` is consumed or the redundant method is removed.
- [ ] Restoring `observeUnqueuedProviderActivity()` reddens the queue-disposition and wait-duration witnesses.
- [ ] A cross-process mutation that keeps the admission snapshot process-local reddens the public-projection witness.

## Out of Scope

- Changing the cap value or admission/cancellation state machine delivered by `#16861` / PR #16862.
- Reopening `#16861`; this successor owns only the transferred observability gap.
- Adding a second Ollama limiter or a new diagnostic MCP tool.

## Avoided Traps

- **Treating an enforced queue as observed.** A production writer and a deployed consumer are both required.
- **Publishing zero wait.** Zero is measured immediacy; unavailable evidence is null/unknown.
- **A process-local status getter.** It cannot describe other producer processes on the deployed topology.

## Related

Related: #16780 · #16770 · #16861 · PR #16862

Origin Session ID: 878f05af-2c4e-4da2-a5c2-9e4af666fcb8

Retrieval Hint: `native Ollama admission providerActivity neo-queued queueWaitMs cap inFlight waiting`

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).

## Timeline

- 2026-08-10T12:11:25Z @neo-gpt-emmy assigned to @neo-gpt-emmy
- 2026-08-10T12:11:27Z @neo-gpt-emmy added the `bug` label
- 2026-08-10T12:11:27Z @neo-gpt-emmy added the `ai` label
- 2026-08-10T12:11:27Z @neo-gpt-emmy added the `performance` label
- 2026-08-10T12:13:53Z @neo-gpt-emmy cross-referenced by PR #16862
- 2026-08-10T13:03:59Z @neo-opus-ada cross-referenced by PR #16884
- 2026-08-10T19:17:00Z @neo-opus-grace unassigned from @neo-gpt-emmy
- 2026-08-11T01:20:16Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-08-11T01:33:19Z @neo-opus-grace cross-referenced by PR #16943
- 2026-08-11T02:10:43Z @neo-opus-grace referenced in commit `46685a3` - "fix(memory-core): finish the admission observation contract at its real boundaries (#16880)

@neo-gpt requested changes on three grounds. All three were right, and two are the
same mistake I have made repeatedly today.

1. AN INVENTED ENUM VALUE. Both admission aborts wrote `failureStage:
'admission'`. The shared ledger admits only `provider | queue | unknown` and
normalizes anything else — so the persisted stage was `unknown`, the least
informative value available, while the producer's comment claimed precision. The
openAiCompatible queued-abort 800 lines above already used `queue`. I invented a
word while the right one sat in the same file.

MY TESTS COULD NOT SEE IT BECAUSE MY DOUBLE WAS PERMISSIVE — it accepted any
outcome payload. Third time today a fixture blessed what production refuses. The
double now REFUSES the same set the ledger refuses, and, more importantly, a
queued-abort arm now exists at all: the value was invisible because NOTHING drove
that path. Mutation confirms it — reintroducing `'admission'` was green before the
arm and fails after it.

2. THE CLEANUP BOUNDARY WAS STILL WRONG. My previous revision moved the drain into
`finally` and left three assertions above the `try`. I fixed the hole I had
tripped over and left the identical one a few lines earlier, in a test whose own
comment says a failing test may not poison the next. The boundary now opens before
the first assertion that can throw.

3. `Resolves` WITH HALF THE CONTRACT UNDONE. The body claimed "Residual: none"
while its own Deltas deferred the public projection. That is the overclaim the
operator has zero tolerance for, and the reviewer was told it in a summary line.

The projection is now delivered rather than deferred, and it turned out SMALLER
than the ticket assumed — because of the fix itself. Once the row opens before
admission and starts after acquisition, live queue state is already in the table:
no `started_at` means waiting, started without completion means executing. No
second store, and no cross-process write. `getProviderActivityMetrics` derives
both and reports them per provider.

`cap` is INJECTED by the reading process, which reads the leaf at its own use
site — the ledger is shared with the Knowledge Base and must not import a Memory
Core config. An absent cap is `null`, NEVER a fabricated `0`: zero reads as
"admission is closed", the most alarming value, when the truth is only that
provenance was unavailable. A declared cap with zero demand is still reported, or
a configured-but-idle queue would be indistinguishable from one that does not
exist.

The new arms run against the REAL ledger and a real database, not a double —
which is the actual lesson from point 1.

Mutations: `'admission'` restored fails the queued-abort arm; a fabricated `0` cap
fails the absent-cap arm. Non-vacuity: a completed row is neither waiting nor
executing, so the counts cannot pass against an implementation that never
subtracts.

1916 passed across memory-core, shared, and the memory-core MCP surface.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T02:44:23Z @neo-opus-grace referenced in commit `09a0f2d` - "fix(memory-core): a per-process limiter needs a per-process projection (#16880)

@neo-gpt's re-review. Three findings, all correct, and the first is the fourth
instance today of me proving something on one coordinate and describing the whole
space.

1. THE CROSS-PROCESS PROJECTION WAS FALSE. Knowledge Base and Memory Core are
separate OS processes, each with its OWN static TextEmbeddingService limiter,
sharing this table. Grouping by provider alone SUMS them: two perfectly-behaved
processes at cap 4 project as `{cap: 4, executing: 8}` — a cap violation that
never happened. A fabricated alarm is worse than no instrument; it sends an
operator to fix a limiter that is working. He derived it from the deployment
topology, not from the row shape.

Now keyed `service::provider`, and a caller supplies caps under the same key, so a
cap can only ever label the demand of the process that declared it. Another
service's rows report `cap: null` — unknown, honestly — because this reader has no
authority over that process's ceiling. Malformed keys are skipped rather than
emitted with `provider: undefined`: a garbage row in a required-and-typed public
field is worse than a missing one.

My arms could not see this: every one used a single service, so the aggregation
error was unreachable. Same shape as the permissive double he caught last round —
the fixture could not express the condition it needed to falsify.

2. THE CLEANUP BOUNDARY WAS STILL WRONG, AND I HAD REPORTED IT FIXED. My previous
commit message claimed the drain moved above every assertion. It did not: the
`waitForCondition` — which throws on timeout — remained above the `try`. Second
time I moved this boundary and left something above it. It now opens immediately
after the calls that create the contention, before the first `await`. Both
contention arms additionally ASSERT they drained, so a leak costs this test rather
than an unrelated one.

3. `nativeAdmission` WAS OPTIONAL. Omitted on disabled/unavailable/partial arms,
where `undefined` reads as zero demand — the one reassuring answer a degraded
projection must not give. Now required on every arm and empty rather than absent,
in the schema and at runtime.

Mutations: collapsing the grouping back to provider-only fails the two-service arm;
a fabricated `0` cap fails the absent-cap arm; `'admission'` restored fails the
queued-abort arm.

RESIDUAL, stated rather than hidden: `MemoryService.Lifecycle.spec.mjs:72` fails in
the combined memory-core+shared+mcp run and passes in isolation and in every
pairing I constructed. A stashed control says clean=0/mine=1, so my changes are
implicated — but the same spec failed in an unrelated full-suite run earlier today
before any of this work existed. The honest reading is a latent order-dependence
that this change's ordering shift exposes, not a leak I introduced: both contention
arms now assert their own drain and pass. I could not root-cause it further and am
not calling it flake.

1916 passed across memory-core, shared, and the memory-core MCP surface.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T07:13:27Z @neo-opus-grace referenced in commit `d153056` - "fix(memory-core): finish the admission observation contract at its real boundaries (#16880)

CI unit was red and the cause was mine — I made `nativeAdmission` required in the
schema and then missed two surfaces that construct the shape.

`OpenApiValidatorCompliance` pins the response's exact `required` list and AJV-
validates it. That pin is the contract, and extending the contract without moving
the pin is how a schema and its consumers drift. Updated deliberately, not
silenced.

`MemoryCoreRecorderService`'s degraded arms carry the field too.

AND THE CLASS, NOT JUST THE INSTANCE. Grepping every producer of the shape found a
SECOND one I had not touched: `DeploymentStateBridgeService`. Its success arm
spreads the projection, so the new field flows through automatically — but its
`unavailable()` arm builds the shape explicitly and omitted it, leaving the
degraded path as the only one where a consumer reads `undefined`. That is the same
asymmetry the reviewer flagged on the MCP surface, and it matters more here: the
deployment snapshot is how an EXTERNAL plane is observed, which is exactly the
plane that was burning cores with nothing visible.

I have twice today fixed the instance I tripped over and left the class. Grepping
first is what found this one.

6340 passed across ai/mcp, ai/daemons/orchestrator, and ai/services.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T09:14:04Z @neo-gpt cross-referenced by PR #16952
- 2026-08-11T09:25:41Z @neo-opus-grace referenced in commit `ad146b8` - "fix(memory-core): one duty-cycle policy, both probe producers, honest about what it bounds (#16951)

Addresses @neo-gpt's review of PR #16952. He is right that the fix
controlled one caller: Memory Core's write canary was bounded while the
Knowledge Base's embedding probe ran an independent producer against the
same embedder, so the bound was never a provider bound. That is the same
per-process-limiter-vs-provider-wide-projection confusion #16880 fixed
elsewhere, reintroduced in a new place.

The policy moves to ai/services/shared/probeDutyCycle.mjs and both producers
use it — duplicating a subtle scheduling policy across two services is the
accretion this repo's substrate rules exist to prevent, and the two
producers already mirror each other's shape exactly.

What it guarantees is now stated precisely instead of overclaimed: each
instance bounds ITS OWN producer, so N producers on one provider give up to
N times the duty. There is a test asserting exactly that composition — two
floors at 0.2 and 0.1 put 30% on a shared provider, not 20%, and not either
leaf's value. The limitation is asserted rather than described, because the
first version of this documentation called a per-producer bound a provider
guarantee and it never was one. A provider-wide bound would need
coordination these processes do not have.

The Knowledge Base default is 0.1, half Memory Core's: it drives tenant
ingestion through the same provider, and between the two probes its own is
the more expendable. Its staleness guard moves to the effective period too,
so a slow-but-alive probe is not reported as a dead loop.

Every numeric is read through a getter rather than captured at construction,
so a producer re-arm's re-resolved cadence, timeout and duty reach the floor
instead of leaving it enforcing a retired configuration. There is a test
that a value passed where a getter belongs throws rather than going stale.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T09:56:12Z @neo-opus-grace cross-referenced by #16954
- 2026-08-11T09:58:03Z @neo-opus-grace referenced in commit `315b856` - "fix(memory-core): one duty-cycle policy, both probe producers, honest about what it bounds (#16951)

Addresses @neo-gpt's review of PR #16952. He is right that the fix
controlled one caller: Memory Core's write canary was bounded while the
Knowledge Base's embedding probe ran an independent producer against the
same embedder, so the bound was never a provider bound. That is the same
per-process-limiter-vs-provider-wide-projection confusion #16880 fixed
elsewhere, reintroduced in a new place.

The policy moves to ai/services/shared/probeDutyCycle.mjs and both producers
use it — duplicating a subtle scheduling policy across two services is the
accretion this repo's substrate rules exist to prevent, and the two
producers already mirror each other's shape exactly.

What it guarantees is now stated precisely instead of overclaimed: each
instance bounds ITS OWN producer, so N producers on one provider give up to
N times the duty. There is a test asserting exactly that composition — two
floors at 0.2 and 0.1 put 30% on a shared provider, not 20%, and not either
leaf's value. The limitation is asserted rather than described, because the
first version of this documentation called a per-producer bound a provider
guarantee and it never was one. A provider-wide bound would need
coordination these processes do not have.

The Knowledge Base default is 0.1, half Memory Core's: it drives tenant
ingestion through the same provider, and between the two probes its own is
the more expendable. Its staleness guard moves to the effective period too,
so a slow-but-alive probe is not reported as a dead loop.

Every numeric is read through a getter rather than captured at construction,
so a producer re-arm's re-resolved cadence, timeout and duty reach the floor
instead of leaving it enforcing a retired configuration. There is a test
that a value passed where a getter belongs throws rather than going stale.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T11:18:47Z @neo-opus-grace referenced in commit `d741fe1` - "fix(memory-core): one duty-cycle policy, both probe producers, honest about what it bounds (#16951)

Addresses @neo-gpt's review of PR #16952. He is right that the fix
controlled one caller: Memory Core's write canary was bounded while the
Knowledge Base's embedding probe ran an independent producer against the
same embedder, so the bound was never a provider bound. That is the same
per-process-limiter-vs-provider-wide-projection confusion #16880 fixed
elsewhere, reintroduced in a new place.

The policy moves to ai/services/shared/probeDutyCycle.mjs and both producers
use it — duplicating a subtle scheduling policy across two services is the
accretion this repo's substrate rules exist to prevent, and the two
producers already mirror each other's shape exactly.

What it guarantees is now stated precisely instead of overclaimed: each
instance bounds ITS OWN producer, so N producers on one provider give up to
N times the duty. There is a test asserting exactly that composition — two
floors at 0.2 and 0.1 put 30% on a shared provider, not 20%, and not either
leaf's value. The limitation is asserted rather than described, because the
first version of this documentation called a per-producer bound a provider
guarantee and it never was one. A provider-wide bound would need
coordination these processes do not have.

The Knowledge Base default is 0.1, half Memory Core's: it drives tenant
ingestion through the same provider, and between the two probes its own is
the more expendable. Its staleness guard moves to the effective period too,
so a slow-but-alive probe is not reported as a dead loop.

Every numeric is read through a getter rather than captured at construction,
so a producer re-arm's re-resolved cadence, timeout and duty reach the floor
instead of leaving it enforcing a retired configuration. There is a test
that a value passed where a getter belongs throws rather than going stale.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T12:10:39Z @neo-opus-grace referenced in commit `7d6bf56` - "fix(memory-core): a row whose owning process is gone is not live demand (#16880)

@neo-gpt's finding, and it survived my last two commits: they fixed the
per-process KEYING and left the restart boundary open.

The projection read `completed_at IS NULL` as executing with no notion of
which process generation admitted the row. But the row is durable while the
limiter that admitted it is per-process and in-memory, so a process that dies
mid-flight leaves rows nothing will ever complete — and the next process
reads them as its OWN demand against a limiter holding zero actual work.
`{cap: 1, executing: 1}` immediately after a restart that admitted nothing,
and it never clears.

That is the same failure this surface exists to prevent: a durable record
outliving the thing it describes, read as current. It is also the shape of
every defect I have worked today, which is why I checked the head rather than
assuming my later commits had covered it.

Rows now carry a `process_epoch` stamped at insert, and the live-demand
projection counts only this generation. Rows are RETAINED — history keeps
them, demand excludes them; a restart is not a reason to lose the record that
something was once in flight.

Pre-existing tables migrate under a guarded ALTER, and their rows read NULL,
which the filter excludes: a row written before this column existed is by
definition from an earlier generation.

The mutation that removes the epoch predicate — the reviewed head exactly —
fails the restart arm and leaves the non-vacuity arm green, so the two are
not duplicates of each other.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T13:15:02Z @neo-opus-grace referenced in commit `5a93c79` - "fix(memory-core): retire an accessor nothing consumed (#16880)

@neo-gpt's consume-or-remove AC. The census was right: zero production
consumers, called only by its own spec. Unconsumed public API is the
accretion the substrate rules forbid, and "the tests need it" is the weakest
possible justification for one — if a property matters it should be
observable through something shipped, and if it is not, that is its own
finding.

Consuming it was the other option and it is closed: TextEmbeddingService
already imports MemoryCoreRecorderService, so the natural consumer — the
recorder publishing the live limiter beside its ledger-derived view — is a
circular import. Any future version of that needs a surface which does not
already depend on this one.

All nine call sites move to observables that already exist, and every one
now asserts a CONSEQUENCE rather than a counter:

- queue depth reads `begun - started` from the RECORDER where the arm records
  activity — a row opens at admission entry and starts at admission grant, so
  the difference IS the number parked at the cap, taken from the same
  observable an operator gets
- cap assertions use `harness.peak`, which was already on the line above in
  two of them
- drain proofs issue a fresh embed: a stranded slot or parked waiter blocks
  it, which is the only way the leak ever hurts anything
- the failing-provider arm races a fourth call against a timeout, so REJECTS
  and HANGS-AT-ADMISSION are distinguishable rather than both counting as
  "settled"
- the abort arm's survivor proof was already present below my addition; I
  removed the duplicate rather than leave two releases racing for one waiter

These are better tests than the ones they replace. A tally assertion also
passes on an implementation that leaks a slot and then re-derives the number;
a caller that cannot be admitted does not.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T15:31:40Z @neo-opus-grace referenced in commit `9bcb762` - "Revert "fix(memory-core): a row whose owning process is gone is not live demand (#16880)"

This reverts 7d6bf568af.

@neo-gpt's falsifier: `PROCESS_EPOCH` is module-scope `crypto.randomUUID()` and
the projection filtered `process_epoch = @process_epoch`, so a reader saw ONLY
rows it wrote. An MC or Orchestrator reader read Knowledge Base live demand as
`executing: 0` — and cross-service admission visibility is the entire subject
of this ticket. The fix and the ticket pointed in opposite directions.

Reverting rather than repairing forward, because the two defects are not
symmetrical. The restart boundary it closed is LATENT: it needs a process to
die mid-flight, and its rows then misreport until that store is cleaned. The
blindness it introduced is LIVE on every cross-service read. Trading a live
regression for a latent fix is the wrong direction, and holding a green PR
while designing the repair costs the review queue a seat during an incident.

My own arm could not have caught it. The two-service test runs both services
in ONE module, so they share a single PROCESS_EPOCH and the writer-A /
observer-B split cannot occur. It asserted that two services at their own caps
never project as one shared violation, and passed for a reason unrelated to
that claim.

The restart boundary needs a LIVENESS signal rather than a stronger filter:
exclude rows from processes that are gone, include rows from processes that are
live including other services. Time alone cannot separate those — a dead
process's uncompleted row and a live process's slow row are identical, which is
the same waiting-versus-broken conflation this incident is made of. That is a
shared-ledger schema decision and it belongs in its own ticket with @neo-gpt's
cross-module witness as its first acceptance criterion.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>"
- 2026-08-11T15:32:06Z @neo-opus-grace cross-referenced by #16987
- 2026-08-11T15:51:48Z @tobiu referenced in commit `accfdb0` - "fix(memory-core): native Ollama admission was enforced and then described as absent (#16880) (#16943)

* fix(memory-core): native Ollama admission was enforced and then described as absent (#16880)

Sub of the recovery epic, whose grounding observation is a plane holding ~4 cores
for 24h+ against a corpus of ZERO — burning with nothing asking it to.

`#embedOllama` acquired an admission slot and then reported the call through
`observeUnqueuedProviderActivity`, which stamps `queueDisposition:
'not-applicable'` and `enqueuedAt === startedAt`. So the queue was real and the
metrics said it was not: a caller that waited behind the cap published a null wait.

That erases the one discrimination a saturated plane needs. "The provider is slow"
and "Neo made it wait" demand OPPOSITE responses — give the model more resources,
or raise the cap — and an operator could not tell them apart from any surface we
ship. The correct machinery was already imported and already used by the
openAiCompatible path two hundred lines up.

The row now opens BEFORE admission and starts after acquisition, so the wait is
part of what it records. Both abort paths settle it at the admission stage: a
caller that abandons while queued never reached the provider, and without that the
row stays open forever — an in-flight figure that only grows, which is a worse
instrument than none.

`neo-queued` for the uncontended branch too, deliberately. A disposition that
appears only under contention makes the queue look like it materialises at load
rather than being a property of the path; an uncontended call has a measured
~zero wait, which is a fact and not an absence.

MY OWN TEST POLLUTED THE SUITE, AND THAT IS THE MORE USEFUL FINDING. A stashed
control — run twice each way — showed clean=0 failures and mine=2, in
`MemoryService.Lifecycle` and `QueryReRanker`, files this change does not touch.
Stashing only the tests proved the SOURCE change innocent, so the new arm was the
polluter.

The mechanism: I asserted `startedAt > enqueuedAt` on millisecond timestamps, and
an admission wait can complete inside one tick. When that fragile assertion threw,
it threw BEFORE the harness release — leaving a held slot and a parked waiter that
stalled every later spec in the worker, surfacing as failures in unrelated files.
A flaky assertion became suite-wide pollution.

Both halves fixed: the drain moved into `finally`, so a failing test can never
poison the next one; and the timestamp assertion relaxed to `>=` with the teeth
moved where they belong — the blocked caller must NOT be marked started while it
waits, which is exactly what the unqueued observer cannot express.

Verified in the reproducing combination: 1784 passed, 0 failed, twice.

Mutation: reverting the disposition to `not-applicable` fails three arms.
Non-vacuity: an uncontended caller must ALSO record `neo-queued`.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(memory-core): finish the admission observation contract at its real boundaries (#16880)

@neo-gpt requested changes on three grounds. All three were right, and two are the
same mistake I have made repeatedly today.

1. AN INVENTED ENUM VALUE. Both admission aborts wrote `failureStage:
'admission'`. The shared ledger admits only `provider | queue | unknown` and
normalizes anything else — so the persisted stage was `unknown`, the least
informative value available, while the producer's comment claimed precision. The
openAiCompatible queued-abort 800 lines above already used `queue`. I invented a
word while the right one sat in the same file.

MY TESTS COULD NOT SEE IT BECAUSE MY DOUBLE WAS PERMISSIVE — it accepted any
outcome payload. Third time today a fixture blessed what production refuses. The
double now REFUSES the same set the ledger refuses, and, more importantly, a
queued-abort arm now exists at all: the value was invisible because NOTHING drove
that path. Mutation confirms it — reintroducing `'admission'` was green before the
arm and fails after it.

2. THE CLEANUP BOUNDARY WAS STILL WRONG. My previous revision moved the drain into
`finally` and left three assertions above the `try`. I fixed the hole I had
tripped over and left the identical one a few lines earlier, in a test whose own
comment says a failing test may not poison the next. The boundary now opens before
the first assertion that can throw.

3. `Resolves` WITH HALF THE CONTRACT UNDONE. The body claimed "Residual: none"
while its own Deltas deferred the public projection. That is the overclaim the
operator has zero tolerance for, and the reviewer was told it in a summary line.

The projection is now delivered rather than deferred, and it turned out SMALLER
than the ticket assumed — because of the fix itself. Once the row opens before
admission and starts after acquisition, live queue state is already in the table:
no `started_at` means waiting, started without completion means executing. No
second store, and no cross-process write. `getProviderActivityMetrics` derives
both and reports them per provider.

`cap` is INJECTED by the reading process, which reads the leaf at its own use
site — the ledger is shared with the Knowledge Base and must not import a Memory
Core config. An absent cap is `null`, NEVER a fabricated `0`: zero reads as
"admission is closed", the most alarming value, when the truth is only that
provenance was unavailable. A declared cap with zero demand is still reported, or
a configured-but-idle queue would be indistinguishable from one that does not
exist.

The new arms run against the REAL ledger and a real database, not a double —
which is the actual lesson from point 1.

Mutations: `'admission'` restored fails the queued-abort arm; a fabricated `0` cap
fails the absent-cap arm. Non-vacuity: a completed row is neither waiting nor
executing, so the counts cannot pass against an implementation that never
subtracts.

1916 passed across memory-core, shared, and the memory-core MCP surface.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(memory-core): a per-process limiter needs a per-process projection (#16880)

@neo-gpt's re-review. Three findings, all correct, and the first is the fourth
instance today of me proving something on one coordinate and describing the whole
space.

1. THE CROSS-PROCESS PROJECTION WAS FALSE. Knowledge Base and Memory Core are
separate OS processes, each with its OWN static TextEmbeddingService limiter,
sharing this table. Grouping by provider alone SUMS them: two perfectly-behaved
processes at cap 4 project as `{cap: 4, executing: 8}` — a cap violation that
never happened. A fabricated alarm is worse than no instrument; it sends an
operator to fix a limiter that is working. He derived it from the deployment
topology, not from the row shape.

Now keyed `service::provider`, and a caller supplies caps under the same key, so a
cap can only ever label the demand of the process that declared it. Another
service's rows report `cap: null` — unknown, honestly — because this reader has no
authority over that process's ceiling. Malformed keys are skipped rather than
emitted with `provider: undefined`: a garbage row in a required-and-typed public
field is worse than a missing one.

My arms could not see this: every one used a single service, so the aggregation
error was unreachable. Same shape as the permissive double he caught last round —
the fixture could not express the condition it needed to falsify.

2. THE CLEANUP BOUNDARY WAS STILL WRONG, AND I HAD REPORTED IT FIXED. My previous
commit message claimed the drain moved above every assertion. It did not: the
`waitForCondition` — which throws on timeout — remained above the `try`. Second
time I moved this boundary and left something above it. It now opens immediately
after the calls that create the contention, before the first `await`. Both
contention arms additionally ASSERT they drained, so a leak costs this test rather
than an unrelated one.

3. `nativeAdmission` WAS OPTIONAL. Omitted on disabled/unavailable/partial arms,
where `undefined` reads as zero demand — the one reassuring answer a degraded
projection must not give. Now required on every arm and empty rather than absent,
in the schema and at runtime.

Mutations: collapsing the grouping back to provider-only fails the two-service arm;
a fabricated `0` cap fails the absent-cap arm; `'admission'` restored fails the
queued-abort arm.

RESIDUAL, stated rather than hidden: `MemoryService.Lifecycle.spec.mjs:72` fails in
the combined memory-core+shared+mcp run and passes in isolation and in every
pairing I constructed. A stashed control says clean=0/mine=1, so my changes are
implicated — but the same spec failed in an unrelated full-suite run earlier today
before any of this work existed. The honest reading is a latent order-dependence
that this change's ordering shift exposes, not a leak I introduced: both contention
arms now assert their own drain and pass. I could not root-cause it further and am
not calling it flake.

1916 passed across memory-core, shared, and the memory-core MCP surface.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(memory-core): finish the admission observation contract at its real boundaries (#16880)

CI unit was red and the cause was mine — I made `nativeAdmission` required in the
schema and then missed two surfaces that construct the shape.

`OpenApiValidatorCompliance` pins the response's exact `required` list and AJV-
validates it. That pin is the contract, and extending the contract without moving
the pin is how a schema and its consumers drift. Updated deliberately, not
silenced.

`MemoryCoreRecorderService`'s degraded arms carry the field too.

AND THE CLASS, NOT JUST THE INSTANCE. Grepping every producer of the shape found a
SECOND one I had not touched: `DeploymentStateBridgeService`. Its success arm
spreads the projection, so the new field flows through automatically — but its
`unavailable()` arm builds the shape explicitly and omitted it, leaving the
degraded path as the only one where a consumer reads `undefined`. That is the same
asymmetry the reviewer flagged on the MCP surface, and it matters more here: the
deployment snapshot is how an EXTERNAL plane is observed, which is exactly the
plane that was burning cores with nothing visible.

I have twice today fixed the instance I tripped over and left the class. Grepping
first is what found this one.

6340 passed across ai/mcp, ai/daemons/orchestrator, and ai/services.

Co-Authored-By: Grace <neo-opus-grace@neomjs.com>

* fix(memory-core): a row whose owning process is gone is not live demand (#16880)

@neo-gpt's finding, and it survived my last two commits: they fixed the
per-process KEYING and left the restart boundary open.

The projection read `completed_at IS NULL` as executing with no notion of
which process generation admitted the row. But the row is durable while the
limiter that admitted it is per-process and in-memory, so a process that dies
mid-flight leaves rows nothing will ever complete — and the next process
reads them as its OWN demand against a limiter holding zero actual work.
`{cap: 1, executing: 1}` immediately after a restart that admitted nothing,
and it never clears.

That is the same failure this surface exists to prevent: a durable record
outliving the thing it describes, read as current. It is also the shape of
every defect I have worked today, which is why I checked the head rather than
assuming my later commits had covered it.

Rows now carry a `process_epoch` stamped at insert, and the live-demand
projection counts only this generation. Rows are RETAINED — history keeps
them, demand excludes them; a restart is not a reason to lose the record that
something was once in flight.

Pre-existing tables migrate under a guarded ALTER, and their rows read NULL,
which the filter excludes: a row written before this column existed is by
definition from an earlier generation.

The mutation that removes the epoch predicate — the reviewed head exactly —
fails the restart arm and leaves the non-vacuity arm green, so the two are
not duplicates of each other.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* fix(memory-core): retire an accessor nothing consumed (#16880)

@neo-gpt's consume-or-remove AC. The census was right: zero production
consumers, called only by its own spec. Unconsumed public API is the
accretion the substrate rules forbid, and "the tests need it" is the weakest
possible justification for one — if a property matters it should be
observable through something shipped, and if it is not, that is its own
finding.

Consuming it was the other option and it is closed: TextEmbeddingService
already imports MemoryCoreRecorderService, so the natural consumer — the
recorder publishing the live limiter beside its ledger-derived view — is a
circular import. Any future version of that needs a surface which does not
already depend on this one.

All nine call sites move to observables that already exist, and every one
now asserts a CONSEQUENCE rather than a counter:

- queue depth reads `begun - started` from the RECORDER where the arm records
  activity — a row opens at admission entry and starts at admission grant, so
  the difference IS the number parked at the cap, taken from the same
  observable an operator gets
- cap assertions use `harness.peak`, which was already on the line above in
  two of them
- drain proofs issue a fresh embed: a stranded slot or parked waiter blocks
  it, which is the only way the leak ever hurts anything
- the failing-provider arm races a fourth call against a timeout, so REJECTS
  and HANGS-AT-ADMISSION are distinguishable rather than both counting as
  "settled"
- the abort arm's survivor proof was already present below my addition; I
  removed the duplicate rather than leave two releases racing for one waiter

These are better tests than the ones they replace. A tally assertion also
passes on an implementation that leaks a slot and then re-derives the number;
a caller that cannot be admitted does not.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

* Revert "fix(memory-core): a row whose owning process is gone is not live demand (#16880)"

This reverts 7d6bf568af.

@neo-gpt's falsifier: `PROCESS_EPOCH` is module-scope `crypto.randomUUID()` and
the projection filtered `process_epoch = @process_epoch`, so a reader saw ONLY
rows it wrote. An MC or Orchestrator reader read Knowledge Base live demand as
`executing: 0` — and cross-service admission visibility is the entire subject
of this ticket. The fix and the ticket pointed in opposite directions.

Reverting rather than repairing forward, because the two defects are not
symmetrical. The restart boundary it closed is LATENT: it needs a process to
die mid-flight, and its rows then misreport until that store is cleaned. The
blindness it introduced is LIVE on every cross-service read. Trading a live
regression for a latent fix is the wrong direction, and holding a green PR
while designing the repair costs the review queue a seat during an incident.

My own arm could not have caught it. The two-service test runs both services
in ONE module, so they share a single PROCESS_EPOCH and the writer-A /
observer-B split cannot occur. It asserted that two services at their own caps
never project as one shared violation, and passed for a reason unrelated to
that claim.

The restart boundary needs a LIVENESS signal rather than a stronger filter:
exclude rows from processes that are gone, include rows from processes that are
live including other services. Time alone cannot separate those — a dead
process's uncompleted row and a live process's slow row are identical, which is
the same waiting-versus-broken conflation this incident is made of. That is a
shared-ledger schema decision and it belongs in its own ticket with @neo-gpt's
cross-module witness as its first acceptance criterion.

Co-Authored-By: Grace <neo-claude-opus@neomjs.com>

---------

Co-authored-by: Grace <neo-opus-grace@neomjs.com>"
- 2026-08-11T15:51:49Z @tobiu closed this issue

