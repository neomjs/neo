---
id: 15112
title: 'Floating aligned elements: RA-9 position:fixed cascade + RA-10 null target-offsetParent observer guards'
state: CLOSED
labels:
  - bug
assignees:
  - neo-opus-grace
createdAt: '2026-07-12T21:57:58Z'
updatedAt: '2026-07-12T23:06:30Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15112'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-12T23:06:30Z'
---
# Floating aligned elements: RA-9 position:fixed cascade + RA-10 null target-offsetParent observer guards

Two placement-independent framework root-fixes for the alignment / floating-element subsystem, generalized out of the tab-overflow work (parent #14771, closed) so they land on their own authority. Both are generic — any floating aligned element, not just the tab-overflow control.

## RA-9 — floating-button `position: fixed` cascade
`resources/scss/src/button/Base.scss`. A floating button carries both `.neo-floating {position: fixed}` (component/Base) and `.neo-button {position: relative}` (button/Base). Equal single-class specificity → `.neo-button` wins on source order → the floating button resolves to `position: relative`, pinning it to its `offsetParent` instead of the viewport, so `alignTo` lands it at the wrong coordinates. Fix: the compound `.neo-button.neo-floating {position: fixed}` (higher specificity) restores fixed positioning. Generic to any floating button.

## RA-10 — null **target**-offsetParent observer guards
`src/main/DomAccess.mjs`. `align()` stores `data.offsetParent = data.targetElement.offsetParent` — the alignment **target's** layout parent — and `addAligned` / `removeAligned` observe/unobserve it via the align `ResizeObserver`. When the target's `offsetParent` is **null** — a `position: fixed` target (e.g. the floating overflow button once RA-9 pins it, when its own menu aligns *to* it) or a body/root target — `observe(null)` / `unobserve(null)` throws *"parameter 1 is not of type 'Element'"*. Fix: null-guard both call sites.

Precise object model (the correction that motivated this leaf): the observed field belongs to the **target**, not the aligned subject. The subject's positioning (RA-9) and the target's null `offsetParent` (RA-10) are separate facts that co-occur when a menu aligns to a now-fixed button.

## Evidence
Both fixes were exercised by the tab-overflow owner-exact-geometry L3 journey (passing on #15062's branch). The durable replay re-homes to #15099 (dense-workstation scene). No unit coverage: RA-9 is a CSS cascade (not unit-testable) and RA-10's guard is a main-thread DOM path — the e2e is the test-of-record.

## Acceptance
- [ ] A floating `.neo-button.neo-floating` resolves to `position: fixed` (RA-9).
- [ ] Aligning to a target whose `offsetParent` is null does not throw (RA-10).
- [ ] No regression in existing align behavior (owner-geometry L3 stays green when it replays under #15099).

Delivered by PR #15109. Parent #14771 (closed). Related: #15098 (tab-native successor, non-closing), #15099 (durable e2e replay).

## Timeline

- 2026-07-12T21:57:58Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-07-12T21:58:00Z @neo-opus-grace added the `bug` label
- 2026-07-12T22:01:53Z @neo-opus-grace referenced in commit `bd701c5` - "fix(main): correct RA-10 comment causal model — the TARGET's offsetParent, not the subject's (#15112)

Per Euclid's #15109 review: align() stores data.offsetParent = data.targetElement.offsetParent, so the
observed/guarded field is the alignment TARGET's layout parent (null when the target is position:fixed or
body/root), NOT the aligned subject's. The two DomAccess null-guard comments attributed the nullability to
the fixed subject — corrected to the target. Guard code unchanged; it was already correct.

Co-Authored-By: Claude Opus 4.8 <opus@anthropic.com>"
- 2026-07-12T22:01:55Z @neo-opus-grace cross-referenced by PR #15109
- 2026-07-12T22:01:56Z @neo-opus-grace cross-referenced by PR #15108
- 2026-07-12T23:06:30Z @tobiu closed this issue
- 2026-07-12T23:06:30Z @tobiu referenced in commit `ca98cf4` - "fix(button/main): floating-button position:fixed cascade + DomAccess null-offsetParent guards (#14771) (#15109)

* fix(button): floating buttons keep position:fixed via .neo-button.neo-floating (#14771)

.neo-floating sets position:fixed (component/Base.scss) but .neo-button sets
position:relative — equal single-class specificity, and .neo-button wins on
source order, so a floating button is pinned to its offsetParent instead of the
viewport and alignTo's computed left/top land it at the wrong coordinates
(cycle-4 RA-9: fresh-port capture showed the overflow control at y~310 while the
toolbars sit at y~64..202). A higher-specificity .neo-button.neo-floating
compound selector restores position:fixed. Framework correction proven on
@neo-gpt's fresh-port harness (strengthened journey 1/1).

Co-Authored-By: Claude Opus 4.8 <opus@anthropic.com>

* fix(main): null-guard align offsetParent observe/unobserve for fixed subjects (#14771)

RA-10 root fix carved out of #15062 to land independently of the tab-overflow
placement. A fixed-position aligned subject has a null offsetParent, so
DomAccess.addAligned/removeAligned must null-guard the ResizeObserver
observe/unobserve calls. Generic — applies to any fixed aligned element.

Co-Authored-By: Claude Opus 4.8 <opus@anthropic.com>

* fix(main): correct RA-10 comment causal model — the TARGET's offsetParent, not the subject's (#15112)

Per Euclid's #15109 review: align() stores data.offsetParent = data.targetElement.offsetParent, so the
observed/guarded field is the alignment TARGET's layout parent (null when the target is position:fixed or
body/root), NOT the aligned subject's. The two DomAccess null-guard comments attributed the nullability to
the fixed subject — corrected to the target. Guard code unchanged; it was already correct.

Co-Authored-By: Claude Opus 4.8 <opus@anthropic.com>

---------

Co-authored-by: Claude Opus 4.8 <opus@anthropic.com>"

