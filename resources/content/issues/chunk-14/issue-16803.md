---
id: 16803
title: 'Tour-reset geometry admission must carry the reconciler outcome, not the request'
state: CLOSED
labels:
  - bug
  - ai
  - refactoring
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-08-09T15:26:14Z'
updatedAt: '2026-08-09T15:43:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16803'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-09T15:43:53Z'
---
# Tour-reset geometry admission must carry the reconciler outcome, not the request

## Context

`#16412` asks the two tour-reset call sites to stop declaring an unverified `geometryOnly: true`. Implementing it surfaced that the call site was never the right place to fix, and the ticket's prescription — derive the flag from a document compare — cannot be made correct. This leaf carries the shape that replaced it, so `#16412` can keep its unmet recorded-tour criterion open rather than being closed by a partial delivery.

## The Problem

A derived predicate at the call site duplicates a rule the consumer already owns, and duplicated it **more weakly**. Measured at `e4082b2723`: a four-category `DockTopologyDiff` test returns `true` for active-tab-only, split-orientation-only, structural child-order-only and auto-hide-only resets, every one of which `DockProjectionReconciler.reconcileStableTopology` refuses from its in-place path. A predicate that is weaker than its consumer's authority is worse than the literal it replaced, because it looks proven.

Strengthening the predicate does not fix the class — it reproduces it one axis later, and drifts again the next time the reconciler's admission rule changes.

## The Architectural Reality

- `apps/workstation/view/Workspace.mjs` — `refreshDockWorkspace({geometryOnly})` forwards the flag both into `DockProjectionReconciler.reconcileProjection` and, afterwards, into `DockFlip.play`.
- `src/dashboard/DockProjectionReconciler.mjs` — `geometryOnly` only gates whether `reconcileStableTopology` is **attempted**; it returns `null` on any node/type/ancestry/order/orientation delta and falls through to the staged transaction. Passing the flag here is safe by construction.
- `src/main/addon/DockFlip.mjs` — `play({geometryOnly})` declares something strictly stronger: *"no topology swap can be pending"*.

One flag serves two different contracts. The reconciler reads it as a **request**; DockFlip reads it as a **fact**. Only the second can be wrong, and only after the reconciler has already decided.

## The Fix

Report the path actually taken and forward that instead of the request:

- `reconcileProjection` returns `landedInPlace` at both exits — `true` from the in-place branch, `false` from the staged transaction.
- `Workspace.refreshDockWorkspace` destructures it and passes it to `DockFlip.play`.
- The call sites go back to requesting `geometryOnly: true`, which is now honestly an admission request.
- Any call-site predicate is deleted rather than strengthened.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `DockProjectionReconciler.reconcileProjection` return | `src/dashboard/DockProjectionReconciler.mjs` | Adds `landedInPlace` to both return shapes; all existing fields unchanged | Purely additive — existing destructuring consumers are unaffected | JSDoc at both returns | dashboard + workstation suites |
| `Workspace.refreshDockWorkspace` `options.geometryOnly` | `apps/workstation/view/Workspace.mjs` | Documented as an admission REQUEST, never a stable-topology claim | `operation === 'resizeSplit'` derivation untouched | JSDoc at the option | seam tests below |
| `DockFlip.play` `options.geometryOnly` | `src/main/addon/DockFlip.mjs` | Receives the reconciler's outcome; contract wording unchanged | Existing geometry guards remain the second line | play() JSDoc | seam tests below |

## Decision Record impact

none

## Acceptance Criteria

- [ ] `reconcileProjection` reports `landedInPlace` from both the in-place and staged exits
- [ ] `DockFlip.play` receives that outcome; the caller's request never reaches it
- [ ] A staged fallback is not reported as geometry-only even when geometry-only was requested
- [ ] A proven in-place landing keeps its admission, so the change is not the trivially safe always-false
- [ ] Both seam tests fail if `DockFlip` is never reached, rather than passing on a null capture
- [ ] No call-site topology predicate remains in `Workspace`
- [ ] `resizeSplit` derivation behaviour is unchanged
- [ ] Dashboard and workstation suites green

## Out of Scope

- The recorded-tour reset receipt (`#16412` AC4) — an e2e/on-camera artifact this seat cannot capture; it stays open on `#16412`
- `reconcileStableTopology`'s own admission rule, which is the authority this change defers to rather than modifies
- The `#16406` tab-drag defect

## Avoided Traps

- **Strengthening the predicate.** An exact topology projection covering identity/type/ancestry/order/orientation plus active and auto-hide state would pass today's counterexamples and still be a second copy of someone else's rule. The duplication is the defect, not its current precision.
- **Editing the failing call-site test green.** The assertion expecting `geometryOnly: false` at the call site is removed because that contract moved downstream; keeping it would pin the superseded design.

## Related

Refs #16412 · `#16403` (made the flag load-bearing) · `#16391` (the landed-in-place discriminator) · `#14650` (the `dockZone.v1` diff vocabulary the rejected approach would have used)

Live latest-open sweep: checked latest 20 open issues at 2026-08-09T15:25:18Z; no equivalent found. A2A in-flight sweep: no claim on this scope.

Retrieval Hint: "tour reset geometryOnly landedInPlace reconciler outcome not request DockFlip admission"


## Timeline

- 2026-08-09T15:26:14Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-09T15:26:15Z @neo-opus-ada added the `bug` label
- 2026-08-09T15:26:15Z @neo-opus-ada added the `ai` label
- 2026-08-09T15:26:15Z @neo-opus-ada added the `refactoring` label
- 2026-08-09T15:26:15Z @neo-opus-ada added the `architecture` label
- 2026-08-09T15:27:39Z @neo-opus-ada cross-referenced by PR #16788
- 2026-08-09T15:28:03Z @neo-opus-ada cross-referenced by #16412
- 2026-08-09T15:43:53Z @tobiu referenced in commit `04d15fc` - "fix(dashboard): DockFlip is told the reconciler outcome, not the request (#16803) (#16788)

* fix(workstation): derive the tour-reset geometryOnly flag instead of asserting it (#16412)

Both tour-reset call sites reset the dock model to the initial document and
then declared `geometryOnly: true` over what can be a topology change. A
reset to the initial document is only geometry-only while the live layout
still shares the initial topology; after a tear-out, split or convert it is
a node-replacing change wearing a stable-topology declaration.

Since #16403 that flag is forwarded into DockFlip.play, whose contract reads
"no topology swap can be pending", so the declaration became load-bearing.
The existing guards absorbed a false one only by accident of their own
strictness, never by contract.

Both sites now derive the flag from a topology compare of the document being
replaced against the one being reset into place, via the dockZone.v1
vocabulary that already exists in DockTopologyDiff. The declaration becomes a
proof. The resizeSplit derivation is untouched.

Topology is the four structural categories; resizes and autoHideFlips are
excluded deliberately, because moving or hiding a node without replacing one
is exactly the case the flag exists to admit.

Fail-closed on diff errors, which is the trap in this shape: diffDockDocuments
gates each input through a shape walk and returns EMPTY categories alongside
`errors` for an unparseable document, so a categories-only test would read
"topology-stable" for a document it could not parse and hand the fast path to
the input least deserving of it.

One existing spec asserted the entry projection receives geometryOnly:true
while feeding a `{nodes: {marker: 'live'}}` stub. With a derived flag that
stub fails the shape gate, so the assertion would have become a statement
about an unparseable fixture. The fixture is now a real same-topology
document, which keeps the test asserting the invariant its name claims, and a
paired case pins the divergent half at the same call site.

* fix(dashboard): report the reconciler's actual path so DockFlip stops being told the request (#16412)

@neo-gpt's counterexamples are confirmed: the previous derivation returned
true for active-tab-only, split-orientation-only, structural child-order-only
and auto-hide-only resets, all of which reconcileStableTopology refuses. A
predicate that duplicates a weaker rule than its consumer's authority is
worse than the literal it replaced, because it looks proven.

Reading the seam changed the fix. geometryOnly gates whether the in-place
path is ATTEMPTED, and that attempt returns null on any structural delta and
falls through to the staged transaction, so passing it to the RECONCILER is
safe by construction. The defect is that the same flag was then forwarded to
DockFlip.play, whose contract reads "no topology swap can be pending". One
flag serving two different contracts.

reconcileProjection now reports landedInPlace at both return points, and
Workspace forwards that outcome to DockFlip instead of the request. The
predicate is DELETED along with its DockTopologyDiff import: there is no
longer anything to duplicate, and the four counterexamples become unreachable
at the source rather than caught by a stronger guard.

Tests move to the seam accordingly. The call-site assertion expecting
geometryOnly false is removed rather than edited green, because that contract
genuinely moved downstream; two new cases pin it where it now lives — a
staged fallback is never reported as geometry-only even when requested, and a
proven in-place landing keeps its admission so the fix cannot be the
trivially safe always-false. Both carry a non-null capture guard so a
DockFlip that was never reached fails instead of passing silently.

569 passed across the dashboard and workstation suites."
- 2026-08-09T15:43:53Z @tobiu closed this issue

