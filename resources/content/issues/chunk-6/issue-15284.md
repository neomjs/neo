---
id: 15284
title: 'FM cockpit operable-cold banner — the surface names WHY it shows sample data (W2 #14560)'
state: CLOSED
labels:
  - enhancement
  - design
  - ai
assignees:
  - neo-fable
createdAt: '2026-07-16T16:49:11Z'
updatedAt: '2026-07-16T18:39:26Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15284'
author: neo-fable
commentsCount: 1
parentIssue: 14560
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-16T18:39:26Z'
---
# FM cockpit operable-cold banner — the surface names WHY it shows sample data (W2 #14560)

## Context

W2 operable-cold UI leaf under #14560, the Fleet Manager cockpit design epic. The Brain-side fleet service spine and bridge remain under #13015; this ticket renders the truth those surfaces expose.

PR #15290's cycle-1 review falsified the original ongoing-liveness premise: `loadRoster` / `loadActivity` run at construction (plus roster refreshes after successful lifecycle intents), and failure exits preserve last-known owner state. This ticket is therefore narrowed to the honest **initial-cold** slice. The required live→loss→recovery owner, retained reason, timer lifecycle, and Neural Link journey move non-optionally to #15293.

## The Problem

On a fresh cockpit boot without a reachable fleet transport, the roster and activity surfaces correctly retain their sample data, but the shell does not explain why. The operator sees sample residents and unavailable controls without a cause or an operable remedy.

## Intended Solution

- Derive one shell-level banner from the existing owner-held `gridAdapterState` / `streamAdapterState` values. `sample` (cold) takes precedence over `stale` (degraded), which takes precedence over `live`.
- Cold names the cause and the already-shipped remedy: `npm run ai:fleet-server`.
- Degraded names the last-known-data condition. Fully live renders nothing: nominal earns zero pixels.
- The banner is a render-only consumer. This leaf adds no probe, poller, transport, or new truth owner.

## Acceptance Criteria

- [ ] Pure derivation covers the full `sample|stale|live` × `sample|stale|live` matrix; only `live × live` hides the banner.
- [ ] One shell-level `role="status"` slot renders the initial-cold cause + shipped remedy and the degraded last-known state.
- [ ] A real recording-slot unit witness proves `cls` / `hidden` / `html` mutation; a missing slot is a guarded no-op.
- [ ] No new probes/transports and fail-closed data behavior remains unchanged.
- [ ] Both themes remain coherent and an equal maintainer's design signoff is recorded on PR #15290; named-reviewer rate limits are not a queue lock.

## Contract Ledger

| Surface | Truth source | Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `deriveSpineBanner` | owner-held adapter states | pure precedence map → `{hidden, kind, text}` | n/a | module JSDoc | 3×3 matrix spec |
| Banner slot | toolbar reference `fleet-spine-banner` | writes `cls` / `hidden` / `html` after each current load exit | missing reference → guarded no-op | `syncSpineBanner` JSDoc | real recording-slot witness |
| Cold remedy | shipped npm surface | names `npm run ai:fleet-server` | n/a | copy in the rendered status | copy spec |
| Live loss/recovery | #15293 owner | advances adapter states and retained reason without reload | fail-closed data preserved | #15293 | owner-level unit + NL journey |

## Out of Scope

- Ongoing transport-loss/recovery ownership, retained degraded reason, timer lifecycle, and the live→loss→recovery Neural Link journey: #15293 (required successor).
- The boot/composed-launch seam itself: #15283.
- Per-agent wake/throttle truth: #15273.

## Related

Parent: #14560 · service-spine sibling: #13015 · implementation: PR #15290 · required successor: #15293 · design record: D#15249.

Origin Session ID: 5bbd6fb6-07d8-4f7e-b3ba-cccb66eddcf3
Retrieval Hint: "cockpit operable-cold banner fleet server offline sample data honest chrome"


## Timeline

- 2026-07-16T16:49:12Z @neo-fable added the `enhancement` label
- 2026-07-16T16:49:13Z @neo-fable added the `design` label
- 2026-07-16T16:49:13Z @neo-fable added the `ai` label
- 2026-07-16T16:49:21Z @neo-fable added parent issue #13015
- 2026-07-16T16:57:15Z @neo-fable cross-referenced by PR #15288
- 2026-07-16T16:59:05Z @neo-fable assigned to @neo-fable
- 2026-07-16T17:09:47Z @neo-fable cross-referenced by PR #15290
- 2026-07-16T17:53:34Z @neo-fable cross-referenced by #15293
### @neo-fable - 2026-07-16T17:53:58Z

## Scope narrow + Contract Ledger (cycle-1 truth-fold, PR #15290)

Emmy's cycle-1 review falsified this ticket's central premise: **the loads do not re-poll** — they run at construction (+ roster refresh after successful lifecycle intents only), and failure exits fail-closed PRESERVE last-known owner states. So the live→loss→recovery transition this ticket's AC-2/AC-4 assumed has no owner in current source, and 'no new probes' (binding here) means this leaf cannot build one.

**Narrowed scope (this ticket, closed by PR #15290):** the initial-cold slice —
- the pure `deriveSpineBanner` reducer (sample>stale>live precedence, nominal earns zero pixels),
- the shell banner slot + `syncSpineBanner` consumer (real slot-mutation witnessed, not just guarded no-op),
- cold copy naming cause + the SHIPPED remedy (`npm run ai:fleet-server` at this head),
- both-theme design signoff (Grace) recorded on the PR.

**Moved out, non-optionally → #15293 (the liveness owner):** loss/recovery adapter-state transitions without reload, retained degraded reason, the NL live→loss→recovery journey. #15293 explicitly owns the probe this ticket was forbidden.

## Contract Ledger (narrowed leaf)

| Surface | Truth source | Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `deriveSpineBanner` | owner-held `gridAdapterState`/`streamAdapterState` | pure precedence map → {hidden, kind, text} | n/a (pure) | module JSDoc | 3×3 matrix spec |
| Banner slot | toolbar shell slot `fleet-spine-banner` | `syncSpineBanner` writes cls/hidden/html on every load exit | null reference → guarded no-op | JSDoc | real slot-mutation witness (recording stub) |
| Cold remedy copy | shipped npm surface | names `npm run ai:fleet-server` (exists on dev at this head; stays the mid-session restart remedy post-#15288) | n/a | `learn/agentos/RunningTheFleetCockpit.md` §without-transport (via #15288) | copy spec |
| Liveness transitions | — | NOT this leaf | — | — | → #15293 |

— Mnemosyne (@neo-fable, Claude Fable 5). Session 5bbd6fb6-07d8-4f7e-b3ba-cccb66eddcf3.

- 2026-07-16T17:56:43Z @neo-fable referenced in commit `416244a` - "fix(agentos): spine banner cycle-1 truth-fold — shipped remedy, honest liveness boundary, real slot witness (#15284)

The cold copy now names the remedy that exists at this head (npm run
ai:fleet-server). The syncSpineBanner JSDoc states the boundary instead of
overshooting it: a call is not a truth transition — loads run at construction
plus after successful lifecycle intents, and failure exits fail-closed
PRESERVE last-known owner states, so the loss/recovery transition belongs to
the dedicated liveness owner (successor ticket). The consumer seam gains a
REAL recording-slot witness (cold/degraded/live writes + guarded no-op), and
the owner-truth immobility boundary is pinned as a spec: once live, a thrown
load preserves live and the slot stays hidden."
- 2026-07-16T18:24:41Z @neo-gpt changed title from **FM cockpit operable-cold banner — the surface names WHY it shows sample data (W2 census leaf 2, #13015)** to **FM cockpit operable-cold banner — the surface names WHY it shows sample data (W2 #14560)**
- 2026-07-16T18:24:49Z @neo-gpt removed parent issue #13015
- 2026-07-16T18:24:50Z @neo-gpt added parent issue #14560
- 2026-07-16T18:39:26Z @tobiu referenced in commit `76f9054` - "feat(agentos): the spine banner — the cockpit names WHY it shows sample or last-known data (#15284) (#15290)

* feat(agentos): the spine banner — the cockpit names WHY it shows sample or last-known data (#15284)

The fail-silent half of 'FM is not functional' closed: the adapter layer always knew
why the surface degraded (sample/stale/live states), but no surface said it.

- spineBanner.mjs: the pure per-SPINE derivation (sibling of the other pure fleet
  modules) — sample (cold) beats stale (degraded) beats live; cold names the cause
  AND the one-command remedy (npm run cockpit); a fully live spine renders NOTHING
  (zero nominal pixels, the exception-based discipline applied to chrome).
- One toolbar slot (fleet-spine-banner, role=status for polite announcement), synced
  from EVERY loadRoster/loadActivity exit — including the no-bridge guards, because
  absence IS the cold truth. Render-only over existing state: the routing matrices
  stay the sole writers; no new probes.
- Witnesses: the full 3x3 derivation matrix + cause/remedy/zero-pixel pins (5 specs);
  the three load-routing fakes gained the REAL sync method (their null getReference
  makes it a guarded no-op — production code, no stub drift).

Fleet battery: 180/180 exit-checked; two-window e2e re-run green.

* fix(agentos): spine banner cycle-1 truth-fold — shipped remedy, honest liveness boundary, real slot witness (#15284)

The cold copy now names the remedy that exists at this head (npm run
ai:fleet-server). The syncSpineBanner JSDoc states the boundary instead of
overshooting it: a call is not a truth transition — loads run at construction
plus after successful lifecycle intents, and failure exits fail-closed
PRESERVE last-known owner states, so the loss/recovery transition belongs to
the dedicated liveness owner (successor ticket). The consumer seam gains a
REAL recording-slot witness (cold/degraded/live writes + guarded no-op), and
the owner-truth immobility boundary is pinned as a spec: once live, a thrown
load preserves live and the slot stays hidden."
- 2026-07-16T18:39:26Z @tobiu closed this issue

