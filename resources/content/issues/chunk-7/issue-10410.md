---
id: 10410
title: 'Wake Substrate post-#10404 hardening: idempotency + duplicate deduplication'
state: CLOSED
labels:
  - bug
assignees:
  - neo-opus-ada
createdAt: '2026-04-27T10:22:35Z'
updatedAt: '2026-06-07T17:22:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/10410'
author: neo-gemini-pro
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-04-27T11:36:49Z'
---
# Wake Substrate post-#10404 hardening: idempotency + duplicate deduplication

# Wake Substrate post-#10404 hardening: idempotency + duplicate deduplication

### Context
During the bidirectional smoke test of the wake substrate post-#10404 merge, Claude (`@neo-opus-ada`) empirically discovered two distinct bugs in the wake infrastructure related to idempotency and duplicate deduplication.

### Bug 1: Bootstrap Idempotency Miss
Calling `manage_wake_subscription({action: 'bootstrap'})` when a subscription already exists for the tuple `(agentIdentity, trigger, harnessTarget)` should return `status: 'existing'` and avoid creating a duplicate. Instead, the action created a duplicate sub (e.g., `WAKE_SUB:e5f96999` alongside existing `WAKE_SUB:ca08d381`) populated with the new template metadata.

**Possible causes:**
- The `db.nodes.items` iteration in `WakeSubscriptionService.mjs:bootstrap` failed to surface the old sub (potential cache miss).
- The `trigger`/`harnessTarget` shape on the old sub differs from the template post-load.
- Flawed tuple-matching logic.

### Bug 2: Duplicate-Wake Delivery
A single A2A message (`MESSAGE:626881c7`) generated **TWO** wake notifications for the receiving agent, both citing the exact same subscription ID (`Subscription: WAKE_SUB:ca08d381`).

**Possible causes:**
- The Bridge daemon poll-loop iterates `trace × sub` pairs without strict per-`(sub, trace)` deduplication before `osascript`/prompt injection.
- The Coalescing engine flushed twice within the 30s window.
- `add_message` created two `SENT_TO` edges for the same MESSAGE id.

### Proposed Solution
1. **Idempotency Fix:** Audit and patch `WakeSubscriptionService.mjs:bootstrap` to correctly identify existing subscriptions for the target tuple, preventing the generation of duplicate subscription nodes.
2. **Deduplication Fix:** Introduce a `(sub, trace)` deduplication map/set in `bridge-daemon.mjs` (or the coalescing logic) before invoking the hardware interrupt/wake script.

**Note:** The duplicate `WAKE_SUB:ca08d381` is currently active and intentionally being kept alive by Claude (`@neo-opus-ada`) for live-reproducible testing. Do not forcibly unsubscribe it until the fix is developed and validated against it.

## Timeline

- 2026-04-27T10:22:36Z @neo-gemini-pro added the `bug` label
- 2026-04-27T10:24:32Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-04-27T10:35:04Z @neo-opus-ada cross-referenced by PR #10412
- 2026-04-27T10:41:30Z @neo-opus-ada cross-referenced by PR #10411
- 2026-04-27T11:36:50Z @tobiu closed this issue
- 2026-04-27T11:36:50Z @tobiu referenced in commit `1b52925` - "fix(memory-core): bootstrap idempotency uses raw SQL (#10410) (#10412)

The in-memory cache iteration (db.nodes.items) silently missed WAKE_SUBSCRIPTION
nodes from prior MC instances post-restart — the cache is lazy-loaded and
cross-restart subs aren't pre-loaded unless explicitly accessed. Empirical
anchor: bootstrap created duplicate WAKE_SUB:e5f96999 alongside existing
WAKE_SUB:ca08d381 because the cache lookup didn't surface the cross-restart sub.

Fix: query SQLite directly via GraphService.db.storage.db.prepare() — same
approach as bridge-daemon's getActiveShapeCSubscriptions. Cache iteration
preserved as fallback for test environments without raw SQLite storage.

All 25 existing WakeSubscriptionService.spec.mjs tests pass.

Bug 2 from #10410 (duplicate-wake delivery citing same sub ID twice) is
deferred to follow-up — root cause needs more diagnostic data on whether
dual subs both fire vs same sub fires twice. Reproducer preserved.

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>"
- 2026-04-27T11:42:21Z @neo-opus-ada cross-referenced by #10419
- 2026-04-27T12:09:08Z @neo-opus-ada cross-referenced by PR #10423
- 2026-04-27T13:06:32Z @neo-opus-ada cross-referenced by PR #10425
- 2026-04-27T13:21:01Z @neo-opus-ada referenced in commit `f12d968` - "feat(bridge-daemon): persist diagnostic logs to disk (#10419)

Closes #10419. Diagnostic substrate that unblocks future wake-failure
investigations including Bug 2 of #10410 and the Leonard demo failure.

**The Problem:**
`bridge-daemon.mjs` previously emitted all diagnostic lines via
`console.log` / `console.error` only — terminal scrollback was the
sole audit trail. Multiple wake-failure investigations this session-arc
(Leonard demo failure, duplicate-wake reproductions, missed-wake
windows) hit the same diagnostic boundary: the daemon's log lines
existed momentarily then evaporated.

**The Fix:**
- New `writeLog(level, message)` helper writes to BOTH stdout (live
  terminal observability) AND `.neo-ai-data/wake-daemon/bridge.log`
  (persistent audit trail).
- Line format: `[ISO-timestamp] [PID:NNN] [LEVEL] message` —
  greppable, per-line correlation with daemon process + chronology.
- Daily rotation: previous-day file renamed to `bridge.log.YYYY-MM-DD`
  before appending today's first line. `rotateLogIfNewDay` runs on
  every `writeLog` call (cheap; just a stat + day-string compare).
- Startup pruning: archive files older than `LOG_RETENTION_DAYS` (30)
  are unlinked at daemon boot. Best-effort; per-file failures don't
  gate startup.
- All existing `console.log` / `console.error` / `console.warn` sites
  replaced with `writeLog('INFO'|'ERROR', ...)` calls. Internal
  console-write inside `writeLog` itself is preserved (it IS the
  console-write path).

**Failure semantics:**
Log writes are best-effort. File-write failures are silently swallowed
— daemon liveness MUST NOT depend on log integrity. Console-write
preserves the original behavior so terminal observability is
unchanged even when file substrate fails.

**Test evidence:**
- Existing `bridge-daemon.spec.mjs` test (which captures stdout
  delivery output) continues to pass — `writeLog` preserves the
  original console-write path.
- New assertions in the same test verify the persistent log file
  exists, contains the expected delivery line, and conforms to the
  documented format (ISO timestamp + PID + INFO/ERROR level
  prefix). All assertions pass.

**Compounds with #10423** (PID-lock, awaiting merge): once both
land, daemon-restart events get logged with timestamp + old-PID +
new-PID, making "wait, why did the daemon restart?" investigations
recoverable from disk audit alone.

**Out of scope:** SQLite-based logging (heavier substrate than
diagnostic use-case warrants); centralized agent-wide logger
(`logger.mjs` is for in-process Neo classes, not standalone scripts);
real-time alerting on log patterns (Phase-2 if substrate justifies)."
- 2026-04-27T13:23:18Z @neo-opus-ada referenced in commit `45a02c1` - "feat(bridge-daemon): persist diagnostic logs to disk (#10419)

Closes #10419. Diagnostic substrate that unblocks future wake-failure
investigations including Bug 2 of #10410 and the Leonard demo failure.

**The Problem:**
`bridge-daemon.mjs` previously emitted all diagnostic lines via
`console.log` / `console.error` only — terminal scrollback was the
sole audit trail. Multiple wake-failure investigations this session-arc
(Leonard demo failure, duplicate-wake reproductions, missed-wake
windows) hit the same diagnostic boundary: the daemon's log lines
existed momentarily then evaporated.

**The Fix:**
- New `writeLog(level, message)` helper writes to BOTH stdout (live
  terminal observability) AND `.neo-ai-data/wake-daemon/bridge.log`
  (persistent audit trail).
- Line format: `[ISO-timestamp] [PID:NNN] [LEVEL] message` —
  greppable, per-line correlation with daemon process + chronology.
- Daily rotation: previous-day file renamed to `bridge.log.YYYY-MM-DD`
  before appending today's first line. `rotateLogIfNewDay` runs on
  every `writeLog` call (cheap; just a stat + day-string compare).
- Startup pruning: archive files older than `LOG_RETENTION_DAYS` (30)
  are unlinked at daemon boot. Best-effort; per-file failures don't
  gate startup.
- All existing `console.log` / `console.error` / `console.warn` sites
  replaced with `writeLog('INFO'|'ERROR', ...)` calls, including the
  PID-lock singleton sites added in #10423 (PID transitions are now
  persisted alongside delivery events). Internal console-write inside
  `writeLog` itself is preserved (it IS the console-write path).

**Failure semantics:**
Log writes are best-effort. File-write failures are silently swallowed
— daemon liveness MUST NOT depend on log integrity. Console-write
preserves the original behavior so terminal observability is
unchanged even when file substrate fails.

**Test evidence:**
- Existing `bridge-daemon.spec.mjs` test (which captures stdout
  delivery output) continues to pass — `writeLog` preserves the
  original console-write path.
- New assertions in the same test verify the persistent log file
  exists, contains the expected delivery line, and conforms to the
  documented format (ISO timestamp + PID + INFO/ERROR level
  prefix). All assertions pass.

**Compounds with #10423** (PID-lock, merged): daemon-restart events
get logged with timestamp + old-PID + new-PID, making "wait, why did
the daemon restart?" investigations recoverable from disk audit alone.

**Out of scope:** SQLite-based logging (heavier substrate than
diagnostic use-case warrants); centralized agent-wide logger
(`logger.mjs` is for in-process Neo classes, not standalone scripts);
real-time alerting on log patterns (Phase-2 if substrate justifies).

Co-Authored-By: Claude Opus 4.7 (1M context) <claude@anthropic.com>"
- 2026-04-27T14:04:34Z @neo-gemini-pro cross-referenced by #10430
- 2026-04-27T14:14:33Z @neo-gemini-pro cross-referenced by PR #10431
- 2026-04-27T14:18:09Z @tobiu referenced in commit `4d6c703` - "feat(bridge-daemon): persist diagnostic logs to disk (#10419) (#10425)

Closes #10419. Diagnostic substrate that unblocks future wake-failure
investigations including Bug 2 of #10410 and the Leonard demo failure.

**The Problem:**
`bridge-daemon.mjs` previously emitted all diagnostic lines via
`console.log` / `console.error` only — terminal scrollback was the
sole audit trail. Multiple wake-failure investigations this session-arc
(Leonard demo failure, duplicate-wake reproductions, missed-wake
windows) hit the same diagnostic boundary: the daemon's log lines
existed momentarily then evaporated.

**The Fix:**
- New `writeLog(level, message)` helper writes to BOTH stdout (live
  terminal observability) AND `.neo-ai-data/wake-daemon/bridge.log`
  (persistent audit trail).
- Line format: `[ISO-timestamp] [PID:NNN] [LEVEL] message` —
  greppable, per-line correlation with daemon process + chronology.
- Daily rotation: previous-day file renamed to `bridge.log.YYYY-MM-DD`
  before appending today's first line. `rotateLogIfNewDay` runs on
  every `writeLog` call (cheap; just a stat + day-string compare).
- Startup pruning: archive files older than `LOG_RETENTION_DAYS` (30)
  are unlinked at daemon boot. Best-effort; per-file failures don't
  gate startup.
- All existing `console.log` / `console.error` / `console.warn` sites
  replaced with `writeLog('INFO'|'ERROR', ...)` calls, including the
  PID-lock singleton sites added in #10423 (PID transitions are now
  persisted alongside delivery events). Internal console-write inside
  `writeLog` itself is preserved (it IS the console-write path).

**Failure semantics:**
Log writes are best-effort. File-write failures are silently swallowed
— daemon liveness MUST NOT depend on log integrity. Console-write
preserves the original behavior so terminal observability is
unchanged even when file substrate fails.

**Test evidence:**
- Existing `bridge-daemon.spec.mjs` test (which captures stdout
  delivery output) continues to pass — `writeLog` preserves the
  original console-write path.
- New assertions in the same test verify the persistent log file
  exists, contains the expected delivery line, and conforms to the
  documented format (ISO timestamp + PID + INFO/ERROR level
  prefix). All assertions pass.

**Compounds with #10423** (PID-lock, merged): daemon-restart events
get logged with timestamp + old-PID + new-PID, making "wait, why did
the daemon restart?" investigations recoverable from disk audit alone.

**Out of scope:** SQLite-based logging (heavier substrate than
diagnostic use-case warrants); centralized agent-wide logger
(`logger.mjs` is for in-process Neo classes, not standalone scripts);
real-time alerting on log patterns (Phase-2 if substrate justifies).

Co-authored-by: tobiu <tobiasuhlig78@gmail.com>
Co-authored-by: Claude Opus 4.7 (1M context) <claude@anthropic.com>"
- 2026-05-04T20:48:53Z @neo-gpt cross-referenced by #10717
- 2026-05-04T21:10:15Z @neo-gpt cross-referenced by PR #10719
- 2026-05-11T05:07:13Z @neo-opus-ada cross-referenced by #11182
- 2026-06-04T14:20:30Z @neo-gpt cross-referenced by #12446

