---
number: 16133
title: 'fix(dashboard): reconcile re-entry drag ownership (#16122)'
author: neo-gpt-emmy
state: OPEN
createdAt: '2026-07-29T13:55:08Z'
updatedAt: '2026-07-29T15:07:37Z'
closedAt: null
mergedAt: null
head: codex/16122-reentry-proxy
base: dev
url: 'https://github.com/neomjs/neo/pull/16133'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #16122

Related: #16117

Re-entry now closes both owners of the same drag generation at the existing
`DockTabSortZone#endWindowDrag()` boundary. The worker restores the live DOM proxy and un-parks
base sorting, while the main addon leaves native-window movement before the next pointer frame can
route toward the retired popup. A headed physical-pointer regression now keeps one pointer gesture
down across popup birth, source-window re-entry, popup retirement, and two later proxy moves.

Evidence: L3 (exact-head headed physical-pointer journey with accelerated GL and pointer/proxy rect receipt) → L3 required for the frozen-proxy repair. Residual: AC1, AC5 exact-once retirement count, and AC6-AC8 remain pre-merge gates [#16122].

## Deltas from ticket

The product repair remains the narrow worker/main ownership reconciliation. The new regression
proves the previously missing temporal contract: after re-entry, a `(100, 60)` pointer delta produced
the same `(100, 60)` proxy delta, while both samples preserved the source grab offset at
`(35.9765625, 24.7)`.

The journey also pins zero dock-document mutation, same pane identity, continuing feed heartbeat,
and popup retirement during the uninterrupted gesture. It does not claim retained pixel frames,
re-exit generation behavior, or the full terminal/cancel matrix yet.

## Test Evidence

- Exact PR head `5ce257663f`: `npx playwright test test/playwright/e2e/workstation/WorkstationDragAffordancesNL.spec.mjs -c test/playwright/playwright.config.e2e.mjs --grep "same-gesture tear-out re-entry" --workers=1 --headed` — 2/2 passed, including accelerated Apple M5 Max GL; journey 5.1s.
- The ordinary E2E launch profile includes `--disable-frame-rate-limit`.
- Exact receipt: pointer `(518, 350.7) → (618, 410.7)`; proxy `(482.0234375, 326) → (582.0234375, 386)`; identical grab offsets before and after.
- Exact PR head `5ce257663f`: `npm run test-unit -- test/playwright/unit/dashboard/DockTabSortZone.spec.mjs` — 36/36 passed.
- Current-head CI: pending after the branch update.
- Retained post-conversion pixel frames: `None found`; AC8 remains merge-blocking.

## Post-Merge Validation

- [ ] Re-run the headed one-gesture journey against merged `dev` after AC6-AC8 are completed before merge.

## Commits

- `4e3c1ed758` — reconcile worker and main drag ownership on re-entry.
- `5ce257663f` — add the uninterrupted physical-pointer motion receipt.

## Evolution

The implementation lane was deliberately narrowed after the sibling popup-over-popup experiment
proved too broad for an honest single handoff. The first review candidate isolated the split-brain
repair; this update replaces its L1-only boundary with an exact-head L3 motion witness while keeping
the still-unproven pixel and terminal-matrix claims explicit.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session f0894e41-9fa7-4660-9c6e-d127e2d123f7.
