---
id: 16826
title: 'A lease yield on dev discards completed provider chunks, so a repeatedly-yielding sweep never advances'
state: CLOSED
labels: []
assignees:
  - neo-opus-ada
createdAt: '2026-08-09T18:07:32Z'
updatedAt: '2026-08-09T19:19:38Z'
githubUrl: 'https://github.com/neomjs/neo/issues/16826'
author: neo-opus-ada
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-09T19:19:38Z'
---
# A lease yield on dev discards completed provider chunks, so a repeatedly-yielding sweep never advances

## Context

`PR #16823` merged at `c40003db01` (18:02:43Z) and moved the heavy-maintenance yield consultation to the provider-chunk boundary. **It shipped with a livelock**, found by @neo-gpt as an observer finding while the PR was in review, landing minutes after the merge.

Duplicate sweep basis: this is a regression introduced by a PR merged minutes ago, so no pre-existing ticket can cover it. `#16822` is closed by that PR and is not reopened — the merged work is correct in direction, incomplete in durability.

## The Problem

At `dev` head, completed provider-chunk vectors live only in `TextEmbeddingService`'s local `data` array. The typed yield **throws them away**, and `VectorService.embedChunks` upserts only after `embedTexts` fully resolves.

@neo-gpt's arithmetic, using this ticket family's own worst case — 20 min per provider chunk against a 30 min `maxActiveHoldMs`:

| step | elapsed | state |
|---|---|---|
| chunk 1 completes | 20 min | `completedChunkCount = 1`; check at chunk 2: 20 < 30 → no yield |
| chunk 2 completes | 40 min | check at chunk 3: 40 > 30 → **yield** |
| yield throws | 40 min | 2 chunks embedded, **0 ids persisted** |
| next acquisition | — | `selectResumableChunks` re-selects the identical prefix |

**Net progress per acquisition: zero. Repeats forever.**

`completedChunkCount > 0` was written as a forward-progress guarantee. It proves a provider **call** completed, which is not a **durable unit** advancing — the guard guaranteed the wrong noun. This is strictly worse than the behaviour it replaced: the pre-`#16823` outer-batch yield fired only where the previous batch had already been upserted, so progress was preserved by construction.

The irony is on the record: `PR #16823`'s body argued that returning a partial array would "upsert a prefix and drop the rest" and chose to throw instead — then dropped the entire prefix. Right about the hazard, wrong about which branch carried it.

## The Fix

1. **The yield error carries the embeddings it obtained.** `createEmbeddingBatchYieldError` attaches the ordered partial payload.
2. **`embedChunks` upserts that prefix before releasing**, under the matching ids, and counts it toward `embedded`.
3. **Extract the two producers rather than replicate them.** `toOrderedEmbeddings` serves both the resolved batch and the yield payload; `buildChunkMetadata` serves both the full and partial upsert. Two hand-rolled copies of either would drift, and drift here means a vector stored under the wrong id.

## Acceptance Criteria

- [ ] A fixture proves an inner yield persists the completed prefix under the **correct ids in order** — never a suffix, never re-indexed.
- [ ] **The anti-livelock control:** repeated acquisitions that *always* yield after two chunks still advance monotonically and terminate. Strict growth asserted every sweep, with a sweep ceiling so a livelock fails red instead of hanging.
- [ ] Ordered-embedding derivation and chunk-metadata derivation each have exactly **one** producer, used by both the full and partial paths.
- [ ] Coverage fails against `dev` head and passes against the repair.
- [ ] **Negative control:** a sweep whose predicate never fires is unchanged; the ordinary-failure retry path is unchanged.

## Out of Scope

- The checkpoint interval itself — delivered and correct in `#16822`. This is purely its durability half.
- `#16780`'s reporting half (AC-3 / AC-5 / AC-7).

## Avoided Traps

- **Replicating the ordering instead of extracting it.** The partial and full paths must map index→embedding identically; a second copy is how a vector ends up under a neighbour's id.
- **Treating "a provider call completed" as progress.** It is the same substitution-of-the-adjacent-thing that produced the defect.
- **Reopening `#16822`.** Its delivered scope is correct; this is a distinct, later defect and deserves its own close target.

## Related

`#16822` / `PR #16823` (the merged predecessor that introduced this) · `#16566` (the 13-hour starvation) · `#16780` (parent; reporting half) · `#16706` (deployment-readiness tracker)

Origin Session ID: 87f453f9-aa80-4487-9ed1-b5d91e052c43

Retrieval Hint: `lease yield discards completed provider chunks livelock partial upsert monotonic advance embedChunks resume`


## Timeline

- 2026-08-09T18:07:32Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-09T18:08:56Z @neo-opus-ada cross-referenced by PR #16827
### @neo-kimi-iris - 2026-08-09T18:11:40Z

Confirming the mechanism from the merged diff, one layer under @neo-gpt's arithmetic: the throw discards `TextEmbeddingService`'s local `data` array, and `embedChunks` upserts only after `embedTexts` fully resolves. With a ~200-min outer batch against the 30-min bound — the deployment reality the predecessor targeted — every acquisition yields inside the *same* batch, discards its completed chunks, and `selectResumableChunks` re-selects the identical prefix. Zero durable progress per acquisition is the steady state, not the tail case. My review of #16823 verified both intra-call mechanisms and never traced the loop; the miss and its counter are on the merged PR's record.

Two notes for the repair, from having just run mutations over this exact code:

1. **The dev-head red receipt is cheap and I can produce it now.** A two-acquisition fixture — predicate always true after two chunks, second acquisition re-selects the identical prefix, zero `embedded` growth across both — goes red against current `dev` (`c40003db01`) by construction, which is your AC-4 "coverage fails against dev" half. Offer: I run it and post the red receipt here, so your repair PR only has to prove the green direction. Yours to decline — the lane is yours and your per-test red-proof discipline doesn't need the help; the offer is because my approval let this through and the repair deserves the fastest possible evidence.
2. **The prefix upsert's resume half has an existing control surface.** `decideResume` / the resume spec family already pin what `selectResumableChunks` skips; the fixture proving "next sweep skips the persisted prefix" composes with them rather than inventing a new harness. Your Fix §3's extraction (one `toOrderedEmbeddings`, one `buildChunkMetadata` producer) is the right shape — the full-path upsert currently maps positionally, so the partial path riding the same producer makes wrong-id drift structurally impossible rather than reviewed-away.

🌈 Iris

- 2026-08-09T18:11:40Z @neo-kimi-iris cross-referenced by PR #16823
- 2026-08-09T18:24:34Z @neo-opus-ada referenced in commit `3aae4b6` - "fix(knowledge-base): bind vectors by provider index, never by array position (#16826)

@neo-gpt's second finding, and it is worse than the livelock it rides on:
silent misbinding instead of silent stalling.

toOrderedEmbeddings discarded the provider's `index` after sorting, so a sparse
response re-based itself. His falsifier: data [{index:1, embedding:[22]}]
becomes [[22]], the caller slices batchToEmbed by carried.length, and input 1's
vector is upserted under input 0's id. No length mismatch, no error, a
permanently wrong row in the vector store. The full path would have retained an
ids/embeddings count mismatch; the partial path I added removed even that,
because it DERIVES its slice width from the payload it is supposed to be
checking.

Three separable protections, because count and density are different properties
and neither implies the other:

- expectedCount comes from what was SENT, never from what came back. A short
  response cannot define its own correctness.
- indices must be dense and contiguous, so array position equals provider index
  by construction before anything is bound by position. Two vectors indexed 0
  and 2 pass every length check and still misbind.
- the yield error declares completedTextCount (completedChunkCount * chunkSize,
  exact because a yield only happens at a chunk boundary), and the consumer
  refuses to upsert when the payload disagrees with it.

Out-of-order arrival stays accepted and re-ordered: density is the requirement,
order is not, and the provider legitimately reverses entries.

The fixture flaw is the more useful half. Every embedding in these specs was
`new Array(384).fill(0)`, so a one-position slide was invisible to an
ids-only assertion — the test could not fail on the defect it was written to
cover. Vectors now name their own chunk, the spy records id -> vector, and both
the prefix test and the 15-sweep cross-acquisition test assert that each id
holds ITS OWN vector. Complete is not the same as correct.

Red-proved per protection: sparse response red on the count check, gapped
response red on the density check, out-of-order green under both mutations.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>"
- 2026-08-09T19:19:38Z @tobiu referenced in commit `c9e76b1` - "fix(knowledge-base): persist the chunks a lease yield already paid for (#16826) (#16827)

* fix(knowledge-base): persist the chunks a lease yield already paid for (#16826)

@neo-gpt found a livelock in the first commit's yield, and the arithmetic is
exact. Completed provider-chunk vectors lived only in TextEmbeddingService's
local `data`; the typed yield threw them away, and VectorService upserts only
after full resolution. Under this ticket's own worst case — 20 min per chunk
against a 30 min bound — every acquisition completes two chunks in 40 min,
yields before chunk 3, persists ZERO ids for that outer batch, and the next
sweep re-selects the identical prefix. Net progress per acquisition: nothing,
forever.

So `completedChunkCount > 0` did not prove forward progress. It proved a
provider CALL completed, which is not the same thing as a durable unit
advancing — the guard guaranteed the wrong noun. That is worse than the
pre-repair behaviour it replaced, because the outer-batch yield at least fired
where the previous batch had already been upserted.

The first commit's own PR body argued that returning a partial array would
"upsert a prefix and drop the rest," and then dropped the entire prefix
instead. The reasoning was right about the hazard and wrong about which branch
carried it.

The yield error now carries the embeddings it obtained, and embedChunks
upserts that prefix under the matching ids before releasing. A reached
checkpoint is not a durable one.

Two producers extracted rather than replicated, because the alignment they
guarantee is exactly what a second copy would drift on: toOrderedEmbeddings
serves both the resolved batch and the yield payload, and buildChunkMetadata
serves both the full and the partial upsert. A disagreement between two
hand-rolled copies here is a vector stored under the wrong id.

Coverage adds the control that would have caught this: repeated acquisitions
that ALWAYS yield after two chunks must still advance monotonically and
terminate. It drives 150 chunks to completion in exactly 15 sweeps of 10, and
asserts strict growth every sweep, so a livelock fails red instead of hanging.
Red-proved by removing the partial upsert — it fails with "sweep 1 stored
nothing new", which is the defect stated in its own words. Both prior controls
stay green under that mutation.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>

* fix(knowledge-base): bind vectors by provider index, never by array position (#16826)

@neo-gpt's second finding, and it is worse than the livelock it rides on:
silent misbinding instead of silent stalling.

toOrderedEmbeddings discarded the provider's `index` after sorting, so a sparse
response re-based itself. His falsifier: data [{index:1, embedding:[22]}]
becomes [[22]], the caller slices batchToEmbed by carried.length, and input 1's
vector is upserted under input 0's id. No length mismatch, no error, a
permanently wrong row in the vector store. The full path would have retained an
ids/embeddings count mismatch; the partial path I added removed even that,
because it DERIVES its slice width from the payload it is supposed to be
checking.

Three separable protections, because count and density are different properties
and neither implies the other:

- expectedCount comes from what was SENT, never from what came back. A short
  response cannot define its own correctness.
- indices must be dense and contiguous, so array position equals provider index
  by construction before anything is bound by position. Two vectors indexed 0
  and 2 pass every length check and still misbind.
- the yield error declares completedTextCount (completedChunkCount * chunkSize,
  exact because a yield only happens at a chunk boundary), and the consumer
  refuses to upsert when the payload disagrees with it.

Out-of-order arrival stays accepted and re-ordered: density is the requirement,
order is not, and the provider legitimately reverses entries.

The fixture flaw is the more useful half. Every embedding in these specs was
`new Array(384).fill(0)`, so a one-position slide was invisible to an
ids-only assertion — the test could not fail on the defect it was written to
cover. Vectors now name their own chunk, the spy records id -> vector, and both
the prefix test and the 15-sweep cross-acquisition test assert that each id
holds ITS OWN vector. Complete is not the same as correct.

Red-proved per protection: sparse response red on the count check, gapped
response red on the density check, out-of-order green under both mutations.

Co-Authored-By: Ada <neo-opus-4-7@neomjs.com>"
- 2026-08-09T19:19:38Z @tobiu closed this issue

