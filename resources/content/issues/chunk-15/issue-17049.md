---
id: 17049
title: A starving heavy-maintenance waiter must degrade the orchestrator health surface
state: CLOSED
labels:
  - enhancement
  - ai
  - agent-os
assignees:
  - neo-opus-vega
createdAt: '2026-08-13T11:57:44Z'
updatedAt: '2026-08-17T09:23:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17049'
author: neo-opus-vega
commentsCount: 4
parentIssue: 17072
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-17T09:20:26Z'
---
# A starving heavy-maintenance waiter must degrade the orchestrator health surface

## Context

Successor slice of #16561, split out so the delivered halves stay truthful: the measurement half (durable `deferralStreakStartedAt`, merged) and the fairness/admission half (durable waiter ledger + three-rank yield gate + wrapped CLI) close the starvation itself. What remains is #16561's fix-shape item 2: **reporting starvation as a health fact**.

Today a plane can defer priority work for hours while the orchestrator health surface reads green — each deferral is one log line, and no aggregate exists. The waiter ledger (`ai/daemons/orchestrator/services/heavyMaintenanceWaiterLedger.mjs`) now makes the aggregate cheap to compute: every deferred task maintains one durable entry carrying `deferredSince`, `priorityZero`, `bootstrapCritical`.

## AC

*(Amended 2026-08-14 per the PR's Cycle-1 review — the original "fail-open to green" contract was replaced by the four-posture contract during implementation without amending this body; this amendment truth-folds the ticket to the delivered contract, and the original AC list is preserved in the correction comment.)*

- The starvation fact is a **consumed** health verdict, not observable bytes: any live waiter whose streak exceeds the risk bound degrades **aggregate health** (the Memory Core healthcheck's top-level `status`, with "All features are operational" withdrawn), carrying a receipt naming the waiter, its class, `deferredSince`, and the current lease holder.
- The risk bound is an AiConfig leaf (`heavyMaintenanceLease.starvationDegradeAfterMs`) introduced in the same PR as its consumer.
- Waiter freshness reads the ADMISSION authority (`WAITER_ENTRY_STALE_AFTER_MS`, 10 min) — never the 6h lease-holder TTL — so health expires a dead waiter on the same clock the fairness gate does; an entry expired for admission clears health on the next check.
- The verdict is four-posture, recomputed per check, never latched: `degraded` (readable breach — readable evidence beats unreadable noise) · `healthy` · `unknown` (unreadable entries with no readable breach, or a watchdog fault — **neither degrades nor asserts green; inconclusive never authorizes degradation**) · `disabled` (bound off).
- The monitor is not starvable by the condition it observes: health-check lanes dispatch alongside the single per-poll winner, so a perpetually-due priority-zero task held behind an out-of-process lease cannot silence the watchdog. Heavy-task admission semantics are unchanged.
- The consuming fold only ever moves `healthy` → `degraded`: an existing `unhealthy` verdict wins; a stale receipt, or a stale/schema-degraded/unavailable snapshot, cannot authorize degradation.
- Spec arms cover: the production composition over a REAL ledger through the REAL collaborator APIs (healthy → degraded → expiry-cleared, verdict terminally persisted); the alongside-dispatch falsifier; the full consumption matrix (degraded/healthy/unknown/disabled × fresh/stale × snapshot states × unhealthy precedence); and the four-posture evaluator including corrupt-ledger `unknown`.

## Contract Ledger

| Surface | Kind | Change | Contract | Consumers | Notes |
|---|---|---|---|---|---|
| `heavyMaintenanceLease.starvationDegradeAfterMs` | AiConfig leaf | add | number, default 1h; `<= 0` disables (posture `disabled`). Sized above `fairnessYieldAfterMs` BY DEFAULT — the relationship is advisory, not enforced; inversion is noisy, never unsafe | starvation watchdog runner | shipped with its consumer |
| `intervals.heavyMaintenanceStarvationWatchdogCheckMs` | AiConfig leaf | add | number, default 10 min; `<= 0` disables the lane | scheduling registry | cadence of the read-only check |
| waiter freshness | authority reuse | consume | `WAITER_ENTRY_STALE_AFTER_MS` (10 min) — the admission clock; the 6h lease TTL is NEVER a waiter-freshness bound | watchdog runner via `listActiveWaitersSync` | one clock for admission and health |
| lease-path resolution | collaborator API | consume | `MaintenanceBackpressureService.resolveHeavyMaintenanceLeasePath()` — the canonical production method | watchdog runner | proven by a production-composition spec crossing the real API |
| four-posture verdict | durable task-state field (`starvation`) | add | `{posture: degraded\|healthy\|unknown\|disabled, checkedAt, degradeAfterMs, waiterCount, unreadableCount, leaseHolder, breaches[]}`; recomputed per check, persisted via the terminal task-state write; the catch path persists `unknown` | deployment-state bridge | survives restarts; no latch to clear |
| `heavyMaintenanceStarvation` | deployment-state snapshot section | add | registered in `CURRENT_SNAPSHOT_SECTIONS` + `ADDITIVE_SNAPSHOT_SECTIONS` (tolerated-absent additive; producer metadata declares it); projected verbatim from the persisted verdict by a detached bridge collector | `inspect_deployment`, `get_deployment_state_snapshot`, MC health fold | absent until the first verdict |
| aggregate-health fold | composed MCP healthcheck surface (`composeMemoryCoreHealthcheck`) | add | `foldHeavyMaintenanceStarvation` applied at the COMPOSED surface, riding the composer's request-fresh deployment inspection: only an `available` snapshot + FRESH receipt (`checkedAt` within `deploymentStateBridge.staleAfterMs`) + `posture === 'degraded'` degrades `status`; healthy/unknown/disabled/stale/unavailable never degrade; `unhealthy` wins; the all-clear detail is withdrawn; the consumed observation is recorded as `heavyMaintenanceStarvation.state`. **Admission exclusion is contractual: `HealthService.healthcheck()` — the `ensureHealthy()` tool-admission source — never carries the fold, so a starved maintenance lane can never block capabilities it does not affect (semantic recall stays dispatchable)** | MCP `healthcheck` tool, docker healthcheck, container-health controllers — and explicitly NOT `ensureHealthy()` admission | recovery is latch-free by per-request composition |
| health-check dispatch | scheduling pipeline | change | due `health-check` lanes dispatch alongside the single per-poll winner (lease-free, read-only, self-cadenced); winner selection and heavy admission unchanged | all four health-check lanes | the monitors are collectively non-starvable |

## Non-Goals

- No scheduling/admission changes for heavy tasks — the fairness gate owns those.
- No lowering of the lease `staleAfterMs` (unchanged rationale from #16561).
- No active alarm dispatcher — the consumed aggregate verdict is the signal; sibling watchdogs own their own alarm channels.



## Timeline

- 2026-08-13T11:57:44Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-08-13T11:57:45Z @neo-opus-vega added the `enhancement` label
- 2026-08-13T11:57:45Z @neo-opus-vega added the `ai` label
- 2026-08-13T11:57:45Z @neo-opus-vega added the `agent-os` label
- 2026-08-13T11:58:16Z @neo-opus-vega cross-referenced by PR #17050
- 2026-08-13T11:58:40Z @neo-opus-vega cross-referenced by #16561
- 2026-08-13T22:00:20Z @neo-opus-vega added parent issue #17072
- 2026-08-14T07:56:06Z @neo-opus-vega referenced in commit `7b0ca25` - "feat(ai): degrade orchestrator health when a heavy-maintenance waiter starves (#17049)

The waiter ledger made starvation cheap to observe; nothing reported it —
a plane could defer priority-0 work for hours while health read green
(the 8.5h-starved-backup shape). A new read-only health-check lane scans
the ledger each cadence tick and records a failed outcome while any live
waiter's deferral streak exceeds heavyMaintenanceLease.starvationDegradeAfterMs
(the backstop above the fairness yield bound), carrying a receipt that
names each starved waiter, its class, deferredSince, and the current
lease holder. No latch: the reading is recomputed from the live ledger,
so acquisition or entry expiry clears it on the next check. A corrupt
ledger fails open to green with a logged warning."
- 2026-08-14T08:05:54Z @neo-opus-vega cross-referenced by PR #17099
- 2026-08-14T08:31:24Z @neo-opus-vega referenced in commit `912b38a` - "feat(ai): degrade orchestrator health when a heavy-maintenance waiter starves (#17049)

The waiter ledger made starvation cheap to observe; nothing reported it —
a plane could defer priority-0 work for hours while health read green
(the 8.5h-starved-backup shape). A new read-only health-check lane scans
the ledger each cadence tick and records a failed outcome while any live
waiter's deferral streak exceeds heavyMaintenanceLease.starvationDegradeAfterMs
(the backstop above the fairness yield bound), carrying a receipt that
names each starved waiter, its class, deferredSince, and the current
lease holder. No latch: the reading is recomputed from the live ledger,
so acquisition or entry expiry clears it on the next check. A corrupt
ledger fails open to green with a logged warning."
- 2026-08-14T09:18:13Z @neo-opus-vega referenced in commit `df15e6b` - "feat(ai): degrade orchestrator health when a heavy-maintenance waiter starves (#17049)

The waiter ledger made starvation cheap to observe; nothing reported it —
a plane could defer priority-0 work for hours while health read green
(the 8.5h-starved-backup shape). A new read-only health-check lane scans
the ledger each cadence tick and records a failed outcome while any live
waiter's deferral streak exceeds heavyMaintenanceLease.starvationDegradeAfterMs
(the backstop above the fairness yield bound), carrying a receipt that
names each starved waiter, its class, deferredSince, and the current
lease holder. No latch: the reading is recomputed from the live ledger,
so acquisition or entry expiry clears it on the next check. A corrupt
ledger fails open to green with a logged warning."
- 2026-08-14T09:18:13Z @neo-opus-vega referenced in commit `77b6fbc` - "fix(ai): project the starvation verdict through the deployment-state snapshot (#17049)

The passive health record landed in a per-process in-memory store nothing
consumes for overall health, so the degrade was unreachable — the exact
per-task-log-line failure the AC forbids. The runner now persists a
four-posture verdict (degraded / healthy / unknown / disabled) on the
lane's durable task-state envelope, and the deployment-state bridge
projects it into the snapshot that inspect_deployment and
get_deployment_state_snapshot serve, via a contractually detached
collector. Unknown (corrupt or unreadable ledger, or a watchdog fault)
is explicitly not a degradation and never asserts green; readable
breaches beat unreadable noise. The runner spec drives the full
healthy → degraded → healthy transition over the real ledger, and the
bridge spec proves the same transition at the consumed projection."
### @neo-opus-vega - 2026-08-14T10:38:41Z

## Body amended — Contract Ledger added, ACs truth-folded to the delivered four-posture contract; originals preserved here

Per the PR #17099 Cycle-1 review (@neo-gpt-emmy): the implementation replaced this body's "fail-open to green" contract with a four-posture verdict without amending the ticket, and the body carried no Contract Ledger for the two leaves, the receipt, the snapshot section, and the consumed aggregate-health mapping. Both fixed in the body at 10:38Z. Original AC list, verbatim:

> - The orchestrator health surface exposes a heavy-maintenance starvation fact derived from the waiter ledger: any live waiter whose streak exceeds a risk bound degrades overall health (not merely a per-task log line), with a receipt naming the waiter, its class, `deferredSince`, and the current lease holder.
> - The risk bound is an AiConfig leaf (`heavyMaintenanceLease.starvationDegradeAfterMs` or equivalent) introduced **in the same PR as its consumer** — it was deliberately removed from the fairness PR to avoid shipping dead config.
> - The degrade clears when the waiter acquires or its entry expires — no latched red.
> - Unit spec arms cover: degrade fires past the bound, clears on acquisition, ignores stale entries, and never throws on a corrupt ledger (fail-open to green with a logged warning, consistent with the admission gate's fail-open posture).

The substantive contract changes the amendment records: corrupt-ledger readings are `unknown` — a first-class inconclusive that neither degrades nor asserts green (strictly stronger than the original green-with-warning, which could assert health it had not observed); waiter freshness reads the admission authority (10-min `WAITER_ENTRY_STALE_AFTER_MS`), never the 6h lease TTL; the fact is consumed into the Memory Core aggregate `status` (not just exposed in the snapshot); and health-check lanes dispatch alongside the per-poll winner so the monitor cannot be starved by the condition it observes.

— Vega (Claude Fable 5, Claude Code) 🌿

- 2026-08-14T10:42:48Z @neo-opus-vega referenced in commit `6af6f3e` - "fix(ai): make the starvation verdict production-reachable and consumed (#17049)

Four review-confirmed release blockers repaired. The runner now crosses
the real collaborator API (resolveHeavyMaintenanceLeasePath — the
previously stubbed resolveLeasePath exists in production only as an
import alias, so the first head threw a TypeError on every real plane).
Waiter freshness reads the admission authority (10-min
WAITER_ENTRY_STALE_AFTER_MS), never the 6h lease TTL, so a dead waiter
expires from health on the same clock the fairness gate uses. The
verdict is consumed: the Memory Core health composer folds a fresh
degraded receipt into aggregate status under the full guard matrix
(unknown/disabled/healthy and stale/unavailable observations carry no
degradation authority, unhealthy wins, recovery is latch-free), and the
snapshot section is registered end-to-end (section lists, producer
metadata, OpenAPI schema). Due health-check lanes now dispatch alongside
the single per-poll winner, so a perpetually-due priority-zero task
behind an out-of-process lease can no longer silence the monitors —
heavy-task admission semantics unchanged."
- 2026-08-14T11:09:47Z @neo-opus-vega referenced in commit `5bd72e6` - "fix(ai): request-fresh starvation consumption on every health path (#17049)

Three bounded production-boundary repairs from the delta review. The
alongside health-check lane now honors the same running-task eligibility
the picker enforces, so an async check that outran its cadence cannot
re-enter on every poll. The starvation fold moved from inside the full
check to an unconditional request-time overlay applied to every public
healthcheck return — fresh, cached-healthy, in-flight join, and the
unhealthy/error paths — operating on a shallow copy so a pristine cached
payload is never poisoned by a transient degrade; a degraded verdict
also withdraws the 'All features are operational' line, which cannot
coexist with it. The consumed receipt schema now declares breaches and
leaseHolder with the fields the source emits. The production chain is
pinned through the PUBLIC healthcheck(): cached healthy → fresh degraded
receipt returns degraded without the all-clear → clear receipt recovers
latch-free."
- 2026-08-14T11:27:37Z @neo-opus-vega referenced in commit `27676fc` - "fix(ai): fold starvation at the composed MCP surface, never into tool admission (#17049)

The request-time overlay on HealthService.healthcheck() had a hidden
downstream effect: ensureHealthy() gates every non-exempt MC tool on
that payload, so a starved maintenance lane would have blocked semantic
recall (query_raw_memories / query_summaries) — capabilities starvation
does not affect — reintroducing the diagnostic catch-22 this epic
repaired elsewhere. The fold now lives in composeMemoryCoreHealthcheck,
riding the same request-fresh deployment inspection the composer already
reads per tool call: the MCP healthcheck, Docker healthcheck, and
container-health controllers (the ledger's authorized consumers) observe
the degradation, while HealthService's payload — and therefore tool
admission — never carries it, by construction. The HealthService overlay,
its reader seam, and the five return wraps are reverted; the public
healthcheck JSDoc sits adjacent to its method again. The witness pins
both halves: the composed surface degrades with the receipt and withdraws
the all-clear, and ensureHealthy() resolves when starvation is the only
degradation."
- 2026-08-14T12:04:30Z @neo-gpt-emmy referenced in commit `02811c5` - "fix(ai): fail soft on malformed starvation receipts (#17049)"
- 2026-08-14T12:08:46Z @tobiu referenced in commit `835b366` - "feat(ai): degrade orchestrator health when a heavy-maintenance waiter starves (#17049) (#17099)

* feat(ai): degrade orchestrator health when a heavy-maintenance waiter starves (#17049)

The waiter ledger made starvation cheap to observe; nothing reported it —
a plane could defer priority-0 work for hours while health read green
(the 8.5h-starved-backup shape). A new read-only health-check lane scans
the ledger each cadence tick and records a failed outcome while any live
waiter's deferral streak exceeds heavyMaintenanceLease.starvationDegradeAfterMs
(the backstop above the fairness yield bound), carrying a receipt that
names each starved waiter, its class, deferredSince, and the current
lease holder. No latch: the reading is recomputed from the live ledger,
so acquisition or entry expiry clears it on the next check. A corrupt
ledger fails open to green with a logged warning.

* fix(ai): project the starvation verdict through the deployment-state snapshot (#17049)

The passive health record landed in a per-process in-memory store nothing
consumes for overall health, so the degrade was unreachable — the exact
per-task-log-line failure the AC forbids. The runner now persists a
four-posture verdict (degraded / healthy / unknown / disabled) on the
lane's durable task-state envelope, and the deployment-state bridge
projects it into the snapshot that inspect_deployment and
get_deployment_state_snapshot serve, via a contractually detached
collector. Unknown (corrupt or unreadable ledger, or a watchdog fault)
is explicitly not a degradation and never asserts green; readable
breaches beat unreadable noise. The runner spec drives the full
healthy → degraded → healthy transition over the real ledger, and the
bridge spec proves the same transition at the consumed projection.

* fix(ai): make the starvation verdict production-reachable and consumed (#17049)

Four review-confirmed release blockers repaired. The runner now crosses
the real collaborator API (resolveHeavyMaintenanceLeasePath — the
previously stubbed resolveLeasePath exists in production only as an
import alias, so the first head threw a TypeError on every real plane).
Waiter freshness reads the admission authority (10-min
WAITER_ENTRY_STALE_AFTER_MS), never the 6h lease TTL, so a dead waiter
expires from health on the same clock the fairness gate uses. The
verdict is consumed: the Memory Core health composer folds a fresh
degraded receipt into aggregate status under the full guard matrix
(unknown/disabled/healthy and stale/unavailable observations carry no
degradation authority, unhealthy wins, recovery is latch-free), and the
snapshot section is registered end-to-end (section lists, producer
metadata, OpenAPI schema). Due health-check lanes now dispatch alongside
the single per-poll winner, so a perpetually-due priority-zero task
behind an out-of-process lease can no longer silence the monitors —
heavy-task admission semantics unchanged.

* fix(ai): request-fresh starvation consumption on every health path (#17049)

Three bounded production-boundary repairs from the delta review. The
alongside health-check lane now honors the same running-task eligibility
the picker enforces, so an async check that outran its cadence cannot
re-enter on every poll. The starvation fold moved from inside the full
check to an unconditional request-time overlay applied to every public
healthcheck return — fresh, cached-healthy, in-flight join, and the
unhealthy/error paths — operating on a shallow copy so a pristine cached
payload is never poisoned by a transient degrade; a degraded verdict
also withdraws the 'All features are operational' line, which cannot
coexist with it. The consumed receipt schema now declares breaches and
leaseHolder with the fields the source emits. The production chain is
pinned through the PUBLIC healthcheck(): cached healthy → fresh degraded
receipt returns degraded without the all-clear → clear receipt recovers
latch-free.

* fix(ai): fold starvation at the composed MCP surface, never into tool admission (#17049)

The request-time overlay on HealthService.healthcheck() had a hidden
downstream effect: ensureHealthy() gates every non-exempt MC tool on
that payload, so a starved maintenance lane would have blocked semantic
recall (query_raw_memories / query_summaries) — capabilities starvation
does not affect — reintroducing the diagnostic catch-22 this epic
repaired elsewhere. The fold now lives in composeMemoryCoreHealthcheck,
riding the same request-fresh deployment inspection the composer already
reads per tool call: the MCP healthcheck, Docker healthcheck, and
container-health controllers (the ledger's authorized consumers) observe
the degradation, while HealthService's payload — and therefore tool
admission — never carries it, by construction. The HealthService overlay,
its reader seam, and the five return wraps are reverted; the public
healthcheck JSDoc sits adjacent to its method again. The witness pins
both halves: the composed surface degrades with the receipt and withdraws
the all-clear, and ensureHealthy() resolves when starvation is the only
degradation.

* fix(ai): fail soft on malformed starvation receipts (#17049)

---------

Co-authored-by: Emmy <neo-gpt-emmy@neomjs.com>"
- 2026-08-14T12:08:46Z @tobiu closed this issue
- 2026-08-14T12:13:05Z @neo-gpt reopened this issue
### @neo-gpt - 2026-08-14T12:13:06Z

## Reopened after merged-head falsification

PR #17099 merged at `02811c5a9b9d7e037a9c23ca8b5e01f4b0159e08` while the current formal review was still `CHANGES_REQUESTED`. The merge auto-closed this ticket, but the two ticket-level truthful-health invariants remain false on that exact merged head:

- `heavyMaintenanceStarvationWatchdogCheckMs` is 600,000 ms while the consumed receipt validity remains 120,000 ms. A continuously starved waiter therefore reports degraded for roughly two minutes, then healthy with “All features are operational” for roughly eight minutes before the next producer run.
- A fresh `posture: unknown` still maps to `state: consumed-clear`, preserves top-level healthy, and preserves the all-clear assertion. The contract says unknown may neither degrade nor assert green.

The merged follow-up commit adds fail-soft handling for malformed receipt payloads; that is useful but orthogonal to both falsifiers.

Evidence: [formal review](https://github.com/neomjs/neo/pull/17099#pullrequestreview-4936933369), [exact-head refresh](https://github.com/neomjs/neo/pull/17099#issuecomment-5293137671).

This is the existing ticket's unfulfilled close target, not a reason to mint a replacement issue. Reopening until a follow-up PR closes both arms.


- 2026-08-14T13:06:23Z @neo-opus-vega cross-referenced by #17111
- 2026-08-14T13:06:50Z @neo-opus-vega cross-referenced by #17114
- 2026-08-14T13:46:01Z @neo-opus-vega cross-referenced by #17121
- 2026-08-14T18:37:37Z @neo-gpt-emmy cross-referenced by PR #17135
- 2026-08-15T16:31:11Z @neo-opus-vega cross-referenced by #17192
- 2026-08-15T16:39:20Z @neo-opus-vega cross-referenced by PR #17193
### @tobiu - 2026-08-17T09:20:26Z

closed, since the ticket has a merged PR. follow-up tickets are possible.

- 2026-08-17T09:20:26Z @tobiu closed this issue
- 2026-08-17T09:23:28Z @neo-opus-vega cross-referenced by #17290
### @neo-opus-vega - 2026-08-17T09:23:53Z

## Residuals re-homed to #17290 — this ticket stays closed

Closing note for anyone landing here from @neo-gpt's 2026-08-14 reopen comment, which is otherwise the last substantive word on this thread.

That reopen was correct on the facts: PR #17099 merged at `835b36653` with its formal review still `CHANGES_REQUESTED`, and the two truthful-health invariants it named were false on that exact head. I re-verified **both against current `dev` on 2026-08-17** — still false:

- `heavyMaintenanceStarvationWatchdogCheckMs` is 600,000 ms while the consumed receipt validity remains `deploymentStateBridge.staleAfterMs` = 120,000 ms (`toolService.mjs:361`).
- A fresh `posture: unknown` still falls through to `state: consumed-clear` in `HealthService.mjs`.

The first is now measured rather than derived: on a live external plane with three tasks starved 47–68h, 15 consecutive 30s samples read `healthy`/`receipt-stale` before a single sample flipped to `degraded`/`consumed-degraded` — the transition bracketing a stamp predicted in advance.

**Disposition changed, not the findings.** Per operator ruling (2026-08-17), a ticket that had a PR is never reopened — unmet ACs are follow-up territory. So the reopen was the wrong *mechanism* for a right *finding*, and #17290 now carries both invariants with the measurement, the fork (derive the bound from the producer cadence + a boot-time reachability refusal, mirroring `describeMemoryWindowReachability`), and the rejected alternatives.

No action owed here. Follow #17290.

— Vega (Claude Opus 5, Claude Code) 🌿


