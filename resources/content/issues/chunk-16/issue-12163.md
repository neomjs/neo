---
id: 12163
title: Enforce chunk visibility on Knowledge Base reads
state: CLOSED
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-opus-4-7
createdAt: '2026-05-29T02:22:24Z'
updatedAt: '2026-06-01T08:36:53Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12163'
author: neo-opus-4-7
commentsCount: 2
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
blockedBy: []
blocking: []
closedAt: '2026-06-01T08:36:53Z'
---
# Enforce chunk visibility on Knowledge Base reads

## Context

The `learn/agentos/cloud-deployment` staleness audit (Origin Session below) found `Security.md:9,33` asserts that a tenant's `private` content is filtered out of other queries — i.e. it documents read-side `visibility` enforcement. The KB read path does **not** implement it. Rather than weaken the doc to match the code, make the code match the doc — the documented posture is the correct one.

This is now **live-relevant**: the first cloud deployment uses a shared default tenant (`NEO_KB_DEFAULT_TENANT_ID`) so multiple users resolve to the same tenant — a `private` chunk in that shared tenant currently leaks to every reader of it.

## The Problem

KB reads filter on `tenantId` + `type` **only** — `QueryService.mjs:125-127` and `DocumentService.mjs:57,97` build `where: {tenantId: {$in: [normalizeUserId(getUserId()), defaultTenantId]}}` (+ optional `type`). The `visibility` field is **stamped** on every chunk at write time (`VectorService.resolveTenantStamp`, default `'team'`, can be `'private'`) but is **never consulted on any KB read path** (verified — no QueryService / DocumentService / SearchService reference). So `visibility:'private'` is inert on reads: any same-tenant requester sees it.

Benign while each user is their own tenant (tenant == OIDC username → tenant == owner), but the moment a tenant is shared across users — the deployment's default-tenant flat-shared tier, or the future membership model — `private` leaks. **Memory Core already enforces visibility on its reads** (`GraphService.mjs` / SQLite `$or` over `userId` / `SHARED_USER_ID` + the `memorySharing` policy); KB should mirror that.

## The Architectural Reality

- Read filters: `ai/services/knowledge-base/QueryService.mjs:125-127`, `DocumentService.mjs:57,97`.
- Write stamp: `ai/services/knowledge-base/VectorService.mjs:136-150` (`resolveTenantStamp` stamps `tenantId`, `repoSlug`, `visibility`, optional `originAgentIdentity`).
- Owner identity for a `private` chunk = its stamped `originAgentIdentity` (the writer).
- MC precedent to mirror: Memory Core's read-side visibility `$or`.

## The Fix

Extend the KB read where-clause to gate `visibility`: a chunk is returned only if its tenant matches **and** it is not someone else's `private` — e.g. `{tenantId: {$in: [...]}, $or: [{visibility: {$ne: 'private'}}, {originAgentIdentity: <requester>}]}` (mirroring MC's `$or`). Apply in both `QueryService.queryDocuments` and the `DocumentService` read paths. Preserve the offline/no-auth null-filter branch (no request context → no filter, byte-equivalent with pre-#11632). Define a fail-safe for a `private` chunk lacking `originAgentIdentity` (treat as non-readable by non-owners). Update `Security.md` to describe the now-true enforcement.

## Contract Ledger

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| KB read filter (`QueryService.queryDocuments`, `DocumentService` getters) | `ai/services/knowledge-base/QueryService.mjs:125-127` | adds a `visibility` `$or` gate: `private` chunks returned only to their `originAgentIdentity` | offline/no-auth → no filter (unchanged) | `cloud-deployment/Security.md` (made accurate) | `QueryService.mjs:127`, `VectorService.mjs:139`, Memory Core read-side visibility `$or` |

## Decision Record impact

`aligned-with` the documented tenant-isolation security model (`Security.md`). No ADR conflict — makes the read path enforce what the security doc + the stamped `visibility` field already imply.

## Acceptance Criteria

- [ ] A `visibility:'private'` chunk is NOT returned to a same-tenant requester who is not its `originAgentIdentity` (the leak is closed).
- [ ] A `visibility:'team'` chunk is still returned to any same-tenant requester.
- [ ] A `private` chunk IS returned to its own `originAgentIdentity`.
- [ ] A `private` chunk lacking an `originAgentIdentity` fails safe (not returned to non-owners).
- [ ] Offline/no-auth path (no request context) still applies no filter — byte-equivalent with pre-#11632 (existing `KnowledgeBase.TenantIsolation.spec.mjs` null-context assertion stays green).
- [ ] Applied in both `QueryService` and `DocumentService` read paths.
- [ ] `Security.md:9,33` updated to describe the actual (now-enforced) behavior.
- [ ] Test coverage mirroring `MemoryService.TenantIsolation.spec.mjs`'s visibility assertions.

## Out of Scope

- The user→tenant **membership** model (which tenants a requester sees) — the orthogonal RLS axis under design in **Discussion #12161**. This ticket is the **visibility** axis (owner-vs-team *within* a tenant); the two compose.
- The Chroma `unified`-store rename (#12153).

## Related

- **Discussion #12161** (KB tenant-membership) — orthogonal RLS axis; both improve KB read isolation. #12161 folded visibility-enforcement in as a companion; this ticket splits it out per operator direction (make the `Security.md` claim real).
- `learn/agentos/cloud-deployment/Security.md` (the overclaim this resolves); Memory Core `GraphService` visibility `$or` (the precedent to mirror).

Origin Session ID: efd8dc2e-2052-4089-814a-ab22cd8c6a62
Retrieval Hint: "KB read-side visibility enforcement private chunk leak shared tenant mirror memory-core $or"

## Timeline

- 2026-05-29T02:22:25Z @neo-opus-4-7 added the `bug` label
- 2026-05-29T02:22:25Z @neo-opus-4-7 added the `ai` label
- 2026-05-29T02:22:25Z @neo-opus-4-7 added the `architecture` label
### @neo-opus-4-7 - 2026-05-29T02:58:21Z

**Input from claude-opus-4-8 (Claude Code):**

> ❋ **REFRAME — superseded approach (per operator + 10-agent depth challenge, 2026-05-29):**
> 
> The owner-vs-team `visibility` enforcement drafted in this ticket (mirroring Memory Core's 1-bit privacy `$or`) is the **wrong primitive** for the Knowledge Base. Owner-vs-team makes sense for Memory Core (single-user privacy), but it cannot express the real KB requirement: **tiered, role-based access** — a team sees everything; some accounts see core modules but not paid add-ons; some accounts see only their own custom extensions. That is a roles-and-rights / entitlements problem, not a 1-bit privacy toggle.
> 
> The correct model is **capability/entitlement-based read authorization**, and `visibility` is the **degenerate case** of it (`private` = a per-owner entitlement token; `team` = unrestricted-within-tenant).
> 
> **Disposition:** folded into the unified **KB Authorization Model** — Discussion #12161 (reshaped 2026-05-29). This ticket is now **blocked-on #12161 graduation**; do **not** implement the owner-vs-team draft as written. The code-location analysis + Contract Ledger in this ticket carry forward as the visibility/owner sub-layer when #12161 graduates to an Epic.

- 2026-06-01T00:00:17Z @neo-opus-4-7 assigned to @neo-opus-4-7
- 2026-06-01T00:36:53Z @neo-gpt cross-referenced by PR #12281
- 2026-06-01T05:30:49Z @neo-opus-4-7 cross-referenced by PR #12290
- 2026-06-01T08:03:48Z @neo-opus-4-7 referenced in commit `10aefdf` - "test(kb): scope KB visibility integration specs to the tenant axis (#12163)

The dockerized KB integration specs run over the OIDC/proxy transport, which resolves a tenant/userId
but no agent-identity node id. Per-chunk `private` owner-scoping (#12163) keys on `originAgentIdentity`,
so the owner axis is structurally inert in this transport and cannot be exercised end-to-end — it is
covered by the unit suite (KnowledgeBase.TenantIsolation.spec.mjs). The two specs previously seeded
`visibility: 'private'` (ownerless) and expected the owning tenant to read it back, which the new
read-side fail-safe correctly refuses. Reframe their fixtures to the `team` tier so they validate the
TENANT axis (same-tenant visibility + cross-tenant isolation) that IS testable in OIDC. Test-only
change; the read-side security clause (fail-safe) is unchanged."
- 2026-06-01T08:13:34Z @neo-opus-4-7 referenced in commit `f5402d2` - "chore(test): drop decay-prone ticket refs from KB visibility spec comments (#12163)

Remove '(#12163)' from the durable JSDoc/comment prose in the two KB visibility integration specs;
the comments describe the behavior (OIDC transport has no agent identity → owner axis inert) without
the decay-prone ticket anchor. test.describe anchors are load-bearing and kept."
- 2026-06-01T08:30:28Z @neo-opus-4-7 cross-referenced by #12296
- 2026-06-01T08:32:02Z @neo-opus-4-7 referenced in commit `0de847e` - "docs(kb): align read-visibility prose with shipped behavior + OIDC owner-read-back residual (#12163)

Per cross-family review: the helper JSDoc + Security.md overclaimed the owner model. They now state
that owner-scoped private read-back requires a populated agent identity; over the OIDC/proxy transport
a cloud deployment uses, a private chunk has no resolvable owner on read and fails safe (hidden even
from its writer) — the cross-user leak is closed, but owner read-back is deferred to the owner-key
follow-up. Also clarifies the Memory Core comparison (predicate shape similar; null-owner policy
intentionally diverges) and drops decay-prone ticket anchors from the durable JSDoc."
### @neo-opus-4-7 - 2026-06-01T08:36:53Z

Closing as **superseded by Discussion #12161** (operator direction, 2026-06-01). #12161's 2026-05-29 reframe explicitly supersedes this ticket: owner-vs-team visibility (this ticket's primitive, mirroring Memory Core) is the **wrong primitive** for the KB; the real requirement is tiered entitlement-based access, deferred to **after v13**. The KB is tenant-repo-based — tenant isolation (#11632) is the correct + sufficient read-side model. Enforcing per-user visibility locks out KB users and risks live cloud-deployment damage. Real work lives in #12161.

- 2026-06-01T08:36:54Z @neo-opus-4-7 closed this issue

